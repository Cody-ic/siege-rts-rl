# `rts_core` 交接说明

> **交接人** zhxxx233 → **接手人** ArLiangz　　**日期** 2026-08-28（第 2 次上机）
>
> **本文只放"别处没写、或写在会被淹没的地方"的东西。**
> 设计契约在 `CLAUDE.md`，导航在 `README.md`，两者都不在这里重复。
> 本文最核心的一节是第 4 节——那十三条目前**只活在 PR #23 的评论串里**，
> 而评论串会被后续讨论盖掉。若本文与 `CLAUDE.md` 冲突，以 `CLAUDE.md` 为准。

---

## 1. 你接的是什么，不是什么

**是**：`rts_core` 的**公开接口契约**——`step()` 的形状、观测通道注册表、动作与命令枚举、
实体数据布局、回放格式。这是 `README.md` 未认领工作表第 1 项，也是全项目最高优先级：
`render/`、`bindings/`、`train/`、`tests/` 四个模块都在等它的形状。

**不是**：战斗解算、寻路、视野、经济、波次编排。这四块**故意**只留声明——
它们是可认领的并行任务，不该由定契约的人一并写掉（另有一名成员 omg321-975 至今零提交，
契约定了他才有活可领）。

**已经在库里、你不用重做**的是「确定性地基」那一层：tick、实体句柄、PRNG、状态哈希、
双工具链 CMake、确定性守卫。它被**刻意切成对花名册与地形枚举零依赖**的一层，
所以它不构成对接口的预先承诺——你可以在它之上自由设计，不必迁就它。

---

## 2. 先读什么（不要按 `README.md` 那个顺序，那是给完全新人的）

你要写接口，所以路径不一样：

| # | 读什么 | 为什么 | 花多久 |
|:-:|---|---|---|
| 1 | **本文第 4、6 节** | 已定死的设计点 + 红线。跳过这两节写代码，大概率要重写 | 15 min |
| 2 | `rts_core/include/rts/types.hpp` | 156 行里 56 行是注释，且全在解释"为什么"，是地基的设计说明书 | 10 min |
| 3 | `CLAUDE.md`「rts_core 实现约定」+「确定性要求」+「RL 设计决策」 | 你要写的东西的硬约束都在这三节 | 20 min |
| 4 | `CLAUDE.md`「关于数值」 | **组内明文规则**，违反过一次、专门开 PR 订正过。见第 6 节红线四 | 5 min |
| 5 | `地图与场景设计.md` 4.1 / 4.2 / 4.2.1 | 地形枚举与「一格画两张图」，`terrain.hpp` 直接照它写 | 15 min |
| 6 | PR #23 全部评论串 | 第 4 节是它的摘要，但**原文有推导过程**，你若想推翻某条得先读它 | 40 min |

**暂时别读**：`守方AI与协同演化.md`（#15）、`演示与可视化清单.md`（#16）、
`无尽模式与地形分层.md`（#23）。三份都是**未合并的提案**，待拍板事项已合并到 **issue #26**
（唯一的表决场所，逐条给了定性与依赖排序）。它们对接口的影响已经被我提炼进第 4、5 节了；
读原文只会让你以为有一堆事在阻塞你——**没有**，见第 9 节。

**注意 #23 已被重写过一轮**（原 §2 作废、原 §7/§8 合并成「堡垒等级」一条、新增 §1.4）。
若你读到「守方兵种等级轴 + 维持费」那套，那是旧版。对你有影响的只有一条，见第 5 节末尾。

---

## 3. 已入库的地基：每个文件为什么这样

10 个文件、654 行 C++、约 330 行 CMake，ctest 全绿。
**四种组合都验过**：Windows/MSVC 与 Linux/GCC × Release 与 Debug
（Debug 是 `assert` 唯一会被编译的配置，见第 7 节第 8 条；Linux 侧由 #39 补上）。

ctest 条数这里刻意不写 —— 它今天从 7 变成 8（#31 加了 `map_gen_self_test`），
而 #38 正是因为另一处写死的条数过期才开的。**要数就 `ctest --test-dir build -C Release -N`。**

| 文件 | 关键决定与理由 |
|---|---|
| `include/rts/types.hpp` | `Tick` = `int32`（差值为负是合法中间结果）；`Side` 只有两个成员且 `kSideCount == 2`（**这条会咬你，见第 4 节第 5 条**）；`Vec2`/`GridPos` 用 float/int16；`Handle<Tag>` = 索引 + 代数打包进 `uint32`，**索引放高位**使句柄序 = 下标序 |
| | 底部一组 `static_assert` 把基元类型大小钉死。**它们是回放格式承诺**——改一个，所有已录回放静默失效 |
| `include/rts/rng.hpp` `src/rng.cpp` | 手写 xoshiro128\*\*，不用 `<random>`：**分布适配器的实现是各标准库自定的**，同引擎同种子在 MSVC 与 libstdc++ 下数列不同。`state()`/`set_state()` 是接口的一部分（回放要存恢复），不是调试便利。黄金向量由独立 Python 实现另算一遍，**不是从 C++ 输出反抄的** |
| `include/rts/hash.hpp` | FNV-1a 64。选它**只因为它顺序敏感**——顺序不敏感的哈希会对「内容相同、遍历顺序不同」给出假绿灯，而那正是本项目最要防的一类 bug，回放测试就白做了。`feed_pod` 的 `static_assert(has_unique_object_representations_v)` 拦的是「含填充字节的结构体按字节哈希 → 回放偶发失败」 |
| `CMakeLists.txt` | 构建类型守卫**跨 `project()` 分两半**。**改它之前必须先读里面那段注释**，它曾经写错过（详见第 7 节第 1 条） |
| `cmake/CompilerWarnings.cmake` | MSVC `/permissive-` `/Zc:__cplusplus` `/W4 /WX` + 四条显式抬起的转换类警告；GCC 侧 `-Wconversion -Wsign-conversion`。两侧刻意保持**严格度对称**。显式不开 `-ffast-math` / `/fp:fast` |
| `rts_core/CMakeLists.txt` | 「头文件自足性守卫」：给 `include/rts/` 下每个头各生成一个只含它自己的 TU 编进库，用 **GLOB 而非手写名单**（名单会被忘掉，而"忘掉"正是要防的）。**你新加的每个头都自动纳入** |
| `tests/CMakeLists.txt` | ctest 按 **ASCII 标签**注册（用例名是中文，当命令行过滤器会在 cp936 下被弄乱，结果是 ctest 报「No tests ran」——**测试被静默跳过比测试失败危险**）。另有一条不带过滤器的 `rts_tests_all`，因为标签清单是白名单 |
| `tools/check_determinism_bans.py` | 把 `CLAUDE.md` 的确定性禁令变成会红的 ctest。自带 `--self-test`（13 条应报 + 12 条不应报）防它自己退化成永远绿；扫到 0 个文件或路径不存在**一律判失败** |

---

## 4. 已经在 PR #23 讨论里定死的十三条（**最重要的一节**）

这些是我与 Cody-ic 在 PR #23 评论串里逐条辩过、双方明确同意的。**它们尚未写进任何
`.md` 文件，只存在于评论串里。** 你接手后：照做不需要再问；想推翻**必须在 PR 里说明**，
不要静默改——对面记得每一条。

### 4.1 接口形状（4 条）

**① `step()` 对「哪一侧由谁控制」保持中立。** 取两段式 `submit(Side, …)` 入队 +
`advance(ticks)` 推进，**不要** `step(defender_cmds, attacker_actions)`。
理由：人类输入异步到达，RL 一次提交一批；两段式同时容纳两者。
**这条同时让 #15（训练时守方也上 RL）退化为训练回路的改动，而不是接口重写**——
所以 #15 不阻塞你，反过来它依赖你。

**② 输入分两级，都离散、都按侧参数化。** `UnitAction`（战术，每 4–8 tick 决策一次）
与 `Command`（宏观 / 玩家级）。**「驻守墙段槽位」归 `Command`，不进 `UnitAction`**——
这样战术动作枚举一个字不动，`CLAUDE.md`「没有兵种专属动作」得以保住。

**③ 观测通道做成带版本号的注册表。** Python 侧从模块读 `kVersion` / `Count` / `kK`，
**不硬编码**。地形枚举本周刚从 3 变 5、还可能再动，硬编码通道数迟早**静默错位**。

**④ `WorldView` 把「渲染层只读」变成编译期保证**，不靠约定。不变量 2 因此写不坏。

### 4.2 数据布局（3 条）

**⑤ 中立可破坏障碍另开一组句柄 `ObstacleId` + 自己的扁平数组，不带 `Side` 字段。**
**不要加 `Side::Neutral`。** 理由不只是"多一维永远为空的槽位"：
**观测通道是按侧成对定义的，加进去会让通道数变成奇数并波及 Python 侧的解包**——
那是会静默错位的破坏形式。这与 `UnitId`/`BldId` 已是两组不可互换句柄同源。

**必须在契约那一版就留出位置**，因为回放字节布局与 `state_hash` 的喂入顺序都要固定；
事后加一组实体会让所有已录回放失效。（依据是 #23 的 §6「可破坏障碍」，
Cody-ic 自己判断它「最不可能被否掉」；即使被否，留一组空数组的代价是一个 tag 类型。）

**⑥ 每侧的迷雾 / 侦查记忆图是一等公民**（`FogLayer` per `Side`）。
`CLAUDE.md` 的 RL 硬要求第 1 条是「AI 的观测必须是它自己的迷雾状态，不得是 ground truth」；
#15 第 4 节与 #16 头号演示点（双视图）都要求它存在。

**⑦ 回放 = 种子 + 指令流 + 一份跨波状态快照。** 光有前两者**不足以从中途重放**——
攻方的等级分配与空军跨波留存**已经是**跨波永久状态（不是 #23 §7 引入的，是它让这件事不能再拖）。

### 4.3 观测语义（3 条）

**⑧「敌方单位等级」通道存**和**，不存平均。** 三条理由：等级从 1 起 ⇒「和 = 0」⟺「该格无敌人」，
不需要额外 mask 通道；和是线性的，与卷积、池化、下采样都可交换，平均不是；
网络能自己从和 ÷ 密度还原平均，反过来不行。

**⑨ 归一化的分母用「本波名义等级 `L(w)`」（= 兵力预算 ÷ 编成位），两侧共用同一个分母。**
量纲上：等级和 ÷ `L(w)` = 「这格有多少个本波标准兵当量」，与密度通道同量纲。
**任何固定的归一化常数都会破坏「波数本身即构成课程」**——第 10 波与第 100 波之间输入尺度漂移 100 倍，
而"低波次学到的表示在高波次仍可用"是我们省掉手工 curriculum 的**唯一依据**。

**⑩ 血量进观测存比例 `hp / max_hp`，不存绝对值。** 与第 ⑨ 条同病同治：
绝对血量随等级无界增长，同样会让课程迁移失效。比例天然落在 `[0,1]`。
**附带后果：血量字段宽度根本不传到 Python 侧**（这条对第 5 节那个未决问题很关键）。

**⑪ 加一条 `static_assert(kMinUnitLevel == 1)`，连注释一起。**
哪天有人把等级改成 0 基（数组下标就是 0 基，很自然），第 ⑧ 条那个通道会**静默失效**：
不报错、不崩，AI 只是再也分不出空地和一个 1 级兵。

### 4.4 战斗解算（1 条，含四个子条）

**⑫ 战斗解算全部走整数，克制倍率用千分比（1000 = 1.0×）。** 主理由**不是**跨平台位级一致，
而是：**成本产出矩阵是跑仿真算出来的，它既是调数值的依据、也是答辩材料。
若战斗含浮点，服务器上标定出的矩阵与 Windows 上演示时的实际战果可以对不上号，而两边都"没有 bug"。**
一个在演示机上无法复现的交付物不是精度问题，是坏的交付物。

范围要**诚实**：它只让**战斗解算**脱离平台差异，**不让整个回放跨平台可复现**（移动与寻路仍是浮点）。
别让后来者读成后者。

四个子条：

- **结果必须为正**（写成结构约束，不写成「钳到 1」这个数值）。0 伤害凭空造出一种免疫，
  违反「克制的本质是资源效率，不是二元胜负」。而它不是边角：高度惩罚与克制倍率会串起来，
  `3 × 0.4 × 0.5 = 0.6 → 0`，而低等级 = 第 1 波 = **演示开局**
- **截断只能发生一次，且由一个 helper 强制**。倍率作为**列表**传进去、由它做唯一那次除法，
  倍率个数增加时调用方不改任何东西。链式调用会静默少算：
  `5×1500×1500/1e6 = 11` vs `(5×1500/1000=7)×1500/1000 = 10`
- **四舍五入，不向下截断**（`(积 + 500'000) / 1'000'000`）。理由是偏置：
  向下截断每次少算至多 1 点，在低伤害区是 20–30% 的相对偏差，**全局稳定偏向防守方**
- 伤害解算的**中间量**必须比存储宽：克制是乘法的，`伤害 × 倍率 × 高度惩罚` 两个 10⁶ 相乘就是 10¹²

### 4.5 批处理（1 条）

**⑬ `BatchedEnv` 的线程池大小必须是构造参数，不能写死。** 服务器是 96 线程的共享账号，
**四个人各起 96 个 worker 是默认结果而不是失误**。tmux 命名解决「谁在跑」，解决不了「各占多少核」。
（实测背景见 #22：网络开销可忽略、A800 只用了 14 MiB，所以多组实验共享一张卡毫无压力，
被串行化的是 CPU 而不是 GPU。）

---

## 5. 一个必须你来答的未决问题

**血量与伤害的存储宽度：`int32` 还是 `int64`？**

这条**没有结论，球在我们这边**，是 Cody-ic 在 2026-08-28 04:22 的评论里提出的反建议，
我当时在做素材合规审查、没有回复它。你接手后要么答它、要么在 PR 里带着它一起评审。

- 我原先的建议：`int32` + 把依据写在 `static_assert` 旁边
- **他的反建议 `int64`，论证是「解耦」**，而且这个论证是对的：
  我算的 `int32` 溢出点（`w ≈ 4.6×10³`）**依赖两个都还没标定的数**（训练截断波数 W、基础血量），
  两个未知数相乘算出来的余量不是余量。
  他由此提炼出一条我认为应当直接写进契约的规则：

  > **回放格式承诺不得依赖任何待定数值。** 选宽度时应选到「该数值的整个候选区间都不可能影响它」，
  > 而不是选到「按某个假定值刚好够」。

- `int64` 的代价接近零：单波单位数硬上限约 40，每实体多 4 字节；
  **血量进观测的是比例（第 ⑩ 条），所以宽度根本不传到 Python 侧**；
  `feed_pod` 对 8 字节与 4 字节一视同仁；而伤害中间量本来就要 64 位，
  用 `int64` 存反而少了"存 32 算 64 再截回"这一层会写错的地方
- 若你仍要 `int32`，他的替代方案是把它写成**一条会红的检查**而不是注释

**我的看法**：`int64` 更省事，且那条「格式承诺不得依赖待定数值」的规则值得单独写进契约文档。
但这是你的 PR，你来定，说明理由即可。

### 另有三件 Cody-ic 在 #24 复核里点名留给契约那一版的

他明说这三条**不必在 #24 处理**，但第一条是他直接指给接口契约 PR 的：

1. **确定性守卫目前只扫 `rts_core/`，建议在契约那一版把扫描目录一起扩到 `bindings/` 与 `game/`。**
   他的理由是对的：**`bindings/` 的观测打包那一层完全可能出现 `unordered_map`**，
   而那正是守卫要防的。改一行 `add_test` 的参数即可，别等出问题
2. `determinism_bans_empty_is_red` 用 `WILL_FAIL`，**分不清"空目录导致的红"与"脚本崩溃导致的红"**。
   覆盖上没漏（脚本真崩时 `determinism_bans` 那条会同时红），只是这条测试本身诊断力弱。记一句即可
3. 三个自动生成的 TU 编进了 `rts_core`，于是交付的静态库里带三个 `probe_*` 符号
   （是为绕开 LNK4221 的取舍）。**打包交付时若有人问起，得有个答案**

**~~顺带一件他还没答的：`TreeSmall` / `RockSmall` 改名~~ —— 已解决，#30 已合入。**
那组 `ObstacleId` 的类型枚举成员现在有名字了，三个都 ≤7 字符：

| 中文 | 标识符 | 视觉 |
|---|---|---|
| 树桩 | `Stump` | 木质、贴地 |
| 幼树 | `Sapling` | 单株细树，明显矮于密林 |
| 碎石 | `Rubble` | 一小堆碎石，矮而散 |

`CLAUDE.md` 已为这一类**单开一张命名对照表**并写明它**不吃三轴定位**（那三轴是为参与克制
矩阵的战斗单位设的，障碍物不移动、不攻击、不进克制矩阵，只有血量一条属性）。
表下注明「随 §6 一同待议」——若 §6 被否，这三者退回纯景物，但**你按第 ⑤ 条留的位不受影响**。

### 另有两条是本文写完之后才浮出来的，都落在你的解算上

**其一，`CLAUDE.md` 有一处内部矛盾，战斗解算会同时消费这两句：**

> 建筑与城墙走**与单位完全相同的战斗解算**（血量、护甲类型、承伤计算），仅护甲类别不同
>
> **破坏速率是兵种的固定属性，与等级无关。** ……等级只让他们在火力下活得更久，不让他们拆得更快

若真是同一套解算、而等级又提升伤害，**破坏速率就会随等级涨**，「满级步兵一拥而上瞬间破墙」
这条被明令封死的形态直接复活。两条不能同时成立。**我原本主张：破坏速率是独立于伤害的通道**
（它保住那把结构锁），但这条要在契约里明写成哪一种，别留给实现去猜。
背景与推导在 #26 的议程更新第二节与 #23 我的评论。

**这处矛盾现在有了第二个候选解，而且是相反方向的，你要在两者之间选。**
#23 最新一版新增 §7.6：城墙 / 城门可升级，且**强制**破坏速率随等级。它的论证是——
墙可升级之后，只升墙 ⇒ 破墙时间 → ∞（龟缩复活）；只改速率 ⇒ → 0（三阶段中段消失）；
所以真正该守的不变量不是「破坏速率是常数」，而是「**破墙时间必须落在一个区间里**」。
这个论证我认为站得住：它给出了标定靶子，而「速率恒定」在守方也有等级轴之后确实守不住原意。

于是两个候选是：

| | 破坏速率 | 结构锁靠什么守 | 对你的代价 |
|---|---|---|---|
| 甲（我原本的主张） | 独立通道，与等级无关 | 速率恒定 | flow field 份数 = 兵种尺度的常数（`CLAUDE.md` 5.1 现行写法） |
| 乙（#23 §7.6） | 随等级，与墙的等级同乘 | 破墙时间落在区间内 | **flow field 变成「兵种 × 等级档」** |

**注意乙的代价直接落在寻路上**，所以这不是纯设计问题、你有发言权。
两条一起答（本节这条 + 第 9 节表里那条），别分开——它们是同一处矛盾的两端。

**其二，「斜向能不能穿角」这条移动模型的约定，现在有下游消费者了。**
`tools/map_gen/` 的校验器（#31 / #33）已经被迫先取了最保守的一种（两个正交邻格都可通行
才允许斜向），并写明「接口定稿后回来对齐」。**注意保守方向不是永远安全的**：
对「必须连通」型的检查（走廊可达、无孤岛）保守 = 多报；对「必须存在一条通路」
（初始城圈的缺口）与「不得存在一条通路」（森林遮蔽通道）两类，极性是反的。
所以这条约定越早定死越好，它不只是 `rts_core` 内部的事。

---

## 6. 红线（不可协商，越线要专门开 PR 订正）

**一、三条不变量**（`README.md`「三条不变量」有全文）。它们保护的是训练吞吐量与可复现性，
不是语言纯净度。你写接口时最容易踩的是第 1 条：
**热路径内不得向上回调 Python**——Python 一次调用推进 N 个 tick，
绝不能每单位每 tick 回调一次上层，否则吞吐量塌一到两个数量级。

**二、扁平数组 + 整型 ID，禁逐单位堆分配、禁虚函数分派。**
`CLAUDE.md` 已经写明**主要理由是确定性而不是性能**，也写明了两点澄清
（堆分配本身不破坏确定性、虚分派本身也是确定的）。**注意最后那句：
用了扁平数组也不等于自动确定**——若在别处用 `unordered_map<int, State>` 驱动遍历，一样会坏。

**三、确定性禁令是会红的 ctest，不是文字。** 违反 `unordered_*` / C 库随机 / 挂钟 /
指针作 key 会在构建时红。确属误报时在**行末**加 `// determinism-ok: <理由>` 放行——
**刻意要求写理由，使放行可评审**。别去改检查器来绕过它。

**四、凡关系到平衡性与玩家体验的数值，一律保持待定。**
这是**组内明文要求**，不是建议，而且**违反过一次**（地图规范曾把集结点数量写成「定为 4」，
专门开 PR #17 订正）。血量、伤害、射程、速度、造价、破坏速率、编成位占用、
收入曲线、地图尺寸、集结点数量、空军上限——全部待定。
写代码时的落地方式：**数值以占位常量放进 JSON 数据文件，调整不触碰代码**；
文档里举例必须标注「占位值」。

**必须会区分「结构约束」与「数值」**：「每个集结点对应一条性质不同的走廊」是结构约束，
现在就能定死；「集结点有几个」是数值，现在不定。
**第 4 节那十三条全部是结构约束**，所以它们不违反这条规则——注意第 ⑫ 条我们是怎么处理的：
「伤害结果必须为正」写成结构约束，而不是写成「钳到 1」这个数值。

**五、`课程设计最终交付项.md` 是老师原文，一个字都不要动。**

**六、`日志.md` 只改自己那一行。** 它是答辩「人员分工」页的直接依据，
而那一页是**个人成绩的重要依据**。你那一行现在写的是「待本人补充」，**只有你能填**。
每次上机后当场补录——`git log` 能补齐"改了什么"，补不齐时间线、讨论结论与个人分工。

**七、每个人推自己的提交，git 身份用 `--local` 配、不要动全局。**

```bash
git config --local user.name  "ArLiangz"
git config --local user.email "<你的 GitHub noreply 邮箱>"
echo ".claude/" >> .git/info/exclude    # 个人配置不要进团队仓库
```

---

## 7. 这个仓库已经踩过的坑（**每一条都真的发生过**）

第 1–7 条是同一类缺陷，**上机第 2 天在 #24 一次性修掉的**：
**失败时本该有反应，实际一声不吭。** 共同成因不是判断力，
而是**验证路径只走了 happy path**（VS 多配置生成器 + Python 齐备 + 头文件跟着 Catch2 一起编）。
七处全在那条路径旁边。你写契约时会新增头文件、新增测试、新增 CMake 分支，
**同一类缺陷的产生条件全部具备**。

| # | 现象 | 成因 | 现在有什么守卫 |
|:-:|---|---|---|
| 1 | `cmake -B build` 在 Windows 上给的是 **Debug** | 守卫判空且放在 `project()` 之后，而 `Platform/Windows-MSVC.cmake:153` 已把 `Debug` 写进缓存。**「用户显式给过没有」这个信息只在 `project()` 之前存在** | 判断跨 `project()` 分两半，四种场景实测过 |
| 2 | 注释点名的 C4365 从来没开 | 它是 off-by-default 的。后果是**两套工具链严格度不对称**：本地干净、服务器直接失败 | 已补 `/w14365`，与 GCC 的 `-Wsign-conversion` 对称 |
| 3 | 确定性守卫扫到 0 个文件返回**绿** | 路径写错 / 目录改名 / 将来拆子目录忘改 `add_test` | 0 文件与路径不存在一律判失败 + 一条 `WILL_FAIL` 的 ctest |
| 4 | 找不到 Python3 时守卫整条**静默消失**，ctest 从 5 条变 3 条一样全绿 | 用的是 `message(WARNING)` | `RTS_REQUIRE_DETERMINISM_GUARD` 默认 ON，找不到就 `FATAL_ERROR`。「守卫不在」成为需主动声明的决定 |
| 5 | `1'000` 这种数字分隔符让守卫吃掉整行，同行的 `rand()` 查不出来 | 见单引号就当字符字面量起点 | `is_digit_separator()`；自检 13 + 12 条 |
| 6 | `types.hpp` 漏 `<compare>` 却一直编得过 | 测试文件先 include 了 Catch2，把它顺带拉进来。**还可能只在一套标准库上炸** | 头文件自足性守卫（GLOB，你新加的头自动纳入） |
| 7 | ctest 标签清单是**白名单**：标签写错抓得到、**漏登记抓不到** | 那条 ctest 条目根本不存在，ctest 仍全绿 | 一条不带过滤器的 `rts_tests_all` |

**第 7 条对你尤其现实**：你要加 `replay_test.cpp` 与 `determinism_test.cpp`，
大概率带 `[replay]` / `[determinism]`。忘了改清单，就是**新写的回放测试一次都没跑过**——
而回放测试是本项目主要的防 bug 手段。现在有 `rts_tests_all` 兜着，但**仍应顺手把新标签加进清单**，它是定位用的。

**第 8 条是第 1–7 条那一类缺陷的第八例，而它是在修完前七条之后、由 #24 的复核抓出来的**：

**8. 我的验证只跑了 Release，于是 `assert(index <= kMaxIndex)` 那行在我的验证里一次都没编过。**
`NDEBUG` 把它整行编掉了。Cody-ic 复核时补了 Debug 一列，并真把那条 assert 触发了一次
（`Assertion failed: index <= kMaxIndex ... line 106`）。
**这正是前七条的共同成因又发作了一次**：修的时候我盯着"守卫会不会红"，
却漏了"我这条验证路径覆盖了几个配置"。
**所以第 8 节的验收标准里 Release 与 Debug 两列都要跑**——凡是你新加的
`assert` / `static_assert` / 仅 Debug 生效的检查，只跑 Release 等于没验。

再两条与代码无关但会浪费你时间的：

**9. 中文 + cp936 控制台。** 用 `python -c "..."` 传中文字面量会被弄乱，
产生**假的**检查结果（我因此一度以为自己发布的三条结论是错的）。
写成 UTF-8 的 `.py` 文件、用 `PYTHONIOENCODING=utf-8` 跑。ctest 按 ASCII 标签注册是同一个原因的产物。

**10. ~~Linux/GCC 侧一次都没编过。~~ 已由 #39 做掉，但留下的教训对你更直接。**

Cody-ic 在服务器上跑了首次构建（Release + Debug 双配置 7/7 全绿、零警告，
`-Werror` 全程开着、没降级任何开关），顺带验掉了我在 #24 靠推理消掉的两处跨工具链
风险——`/w14365` 与 GCC `-Wsign-conversion` 的严格度对称、构建类型守卫在两平台一致。

**对你有用的是它暴露的四处，因为它们全都只在 Linux 侧看得见，而三处的症状是
「不报错，只是悄悄变差」**——这正是你即将新增大量 C++ 时的主要风险形态：

| # | 现象 | 为什么在 Windows 上看不见 |
|:-:|---|---|
| 1 | Catch2 的 FetchContent 卡住 configure | 服务器到 `github.com:443` 的 TCP **间歇性**不可达（DNS 正常、`codeload` 同期通）。已加兜底 URL + `TIMEOUT 20`，安全性靠现成的 `URL_HASH`（我独立复现过：codeload 那份归档的 SHA256 与声明值逐字符相同） |
| 2 | `build-Release/` 不被 `.gitignore` 挡住 | Makefiles 是**单配置**生成器，Release 与 Debug 必须各占一个目录；VS 是单目录多配置，本地永远不会出现 |
| 3 | `*.sh` 被转成 CRLF 后直接跑不了 | shebang 变成 `#!/usr/bin/env bash\r`，报 `/usr/bin/env: no such file or directory`——**而那个文件明明就在**。报错位置不是真因 |
| 4 | `chmod +x` 不进 git 索引 | Windows 上 `core.filemode` 默认 false，本地 `chmod` 是个假动作；到服务器上是 644、`Permission denied` |

**「首次」做完了，「频繁」那一半是每个人的事**，也包括你——`tools/server/sync.sh`
加 `build.sh` 已入库，动了 `rts_core/` / `tests/` / CMake 之后跑一次约两分钟。
你要新增的代码面比谁都大，**攒一批再编正是 `CLAUDE.md` 明令要避免的**。

---

## 8. 怎么编、怎么测

`PATH` 里通常没有 cmake，VS2022 自带一份：

```bash
CM="/c/Program Files/Microsoft Visual Studio/2022/Community/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/cmake.exe"
"$CM" -G "Visual Studio 17 2022" -A x64 -B build
"$CM" --build build --config Release
"${CM%cmake.exe}ctest.exe" --test-dir build -C Release --output-on-failure
```

首次配置经 FetchContent 下载 Catch2（约 1 MB，**需联网**）；之后可加
`-DFETCHCONTENT_FULLY_DISCONNECTED=ON` 离线复用。挑单条用例：

```bash
ctest --test-dir build -C Release -R rts_tests_rng    # 按标签
./build/tests/Release/rts_tests.exe "<用例名>"         # 按用例名（中文，不要经 ctest）
```

开关：`RTS_BUILD_TESTS`(ON) / `RTS_BUILD_RENDER`(**OFF**，训练构建因此根本不配置 `render/`) /
`RTS_WARNINGS_AS_ERRORS`(ON，仅第一方代码) / `RTS_REQUIRE_DETERMINISM_GUARD`(ON)。

**验收标准——每条都要真跑过，不是「应该能过」**：

1. `/W4 /WX` 下配置 + 构建**零警告**
2. `ctest` 全绿（先 `ctest -N` 看当前有哪些，**别照抄任何文档里写死的条数**——
   它今天已经从 7 变成 8，而 #38 就是因为一处写死的条数过期才开的）
2b. **上面两条 Release 与 Debug 各跑一遍。** 不是形式主义：
   `NDEBUG` 会把 `assert` 整行编掉，所以 Debug 是它唯一被编译的配置。
   我在 #24 只跑了 Release，那条新加的 `assert` 在我的验证里一次都没编过（第 7 节第 8 条）
2c. **Windows 与 Linux 各跑一遍。** #39 已把服务器侧打通（`tools/server/sync.sh`
   加 `build.sh`，约两分钟），而它暴露的四处全都只在 Linux 侧可见——见第 7 节第 10 条
3. **故意破坏一次，确认守卫真的变红，然后还原并确认 `git diff` 为空。**
   这一步不能省——#24 那七处缺陷的本质就是"没人验证过失败路径"。
   我做过的两次：往 `rts_core/` 塞一行 `std::unordered_map` → 确定性守卫红；
   摘掉 `#include <compare>` → `rts_core` 直接编译失败
4. 你新增：同种子跑两遍，**逐 tick 哈希序列完全一致**
5. 你新增：录一段命令流 → 重放 → 逐 tick 哈希一致
6. **性能相关的改动一律在 Release 下测量**，Debug 构建的仿真吞吐不具参考意义

---

## 9. 未决事项：哪些阻塞你，哪些不

三份未合并提案累计 **19 项待拍板**。我逐条查过：**没有一项在结构上阻塞接口契约**，
前提是你按第 4 节做（侧中立、数据驱动、留位 `ObstacleId`）。

| 未决事项 | 阻塞你？ | 你要怎么办 |
|---|:-:|---|
| #15 训练时守方也上 RL（6 项） | **否** | 它的排期表把「阶段 0 = 接口 + 骨架 + 回放」的前置写成「无。**当前的瓶颈**」。第 4 节第 ① 条一做，它就退化为训练回路的改动 |
| #16 演示与可视化（5 项） | **否** | 影响 `render/` 预算，不影响接口。唯一相关的是双视图要求 `FogLayer` 存在 → 第 ⑥ 条已覆盖 |
| #23 无尽模式与地形分层（8 项） | **部分** | §6 可破坏障碍 → 留位 `ObstacleId`（第 ⑤ 条）；§7 守方等级轴 → 敌方等级通道（第 ⑧⑨⑪ 条）。**两者都按"会通过"留位，代价是一个 tag 类型 + 一个通道** |
| `地图与场景设计.md` 2.1 甲还是乙 | **否** | 影响 `buildable` 怎么算，不影响 `Terrain` 枚举的形状。两人一致倾向乙，卡在一处范围收缩（城区固定 ⇒ 墙类决策只剩「修旧」），**这条不该由两个人拍** |
| 全部数值 | **否** | 按红线四放 JSON 数据文件 |
| 血量宽度 int32/int64 | **是** | 见第 5 节。这是唯一一条真的要你先答的 |
| ~~`TreeSmall`/`RockSmall` 改名 + 第三类实体的命名对照表~~ | ~~是~~ **已解决** | #30 已合入：`Stump` / `Sapling` / `Rubble`，命名对照表已开 |
| 「破坏速率与伤害是不是同一条通道」 | **是** | `CLAUDE.md` 两句互相矛盾，见第 5 节末尾。战斗解算绕不开它 |
| 「斜向穿不穿角」 | **是**（但很小） | 校验器已在等这条，见第 5 节末尾 |
| ~~**Linux/GCC 首次构建**~~ | ~~否，但紧迫~~ **已解决** | #39 做掉了（双配置 7/7 全绿、零警告，并验掉我在 #24 靠推理消掉的两处跨工具链风险）。**但责任转移给你了**：脚本已入库，「频繁」那一半从现在起是每个提交者的事，而你要新增的代码面比谁都大。见第 7 节第 10 条那四处 |
| **城墙可升级 ⇒ flow field 按「兵种 × 等级档」** | **是**（若 #23 §7.6 通过） | #23 最新一版提出城墙/城门可升级、且**强制破坏速率随等级**。它把 `CLAUDE.md`「破坏速率与等级无关」那条结构锁换成「破墙时间必须落在一个区间里」。**对你的直接后果是 flow field 的份数从「兵种尺度的常数」变成「兵种 × 等级档」**——`CLAUDE.md` 5.1 现在明写前者。这条与上面「破坏速率与伤害是不是同一条通道」是同一处矛盾的两端，**一起解，不要分开答** |

---

## 10. PR 状态与合并顺序

**截至 2026-08-28 第 2 次上机结束。**`main` 已含下面全部「已合入」的内容，直接从 `main` 开你的分支。

| PR | 内容 | 状态 |
|---|---|---|
| #24 | 七处「该红却绿」修复 + 三道新守卫 | **已合入** |
| #22 | 训练服务器环境实测（GCC 13.3 / CMake 3.28.3 / python3 3.12.3 都在位） | **已合入** |
| #27 | 素材三族重做、场景预览重写、`check_assets.py` | **已合入**。顺带修出「线性结构的 `d` 是走向不是朝向」，已写进 `地图与场景设计.md` 4.2.1.1 |
| #30 | 可破坏障碍改名与独立成组、命名对照表 | **已合入**。你的 `ObstacleId` 枚举成员名由它给出 |
| #28 / #32 | 两行日志 | **已合入** |
| #38 | 订正 `CLAUDE.md` 对构建类型的转述 + 补四处开工必需的事实 | **已合入**。**这一条你开工第一天就会用到**：Windows 默认是 VS 多配置生成器，`cmake --build build` 不给 `--config` 拿到的是 **Debug**；另含地基已提供的基元清单与 ±0.0 那个坑 |
| #39 | 首次 Linux/GCC 构建（双配置 7/7）+ 服务器同步/构建脚本 | **已合入**。见第 7 节第 10 条——它暴露的四处全都只在 Linux 侧可见 |
| #31 | `tools/map_gen/` 地基（@omg321-975） | **已合入**。纯 Python，与你无关 |
| #33 | 第 8 节校验器 8 条（@omg321-975） | 评审中，我提了 CHANGES_REQUESTED。**有一处与你相关**：见第 5 节「斜向穿角」那条 |
| #34 | README 同步 map_gen 进度 | 需 rebase（与 #39 撞同几行） |
| #23 | 无尽模式与地形分层 | **提案，不要合。** 已重写过两轮：最新一版提出**城墙可升级、且强制破坏速率随等级**，那会改掉 `CLAUDE.md` 一条结构锁，并**落到你头上一项**（flow field 从「兵种」变成「兵种 × 等级档」）。见第 5 节 |
| #16 / #15 | 演示清单 / 守方 AI | **提案，不要合** |
| #26 | 三份提案的表决议程 | issue，**唯一的表决场所** |

**下一个该合的是你的契约 PR。** 它现在不被任何东西挡着。

分支与 PR 规矩：

```bash
git checkout -b feat/rts-core-contract          # 类型前缀：docs/ design/ tools/ feat/ fix/
git commit                                       # 写"为什么"，不只写"是什么"
git push -u origin feat/rts-core-contract
gh pr create --base main
```

不直接推 `main`。**PR 描述要写清为什么这么改**——四个人都在用 Claude Code，读 PR 是同步认知最有效的途径，本仓库现有 PR 可以直接当模板。

---

## 11. 建议的第一天动作顺序

1. **拉 `main`，按第 8 节编一遍、测一遍，Release 与 Debug 各一次。**
   先确认你的机器能跑通再动笔——这一步同时是对第 7 节那十条坑的一次实地核对
2. **读第 4 节 + `types.hpp`**，然后决定第 5 节那个 `int32`/`int64`
3. **在 PR 里先发一版「只有头文件 + 契约文档、没有实现」的草案**，
   把第 4 节十三条逐条标「照做 / 我要改，理由是」，把第 5 节两条问题挂上去。
   接口是四人共用的契约，**评审那一轮不该被实现细节掺进来**——
   这也是 #20 当初刻意只落地基、不碰接口的同一个理由
4. 契约合入后，`CombatSystem` / `Pathfinder` / `VisionSystem` / 参数化脚本守方
   成为可认领任务，**写进 `README.md` 未认领工作表**——
   omg321-975 至今零提交，他需要一块不依赖你后续工作的活
5. **当场补 `日志.md` 你那一行**（现在是「待本人补充」）

---

## 12. 交接后我这边还在做的

- #15 / #16 是我的提案，19 项待拍板里我负责的那些由我在小组会上过
- `tools/map_gen/`（生成器 + 校验器，15 条校验里 10 条不含数值）与占位产图路线的去留
  仍**未认领**，两块都是纯 Python、都不依赖你

有任何一条第 4 节的设计点你不同意，直接在 PR 里写理由——**那十三条里有五条是对面推翻我
或我推翻对面之后的版本**，所以推翻是这个项目的正常工作方式，静默改才不是。
