# `rts_core` 接口契约

> **状态：公开接口已全部落地。** 枚举、观测注册表、`World` / `WorldView` / 迷雾 /
> 地形，以及**回放格式与骨架**。剩下的是机制（1c）与两个下游模块
> （`rts/obs_macro.hpp`、`BatchedEnv`）。
>
> **1c 的归属：@ArLiangz 认领（#28），@zhxxx233 自 2026-08-30 起接手推进（#57）。**
> 四块（战斗 / 寻路 / 视野 / 经济）全做，不预先切一半留着——理由与进度都在 #57。
> **机制之前先落一件前置的事**，见 §1.1.2。
>
> 本文记录的是**决定与理由**；**形状看头文件**。两者刻意分开：
> 头文件会随实现走，而「为什么是这样」不该跟着漂。
> 凡本文与头文件冲突，**以头文件为准**，并请顺手修本文。

## 0. 这份文档为什么存在

`rts_core` 的公开接口是四人并行开发的第一个跨模块契约——`render/`、`bindings/`、
`train/`、`tests/` 全部依赖它的形状（README 未认领工作表第 1 项：
「**最高优先级 —— 其余三人都在等它**」）。

它的十三条设计点在 2026-08-27/28 于 PR #23 的评论串里由 @zhxxx233 与 @Cody-ic
逐条辩定，**其中五条是双方互相推翻之后才定下的版本**。那批结论此前只存在于
两个地方：PR 评论串，以及 `rts_core 交接说明.md` 第 4 节——而后者在
**尚未合并**的 PR #25 上，且该 PR 已因 main 前进而 CONFLICTING。

「PR 评论会被后续讨论淹没」这句话是我在同一条串上对别人说过的。
所以本文的第一件事就是把它们搬进一份**合并了的**文件。

`rts_core 交接说明.md` 第 4 节因此冗余，可随 #25 一并删掉。

## 1. 已落地

| 头 | 内容 | 落地的决定 |
|---|---|---|
| `rts/types.hpp` | tick、`Side`、`Vec2`、`GridPos`、三组句柄、格坐标换算 | ⑤ |
| `rts/roster.hpp` | 11 单位 + 11 建筑 + 3 障碍 + 3 资源、结构性属性 | ⑪ |
| `rts/terrain.hpp` | 五种地形、三张按格位图 | — |
| `rts/action.hpp` | 战术动作 13 个、8 方向、决策周期 | ② |
| `rts/command.hpp` | 玩家级 / 宏观命令 11 个 | ①② |
| `rts/obs.hpp` | 观测通道注册表、版本号、布局指纹 | ③⑧⑨⑩ |
| `rts/fog.hpp` | 每侧一份的迷雾 / 侦查记忆图，三态 | ⑥ |
| `rts/world.hpp` | 三组扁平数组、槽位池、`submit` / `advance`、掩码、状态哈希 | ①⑤ |
| `rts/world_view.hpp` | 只读视图（编译期保证） | ④ |
| `rts/replay.hpp` | 回放格式、录制器、比对（含平台指纹与哈希口径标签） | ⑦ |
| `rts/hash.hpp` | 状态哈希（补了可在编译期用的 `feed_text`） | — |
| `rts/stats.hpp` | **数值表容器与指纹**（四组分法、`kStatsShapeTag`） | §1.1.2 |

展示名在 `game/display_names.hpp`（中文只出现在展示层）。
花名册与精灵元数据的 1:1 由 `render::SpriteAtlas::verify_roster_covered()` 钉住。
`game::make_world_init()`（`game/world_builder.hpp`）是 `MapData` → `WorldInit`
的唯一接缝，同时充当「契约装不装得下真实消费者」的一道检查。

### 1.1 `World` 本版**没有机制**，边界画在哪

战斗解算、寻路、视野、经济归 1c（**tracker 是 #57**；#28 是 @ArLiangz 的认领记录）。分界是一条明确的线：
**凡是需要一个待标定数值才能推进的，本版不做；凡是不需要的，本版做完。**

> ### ⚠️ 这条分界线**不适用于 1c**，本节因此曾给出一个误导性的印象
>
> 上面那句话是 `World` 作为**数据布局**的分界——它连一个 `damage()` 都没有，
> 所以只能这么切。**但 1c 不受这条限制，因为数值可以做成参数**，而这个模式
> 本文件里已经在用了：
>
> ```cpp
> UnitId spawn_unit(UnitType type, Vec2 pos, std::int32_t level,
>                   std::int64_t hp, std::int64_t max_hp);   // ← hp 由调用方给
> ```
>
> 同一招对战斗解算成立：`damage(UnitId, std::int64_t amount)` 里那个 `amount`
> **从哪来不是 1c 的问题**，是 JSON 数值表的问题。
> **所以「等标定」与「开工写机制」是两件独立的事**——下面那张表里的 ❌ 有一部分
> 只是「本版（契约）不做」，不是「标定之前做不了」。
>
> 逐块判断见 §1.1.1。这条警告是 2026-08-30 补的，起因是 #57 上有人**因为读了
> 上面那句而以为 1c 还不能开工**——而那正是关键路径上最贵的一种误解。

| | 本版 |
|---|---|
| tick、波次阶段、RNG 状态 | ✅ |
| 三组实体的增删、句柄代数、槽位复用 | ✅ |
| 命令入队 + 校验 + 排空 | ✅ |
| `None` / `Summon` / `SelectForce` / `Composition` / `PickSpawn` | ✅ 完整应用（都不需要数值） |
| `Build` / `Repair` / `Cancel` / `Train` / `MoveForce` / `Garrison` / `Clear` | ❌ 但**记账**，见下 |
| 地图里的可破坏障碍装进 `World`（`ObstacleInit`） | ✅ 血量由调用方经 `game::InitialHp` 给，同墙 |
| 清野的破坏与产出 | ❌ 1c（要破坏速率与产出数额，两个都待标定） |
| 攻击前摇、施工进度的递减 | ✅ 纯计数器 |
| 移动、战斗、采集、视野解算 | ❌ 1c |
| 动作掩码的移动 8 位 | ✅ 按地形与穿角判 |
| 动作掩码的攻击 4 位、命令掩码的买得起 | ❌ 默认**允许**，见下 |

> **落地进度（2026-08-30，机制第一、二、三批）：上表的 ❌ 大半已经翻绿。**
> 承伤与死亡、目标选择与攻击（乙）、相邻格移动（撞上自动破坏）、建筑攻击、
> 视野解算属**第一批**（#73）；`Build` / `Repair` / `Cancel` / `Train` /
> `MoveForce` / `Clear` 六种命令的解算、经济收入（采集建筑踩点产出 +
> `Keep` 金币地板）、施工与维修（守方 `Mason` 在半径内才推进工时）属**第二批**。
> 其中 `MoveForce` / `Clear` 解算成**世界状态里的指令**而不是「World 代替单位
> 走路」——守方单兵由脚本驱动，执行层读指令，这是 #46 那条结构决定在机制层的形状。
> **第三批收掉 `Garrison` 与高度优势**（这对明写要成对的）：命令解算成**按格的
> 驻守指令**（同上一条先例；按格不按编队，因为一个编队要能同时驻守多格），
> 登墙在 `World` 内解算（延迟查表、一格一人、钉住、`MoveForce` 即召回、
> 墙塌人落缺口）；高度优势三条幅度全查表（miss / 减伤 / 远程射程加成），
> **墙血 < 一半三条一起消失**（局部破损的二值实现）。**12 种命令自此全部解算**，
> `deferred_command_count()` 按当初的约定已删。`WorldInit::UnitInit` 补了
> `force` 字段（初始局面要能表达「开局就有编制的守军」，且攻方带编队在建局时
> 被拒——「编队是守方概念」由此是结构而不是约定）。
> **第四批收掉冲锋助跑、克制倍率与 `Tower` 齐射**：冲锋动量逐 tick 累距
> （站停 / 撞停归零、前摇冻结、落地耗尽），「伤害 ∝ 助跑距离」机制等级无关；
> 克制倍率**由三轴推导而非平铺 N×N**——唯一的数值克制关系是
> `counters_charge()`（Melee × HeavySlow）打有动量的 `Charge`，幅度随对方
> 动量线性放大，于是「开阔地克、巷战被反克」不需要读地形；`Tower` 齐射是
> 建筑 AOE、落点在承诺那一刻锁定（与 `Ram` 同一条规则），对空建筑结构上
> 忽略 AOE（「AA 只做单体狙击型」）。
> 仍是 ❌ 的只剩：flow field 寻路、在途弹丸实体
> ——都不是命令，没有可记账的输入。表里那处 `game::InitialHp` 已随数值表
> 落地被删（§1.1.2 末尾），初始血量现在直接查 `StatsTable`。

两条要点：

- **`advance()` 不是空操作，这是刻意的。** 一个什么都不做的 `advance()` 会让回放
  测试变成永远绿的摆设——而回放是 CLAUDE.md 列的主要防 bug 手段。
- **那七种命令被计入 `deferred_command_count()` 而不是静默丢弃。**
  于是「本版没做这一类」是一个可以被读出、可以被断言的事实。
  1c 实现它们时把这个计数器一并删掉。

  原来是六种，`Clear`（清野）是第七种。**那次加成员时那条用例照旧全绿**，
  因为它把六种硬编码成一个 `array<..., 6>`——第七种进不了它的视野。
  已改成「两份清单（记账 / 已应用）必须正好铺满整个枚举」，于是下一次加命令时，
  漏登记会当场红。教训与 ctest 标签清单是白名单那条同源。

  **后记（机制第三批）：这条约定已按原话兑现**——12 种命令全部解算，计数器
  与那条用例一并退役。它防的事（加命令时漏写解算分支）改由 `apply_one` 那个
  无 `default` 的穷举 switch 兜住（`-Wswitch` / `/w14062` 在编译期红），
  与花名册那几处穷举 switch 是同一个手法。

### 1.1.1 1c 逐块的数值依赖：**有一个零依赖的子集，可以立刻开工**

上面那张表的 ❌ 是「契约版不做」，**不等于「标定之前做不了」**。逐块核过：

| 块 | 卡数值吗 | 依据 |
|---|:-:|---|
| **承伤与死亡**（`damage()` → 归零 → `kill_unit`） | **不卡** | 伤害量是入参，同 `spawn_unit` 已有的 `hp` / `max_hp` |
| **目标选择**（四个 `Atk*` 打的是谁） | **不卡** | 「最近」是距离比较、「最弱」是血量绝对值比较（`action.hpp` 已定义死）。**射程只决定打不打得到，不决定目标是谁** |
| **动作掩码的攻击 4 位** | **不卡** | 它就是目标选择的产物——「有没有可打的目标」。目标选择落地它跟着落地 |
| 移动：**相邻格推进** | **不卡** | 速度是入参；`TerrainMasks::passable` 与穿角规则都已在 |
| 移动：**flow field 寻路** | **卡** | 墙的代价 = 血量 ÷ 破坏速率，而破坏速率待标定，**且「量化成几档」是一个还没定的形状**（§5.1 / `README` 未定栏） |
| 视野：**格子可见性** | **不卡** | 半径是入参，`blocks_vision` 位图已在，`FogLayer` 三态已在 |
| 七种命令（`Build` / `Repair` / …） | **卡** | 造价与耗时 |
| 清野的破坏与产出 | **卡** | 破坏速率 + 产出数额 |
| 经济采集 | **卡** | 产出速率 |

**所以起点是「承伤 + 目标选择」**，三条理由：

1. **它是所有战斗的地基**，且**能立刻被回放测试覆盖**——`advance()` 里多一个状态
   变化，`state_hash` 就会动，跑歪了当场报。在此之前回放只锁得住格式
2. **它顺带答掉 #42 / #69 那条缺失的接口信息**：前端要画一发箭从 A 飞到 B，
   缺的就是「B 是谁」，而那正是目标选择的产物。弹丸精灵已入库（#69），
   所以这一块落地就解锁 `render/` 的弹道
3. **它不碰任何卡着的东西**，于是「等标定」不构成阻塞

**一条实现时会撞上的设计决定，已定**（不是数值，所以现在就能定）：`AtkNear` 选中的
目标可能**不在射程内**。两条路对 RL 的信号完全不同，所以不能边写边定——否则两种行为
会混在同一份权重里。

| | 语义 | 后果 |
|---|---|---|
| ~~甲~~ | 选中即走过去（`Atk*` 隐含移动） | 掩码可以粗，但 `Atk*` 需要**寻路** |
| **乙 ✅** | 打不到就空转，且掩码精确到「射程内有没有合法目标」 | `Atk*` 与移动保持正交 |

**取乙的决定性理由是依赖方向**：甲会让 `Atk*` 隐含寻路，于是**把一块本来可以立刻
开工的东西（目标选择）绑到一块卡住的东西（flow field）上**——而上面那张表刚刚
论证的正是这两块的数值依赖不同。一个决定若把「不卡」拖成「卡」，它就自己否掉了。

附带理由是它保住动作正交：`Atk*` 自带接近的话，「先走近再打」这个可演示的战术就被
烤进动作里、看不见了，而 `CLAUDE.md` 明确期待包夹之类**涌现**出来。

乙同时把攻击 4 位从「默认允许」升级为精确，而这**不违反**下面那条默认方向——
那条管的是「还判不出来的位」，这四位现在判得出来了：

掩码里未定的位一律**默认允许**，方向是刻意选的：错误地允许只是浪费样本，
错误地禁止会让 agent **永远学不到**那个动作，而后者不会有任何东西提示。

### 1.1.2 但有一件事必须排在机制之前：**数值表的进入路径，以及它的指纹**

全仓库有**七处**代码注释把它称作「那份**还不存在的** JSON 数值表」
（`world.hpp` 三处、`fog.hpp`、`roster.hpp`、`game/map_data.hpp`、`game/world_builder.hpp`），
每一处都在解释「所以这个数值由调用方给」。但**没有任何一处规定它怎么进来**，
而 1c 一动手就必须让它进来——射程、伤害、破坏速率、造价全在里面。

**它是外生输入，因此它变了回放就该失效。** 而现有三个诊断字段一个都抓不到：

| 字段 | 抓得到吗 | 为什么 |
|---|:-:|---|
| `platform_fp` | ❌ | 它管的是浮点可复现性 |
| `hash_tag` | ❌ | **状态布局没变、喂入清单没变**，只是数值不同 |
| `file_hash` | ❌ | 它管文件完整性 |

于是症状会是「第 N tick 状态不一致」，而那句话的意思是**跑歪了**——看到它的人会去查
一个不存在的确定性缺陷。这与 `hash_tag` 当初要消掉的误判是同一种，只是换了个来源。

**解法照抄 `map_content_hash`，它已经解决过完全同一类问题**（「地图在回放录完之后
被改过」）。照抄要抄全：**它有两个落点，各担一职**，只做一个都会退化——

| 落点 | 职责 | 现有先例 |
|---|---|---|
| 进 `state_hash()` | **早报**：数值表改了，**第 0 tick** 就分歧，不必等它影响到某个动作 | `world.cpp` 喂 `map_content_hash_` |
| 进回放头 | **诊断**：把「第 0 tick 不一致」翻译成「是数值表变了」 | `replay.cpp` 头里那 32 字节 |

只做前者会让诊断退化，而且退化得不显眼：**第 0 tick 哈希不一致有三个可能的成因**
（地图变了 / 数值表变了 / 初始单位不同），前两个已经各有字段分开，第三个由
`WorldInit` 表达。少一个字段，三者就并回一句话。

**一处不对称，是有理由的：指纹该由 `World` 自己从表算，不该由调用方给。**
`map_content_hash` 必须由调用方给——`rts_core` 不读地图文件（`replay_verify` 的注释
写着这条纪律），所以它算不出来。数值表不同：表本身就在 `WorldInit` 里、就在手上，
让调用方另外给一个指纹等于给同一件事**两个真相来源**，而两个真相来源迟早只更新一个
（同 `side` 不存在单位数组里的那条理由）。

**代价要认下来，而它正是「现在改」的论据**：改 `state_hash()` 的喂入清单 ⇒
按 `world.hpp` 那条规矩必须把 `kWorldHashTag` 一起改（`World/2` → `World/3`），
**所有已录回放失效**。而现在已录的回放只有 `tests/` 里那几份，都是现生成的。
再往后每实现一条机制就会多一批夹具回放（#57 上那条「每实现一条机制补一份夹具」），
**越晚改越贵**。所以它排在机制之前，不是排在机制里。

形状**只定三条**：

1. **进 `WorldInit`**，不逐单位进 `spawn_unit`（射程是兵种级的，逐单位存是冗余 +
   第二个真相来源），也不留在 1c 内部私有（`advance()` 要用它，最终仍得在 `World` 上）
2. **`game/` 读 JSON**，`rts_core` 不读文件——沿用既有纪律，与 `MapData` 同一条路
3. **按四组分**：按兵种 / 按建筑 / 按障碍 / 全局（克制倍率矩阵、高度加成、等级缩放）

**字段清单刻意不定。** 它随机制展开——写战斗时才知道要哪几个，现在定就是猜。
但有一条余地必须现在留出：**`§1.4`（等级提升哪几个数值）还没定**，所以表里存的应是
「基础值 + 缩放参数」，**不要把缩放规则烤进表的形状**。这是 §4.1 那条规则
（承诺不得依赖任何待定数值）在这里的又一次应用——若表的形状假定了「血量和伤害都随
等级涨」，那么 §1.4 定成「只涨一个」时要改的就不止一个数字。

> **已落地（2026-08-30）。** `rts/stats.hpp`（容器 + 指纹 + `kStatsShapeTag`）、
> `WorldInit::stats`、`state_hash` 喂入（`kWorldHashTag` `World/2` → `World/3`）、
> 回放头 `stats_fp` 字段（`kReplayFormatVersion` 1 → 2）、新判决 `StatsMismatch`、
> `game::StatsLoader`（JSON 进入路径，严格校验：漏一个兵种或拼错一个键都报错，
> 不静默落回默认值）、`game/data/stats_placeholder.json`（**全部占位值**）。
> 第一批字段是承伤 / 目标选择 / 移动 / 视野四块要消费的；上面那条余地照办了
> （`GlobalStats` 两个缩放系数都存，§1.4 定案时归零其一即可）。
> 顺带收编：`game::InitialHp` 删除——表落地后它就是第二个真相来源，
> `make_world_init` 改为直接吃 `StatsTable`。

## 2. 待下一版

| 内容 | 落在哪 | 备注 |
|---|---|---|
| 宏观 / 决策层观测（槽位摘要，非 K×K） | `rts/obs_macro.hpp` | 依赖地图给出的槽位列表，见 §4.4 |
| ~~数值表的进入路径与指纹~~ | ~~1c（前置）~~ | ✅ **已落地**，见 §1.1.2 末尾 |
| 战斗 / 寻路 / 视野 / 经济 | 1c | 上表那几个 ❌ |
| 整数战斗解算与千分比倍率 | 同上 | 决定 ⑫，见 §3.4 |
| `BatchedEnv` | `bindings/` | 决定 ⑬ |

## 3. 十三条

### 3.1 接口形状

**① `step()` 对「哪一侧由谁控制」保持中立。**
取两段式 `submit(Side, …)` 入队 + `advance(ticks)` 推进，**不要**
`step(defender_cmds, attacker_actions)`。人类输入异步到达、RL 一次提交一批，
两段式同时容纳两者。

这条同时让「训练时守方也上 RL」退化为**训练回路的改动**而不是接口重写。
`rts/types.hpp` 里 `Side` 的注释就是这条的落点。

**② 输入分两级，都离散、都按侧参数化。**
`UnitAction`（战术，每 4–8 tick 一次）与 `Command`（玩家 / 宏观级）。
**「驻守墙段槽位」归 `Command`，不进 `UnitAction`**——于是战术动作枚举
一个字不动，CLAUDE.md「没有兵种专属动作」得以保住。

落地时多出一条 #46 带来的后果：**`Command` 同时是守方 RL 决策层的动作空间**，
因为守方 AI 是人类玩家的替身，其动作空间应当恰好等于玩家能下的指令。
所以只需要一套枚举，不是两套。推论：`Command` 必须是玩家指令集的**完备**表达，
`守方AI与协同演化.md` 第 3 节那个「建墙 / 建塔 / 建防空 / 建采集」的四组粗化
**不能烤进 `Command`**——那会让人类玩家没法指定造哪一座建筑。粗化属 `train/`。

**③ 观测通道做成带版本号的注册表。** Python 侧从模块读，**不硬编码**。
地形枚举本周刚从 3 变 5、还可能再动，硬编码通道数迟早**静默错位**。

落地时加强了一层：光有版本号不够，**版本号会被忘记改**。所以另有一个
`kObsLayoutFingerprint`——编译期把通道名、归一化方式、成对标记折成一个 64 位值，
重排即变。分工是**指纹管布局、版本号管语义**（同一个名字含义变了，指纹抓不到）。
训练侧把指纹写进 checkpoint，载入不符即硬失败。

**④ `WorldView` 把「渲染层只读」变成编译期保证**，不靠约定。不变量 2 因此写不坏。

### 3.2 数据布局

**⑤ 中立可破坏障碍另开一组句柄 `ObstacleId`，不带 `Side` 字段。**
**不要加 `Side::Neutral`。** 理由不只是「多一维永远为空的槽位」：
**观测通道是按侧成对定义的**，加进去会让成对通道数变成奇数并波及 Python 侧的解包
——那是会静默错位的破坏形式。

**必须在契约那一版就留出位置**，因为回放字节布局与 `state_hash` 的喂入顺序都要固定；
事后加一组实体会让所有已录回放失效。已落地（`rts/types.hpp`），
代价是一个空 tag 类型。

**⑥ 每侧的迷雾 / 侦查记忆图是一等公民**（`FogLayer` per `Side`）。
CLAUDE.md 的 RL 硬要求第 1 条是「AI 的观测必须是它自己的迷雾状态，不得是
ground truth」；`守方AI与协同演化.md` 第 4 节与演示头号点（双视图）都要求它存在。

`obs.hpp` 里的 `visible` 通道**是独立一条，不能省**：
没有它，「那一格没有敌人」与「那一格看不见」在张量里是同一个 0，
而整套侦查博弈建立在这两者的区别上。

落地时这条推理被迫再走一步，结果是**三态而不是两态**，详见 §4.3。
另外两条落地时的判决：

- **记建筑，不记单位。** 建筑不动，所以「上次看到那里有一段满血的墙」下一波仍是
  有用的情报；单位会走，记住的位置**主动误导**（AI 会朝一个空地包夹）。
- **`FogLayer` 进 `state_hash`。** 记忆是**累积**状态，不是每 tick 从世界重算出来的
  ——两个实体完全一致的世界可以记忆不同，此后立刻分叉。漏掉它等于把分叉的发现时机
  推迟到它影响到某个动作之后。

**⑦ 回放 = 种子 + 指令流 + 一份跨波状态快照。**
光有前两者**不足以从中途重放**——攻方的等级分配与空军跨波留存**已经是**
跨波永久状态。

### 3.3 观测语义

**⑧「敌方单位等级」通道存和，不存平均。** 三条理由：等级从 1 起 ⇒
「和 = 0」⟺「该格无敌人」，不需要额外 mask 通道；和是线性的，与卷积、池化、
下采样都可交换，平均不是；网络能自己从和 ÷ 密度还原平均，反过来不行。

**⑨ 归一化的分母用「本波名义等级 `L(w)`」（= 兵力预算 ÷ 编成位），两侧共用同一个分母。**
量纲上：等级和 ÷ `L(w)` = 「这格有多少个本波标准兵当量」，与密度通道同量纲。
**任何固定的归一化常数都会破坏「波数本身即构成课程」**——第 10 波与第 100 波
之间输入尺度漂移 100 倍，而「低波次学到的表示在高波次仍可用」是我们省掉手工
curriculum 的**唯一依据**。

**⑩ 血量进观测存比例 `hp / max_hp`，不存绝对值。** 与 ⑨ 同病同治。
附带后果：**血量字段宽度根本不传到 Python 侧**（这一条对 §4.1 很关键）。

**⑪ `kMinUnitLevel == 1` 要写成一条会红的检查，连注释一起。**
哪天有人把等级改成 0 基（**数组下标就是 0 基，很自然**），⑧ 那个通道会
**静默失效**：不报错、不崩，AI 只是再也分不出空地和一个 1 级兵。
已落地为 `static_assert`（`rts/roster.hpp`）。

### 3.4 战斗解算

**⑫ 战斗解算全部走整数，克制倍率用千分比（1000 = 1.0×）。**
主理由**不是**跨平台位级一致，而是：**成本产出矩阵是跑仿真算出来的，
它既是调数值的依据、也是答辩材料。若战斗含浮点，服务器上标定出的矩阵与
Windows 上演示时的实际战果可以对不上号，而两边都「没有 bug」。**
一个在演示机上无法复现的交付物不是精度问题，是坏的交付物。

范围要**诚实**：它只让**战斗解算**脱离平台差异，**不让整个回放跨平台可复现**
（移动与寻路仍是浮点）。别让后来者读成后者。

四个子条：

- **结果必须为正**，写成结构约束而不是「钳到 1」这个数值。0 伤害凭空造出一种免疫，
  违反「克制的本质是资源效率，不是二元胜负」。而它不是边角：高度惩罚与克制倍率
  会串起来，`3 × 0.4 × 0.5 = 0.6 → 0`，而低等级 = 第 1 波 = **演示开局**
- **截断只能发生一次，且由一个 helper 强制。** 倍率作为**列表**传进去、
  由它做唯一那次除法，倍率个数增加时调用方不改任何东西。链式调用会静默少算：
  `5×1500×1500/1e6 = 11` vs `(5×1500/1000=7)×1500/1000 = 10`
- **四舍五入，不向下截断**（`(积 + 500'000) / 1'000'000`）。理由是偏置：
  向下截断每次少算至多 1 点，在低伤害区是 20–30% 的相对偏差，
  **全局稳定偏向防守方**
- 伤害解算的**中间量**必须比存储宽：克制是乘法的，两个 10⁶ 相乘就是 10¹²

### 3.5 批处理

**⑬ `BatchedEnv` 的线程池大小必须是构造参数，不能写死。**
服务器是 96 线程的共享账号，**四个人各起 96 个 worker 是默认结果而不是失误**。
tmux 命名解决「谁在跑」，解决不了「各占多少核」。

## 4. 本版答掉的两个未决问题

### 4.1 血量与伤害的存储宽度：取 `int64`

`rts_core 交接说明.md` 第 5 节把这条列为「必须由接手人来答」。**采纳 `int64`**，
理由用 @Cody-ic 的原论证（原提议 `int32` 是 @zhxxx233 的，被推翻）：

`int32` 的溢出点 `w ≈ 4.6×10³` **依赖两个都还没标定的数**（训练截断波数 W、
基础血量）。两个未知数相乘算出来的余量不是余量。

由此提炼出一条**推广到整个契约**的规则，它比这个具体结论更重要：

> **接口与回放格式的承诺不得依赖任何待定数值。** 选宽度、选编码、选归一化时
> 应选到「该数值的整个候选区间都不可能影响它」，而不是选到
> 「按某个假定值刚好够」。

这条规则在本版已经用过两次，都在 `rts/obs.hpp`：
波数取 `log2(1 + wave)` 而不是 `wave / 训练截断波数`；
存活空军存**比例**而不是绝对数（上限本身是一条待标定的曲线）。
两处都不是「顺便」——按待定数当分母写出来的观测，会让 AI 学到一个基于假尺度的
策略，而且看不出来。

`int64` 的代价接近零：单波单位数硬上限约 40，每实体多 4 字节；
血量进观测的是**比例**（决定 ⑩），所以宽度根本不传到 Python 侧；
`feed_pod` 对 8 字节与 4 字节一视同仁；伤害中间量本来就要 64 位，
用 `int64` 存反而少了「存 32 算 64 再截回」这一层会写错的地方。

### 4.2 斜向能不能穿角：暂取「不能」，已给出可引用的名字

`rts/action.hpp` 的 `kDiagonalNeedsBothOrthogonal = true`。

这条本属寻路（ArLiangz 的分片），但它**已经在拖下游**：`tools/map_gen/` 的校验器
（#31 / #33）被迫先取了最保守的一种并写明「接口定稿后回来对齐」。
现在它有了一个可引用的名字，于是两侧不再各存一份假设。

取 `true` 的理由：与校验器现取值一致（改动量为零）；`false` 会让单位从两段墙的
对角缝里穿过去，而墙是「高代价可通行」而非障碍，所以那不是「绕过障碍」
而是**不拆墙就进城**，三阶段攻防的中段直接消失。

**保守方向并不永远安全**，这一点校验器那边已经吃过：对「必须连通」型检查
（走廊可达、无孤岛）保守 = 多报；对「必须存在一条通路」（初始城圈的缺口）
与「不得存在一条通路」（森林遮蔽通道）两类，极性是反的。
所以这条要定死，不能让两侧各取各的保守。

> **「暂取」与「仍需 ArLiangz 确认」已于 2026-08-30 闭合，改为已定。**
> 原写法把它挂在一个人的确认上，而那个人两天内没有对 1c 的任何一条问题回音
> （#57 上两条都没回），于是这条实际处在「谁都不敢引用、又谁都得引用」的状态——
> `tools/map_gen/` 的校验器已经在按它写码了。
>
> **闭合不等于不可改**：要改仍是那一个常量 + 校验器对齐，改动量与两天前一样。
> 变的只是**默认值有主**，不再等一个不确定何时到来的确认。
> 1c 由 @zhxxx233 接手推进（#57），所以这条现在归接手人。

另附一条落地时才发现、且很容易搞反的事实：**格坐标的斜向，是屏幕上的正北东南西**
（等距投影下格轴投成屏幕斜线）。按名字猜会恰好猜反，所以用
`rts::is_grid_diagonal()`，别自己判 Δ。它有一条拿真的 `IsoProjection` 印证的测试。

`World::action_mask()` 现在是这条常量的唯一消费者，`tests/world_test.cpp`
里那条穿角用例拿一段岩壁把它逼出来。

### 4.3 `visible` 通道必须是**三档**——这是本版发现的一处缺陷

第一个提交（枚举与观测注册表那一半）的 `visible` 是布尔。写 `rts/fog.hpp` 时发现它不够：

| 那一格的情况 | `visible` | `wall_hp` |
|---|:-:|:-:|
| 从未侦查过 | 0 | 0 |
| 侦查过，记忆里那段墙已破 | 0 | 0 |

**两行字节完全相同。** 而核心评估指标之一「AI 是否发现并利用已有缺口」
要的恰好是这两者的区别——它的现场证据就是「AI 的记忆图里那段墙是破的 →
它调头去打那里」。分不开的话，AI 学不到「记得的缺口」这个概念，
而对标产品 DiNaO 被批评的正是「敌人无视已有缺口、反复撞加固过的墙」。

改法是 `visible` 取三档（`Vis::Unseen` / `Remembered` / `Visible` → 0 / 0.5 / 1），
通道数、通道名、归一化方式一个字都不动。

**顺带它成了「为什么指纹不够、还要一个版本号」的现成例子**：
`kObsLayoutFingerprint` 因此**完全不变**（名字与 norm 都没动），
而含义变了。只靠指纹的话，一个用旧权重的 checkpoint 会被判为兼容，
然后那份权重里学到的「0 = 看不见」在新语义下是「从未见过」——
不报错、不崩、只是策略是错的。所以 `kObsVersion` 从 1 升到 2。
分工在决定 ③ 里写着，这是它第一次真的用上。

**为什么是等距三档而不是「记忆按时间衰减」**：衰减要一个时间尺度做分母，
而预期波长是待定数值——§4.1 那条规则在这里第三次生效。
波长标定之后可以再加一条独立的「记忆新鲜度」通道，那时改指纹与版本号。

### 4.4 `Command::slot` 的编码：格线性下标 `y*w + x`

这一条与「建筑四组粗化」（决定 ② 的推论）完全同构，所以一起理解：

- **仿真接受的是完备信息。** 玩家能点任意一格，所以 `slot` 必须能表达任意一格。
- `守方AI与协同演化.md` 第 3 节那个「槽位 index，约 60 个」是**地图给出的一个子集**，
  而地图格式里还没有这个字段（`地图与场景设计.md` 未定）。
  从 60 项的动作头映射到具体格属策略侧，归 `train/`。

`PickSpawn` 例外：它的 `slot` 是**集结点下标**，因为集结点本来就是地图给出的一张短表。

**上界必须被检查，而且必须是运行期的**：`slot` 是 uint16 且 `kNoSlot = 0xFFFF` 被占，
所以格数必须 < 65535。地图边长待标定（量级 64–128 ⇒ 最多 16384 格，余量很大），
但 256×256 = 65536 正好越界，于是「按当前量级够用」不构成承诺——
又是 §4.1 那条规则。`World` 的构造函数因此有一条 `cells >= kNoSlot` 的检查。

### 4.5 地形枚举搬到了 `rts_core`

`Terrain` 原先定义在 `game/map_data.hpp`，而依赖方向是反的：
`地图与场景设计.md` 开头把地图格式列为跨模块契约、第一句就是「`rts_core` 要吃它」，
4.2 明写「`rts_core` 载入时展开成三张按格的位图」，而 `game` 链接 `rts_core`、
反过来不行。`World` 要吃地形，枚举就必须在这一层。

`game::Terrain` / `game::ResourceType` 现在是别名，所以 `game/` 与 `render/`
的五十余处调用点一个字都没改。**别名不是「同一件事写在两处」**——
定义只有一份。而「`rows` 逐字符解码 ⇒ palette ≤ 10 项」那条断言**留在 `game/`**：
它约束的是地图文件的行编码，`rts_core` 不读文件。

### 4.6 回放里存什么、不存什么（决定 ⑦）

字节布局看 `rts/replay.hpp` 的注释，不在这里复制一份。这里只记三条判决。

**（一）只存外生输入，不存机制产物。** 回放 = 建局参数 + 输入流 + 周期性状态哈希。
`World` 的公开方法因此分成两类：

| 类 | 例子 | 进回放 |
|---|---|:-:|
| **外生输入**：只能来自人或策略，仿真自己推不出来 | `submit`、`submit_actions` | ✅ |
| **机制产物**：在完整的仿真里由规则算出来 | `spawn_unit`、`kill_unit`、`place_bld`、`begin_next_wave` | ❌ |

第二类不存的理由不是省字节，而是**存了会让回放变成假绿灯**：
若把 `kill_unit` 也记进流里，重放时就照着流把那个单位杀掉——
于是即便战斗解算算出完全不同的结果，回放照样对得上。
那正好把回放存在的意义抵消掉：它要查的就是「解算是不是每次都一样」。
**一份记录了机制产物的回放，测的是它自己的日志，不是仿真。**

代价是诚实的：本版机制尚未实现，所以初始状态必须由 `WorldInit` 完整表达——
这就是 `WorldInit::units` 被加进来的原因（见（三））。

**（二）头里三个字段，各消掉一种误判。** 回放对不上时最贵的不是修 bug，
是分辨这到底是不是 bug：

| 字段 | 消掉的误判 |
|---|---|
| `platform_fp`（编译器/系统/架构） | 跨平台比对被当成确定性缺陷。本项目**天生跨两套工具链**，而 CLAUDE.md 明写回放在两者之间不可复现 |
| `hash_tag`（= `kWorldHashTag`） | 「状态布局改了」被当成「仿真跑歪了」 |
| `file_hash`（全文 FNV-1a） | 「文件截断/损坏」被当成「第 N tick 状态不一致」 |

三者的**处理方式不同，因为契约不同**：`hash_tag` 不同 ⇒ 两边对「状态是什么」的
定义都不一样，比对本身没有意义，所以**当场拦**（哈希碰巧相等也不构成证据）；
地图身份不同同理；而 `platform_fp` 只管**浮点**可复现性，一个纯整数的仿真跨平台
确实给出同一个结果，硬拦会挡掉有用的比对，所以它只在真的对不上时才升格成成因。

**`hash_tag` 顺带答掉了在途弹丸那个悬而未决的问题。** 原先打算的兜底是
「在回放格式里给弹丸预留一组空的实体位」（照决定 ⑤ 的先例）。**不需要了**：
1c 若把弹丸做成第四组实体，`state_hash` 的喂入清单变，改这个标签即可，
失效会以「口径变了、重录」的形式报出来。预留空位反而更糟——
它是一个必须被后人维护、且永远不知道能不能删的坑。

> **它已经用过一次了，而且不是被上面预演的那种改动触发的。**
> `World/1` → `World/2`：§6 表决通过后新增 `CommandKind::Clear`，
> 而 `deferred_` 是 `std::array<..., kCommandKindCount>` 且整块进哈希，
> 于是喂入长度从 11 变 12。
>
> **教训值得单独记一句：改哈希口径的不一定是「加了一组实体」这种显眼的改动。**
> 这次只是往一个枚举末尾加了一个成员，而那个枚举恰好是一个进哈希的数组的长度。
> 下一个动枚举的人请先搜一遍 `kCommandKindCount` 有没有出现在 `state_hash()`
> 的喂入路径上。

**刻意不存 `kObsVersion` 与观测指纹**：回放里没有观测（观测是从状态派生的）。
存进去会暗示「改了观测布局则旧回放失效」，而那不成立，
于是每次动通道都要无意义地重录一批回放。

**（三）`WorldInit::units`。** 在加它之前，`WorldInit` 只能描述
「一座堡垒 + 若干墙段，零个单位」的世界，于是 `submit_actions`
（两条输入通道里字节量占九成的那一条）在任何回放里都走不到——
它要求动作数组长度恰好等于活着的单位数，而那个数恒为 0。
**这是「先定契约、再定回放」这个顺序的第一个产出。**

### 4.7 `state_hash` 漏了待排空的命令队列——写回放时补上

命令队列跨 `submit` / `advance` 边界存活，所以它是仿真状态。漏掉它，
哈希的承诺「同一个哈希 ⇒ 同一个未来」就不成立：两个队列不同的世界会算出
同一个数，然后在下一次 `advance` 分叉。

分叉最终仍会被发现（下一个哈希点），所以这不是一个「会漏掉 bug」的洞，
而是一个**会指错位置**的洞——报出来的 tick 比真正出问题的地方晚。
而回放测试唯一的定位能力恰好是「它指的那个 tick 就是第一个错的 tick」，
所以这个洞正好打在它的价值上。

长度也要一起喂：「守方一条、攻方零条」与「守方零条、攻方一条」拼接之后字节相同。

## 5. 仍然未决

### 5.1 破坏速率：**已定为乙**（随等级缩放）

**随 #23 定案，`CLAUDE.md` 已改。** 原来那处内部矛盾是这样的——战斗解算会同时消费这两句：

> 建筑与城墙走**与单位完全相同的战斗解算**
>
> ~~**破坏速率是兵种的固定属性，与等级无关。**~~

若真是同一套解算、而等级又提升伤害，破坏速率必然随等级涨。**原写法要成立，
只能在「不要做成独立的特例系统」那条明令上挖一个洞**——让建筑的承伤走一条与单位
不同的路径。所以这不是「两个都行、挑一个」，而是**原写法自身不自洽**。

| | 破坏速率 | 结构锁靠什么守 | 代价 |
|---|---|---|---|
| ~~甲~~ | 独立通道，与等级无关 | 速率恒定 | flow field 份数 = 兵种尺度的常数 |
| **乙 ✅** | 随等级，与墙的等级同乘 | **破墙时间落在一个区间内** | flow field 变成「兵种 × 等级档」 |

结构锁换成一条更锐利的不变量：**破墙时间必须落在一个区间里，不趋于 0 也不趋于 ∞。**
配套要求是城墙与城门也在同一条等级轴上（只加血量、两者同倍率）——**这两半必须成对**，
单独任何一条都比原状更糟。

**1c 要认下的那一条：flow field 从「兵种」变成「兵种 × 等级档」。**
缓解办法是把等级量化成**少数几档**破坏速率，而**档数是接口里要定的形状**
（它决定 field 的份数与内存），不能等寻路写到一半才发现。
`无尽模式与地形分层.md` §7.7.5 与 §10 第 4b 项讲了这一点。

**契约本身一个字节都不用改**，理由见下节——这也是当初判断「不阻塞契约」的那条推理，
现在被定案验证了。

### 5.1.1 定案没有触动接口，这一条当初的推理成立

`World` 里与破坏速率相关的东西只有两处，两处都与它随不随等级无关：

- 观测给的是**这个单位自己的**方向场，两条通道（`flow_di` / `flow_dj`），
  与内部维护多少份 field 无关
- `action_mask()` 只按地形与穿角判移动位；「砸墙还是绕路」是**寻路代价**的比较，
  不是掩码的事（墙是「高代价可通行」而非障碍，所以它从来不该出现在掩码里）

**换句话说，定案的全部影响落在 1c 内部，一个字节的接口都不用改。**
这条当初是作为「为什么不阻塞契约」的论证写下的；#23 定案之后它成了一个已验证的事实。

**唯一要 1c 决定的是「量化成几档」**，而那是 `Pathfinder` 的内部形状，
不在公开接口上。它值得在实现前定，因为它决定 field 的份数与内存
（`地图与场景设计.md` 第 3 节那个 36 KB / 张的估算要按档数重算）。

### 5.2 「调哪一支部队」

甲 = 加第三个动作头「编队 index」；乙 = 复用目标头，两步动作。倾向乙。
`rts/command.hpp` 对两者都成立（`MoveForce` 自带 `force` 字段 / `SelectForce`）。

**定了之后其中一条会变成死代码，该删。** 两条并存意味着同一件事有两种字节表达，
而回放里出现哪一种取决于是谁录的。

### 5.3 `kObsK`（K×K 视野边长）

现值 15，**是占位值**。定它要先标定射程，约束是 `kObsK / 2 ≥ 最大射程`：
看不见自己能打到的地方，等于把射程优势从观测里删掉。
这一项是三个数里最不容易静默出错的——K 变了张量形状就变，PyTorch 当场报错。

### 5.4 本波进度进不进观测

**刻意没加。** 它的分母是预期波长，而那是待定数值；用一个错的分母比没有这一项更坏
——前者会让 AI 学到一个基于假时间轴的策略，而且看不出来。等波长标定了再加。

### 5.5 硬要求 1 落在观测打包那一层，**那一层还没有代码**

`WorldView` 是 **god 视角**（真实世界 + 本侧迷雾），它实现的是不变量 2
（渲染层只读），**不是**硬要求 1（AI 只见自己的迷雾）。

分开是有理由的：双视图演示要同时画真实战场与 AI 的记忆图，那必然要同时读到两者；
而逐实体做迷雾过滤会把连续数组切碎、观测打包退化成 gather，正好抵掉 SoA 的全部好处。

所以硬要求 1 的落点是打包器（`bindings/` 与 1c）：**每一条空间通道都必须过
`fog()`**。`WorldView` 把迷雾摆在同一个对象上、且不提供任何别的迷雾来源，
就是为了让打包器无处可躲；除此之外它挡不住一个存心不用它的打包器。

**这是一处需要评审而非编译器把关的地方**，写在这里以免它被读成已经解决。
若打包器写出来之后发现 `FoggedView` 那一层的成本可接受，值得回来把它变成编译期的。

## 6. 红线

- **不变量 1/2/3**（CLAUDE.md「架构：三层隔离」）：tick 热路径内无渲染器 /
  引擎 / 向上回调 Python；渲染层只读；训练回路里 in-process，不走 IPC
- **确定性禁令**（`tools/check_determinism_bans.py`，已注册为 ctest，扫
  `rts_core/` 与 `game/`；`bindings/` 建起来时要加进那一行）
- **头文件自足**（`rts_core/CMakeLists.txt` 的自足性守卫，GLOB，新头自动纳入）
- **禁逐实体多态继承体系**：扁平数组 + 整型 ID。理由是确定性而非性能——
  没有裸指针可用作 key，`unordered_map<Unit*, X>` 那类 bug 就写不出来
- **凡关系到游戏平衡性与玩家体验的数值一律待定**（CLAUDE.md「关于数值」）。
  举例用的数字必须标注为占位值
