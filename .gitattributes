# 换行规范化。
#
# 起因是一个会在训练服务器上表现得毫无道理的失败：`地图与场景设计.md` 6.3 要求回放文件
# 记录地图的 `content_hash` 并在载入时校验。而仓库启用了 `core.autocrlf`，同一份地图 JSON
# 在 Windows 工作区里是 CRLF、在 Linux 上是 LF ——**字节不同，哈希也就不同**。
# 于是在 Windows 上生成并写入哈希的地图，到服务器上会因为哈希对不上而报「地图不匹配」，
# 而看到这个错误的人第一反应一定是去查仿真，不会想到是换行。
#
# CLAUDE.md 那条「回放文件在 Windows 与 Linux 之间不可复现」**不覆盖这一条**：
# 那说的是浮点，这里坏掉的是文件字节，而且它会让**地图本身**在两个平台上被判为两张不同的图。
#
# 主要手段仍是「对规范化后的内容取哈希」（见 6.3），因为那不依赖每个人的 git 配置是否正确。
# 本文件是零成本的第二道防线，顺带解决数据文件在 review 时整片显示为改动的问题。

# 数据文件：会被程序读取、会参与哈希，一律 LF。
*.json  text eol=lf
*.toml  text eol=lf
*.csv   text eol=lf

# 文档：本仓库的设计契约就是这些 .md，四个人并行改同一批文件。
#
# 这条是漏掉的，而且已经出过一次：一次用 Python 脚本批量改文档（写的时候给了
# `newline="\n"`）之后，`git add` 打出
# `warning: in the working copy of 'README.md', LF will be replaced by CRLF`，
# 而 `git ls-files --eol` 显示工作区**已经混着**了 —— 被脚本重写的那两份是 `w/lf`，
# 其余是 `w/crlf`。
#
# 危害与上面「省去无谓的 diff」那条同族但更重：**.md 是评审面最大的文件**。
# 只要两个人的 `core.autocrlf` 不一致，一次一行的改动就会显示成整份文件重写，
# 而那让 review 完全没法做 —— 在只有一个人评审的时候，等于这份改动没人看。
#
# 加它是零风险的：`git ls-files --eol` 显示索引里**全部已经是 `i/lf`**
# （`core.autocrlf=true` 提交时已经规范化过），所以这条只影响 checkout，
# 不改索引、不产生任何内容 diff。已有工作副本在下次 checkout 到该文件时自动收敛。
*.md    text eol=lf

# 构建与源码：跨两套工具链，统一 LF 省去无谓的 diff。
*.cpp   text eol=lf
*.hpp   text eol=lf
*.py    text eol=lf
*.cmake text eol=lf
CMakeLists.txt text eol=lf

# Shell 脚本：这一条不是「省去无谓的 diff」，是**不这么写就跑不了**。
# `tools/server/*.sh` 在 Windows 上编辑、到 Linux 上执行，若被 `core.autocrlf`
# 转成 CRLF，shebang 会变成 `#!/usr/bin/env bash\r`，内核把整个 `bash\r` 当程序名，
# 报 `bad interpreter: /usr/bin/env: no such file or directory`
# ——错误信息指向 `/usr/bin/env`，而那个文件明明就在，于是完全查不到真因。
*.sh    text eol=lf

# 二进制，不做任何换行处理（写出来是为了让意图明确，不靠 git 猜）。
*.png   binary
*.glb   binary
*.gltf  binary
*.blend binary
*.fbx   binary
