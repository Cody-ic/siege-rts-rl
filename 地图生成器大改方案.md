# 地图生成器大改方案（2026-09-02，提案；代码未动）

> **定位。** `tools/map_gen/` 要一次大改，动因两条：① 边长与行军距离解耦
> （`地图与场景设计.md` §3.0，已定、未落地）；② **现在的地图不体现「智斗」，
> 只是一场单纯的攻城战**——这是本文新提出的、更根本的那条。本文把两条合成一份
> 方案，附实测依据、要改的键与函数、要加的校验、以及**必须由组里拍板的决定项**。
> 与 `地图与场景设计.md` 冲突处以本文为**提案**、以那份为现行契约；通过后再合进去。
>
> 与 `攻守实力模型与平衡分析.md` 的分工：那份说「谁强」，本文说「地图让什么事
> 发生」。地图**不是**难度旋钮（该文 §4：集结点距离、城区半径、编队对陷落波零影响），
> 地图的职责是让野战、侦查、佯攻、资源争夺**真的发生**。

---

## 0. 诊断：为什么现在只是攻城战

12 张池图 + demo 攻守脚本 + 实力模型，五条事实合起来就是「一切在门前 5 格决定」：

| # | 事实（实测） | 后果 |
|---|---|---|
| 1 | 城外 `Forest`+`Rock` 合计 0.8–1.2%，争夺带（墙外 2–56 格）阻挡率 1.5–2.9% | 没有掩体、没有隘口。野战若发生也只能是平方律对拼；枪阵、巷战、伏击在城外没有触发条件；宏观层「选集结点」没有任何地形信号 |
| 2 | 城外资源全部落在墙外 1–16 格，且**逐点各占一个解禁波**（31–38 点铺到第 32–39 波）；城外产出 = 城内 | 资源争夺不发生：近到不用出城、慢到不值得规划、赚得不比城里多 |
| 3 | flow field 代价下攻方**从不砸墙**，一律走门或缺口；两座门相对、绕行只多 R 格 | 每个集结点最终都汇到同一两个入口，佯攻 30% 只是撞另一座门的塔；「分兵」没有几何上的意义 |
| 4 | 集结点在墙外 54–58 格，中间 42 格空地；波在进攻阶段开始才生成 | 侦查没有对象、也没有必经之路；`Wraith` 看见墙就撤且无人能追（速度 0.19 > `Ranger` 0.16） |
| 5 | 城内 98% 空地 | 巷战反克枪阵无触发条件（已记 `地图与场景设计.md` §2.2） |

CLAUDE.md §1 把下面这些列为**只能靠地图几何满足**的要求：防御无法覆盖所有墙段、
龟缩拐点、侦查有价值、冲锋助跑（开阔与狭窄同时存在）。现状对前三条是「几何上允许、
但数值与布局让它不发生」，对第四条是「几何上不存在」。

---

## 1. 「智斗」要落成什么几何

把 CLAUDE.md 里靠地图承载的设计要求翻译成可生成、可校验的几何：

| 设计要求 | 几何要求 | 校验形式 |
|---|---|---|
| 宏观层选集结点有信号；玩家能预判 | **每条进攻路线性质不同**：开阔 / 有隘口 / 有林子遮视野 / 过桥 / 路过资源簇，且这些性质**可量出**、不靳标签 | 逐集结点算「最窄处宽度、遮蔽比例、绕行长度、沿途资源数」，要求集结点之间至少两项有显著差异 |
| 枪阵克骑、巷战反克 | 城外存在 2–4 格宽的隘口（岩脊 / 林间 / 桥）与开阔地**同时**存在 | 每条 spawn→keep 最短路的最窄处 ∈ [2, 4] **或**标记为开阔路线；全图两类路线都有 |
| 侦查有价值、`Wraith` 可被猎 | 集结点环外侧有遮视野的林子；`Wraith` 看到「布局」（塔/防空/堡垒）前必须进入某种射程 | 第 14 条不变（禁建环 13）；新增：集结点到城墙的直线上至少一片 `Forest` ≥ 6 格 |
| 部分资源在墙外、拐点反复出现 | 资源簇按**环**分布并**逐簇**解禁：内环早、外环晚；外环在集结点环**之外** | 解禁按簇（同簇同波）；簇心到 keep 距离随解禁波单调；外环簇数 ≥ 内环簇数 |
| 攻其必救有纹理 | 每条进攻路线**路过**至少一簇（攻方顺路就能拆）；各簇混合比不同 | 簇心到某条 spawn→keep 最短路的距离 ≤ 6；三类配额轮转 |
| 防御无法覆盖所有墙段 | 正面总长 8R 与塔的弦长（2×7）之比 ≥ 4 | 第 9 条解阻塞（见 §3.7） |
| 佯攻有意义 | 入口不止两座门：门 + 缺口分布在**不同面**，绕行代价随面不同 | 门与缺口不在同一面；至少三个面各有一个入口 |

---

## 2. 骨架：四个环

```
[城区] R            8–10（§9 已定）
[内环争夺带]        R+4 … R+D−14      近簇、早解禁、隘口与林子、野战主场
[集结点环]          R+D               3–5 个点，任意角度，禁建环 13
[外环]              R+D+14 … 边缘−3   远簇、晚解禁（§3.0 的候选②）
```

边长由环相加得出：`size = 2 × (R + D + 14 + W_far + 3)`。这就是 §3.0「边长是独立的
一元」的具体写法——**D 与 W_far 才是设计量，size 是它们的函数**。

### 2.1 D（集结点到城墙）是本方案最大的待定项

§3.0 把 55 列为「不变」，但 55 是贴边时代 `144/2 − R − 2` 的遗留，从未被独立论证。
它同时管四件事，方向不一致，所以只能由组里权衡：

| D | `Ram` 行军 | `Knight` 先到 | 第 5 条占比 | 内环带深度 | 弓手调兵窗口（无集结期） | size（W_far=18） |
|---|---|---|---|---|---|---|
| 30 | 33 s | 10 s | [0.28, 0.56] | 12 格 | 10 s，跨半圈要 20–29 s ⇒ **来不及** | ≈150 |
| 40 | 44 s | 13 s | [0.37, 0.74] | 26 格 | 13 s，勉强 | ≈170 |
| 55 | 61 s | 18 s | [0.51, 1.02] | 41 格 | 18 s | ≈200 |

- 实力模型说 D 对陷落波零影响，但**弓手集中是唯一大杠杆**，而集中要在主攻到门前完成。
  没有集结期时窗口 = `Knight` 行军时间；有集结期（建造阶段就在集结点生成）时窗口 =
  13 s 建造 + 行军。**所以 D 能不能缩，取决于集结期先不先落地**（代码项，见 §5）。
- 到达离散（`Knight`/`Ghoul`/`Ram` 三批）随 D 线性放大，是「没有压迫感」的成因之一；
  这条由攻方编队（脚本或 RL 学会等最慢的）解，不该由 D 解。
- **建议**：先落地集结期，再取 D ∈ [36, 44]；集结期不落地就维持 55。

### 2.2 W_far 与外环

外环放**最晚解禁的远簇**（§3.0 候选②）。理由：到了后期玩家要扩张就得经过或绕过集结点
环，「强袭集结区通常得不偿失」本来就是既有设计，这让它成为一个真取舍而不是一句话。
风险是变成「必须打一场打不赢的仗」，缓解：外环簇不放在集结点的 ±20° 扇区内，
且远簇的解禁波晚于「玩家已有能力野战」的波段（与堡垒等级曲线一起标）。W_far 取 15–20（占位）。

---

## 3. 逐项改动

### 3.1 集结点（`place_spawns`）

- 位置：集结点环上按角度采样 3–5 个，相邻角差 ≥ 60°，不再限于四条边中点
- 不再「贴地图边」：校验器第 2 条改判「到最近墙格的距离 ∈ [D_lo, D_hi]」
  （`thresholds.json` 的 `spawn_edge_distance_max` 作废，新增 `spawn_wall_distance_range`）
- 禁建环 13 不变（第 14 条不动）
- **新增**：为每个集结点算一组路线特征并写进校验报告（不进地图文件——它们是派生量）：
  最短路长度、最窄处宽度、路上 `Forest` 遮蔽比例、是否过桥、沿途 6 格内的资源簇数

### 3.2 城外地形（`scatter_wild_terrain`）

现在是 24–36 个 3–5 格森林小撮 + 12–20 个 3–6 格岩石小撮，合计不到 1%。改成三种形态叠加：

| 形态 | 规格（占位） | 职责 |
|---|---|---|
| 小撮（现有） | 森林 3–5 格 × 20–30、岩石 3–6 格 × 10–16 | 视觉「大散居」 |
| **林子** | 12–30 格圆团 × 3–5，只放内环与外环、不贴墙、不贴集结点 | 遮视野 ⇒ 侦查有价值、伏击有位置 |
| **岩脊** | 线状 6–15 格 × 2–4，两条岩脊或岩脊+林子之间留 2–4 格的**隘口** | 野战隘口 ⇒ 枪阵 / 巷战有触发条件 |

目标阻挡率：争夺带 **10–20%**（占位；AoE4 对战图林地约 15–25%）。硬约束沿用：
不碰墙/门八邻、不碰集结点八邻、每块 4 连通、块间留一格、第 10/11/23 条照旧。

**隘口的放法不是随机的**：在算出每条 spawn→keep 最短路之后，对其中 1–2 条路线在内环
带上横放一道岩脊并留口；其余路线保持开阔。这样「哪条路窄」成为地图的一部分、
攻守双方都能读到。

### 3.3 资源簇（`place_outer_clusters`）

- 簇数：内环 6–8、外环 6–10（占位；§11.5 的 16–20 是旧货币算的，量级要重算，
  见 `攻守实力模型与平衡分析.md` §8 第 4 条）。**解除「每方位一簇」的上限**：按角度采样，
  簇心间距 ≥ 12
- **解禁按簇**：同簇同波；簇的解禁波按簇心到 keep 的距离排名给出（校验器第 18 条已允许
  同波，是生成器 `_resource_unlock_waves` 自己选了逐点严格排名）。每波开几簇是数值，
  归 `budget_curves.py` 与实力模型一起标
- 类型配额：三类**轮转**分配到簇，不再纯随机 + 强制两金（实测 11/12 张图金 ≥ 石）。
  金的比例另有一条约束：`攻守实力模型与平衡分析.md` §6.1——城内金只够不缩编，
  买活的钱要从城外金簇来，所以**内环至少一簇含金**
- 「路过」约束：每条 spawn→keep 最短路 6 格内至少有一簇（攻其必救顺路发生）；
  同时每簇到任一集结点 ≥ 16（不能在集结区里）
- 簇内点距 ≥ 2、每簇 ≥ 2 种、`_OUTER_CLUSTER_RADIUS` 4：不变

### 3.4 河与桥（`place_water`）

现在是不受管理的随机隘口：桥 1 格宽，6/12 张图有一个集结点被河切断（绕行 +7–13 格），
桥数 1–14。改成受管理的：

- 桥宽 2（`pick_bridges` 的贯穿带取两列）
- 河只允许穿过内环带或外环，不穿禁建环、不贴墙（墙外 ≥ 6 格）
- 被河切断的集结点 ≤ 1，且该路线在 §3.1 的特征里标「过桥」——这就是那条路线的「性质」
- 湖照旧（2–4 片、10–24 格）

### 3.5 城区（`place_ring_walls` / `place_inner_content`）

- R ∈ [8, 10]（§9 已定），周长 64–80，正面总长回到第 9 条区间
- 入口：一对相对城门不变；1–2 个设计缺口**放在没有门的面上**（现在是随机非门格，可能
  与门同面）⇒ 至少三个面各有一个入口，佯攻打「另一个入口」而不是「另一座门的塔」
- 城内：R 缩小后内容照旧装得下（3 塔 + 1–2 兵营 + 3 采集 + 堡垒净空 49 格 + 两片森林
  6 格，R=8 时 289 格里占约 90）。巷战候选仍是 §2.2 的 2+3，本文不加城内阻挡物
- 预置塔 2–3 贴门/缺口不变。**注意**实力模型说门前的塔数决定一切，所以预置塔的数量
  是难度量、不是装饰，动它要连着实力模型看

### 3.6 可破坏障碍（`place_obstacles`）

数量 20–36、只在城外、小撮分组：不变。一次性存量约 600 木 + 300 石，量级合理。
只加一条：不放在隘口里（否则清野等于拆掉地图给的隘口——那也许反而是好设计，**待定**）。

### 3.7 校验器

| 条目 | 改法 |
|---|---|
| 第 1 条 | 文案改：边长由四个环相加得出，不再「由城区半径 + 集结点到城墙推出」 |
| 第 2 条 | 判据改：集结点到最近墙格的距离 ∈ `spawn_wall_distance_range` |
| 第 9 条 | **解阻塞**：城圈已是整环墙实体，正面总长 = 环格数 = 8R，不再需要城区推导；阈值 [64, 80] |
| 新 26 | 争夺带阻挡率 ∈ [lo, hi] |
| 新 27 | 每条 spawn→keep 最短路的最窄处 ∈ [2, 4] 或标记开阔；全图两类路线都有 |
| 新 28 | 解禁按簇：同簇的 `unlock_wave` 相同；簇解禁序按簇心距离单调（第 18 条的逐点版保留） |
| 新 29 | 类型配额：城外三类簇数两两之差 ≤ 1；内环至少一簇含金 |
| 新 30 | 桥宽 ≥ 2；被河切断的集结点 ≤ 1 |
| 新 31 | 「路过」：每条最短路 6 格内至少一簇；每簇到任一集结点 ≥ 16 |
| 新 32 | 入口分布：门与缺口不同面，≥ 3 个面有入口 |

`selftest.py` 对应加 doc 层断言（簇解禁同波、配额、桥宽、隘口存在）。
**`--preview` 仍是「合法但无趣」的唯一防线**，大改后前 20 张要眼看。

### 3.8 `thresholds.json` 键的变更

| 键 | 变更 |
|---|---|
| `generator.size` | 由 `city_radius_range` / `spawn_wall_distance_range` / `outer_band_width` 推导，不再手写；`profiles.*.size_max` 随之 |
| `generator.city_radius_range` | [12,15] → [8,10] |
| `profiles.*.spawn_edge_distance_max` | 作废 → `spawn_wall_distance_range` |
| `generator.spawn_count_range` | [3,4] → [3,5]，角度采样 |
| `generator.outer_clusters_range` / `outer_cluster_span` | 拆成 `inner_band_clusters` / `outer_band_clusters` / `outer_band_width` |
| 新 `wild_blocked_fraction_range`、`woods_range`、`woods_size`、`ridges_range`、`ridge_length`、`gap_width_range` | §3.2 |
| 新 `bridge_width`、`max_river_cut_spawns` | §3.4 |
| 新 `cluster_unlock_per_wave` | §3.3，归口在这里但**取值与实力模型一起标** |

### 3.9 产物

12 张池图重生成（种子段不变）、`build_reference_map.py` 跟进（56 格参考图要重新装：
R=8、D 按 §2.1 定、可能放不下外环——参考图可以没有外环，注明）、`README.md` 状态表、
`地图与场景设计.md` §2.2 / §3.0 / §9 合入本文结论、`CLAUDE.md`「集结区」「资源分布形态」两处。

---

## 4. 地图改完之后，哪些事仍然不会发生（要配套的代码项）

大改只让「事情能发生」。下面四条不在生成器里，缺了它们地图白改
（四条均已落地：前三条 2026-09-02 于 `demo_driver.cpp` / `stats_placeholder.json`，
第四条同日于 `rts_core` 契约面）：

| 项 | 归口 | 为什么地图救不了 |
|---|---|---|
| **集结期**：建造阶段开始即在集结点生成、待命到进攻 ✅已落地 | `demo_driver.cpp` | 没有它侦查没有对象、D 不能缩（§2.1）、HUD「攻方 N」建造阶段恒 0 |
| `Wraith` / `Scout` 速度压到 0.13、`Wraith` 侦查判据改为看见塔/防空/堡垒 ✅已落地（实测取 0.12/0.13，0.13 会与 `Scout` 同速追不上） | `stats_placeholder.json`、`demo_driver.cpp` | 林子挡视野只是让它非走近不可，能不能被追上是速度序 |
| 攻方脚本按最慢单位编队推进 ✅已落地（步速线取最慢兵种中**最靠前**者，slack 3 格） | `demo_driver.cpp`（RL 阶段应涌现） | 到达离散随 D 线性放大，地图只能减轻不能消除 |
| 城外产出倍率（`tier` 进产出） ✅已落地（2026-09-02：`WorldInit::tier_income_permille`，Inner=1000 / Outer=1500 占位值） | 地图格式 + `World`（契约面） | 外环簇再远，赚得和城里一样就没人去（`数值设计` §11.2c） |

---

## 5. 决定项（组里拍板，本文不猜）

1. **D 取多少、集结期先不先落地**（§2.1，两者绑在一起）
2. **外环放远簇（候选②）还是留白（候选①）**（§2.2）
3. 争夺带阻挡率目标（10–20% 是类比 AoE4 的占位）
4. 隘口是「设计放置」（§3.2 的放法）还是纯随机后由校验器筛
5. 桥宽 2 与「≤ 1 个集结点被切」——还是干脆让河不切任何集结点
6. 缺口放在无门面（§3.5）——它把入口从 2 变 3–4，实力模型里塔要跟着分散，是难度量
7. 可破坏障碍能不能放在隘口里（§3.6）

---

## 6. 与实力模型的关系（防止把本文读成平衡修法）

- 实力模型（`攻守实力模型与平衡分析.md` §4）：D、R、编队对陷落波零影响。**本文不改变
  谁强**，它改变的是野战、侦查、佯攻、资源争夺**发生不发生**。
- 但有三处地图量是实力量，动它们要连着模型看：门前预置塔数（§3.5）、入口个数（§3.5 第 2 条：
  塔要分散到更多入口）、内环带里资源簇的数量（§6.1 的金币冗余与石材约束）。
- 野战一旦真的发生，实力模型要补一段「野战 regime」（现在只建了入口），那是模型的下一步，
  不是本文的。

---

## 7. 落地顺序与验收

1. `thresholds.json` 键变更 + `place_spawns` 改环采样 + 第 2 条改判据（§3.1）——先让「解耦」落地
2. 资源簇按环、按簇解禁、配额、路过约束（§3.3）+ 新 28/29/31 条
3. 地形三形态 + 隘口放置（§3.2）+ 新 26/27 条
4. 河桥受管理（§3.4）+ 新 30 条
5. 城区 R、缺口异面（§3.5）+ 第 9 条解阻塞 + 新 32 条
6. 重生成池图、参考图、`--preview` 眼看 20 张、selftest 断言
7. 配套代码项（§4）各自的 PR

验收判据不是「校验器全绿」——那只证明合法。是：`-n 30` 的路线特征报告里，
集结点之间至少两项特征有显著差异（§1 第一行），且两类路线（开阔 / 隘口）都出现。
