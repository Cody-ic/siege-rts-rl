# 演示与可视化清单

> **状态：清单已定（#26 议题 A8）。** 第 9 节问 1 已答，问 2–5 仍待定。
>
> **进演示的只有第 1、2 项。第 3 项（显著性热力图）与第 4 项（非传递性环）
> 改为只用于分析与报告，不上台**——判据是第 0 节那三条里的第一条
> 「不需要解释术语就能看懂」，逐项理由写在各节开头。
>
> **注意「不进演示」不等于「不做」**：第 3 项兼任 RL 调试工具、第 4 项是
> `CLAUDE.md`「评估指标」一节的第一条，两者都落在 `tools/`、不占前端预算。
> 第 3 项最终做到什么程度**留待评估**。
>
> 本文有两个读者：
> **答辩**——它是演示材料的清单与优先级；
> **`render/` 的开发者**（现为 @zhxxx233，见 #40 / #42 / #43）——
> 它是前端的需求来源之一，直接影响工作量估算（见第 8 节）。

## 0. 为什么需要这份清单

老师明确鼓励使用 AI，那么"体现 AI"就不只是"用了 RL"这句话，而要**让人看见**。
但可视化很容易做成一堆炫技面板，因此先定判据。

**一个演示点值得做，当且仅当它同时满足：**

1. **不需要解释术语就能看懂。** 观众不该先学会什么是策略网络才能理解你在展示什么。
2. **它证明的是我们宣称的东西**，而不只是好看。最好能把某个原本主观的评估指标
   变成可展示的客观证据。
3. **成本主要落在"本来就要做的东西"上。** `CLAUDE.md` 已经要求前端兼任 RL 调试工具
   （「调策略时必须可视化观测张量、奖励信号与目标选择」），大部分演示点是它的自然延伸。

第 5 节列了几个**不做**的，理由同样按这三条。

## 1. 双视图：真实战场 vs AI 以为的战场

**优先级最高。** 它是全清单里冲击力与成本比最好的一个。

### 做什么

左右分屏（或叠加切换）：左边是真实战场，右边是**攻方 AI 的侦查记忆图**——
它去过、看过、还记得的东西。没侦查过的区域是空的，侦查过但已过时的区域
显示的是它**记忆中的**样子，而不是现在的样子。

### 为什么冲击力强

它能讲一个观众自己就能跟上的故事：

> 你等 `Wraith` 走了之后才造那三座防空塔。右边这张图上，那片区域还是空的——
> AI 不知道。接着不死鸟飞进来，被打下来了。

一句话同时讲清了三件事：战争迷雾对 AI 对称生效、玩家可以**主动欺骗** AI、
「建造时机是一项决策」不是空话。而这三条都是 `CLAUDE.md` 里已经写死的设计，
此前只存在于文档中，没有任何可展示的形态。

### 成本几乎为零，因为那份数据本来就必须存在

`CLAUDE.md` 的 RL 硬要求第 1 条：「AI 的观测必须是它自己的迷雾状态，
不得是 ground truth」；侦查系统一节又写「波次结构使 AI 的记忆天然过时」。
**这两条合起来要求一个阵营级的侦查记忆图必须已经存在于 `rts_core` 里**，
否则"记忆过时"无从谈起。

所以这个演示点不需要新建任何仿真数据，只需要把已有的那一层画出来。

### 技术要点

- 画的是**阵营级的侦查记忆图**，不是单个单位的 K×K 局部观测。两者不要混淆：
  前者是全局的、跨 tick 累积的；后者是喂给网络的、逐单位逐步的。
- 渲染层只读该层，不写。符合不变量 2。
- 守方 AI 同样有自己的记忆图（`守方AI与协同演化.md` 第 4 节），
  因此 `AI vs AI` 模式下可以做**三视图**：真实 / 攻方所见 / 守方所见。

### 验收

两条，**第二条是第 3 项不上台之后落到这里的**：

1. **欺骗**：玩家在 `Wraith` 离开后建防空，右侧视图不更新，随后 `Phoenix` 进场被击落。
2. **缺口利用**：AI 的记忆图里某段墙是破的，随后它调头去打那里——
   这是 `CLAUDE.md` 头号评估指标「AI 是否发现并利用已有缺口」的**现场**证据。
   同一条指标的**纸面**证据由第 3 项（显著性热力图）给，两者互补：
   台上讲这一条（不需要解释任何术语），报告里放那一张（更硬但要解释）。

第 2 条建议用确定性回放固定成标准片段（见第 9 节问 4）。

## 2. `AI vs AI` 对战模式

### 做什么

攻守双方均由训练好的策略控制，玩家只观战。可调速、可暂停、可切换上面的视图。

### 为什么冲击力强

- 观众能**同时**看到两侧的涌现行为。人类游玩时只能看到攻方那一半。
- 它是"守方也是 RL"这个决定最直接的可见产物。
- 配套的最强演示其实是这一句：**让老师亲自玩一局，再看 AI 守方打同一波打得比他好。**

### 成本

`守方AI与协同演化.md` 落地后基本白拿——需要的只是一个"两侧都接策略"的入口，
以及观战相机与速度控制。真正的成本在守方 AI 本身，而那是另一份文档的事。

### 验收

同一张地图、同一个种子，能分别录下「人类守 vs AI 攻」与「AI 守 vs AI 攻」两段，
放在一起对比。

## 3. 显著性热力图：AI 在看哪里 —— **做，但只用于分析，不进演示**

> **结论（#26 议题 A8）：不作为演示点，但作为分析工具保留。**
> 与第 4 项同一处理，理由也同一条（第 0 节判据 1）。
> **最终做到什么程度留待评估**——先有一版能出图的离线脚本，
> 再看它在调试与举证上实际有多少用，那时才谈要不要往前端做。

### 做什么

选中一个单位，把它的 K×K 观测按区域逐块屏蔽，每屏蔽一次重跑一次推理，
记录动作分布的变化量，画成热力图叠在它周围的战场上。
用遮挡法而非梯度，因此**不需要反向传播、不需要在 C++ 推理器里实现求导**。

### 为什么不上台

判据 1 是「**不需要解释术语就能看懂**」。而这张图要先讲清什么是遮挡法、
什么叫动作分布的变化量、为什么颜色深就代表"在看"，观众才跟得上。
它是全清单里理解成本最高的一项。

原稿建议三项全做，**这是原稿内部的不一致**——它给了判据，却没有拿判据筛自己的清单。

### 为什么仍然要做：它有两个与演示无关的用途

**一、它是 `CLAUDE.md` 头号评估指标的硬证据。** 那条指标是
「**AI 是否发现并利用已有缺口**」，而对标产品 *Diplomacy is Not an Option*
正是被评测批评「敌人无视已有缺口、反复撞击加固过的城墙」——这条要在报告里正面回应。
不做的话只剩「看录像觉得像」。

**二、它是 RL 调试工具，而这本来就是既定需求。** `CLAUDE.md` 已经要求
「前端兼任 RL 调试工具——调策略时必须可视化观测张量、奖励信号与目标选择」。
当 AI 行为古怪时（该打缺口却去撞完好的墙），这张图直接告诉你它在关注什么，
否则只能猜。**这个用途在训练期就要用上，比答辩早得多。**

### 「只用于分析」把成本降了一个量级

这是本次改判最实际的一处：**它不再是前端需求。**

| | 展示版（原方案） | 分析版（现在） |
|---|---|---|
| 落在哪 | `render/`：暂停态按需推理、热力图图层、图例 | **`tools/`：离线 Python 脚本** |
| 输入 | 实时游戏状态 | **回放文件 + 策略权重** |
| 输出 | 叠在战场上的实时图层 | 一张 PNG |
| 占前端预算 | 是 | **否** |

于是它与第 4、5 项同族——都是 `tools/` 的绘图脚本，都不占 `render/` 的 5–8 人日。
而**回放本来就是硬性要求**（确定性回放是核心测试形态），所以输入是现成的。

### 与第 1 项的关系：互补，不是替代

第 1 项（双视图）**仍然升为不可砍**，但理由要改准确一点——不是「它接手了第 3 项」，
而是两者在不同场合各自举证：

| | 第 1 项 双视图 | 第 3 项 显著性图 |
|---|---|---|
| 场合 | **台上**（现场演示） | **纸面**（报告与答辩问答） |
| 强度 | 直观，观众自己跟得上 | 更硬，但要解释 |
| 讲法 | AI 记忆图里那段墙是破的 → 它调头去打那里 | 这一帧它的注意力确实压在残墙上 |

第 1 项的验收条件仍应补上那条：
**能演示一次「AI 记忆图里存在缺口 → 它去打了那个缺口」**，
因为台上只有它。

### 一个附带用途：检出脚本守方被找到的漏洞

`守方AI与协同演化.md` 2.5 末尾承认了脚本执行层的残留劣势——它只会我们写进去的
那几种行为。**攻方 RL 若找到某条脚本行为的破绽并反复利用，这张图是发现它的手段之一**：
注意力持续压在一个我们没预期的地方，通常就是那里有可利用的规律。

这类发现本身是答辩素材（见第 6 节的说明），所以这条不只是排查手段。

## 4. 策略演化时间线与非传递性环 —— **分析照做，但不上台**

> **改用途，不是砍掉**（#26 议题 A8）。这个区分必须写清楚，
> 否则按「砍掉第 4 项」的字面执行会误删一条**已定的核心论证材料**。

### 做什么

把训练过程中各代策略两两对战的胜率矩阵画出来，并高亮其中的非传递性环
（A 克 B、B 克 C、C 克 A）。

### 为什么它不能砍

`CLAUDE.md`「评估指标」一节的**第一条**就是它：**出现非传递性环，就是"策略空间
具备深度"的实验证据**，直接回应"这游戏是否足够偏向智斗"这一立项问题。
那是已生效的设计契约，不是本文提出的可选项。

因守方也是 RL、双方协同演化，这张矩阵从"攻方策略之间"扩展为"攻守双方各代之间"，
证据强度显著提升——单一守方下测出的环，有可能只是那个守方的产物。

### 为什么它不上台

同样是第 0 节判据 1：一张 N×N 的胜率矩阵要先解释什么是非传递性环、
为什么它证明策略空间有深度，观众才跟得上。**现场讲不清，纸面上讲得清。**

于是它的归属改为：

| 用途 | 做不做 |
|---|:-:|
| 系统设计报告的评估章节 | **做**（它是 `CLAUDE.md` 要求的） |
| 答辩问答的备用材料 | **做** |
| 演示流程里的展示环节 | **不做** |

### 成本（不占前端预算）

对手池本来就要维护快照（`守方AI与协同演化.md` 第 5 节），两两对战是 headless 批量跑，
`BatchedEnv` 现成。主要成本是机时，不是代码。**属 `tools/` 的 Python 绘图脚本，
与 `render/` 无关**——这也是它与第 3 项处境不同的原因之一：
砍第 3 项省下的是前端人日，砍第 4 项省不下任何东西。

## 5. 元控制器的后验收敛 —— **保留，而且它比第 4 项更适合上台**

### 做什么

一张随波次更新的图：AI 对"玩家是什么风格"的后验分布。
看着它从均匀分布逐渐收敛到"这人是龟缩流"。

### 为什么

`CLAUDE.md` 已经规定了实现方式（离线训练 3–4 个风格化策略 + 贝叶斯更新或 UCB
在策略间切换），所以成本几乎为零。它的价值是把"AI 适应玩家风格"这句抽象的宣传语
**变成一条看得见的曲线**，而且玩家自己能感觉到策略切换的时点与图上的拐点对得上。

**按第 0 节判据 1 衡量，它明显强过第 4 项**：一条逐渐收敛的曲线不需要解释术语
——「它一开始不知道你是什么打法，打了几波之后认出来了」，一句话讲完。
而同为 `tools/` 绘图、同样几乎零成本。

所以若演示流程里还要再放一张图，**应该是这张，不是第 4 项那张矩阵**。

建议实现用 Thompson sampling 而非纯 UCB——它天然维护后验，正好就是要画的东西。

## 6. 两个工程亮点（不是可视化，但属于"体现 AI"）

### 6.1 在 C++ 侧手写神经网络推理

`交付要求的工程影响.md` 第 1 节已把它列为三种部署方案之一。建议**选它**：

- **这是纯 C++ 工作**，正好是这门课要评的东西。对一门 C++ 课程设计，
  "我们自己实现了卷积与前向传播"比"我们调用了 PyTorch"有说服力得多。
- 顺手消灭"推理时要不要随包分发 Python 运行时"这个待定风险项。
  而守方也上 RL 之后，运行时要跑两个策略网络，这个风险的代价翻倍。
- 规模很小：共享策略网络 + 局部观测 CNN，不用 Transformer/LSTM
  （`交付要求的工程影响.md` 已论证本项目规模不需要）。
- 配套测试是现成的好设计：**固定输入下 C++ 输出 vs PyTorch 输出逐位比对**，
  层序、padding、归一化任一处不符都会被逮住。这条已经写在那份文档里了。

### 6.2 用黑箱优化标定数值

把"人工试凑数值"换成 CMA-ES 或贝叶斯优化：把平衡目标写成目标函数
（克制单位对其目标的资源效率落在区间内、无单位在所有对局中占优、
克制矩阵存在非传递环），让优化器在 headless 批量仿真上搜。

**这条在关键路径上**——数值不做也得标定，用优化器反而更快，
还能发现"这组数值下某单位完全没用"这类人工很难察觉的问题。

与协同演化互补：优化器在 1v1 成本产出矩阵上搜候选，
协同演化在完整博弈的均衡下验收（`守方AI与协同演化.md` 第 8 节）。

## 7. 明确不做的

| 想法 | 为什么不做 |
|---|---|
| LLM 生成战报解说 | 答辩现场要联网 + API key，炸了没有退路；且不是 C++，属于"贴上去"而非"长出来" |
| 自然语言指挥单位 | 给游玩回路引入外部依赖，同上的现场风险，收益是噱头 |
| 宏观层用 MCTS | 有了守方模型技术上可行（可用自家仿真器做 rollout），但宏观层本就是 bandit 尺度，相对 Thompson sampling 的收益不明显。列为"有余力再说" |
| 模仿学习攻方 | 人类不玩攻方，没有可克隆的数据。守方的行为克隆则可行，见 `守方AI与协同演化.md` 第 7 节 |
| 把开发流程本身当演示点 | 四人 × Claude Code + `CLAUDE.md` 作为共享契约确实是真实方法论，但**不直观、上台不出彩**。适合写进系统设计报告的方法论一节，不适合放进演示 |

## 8. 对 `render/` 的工作量影响

`CLAUDE.md` 给 `render/` 的预算是 **5–8 人日**，那是按"框选、编队、建造放置与预览、
HUD、相机控制、血条"估的。本文的演示点是**增量**，需要重新估：

| 项 | 前端增量内容 | 占前端预算 |
|---|---|:-:|
| 双视图 | 分屏 / 切换、记忆图图层、过时区域的视觉区分 | **是** |
| `AI vs AI` | 观战相机、速度控制、双方策略接入的入口 | **是** |
| 显著性 | ~~暂停态的按需推理调用、热力图图层与图例~~ | **否** —— 改为 `tools/` 离线脚本（第 3 节） |

**显著性改为「只用于分析」之后，前端预算的压力明显下降**——它本来是三项里唯一
需要在前端调用推理器的，另两项都只是「把已有数据画出来」。
注意这是改判的**附带好处，不是理由**（理由是第 0 节判据 1）。

第 3、4、5 项现在都落在 `tools/`（Python 绘图 + 离线推理），不占前端预算。
第 6 节两项属 `bindings/` 与 `train/`。

**仍建议重估前端预算**，而不是默认 5–8 人日还能装下剩下这两项。
`render/` 现由 @zhxxx233 认领（#40 / #42 / #43 已落地世界渲染、精灵图集、
等距投影、相机、中文文字与格点选），**重估应由他给基线**——见 #26 议题 R。

## 9. 需要拍板的问题

1. ~~**第 1、2、3 项做几个？**~~ **已定（#26 议题 A8）：演示只做第 1、2 项。**
   结论与原稿的倾向（三个都做）不同，判据是第 0 节自己那条
   「不需要解释术语就能看懂」——原稿给了判据却没有拿它筛自己的清单。

   **但「不上台」不等于「不做」，第 3、4 项都是改用途**：

   | 项 | 演示 | 分析 / 报告 | 落在哪 |
   |---|:-:|:-:|---|
   | 1 双视图 | ✅ **不可砍** | ✅ | `render/` |
   | 2 `AI vs AI` | ✅ | —— | `render/` |
   | 3 显著性 | ❌ | ✅ **兼 RL 调试** | `tools/`（离线） |
   | 4 非传递性环 | ❌ | ✅ `CLAUDE.md` 已定的指标 | `tools/` |
   | 5 元控制器后验 | 有余力可上 | ✅ | `tools/` |

   **第 3 项做到什么程度留待评估**：先出一版离线脚本，看它在调试与举证上实际
   有多少用，那时才谈要不要往前端做。第 1 项的验收条件加了一条（缺口利用）。
2. **`AI vs AI` 是否算进交付的正式功能**（写进可执行程序的菜单），
   还是只做成一个答辩用的启动参数。这影响 UI 工作量。
3. **前端预算重估到多少人日**，以及是否需要第二个人分担 `render/`。

   这一条原先卡在「必须由认领 `render/` 的人在场才谈」。**认领人已就位**
   （@zhxxx233，#40 / #42 / #43），所以基线在这里给出。**下面全部是排期估计，
   不是「关于数值」那条规则管的平衡数值。**

   现有 `game/` + `render/` 约 2700 行，实测约 **2.5–3 人日**。对照
   `CLAUDE.md` 给 5–8 人日列的那张清单：

   | 项 | 状态 |
   |---|---|
   | 相机控制、格点选（框选的前置原语） | ✅ |
   | 世界渲染 / 精灵图集 / 等距投影 / 中文文字 / 截图模式 | ✅（清单里没列，但都是前置） |
   | **实体绘制、血条** | ❌ 不再被接口卡住——`WorldView` 给的是连续数组 |
   | **框选、编队** | ❌ |
   | **建造放置与预览** | ❌ |
   | **HUD**（要引 ImGui + rlImGui） | ❌ |

   **诚实的读法：基础设施那一半做完了，交互那一半一件没做，而剩下的清单比
   5–8 里剩下的那 2–5 人日大。** 到「一个可玩的守方前端」估计还要 5–6 人日，
   总计 **8–9 人日**，已越出原预算。

   **演示两项另计。** 双视图的成本分布容易估错——**数据侧几乎免费**
   （`FogLayer` 本来就必须存在，RL 硬要求第 1 条；`WorldView` 已经把它摆在
   同一个对象上），**贵在版面**：两个视口、相机同步、以及「让观众一眼看出
   两张图哪里不一样」这件事本身。估 **1.5–2 人日**。`AI vs AI` 若只做启动
   参数（问 2 的倾向）约 **0.5 人日**，进菜单要再加。

   **所以这一条的产出不是一个数，是一次取舍。** 三个具体主张：

   - 总预算改到 **10–12 人日**，并接受它是全项目单模块最大的一块
   - **或者**砍「编队」（用「全选 + 点选」替代，代价是操作变笨，但守方兵种
     只有 5 种、单波单位数有硬上限）与「建造预览」（退化为点了就建 + 可撤销），
     省约 1.5–2 人日
   - **不砍双视图**，理由与 #46 一致：它是「AI 是否发现并利用已有缺口」这条
     核心指标的**现场**证据

   是否要第二个人：倾向**不分人，但分时**——`render/` 的剩余工作与 1c 完全
   解耦，可以在等 1c 的间隙推进。

   **连带影响问 2**：预算已经超了，所以「`AI vs AI` 进菜单」要花的那份 UI
   预算现在有了明确的机会成本。这不等于替问 2 做决定，但它把问 2 从
   「要不要顺手做」变成「拿它换掉上面哪一项」。
4. **答辩演示的标准片段用哪几个**，以及是否全部用确定性回放固定下来
   （强烈建议——现场实时跑有翻车风险，而回放是我们本来就有的能力）。

   **「本来就有的能力」现在是实物了**：回放格式、录制器与比对已落地
   （`rts/replay.hpp`）。所以这一条剩下的只有「用哪几个片段」，
   「能不能用回放固定」不再是需要论证的部分。
5. 6.1（C++ 手写推理）**是否就此定为部署方案**，从而关闭
   `交付要求的工程影响.md` 第 1 节那个待定项。

   **这一条已经有排期**：`CLAUDE.md`「项目性质与硬约束」把「推理时是否随包
   分发 Python 运行时」定在**第 3 周**再决定。所以它不是「悬着」，是「还没到
   时候」——不要在此之前反复拿出来议。
