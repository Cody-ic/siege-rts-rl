# 训练服务器环境

> **本文记录的是 2026-08-28 实测的结果，不是申请时的标称配置。**
>
> 每一条都是登上去跑命令得到的。若与你看到的不符，以你当场实测为准并回来改本文——
> 这台机器是**共享**的，别人装了什么、占了多少，都会变。
>
> **本文不含任何口令。** 认证一律走密钥，理由见第 1 节。

## 0. 一句话结论

**硬件对本项目是过剩的，缺的是软件环境。** GCC / CMake / CUDA 驱动齐备，
**`rts_core` 已在这台机器上编译并测试通过**（Release 与 Debug 双配置、7/7、零警告，
见第 5 节）；但 **`train/` 那一侧几乎是空的**：没有 pip、没有 PyTorch、没有 conda。

另外两条会绊人、写在前面免得翻到后面才看到：**没有 `tmux`，只有 `screen`**；
**到 `github.com` 的连通性是间歇性的**（FetchContent 因此卡过一次，已处置）。

## 1. 怎么连

```bash
ssh yys@10.10.60.102 -p 2222
```

**需要在校园网内**；在校外要先挂东南 VPN。

### 主机密钥在 8-27 到 8-28 之间变过一次

| | ED25519 指纹 |
|---|---|
| 8-27 首次连接记录的 | `SHA256:eIPnLRHQW9AX8m1DEc36PrAVY6zUT9L4ZYSLI5OKGIs` |
| 8-28 起（**当前**） | `SHA256:BNofiT8U6litnKom87md1uuHL4duNh3gjAj/ktNKOnc` |
| 同期 RSA | `SHA256:zJ+HAj8thxRzA+OpImkvPhjmR15ZgYk7sMnnHNuPXX0` |

原因是机器重装 / 重建（已向机器所有者确认）。**指纹记在这里是为了让下一个人能核对**：
若你连上去看到的不是上表第二行，**不要直接按 yes**——那意味着密钥又变了一次，
要么是又重装了，要么 2222 这个端口转发到了别的机器。先问清楚再连，
否则你的口令会发给一台你没确认过的主机。

### 用密钥，不要用口令

```bash
ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_gpu -C "你的名字-gpu"
ssh-copy-id -i ~/.ssh/id_ed25519_gpu.pub -p 2222 yys@10.10.60.102   # 只此一次需要口令
ssh -i ~/.ssh/id_ed25519_gpu -p 2222 yys@10.10.60.102
```

写进 `~/.ssh/config` 之后可以直接 `ssh gpu`：

```
Host gpu
    HostName 10.10.60.102
    Port 2222
    User yys
    IdentityFile ~/.ssh/id_ed25519_gpu
```

**注意四个人共用同一个 `yys` 账号。** 这有两个后果，都要当心：

- **家目录是共享的。** 各自的工作放进 `~/<你的名字>/`，不要直接堆在 `~` 下，
  否则互相覆盖只是时间问题。
- **谁跑的任务从进程上分不出来。** 跑长任务前在群里说一声，
  并用 `tmux new -s <你的名字>-<任务>` 命名会话，别人 `tmux ls` 时能看出是谁的。

## 2. 硬件（实测）

| 项 | 实测值 |
|---|---|
| 主机名 / 系统 | `amax` / Ubuntu 24.04 LTS，内核 6.8.0-31 |
| 形态 | **裸机或 VM，不是容器**（无 `/.dockerenv`） |
| GPU | **1 × NVIDIA A800 80GB PCIe** |
| 显存 | 81920 MiB，实测占用 14 MiB（**当时完全空闲**） |
| 驱动 / 算力 | 580.173.02 / compute capability **8.0**（Ampere） |
| CPU | 2 × Intel Xeon Gold 6542Y，24 核/路 × 2 线程 = **96 线程** |
| 内存 | 503 GiB 总量，**285 GiB 可用**（218 GiB 已被他人占用） |
| 磁盘 | `/dev/nvme0n1p1` 6.9 TB，**6.2 TB 空闲**，NVMe |
| 家目录用量 | 296 KB（基本是空的） |

### 这些数字对本项目意味着什么

**瓶颈是 CPU，不是 GPU——「GPU 机时够不够」这个问题问错了方向。**

`CLAUDE.md` 的 RL 设计里，策略网络是**共享参数 + K×K 局部视野张量**，
单波单位数硬上限 20–40。这样一个网络在 A800 80GB 上小到可以忽略，
显存和算力都远远过剩。真正吃资源的是 **`BatchedEnv` 用线程池并行 step N 局仿真**，
那是**纯 CPU** 的工作（不变量 1 明确要求 `rts_core` 完全 headless、热路径不碰 GPU）。

所以：

- **96 线程是这台机器给本项目最重要的资源。** 并行局数的上限由它决定，
  而并行局数直接决定训练吞吐。
- **单卡意味着不能同时跑多组实验。** 这条对 #15（守方也上 RL、攻守协同演化）
  影响很大——协同演化通常要维护对手池并并行评估，单卡下只能串行排队。
  #15 第 6 个待拍板问题「GPU 机时够不够」应当据此重新表述为
  **「单卡串行排队下，协同演化的实验周期是多久」**。
- **共享机器，503 GiB 里已有 218 GiB 被别人占着。** 上表的「空闲」是某一时刻的快照，
  排期时不要按独占算。

**GPU 空闲不等于 GPU 归你。** 跑之前先 `nvidia-smi` 看一眼有没有别人的任务。

## 3. 软件（实测）

### 有的

| 工具 | 版本 | 备注 |
|---|---|---|
| gcc / g++ | **13.3.0** | 另有 10.x 可选（`/usr/bin/g++-10`） |
| cmake | **3.28.3** | ≥ 3.20（本项目最低要求）✅，且 ≥ 3.24 故 `CMP0135` 生效 ✅ |
| make | 4.3 | |
| git | 2.43.0 | |
| python3 | **3.12.3** | |
| CUDA toolkit | **12.8**（`/usr/local/cuda`） | `nvcc` **不在 PATH**，要用得加 `/usr/local/cuda/bin` |
| python3 | 见上 | ⚠️ **它是构建的硬依赖，不是可选项**：`RTS_REQUIRE_DETERMINISM_GUARD` 默认 ON，CMake 找不到 `python3` 会**直接让配置失败**（#24 起）。服务器上它在位，所以不挡路；但下一台机器上看到那个 `FATAL_ERROR` 的人不该以为是配置写坏了 |
| 外网 | pypi.org ✅ codeload.github.com ✅ **github.com ⚠️ 间歇** | 见下 |

#### `github.com` 是间歇性可达的，这条本文初稿写错了

初稿写「github.com ✅，FetchContent 拉 Catch2 没问题」。**首次 GCC 构建时它把 configure 卡住了**，
当场逐层测的结果：

| | 结果 |
|---|---|
| DNS 解析 `github.com` | 正常（`20.205.243.166`） |
| `github.com:443` TCP | **不可达**，连续三次 curl 全失败 |
| `codeload.github.com:443` | 通（301，0.49 s） |
| `objects.githubusercontent.com:443` | 通 |
| `pypi.org` | 通（200，1.18 s） |

约十分钟后再测，`github.com` **又通了**（403，0.30 s——无 UA 的 HEAD 本就是这个码）。
所以它不是被封，是**时通时不通**，而且不通的时候只有 `github.com` 这一个主机名。

**本文初稿的那个 ✅ 不可信，因为当时用的是 `curl -sI`——`-s` 会把连接错误一并吞掉。**
这正是本项目反复在处理的那类问题：一个可能静默通过的检查，比没有检查更坏。
现在这张表的每一行都是用 `curl -sS`（保留错误）加显式 `timeout` 重测的。

**对构建的影响与已做的处置**：`tests/CMakeLists.txt` 的 Catch2 URL 以 `github.com` 开头，
不通时 configure 会先等一个连接超时。黑洞地址实测：不给 `TIMEOUT` 要等 **133 秒**，
给 `TIMEOUT 20` 则 **23 秒**完成。因此该文件已加一个 `codeload` 兜底 URL 与 `TIMEOUT 20`
（兜底那份归档与 `github.com` 重定向过去的**逐字节相同**，已用现成的 `URL_HASH` 验过，
所以兜底不靠信任域名）。网络正常的机器上这两处都不生效。

更省事的做法是给 `~/shared/deps/` 放一份 Catch2 源码，`tools/server/build.sh` 会自动用它、
完全不走网络。

### 没有的

| 缺什么 | 影响 |
|---|---|
| **pip** | `python3 -m pip` 报 `No module named pip`。`train/` 那侧一切都卡在这 |
| **PyTorch** | 训练主依赖，需自行安装（选 cu12x 的 wheel 以匹配 CUDA 12.8） |
| **conda / mamba** | 无任何 conda 发行版 |
| **ninja** | 不影响，用默认的 Unix Makefiles 即可 |
| **clang++** | 不影响，本项目只要求 MSVC + GCC 两条路径 |
| **`python` 这个名字** | 只有 `python3`。写脚本与文档时不要写 `python xxx.py` |

### 两个已知会绊人的点

**`python` 不存在，只有 `python3`。** `tools/` 下的脚本与 `README` 里的命令
若写成 `python`，在这台机器上会直接 `command not found`。
好消息是 CMake 那侧没问题：`tests/CMakeLists.txt` 用的是
`find_package(Python3 COMPONENTS Interpreter)`，找的就是 `python3`，
所以确定性守卫应当能正常注册——**但这一条尚未实测**，见第 5 节。

**Ubuntu 24.04 的系统 Python 是「外部管理」的**（PEP 668）。
直接 `pip install torch` 会被拒绝。装 PyTorch 必须先建虚拟环境：

```bash
sudo apt install python3-venv          # 若无 sudo 权限，需找管理员
python3 -m venv ~/<你的名字>/venv
source ~/<你的名字>/venv/bin/activate
python -m pip install --upgrade pip
python -m pip install torch --index-url https://download.pytorch.org/whl/cu128
```

**是否有 sudo 权限尚未测试。** 若没有，`ensurepip` 或
[get-pip.py](https://bootstrap.pypa.io/get-pip.py) 是不需要 root 的退路。

## 4. 建议的目录约定

四人共用一个账号，约定一下免得互相踩：

```
~/<你的名字>/            各自的工作区
~/<你的名字>/venv/       各自的 Python 虚拟环境
~/shared/                需要共享的东西（数据集、预训练权重）
```

**训练产物（checkpoint、tensorboard 日志）不要往仓库里放**，
放 `~/<你的名字>/runs/`。磁盘有 6.2 TB，不缺这点空间。

## 5. 首次 GCC 构建：**已完成，双配置 7/7 全绿**

**2026-08-28 首次在这台机器上编译成功**，`README.md`「未认领的工作」第 2 项就此关闭。
`CLAUDE.md` 要求「要尽早、频繁地在服务器上跑构建，不要攒到临近答辩才第一次编译
Linux 目标」——第一次做完了，后面就是「频繁」这一半。

| 配置 | 构建 | ctest | 警告 |
|---|:-:|:-:|:-:|
| Release | ✅ | **7/7 通过**（0.26 s） | **0** |
| Debug | ✅ | **7/7 通过**（0.33 s） | **0** |

提交 `86d04f4`（当时的 `main`），g++ 13.3.0 / cmake 3.28.3 / `-j96`。
`-Werror` 全程开着（`RTS_WARNINGS_AS_ERRORS` 默认 ON），**没有降级任何开关**。

一并验掉了三处此前只在 Windows 侧验过、或标着「尚未实测」的：

- **#24 的自足头 TU 在 Makefiles 下正常生成并编译**
  （`build-*/rts_core/self_contained/{hash,rng,types}.cpp` 三个都在）。
  它们由 `file(GENERATE)` 产出，此前只在 VS 生成器上验过
- **#24 的构建类型守卫在 GCC 侧与 Windows 行为一致**，四种场景实测：
  全新目录不给参数 → `Release`；显式 `Debug` → `Debug`；
  复配不给参数 → 仍是 `Debug`（不回弹）；复配改 `RelWithDebInfo` → 生效
- **`python3` 确实被 `find_package(Python3)` 找到**，三条确定性守卫 ctest 全部注册并通过。
  第 3 节那句「应当能正常注册，但这一条尚未实测」就此消掉

### 唯一一处真缺陷：Catch2 的 FetchContent URL

**不是编译问题，是网络。** configure 卡在拉 Catch2 上，逐层测下来
`github.com:443` 当时 TCP 不可达，而 `codeload.github.com` 与 `pypi.org` 都通
（详情见第 3 节那一小节）。已给 `tests/CMakeLists.txt` 加 `codeload` 兜底 URL 与
`TIMEOUT 20`；更省事的做法是往 `~/shared/deps/` 放一份 Catch2 源码，
`tools/server/build.sh` 会自动用它、完全不走网络。

### 顺带修掉的一处

`.gitignore` 只有 `build/` 与 `cmake-build-*/`。**Makefiles 是单配置生成器**，
Release 与 Debug 必须各占一个目录，而 `build-Release/` 这个最自然的命名不在忽略列表里。
Windows 用 VS 生成器是单目录多配置，所以这一条只在 Linux 侧才暴露。已补 `build-*/`。

### 怎么把代码弄上去：`git bundle`，不在服务器上放凭据

仓库是私有的，而这个账号**四人共用**。在共用账号上放 GitHub token 或部署密钥，
等于把自己的凭据交给另外三个人，且它会留在一台共享机器上。

所以走 `git bundle`——git 原生的离线搬仓库手段，带完整历史，服务器侧是一个
真正的 git 仓库（能 log / diff / checkout），但它不认识 GitHub。
首次约 100 MB（校园网内 3.6 秒），之后是**增量** bundle，通常几十 KB。

```bash
tools/server/sync.sh                                    # 本地跑
ssh gpu 'cd ~/<你的名字>/siege-rts-rl && tools/server/build.sh Release'
```

两个脚本都在仓库里（`tools/server/`）。**`build.sh` 放仓库里是有意的**：
它随同步一起上去，服务器上不需要谁再照着文档敲一遍，也就不会各人一个版本。

服务器上的布局：

```
~/shared/siege-rts-rl.git      裸镜像，四人共用一份（省掉各传一次 100 MB）
~/shared/deps/Catch2-3.7.1/    第三方源码副本，build.sh 自动用、完全不走网络
~/<你的名字>/siege-rts-rl      各自的工作区，origin 指向上面那个镜像
~/<你的名字>/logs/             构建日志
```

**手动做镜像时有一个坑**：bundle 里的 `HEAD` 是悬空的，`git clone --mirror` 之后
必须 `git symbolic-ref HEAD refs/heads/main`，否则从镜像克隆会得到一个
**空的 `master` 分支、且不报错**（第一次就撞上了）。`sync.sh` 已经处理，
这里记一句是因为手动操作一定会遇到。

### 一件小事：这台机器**没有 `tmux`，只有 `screen`**

第 1 节建议的 `tmux new -s <名字>-<任务>` 在这台机器上不能用
（`tmux: not found`，且无 sudo 权限装不了）。等价写法：

```bash
screen -dmS cody-gcc-release bash -lc 'tools/server/build.sh Release'
screen -ls                      # 看谁在跑什么
screen -r cody-gcc-release
```

命名约定不变（`<你的名字>-<任务>`）——四人共用一个账号时，它是唯一能分辨
谁在跑什么的手段。

### 那两处曾经预计过不去的风险，**#24 已经把它们都消除了，本次实测确认没有复发**

本文初稿写的是「预计不会一次过，有两处已知风险」。#24 合入后**两条都不再成立**，
如实改掉——它们从「预测」变成了「已处理」，不是「跑通之后回来改」：

| 原风险 | #24 之后 |
|---|---|
| MSVC 与 GCC 的警告严格度不对称：`cmake/CompilerWarnings.cmake` 给 GCC 开了 `-Wconversion -Wsign-conversion`，而 MSVC 侧对应的 **C4365 没开**，于是本地干净、服务器 `-Werror` 直接失败 | **已消除**：补了 `/w14365`。实测 `std::uint32_t f(int x) { return x; }` 现在在 Windows 上就是 `error C2220`，分歧回到本地暴露 |
| `CMAKE_BUILD_TYPE` 的守卫在两平台行为相反：判空放在 `project()` 之后，而 `Platform/Windows-MSVC.cmake` 已把平台默认值定成 `Debug` 写进缓存，于是那段代码只在 Linux/GCC 上有效 | **已消除**：判断改成跨 `project()` 分两半（哨兵在前、判多配置在后）。Ninja 单配置下四种场景实测：不给参数 → Release、显式 Debug → Debug、复配不回弹、VS 多配置跳过 |

**本次实测确认两条都没有复发**：警告数 0（所以 C4365 那条对称性是真的），
构建类型四种场景全对（所以守卫在两平台一致）。

### 第三条同族风险（#24 引入的自足头 TU），也验了

`rts_core` 会多编三个**自动生成**的 TU（`build-*/rts_core/self_contained/*.cpp`），
每个只包含一个公开头，用来保证头文件能被单独 `#include`。
起因是 `types.hpp` 用了 `operator<=>` 却漏了 `<compare>`，而它一直编得过——
测试文件先 include 了 Catch2、把 `<compare>` 顺带拉了进来。

**这类漏 include 本来就可能只在一套标准库上炸**（libstdc++ 与 MSVC STL 的内部
包含图不同），所以它本是这张表上的第三条。那几个 TU 由 `file(GENERATE)` 产出，
此前只在 VS 生成器上验过；**本次确认 Makefiles 下三个都正常生成并编译通过**。

临时绕过编译失败仍是 `-DRTS_WARNINGS_AS_ERRORS=OFF`，但**那是绕过不是修**——
本次一次都没用到。

---

## 附：本文的核实方式

第 2、3 节的每一行都由 2026-08-28 的实测会话得出
（`nvidia-smi` / `lscpu` / `free -h` / `df -h` / 各工具 `--version` / `curl -sS` + 显式 `timeout`）。

**第 5 节是第二次会话的结果，全部是实际跑出来的**：两个配置各一次完整
configure → build → ctest（日志留在服务器 `~/cody/logs/`）、构建类型四种场景各配一次、
以及一次专门的对照实验——把 Catch2 的第一个 URL 换成黑洞地址，量出「不给 `TIMEOUT`
要等 133 秒 / 给 20 秒则 23 秒完成」这两个数，从而确认兜底 URL 真的会接手。

**第 3 节那个 `github.com ✅` 是本文改过的一处错**：初稿用 `curl -sI` 测，
而 `-s` 会把连接错误一并吞掉。现在这张表用 `curl -sS`（保留错误）重测过。
教训与本项目一直在处理的是同一条——**一个可能静默通过的检查，比没有检查更坏**。
