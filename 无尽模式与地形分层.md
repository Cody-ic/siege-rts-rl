# 无尽模式与地形分层（提案，待讨论）

> **本文是提案，不是已定契约。** 它触及 `CLAUDE.md` 与 `地图与场景设计.md` 的
> 硬性约束节，定案前不要据此写码。
>
> **本文不含任何已标定的数值**，一切节奏、层数、比例均待定（见 `CLAUDE.md`「关于数值」）。

## 0. 起因：「无尽模式」四个字从未被设计过

grep 整个仓库，「无尽模式」只出现两次，都是同一句形态描述：

> 玩家为守方……RL 控制的 AI 为攻方，一波一波进攻，**无尽模式**。

除此之外没有任何一节讨论过它。而现有设计里有几条曲线是**为有限波次写的**，
放到无尽下会失效或退化。本文把这些空洞列出来并提出对策。

各条彼此**不独立**：§6 是 §4 的结构要求，§7 需要 §3 供料，
而 **§7 是否必要完全取决于 §1.4**。依赖关系见第 9 节。

**读本文最容易走错的一步**：把 §1.4（等级提升哪些属性）当成一个可以以后再说的数值。
它是**结构选择**，把曲线指数直接翻倍，而 §7 成不成立取决于它的答案——
两条独立地拍会拍出自相矛盾的组合。

**本文经过一轮大幅瘦身**：原 §2（城区随 Keep 升级扩张）、原 §7（守方兵种等级轴 +
维持费全套）、原 §8（守方人数上限）三节被现在的 §7「堡垒等级：唯一的升级轴」
一节替掉，七件机制减为一件。原文都在 git 历史里。

**§7 此后又被评审推翻三次**，写在这里因为三次都指向同一类错误：

1. **上限机制两版都错。** 初稿用写死的档位表、第二稿用随波数抬升的上限曲线，
   **两者都是发明一个旋钮去执行一条不变量**。现在堡垒等级不设上限，
   那条不变量由「顶不满」直接给出（§7.2）
2. **§7.5 把城防与野战合在一个池子里算**，于是得出「单属性情况下不需要守方等级轴」。
   城里有墙和塔兜底，城外一样都没有，而野战是被拐点与 §3 **强制**发生的。
   §7 因此多出第三个输出（兵种等级上限，§7.6），并且它**不再是 §1.4 的下游**
3. **§7.7 新增「城墙 / 城门可升级」**，而它**强制**要求把 `CLAUDE.md` 那条
   「破坏速率与等级无关」一并改掉——两半必须成对表决

共同的毛病是**只算总量、不算它在哪里生效**。第 1 类是把「不变量」和「旋钮」搞混，
第 2、3 类是把两个作用域不同的机制当成同一个。

---

## 1. 最根本的一条：无尽模式没有定义「玩家在追求什么」

其余四条都是机制，这一条是**目标**，且它决定另外几条怎么标定。

### 1.1 现有曲线在无尽下的必然结局

`CLAUDE.md`「波次结构与数值曲线」已定的两条：

| 预算 | 增长 |
|---|---|
| 编成位 | 线性，**硬性封顶约 40** |
| 兵力 | **超线性**，不设上限 |

编成位耗尽后多余兵力只能投等级，而「等级只提升血量与伤害」。于是第 N 波
（N 很大）是 **40 个血量与伤害都是天文数字的单位**。后果有两层：

- **必败是设计内的。** 玩家经济无论怎么涨都是有界的（地图上的资源点是有限的），
  攻方兵力无界。这对生存游戏本身没问题——*They Are Billions* 亦然——
  但它意味着**无尽模式的目标只能是「撑到第几波」这个分数**，而这从未写下来。
  **§1.4 与 §7 一起给出了另一条路**：先把攻方那条曲线的指数降下来（等级只提升单一属性），
  守方再靠「堡垒等级 → 人口上限」跟上，于是「必败」从结构必然变成一场曲线竞赛。
- **战斗解算会退化。** 克制关系是乘法的、等级无关的，所以倍率一直成立；
  但当双方数量级差到一定程度，「谁的数大」压倒一切，
  **克制关系仍在生效却不再决定胜负**。这直接威胁「本作偏向智斗」这个立项论据。

### 1.2 因此需要明确三件事

1. **玩家的追求是「最高波数」这个分数。** 若同意，则 UI 要有波数记录、
   答辩演示要以「撑到第几波」为叙事主线。这是一句话的决定，但它没写就等于没有。
2. **`CLAUDE.md`「拐点位置是主要的难度旋钮」这句要改。** 采纳第 3 条之后拐点会
   变成一个反复出现的节奏而非单点，旋钮随之变成**解禁节奏 vs 强度曲线之比**。
3. **训练时的波次采样需要一个上界。** 「按波次分层采样，波数本身即构成课程」
   在无尽下没有上界。实际必须有一个截断波数 W，超过 W 的波次不再训练
   （因为那里战斗已退化成数值比较，学不到东西）。**W 是一个必须显式定义的量**，
   而且它同时是「战斗解算退化点」的估计——两者应当一起标定。

### 1.3 「必须防范死亡螺旋」这条要重述——它现在写得比它自己的理由要强

`CLAUDE.md` 有一节标题是「**必须防范死亡螺旋**」，下面挂四条对策。
组内提出的疑问是：为什么一定要**完全**防止，让玩家「不容易进入」不就够了？

**这条疑问成立，而且它与那一节自己写下的危害是一致的**——原文说的危害不是「玩家会输」：

> 某一波惨胜、防御全毁、无力重建、之后每波更惨，**玩家明知必输仍需拖很久**。

**「拖很久」是节奏问题，不是公平问题。** 所以那一节的**标题overshoot 了它自己的论证**。

#### 1.3.1 在无尽模式下，「完全防止死亡螺旋」是不自洽的

1.1 已经确定：攻方兵力无界、玩家经济有界，**必败是设计内的**。也就是说
**每一局都以螺旋收尾——螺旋就是结局本身**。完全防止它等于让游戏永不结束。

**而且完全防止会把代价消掉。** `CLAUDE.md` 要求「防线的形状在一波之内会动态退化，
玩家需要实时判断先补哪段、放弃哪段」——**「放弃哪段」只有在放弃真的有长期代价时
才是决策**。一波打崩而毫无余波的游戏没有赌注。

#### 1.3.2 但「不容易进入」也不是准确的目标

「不容易」是个概率描述，而这里要管的事是结构性的。准确的目标是三段：

| 阶段 | 要求 | 由谁保证 |
|---|---|---|
| **进入** | 必须是自己选择的后果，不是随机的 | 波次强度是波数的函数、与玩家表现无关（已定） |
| **在其中** | 要有一条**真实但困难**的出路 | 维修成本低于重建、废墟折扣 |
| **确实没出路** | **快速结算，且过程中始终有决策** | 见 1.3.4；目前只有一半 |

#### 1.3.3 重读那四条对策：它们其实是两类，文档把它们混为一谈

| 对策 | 实际职能 |
|---|---|
| 维修成本显著低于重建 | **给出路**——它不防止进入，它让「抢救残血建筑」成为一种技巧表达 |
| 废墟 / 地基折扣 | 同上 |
| 城内保底收入（资源地板） | **保证决策空间非空** |
| `Keep` 产出极少量金币（兵力地板） | 同上 |

后两条最容易被误读成「保证不会输」。看一下当初给 `Keep` 那条的论证就知道不是：

> 金币完全依赖城内 `Mine` 的话，金矿一旦被点掉玩家**连补兵都做不到**，
> 任何缺口都无法再堵。

**「连补兵都做不到」不是「会输」，是「没有可做的决定」。** 那才是要防的东西。

#### 1.3.4 因此正确的表述

> **要保证的不是「不会输」，而是「输的过程里始终有决策，且不会拖很久」。
> 地板保证的是决策空间非空，不是胜利可能性非零。**

三个实际后果：

1. **地板可以定得更低。** 它只需维持最小的决策能力，不需要支撑翻盘。
   这顺带降低了一个此前担心过的风险——`Keep` 的金币地板意外重开龟缩。
   **这是一个数值后果，方向是「可以更小」。**
2. **需要补一条「快速结算」，而它一半已经有了。** `CLAUDE.md` 已要求
   「必须提供**提前召唤下一波**（附奖励）」，理由写的是「经济起飞后只能干等」——
   但它**同时**解决螺旋期的拖延：你可以主动加速走向结局。
   缺的另一半是**投降 / 结算**（显示「本局第 34 波，最佳 41 波」并让玩家结束），
   零设计成本，是生存类的标准做法。
3. **§7.4 的「买活」正好落在这条重述上。** 它是在崩盘那一刻才出现的决策，
   且被人口上限与金币双重封顶——**不阻止你输，只让输的过程有选择**。
   这正是「决策空间非空」而不是「胜利可能性非零」。

#### 1.3.5 一条反方论据，记下来免得以后当成没想过

**对 RL 训练而言，深度螺旋状态会污染样本。** 若守方也由 RL 控制（#15 的方向），
大量 episode 会花在真实对局里不会出现的崩溃态上，浪费采样预算，
并可能让攻方策略偏向「欺负已经崩掉的守方」。

但这应当用**课程与采样**处理（不采样深度螺旋态），不是用游戏设计处理；
而 AlphaStar 式的 league training 本来就故意包含弱对手。所以可管理，
不构成加强「防止」的理由。

### 1.4 「等级提升哪些属性」是一个控制曲线陡峭度的**结构问题**，待实验决定

**记下来，不在本文定。** 但它必须**先于 §7 定**——§7 成不成立取决于它的答案，
若两条独立地拍，会拍出一个自相矛盾的组合。

#### 1.4.1 现行规则选中了最陡的那一档，而这可能是无意的

`CLAUDE.md` 已定：

> **等级只提升血量与伤害**，绝不改变克制倍率、射程、速度或任何机制。

那条规则的目的是**别让等级改变机制**，不是「让等级尽量陡」。但「血量与伤害都涨」
是三种选法里指数最高的一种：

| 等级提升什么 | 有效战力（血量 × DPS） | 高等级单位读起来是什么 |
|---|---|---|
| **血量 + 伤害**（现行） | **∝ L²** | 又肉又疼 |
| 只提升血量 | ∝ L | 耐打但不更致命；克制者仍能高效解决它，只是慢；齐射与溅射增值 |
| 只提升伤害 | ∝ L | 脆皮高伤；风筝与集火收益暴涨 |

**这不是数值，是结构选择**——它把曲线的指数直接翻倍，而三种选法都满足
「不改变机制性克制」那条原意。

#### 1.4.2 它决定 §7（守方等级轴）是否必要

粗算：攻方 40 个 L 级、守方 P 个 1 级，按「总血量 ÷ 对方总 DPS 比时间」估，
**忽略城墙 / 塔 / 高度加成这些守方独有的乘数**：

| 等级提升什么 | 守方需要的人口 | 结论 |
|---|---|---|
| 血量 + 伤害 | `P > 40·L` | L 超线性 ⇒ 人口得超线性涨，**不可能** ⇒ **守方必须有等级轴** |
| 只提升一项 | `P > 40·√L` | **次线性** ⇒ **人口上限跟得上，守方不需要等级轴** |

这是个粗略启发式，真正的答案要等成本产出矩阵能跑；
**但 `L` 与 `√L` 这个指数差是稳的**，不随细节变。

于是：

> **若等级只提升单一属性，§7 那条守方等级轴整条可以砍掉**——
> 用 §7 现在写的「堡垒等级 → 人口上限」就够了。

#### 1.4.3 怎么定：设计实验，不要在文档里拍

按 `CLAUDE.md`「关于数值」，曲线本身一律待定、等成本产出矩阵与自动标定倒推。
**属性选择这一项虽然是结构的，但它的判据是实验性的**——三种选法都自洽，
差别只在对局手感与曲线陡峭度，而那两样只有能实际对局之后才看得出来。

因此本节的产出**不是结论，是一条待办与它的归口**：

- **谁**：做成本产出矩阵的人（`tools/` 侧），与攻方预算曲线一起标
- **何时**：机制跑通、能实际对局之后
- **怎么判**：三种选法各跑一遍矩阵，看哪一种下克制关系在高波次仍然决定胜负
  （§1.1 说的「战斗解算退化成谁的数大」正是要避免的形态）
- **在此之前**：`rts_core` 侧**不受影响**——等级无论如何都是每单位一个标量，
  改的是解算时怎么用它，不是数据布局

---

## 2. ~~城区随领主堡垒升级而扩张~~（已作废，见 §7）

**本节作废。** 它的存在理由是补候选乙的 2.1.2 那个洞（城区固定 ⇒ 墙类决策只剩「修旧」）。
组内后来倾向 **§3 的「任意位置建造，仅做地形限制」**，那个方案下：

- **没有城区可扩** —— 本节的机制无处附着
- **2.1.2 那个洞本来就不存在** —— 城外可建，「建新」原样保留

`Keep` 因此又变回一个纯负债。**它的新职能改由 §7 承担（人口上限与建筑等级上限），
而那比扩城区更好**：不与「任意位置建造」冲突，且一条轴带两个输出。

原文保留在 git 历史里，不在正文重复。

## 3. 资源点随波数解禁

### 3.1 先选清楚是哪个读法

「随波数出现新资源」有三种读法，差别很大：

| 读法 | 评价 |
|---|---|
| (i) 出现同样三种资源的**新点位** | 可行，但「凭空长出一个矿」需要解释 |
| (ii) 出现**第四种资源** | **不建议**，见下 |
| (iii) 全部点位从第 1 波就在图上、**可见但未解禁**，按波数分批开放 | **推荐** |

**读法 (ii) 不建议**：`CLAUDE.md` 把资源锁定为三种，理由是「每种资源对应一条不同的
决策轴，且不可自由兑换」，并明确指出资源过多会退化为搬砖（TAB 约 7 种、DiNaO 8 种，
4 周内做不动）。一种在第 30 波才出现的资源，要么需要一条新的决策轴
（那是一整块设计工作，且此前 29 波它不存在），要么没有新轴——那就是纯搬砖。

**推荐读法 (iii)**，三条理由：

1. **几何成本为零。** 地图尺寸不变、`size` 不变、flow field 不受影响——
   解禁只是地图数据里一个「起始波数」字段的比较，不动任何一张位图的形状。
   相比之下「地图随波数变大」会动 `size`、动 flow field 分配、动观测的坐标系。
2. **它是免费情报，正好落在既有的情报设计上。** 玩家从第 1 波就**看得见**
   远处那些还不能采的资源点，于是可以提前规划「第几波我要去拿那一簇」。
   这与「波数、大致到达时间」同属免费可见的一侧，不需要新机制。
3. **越晚解禁的越远。** 这是本条真正的价值，见下。

### 3.2 它把「拐点」从一次性事件变成反复出现的节奏

`CLAUDE.md` 现在写的是：

> 城内保底收入按线性增长，波次强度按超线性增长。**必须存在一个拐点**使玩家龟缩
> 不再可行、被迫出城争夺外部资源。**拐点位置是主要的难度旋钮。**

这是为有限波次写的：一个拐点，过了就一直在野战。无尽模式下需要的是**节奏**。

若解禁的簇按距离递增排列，则每一次解禁都是一次新的「你得出城去拿」，
而且距离更远、风险更高。收入曲线因此成为阶梯，强度曲线仍是平滑超线性，
两者之差呈**锯齿**：刚解禁时宽裕，随后逐渐吃紧，直到下一次解禁。

这正是 TAB 的节奏感，而且它给出了本文第 1.2 节要的那个答案：

> **经济上限可以随波数增长，但增量必须靠野战取得。**

这一条同时满足三件此前互相拉扯的要求——无尽模式有经济纵深、龟缩仍然不可行、
野战机制（冲锋助跑、枪阵、风筝）一直有用武之地。

**它也是 §7 的资源来源**：堡垒等级只给上限，人口与建筑等级都要付钱，
而那些钱的增量只能来自解禁出来的新资源簇——**必须靠野战取得**。

### 3.3 待定与代价

- **解禁节奏是数值**，与强度曲线一起标定，现在不定。
- `地图与场景设计.md` 6.2 的 `resources` 条目要加一个「起始波数」字段。
- 4.4 的 `tier`（`inner` / `outer`）目前是「平衡标注，不影响仿真」。
  起始波数**会影响仿真**（未解禁的点不能建采集建筑），所以它不能混进 `tier`，
  要单独一个字段。
- 校验器要查：解禁序列按距离单调递增；且第 1 波已解禁的簇足以支撑开局。

---

## 4. 建造限制：**任意位置建造，仅做地形限制**

> **本节推翻 2.1 现有的「倾向候选乙」结论，也推翻了甲。不宜由两三个人拍。**

组内提出：**应当允许玩家在任意位置建造，仅仅做地形限制即可。**
我认为这个方向是对的，而且它比甲乙两案都强——但「仅地形限制」有一个前提，
见 4.2.1；不加那个前提，它不是限制，是没有限制。

### 4.0 它为什么比甲乙都强

| | 候选甲 | 候选乙 | **仅地形限制** |
|---|---|---|---|
| 额外机制 | `no_build` mask | 城区 mask + 前哨点 | **无**，`buildable` 只来自地形位图 |
| 规则可见 | ✗ 盖在看起来正常的平地上 | ✓ 城区边界 | ✓ **地形本身就是解释** |
| 墙类的「建新」 | 保留 | **被削掉**（2.1.2 那个洞） | **保留** |
| 「哪一簇值得围」这个决策 | 保留 | 削弱 | 保留 |

**它顺带解决了 2.1.2**——乙案唯一未决的那条（城区固定 ⇒ 墙类决策只剩「修旧」）
在这里根本不存在，因为城外可建。这也是原 §2 被作废的直接原因：
§2 只是为补那个洞而存在的。

### 4.1 原提法按字面写会**反向生效**

提案原话是「资源附近要伴随障碍物，防止能彻底围住」。若「障碍物」指 `Rock`（巨岩），
**这条会起相反的作用**：

`Rock` 是不可通行 + 不可建造。一条被岩壁打断的墙线**仍然是封闭的**——
岩 + 墙 + 岩 + 墙 依然围死。`地图与场景设计.md` 2.2 正是把这一点当作**给玩家的好处**：

> **天然岩壁承担大部分周长。** 城区其余方向由 `Rock` 封死，只有走廊口需要人工墙。

所以在外部资源簇周围摆岩壁，等于**替玩家把大半圈墙免费砌好了**。

### 4.2 但换成 `Forest` 就成立，而且比甲乙两案都好

`Forest` 的标志位是**可通行 ✓ / 不可建造 ✗ / 挡视野 ✓**，这个组合恰好是需要的：

| 玩家想做的 | 森林带的效果 |
|---|---|
| 砌一圈墙围住 | **建不了**——森林不可建造，墙圈画不出来。**但必须配 4.2.1 的连通条件，否则可绕开** |
| 三面砌墙 + 架塔（甲的那个洞） | **削弱但未封死**，见 4.2.2 |
| 在 `Plain` 缺口上架塔覆盖 | **覆盖被废**——森林挡视野，塔看不进林子 |

而它：

- **不需要任何新机制**：`Forest` 是既有枚举项，位图、寻路、视野全都现成
- **是看得见的**：候选乙的「原始理由」（`no_build` 盖在看起来正常的平地上、
  容易被读成 bug）在这里以另一种形式成立了——森林是画出来的地形，
  「林子里不能盖房」不需要任何解释。而这次它**不是渲染问题**，
  因为解释规则的东西就是仿真里的那个地形本身，不是一层遮罩

#### 4.2.1 必须逐字继承甲的另一半：到地图边界的连通条件

**上一版的 §4.2 漏了这一条，那个「封围起来」的 ✓ 因此是继承来的印象、不是森林带自己挣的。**

森林带阻止你**穿过它**砌墙，但不阻止你**绕着它**砌墙：

> 在森林带**之外**画一圈更大的墙。全程在 `Plain` 上，一格森林都不碰，簇照样被围死。

代价只是墙更长——**纯数值劝退**，正是「结构封死，而非数值劝退」要封的形态。
实际还更便宜：2.2 写了「天然岩壁承担大部分周长」，簇附近若有岩体可以借力，
那圈墙连闭合都不必自己走完。

甲的原文本来有这一半，森林版必须逐字继承、只把 `no_build` 换成 `Forest`：

> **对每个外部资源点，存在一条从它出发、到地图边界、全程位于 `Forest` 内的路径。**

补上之后 ✓ 才是真的：森林不可建造，任何闭合墙线都必须跨过那条通道。

#### 4.2.2 但它与校验器第 10 条是正面对立，且有一个机制后果

补上连通条件之后，两条校验**方向相反、作用在同一层**：

| | 要求 |
|---|---|
| 第 7 条（森林版） | 每个外部簇**必须有**一条通到地图边界的连续森林通道 |
| 第 10 条 | 森林**不得构成**从任一集结点直达城墙的连续遮蔽通道 |

**可满足**（通道可以在远离集结点的地方接到边界），但生成器的可行域明显变窄，
而第 9 节的生成器是「不通过就丢弃重生成」——可行域一窄，丢弃率就上去。

更要紧的是第二个后果，而它不是数值：

> **每一条被强制要求的森林通道，本身就是一条从地图边界通到玩家外围经济的遮蔽潜行道。**
> AI 会学会用它。

这未必是坏事——它给 2.2 的「经济侧走廊」补了牙，也让 `Scout` 在外围有事可做。
但它是**机制后果**，调「森林覆盖率上限」调不掉，所以必须是一个**明写的设计决定**，
不能是把甲改成森林之后才被人发现的副作用。

#### 4.2.3 「封设防」应当是「部分」，不是 ✓

甲之下**簇本身是 `Plain`、可建造**（必须如此，否则采集建筑都盖不上去），
禁建区只是它周围的一圈。于是玩家的做法从「三面砌墙 + 架塔」退化成
**「直接把塔架在采集建筑旁边」**——没有墙，但塔照样开火。

森林带确实削弱了它，而且削弱的方式很好：
**森林挡视野把杀伤区压成一条薄带，而那条带的深度是几何上界，钱买不到。**
这是真正的结构收益。

但**塔的数量仍然随钱增长**，薄带里塔越多、瞬时火力越高。
所以「外部收入靠建筑守」被**压低了，没有被封死**。

这一格上**候选乙严格更强**：它禁止城外一切建造，例外只有地图预设的 N 个前哨点，
且 `N < 簇数` 是地图作者写死的常数。

#### 4.2.4 一条独立于甲乙之争的价值：森林挡视野是**双向**的

**玩家也看不见有人来摸自己的外围矿场。**

于是外围经济的防守从「架几座塔覆盖住」变成「**要么驻兵，要么认损**」——
而这正是「部分资源点在墙外」真正要保护的东西（野战必须发生），
也给 `Ranger`（出城的执行手段）与 `Scout`（外围侦查）各自一个明确职能。

**关键是这条在甲和乙下都成立。** 因此建议**把森林带从甲乙之争里摘出来**：
不论 2.1 最后选甲还是选乙，外部簇周围铺森林带都值得采纳。
它在乙之下不是冗余——**乙用「城外不可建」封住建造，森林带另外封住视野**，
两者管的不是同一件事。

### 4.3 因此 2.1 的结论可能要重开

评审否掉候选甲的理由是「甲只封死围起来、没封死设防」。若 `no_build` 环换成
**森林带**，那个洞就被封上了，甲的两条原始缺点（不可解释、洞可用钱扩大）同时消失。

那么候选乙相比森林带甲还剩什么优势？

| | 森林带甲 | 候选乙（城区 + 前哨） |
|---|---|---|
| 封「围起来」 | ✓ **但须补 4.2.1 的连通条件** | ✓ |
| 封「设防」 | **部分**（杀伤区深度有几何上界，但塔数仍随钱涨，4.2.3） | ✓（城外不可建） |
| 规则可见 | ✓（地形本身） | ✓（城区边界） |
| 「哪一簇值得围」这个决策 | **保留**（不同簇的森林带疏密可以不同） | 削弱（改为「哪一簇值得设前哨」） |
| 墙类的「建新」 | **保留**（城外仍可建） | 被削掉，且已无 §2 可补 |
| 实现成本 | 零新机制 | 一张城区 mask（2.3 本来就要） |
| 对守方 RL | 槽位集合大 | 槽位集合小而稳定 |

两案都成立，取舍点在**「墙外能不能建」这件事本身要不要保留**。这是范围问题，
不是实现问题，所以应当和 2.1.2 那条一起进小组议程。

**两条与第 10 条的关系见 4.2.2**——补上连通条件之后它们是正面对立而非仅需联合校验。

**另注意 4.2.4**：森林带有一部分价值（双向挡视野 ⇒ 外围经济只能驻兵或认损）
**在甲乙两案下都成立**，因此那部分不应卷进这场比较，应当无条件采纳。

---

## 5. 资源建筑与地形必须在**形体**上区分，不是靠配饰

### 5.1 上一轮的修法方向对、力度不够

#19 评审指出「采石场与碎石、岩壁同为三堆灰石头、只有大小之差」，
当时的修法是给三座资源建筑加**旗 + 一件加工构件**，并把原则写成
「人造 = 玩家建筑，纯自然 = 景物或地形」。

方向是对的，但**力度不够**：主体质量仍然是那堆天然石头，人造部分只是配饰。
于是剪影层面 `Quarry` 与 `Rock` 依然是同一族。旗子能区分，但玩家在游戏节奏下
读的是剪影，不是配饰。

### 5.2 该反过来：建筑是主体，资源是点缀

原则应当是：

> **采石场不是一堆石头，是「开采石头的建筑」。**
> 伐木场不是木料，是锯木厂。金矿场不是矿石，是矿井口。

即人造结构占**主体质量**，资源本身只作小面积点缀。这样三座资源建筑与
`Rock` / `Forest` / `TreeBig` / `Stump` / `RockSmall` 在剪影上就属于两个族。

这条为什么值得花工时：「攻其必救」是头号演示点之一，
而它成立的前提是**观众能看出 AI 是奔着采石场去的**。若采石场读作岩壁，
那段演示就变成噪声。

### 5.3 与第 6 条的联动

第 6 条给景物（矮木桩、碎石）一个机制职能之后，视觉上就需要**三个可区分的族**：

| 族 | 例 | 机制 |
|---|---|---|
| 人造建筑 | `Quarry` / `Lumber` / `Mine` | 我的经济建筑，必须守 |
| 可破坏障碍 | `Stump` / `TreeSmall` / `RockSmall` | 有血量，能被打穿开路 |
| 不可破坏障碍 | `Rock` / `Forest` | 永久，防线可以依靠它 |

这三族**必须一眼分清**，否则玩家会把防线靠在一段其实能被打穿的障碍上。

---

## 6. 障碍物分可破坏与不可破坏

### 6.1 实现成本比看起来低：它就是一段中立的墙

`地图与场景设计.md` 5.1 已经把墙定义成**高代价可通行**：

```
cost(墙段) = 墙段当前血量 / 该单位的破坏速率
```

**可破坏障碍物完全是同一个东西**——一段没有归属、不参与战斗的「墙」。因此：

- **寻路零改动**：同一个代价公式，「绕远走走廊」与「就近打穿木桩」自动比较，
  不需要写「寻找可破坏障碍」的逻辑
- **flow field 数量不变**：5.2 说 field 数 = 不同破坏速率的种类数，
  而破坏速率是兵种属性，与被破坏的对象无关
- **战斗解算零改动**：`CLAUDE.md` 已要求「建筑与城墙走与单位完全相同的战斗解算」

一处必须注意：**可破坏障碍有血量，所以它不能待在地形层。** 地形是静态枚举、
展开成三张位图；带血量的东西必须进实体扁平数组（与建筑同类，只是无归属、无战力）。
这与 4.3「`no_build` 是独立的一层 mask，不是地形类型」是同一种切分理由。

### 6.2 一条提案里没说的收益：它让景物第一次有了机制

`Stump` / `TreeSmall` / `RockSmall` 目前在花名册里标的是「景物，**无机制**」。
把它们变成可破坏障碍，等于给它们一个职能——而这反过来**为第 5 条要的视觉区分
提供了正当理由**：不是为了好看才把它们画得不一样，是因为它们机制不同。

第 5 条与第 6 条互为支撑，应一起采纳。

### 6.3 一条推导：`Forest` 必须不可破坏，这是第 4 条要求的

提案说「巨岩和密林不可破坏」。若采纳第 4 条的森林带方案，
**这不再是风味选择，而是结构要求**：

若森林可破坏，玩家就能清掉外部簇周围的森林带，然后照旧砌墙——
第 4 条整个失效，龟缩退化解复活。

同理**玩家不得拥有任何清除地形的手段**（无论叫「开荒」还是「整地」）。
按「结构封死，而非数值劝退」，这不能靠「清地很贵」来防。

`Rock` 不可破坏的理由则是 2.2：它「承担大部分周长」，
可破坏则玩家的城墙线随时可能被从侧后打穿，
而「需人工设防的正面总长」这条校验（第 9 条）也就不再有意义。

### 6.4 可读性是硬要求

`CLAUDE.md`「风味名不得遮蔽机制可读性」在这里的具体形式是：
**可破坏 / 不可破坏必须一眼读出**，否则玩家会把防线靠在一段能被打穿的障碍上，
而那是一次不可挽回的误判。

提案给的例子本身已经编码了规则，直接采纳即可：

> **木质、矮、稀疏 = 可破坏；石质、巨大、密集 = 不可破坏。**

### 6.5 待定

- 各类可破坏障碍的血量——**数值，待定**
- 是否所有兵种都能破坏障碍物（倾向：与墙一致，都能，速率为兵种属性）
- 空中单位对障碍物的破坏速率（倾向 0，与墙一致，理由同 5.2）
- 可破坏障碍被打掉后是否留废墟/是否可再生（倾向都不做，避免引入地形动态生成）

---

## 7. 堡垒等级：**唯一**的升级轴

> 本节替掉了原来的三节：~~§2 城区随 Keep 升级扩张~~、~~§7 守方兵种等级轴~~、
> ~~§8 守方人数上限~~。原文都在 git 历史里。
>
> **替换的动机是成本。** 原 §7 摊了七件东西（升级购买、维持费、付不起自动降级、
> 维持费地板、UI、观测等级通道、跨波状态进回放），而这个项目**仿真本体一行都没有**。
> 一个特性摊七件，比例不对。
>
> **本节被评审推翻过两次，两次都是同一个毛病。** 初稿用「写死的档位表」、
> 第二稿用「随波数抬升的上限曲线」来封住早期经济复利，两者都是**发明一个旋钮
> 去执行一条不变量**。现在没有上限，那条不变量由 §7.2 的「顶不满」直接给出。
> 记在这里是因为「给它加个上限」是一个非常自然的念头，下一个人多半还会想到。

### 7.1 一条轴，三个输出

```
堡垒等级 ─┬─→ 人口上限          （数量闸门）
          ├─→ 建筑等级上限      （城防的质量闸门）
          └─→ 兵种等级上限      （野战的质量闸门，见 §7.5 / §7.6）
```

第三个输出是后加的：初稿只有前两个，而 §7.5 论证了**那样野战无法打**——
城里有墙和塔兜底，城外一样都没有。

`Keep` 目前只是一个负债加一条极少量金币的兵力地板；本节给它一份正经工作。
而 §3 采纳「任意位置建造」之后**没有城区可扩**，所以原 §2 那个方案已经无处附着——
这条比它好，因为它不与「任意位置建造」冲突。

**升堡垒只抬天花板，不改变任何已有建筑。**

> **升到 N 级不会让城里的塔跟着变成 N 级，也不会让城外的兵跟着变成 N 级。**
> 它只是让「塔 / 兵最高能到 N 级」这件事成立；每一座塔仍然要**单独付钱**升级，
> 每个兵仍然要**按等级付钱征召**（§7.6），人口也仍然要逐个花金币。

这句要写死，因为「建筑等级上限 = 堡垒等级」这个写法容易被读成「建筑等级 = 堡垒等级」，
而那是两个完全不同的机制：后者是升一次全城变强（无取舍），前者是升一次**多出一批
可以花钱的地方**（有取舍）。

因此**在你还用不满当前上限时升堡垒就是浪费**，决策是
「现在扩容，还是先把这一波顶过去」——标准的经济 vs 防御 tempo 取舍。

这一点是刻意检查过的：`CLAUDE.md` 用「没有取舍——你总是要造、放哪儿都行」
否掉过登墙阶梯，本节必须过同一道关。它过了，因为**升级只买上限、不买战力**。

**一条顺带的好处**：每次升堡垒之后，玩家面前会多出一串「现在可以升但钱不够全升」
的建筑，于是**必须挑**。这正好是既有约束
「防御建筑造价高到无法覆盖所有墙段……**建筑定义防御的形状**」在时间维度上的复现——
原来那条管的是「这一圈墙你只守得住几段」，这条管的是「这一批升级你只升得起几座」。
两条同源，不需要新的平衡论证。

### 7.2 早期经济复利被「顶不满」封住，而不是被一个上限封住

原 §7.4 用**维持费**来防「买断式升级 = 早期经济决定难度 = TAB 那条致命缺陷」。
本节不需要维持费——但**理由不是本节初稿写的那个**，那一版错了两次，都记在这里。

#### 初稿的写法与它的两个毛病

初稿写的是：「你不可能在第 10 波把塔升满，堡垒等级不允许……**档位表由地图 / 配置
写死**，不是一个可以靠钱扩大的旋钮。锁，不是租金。」

**毛病一：写死的档位表在无尽模式里是坏的。** 顶满之后后期资源没有出口，
「经济决定你能买多少」失效；修旧 / 建新 / 造兵 的三方决策退化成两方。
更要紧的是它**拆掉 §7.1 自己给出的好处**——§7.1 说这条轴的价值在于
「每次升堡垒之后，玩家面前会多出一串『现在可以升但钱不够全升』的建筑，
于是**必须挑**」，而那要求天花板一直在抬。档位表顶满，这个机制就停了。
§7.1 与 §7.2 因此在无尽模式下互相矛盾。

**毛病二：改成「上限是波数的函数」同样不对，而且错得更隐蔽。**
第二稿想用一条随波数抬升的曲线替掉档位表，但那条上限：

- **几乎永不生效。** 要到堡垒 N 级得攒够 `cost(1..N)`，而每波收入被地图上有限的
  资源点封顶，所以「N 级不会出现在第 T(N) 波之前」**已经由造价曲线保证了**。
  再加一条波数上限，约束的是一个本来就到不了的状态
- **抢堡垒等级本就是被支配策略**，§7.1 已经写明「在你还用不满当前上限时升堡垒
  就是浪费」。买了天花板却不买天花板底下的东西，等于什么都没买
- **它没有标定靶子。** 这条曲线必须**高于**经济能负担的（否则就变成真限制、扭曲玩法），
  那**低于**什么？说不出来。一条只有下界、而下界又由别的机制保证的约束，
  没有可标定的位置

第三条是致命的，它正撞上 `CLAUDE.md`「区分结构约束与数值」：
**一条没有标定靶子的约束不是结构约束，是一个没有含义的数字。**
这与本文 §4 评审里赞同过的那句「用一个数值旋钮去定义一条结构约束，方向就错了」
是同一个毛病——**用旋钮去执行一条不变量，而该交付的是那条不变量本身。**

#### 因此：堡垒等级不设上限

**没有档位表，也没有波数曲线。** 它由经济自然限速——每波收入有界，
升级造价随等级增长，所以等级增长本来就是慢的。

#### 那「早期经济复利」靠什么封住：覆盖率

档位表其实**从来没有在做这件事**。有好经济的玩家只是更早**贴着**天花板，
有坏经济的贴不到；档位表管的是「什么时候能贴到」，不是「贴到之后强多少」。

真正封住它的是**顶不满**：

> 天花板底下的结构数 × 升级造价随波数超线性增长，而每波收入被有限的资源点封顶，
> 所以**能维持在天花板上的结构比例逐波下降**。攻方永远是满预算，玩家永远只能挑几段。

三件事一起落地，且都不需要新机制：

| | 由什么保证 |
|---|---|
| **必败是设计内的**（§1.1） | 来自「顶不满」，而不是「撞到一个硬上限」——比硬上限自然 |
| **TAB 那条致命缺陷** | 早期经济好只是让你贴着天花板，**永远超不过它**；复利被结构封住 |
| **不重开龟缩** | 顶满全城的开销随波数超线性，收入不随 |

一个附带的好处：波数越高越必须**集中**升级，于是防线形状越锐利——
「建筑定义防御的形状」这条随时间**加强**而不是变钝。

因此原 §7.4 那四件东西（维持费、按波结算、付不起自动降级、1 级维持费为零的护栏）
**全部删掉**，结论与初稿相同，理由换成上面这条。

### 7.3 人口上限顺带把原 §8 那个隐藏限制变成可见的进度

原 §8 的结论是「守方人数上限从硬约束（RL 可训练性）变成待定数值
（演示可读性 + 前端预算）」。那个分析仍然成立，但它当时只能给出一个**藏起来的数字**。

改成「人口上限随堡垒等级增长」之后，同一个限制变成了**玩家看得见的进度条**——
既好懂，又把旋钮从「一条要解释的隐形上限」换成了**每级给多少人口**这条曲线，
而后者可以和其余数值一起用成本产出矩阵标定。

（初稿这里写的旋钮是「档位表」，随 §7.2 一并作废。）

§8 因此收窄为对 RL 与演示两条约束的记录，见该节。

### 7.4 「买活」：金币可以买回**节奏**，不能买回**位置**

组内提议：士兵大量死亡后消耗金币即时召唤补回。**可以，但有一条必须守住。**

`CLAUDE.md` 允许波次中花任何资源，限制器是**时间与暴露**：
「金币的兵要训练要时间，从城内走到缺口也要时间」。买活把**训练时间**换成钱，没问题；
但若召唤点能跟着缺口走，就把**行军时间**也买掉了——而那条是结构性的限制器。
砍掉它，「压制墙头 → 破墙 → 缺口攻防」三阶段直接作废，
等于玩家可以无限即时填缺口，攻方创造缺口的全部投入被抹平。

所以三条边界：

| 边界 | 理由 |
|---|---|
| **召唤点固定在 `Keep` / `Barrack`**，单位照样走过去 | 保住「行军时间」这个结构性限制器 |
| **不得突破人口上限** | 买活只买节奏、不买规模，因此不会压倒战术 |
| 价格随本波已买活次数递增 | 数值，待定 |

**副作用是好的**：它给金币一个木材石材都没有的独占用途，强化了
「三种资源各对应一条决策轴」；而且它正好落在 §1.3.4 那条重述上——
**买活是在崩盘那一刻才出现的决策**，被人口上限与金币双重封顶，
所以它不阻止你输，只让输的过程有选择。

### 7.5 「守方够不够」必须分**城防**与**野战**两处算，不能合在一起

初稿这一节只问了一个总量问题（`P` 对不对得上 `L`），并据此得出「若等级只提升
单一属性，则**不需要任何守方等级轴**」。**那个结论是错的**，因为它把城防和野战
当成同一个池子。

#### 7.5.1 §7 给守方的三样，只有一样在野战里生效

| | 城防 | 野战 |
|---|:-:|:-:|
| 人口上限 | ✅ | ✅ |
| 建筑等级上限 | ✅ | ❌ 城外没有墙和塔 |
| 买活（§7.4） | ✅ | 只买**节奏**，不买强度 |

于是城里有三条兜底、城外只有一条，而**城外那一条被演示可读性封顶**（§8.2）。

#### 7.5.2 而野战不是可选项，是被强制的

四条既有设计都要求它发生：

- `CLAUDE.md`：「必须存在一个**拐点**使玩家龟缩不再可行、**被迫出城**争夺外部资源。
  城内收入若能一路撑到通关，**野战机制（骑兵冲锋助跑、枪阵、风筝）与"攻其必救"
  全部失效**」
- `Ranger` 的存在理由整条就是「**出城的执行手段**：摸攻城锤、争夺墙外矿场」
- 「部分资源点在墙外」是明写的**两个不能砍的机制之一**
- **本文 §3 让它更严重**：解禁越晚的簇越远，所以后期玩家必须出城**更远**

把这些和上一小节合起来，形态是：**经济逼你出城 → 城外只有人数 → 人数被可读性封顶
→ 攻方等级无界**。于是拐点从「一个新的决策空间」变成**一道死刑**。

那不是取舍，是陷阱。而且它连带毁掉四条：野战机制、攻其必救、`Ranger` 的存在理由、
以及 §3 自己——**守不住的资源不是纵深**。

#### 7.5.3 三条替代路子都不行

| 路子 | 为什么不行 |
|---|---|
| 靠人数 | 被演示可读性封顶（§8.2），而 `P > 40·√L` 即使在最有利的 §1.4 分支下也是无界的 |
| 靠前推建筑（§4 允许任意位置建造，那就在外围矿场架塔） | **自我拆台。** 否掉 2.1 候选甲的理由正是「只封死『围起来』，没封死『设防』」——用建筑解决野战，等于取消野战 |
| 科技 / 装备升级 | `CLAUDE.md` 明文不做（无科技树），且它是一整块新设计 |

**所以守方兵种等级是必需的，不是 §1.4 分支下的可选项。** §1.4 的答案只改变它需要
多陡，不改变它是否需要。具体形态见 §7.6。

（初稿这里还写「连带把维持费那套一起请回来」，那半句作废：按 §7.2，
守方等级同样由「顶不满」封住早期复利，不需要维持费。）

**所以 §1.4 必须先于本节定。** 两条独立地拍会拍出自相矛盾的组合。

### 7.6 兵种等级：**征兵时选级**，不是逐个升级

这是堡垒等级的第三个输出，也是 §7.5 那个洞的补法。

> **N 级堡垒 ⇒ 玩家可以征召 1 至 N 级的士兵，等级越高越贵，数量只受人口上限与资源限制。**

#### 7.6.1 为什么是「征兵时选级」而不是「买一次升级整个兵种」

我原先想的是「按兵种买等级，买了之后该兵种全体变成 N 级」。**征兵时选级更好**，
三条理由，第二条最重要：

1. **它不引入新的经济系统。** 等级只是征兵造价的一个参数，不需要单独的升级采购、
   不需要「已买等级」这份状态。原 §7 那七件东西里有一半是为升级经济服务的
2. **它和攻方现有的机制是同一条决策，因此不需要新的平衡论证。** `CLAUDE.md` 已写：
   > 兵力**允许不均匀分配**，攻方由此可以选择编成形状（40 个低级杂兵铺开消耗 /
   > 8 个高级精英硬凿一点 / 一名满级精英带一群炮灰）

   守方现在有了对称的那一半：**人口上限管数量，金币管质量。** 两个上限、两条决策，
   而且它们互相牵制——人口紧张时该买贵的，钱紧张时该买多的
3. **它不触发「个体老兵制」那条禁令。** 那条禁的是**战斗中获得等级**，理由是
   「惨胜额外附带**战力永久倒退**，正是防死亡螺旋要防的形态」。
   征兵时付钱买等级不会：损失一个 5 级兵与损失等价造价的若干 1 级兵，代价相同

#### 7.6.2 「任意个」要读作「人口上限内任意个」

否则守方单位数无界，§8.2 那两条封顶（演示可读性、前端预算）就没了。
**人口上限仍是唯一的数量闸门**，本节只加质量这一维。

#### 7.6.3 已有士兵可以批量升级——但它必须吃时间

**必须有批量升级**，否则每次升堡垒，玩家的存量部队就成了低级累赠，
理性操作变成「把老兵送死再重招」——那是纯杂活，且极难看。

定价取**差价**（目标等级造价 − 当前等级造价）即可，于是它在经济上等价于
「卖掉再重招」，不引入新的经济，只是省掉操作。

**但它不能是瞬发的。** 若瞬发，「缺口出现 → 一键全军升到满级」就成了一次
波次进行中的免费战力尖峰，而 `CLAUDE.md` 对这类事的一贯处置是
**「限制器是时间与暴露，不是禁令」**。所以：

- 升级需要时间，且**升级中的单位必须在 `Barrack` / `Keep`**——于是批量升级
  等于**把部队从防线上撤下来**，代价与「驻守槽位上去的单位被钉在那一段」同源
- 这顺带给了 `Barrack` 位置决策一个新维度（离前线近则撤回快，也更容易被点掉），
  与它原有的「补员快 / 易被拆」是同一条轴

**波次进行中不禁止批量升级**，与三种资源的处置保持一致。

#### 7.6.4 连带要认下的三条

1. **逐字照抄攻方那条「等级只提升血量与伤害」。** 这条在守方一侧理由**更强**：
   满级 `Archer` 若射程变远，玩家就不再需要另外四个兵种，
   **守方那半张克制二部图会直接塌掉**
2. **攻方观测要加「敌方单位等级」通道。** 但**建筑等级已经要这条了**，两者共用；
   而攻方自己的单位等级标量早已在观测里（`CLAUDE.md` RL 一节），所以是对称的
3. **`CLAUDE.md`「倾向于保持精简（无科技树）」正式作废。** 一条轴三个输出仍远不是
   科技树（无分支、无解锁、无前置），但要明确认下来，不要事后当成漂移

#### 7.6.5 一个连带好处：它给 §10 第 1 项提供了唯一有效的测试场

§10 第 1 项（等级提升哪些属性）的判据是「哪种选法下克制关系在高波次仍决定胜负」。
**这个判据只能在野战里测**——城墙攻防里几乎没有机动，冲锋助跑、枪阵克骑、
风筝这些**机制性克制**在那里根本不发生。

双方都有等级之后，野战才成为一个双方都在缩放的场景，
「等级只改数值、不改机制」这条规则也才真正被验证过。

### 7.7 城墙与城门也在这条轴上，而这**强制**破坏速率随等级

`Wall` 与 `Gate` 都在建筑花名册里，所以「建筑等级上限 = 堡垒等级」本来就覆盖它们。
写成单独一节，是因为它连着一条 `CLAUDE.md` 现行规则的**必要修改**——
两者必须**成对采纳**，单独任何一条都比现状更糟。

#### 7.7.1 墙的等级只能是血量；门必须同倍率

- **只加血量。** `CLAUDE.md`「等级只提升血量与伤害」，而墙没有伤害，
  所以这一条在墙上是唯一解，很干净
- **`Gate` 必须一起能升，且与 `Wall` 同倍率。** 城门是「木制，破坏速率高于城墙——
  **结构上的既定薄弱点**」。只升墙不升门，门就从「薄弱点」变成「唯一入口」，
  「打哪儿」这个决策消失。同倍率则「门破得更快」这个**比例**在每一级都保持
- 这与「克制用乘法加成」是同一条道理：**乘法在等级缩放下保持不变，加法会被淹掉**

#### 7.7.2 破坏速率必须随等级——否则破墙时间趋于无穷

`CLAUDE.md` 现行规则：

> **破坏速率是兵种的固定属性，与等级无关。** 这是一把结构锁……
> 高波次的满级步兵一拥而上瞬间破墙，即落入"低效不等于不可行"的陷阱。

这条规则是在**守方一条等级轴都没有**的时代写的。墙可升级之后：

| 守方墙等级 | 破坏速率 | 破墙时间随波数 | |
|---|---|---|---|
| 可升级 | 与等级无关 | **→ ∞** | 龟缩复活 |
| 不可升级 | 随等级 | **→ 0** | 三阶段的中段消失 |
| **可升级** | **随等级** | **落在一个区间内** | ✅ |

所以真正该守的不变量不是「破坏速率是常数」，而是：

> **破墙时间必须在可玩波段内落在一个区间里——不趋于 0，也不趋于 ∞。**

趋于 0 则「压制墙头 → 破墙 → 缺口攻防」的中段消失；趋于 ∞ 则龟缩。

#### 7.7.3 `Ram` 的存在理由不会丢

`CLAUDE.md` 规避的是 Stronghold 那条批评（「手持武器步兵能砍穿石墙**导致攻城器械
毫无意义**」）。**同一个乘数作用在所有兵种上，攻城锤对步兵的比例优势因此不变**——
又是 7.7.1 那条乘法性质。所以这条批评不会回来。

#### 7.7.4 顺带修掉一处内部矛盾

`CLAUDE.md` 另有一条**实现要求**：

> 建筑与城墙走**与单位完全相同的战斗解算**（血量、护甲类型、承伤计算），
> 仅护甲类别不同且不可移动……**不要做成独立的特例系统**

等级提升伤害 + 建筑走同一套解算 ⇒ 等级就该提升对建筑的伤害。
**「破坏速率与等级无关」只能靠在那个明令不许开特例的地方挖一个洞来实现。**

所以本节不是加规则，是**删掉一个例外、把规则变均匀**：
等级提升对一切目标的伤害，包括建筑。这比现状简单。

#### 7.7.5 一处真代价，必须进 `rts_core` 的接口契约

`CLAUDE.md` 的寻路那条：

> 代价 = 墙血量 ÷ 该单位破坏速率……**因破坏速率与等级无关，field 数量是兵种尺度的常数**

破坏速率随等级之后，「墙格代价 ÷ 平地代价」这个**比值**随等级变化，
于是 flow field 数量从「兵种」变成「兵种 × 等级档」。

缓解办法是把等级量化成少数几档破坏速率，但**那是 `Pathfinder` 的形状，
必须在接口契约里定，不能等寻路写到一半才发现**。这条列进 §10。

### 7.8 待定

- **每级给多少人口、建筑等级上限如何随堡垒等级走**——数值，待定
  （初稿这里写的是「档位表」，随 §7.2 一并作废）
- **建筑等级提升什么**——与 §1.4 同一个问题：塔的等级若同时涨血量与伤害，
  防御侧也会有 `L²`。倾向与单位取同一种选法，理由是可读性（同一个「等级」概念，
  两处含义不同会很难解释）
- **升堡垒的代价形态**（一次性资源 / 需要时间 / 需要停机）——待定
- 买活的价格曲线——数值，待定
- **等级 L 的士兵造价曲线**——数值，待定。它有靶子：`k` 个 1 级兵与 1 个 `k` 级兵
  应当在成本产出矩阵里大致等价，否则「人口上限管数量、金币管质量」退化成单一最优解
- **批量升级需要多久、是否随跨越的级数增长**——数值，待定。靶子是
  「撤下防线的时间足以构成一次真实的取舍」，与驻守槽位的下墙延迟同源
- **破坏速率随等级的曲线，以及它与墙血曲线的相对关系**——数值，待定。
  但它**有明确的标定靶子**（7.7.2 那条区间），归口与训练截断波数 `W` 相同：
  等成本产出矩阵能跑，从退化点反推。**这正是被作废的那条堡垒上限所缺的东西**

## 8. 守方人数上限：约束仍在，但它现在是 §7 的一个输出

本节原来提「守方人数上限可以非对称」，那个分析仍然成立，但结论已被 §7 吸收——
人口上限现在**随堡垒等级增长**，是一条可见的进度而非隐藏的硬限制。
保留本节是因为其中两条约束**不随那个改动而消失**，标定人口曲线时要照它们来。

### 8.1 前提在正式游玩时成立，在训练时不成立

单波 20–40 那条上限的原文理由是 RL 现实（被引的工作是 **40v60**，
所以规模不对称并非没有先例）。但 `日志.md` 8-27 已定「**训练时守方也由 RL 控制**」，
而训练恰恰是那条上限唯一约束的场合。

**可化解**：那条上限约束的是**逐单位的战术策略**（多智能体微操），
而 `守方AI与协同演化.md` 提的是**宏观层**。于是分界是——

> 只要守方的战术层永远不是逐单位 RL 策略（保持脚本 / 规则驱动），
> 守方的单位数就不受那条 RL 上限约束。

这是一条**长期承诺**而非排期选择，属 #15 范围（该文已收作它的第 9-7 条）。

攻方侧成本接近零：观测用**密度通道**，守方单位多只是通道值更高、不是更多通道；
动作枚举「攻击最近 / 最弱敌人」同样与数量无关。

### 8.2 真正给人口上限封顶的是两条与 RL 无关的

- **演示可读性。** 上限原文的第二条理由是「20 个单位的战术意图玩家看得懂，
  **数百单位的混战无法展示 AI 的智能**」。这条对守方一侧同样成立：
  屏幕上一团一百多个单位时，包夹、佯攻调虎离山、点杀维修工全都看不出来，
  而那些是**答辩的核心论证材料**。
- **前端预算。** 大量单位意味着真正的军队管理 UX（编队、阵型、多选）。
  `render/` 现有 5–8 人日，而 #16 已指出该预算不含演示与可视化功能。

**所以人口曲线的上界由这两条定，不由 RL 定。** 这个区别重要：
RL 可训练性是外部现实、谈不动；演示可读性与前端预算是我们自己的取舍、可谈。

注意这与 §7.2 作废堡垒等级上限**不冲突**：那里作废的是「堡垒能升到几级」，
这里说的是「每级给多少人口」。前者没有标定靶子，后者有两条（可读性、前端预算），
而**有没有靶子正是一条约束该不该存在的判据**。

---

## 9. 各条之间的依赖

```
§1.4（等级提升哪些属性）  ★ 必须最先定
   └── 它决定守方等级轴要**多陡**，不决定它是否需要（§7.5 已定：需要）。
       初稿这里写「单属性 ⇒ 人口上限就够」，那是把城防与野战合在一个池子里算的结果

§1（无尽模式的目标 + 训练截断波数 W 的归口）
   └── 决定其余各条怎么标定

§3（资源点随波数解禁，越晚越远）
   ├── 把「拐点」从单点变成节奏，是无尽模式的经济纵深来源
   └── 是 §7 的资源来源（堡垒等级只给上限，人口与建筑等级都要付钱）

§4（任意位置建造，仅地形限制）
   ├── 必须配 4.2.1 的到边界连通条件，否则可绕开 ⇒ 龟缩复活
   ├── 要求 §6 里 Forest 不可破坏，且玩家不得有清除地形的手段
   └── 它作废了原 §2，并顺带解决 2.1.2

§7（堡垒等级：一条轴，三个输出）
   ├── 需要 §3 供料——而且是**双向**的：§3 是 §7 的钱，§7 是拿到 §3 的手段
   │      （新解禁的簇越远，去拿它越需要野战战力，而那来自 §7.6）
   ├── 建筑等级的**上限**（不是等级本身）由堡垒等级给出；堡垒等级**本身不设上限**，
   │   早期经济复利由「顶不满」封住（§7.2），因此既不需要维持费、也不需要档位表
   ├── §7.5 → §7.6（兵种等级上限：征兵时选级 + 批量升级）
   │      └── **不是 §1.4 分支下的可选项。** §1.4 只决定它需要多陡，不决定它是否需要：
   │          城防有墙和塔兜底，野战一样都没有，而野战是被 §3 与拐点强制发生的
   └── §7.7（墙/门可升级 ⇄ 破坏速率随等级）—— **这两半必须成对**，
       单独任何一条都比现状更糟：只升墙 ⇒ 破墙时间 → ∞；只改速率 ⇒ → 0
          └── 后果落在 `Pathfinder`：flow field 从「兵种」变成「兵种 × 等级档」，
              属接口契约，须先于寻路实现定下（§10 第 4b 项）

§5（资源建筑形体级区分）← 与 §6 互为支撑：机制不同才有理由画得不同
§6（可破坏 / 不可破坏障碍）← 给景物机制职能；Forest/Rock 不可破坏是 §4 与 2.2 的结构要求

§8（守方人数上限）← 已被 §7 吸收，只保留两条给人口曲线定上界的约束
```

**最小可采纳集合**：`{§1, §1.4 的归口, §3, §5, §6}`。这几条互不冲突、
不触碰 2.1 的建造之争，且 §5 / §6 的素材部分**已经落地**。

§4 会改变 2.1 的结论，须先议。

§7 **不再需要等 §1.4**：§7.5 证明守方等级轴是必需的，与 §1.4 的答案无关；
§1.4 只影响它的曲线陡度，而那本来就是待标定的数值。

## 10. 需要小组拍板的 —— **已表决完毕**

> **本文随 #23 合并、结论以 #53 落地（2026-08-29）。** 落点是 `CLAUDE.md`（约 14 处）、
> `rts_core 接口契约.md` §5.1、`地图与场景设计.md` 2.1 / 4.3 / 第 8 节。
>
> **本节保留全部题面**，因为它是那些结论的**理由**唯一的记录处——落地的文档只写结论，
> 不写「为什么另一个选项更糟」，而后来者要改一条决定时需要的正是后者。
>
> 其中 4 / 4a / 4c 三项是**范围决定**，本文自己写着不宜由两三个人拍，交小组定，
> **三项都按推荐通过**。第 2、3 项**仍未定**，别把这张表读成全绿。

| # | 问题 | 谁受影响 |
|:-:|---|---|
| 1 | ~~**`§1.4` 等级提升哪些属性**（血量+伤害 / 只血量 / 只伤害）~~ **已定的是归口与判据，不是答案**——而本项原本要求的就只是这个。判据：三种各跑一遍成本产出矩阵，看哪种下克制关系在高波次仍决定胜负，而**它只能在野战里测**（城墙攻防里几乎没有机动，冲锋助跑 / 枪阵克骑 / 风筝根本不发生，§7.6.5）。已落进 `CLAUDE.md` 等级规则那一节。**答案仍待标定**，归口与训练截断波数 `W` 相同 | 全部；尤其攻方预算曲线与野战平衡 |
| 2 | **仍待定：无尽模式的目标是不是「最高波数」这个分数？** 若是，UI 要有波数记录、答辩叙事要跟。**注意 `CLAUDE.md` 已定的「中位存活波数」不是本项的答案**——那是**评估指标**（我们拿什么衡量一个策略），本项问的是**玩家的游戏目标**（屏幕上显示什么、玩家在追求什么）。两者可以不同，而 UI 只跟后者 | `render/`、答辩材料 |
| 3 | **仍待定：训练截断波数 `W` 的归口**（属「数值」，今天不能定值）。产出应是「谁在什么条件下标定它」——等成本产出矩阵能跑，从「战斗退化点」反推。**#53 没有落地这一项**，`CLAUDE.md` 里至今没有它的归口 | `train/`、评估指标 |
| 4 | ~~**接受「堡垒等级」这一条升级轴吗？**~~ **已定：通过。** 人口上限 + 建筑等级上限 + 兵种等级上限 + 买活；**堡垒等级本身不设上限**，早期经济复利由「顶不满」封住而非由档位表封住（§7.2；初稿的档位表与第二稿的波数曲线都已作废，理由在该节）。落在 `CLAUDE.md`「守方的一条升级轴：堡垒等级」 | 范围、UI、`rts_core` |
| 4c | ~~**兵种等级：征兵时选级 + 已有部队可批量升级**~~ **已定：通过。** 它不是「§1.4 若不利才需要」的备选——§7.5 论证了**没有它就打不了野战**，而野战被拐点与 §3 强制发生。连带 `CLAUDE.md`「倾向无科技/升级」正式作废，**但作废的只是「一条都不做」，不是「可以随便加」**：仍然无科技树、无分支、无解锁 | `rts_core`、`render/`（采购与批量升级面板）、平衡 |
| 4a | ~~**城墙 / 城门可升级（只加血量、同倍率）+ 破坏速率改为随等级——两条必须成对表决**~~ **已定：成对通过。** 而**定案的性质与题面不同，这一点值得记住**：它不是「甲乙都行、挑一个」，而是**原写法自身不自洽**——「破坏速率与等级无关」与「建筑与城墙走**与单位完全相同的战斗解算**」直接冲突，要成立只能在「不要做成独立的特例系统」那条明令上挖一个洞。结构锁因此换成一条更锐利的不变量：**破墙时间必须落在一个区间里，不趋于 0 也不趋于 ∞**。同时结算了契约 §5.1 悬着的甲/乙 | `CLAUDE.md` 三处、`rts_core`、平衡 |
| 4b | ~~**`Pathfinder` 的 flow field 从「兵种」变成「兵种 × 等级档」**~~ **已定：是 4a 的直接后果**，已写进 `CLAUDE.md` 寻路那句与契约 §5.1.1。**1c 唯一要定的是量化成几档**，且**必须先于寻路实现定下**——等寻路写到一半才发现，改的就是接口 | 1c |
| 5 | ~~**`§4` 建造限制改为「任意位置建造，仅地形限制 + 到边界连通条件」？**~~ **已定：通过**，即 `地图与场景设计.md` 2.1 改用**森林带**，候选甲与乙都作废，原 §2 一并作废。**校验器第 7 条随之可实现，已落地**（判据是「每个 `outer` 资源点存在一条 4 连通的全 `Forest` 路径通到地图边界」） | `地图与场景设计.md` 2.1、`tools/map_gen/`、守方 RL 的槽位集合 |
| 6 | ~~**买活的三条边界**~~ **已定：照收**（召唤点固定、不得突破人口上限、价格递增），落在 `CLAUDE.md`「守方的一条升级轴：堡垒等级」那张表下方 | `rts_core`、`render/` |
| 7 | ~~守方战术层是否**永不**采用逐单位 RL（§8.1）~~ **已定：本学期不做，但不写成「永不」**（措辞取自 #26 A7；结论记在 `守方AI与协同演化.md` 9-7，区别是实质的，见那里） | `守方AI与协同演化.md`、`train/` |
| 8 | ~~`render/` 预算重估~~ **已给出基线，数字仍待小组认下。** 基线在 `演示与可视化清单.md` 第 9 节问 3（随 #52）：**基础设施那一半做完了，交互那一半一件没做**，到「一个可玩的守方前端」总计 **8–9 人日，已越出原 5–8 预算**，演示两项另计 1.5–2。**仍待定：认不认这个数字、以及是否要第二个人分担**。另注意这个估计**没有把 4c 带来的采购与批量升级面板算进去**，所以它是下限 | `render/`、#16 |

**当初写的表决顺序**（第 1 项最先，4a / 4c 依赖 4，4b 依赖 4a，4a 两半不得拆开投）
**已按此执行**。保留它是因为其中一条判断值得留着：**4c 若被否，等于承认「拐点之后玩家
无法野战」**，那会同时作废 `CLAUDE.md`「必须存在一个拐点使玩家被迫出城」、`Ranger`
的存在理由、「部分资源点在墙外」这条不能砍的机制，以及本文 §3 自己。它通过了，
所以这四条都还站着——但**将来若有人想回头砍掉兵种等级，要连带处置的就是这四条**。

### 10.1 本节没有列、却同样悬着的一项：§6 的机制那一半

**§6（障碍物分可破坏与不可破坏）从来没有进过上面那张表**，因为 §9 的「最小可采纳集合」
把它算作「素材部分已经落地」。但 §6 有两半，只有一半落了地：

| §6 的两半 | 状态 |
|---|---|
| `Forest` / `Rock` **不可破坏**，且玩家没有清除地形的手段 | ✅ **已落地**，随 §4 一同——它是 §4 的**前置**（`地图与场景设计.md` 2.1.5）。少了它，2.1 整节退化为数值劝退 |
| `Stump` / `Sapling` / `Rubble` 变成**有血量的可破坏障碍** | ❌ **仍待议。** `CLAUDE.md` 那张「命名对照表：可破坏障碍」至今写着「随本文 §6 一同待议」 |

**这个分裂有一处具体后果**：`地图与场景设计.md` §8.1 的**第 19 条**（`Forest` 与 `Rock`
不得出现在可破坏障碍列表里）预设了「存在一份可破坏障碍列表」，而那份列表来自上表第二行。
所以第 16..20 条里**第 19 条等 §6、第 18 条等 6.2 的解禁波数字段，只有第 16 / 17 / 20 条
现在就能写**。

## 11. 连带修订的既有文档 —— **本文已通过，这里是落地情况**

> **下表大部分已由 #53 落掉**（`CLAUDE.md` 约 14 处、`地图与场景设计.md` 2.1 / 4.3 /
> 第 8 节、`rts_core 接口契约.md` §5.1）。表本身**原样保留**，它是「为什么要这么改」
> 的记录；**下面这份短清单才是当前状态**——仍未落地的只有这几条，且各有不同的原因：
>
> | 仍未落地 | 为什么 |
> |---|---|
> | 防死亡螺旋节的「**投降 / 主动结算**」那半条 | 重述已落地，但「快速结算」**没有任何实现手段**。**这是本次落地新产生的缺口，不是遗留问题**——重述之前根本没有这条要求 |
> | 4.1 地形表的「**可破坏**」一列；4.2.1 的第三类实体映射；5.1 复用墙的代价公式 | **三条全部依赖 §6 的机制那一半，而它从未被表决**，见 10.1。（4.1 那一行的另一半——森林带与第 10 条的正面对立——**已落地**，在 2.1.3） |
> | 4.4 / 6.2 的 `resources`「**解禁波数**」字段 | 一次**跨模块契约变更**：连带 `rts_core` 的载入、`tools/map_gen/` 的读写、以及第 18 条校验。字段名与取值形式也还没定 |
> | 第 8 节新增校验 | **第 7 条已落地**（森林带判据）。第 16..20 条编号与判据已排定、**一条没实现**：第 18 条等上一行，**第 19 条等 §6**，**第 16 / 17 / 20 条现在就能写** |
> | `演示与可视化清单.md` 前端预算要含**军队管理 UX 与升级面板** | #52 给的 8–9 人日基线**没把这两项算进去**，所以那是下限而不是估计值 |

| 位置 | 改什么 |
|---|---|
| `CLAUDE.md` 等级规则 | **「等级只提升血量与伤害」需按 §1.4 的结论复核。** 那条规则的原意是「别让等级改变机制」，而三种属性选法都满足原意、指数却差一倍——现行写法选中了最陡的一档，可能是无意的 |
| `CLAUDE.md` 波次曲线节 | 「拐点位置是主要的难度旋钮」→ 改为「解禁节奏与强度曲线之比」 |
| 同 | 「单波单位数上限 20–40」要区分攻守：攻方仍是硬上限（RL 现实），守方改为**随堡垒等级增长的人口上限**，其上界由演示可读性与前端预算定（§8.2） |
| `CLAUDE.md` 待定事项 | 「倾向无科技/升级」按 §7 的结论订正为「**一条升级轴：堡垒等级**，三个输出（人口上限 / 建筑等级上限 / 兵种等级上限），无分支无解锁」 |
| `CLAUDE.md` 花名册前言或新一节 | 守方单位增加「**征兵时选等级**（1..堡垒等级），等级越高越贵；等级只提升血量与伤害；已有部队可批量升级，取差价、需时间、须在 `Barrack`/`Keep`」（§7.6） |
| `CLAUDE.md` 波次结构节 | 「兵力允许不均匀分配」那段现在**双向成立**——守方也在选编成形状（人口上限管数量、金币管质量），可直接引它做守方侧的说明，不需要新论证 |
| `CLAUDE.md` RL 设计决策 | 观测的「敌方单位等级」通道：守方单位等级与建筑等级**共用同一条**，不新增通道数 |
| `CLAUDE.md` 建筑花名册 | `Keep` 增加「可升级，抬高人口上限与建筑等级**上限**；**不改变任何已有建筑**，每座仍需单独升级」。`Wall` / `Gate` 增加「可升级，**只加血量**，两者同倍率以保住『门破得更快』这个比例」（§7.7.1） |
| `CLAUDE.md` 结构破坏规则节 | **「破坏速率是兵种的固定属性，与等级无关」改为随等级缩放**（§7.7.2）。要连带换掉理由：现行的「满级步兵一拥而上瞬间破墙」是手挥式的，锐利版本是「破墙时间必须落在一个区间里，不趋于 0 也不趋于 ∞」。**这一条与花名册那条必须一起改**，单独任何一条都比现状更糟 |
| `CLAUDE.md` 同节 | 「Stronghold 那条批评」的规避理由要补一句：同一个乘数作用在所有兵种上，**`Ram` 对步兵的比例优势不变**，所以随等级缩放不会让攻城器械失去意义（§7.7.3） |
| `CLAUDE.md` 寻路那句 | 「**因破坏速率与等级无关，field 数量是兵种尺度的常数**」不再成立，改为「兵种 × 等级档」，并指向接口契约里的量化决定（§7.7.5、§10 第 4b 项） |
| `CLAUDE.md` 战斗解算那条 | 不需要改，但值得在 §7.7.4 交叉引用：**现行的破坏速率规则正是在这条「不要做成独立的特例系统」上挖的洞**，改完之后两条才真正一致 |
| `CLAUDE.md` 资源表 | 金币增加「买活」这一用途；**「维持费」那个空词仍未被定义**，本版不再用它，需另行处置 |
| `CLAUDE.md` 防死亡螺旋节 | 按 §1.3 重述为「快速结算 + 始终有决策」；补「投降 / 结算」这半条 |
| `CLAUDE.md` 评估指标 | 无尽模式下应看「中位存活波数」而非胜率 |
| `CLAUDE.md` RL 设计决策 | 观测通道语义：**存和不存平均**（零无歧义、和是线性的）；**血量存比例 `hp/max_hp`**（绝对值随等级无界增长会让课程迁移失效）；归一化分母用**本波名义等级**且两侧共用 |
| `地图与场景设计.md` 2.1 | 视 §4 的结论——若采纳，甲与乙都作废，`buildable` 只来自地形位图，另加一条「到边界的不可建造连通路径」校验 |
| 同 4.1 | 地形表增加「可破坏」一列；森林带与校验器第 10 条的**正面对立**（一条要求必有通到边界的森林通道，一条禁止森林构成直达城墙的遮蔽通道） |
| 同 4.2.1 | 补第三类实体（可破坏障碍）的映射；并**并列一条 `run_dir()`**——线性结构的 `d` 是走向不是朝向，同样指向 `preview_map.py` |
| 同 4.4 / 6.2 | `resources` 增加「起始波数」字段（不可混进 `tier`，因它影响仿真） |
| 同 5.1 | 明写可破坏障碍复用墙的代价公式 |
| 同第 8 节 | 新增校验：解禁序列按距离单调；外部资源点到边界的不可建造连通路径；森林带与第 10 条联合校验 |
| ~~`CLAUDE.md` 命名对照表~~ | ✅ **已由 #30 落地**：可破坏障碍单开一张表（第三类实体，不吃三轴定位） |
| ~~同命名纪律~~ | ✅ **已由 #30 落地**：`TreeSmall`→`Sapling`、`RockSmall`→`Rubble`（≤7 字符豁免的前提被 §6 破坏）、`TreeBig`→`ForestB`（`Forest` 变体须同前缀）。**注意 §6 若被否，前两条会回落到豁免范围内**，而 `ForestB` 那条与 §6 无关、仍然成立 |
| `守方AI与协同演化.md` | 守方战术层是否可能上逐单位 RL（§8.1 的前置，已收作其 9-7） |
| `演示与可视化清单.md` | 前端预算重估要含军队管理 UX 与升级面板 |
| ~~`tools/sprite_gen/assets.json`~~ | ✅ §5 / §6 的素材部分与两条命名订正**均已落地**（#27 / #30） |
