# 创新点与答辩讲法

> **这份文档解决一个具体问题：我们的创新点讲不准，就会从加分项变成扣分项。**
>
> 起因是 2026-08-29 的组内讨论。有人说「扁平数组这套设计甚至能当创新点」——
> **这句话是对的**，但它至少有三种讲法，其中两种会让老师听到
> 「他们在解释为什么没做到要求」。本文把讲准的版本写下来，并给出可直接念的讲稿。
>
> **本文不是宣传稿。** 每条讲法都标了它**现在**成不成立
> （✅ 已在库 / ⏳ 依赖 1c / ⚠️ 依赖训练跑起来），答辩前必须照第 6 节复核一遍。
> **讲一条当场被追问穿的创新点，比不讲它更糟。**

---

## 0. 三种失败形态，与三条判据

### 要防的三种讲法

| 形态 | 例子 | 老师实际听到的 |
|---|---|---|
| **讲泛** | 「我们用了数据导向设计 / ECS 架构」 | 一个术语，不是一个成果。追问「那跟别人有什么不同」会当场答不上 |
| **讲成辩解** | 「我们没用继承，因为要保证确定性」 | 「他们在解释为什么没满足要求」。**而交付要求里恰好有『类对象设计』**，这条最危险 |
| **讲成炫技** | 「我们的 AI 用了强化学习」 | 「调了个库」 |

第二种是本文的主要防范对象。它的毛病在**主语**：句子的主语是「我们没做的事」。
同一件事换个主语就成立了——「我们用数据布局保证了一类 bug 写不出来」。

### 判据：一条创新点讲准了，要同时满足三件

1. **说得出它防住了什么具体的坏事**（不是「更好」，而是「没有它会出现 X」）
2. **说得出没有它会怎样**，且反事实要具体到**症状**
3. **当场能指出证据**：一段代码、一条会红的测试、或一次可复现的演示

第 3 条是分水岭。**能演示的创新点与不能演示的，在答辩里是两个量级。**
本文每一条都配了证据栏，凡是证据栏写「⏳」的，答辩前要么补上，要么降级为「设计目标」而不说成成果。

---

## 1. 一条主线：把不希望发生的事变成「写不出来」

这是把全项目串起来的一句话，也是最该背下来的一句：

> **我们全项目只用一种手法：不靠纪律、也不靠劝退，而是让不希望发生的事在代码里没有语法形式。**
> 它在**代码层**的实例是数据布局，在**游戏设计层**的实例是「结构封死，而非数值劝退」。

比分别讲两个点强的地方：它把两处看起来无关的设计变成**同一个方法论的两次应用**。
方法论比技巧更像研究，也更好回答「你们的创新点是什么」这种开放问题。

### 1.1 代码层：数据布局让一类 bug 写不出来 ✅

**一句话版**

> 仿真里**没有裸指针可以拿来当容器的 key 或排序依据**，所以「两次运行因为对象地址
> 不同而得出不同结果」这种 bug，在我们的代码里**没有语法形式**可以表达。

**三分钟版就讲下面这条因果链，一环都不要跳**——跳掉第 3 环，听众就不明白为什么值得为它改架构：

1. RL 训练必须可复现 → 主要防 bug 手段是**确定性回放测试**：存种子 + 指令流，重放必须逐 tick 状态一致
2. 那个测试最常见的破坏源是**遍历顺序取决于内存地址**：`unordered_map<Unit*, X>` 的迭代顺序、按指针值排序
3. **这类 bug 不崩、不报错**，只是两次运行不一样；而它的症状表现为「训练不收敛」，于是人会去查网络、查超参、查奖励函数，**不会去查存储层**
4. 所以我们不给它留语法形式：实体存在扁平数组里，跨 tick 引用一律是「索引 + 代数」的整型句柄

**黑板上三行就够**：

```cpp
std::vector<Unit*> units;                           // 常见写法：地址可当 key → 顺序可变
std::vector<Vec2> pos_;  std::vector<int64_t> hp_;  // 我们：只有下标
UnitId id;                    // 索引 + 代数，不是指针；三种句柄编译期不可互换
```

**证据（当场可指，全部已在库）**

| 证据 | 在哪 |
|---|---|
| 把确定性禁令变成**会红的 ctest**：扫 `unordered_*`、C 库随机函数、标准库分布适配器、挂钟、以指针为 key 的有序容器 | `tools/check_determinism_bans.py`，注册为 ctest |
| **守卫自己有自检**（13 条应报 / 12 条不应报），防它退化成永远绿 | 同上 `--self-test` |
| **5 条 `WILL_FAIL` 条目**，专门验「守卫自己能不能变红」 | `tests/CMakeLists.txt` |
| 三种句柄**编译期**不可互换（`Handle<Tag>` 的 Tag 模板），传错一组编不过 | `rts/types.hpp` |
| 把「哈希口径变了」与「仿真真的跑歪了」在诊断上分开 | `kWorldHashTag`（`rts/world.hpp`） |

最后一条是这条创新点里**最能体现工程判断的细节**，而且**它真的用上过一次**：

> 组内表决通过一条新机制后，命令枚举末尾加了一个成员——而那个枚举恰好是一个
> 进哈希的数组的长度，于是所有旧回放失效。**标签让它报成「这份回放是用 World/1
> 录的，本二进制是 World/2，重录」，而不是「第 0 tick 状态不一致」**；
> 后者会让人去查一个不存在的确定性缺陷。

**这个例子值得在答辩时讲出来**，因为它正面回答「你们怎么知道这套真的有用」：
它不是设想出来的场景，是真的发生过一次，而且触发它的改动**看起来完全无害**。

### 1.2 游戏设计层：结构封死，而非数值劝退 ✅

**一句话版**

> 攻方预算随波数**超线性**增长，所以「低效」不等于「不可行」——任何只靠高定价
> 压住的策略，到高波次都会因规模重新变得可行，**而 RL 一定会找到**。
> 所以凡是不希望 AI 采用的策略，我们都在结构上封死。

**这条是被 RL 的性质逼出来的，不是从设计教科书抄的。**
人类玩家会被"不划算"劝退，RL 不会——它只优化回报。
这个区别就是本条的全部来源，也是「为 RL 对手设计游戏」与「为人类设计游戏」的实质差异。
**这一点讲清楚，本条就从一条设计习惯升级成一条论证。**

| 手法 | 实例 |
|---|---|
| 用**硬性数量上限**而非高价 | 单波单位数硬上限（源自 RL 现实：多智能体微操研究普遍限制在 20–30 单位以内）；空军数量硬上限 |
| 用**伤害为 0** 而非「伤害极低」 | `Phoenix` 对城墙与城门破坏速率**恒为 0** ——「压制墙头 → 破墙 → 缺口攻防」三阶段的中段因此完整保留 |
| 用**机制不可能**而非收益不划算 | 可破坏障碍**不可再生**：可再生 + 破坏产出资源 = 无限资源循环 |
| 用**唯一实现处**而非「记得两处都改」 | `TerrainMasks::buildable` 已与 `no_build` 合成过，调用处不许再 AND 一遍 |
| 用**编译期断言**而非注释 | `static_assert(kMinUnitLevel == 1)`（`rts/roster.hpp`） |

最后一条特别适合讲，因为**它防的是一个未来的、看起来完全合理的改动**：

> 观测里「敌方单位等级」那条通道存的是**和**而不是平均，于是「和 = 0 ⟺ 该格无敌人」，
> 不需要额外的 mask 通道——**而这个设计成立的前提是等级从 1 起**。
> 哪天有人把等级改成 0 基（**数组下标就是 0 基，这个改动非常自然**），
> 那条通道会静默失效：不报错、不崩，AI 只是再也分不出「空地」和「一个 1 级兵」。
> 所以它必须是编译期的。

还有一个更精巧的例子，**它同时属于第 1.1 条**——回放**只存外生输入，不存机制产物**：

> 若把 `kill_unit` 也记进回放流里，重放时就照着流把那个单位杀掉，于是
> **即便战斗解算算出完全不同的结果，回放照样对得上**——测的就变成了它自己的日志。

这是「该红却绿」的教科书形态，而它同样靠**结构**解决：回放格式里根本没有放机制产物的位置。

### 1.3 为什么这两条是同一件事

两条的形状完全一样。**答辩时把这张表画出来，一条创新点就变成了一条方法论**：

| | 代码层 | 游戏设计层 |
|---|---|---|
| 不希望发生的事 | 拿地址当 key 或排序依据 | AI 采用退化策略（无限龟缩、单一兵种海、无成本风筝、早期无脑 rush） |
| 不管用的办法 | 写在文档里「请不要这样」 | 把价格定高一点 |
| 为什么不管用 | 人会忘，而且症状不报错 | RL 不被"不划算"劝退，且预算超线性增长 |
| 我们的办法 | 让它**没有语法形式** | 让它**机制上不可能** |
| 怎么证明有效 | 会红的 ctest + 确定性回放测试 | 成本产出矩阵 + 策略池胜率矩阵 |

---

## 2. 正面回答「类对象设计」：多态落在兵种，不落在每个实体 ✅

这条既是创新点，也是交付要求「描述软件的**类对象设计**」的直接答案。
**它必须与第 1.1 条一起讲**——单独讲第 1.1 条就会滑向「讲成辩解」那种失败形态。

**一句话版**

> 我们的继承体系落在**兵种**上，而不是**每个单位**上：`UnitBehavior` 基类 +
> 11 个兵种派生类，每个兵种一个静态存储期实例，共 11 个。
> **界线不是「用不用虚函数」，而是实例的数量与生命周期。**

**为什么这个落点更对，而不只是一个折中**

- 设计文档自己要求「克制关系由三条正交轴**推导**，不要退化成平铺的 N×N 伤害倍率表」。
  「推导」是**行为**，而「基类给通则 + 派生类只 override 例外」正好让每条例外与它的理由待在同一处
- 全表唯一的 override 在 `Phoenix`：对城墙与城门破坏速率恒为 0。
  **写在它自己的类里，而不是在基类加一个 `if (type == Phoenix)`** ——
  那个 if 会让「为什么」离开它所属的兵种，而这条恰恰最容易被后人当成数值调掉
  （「让不死鸟稍微能拆一点墙」听起来无害，实际废掉三阶段攻防的中段）
- 它**不替换** `roster.hpp` 的 `constexpr` 函数，而是委托给它们。
  替换会丢三样：8 条 `static_assert` 用着它们、无 `default` 的穷举 switch 由
  `/w14062` 与 `-Wswitch` 保证完备、热路径零开销。
  虚函数丢掉的正是第二条保证（**漏一个派生类编译器不管**），
  由 `tests/unit_behavior_test.cpp` 拿 11 个派生类与那些 `constexpr` 函数**互相印证**补回来

**这一层刻意一个数值都不放**，判据写在头文件里：**若某项会随平衡标定而变，它就不属于这里。**
所以血量、伤害、射程、速度、造价、破坏速率一个都不在，只放那些被逐条写成
「结构性决定，不要改」的东西。这条边界本身就是可讲的——它是「机制多样性由兵种承担、
等级只管数值」那条硬约束在代码里的落点。

### 2.1 那次撤回，就是「难点与解决方法」那一页 ✅

**这段比继承图本身更有说服力，因为它展示的是判断力而不是画图能力。**

项目中途有过一次方向变更并被撤回，全程在 issue #64 / PR #63 / PR #65 里留了痕：

1. 先提出把继承落在**兵种**上（11 个静态单例，扁平数组不动），实现并合入
2. 两小时后**自己推翻**，改为教科书写法（`Unit` 基类 + 11 个派生类 + `vector<unique_ptr<Unit>>`），
   写了 823 行，理由是「一张 `Unit → Archer / Spear / …` 的继承图老师一眼就懂」
3. 组内讨论后**撤回第 2 版，回到第 1 版**

撤回的依据是三条可检验的事实，不是口味：

| | |
|---|---|
| **收益没拿到** | 第 2 版实现出来，11 个兵种里 **10 个是 `using ArcherUnit = TypedUnit<…>` 模板别名**，只有 `PhoenixUnit` 真写了 `class … : public`。类图画出来是 `Unit → TypedUnit<Archer, Ranged, Single, LightFast>`，不是 `Unit → Archer`——而那张图是它宣称的唯一收益 |
| **多态面逐格相同** | 第 2 版与被它替换的第 1 版：三轴都返回常量、`can_engage` 都是零 override、`can_break_structure` 都只有 `Phoenix` 一个 override。**代价付了，实质没变** |
| **根因是时机** | 两版都薄，是因为当时还没有可多态的行为——会厚的那些（冲锋助跑加成、溅射对散开阵型的衰减、攻击前摇与转身、对建筑特攻）全在机制层，尚未实现 |

**答辩讲法**：这一页讲的不是「我们选对了」，而是
**「我们用可检验的判据推翻了自己的方案，两次」**。
配一张两版对照表，比任何架构图都更像工程。

### 2.2 顺带解决的一件事

`交付要求的工程影响.md` 曾挂着**唯一**一条需要向老师确认的事项：
「系统层为类、实体为扁平数组，是否满足『类对象设计』的要求」。
它已随 `UnitBehavior` 那一层划掉——报告里现在有一套真实的
`基类 + 11 个派生类` 继承体系，虚函数、`override`、多态分派都在核心逻辑里，
而不是为报告造的壳子。

**答辩时能回答第二问「仿真里真的用了吗」**：`can_engage` 与
`can_break_structure` 是目标合法性判定的**唯一实现处**，战斗解算落地时走的就是它。

> ⚠️ **注意时态。** 战斗解算属 1c，撰写本文时尚未实现。
> 准确的讲法是「它是战斗解算落地时唯一的目标合法性判定入口」——
> 这是真的；而「战斗解算就走它」承诺了一段现在还不存在的代码。
> **答辩前照第 6 节复核这一条。**

---

## 3. 其余四条创新点的「讲准」版

| # | 讲泛的说法 ❌ | 讲准的说法 ✅ | 证据 | 状态 |
|:-:|---|---|---|:-:|
| 1 | 「我们的 AI 用了强化学习」 | 「敌方不是脚本，是**逐单位**由一个共享策略网络驱动的。我们最直接的对标产品被评测明确批评『**敌人无视已有缺口、反复撞击加固过的城墙，明摆着的弱点被浪费**』——那是脚本 AI 的典型失效，而『AI 是否发现并利用已有缺口』正是我们的**显式评估指标**」 | 双视图演示 + 评估指标 | ⚠️ |
| 2 | 「有战争迷雾」 | 「AI 的观测是**它自己的迷雾状态**，不是 ground truth，所以玩家可以主动骗它——等它的斥候走了再造防空塔。演示时并排放两张图：真实战场 vs AI 的记忆图，观众能**看到**它的记忆里那段墙是破的、于是它调头去打那里」 | `FogLayer` 每侧一份、`Vis` **三态** | 迷雾 ✅ / 双视图 ⏳ |
| 3 | 「攻守双方都是 AI」 | 「训练时守方也由 RL 控制，但**只在决策层**：攻方 RL 控制每一个单位，守方 RL 只下玩家会下的那些指令（建造 / 经济 / 造兵 / 维修 / 调兵），**守方单兵由参数化脚本驱动**。这是刻意的不对称——守方 AI 是人类玩家的替身，它的动作空间应当等于玩家实际能下的指令」 | 设计文档 + 训练曲线 | ⚠️ |
| 4 | 「我们的游戏策略很丰富」 | 「我们用策略池两两对战的胜率矩阵**实验性地证明**策略空间有深度：若矩阵里出现 A 克 B、B 克 C、C 克 A 的**非传递性环**，就说明不存在唯一最优策略。因为守方也是 RL、双方协同演化，这张矩阵从『攻方策略之间』扩展为『攻守各代之间』」 | `tools/` 离线脚本 | ⚠️ |

三条讲法上的细节，都很小但很能说明"想清楚了"：

- **第 2 条的迷雾是三态不是两态**（从未侦查 / 记忆中 / 当前可见）。两态的话
  「从未侦查过」与「记忆里那段墙是破的」**不可区分**，而后者正是要演示的东西
- **第 3 条的不对称不是省事**，它从结构上封死了一个坑：守方若也逐 tick 微操，
  就会打出人类做不到的操作，而攻方会过拟合到那些操作上
- **第 4 条不进演示，只进报告。** 判据是「不需要解释术语就能看懂」，
  而「非传递性环」要先解释。同理显著性热力图也只用于分析

---

## 4. 讲稿

### 4.1 三分钟版（可直接念）

> 我们做的是一个不对称的波次生存 RTS：玩家守城，进攻方由强化学习驱动。
> 差异化的地方不是"有 AI"，而是**进攻方的每一个单位都由策略网络控制**，
> 不是脚本。我们最直接的对标产品被评测批评过一句话——"敌人无视已有缺口，
> 反复撞击加固过的城墙"，那是脚本 AI 的典型失效，也是我们的核心评估指标之一：
> **AI 会不会发现并利用已经存在的缺口**。
>
> 但今天我想讲的创新点不在游戏本身，而在**我们解决问题的方式**。
> 我们全项目只用一种手法：**不靠纪律、也不靠劝退，而是让不希望发生的事
> 在代码里没有语法形式。**
>
> 举第一个例子。RL 训练必须可复现，所以我们主要的防 bug 手段是**确定性回放测试**：
> 存一个随机种子加一串指令，重放必须逐 tick 状态一致。
> 这个测试最常见的破坏源是**遍历顺序取决于内存地址**——比如用对象指针当哈希表的键。
> 它的可怕之处是**不崩、不报错**，只是两次运行结果不一样；
> 而症状表现为"训练不收敛"，于是人会去查网络、查超参、查奖励函数，
> **不会去查存储层**。
>
> 所以我们不给它留语法形式：实体存在扁平的连续数组里，跨 tick 引用一律是
> "索引 + 代数"的整型句柄，而且三种句柄在**编译期**互不相通，传错一组编不过。
> 这样一来，那类 bug 不是"我们注意不要写"，是**写不出来**。
>
> 第二个例子在游戏设计这一侧，形状完全一样。攻方的预算随波数**超线性**增长，
> 所以"低效"不等于"不可行"——任何只靠把价格定高来压住的策略，
> 到高波次都会因为规模重新变得可行，**而 RL 一定会找到**。
> 人类玩家会被"不划算"劝退，RL 不会，它只优化回报。
> 所以凡是我们不希望 AI 采用的策略，一律在**结构上**封死：
> 用硬性数量上限而不是高价，用伤害为零而不是"伤害极低"，
> 用机制上不可能而不是收益不划算。
>
> 这两件事是同一个方法论的两次应用，而这也决定了我们的类设计。
> 我们的继承体系落在**兵种**上而不是每个单位上：一个 `UnitBehavior` 基类加
> 11 个兵种派生类，每个兵种一个静态实例。
> 界线不是"用不用虚函数"——虚函数本身是确定的——而是**实例的数量与生命周期**：
> 11 个长生命周期的兵种单例，不是每个单位一次堆分配。
>
> 这个落点我们不是一次选对的。中途有人提出改成教科书写法，
> 给每个单位一个对象，写了八百多行，理由是"那张继承图老师一眼就懂"。
> 我们撤回了它，依据是三条可检验的事实：
> 收益没拿到——11 个类里 10 个其实是模板别名；
> 多态的实质一格都没变——两版都只有一个真正的 `override`；
> 根因是**时机**——真正需要按兵种分支的行为都在机制层，那时还没写。
> **这一页我们放在"难点与解决方法"里，因为它展示的不是我们选对了，
> 而是我们会用可检验的判据推翻自己的方案。**

### 4.2 一分钟版（时间被压时用）

> 我们做的是玩家守城、进攻方由强化学习逐单位控制的波次生存 RTS。
>
> 创新点是一条方法论：**让不希望发生的事在代码里没有语法形式。**
> 因为 RL 训练要可复现，我们主要的防 bug 手段是确定性回放测试，
> 而它最常见的破坏源——拿对象地址当哈希表键——**不崩不报错，只表现为训练不收敛**，
> 人根本不会往存储层去查。所以我们不靠"注意不要写"，
> 而是让实体只存在扁平数组里、只用整型句柄引用，那类 bug 写不出来。
>
> 同一个手法用在游戏设计上：攻方预算超线性增长，"低效"不等于"不可行"，
> RL 不会被不划算劝退，所以退化策略一律在**结构上**封死，不靠定价。
>
> 类设计跟着这条线走：继承落在**兵种**上（11 个静态单例），不落在每个单位上。
> 这个落点我们中途推翻过一次自己的方案又撤回，过程在"难点与解决方法"那页。

### 4.3 高频追问 × 答法

| 追问 | 答法 |
|---|---|
| **「这不就是 ECS 吗？」** | ECS 的主要动机是性能（cache 局部性、批处理）。我们的**主要**动机是确定性，性能是附带收益。所以我们没有做 ECS 的那一套——没有 system 调度器、没有 component 注册表、没有 archetype——只取了「实体存在扁平数组里、用整型句柄引用」这一条。**动机不同，取的子集也不同。** |
| **「堆分配会破坏确定性吗？」** | 不会。`vector<unique_ptr<Unit>>` 按索引遍历的顺序等于插入顺序，与地址无关。我们避开它**不是因为它本身不确定，而是因为它让那类错误变得可写**。这个区分我们写在设计文档里。 |
| **「虚函数不确定吗？」** | 也确定，vtable 调用是确定的。虚函数的代价是性能与内存布局——vptr、堆分配与指针间接、无法内联，与确定性无关。**所以我们并没有禁虚函数**，我们的兵种层就是虚函数。 |
| **「用了扁平数组就一定确定了吗？」** | 不。若在别处用 `unordered_map<int, State>` 驱动仿真遍历，一样会坏。这条约束**消除的是一类错误，不是全部**——剩下的靠那个会红的守卫脚本和回放测试。 |
| **「你们怎么知道这套真的有用？」** | 它真的救过一次。有一次改动只是往一个枚举末尾加了一个成员，而那个枚举恰好是一个进哈希的数组的长度，于是所有旧回放失效。我们给状态哈希留了一个**口径标签**，所以它报的是「这份回放是用旧口径录的，重录」，而不是「第 0 tick 状态不一致」——**后者会让人去查一个不存在的缺陷。** |
| **「这个继承体系解决了什么问题？」** | 让每条**结构性例外**与它的理由待在同一处，而不是散在一张 N×N 表里——那是我们设计文档自己的要求（克制关系要由三条正交轴推导，不要退化成平铺表）。具体例子：不死鸟对城墙破坏速率恒为 0，这条写在它自己的类里 |
| **「11 个类换 1 个 override，值得吗？」** | **现在**只有 1 个，因为机制层还没写；会厚的那些都在机制层——冲锋助跑加成、溅射对散开阵型的衰减、攻击前摇。这个问题我们内部辩过，而且**正是它让我们撤回了另一版方案**：那一版付了更大的代价拿到同样的 1 个 override |
| **「为什么不给每个单位一个对象？」** | 因为设计上**刻意不存在**「同兵种两个个体行为不同」——等级只改血量与伤害绝不改机制、没有兵种专属动作、机制性克制必须等级无关。逐单位多态没有东西可承载。我们试过一版，11 个类里 10 个是模板别名 |
| **「你们的类图在哪？」** | 三层：`UnitBehavior` + 11 个兵种派生类（`rts_core`）；系统层的 `World` / `Pathfinder` / `CombatSystem` / `VisionSystem` / `BatchedEnv`；前端 `game/` + `render/` 的 `MapLoader` / `SceneModel` / `WorldBuilder` / `SpriteAtlas` / `SceneRenderer` / `CameraController` 等 |
| **「为什么不用游戏引擎？」** | 课程要求核心逻辑是 C++，这封死了引擎最省力的用法（用它的脚本语言写逻辑）。我们只能把 C++ 编译成原生插件，而那恰是引擎最别扭的用法；更实际的是**Windows 独占锁定已加载的 DLL**，编辑器开着就没法重编。仿真全部自研，引擎的物理、碰撞、场景图、内建 ECS 我们一概不能用——**用了反而破坏确定性与 headless 要求**。剩下的只有渲染、输入、UI 与打包，而那些 raylib 与 ImGui 已经覆盖 |
| **「难度会随玩家表现调整吗？」** | **不会，这是刻意的。** 攻方预算严格是波数的函数，与玩家表现无关；适应只改**风格**（兵种配比、打哪个目标），绝不改**强度**。若难度可因表现下调，知情玩家会故意前期打烂来压低后期难度——**那会把我们的核心卖点变成可套利的漏洞** |

---

## 5. 讲错的版本（对照检查）

写 PPT 与练讲时逐条对一遍。左边这些**不要说**：

| ❌ 不要说 | 为什么 | ✅ 改成 |
|---|---|---|
| 「我们用扁平数组，性能更好」 | 40 个单位的规模下这不是瓶颈，会被追问穿 | 「主要理由是确定性，性能是附带收益」 |
| 「我们没用继承，因为要保证确定性」 | 主语是没做的事；**而且事实上我们用了继承** | 「我们的继承落在兵种上，不落在每个实体上」 |
| 「我们用了 ECS 架构」 | 不准确，且引出一堆我们没做的东西 | 「我们只取了 ECS 的一条：实体在扁平数组里、用整型句柄引用」 |
| 「面向对象会破坏确定性」 | 明显错。内存布局与是否面向对象**正交**，一个类完全可以封装扁平数组 | 「破坏确定性的是拿地址做决策，不是面向对象」 |
| 「虚函数会破坏确定性」 | 错，vtable 调用是确定的 | 「虚函数的代价是性能与布局，与确定性无关」 |
| 「我们的 AI 会自己学会所有战术」 | 无法验证的承诺 | 「克制二部图里写死的战术行为进脚本，图里没写的（包夹、佯攻、择机）才留给 RL 涌现」 |
| 「AI 会根据玩家水平调整难度」 | **与我们的设计正相反**，且是可套利的漏洞 | 「适应只改风格，绝不改强度」 |
| 「我们的游戏平衡性已经调好了」 | 全部数值待标定，这是明写的团队纪律 | 「我们建了成本产出矩阵作为标定依据，数值统一倒推」 |
| 「回放测试保证了仿真没有 bug」 | 过强。它保证的是**可复现**，不是正确 | 「它把一类最难查的 bug——不可复现——变成会红的测试」 |

**一条通则**：凡是句子的主语是「我们没有 X」，都改写成「我们用 Y 保证了 Z」。

---

## 6. 答辩前必须复核的清单

**每一条都要在答辩前重新核一次**，因为本文写于 2026-08-29，而机制层（1c）当时一行都没有。
**核的办法是能不能当场指出证据**；指不出的，降级为「设计目标」，不要说成成果。

| # | 讲法 | 现在 | 复核什么 |
|:-:|---|:-:|---|
| 1 | 数据布局让一类 bug 写不出来 | ✅ | 无需复核，代码与守卫都在 |
| 2 | 守卫会红、且自己有自检 | ✅ | 跑一次 `--self-test` 与那 5 条 `WILL_FAIL` |
| 3 | 哈希口径标签救过一次 | ✅ | 无需复核，`World/1 → World/2` 在 git 历史里 |
| 4 | 结构封死的五个实例 | ✅ | 无需复核 |
| 5 | 继承落在兵种上、11 个派生类 | ✅ | 无需复核 |
| 6 | 「战斗解算走 `can_engage`」 | ⏳ | **1c 落地了吗**。没落地就只能说「它是唯一的目标合法性判定入口」 |
| 7 | 确定性回放测试是主要防 bug 手段 | ⏳ | 1c 之前，回放验的是「输入 → 状态」的确定性，**还没有战斗解算可验**。讲的时候别暗示它已经在防机制的 bug |
| 8 | 双视图（真实战场 vs AI 记忆图） | ⏳ | `render/` 做了吗。**这一项不可砍**，它同时是核心评估指标的现场证据 |
| 9 | 「AI 发现并利用已有缺口」 | ⚠️ | 训练跑起来了吗、有没有录到那一局 |
| 10 | 攻守协同演化 | ⚠️ | 到了上线顺序的第几步 |
| 11 | 非传递性环 | ⚠️ | 矩阵跑出来了吗、**真的有环吗**。没有环就不要说这条 |
| 12 | 涌现行为录像 | ⚠️ | 有几段。**意料之外的行为比清单内的更有力**，训练期发现时当场记录 |

> **第 11 条要特别小心**：非传递性环是一个**实验结果**，不是一个设计承诺。
> 矩阵跑出来没有环，那就说「我们建了这个工具，目前观察到的是……」，
> 不要说成"我们证明了策略空间有深度"。

---

## 附：这份文档自己也遵守它讲的那条

写本文时核了三处断言，**三处各查出一个问题**，都已修正——
记在这里是因为它们正是本文要防的形态，而且**三处全都是「读起来完全合理」的那种错**：

| # | 我写的 | 核出来是 | 怎么修 |
|:-:|---|---|---|
| 1 | 「`range()` 不接受 level 参数，于是『射程随等级变』在类型层面写不出来」（PR #65 评论） | **`UnitBehavior` 里根本没有 `range()`。** 那句来自 issue #64 的**提案草图**，而落地版刻意**一个数值都不放** | 已在 #65 更正，本文不采用这条讲法 |
| 2 | `ObstacleInit` 的注释：「`World` 没有任何『造一个障碍』的入口，于是『可再生』这件事写不出来」 | **`place_obstacle()` 当时是公开的**，那条保证只是一条约定，**而注释读起来像是结构保证** | **改代码不改注释**：`place_obstacle` 改私有，只有构造函数调它；受影响的用例改走 `WorldInit`；破坏性验证确认外部调用报 `C2248` |
| 3 | 「那 11 个兵种单例必须无状态，可以 `static_assert(std::is_empty_v<…>)`」 | **`is_empty_v` 对任何多态类恒为 false**（虚函数带来 vptr）。照那么写会得到一条**永远失败**的断言——「该红却绿」的镜像，一样没用 | 改用 `sizeof(派生) == sizeof(UnitBehavior)`（基类只有一个 vptr、无尾部填充，比对是紧的），已加进 `unit_behavior.cpp`。破坏性验证：加一个 `int` 字段 → `C2338` |

三条的共同点值得单独说：**它们全都不是「不知道」，而是「没核」**。
第 1 条来自记忆里的提案草图、第 2 条是自己两小时前写的注释、
第 3 条是一个用得太顺手以至于没查文档的类型特征。

而这正好是本文第 0 节那条判据的来源：**「当场能指出证据」不只是给老师看的，
更是给自己用的**——凡是说不出证据在哪一行的讲法，八成没核过。

第 2 与第 3 条都是本文主线的自我应用：
- 第 2 条 —— 发现一处「文档声称是结构、实际是约定」，**修的是代码**
- 第 3 条 —— 把一条只写在注释里的纪律**变成编译期会红的检查**，并验证它真的会红
