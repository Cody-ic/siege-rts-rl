# RL 阶段交接：已暂停，未替换正式模型

用户因额度限制要求暂停。训练和评估进程已停止；本次仅整理、提交现有成果。
后续训练、评估和自动推进须等待用户明确恢复指令。PR 可用于审查已实现的功能，不能据此宣称 RL 已完成或策略已稳定改善。

## 已实现与验证

- 攻方可恢复 PPO、多地图/等级/混合兵种训练、真实守方及准备阶段、固定参考策略与稳定 KL。
- 经济任务推理合同 v2 与旧 v1 兼容；固定袭扰编队及队长继承，真实游戏目标、模型加载、快照和日志恢复验证。
- 守方宏观 BC/PPO、原生推理、可恢复采样，以及完整对局结束后的冻结对手轮换。
- 攻方陪练新增均衡、城防、机动三种真实守方风格；修复风格与入口/等级取余周期绑定。
- 最新 38 项 PPO 运行时测试、4 项风格专项测试通过；默认守方 480 次世界哈希对照一致。
  3072 个采样编号覆盖 168/168 个地图×入口×等级×风格组合，这是分配覆盖，不是完成了 3072 局。
- 冻结对手轮换的 3 项调度、8 项宏观训练回归通过；真实脚本/v1、脚本/v2 轮换恢复检查通过。

证据与历史实验详见 [验证记录](rl-stability-and-deployment.md)。这些是分项验证，当前 PR 未返回 CI 检查记录，不能写成 CI 全绿。

## 策略质量结论

| 实验 | 原模型→候选 | 结论 |
|---|---|---|
| 攻方 shared update64，四新种子单波复测 | 35→41/64 胜；建筑价值 23555→26415；自损 195387→163297 | 单波汇总门槛通过，但一级胜局 14→12 |
| 同一攻方候选，实际六波游戏 | 攻破城市 8→7/8；守方累计守过波数 21→30 | 四组波数退步、四组相同，不采用 |
| 教师纠正、独立价值网络 | 开发对照未改善 | 失败结果保留，不设为默认 |

攻方跨波对照包含旧 v1→新 v2 的目标接入变化，不能全部归因于权重。
原模型攻破率已达上限，预定“更多攻破”条件无法超越；候选在两项结果上均退步，结论不依赖此门槛缺陷。

## 守方轮换训练与暂停结果

固定预算训练已完成 64 次更新、8192 次宏观决策、163540 tick；16 个完整城市对局覆盖四图×三对手全部 12 种组合。
最终模型固定为 update64，没有根据评估回选检查点。

[暂停评估归档](rl-results/2026-09-09-macro-league-paused.json)核验计划输入和报告身份，保存全部 31 个已完成结果及 17 个缺失案例。
以下仅汇总完成的配对，不能混合未配对结果比较：

| 攻方陪练 | 完整配对/计划 | 原守方→候选累计守过波数 | 六波完成数 |
|---|---|---|---|
| 脚本 | 8/8 | 9→4 | 0→0 |
| 保留 v1 RL | 7/8 | 21→23 | 0→0 |
| 实验 v2 RL | 0/8 | 尚无数据 | 尚无数据 |

候选对脚本攻方退步，已不符合“每类对手均不低于原模型”的采用门槛。
其余对手尚未完整评估；不宣称轮换训练带来整体收益，也不替换正式模型。

## 保留与恢复位置

- 当前正式 `play.bat` 仍默认使用脚本；本 PR 不部署实验候选。
- 守方最终权重：`runs/macro-league-20260909/frozen-update64.pt`，SHA256 `01e685c1602ee3b275c3b74a383c28f62f8e917ad4da625cdfa020b0255e35de`。
- 已完成评估：`runs/macro-league-evaluation-20260909/`；[冻结评估计划](rl-results/2026-09-09-macro-league-evaluation-plan.json)。
- 权重、原生构建和试玩包本地保留，未随本 PR 提交；可核验结果、计划和代码已入库。
- 收到恢复授权后，先核验计划及已完成报告，只补缺失案例。现有本地执行脚本会拒绝已存在输出，需要先加入已验证结果跳过逻辑，不能整批重跑。
- 旧训练目录必须使用其原源码与原生模块恢复；不要放宽合同校验。攻方局中中断会重建未完成环境，不等于原生世界无损续接。

待完成：剩余冻结评估、三风格攻方训练及泛化验证、完整协同演化、攻方宏观学习、完整长局与最终交付验证。
