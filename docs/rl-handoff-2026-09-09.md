# RL 临时交接给 Cody-ic：本地暂停，未替换正式模型

用户因额度限制要求本地暂停，并明确要求通过 PR #163 将整个未完成任务临时交给 **Cody-ic** 继续跟进。训练和评估进程已停止；本地不自动恢复。
交接负责人为 Cody-ic，接手范围包括结果审查、训练与评估、游戏接入和最终验收。历史归档中的“等待恢复授权”记录的是当时的本地暂停状态，不阻止此次明确委托的队友接手。PR 不代表 RL 已完成或策略已稳定改善，也不自动批准实验模型上线。

## 接手顺序与完成条件

1. 先审查下方失败结论及 `docs/rl-results/` 的冻结计划，保留原模型和脚本默认入口。不要因训练奖励或单波胜率上升而部署候选。
2. 核对下方交接材料。源码、结果与计划已在 PR；模型及冻结运行环境仍需从原工作机另行取得。只拉取 PR 不能精确续跑旧实验。
3. 若取得完整原环境，核验冻结计划的全部文件摘要和模拟指纹，再按暂停归档的 `missing` 清单补 17 个案例。已完成 31 个案例不得覆盖；既有候选对脚本退步，不应为让门槛通过而重新挑检查点或修改评估条件。补齐用于诊断，不是当前候选上线的前提补丁。
4. 优先分析攻方真实多波退步、守方对脚本退步的原因；每次只改变一个主要因素。三种守方风格的实现已验证，但尚未进行三风格训练。新实验使用独立目录、预先固定种子/预算/采用门槛，并保留旧权重作为对照。
5. 按真实引擎训练路线推进：攻方战术对多种守方 → 守方宏观及冻结对手轮换 → 完整协同演化 → 攻方宏观。先得到稳定泛化结果，再扩大训练规模。
6. 最终交付须包含独立留出地图/种子的完整配对评估、逐案例退步记录、导出后真实游戏验证、跨启动存档恢复和长局表现。Python 采样与原生采样分别验证，未测到的波数不宣称已掌握。

请在 PR 下记录接手状态、运行环境、下一份冻结计划和结果链接；资源不足或缺失文件时先明确缺项，不以新环境结果冒充旧实验续跑。共用服务器其他项目的 GPU 任务不得中止或抢占。

## 已实现与验证

- 攻方可恢复 PPO、多地图/等级/混合兵种训练、真实守方及准备阶段、固定参考策略与稳定 KL。
- 经济任务推理合同 v2 与旧 v1 兼容；固定袭扰编队及队长继承，真实游戏目标、模型加载、快照和日志恢复验证。
- 守方宏观 BC/PPO、原生推理、可恢复采样，以及完整对局结束后的冻结对手轮换。
- 攻方陪练新增均衡、城防、机动三种真实守方风格；修复风格与入口/等级取余周期绑定。
- 最新 38 项 PPO 运行时测试、4 项风格专项测试通过；默认守方 480 次世界哈希对照一致。
  3072 个采样编号覆盖 168/168 个地图×入口×等级×风格组合，这是分配覆盖，不是完成了 3072 局。
- 冻结对手轮换的 3 项调度、8 项宏观训练回归通过；真实脚本/v1、脚本/v2 轮换恢复检查通过。

证据与历史实验详见 [验证记录](rl-stability-and-deployment.md)。这些是分项验证，当前 PR 未返回 CI 检查记录，不能写成 CI 全绿。

## 策略质量结论

| 实验 | 原模型→候选 | 结论 |
|---|---|---|
| 攻方 shared update64，四新种子单波复测 | 35→41/64 胜；建筑价值 23555→26415；自损 195387→163297 | 单波汇总门槛通过，但一级胜局 14→12 |
| 同一攻方候选，实际六波游戏 | 攻破城市 8→7/8；守方累计守过波数 21→30 | 四组波数退步、四组相同，不采用 |
| 教师纠正、独立价值网络 | 开发对照未改善 | 失败结果保留，不设为默认 |

攻方跨波对照包含旧 v1→新 v2 的目标接入变化，不能全部归因于权重。
原模型攻破率已达上限，预定“更多攻破”条件无法超越；候选在两项结果上均退步，结论不依赖此门槛缺陷。

## 守方轮换训练与暂停结果

固定预算训练已完成 64 次更新、8192 次宏观决策、163540 tick；16 个完整城市对局覆盖四图×三对手全部 12 种组合。
最终模型固定为 update64，没有根据评估回选检查点。

[暂停评估归档](rl-results/2026-09-09-macro-league-paused.json)核验计划输入和报告身份，保存全部 31 个已完成结果及 17 个缺失案例。
以下仅汇总完成的配对，不能混合未配对结果比较：

| 攻方陪练 | 完整配对/计划 | 原守方→候选累计守过波数 | 六波完成数 |
|---|---|---|---|
| 脚本 | 8/8 | 9→4 | 0→0 |
| 保留 v1 RL | 7/8 | 21→23 | 0→0 |
| 实验 v2 RL | 0/8 | 尚无数据 | 尚无数据 |

候选对脚本攻方退步，已不符合“每类对手均不低于原模型”的采用门槛。
其余对手尚未完整评估；不宣称轮换训练带来整体收益，也不替换正式模型。

## 保留与恢复位置

- 当前正式 `play.bat` 仍默认使用脚本；本 PR 不部署实验候选。
- 守方最终权重：`runs/macro-league-20260909/frozen-update64.pt`，SHA256 `01e685c1602ee3b275c3b74a383c28f62f8e917ad4da625cdfa020b0255e35de`。
- 已完成评估：`runs/macro-league-evaluation-20260909/`；[冻结评估计划](rl-results/2026-09-09-macro-league-evaluation-plan.json)。
- 权重、原生构建和试玩包本地保留，未随本 PR 提交；可核验结果、计划和代码已入库。
- Cody-ic 接手后，先核验计划及已完成报告，只补缺失案例。现有本地执行脚本会拒绝已存在输出，需要先加入已验证结果跳过逻辑，不能整批重跑。
- 旧训练目录必须使用其原源码与原生模块恢复；不要放宽合同校验。攻方局中中断会重建未完成环境，不等于原生世界无损续接。

待完成：剩余冻结评估、三风格攻方训练及泛化验证、完整协同演化、攻方宏观学习、完整长局与最终交付验证。

## 尚需另行移交的本地材料

原工作目录为 `C:\Users\zhuozhuo233\Desktop\cpp-class-design`。以下路径均相对此目录；它们未上传 GitHub，也不能假设共用服务器已有相同文件。请联系本 PR 发起方取得材料。

| 材料 | 路径 / 用途 |
|---|---|
| 保留攻方权重及 v1 导出 | `runs/prepared-final-20260909/`，包括 `candidate.pt` 和 `export/attacker.onnx` |
| 被拒绝的攻方候选及 v2 导出 | `runs/critic-isolation-shared-20260909/`，包括 `frozen-update64.pt` 和 `export-v2/attacker.onnx`；作为冻结陪练和诊断对照，不是部署推荐 |
| 原守方及其原生导出 | `runs/macro-joint-20260909/`，包括 `frozen-update32.pt` 和 `export-native-v2/` |
| 轮换训练完整目录 | `runs/macro-league-20260909/`，包含最终 `frozen-update64.pt`；此候选尚未导出原生模型 |
| 原始逐局报告 | `runs/macro-league-evaluation-20260909/`；31 个报告内容及各自 SHA256 已同时归档到 PR |
| 冻结评估原生模块 | `build-league-onnx/bindings/Release/`，Python 3.13 Windows x64 模块及 ONNX Runtime DLL；模拟指纹 `7c46c04e8a23557f240825238a2fd941506719035b45c5abaa1b3d50c8f5f6ca` |
| 原 Python 环境 | `.review/torch-env/`，Python 3.13.5、PyTorch 2.14 CPU、NumPy 2.5.2；环境目录不能简单跨系统拷贝后视为可用 |
| 原实验辅助脚本 | `.review/evaluate_macro_league.py`、`.review/archive_paused_league.py`、`.review/macro_league_trial.py`、`.review/freeze_macro_league.py` |

冻结评估计划 `files` 字段是模型、原生模块、源码、地图和数值文件摘要的权威清单。原评估执行脚本的历史副本见 [evaluation-worker.py.txt](rl-handoff/evaluation-worker.py.txt)，仅供审查执行顺序与恢复改造，不是即取即用的跨平台启动器：其中有原机路径，`prepare` 会创建计划，评估分支遇到已有报告会拒绝继续。不要重新生成或覆盖原计划。

如果只能在 Linux 服务器或新构建上继续，应另建实验计划并记录环境差异，重新做双方成对对照；不要更改旧计划摘要后将新结果并入旧 31 局。确需迁移旧权重时使用训练入口明确支持的权重初始化流程，不能取消源码/模拟合同校验或称为优化器与世界状态无损恢复。通用构建、训练和评估入口见 [train/README.md](../train/README.md)。
