# RL 续训稳定性与游戏接入（2026-09-08）

2026-09-09 联合陪练入口已贯通：Python 完整对局现在可复用已冻结的攻方 ONNX，
训练记录及评估报告都包含对手身份。实测战斗结果与脚本对手不同，状态分支一致，
两次小更新的连续训练/恢复训练参数及对局哈希一致，换对手续训被拒绝。
证据：[冻结对手集成验证](rl-results/2026-09-09-macro-frozen-opponent.json)。
这只证明真实模型陪练及恢复链路成立，未证明守方战斗能力提升。
旧宏观权重已按显式权重迁移方式导入新训练目录，原始文件保留；36 个局面在旧/新绑定下
观测、合法命令、决策和值输出、对局哈希完全一致，证据见
[迁移对照](rl-results/2026-09-09-macro-transfer.json)。
四地图联合训练已完成 32 次更新 × 128 决策、81776 tick，攻方固定为已交付的 prepared 模型；
训练前后使用同一开发集（地图 7/9，种子 101/102，6 波上限）比较。这些局面已有调参历史，
不能称作全新独立留出集；后续仍需冻结候选再做独立评估。
训练前开发集结果为 [3,3,3,4] 波（平均 3.25），最终模型为 [4,2,6,2]（平均 3.5）：
两局改善、两局退步，六波完成数由 0/4 到 1/4，不能认为退化问题已经解决。
完整证据见 [联合训练开发集对照](rl-results/2026-09-09-macro-joint-development.json)。
两模型已冻结，并在启动独立评估前保存
[地图 2/3、种子 301/302 的计划](rl-results/2026-09-09-macro-joint-holdout-plan.json)。
独立评估完成：原模型 [3,4,2,2]，联合训练最终模型 [3,4,4,2]，平均 2.75→3.25 波，
一局改善、三局持平，无退步，六波完成率均为 0/4。
[完整计划核对与逐局证据](rl-results/2026-09-09-macro-joint-holdout.json) 保留两个模型的内容身份。
这是四个配对局面的初步正向结果，不是统计显著性证明，也未解决开发集上的两局退步。
保留最终模型作为联合陪练候选，原示范模型继续保留；不得称作已掌握长期守城或 70 波。
联合训练成效与宏观模型游戏运行时接入仍未完成。

额外移除了宏观 PPO 每步重复的下一状态价值编码，复用下次决策值、仅在批次末尾补算。
两次更新对照中，三场完整败局的记录、模型、优化器、固定参考和随机状态均与原实现一致；
证据见 [价值推理复用](rl-results/2026-09-09-macro-bootstrap-reuse.json)。
这证明本次重复计算可移除，但单次并发运行计时不足以证明稳定的整体训练提速。

本轮先解决已有突破能力在 PPO 续训中退化的问题，再补训练范围与部署接口。旧服务器权重、此前示范初始化及对照目录均保留。工作在本地 CPU 上完成，未干扰共用服务器其他项目。

## 奖励与训练更新

一个编队阵亡时，它的 GAE 链终止，但原先共享的势函数奖励仍保留其他存活编队的下一状态势能。其折扣回报因此多出一个与阵亡时刻有关的边界项，不再只是固定的初始势能偏移。四局真实轨迹中记录到 14 次这样的提前终止；其中一例残留项为 −0.2638448973。

现在为每个编队定义终止状态势能为零：阵亡且世界尚未结束时，从该编队的最后一步扣除 `gamma × 加权下一状态势能`；普通任务奖励保持原样，存活编队在 rollout 边界继续 bootstrap。终局、阵亡、行压缩和存活跨 rollout 的测试均覆盖。这是公式正确性修复，不能仅凭它断言所有训练退化都已解决。

另加两个显式选项：

- `--value-features detached`：价值损失只更新价值输出层，避免未训练好的价值头直接改动策略共享特征。它不是完整的 PPG 算法。
- `--reference-coef 1`：对已初始化的策略加入 `KL(冻结参考策略 || 当前策略)` 约束。参考模型随完整检查点一起保存；删除原始初始化文件后仍能恢复。新实验必须明确提供初始化权重，不能把随机网络悄悄当作可靠参考。

两个选项默认关闭。加入选项、尚未修改奖励公式时，与 v2 学习器做了一次真实 1,024 步更新对照，12 个模型张量逐一完全相等。之后的奖励边界修正会有意改变训练结果，并提升学习器合约版本。

参考动机：[策略与价值特征相互干扰的 PPG 论文](https://proceedings.mlr.press/v139/cobbe21a/cobbe21a.pdf)、[示范策略正则化研究](https://deepmind.google/research/publications/41182/)。这里采用的具体损失和默认参数仍需由本项目实验验证。

## 一级亡灵步兵的完整距离复测

全部从此前 `flow_breach` 示范模型初始化，各组训练 32,768 env-step，经济奖励、真实脚本守方、16 个环境、CPU。原先默认 PPO 的两图四种评估全部为 0/32 胜；仅分离价值梯度的小样本对照仍为 0/8 胜。加入参考约束后能保留已有突破能力。

修正奖励边界并采用分离价值梯度 + 参考系数 1 后，冻结模型在每张图、每种选动作方式各评估 32 局：

| 地图 | 最大概率动作胜局 | 按概率采样胜局 | 有建筑接触 |
|---|---:|---:|---:|
| `gen_01001000`（训练图） | 16/32 | 17/32 | 两种均 32/32 |
| `gen_01002000`（诊断图） | 6/32 | 15/32 | 两种均 32/32 |
| `gen_01003000`（第三图） | 7/32 | 8/32 | 两种均 32/32 |

示范初始化对应胜局为 16/17、6/14、7/8；本轮结果支持“续训保住已有能力”，不构成统计显著提升的证据。第三图的确定性策略从 26/32 建筑接触恢复到 32/32，但仍有入口只能造成很少伤害。三图已经用于诊断，后续不能再称为从未见过的最终测试集。

原始运行：`runs/stability-pilot-20260908`（修正前对照）、`runs/terminal-pilot-20260908`（修正后对照与完整复测）。每个报告保留模型、地图、数值表和评估源码哈希，以及逐局种子、入口和结局。新混兵训练另外使用 `runs/mixed-pilot-20260908`。

仓库内保存 [22 份报告、320 局的复核数据](rl-results/2026-09-08-stability.json)，包含全部逐局结局与战果；较大的逐步轨迹留在上述原始目录，复核数据记录对应文件哈希。

## 多地图、混兵、等级

`--map-pool` 接受有序的逗号分隔地图路径。地图由世界种子对池大小取模选择；守方陪练使用同一规则，在每次换局时重新绑定正确地图。测试将混合批次与逐图独立运行的观测逐项对比，并覆盖同一槽位换成另一张地图。

`--roster mixed` 使用 3 队亡灵步兵、2 队亡灵法师、2 队鬼域骑士、1 队攻城锤、1 队不死鸟。每队人数直接读取游戏定义；幽影窥使的侦查仍由脚本负责。`--levels 1,4,8,16` 控制初始等级，单位满血与等级缩放一致。地图、入口、等级按交叉组合遍历，避免相同取模把一张地图永远锁在某个入口或等级。

```bash
python train/demonstrations.py collect --out-dir runs/mixed/data \
  --map-pool game/data/maps/pool/gen_01001000.json,game/data/maps/pool/gen_01004000.json \
  --roster mixed --levels 1,4,8,16 --teacher flow_breach
python train/demonstrations.py fit --data runs/mixed/data/demonstrations.npz \
  --run-dir runs/mixed/fit --epochs 16
python train/ppo.py --run-dir runs/mixed/ppo --init-weights runs/mixed/fit/policy.pt \
  --map-pool game/data/maps/pool/gen_01001000.json,game/data/maps/pool/gen_01004000.json \
  --roster mixed --levels 1,4,8,16 --curriculum 1 \
  --value-features detached --reference-coef 1 --total-steps 32768
```

评估逐图进行，不把失败地图藏在一个平均数里。跨图复测使用独立的 `policy.pt`，显式指定地图、兵种、等级、守方与时限；完整检查点仍严格校验原训练合约。

## 导出与实际游戏推理

普通构建默认不引入 ONNX Runtime。可选 CPU 推理使用官方 1.29.0 SDK；Windows/Linux x64 自动下载均锁定 SHA256，也可用 `RTS_ONNX_ROOT` 指定已有 SDK。发布时保留随可执行文件复制的运行库与许可文件。

```bash
cmake -S . -B build-rl -DRTS_BUILD_RENDER=ON -DRTS_WITH_ONNX=ON
cmake --build build-rl --config Release
# 导出环境另需 torch、onnx、onnxruntime，以及对应版本的 rts_native。
python train/export_policy.py --checkpoint runs/attacker/policy.pt \
  --output runs/export/attacker.onnx --probe build-rl/tools/policy_probe/Release/policy_probe.exe \
  --unit-types Ghoul --min-level 1 --max-level 1
build-rl/render/Release/rts_render.exe --rl-policy runs/export/attacker.onnx
```

示例是 Windows 多配置构建路径；Linux 单配置可执行文件没有 `Release/` 和 `.exe`。声明的兵种与等级必须来自实际训练及评估覆盖，不能仅为启用更多单位而扩大范围。

导出器使用真实冻结策略轨迹抽样，分别验证 1、9、32 个 agent 的 PyTorch/ONNX 输出和合法动作，再调用编译后的 C++ 程序复核。首个已验证模型抽取 271 步中的 54 个观测，最大 logit 误差约 7.63×10⁻⁶，动作完全一致。32 agent 的 C++ 推理均值约 1.83 ms；这只计模型前向，不含方向场与观测打包，也不是完整帧耗时。

游戏显式加载 `--rl-policy` 后显示 `RL: N squads`。只有模型声明支持的编队由网络控制，其余保留脚本。模型输入来自攻方迷雾视角，观测目标、归一化、13 动作掩码、编队队长观测与广播规则匹配当前训练接口，决策间隔来自模型元数据。首波实际截图确认 6 支编队在使用模型。

这里有一个需要继续评估的行为边界：训练接口将队长动作广播给成员；默认游戏脚本则让成员分别采样方向场。实验性模型模式明确采用前者，不在接入时偷偷改成另一套执行规则。未来若调整这个编队契约，必须同时更新训练和运行端并重新验证，不能直接复用旧成绩。

存档记录模型内容标识。读取 RL 对局必须提供同一个模型；完整快照与操作日志回放的后续状态哈希均已验证一致。普通对局仍保存为 v3，RL 对局使用 v4，让旧程序明确拒绝它，避免用脚本静默接替。默认 `play.bat` 仍进入正式游戏；当前模型尚未作为完整攻方 AI 自动启用。

显式加载 RL 模型时，默认将对局与日记保存到用户存档目录的 `rl/<模型内容标识>`
子目录，避免实验覆盖普通玩家进度。不同模型各自保存；相同内容的模型移动位置后
仍使用原目录。显式提供 `--save-dir` 时遵循调用者指定目录。

## 2026-09-09 混兵结果与实战边界

4 张训练地图为 `01001000 / 01004000 / 01005000 / 01006001`，等级为 1、4、8、16。
先采集 30,471 个破口示范样本，训练 16 轮，再使用分离价值梯度和参考约束进行
32,768 env-step PPO 续训。训练阶段 123 个完整 episode 中 118 次攻破堡垒；这不是独立测试胜率。

冻结 PPO 在训练图和两张诊断图、等级 1/8、两种动作选择方式上各跑 8 局。
训练图一级为两种各 6/8，其余条件为 8/8。等步数随机初始化对照所有条件均为 0/8。
该对照同时改变初始化和稳定化配置，只能比较整套训练方案，不能归因到某一个参数。

另外三张此前未参与训练的新地图，每种条件 8 局的结果如下：

| 地图 | 一级最大概率动作 | 一级按概率采样 | 十二级 / 三十二级 |
|---|---:|---:|---:|
| `01007000` | 6/8 | 8/8 | 两种各 8/8 |
| `01008000` | 0/8 | 1/8 | 两种各 8/8 |
| `01009000` | 6/8 | 6/8 | 两种各 8/8 |

全部条件有建筑接触，但 `01008000` 一级仍明显薄弱。12 和 32 不在训练等级列表中；
这些小样本结果不等于覆盖任意等级，发布模型仍只声明 1–16 级。

真正游戏循环另跑 3 张地图、固定种子、前 6 波，守方宏观陪练开启，包含准备时间、
征兵、经济和实际攻方编成。脚本、示范模型、PPO 模型均为攻方 1/3 胜，胜出的地图不同。
PPO 尚未证明优于原脚本，因此暂不作为默认攻方。每局最多观察到 11 支受模型控制的编队；
该数字不代表每一波的所有单位均由模型控制。

[480 局训练环境评估、9 局真实多波对局及导出核验](rl-results/2026-09-09-mixed.json)
保留逐局结果和来源哈希。模型的 PyTorch / ONNX / C++ 合法动作一致；
已验证五种战斗兵种及声明等级上下界。模型文件没有随源码入库。

## 可选的守方准备阶段

`--defender-prepare-ticks 900` 让守方先用真实脚本建设 900 tick，再生成攻方编成，
进入战斗阶段。准备过程保留建筑、征兵进度、资源、守方脚本状态；攻方不会提前暴露或
在准备中承伤。战斗仍有完整的 `--max-ticks` 时限，准备期间的战果不会进入训练奖励。
示范采集、PPO 和冻结评估均接受这个参数，默认 0 保留此前对照。

它缩小了开局差距，但还不是从完整多波存档采样：目前仍为固定混兵、一个入口、
单个等级的训练波次，不能把这个设置称为与完整游戏分布完全一致。

首轮 8,192 步的小实验在 `01001000 / 01008000` 一级、完整距离各 8 局复测中，
续训前后均为 4/8 与 3/8，未显示胜局改善；破口脚本对应为 4/8 与 1/8。
[48 局逐局记录](rl-results/2026-09-09-prepared-pilot.json)保留了这一阴性结果。
随后在新目录保留优化器状态继续到 65,536 步，原小实验检查点未覆盖。

游戏端允许实际编队数超过训练缓冲区的 32 行：使用连续小批次推理并将结果逐队回填，
不截掉超额编队。65 支编队、130 个单位的测试覆盖最后不足一批的结果与成员广播。

## 准备阶段续训后的最终候选（2026-09-09）

65,536 步训练累计 193 个完整 episode，其中 137 次攻破堡垒，56 次超时。
冻结后在前述两张图的一级、两种动作模式各 8 局复测，仍分别为 4/8、3/8，
没有展示训练环境胜局提升。真实多波对照则出现了局部改善：

| 前六波、三张地图各三个种子 | 原脚本 | 旧混兵 PPO | 准备阶段 PPO |
|---|---:|---:|---:|
| 攻破堡垒 | 1/9 | 2/9 | 3/9 |

新候选的三次胜利全部集中在 `01001000`，其中种子 1 提前到第五波，其余两局第六波。
额外对 `01007000 / 01008000 / 01009000` 做种子 1 的真实六波对照，脚本和新候选
都为 0/3。样本小、提升集中，不能宣称普遍优于脚本，更不能据此宣称掌握完整 70 波。

[冻结复测、33 局真实对照与导出证据](rl-results/2026-09-09-prepared-final.json)
复用了此前种子 1 的脚本与旧模型结果，不把复用数据算成新增独立实验。
候选 ONNX SHA256：`2e62e27f1b2b7002e9e727a09a58f0357909cc0ee01ee76e1db0477bdf52c7c3`。
五种战斗兵种、等级 1–16；C++ 与 Python 合法动作一致，32 编队前向约 1.98 ms，
不含方向场与打包。已生成包含运行库、素材、模型、许可、逐文件校验值的 Windows
独立实验试玩包，并验证实际加载与战斗运行。正式入口仍使用脚本。

## 守方宏观训练接口（2026-09-09）

新增完整游戏环境 `TrainingCampaign`，运行实际波次编成、建造与经济循环，跨波保留城市
和幸存者。内存分支复制可对同一局势进行成对决策实验；它不替代磁盘断点恢复。
守方区域观测只统计当前可见敌人，敌人在迷雾外的数量、血量与等级不会进入模型输入。
Python 接口返回独立数组，并提供玩家级命令的合法性掩码；接口契约见 `train/README.md`。

C++ 的宏观观测与完整环境测试通过 44 项断言，包含隐藏敌人不改变观测、可见敌人会改变观测、
资源与等级限制，以及分支推进的一致性。Python 另核验数组所有权、掩码检查不改变对局、
提前召唤限制及真实命令执行。这些是环境正确性证据，尚不是守方模型的学习成绩。

## 守方宏观 PPO 小规模试跑（2026-09-09）

宏观条件策略与完整对局 PPO 循环已接通，使用合法动作掩码，波次边界截断回报而不重置城市。
检查点原子保存模型、优化器、随机状态与当前对局命令日志，重启时重放并校验完整状态哈希。
连续训练与退出恢复的短测得到逐参数完全相同的下一轮更新；长局恢复仍有日志重放成本。

在 `01001000 / 01008000` 上从随机守方网络做 CPU 试跑：5 轮、160 次宏观决策，
累计守过 16 个波次、14 次败局；这是随训练变化的策略统计，不能当成独立胜率。
第 4 轮完整退出进程后恢复，完成第 5 轮，旧第 4 轮检查点单独冻结保留。
[配置、契约、逐轮记录和检查点校验值](rl-results/2026-09-09-macro-pilot.json)
用于证明真实训练与恢复链路，不用于证明守方已优于脚本。

## 首轮守方独立评估：尚未学会守城

冻结第五轮模型，在未训练的 `01007000 / 01009000` 各用种子 101、102 评估前六波：
现有脚本完成 2/4，初始随机网络与训练网络均为 0/4，八局网络对局都在第一波失守。
[12 局冻结对照](rl-results/2026-09-09-macro-heldout.json)说明小规模试跑没有展示独立学习收益。
两网络的结构、随机种子和决策间隔一致；脚本保持实际游戏使用的更高频批量决策和辅助指令，
因此不能把这一比较当成纯粹算法优劣的单因素实验。

进一步在上述两图的种子 101 上，把网络决策间隔从训练时的 500 tick 缩短到 20 tick，
初始与训练网络依然全部第一波失守；训练网络分别发出 22、26 次拆除命令。
[6 局频率诊断](rl-results/2026-09-09-macro-cadence20.json)复用了评估地图和种子，
必须作为诊断，不计作新的未见样本。频率不足不是唯一问题，基础策略仍接近随机。
后续先引入与模型动作界面一致的脚本示范，学习建城/招募基础，再续接 PPO 和独立评估。

## 单命令示范学习：发现等待样本偏置

教师与学生每次都只提交一条合法命令，辅助调兵指令不用于这轮示范。查询教师不改变对局；
学生仍使用迷雾过滤的观测，教师本身沿用现有脚本信息，属于离线特权教师。
两张训练图共采集 400 条示范，其中等待 357 条、招募 15 条、建造 12 条、清野 8 条、
维修 6 条、升级 2 条。训练 8 轮后示范平均负对数似然降至约 0.728，
但复用前述评估地图和种子的 4 局依然全部第一波失守。
[采集统计、学习记录和 12 局对照](rl-results/2026-09-09-macro-imitation.json)
是诊断结果，不能因训练损失下降宣称学会守城；示范中 89.25% 的等待占比是后续调整重点。

模仿训练支持按完整轮恢复，并已验证与连续训练下一轮参数完全一致；转入 PPO 后保留
初始化来源，禁止再由模仿程序覆盖 PPO 成果。更早冻结实验所需原生模块保存在
`.review/campaign-native-632f730`，本轮示范原始采集源码保存在对应 `runs/` 目录。

## 等待重采样与教师能力对照

在同样每 20 tick 一条命令、没有辅助调兵的限制下，脚本教师在上述两图、两个种子
分别守过 3、3、4、3 波。这说明学习器的控制接口本身允许超越首波失守，
但该教师也不是可靠的六波通关策略，不能作为最终成绩上限的证明。

保持 400 条示范，将有效操作和等待的抽样概率质量各设为 50%，训练 12 轮后，
复用上述四个诊断局面仍全部首波失守。完整保留
[12 局模型对照、4 局单命令教师及抽样契约](rl-results/2026-09-09-macro-balanced.json)。
训练轮数也从 8 改为 12，因此不是仅调整抽样比例的严格单因素消融；没有提升是此次实测结论。

代码检查另发现 v1 位置头的表达限制：同一个 16×16 区域内，局部特征相同，
位置分数只随 x/y 线性变化，缺少逐格城防信息。这能解释它难以表达精确缺口位置的可能性，
但尚未通过修改后的对照实验确认其影响。

## v2 逐格城防观测

新增全分辨率公开地形与己方建筑状态，逐格卷积特征同时进入决策和位置评分。
模型仍可以选择所有合法格子，不通过规则将位置提前限制为教师选址。
测试验证同一粗区域内相邻墙格在新输入中可区分；位置头可以选中区域内部格子，
而不只因线性坐标偏好选择边缘。隐藏敌人不改变逐格输入的测试同时通过。
11 项宏观 Python 测试与 4 项真实绑定测试通过，覆盖新输入下的概率重算和精确恢复；
C++ 宏观观测/完整环境通过 22,455 项断言。

保持两图 400 条示范、有效操作/等待各半重采样和 12 轮训练，新版在复用的四个诊断局面
随机采样动作时守过 0、0、5、2 波，旧版重采样模型对应全为 0 波。
另用两图种子 101 做条件最大值选取，分别守过 0、2 波。
[训练契约、12 局采样对照和 6 局确定性对照](rl-results/2026-09-09-macro-detail-v2.json)
记录首次局部改善，但所有学习器仍未完成六波目标，且两次训练初始化因新增网络结构而不同。
这是表示改动后的诊断结果，不能当作新未见地图上的泛化证明。下一步需扩充局面并做 PPO
续训，最后用尚未用于调参的地图/种子复核，不能继续只在这四个案例上优化。

## v2 示范接 PPO：新地图出现退化

在独立目录从 v2 示范权重接续 16 轮 PPO（1,024 次宏观决策），保留原示范检查点。
评估前固定三张未用于本轮守方训练和先前调参的地图 `01010001 / 01011000 / 01012000`，
各用种子 201、202；六波上限，采样动作，每 20 tick 一条命令。

| 对应六组地图/种子 | 10/201 | 10/202 | 11/201 | 11/202 | 12/201 | 12/202 | 平均守过波数 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 示范模型 | 3 | 3 | 5 | 3 | 5 | 5 | 4.00 |
| 接续 PPO | 2 | 3 | 3 | 2 | 4 | 5 | 3.17 |

六波完成率两者均为 0/6；4 组退步、2 组持平，没有组改善。
[预先固定的方案、完整训练记录和 18 局原始结果](rl-results/2026-09-09-macro-ppo-unseen.json)
中，脚本基线仅运行一次，没有在 PPO 后评估中重复计数。这是小样本退化证据，
不能因为新地图上能守过数波，就宣称本次 PPO 提升了策略。

示范模型保留为当前守方候选，PPO 模型单独冻结，不覆盖它。下一步给续训加入已有策略的
保留约束，在开发用地图验证；上述六组案例已用于识别退化，不能再作为完全未见的最终验收集。

## 固定参考约束：机制成立，首轮未见明确收益

新增显式模型热启动和固定参考约束，参考权重随检查点保存，恢复时不替换为漂移后的模型。
对完整命令概率使用重要性修正的 `KL(当前 || 参考)` 采样估计；小动作集测试证明期望值
及梯度与直接计算一致，真实训练测试证明参考模型未更新且精确恢复成立。

从同一 v2 示范权重、相同重新设定的随机种子，各训练 16 轮/1,024 决策，
一组约束权重为 0，另一组为 1。在开发用 `01007000 / 01009000`、种子 101/102 上，
无约束守过 0、0、4、5 波，有约束守过 0、0、5、4 波，平均均为 2.25 波。
[两组完整训练配置、固定参考核验及 8 局冻结对照](rl-results/2026-09-09-macro-anchor.json)
不支持“约束已解决退化”的结论。此处重新热启动了随机状态，与之前复制完整检查点的
续训起点不同，必须使用本轮匹配的无约束对照，不能跨实验挑选较差结果作基线。

保留该可选约束作为稳定性工具，默认权重仍为 0。下一步扩大采样窗口和训练地图覆盖，
使早期建城决策能得到更充分的波次结果反馈；不能把训练步数或 KL 数值本身当作成果。

## 更长反馈窗口与四图覆盖：仍未解决退化

从同一 v2 示范权重开启独立实验，训练图扩为 `01001000 / 01004000 / 01005000 / 01006001`，
每轮采样 256 次（原为 64），gamma=.999、GAE lambda=.99，固定参考权重 1。
24 轮共 6,144 次决策、122,410 tick，完成 27 个对局，四图均有结束对局记录。
开发集结果为 0、0、2、2 波，原示范模型为 0、0、5、2 波，仍无改善。
[配置、逐轮及逐对局记录、冻结评估和配对差值](rl-results/2026-09-09-macro-long-horizon.json)
完整保留。地图、预算和反馈参数同时改变，因此不能从该实验单独归因某个参数有效或无效。
原示范模型继续保留，后续先诊断优化更新及共享特征的影响，而不直接扩大训练量。

## 守方离线推理导出

已有 v2 示范模型拆分为可缓存的编码器和条件解码器 ONNX，图文件共约 0.61 MiB。
两张真实地图各四次决策的 Torch/ORT 输出与最终命令一致，另验证 41×53 动态分辨率、
不同解码前缀及类型/等级边界输入。[图文件、模型与导出源码校验清单](rl-results/2026-09-09-macro-export.json)
明确标记 `game_runtime_integrated=false`：这是离线推理工件，尚未接入守方 C++ 游戏运行时，
更不构成守城能力提升的证据。普通游戏入口保持原有行为。

## 价值分支干扰诊断：存在干扰，但隔离未改善成绩

对八个真实局势只做三次价值更新、完全不施加动作目标时，共享结构的选中命令对数概率
最大变化约 0.106；隔离价值特征后变化为 0，价值输出仍有更新。
[受控诊断](rl-results/2026-09-09-macro-critic-probe.json)证明价值梯度可以改写动作策略，
不等于证明此前完整 PPO 退化由它单独造成。

同一示范权重、种子、16 轮预算和固定参考约束下，完整 PPO 对照在四个开发案例中，
共享版本守过 0、0、5、4 波，隔离版本守过 0、0、3、5 波。
[8 局冻结评估及两组训练配置](rl-results/2026-09-09-macro-critic-isolation.json)
没有显示整体提升。因此 `--detach-critic-features` 作为可恢复的诊断选项保留，默认关闭。
后续补齐学习器与已训练攻方模型的联合陪练入口，不继续只在固定脚本对手下微调。

## 尚未完成

更大样本独立测试、完整多波局面采样、宏观编成学习和守方宏观 RL 陪练仍需继续推进。
既有攻方候选仍来自固定编成的单波训练。新完整环境虽已支持实际波次编成与跨波经济，
守方候选、网络、训练与恢复已完成小规模验证，但独立评估未显示有效策略；单纯增加旧环境训练步数不能
替代这些场景覆盖。实验试玩包可用，完整 RL 目标仍未完成。
