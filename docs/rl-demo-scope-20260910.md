# 固定地图演示范围

根据 2026-09-10 用户的时间约束，近期目标收敛为固定地图上的可复现演示，不再以完成通用策略发布为本轮交付条件。

## 地图与验收

固定使用当前已有完整对照结果的三张地图，不按新模型成绩临时换图：

| 演示编号 | 地图 | 诊断等级 | 评估种子 |
|---|---|---|---|
| A | `game/data/maps/pool/gen_01007000.json` | 1、4 | 100001 |
| B | `game/data/maps/pool/gen_01008000.json` | 1、4 | 200001 |
| C | `game/data/maps/pool/gen_01009000.json` | 1、4 | 300001 |

每个案例 32 局，沿用当前评估的入口与随机调度。上述地图是演示开发集，可以用于后续定向改进，不再称为独立留出集。演示启动须显式选择地图，不能依赖默认随机选图；实际游戏演示种子和存档另行固定并验证，不能把评估种子直接当作前端启动参数。

## 当前训练与模型选择

正在运行的 `anchor-20260910` 配对实验保持原配置，训练后会自动评估上述三张图。它仍在原四张训练图上训练；本次只收敛交付范围，不偷偷修改正在运行的实验，也不把旧记录说成在演示图上训练的结果。

本轮结束后不自动追加新地图或多训练种子实验。若候选未通过现有逐案例标准，保留原始权重作为演示候选，不展示上一轮 0/192 的退化模型。原始权重 SHA-256：`f05c7c5d9452a26c9e8e1507d88a13737e3353a565cb87b7e5bb7d37e9c1fefe`。

选定权重后，仍须完成导出、游戏实际加载、三张图的演示流程和所需存档恢复检查。这些是演示是否能运行的检查，不扩展成大规模泛化研究。在这些检查完成前，不宣称展示包已就绪。

## 展示表述

可以说明「在三张固定演示地图上训练或调优、评估的 RL 策略」，并准确区分模仿初始化、PPO 增益与脚本行为。不得把固定场景成绩表述为任意地图泛化能力，或把尚未进入实际游戏验证的 Python 评估成绩当作长局效果。
