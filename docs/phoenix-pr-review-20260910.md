# 不死鸟 PR 评审修复

2026-09-10，PR #174。

- 被击落的身份在重生队列中继续占用名额，避免下一波立即生成替代鸟。第 W 波被击落、冷却 N 波时，W+1 至 W+N 波缺席，W+N+1 波以原身份返回；N=0 时下一波返回。
- 返回数量受当波编成限制，缩编时暂存多余的花名册成员。
- 身份查询检查单位存活、世代和类型，失效句柄不会读出旧身份。
- 存档 v4 保存花名册、冷却、场上身份映射、身份计数器和两个新增曲线参数，并校验范围及重复身份。旧 v3 不兼容，保留独立旧版备份；永久日记仍为 v1。

回归覆盖三波及零波冷却、同一身份重生、同时存在花名册和冷却队列的快照恢复，以及跨多个后续波次的世界哈希一致。存档测试同时修正 Windows 中文数据路径转换。

原 PR 中的平衡统计来自修复前版本，不能作为当前版本的效果证据。本次未重新运行配平实验。#173 的防空响应、避开防空与撤离阈值改进仍待完成；#175 的日记/彩蛋部分也未在此实现。

最终验证：Windows/MSVC Release，渲染关闭，完整 CTest **51/51 通过**，耗时 125.07 秒；差异空白检查通过。
