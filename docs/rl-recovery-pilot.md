# 2026-09-08：重启恢复、旧权重评估与短训

## 测试条件与边界

服务器验证代码为 `c7644c7`（#161 的 CUDA 修复），与合并后的 `8697547` 只有说明文档差异。原生绑定为 Linux Release / GCC 13.3；Python 3.12.11、PyTorch 2.8.0+cu128。基础版本 54/54 CTest、修复后的 15 项运行时检查，以及 GPU 保存恢复、旧权重热启动均已通过。

以下都是当前单张地图、一级、9 支亡灵步兵编队的战术层结果。不是混编进攻，也不是完整游戏 AI 的胜率。旧模型是在旧规则下训练的，以下是在**当前规则下复测**，不能直接当作原实验成绩。

## 旧权重没有全部丢失

| 权重 | 保存步数 | 可恢复内容 |
|---|---:|---|
| economic-defender 归档 | 40,009,728 | 网络权重 |
| victory-defender 归档 | 40,009,728 | 网络权重 |
| economic-empty 周期文件 | 12,288,000 | 网络权重 |

三份均经过只读加载及有限值检查。旧格式没有优化器、课程与计数，使用 `--init-weights` 热启动，不能声称完整续跑。空城对照日志最后为 12,845,056 步，超出周期文件的 557,056 步没有保存进去。原文件保留作历史对照。

## 固定策略评估

每项 32 局、种子从 100001 起、8 个并行环境；使用冻结的最大概率动作。所有模型都面对当前真实脚本守方，包括原先在空城训练的模型。课程近距离为 `frac=0.15`，实际集结点为 `frac=1`；每局上限 2400 tick，每个动作推进 6 tick。评估使用 CPU、4 个仿真线程和 1 个 PyTorch 线程，GPU 对评估进程隐藏。

| 策略 | 近距离胜局 | 实际距离胜局 | 实际距离打到建筑 | 近距离平均建筑伤害 |
|---|---:|---:|---:|---:|
| 旧 economic-defender | 0/32 | 0/32 | 0/32 | 3,117.50 |
| 旧 victory-defender | 8/32 | 0/32 | 0/32 | 3,230.25 |
| 旧 economic-empty | 0/32 | 0/32 | 0/32 | 1,968.75 |
| 规则 flow 基线 | 16/32 | 0/32 | 32/32 | 2,602.50 |

旧 victory 权重值得作为后续热启动候选，但尚未与新版本从零训练做同预算对照。三份旧模型在实际距离均未接触建筑，说明当前部署条件下存在明显的远距离行为缺口。规则基线能从实际距离接触建筑，因此不能把模型没有接触敌人直接归因于地图没有可走路径。

规则基线在实际距离也没有获胜，后续要继续检验战斗难度和推进能力。**0/32 不证明任务不可获胜**：单项 Wilson 95% 区间的上界约为 10.7%。这些固定种子用于诊断和方案选择，不替代独立最终测试集，也未覆盖多地图和多训练种子。

## GPU 占用时的 CPU 方案

本轮发现 GPU 上有无关项目的任务，未修改其进程、优先级、显卡模式或资源配置。观察到计算模式为 Default、MIG 未启用；有 MPS 控制程序，但未发现运行中的服务。没有发现可用的 Slurm / PBS / LSF 命令；这只是当前账号可见范围内的结果，不代表管理员没有其他管理机制。

显存余量不等于算力余量，GPU 利用率表示采样时段内执行内核的时间比例，并不是可以直接分配的算力份额。MPS 的线程限制也不等于独占资源预留。参见 [NVIDIA 利用率定义](https://docs.nvidia.com/deploy/nvidia-smi/index.html) 与 [MPS 资源配置说明](https://docs.nvidia.com/deploy/mps/when-to-use-mps.html)。是否同卡共享应遵守服务器安排，不能通过终止别人的任务获得性能。

先测纯 CPU 是否足以做初筛，而不是把整个流程挂起：

| 设备 | 并行环境 | 仿真线程 / PyTorch 线程 | 两次更新步数 | 训练计时 | 吞吐 |
|---|---:|---:|---:|---:|---:|
| A800（先前空闲时） | 64 | 4 / 1 | 8,192 | 21.39 秒 | 383 env-step/s |
| CPU（本轮） | 64 | 4 / 4 | 8,192 | 34.21 秒 | 239 env-step/s |

均为近距离课程开头的短测，不含进程启动与初始建局；运行时刻、机器负载和 PyTorch 线程配置不同，不能当作严格的硬件对照或全程耗时预测。CPU 方案足以先做有限预算筛选。CPU 训练必须设置 `CUDA_VISIBLE_DEVICES=`，避免 CUDA 版 PyTorch 的随机状态保存等辅助路径意外占用 GPU。

## 三组短训

按每组累计 200,000 env-step、64 个环境、4 个仿真线程、4 个 PyTorch 线程串行执行。分组仍是 economic + 真实守方、victory + 真实守方、economic + 空城；胜利奖励均为 2000。每组使用独立目录，每次更新保存，阶段结束再冻结评估。实际步数可向上取整不到一个环境批次。

本次三组是从同一训练种子新建的对照，不把旧模型热启动混入其中；旧权重仍保留，可另建同预算热启动实验。短训已启动，尚未完成，不能声称胜率改善。

验证目录：`/home/yys/Cody-ic/rl-validation.8UmPfz/checkout`。
旧模型报告：`runs/legacy-eval-20260908/summary.json`。
短训产物：`runs/pilot-20260908/`。

恢复 CPU 短训的等价命令如下；运行目录锁会拒绝重复启动：

```bash
cd /home/yys/Cody-ic/rl-validation.8UmPfz/checkout
export CUDA_VISIBLE_DEVICES=
export RTS_BINDINGS_DIR="$PWD/build/bindings"
export RTS_RUN_ROOT="$PWD/runs/pilot-20260908"
tools/server/run_defender_ab.sh --device cpu --envs 64 --threads 4 \
    --torch-threads 4 --milestones 200000
```

未来切回 GPU 时应恢复合适的 `CUDA_VISIBLE_DEVICES`，并显式选择 `--device cuda`；设备与线程属于允许变更的运行参数。未完成原生对局会按检查点恢复约定重新生成，不保证与未中断训练逐位相同。
