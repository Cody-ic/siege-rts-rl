# 游戏分支整合（2026-09-14）

本分支以 #178 的 e35084ed 为基础，只移植 #184 的 bb76712d、2edf2ecc、7ee1d351 三个游戏提交，未合入 codex/local-playability-base 的 RL 历史。它仍依赖 #178，建议先合 #178，再重定本 PR 到 main 并核对差异。

用户明确采用按局解锁剧情：新局重置，存档继续恢复，开发者仅临时预览。修正帮助中的“永久解锁”和文档中的旧规则。

工匠默认寻路改变，因此交互存档升为 v5；v4 已用于未合并的不死鸟分支。v3/v4 拒绝直接载入，保留 legacy 备份，旧局需使用对应旧版继续。新增回归核验拒绝、备份内容及新存档不能覆盖旧备份。未实现旧局迁移；未来整合不死鸟或 RL 存档字段需重新统一合同。

本次在独立工作区构建渲染开启、ONNX/绑定关闭的 Windows/MSVC Release（/O2 /Ob2 /DNDEBUG）。首次受限配置导致 Release 参数不完整，该轮验证已中止，不作为通过证据；修正后重新构建并执行完整回归。实际测试结论见 PR 描述。

已目视检查建造预览及活墙/木栅截图；没有进行真人鼠标交互试玩。图鉴和剧情截图测试属于渲染路径检查，不能替代长局试玩。没有运行 RL 训练或改变模型默认值。
