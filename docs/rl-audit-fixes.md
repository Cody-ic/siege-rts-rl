# PR #180 实施状态

## 已实施：把实际战果与代理指标分开

- 单波评估新增 `outcomes`，显示“有建筑伤害但零摧毁”的对局数，以及击杀、建筑摧毁和损失统计。伤害不换算为永久经济损失。
- 侦察与工匠击杀分开计数。旧 `scouts_killed` 保留历史非战斗单位口径，避免悄悄取消旧配方中的工匠奖励。
- 不死鸟战斗阵亡独立记录；直接退场不计阵亡。此计数不推断重生等待期间的战略价值。
- 被击杀守军按当前兵种和等级记录补充金币报价，不用血量代替该报价，也不声称守方已经付钱补兵。
- 实际维修木材在成功接受维修、扣款时入账；拒绝、重复下令及后续读取不重复收费。它包含修复开局残血，尚不能自动归因于某次攻击。
- 连续波次新增正式评估入口，配对开局城市哈希，按波保存双方战果和期末资源。城市跨波保留；观察上限单列；报告中止时不冒充 complete。
- 原实验采用报告保留新战果摘要，并明确单波门槛仅为初筛，完整游戏验证仍必需。

新增记账只用于诊断，奖励系数为零。原伤害、胜利及损失权重不变；旧奖励不会因字段改名而悄悄变化。tally 从 8 扩为 13 列，原有列顺序保持，新布局进入检查点合同，旧目录仍需原源码/绑定恢复。

## 尚需实验，不能宣称已修复

威胁与经济中断的边际价值、修复价格、gamma、牺牲编队的远期信用、观测目标类别、动作选择能力，以及训练编成到游戏编成的迁移。审计提出这些问题不等于已经证实某个修法；本次未声称修复了 RL 战斗能力，未启动新训练。

下一步以 `attacker_campaign_evaluate.py` 的逐波记录为依据，对既定三图分别控制编成、决策间隔和目标选择，再标定奖励。维修报价、即时伤害、实际支出和后续攻堡必须继续分开，不能直接相加后当作“真实价值”。

## 验证

PR #181 评审修复：连续报告版本升为 `report_version=2`，`city` 改用只读原生诊断接口，
资源、人口、等级与血量保留实际整数，不再把归一化观测当作城市状态。
模型观测保持不变。评估将模型、地图、数值表冻结到输出目录的 `inputs/`，按副本内容
记录 SHA256；每局前后及最终完成前同时复核原文件和副本，变化即停止，报告保持
`complete=false`。新增测试覆盖整数口径、诊断不改变状态、稳定输入完成，以及原文件
或副本在种子配对之间变化后不得宣告完成。

2026-09-12 本地复验：Windows/MSVC Release 编译通过；连续评估测试 6/6 通过，
覆盖真实脚本波次、实际资源口径、固定副本与原文件的原生状态一致性，以及输入变更
防护。C++ 全量 458 个用例、62,906 条断言通过；其余 19 项非 C++ CTest 全部通过。
`git diff --check` 通过。本次未开启 ONNX，文件冻结的原生对局测试使用脚本控制器，
完整冻结模型推理对照仍未运行；没有启动新的训练实验。

Release 构建开启 Python 绑定和训练集成测试。首轮全套 68 项后，对失败及变更相关项重新构建复测：8/8 通过，包括全量 C++、存档、经济记账、训练恢复、冻结单波评估、连续波次和结果汇总；此前其他 60 项通过。全量 C++ 覆盖新增计数及确定性回归。`git diff --check` 通过。

本次构建关闭 ONNX，连续评估已测试原生脚本的波次边界、城市状态和 tick 截断；尚未执行新绑定下完整冻结模型对照。测试中的短训练只作回归，不是新奖励训练实验。
