# 攻方加强方案（2026-09-04，提案；代码未动）

> **定位。** `配平工作交接.md` §0 记着一条现状：脚本守方在三张池图上**全部撑满
> 60 波**，「无尽模式必败」在 60 波内不成立。而 §2.8 又查出守方脚本还差一整条
> 战略轴（扩张）⇒ **真人守方严格更强**，那条结论 a fortiori 更成立。
> 本文提出攻方该怎么加强，并给出一条与直觉相反的主结论。
>
> **证据一律在 `配平工作交接.md` §2.8–2.10，本文不复制**（同一件事写在两处必然
> 漂移）。这里只写：提案是什么、为什么、有什么风险、怎么验。

---

## 0. 主结论：加强攻方**不是**调 `Σ√B`

这是本文最要紧的一句，因为它推翻了我们此前默认的做法（调 `power_alpha` /
`slots_per_wave`）。

实测：攻方的兑现战力 `Σ√B` 从第 1 波的 **6.0** 涨到第 60 波的 **275.1**
（**46 倍**），在第 50 波前后已经追平甚至反超守方——

**而破口在第 42 波之后彻底消失了**，第 48 波起攻城锤 **7/7 全部半路阵亡**，
攻方每波 76 人进场、剩 1–2 人。

⇒ **`Σ√B` 不是绑定约束。** 继续抬它只会造出更多打不进来的兵，还顺带把编成位
推得更远离 `CLAUDE.md` 要求的 20–40 可训练区间（`slots_at(60)` 已是 **77**）。

### 0.1 那么绑定的是什么

第 60 波守方的 `Wall` 只剩 **28/80** —— **墙早就不是障碍了**，
挡住攻方的是 **74 座 `Tower` 的火力网**。

而攻方对「塔海」的设计答案只有一个：**`Phoenix`**
（`Ram` 的 AOE 要先走到墙下，`Shade` 压的是墙头守军不是塔）。
它被钉在 **1 只**。

---

## 1. 提案 A：放开 `Phoenix` 的数量上限

**改动**：`WaveCurve::phoenix_per_waves` 由 `0` 改为正数（`0` = 恒 `phoenix_base`）。
`phoenix_cap` 随之抬起。**参数早就在，一次都没开过。**

**设计依据是 `CLAUDE.md` 自己的原文**，这不是新机制：

> 硬性数量上限，**上限可随波数缓慢放开**，但增速必须远低于预算增速。

**为什么排第一**（实测，60 波）：

- `Phoenix` **58/60 波出场，只在 12 波里被击落（存活 79%）**
- 它打的正是设计要它打的：`phoenix_events` 全是 `{kind: Bld, target: Mine}`
- **守方全程只有 2 座 `Flak`**，对着 74 座 `Tower`

⇒ 「**AA 的机会成本**」那个两难——`CLAUDE.md` 称作「本作智斗最可读的载体」
——**在仿真里从未成立过**：1 只 `Phoenix` 不值一座 `Flak`，所以脚本不建、
真人玩家也不会建。放开上限是**一次改动同时激活两件事**：攻方对塔海的答案，
与守方的防空两难。

**风险与护栏**：

1. `CLAUDE.md` 要求「增速必须远低于预算增速」——所以 `phoenix_per_waves`
   要取得**足够大**（每 N 波才 +1），不能跟着 `power_at` 走。这条是结构，
   不是旋钮的取值问题。
2. 空军仍受另外三条结构约束保护，本提案**一条都不碰**：对城墙城门破坏速率
   为 0、跨波留存、击落后需 N 波重生。
3. **反向风险**：守方脚本 `flak_target = 2` 是写死的。放开 `Phoenix` 之后
   脚本若不跟着建防空，量到的就不是「攻方变强了」而是「陪练不会防空」
   ——同 §2.7 那个教训。**A 必须与「`flak_target` 随观测到的空军数走」
   一起落地。**

---

## 2. 提案 B：攻方的目标选择（现在只有一个目标）

**现状**（`game/src/demo_driver.cpp:594`）：

```cpp
const rts::GridPos goal[1] = {w_.keep_pos()};   // 攻方 flow 的唯一目标
```

**所有战果都是「路过顺手」。** 而顺手已经把守方的采集建筑**钉在 6 座**
（60 波：毁 22 建 28，churn 近 1:1）——那 6 座不是脚本选的数量，
是攻方给它留的。

刻意打经济 = 直接掐守方**唯一**的成长引擎：石材 → `K` → 人口上限 +
兵种等级上限 + 建筑等级上限 + 塔。

**这就是 `CLAUDE.md` 的「攻其必救」，而奖励函数早已设计好**：

> 把**摧毁建筑的即时奖励设为该建筑的重建成本**——这个权重等于给玩家造成的
> 实际资源损失，是有原则的推导而非手工试凑。

**所以 B 本质上是 RL 该学的东西，脚本版只需要一个占位策略**（例如按
「重建成本 ÷ 到达代价」排序选目标，一波一次决策，落在宏观层）。
它同时是 `波次分段与侦查时序.md` 那个「侦查消费端」的前提——
方向与目标一旦读情报，「杀掉 `Wraith` 让 AI 带错情报」才第一次有收益。

**风险**：目标选择一旦不是 `keep`，`flow_` 的缓存键要跟着变（现在按
`(兵种, 档)` 缓存，目标集合是常量）。这是实现细节，但漏了会静默用错 field。

---

## 3. 提案 C：接回 `slots_cap = 40`

纯攻方算术，**与守方长什么样无关**：

| | 无硬顶（现行） | 硬顶 40 |
|---|---|---|
| 第 60 波 位子 / 名义等级 | 77 / 56 | 40 / **110** |
| 攻城锤血量 | 2353 | **3249（+38%）** |
| 锤血增长指数（w20→60） | 0.186 | **0.480** |
| 总 `Σ√B` | 278.7 | 199.9（**−28%**） |

用一个已证**不绑定**的量（`Σ√B`）换一个**绑定**的量（单位承伤）。

**它本来就欠着**：`CLAUDE.md`「单波单位数上限 20–40 是硬性约束」，
而 `slots_at(60) = 77` 早已冲出去。2026-09-01 撤下硬顶时的理由是
「40 是随手标的、没有依据」——**那个理由现在反过来了**：取值该由入口赛跑定。
RL 那条文献只给区间，赛跑给具体值。

**连带**：硬顶一旦咬住，多余预算**只能**进等级，正是
`CLAUDE.md` 的设计原意（`demo_driver.cpp` 里那条「实现与原意相反」的订正注
可以一并销掉）。

**风险**：`Σ√B` 降 28%，若入口赛跑那一侧的收益不及预期，净效果可能是攻方变弱。
所以 **C 的判据必须是 `rams_died_en_route` 与 `breach`，不是 `Σ√B`**。

---

## 4. 提案 D：给守方宏观层加「扩张 + 重建战损墙」

**不是加强攻方，但必须同一轮做。** 理由见 `配平工作交接.md` §2.8：
`DefenderMacro` 从结构上不可能扩张（`ring_r_` 构造时算死、`gaps_` 只含开局
的设计缺口不含战损），于是 60 波下来 `Wall` 76 → 28、只重建了 2 格。

不加的话，A/B/C 都是在打一个「不会扩张、墙塌了不补」的假守方——
**同 §2.7 那个教训，而那次的代价是几乎所有实测读数作废。**

---

## 5. 明确**不做**的（写下来免得被当成遗漏）

| | 为什么不做 |
|---|---|
| 抬 `power_alpha` / `θ` | §0。`Σ√B` 已涨 46 倍而破口消失，抬它只是造更多打不进来的兵 |
| 抬 `k`（`hp_permille_per_level`） | 收益在**守方**一侧（守方 `K` 是计数器、攻方等级是从预算反解的），而现在的问题是守方太强 |
| 木材产出比例（`Lumber` 14 → 9） | 纯算术、随时能做，但它不影响胜负，别占这一轮 |
| 让塔不再是永久存量 | 降级理由（「塔位耗尽是几何问题」）已撤回（§4.1d），重新回到候选池；但要等 D 落地后才能量它的真实打击面 |

---

## 6. 待拍板

1. **`phoenix_per_waves` 取多少**。结构要求是「增速远低于预算增速」，
   而 `power_at` 是 `w^1.25` ⇒ 空军增速至多线性、且斜率要小。
   建议每 8–12 波 +1（占位），`phoenix_cap` 给 5–6。
2. **`flak_target` 怎么随空军数走**。它现在是写死的 2。若做成「按观测到的
   空军数」，要注意情报口径——`DefenderMacro` 只读免费情报 + 迷雾内可见
   （2026-09-04 组内定），不能读 god 视角的编成。
3. **`slots_cap` 取 40 还是别的数**。40 是 `CLAUDE.md` 区间的上端；
   入口赛跑可能要求更小。
4. **B 的占位策略形状**：一波一次（宏观层）还是逐单位（战术层）？
   倾向宏观层——`CLAUDE.md`「宏观层是 bandit 尺度」。
5. **D 里守方扩张的触发条件**：脚本按什么判断「该扩张了」？
   这是真决策（周长翻倍 vs 收入复利），不该写成常数。

---

## 7. 验收判据（**不是 `Σ√B`**）

| 量 | 现在 | 期望方向 |
|---|---|---|
| `rams_died_en_route / rams_spawned` | 第 48 波起 **7/7** | 显著下降 |
| 出现破口的波数占比 | 第 42 波后 **0** | 恢复并持续 |
| 攻方波末存活 | 76 → 1–2 | 上升 |
| 守方 `Quarry`/`Mine`/`Lumber` 稳态数 | 6 / 6 / 4 | 下降（B 生效） |
| 守方 `Flak` 数 | 全程 2 | 上升（A 生效，且证明两难成立） |
| 中位存活波数 | 3 张图 60 波未陷落 | **目标值仍未定**，见交接 §3 第 1 项 |

跑法见 `配平工作交接.md` §6。一局 5–6 分钟，3 张图约 17 分钟。
