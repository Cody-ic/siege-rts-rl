# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 当前状态

**代码骨架已落地，但仿真本体尚未开始。** 已入库的内容是：

| 文件 | 性质 |
|---|---|
| `README.md` | 导航与协作约定，**新成员先读这份** |
| `CLAUDE.md`（本文件） | 设计契约，四人对齐的依据 |
| `课程设计最终交付项.md` | 老师原文，**不得改动** |
| `交付要求的工程影响.md` | 交付要求对架构与排期的约束 |
| `地图与场景设计.md` | 地图数据格式与场景结构约束（第二个跨模块契约） |
| `日志.md` | 上机日志，答辩「人员分工」页的依据 |
| `CMakeLists.txt` / `cmake/` | 双工具链构建配置（MSVC + GCC），`render/` 默认不参与 |
| `rts_core/` | 确定性地基 + **接口契约（已全部落地）**：花名册与地形枚举、动作、命令、观测注册表、`World`、只读视图、迷雾、**确定性回放**。**机制仍是一行都没有**：战斗、寻路、视野、经济全在 1c |
| `rts_core 接口契约.md` | 接口的**决定与理由**（形状看头文件）。含 PR #23 评论串里辩定的十三条，以及逐版答掉的七条 |
| `tests/` | Catch2（经 FetchContent）。条目数不写死，要数就 `ctest -N` |
| `tools/sprite_gen/` | 纯 Python 精灵预渲染流水线，35 实体 × 4 朝向 × 逐实体状态 = 476 张成品已入库（`idle` / `move` / `attack` / `work`） |
| `tools/check_determinism_bans.py` | 把本文的确定性禁令变成会红的 ctest |
| `tools/check_cmake_freshness.py` | 把「构建目录的配置比 `CMakeLists.txt` 旧」变成会红的 ctest。**`ctest` 单独跑不会发现这件事**，会给出一个绿色的子集 |
| `.mailmap` / `.gitattributes` | 作者名规范化；数据文件强制 LF（理由见 `地图与场景设计.md` 6.3） |

`守方AI与协同演化.md` 与 `演示与可视化清单.md` **已经合并且方向已定**（#15/#16 合入，
结论落地见 #46）。两者都实质改变了范围，还没读过的请补上——尤其是
「守方 RL 只在决策层、单兵由参数化脚本驱动」这一条，它决定了命令枚举的形状。

提案 `无尽模式与地形分层.md` **§6 的机制那一半也已表决通过**（2026-08-29 组内口头讨论，
issue #58）：`Stump` / `Sapling` / `Rubble` 变成有血量的可破坏障碍，
**并附一条原提案没有的机制——破坏后产出资源**。它同样改变了范围：
地图格式多一个 `obstacles` 字段（6.2）、命令枚举多一条 `Clear`、
`kWorldHashTag` 因此从 `World/1` 变 `World/2`（旧回放要重录）。
四条结构决定见该文 6.6.2，命名与产出映射见下文「命名对照表：可破坏障碍」。

**`rts_core` 的公开接口已全部定稿。** `render/`、`bindings/`、
`train/`、`tests/` 依赖的形状都在了：

| | 状态 |
|---|---|
| 花名册与地形枚举、战术动作、玩家/宏观命令、观测通道注册表 | **已定**，见 `rts_core 接口契约.md` §1 |
| `World` 数据布局、`submit`/`advance`、只读视图、迷雾三态、动作与命令掩码、状态哈希 | **已定**，同上 |
| 回放格式、录制器、比对骨架 | **已定**，同上（决定与理由见 §4.6 / §4.7） |

**因此下一个最高优先级是机制本身（1c）**，不再是契约。

`World` 已定的是**形状与状态**，不是机制：战斗解算、寻路、视野、经济全部留给 1c。
分界是一条明确的线——**凡是需要一个待标定数值才能推进的都没做**，
逐项清单见 `rts_core 接口契约.md` §1.1。其中七种命令被**记账**而不是静默丢弃
（`World::deferred_command_count()`），所以「本版没做这一类」是可以被断言的事实。

本文件记录的是团队已确定的设计决策与架构约定，用于保证四名成员（均使用 Claude Code）
产出的代码能够对齐。实现新模块时请遵循此处的结构，不要另起一套。
新加入的成员请先读 `README.md`（导航、协作约定、环境准备、未认领的工作）。

## 项目性质与硬约束

- 这是**C++ 程序设计课程**的大作业。核心逻辑必须是 C++ —— 这条决定了下面所有架构选择。
- **当前不使用游戏引擎**，前端用 C++ 图形库自绘。Unity 曾在计划内，评估后放弃；Unreal 与 ML-Agents 同样不在方案内。**这是一项基于当前约束的工程选择，不是硬性原则**——不可动摇的只有下文「架构：三层隔离」中的三条不变量，引擎与否只是当下满足它们的方式，理由与重新评估的判据见「前端：为什么当前不经引擎」。前端因此没有 C# 层，"C# 前端是否满足课程要求"这一风险项随之消失。**但这不等于交付物必须是纯 C++**——课程要求的是核心逻辑为 C++（见下文「项目主体是 C++，但不要求全栈纯 C++」），并未禁止交付包内含 Python。推理时是否随包分发 Python 运行时属于部署方式选择，见 `交付要求的工程影响.md`，第 3 周再定。
- 全体成员在 **Windows** 上开发，训练在 **Linux GPU 服务器**上进行。
- **交付要求见 `课程设计最终交付项.md`（老师原文，不得改动）与 `交付要求的工程影响.md`（我们的应对与推断）。** 后者区分了"要求原文规定的"与"我们据此做的推断"，并记录了唯一一项需向老师确认的事宜。
- **地图数据格式、地形枚举与场景的结构约束见 `地图与场景设计.md`。** 它是四人并行开发的第二个跨模块契约（第一个是 `rts_core` 公开接口）：`rts_core` 吃它、`render/` 画它、`train/` 采样它、`tools/` 生成与校验它。有一批设计要求（防御无法覆盖所有墙段、龟缩拐点、侦查有价值、冲锋助跑）只能靠地图几何满足或违背，数值改不动。
- 周期 **4 周，4 人**。范围已刻意收紧，见下。
- 游戏形态是**不对称波次生存**：玩家为守方（建墙、造防御建筑、经营经济、指挥部队），RL 控制的 AI 为攻方，一波一波进攻，无尽模式。形态上接近 *They Are Billions*，差异化卖点是攻方由 RL 驱动而非脚本。**注意区分游玩与训练**：正式游玩时守方是人类玩家，但训练时守方同样由 RL 控制（攻守协同演化），见 `守方AI与协同演化.md`。
- **经济系统是既定范围内的内容**，不是可选项。守方双层资源分布：城内资源点提供保底收入，城墙之外的资源点提供增量收入但需派兵争夺。

### 守方多资源：三种，各对应一条决策轴

玩家资源是多维的（与攻方的单一维度相对），但**只做三种**。TAB 有约 7 种、DiNaO 有 8 种，4 周内做不动，且资源过多会退化为搬砖。原则是**每种资源对应一条不同的决策轴，且不可自由兑换**：

| 资源 | 用途 | 决策轴 |
|---|---|---|
| 石材 | 永久建筑的**主体**：墙体、塔身、地基 | 硬防御的上限 |
| 木材 | **每座建筑的配料** + 维修 + 应急工事 | 恢复速度与可维持性 |
| 金币 | 部队、征兵时选等级、**买活** | 机动人力 |

**三种资源的收入**（即流量）**都来自地图上的资源点**（对应采集建筑 `Quarry` / `Lumber` / `Mine`），
不存在凭空给的产出——**唯一的例外是领主堡垒产出的极少量金币，它是一条护栏，理由见下**。

除此之外还有一项产出，但它**不是收入**，所以不构成上面那条的例外：**清掉可破坏障碍
（`Stump` / `Sapling` / `Rubble`）会产出一次性资源**。区别是实质的——资源点是**流量**、
可持续、可被攻方切断；清野是地图上一份**有限存量**，拿完就没有，**不改变任何收入曲线的
斜率**。因此它不影响「城内保底收入按线性增长」与那个拐点的存在性。
映射、四条结构决定与待定项见「命名对照表：可破坏障碍」与 `无尽模式与地形分层.md` 6.6。

#### 领主堡垒的金币产出是「兵力地板」

金币管**人力**，而人力是唯一能在波次进行中即时投入的手段（石材建得慢、木材只能补形状）。
若金币完全依赖城内的 `Mine`，那么金矿一旦被点掉，玩家**连补兵都做不到**——
这比石材或木材被切断急得多：那两种只是让防线上限下降或修不动，
而金币断供意味着**任何缺口都无法再堵**，一波之内就会被推平，此后每波更惨。
这正是「防死亡螺旋」一节要防的形态，而其余三条对策（维修便宜、废墟折扣、
城内保底）都救不了它——因为它们本身就要花资源。

因此 `Keep` 产出**极少量**金币。它的定位是**地板而非收入**：

- 数额只保证玩家永远能补最基本的兵，绝不足以支撑一支军队（具体数值待定）
- 它天然不可被摧毁——堡垒丢失即败，所以只要还没输，这条地板就一定在。
  这是"地板"最理想的性质，用别的建筑都做不到
- 不重开龟缩：极少量的金币买不起足以龟缩的兵力，且石材与木材仍必须靠资源点

**注意这条与"三种资源都由资源点产出"不冲突，而是它的一个有意的例外。**
若哪天想删掉它，必须先给金币断供找到另一条护栏——它不是历史遗留，是护栏。

**木材是所有永久建筑的配料，不只是维修材料。** 只让它管维修的话，开销被
「维修成本显著低于重建」这条反死亡螺旋规则压住上限，木材永远花不完，
"这波该攒哪种"就不成其为决策；连带攻方拆伐木场也不疼，"打哪一种"的纹理
跟着失效。作为配料，它的开销随建设规模一同超线性增长。

#### 波次进行中不禁止任何资源的使用

**限制器是时间与暴露，不是禁令。** 三种资源在波次中都可以花：

| 资源 | 波次中花它做什么 | 天然限制 |
|---|---|---|
| 石材 | 新建 / 重建墙段与塔 | **慢**，且工地就在杀伤区内，守不住就建不完 |
| 木材 | 抢修残血结构、即时放置木栅拒马 | 快，但只能补形状、不能扩形状 |
| 金币 | 征兵补缺口 | 训练要时间，从城内走到缺口也要时间 |

用禁令来区分三者是错的做法，试过一版又改回来了：

- **锁金币直接毁掉战术层。** 防御建筑造价高到必然留有薄弱段，缺口本就该由机动
  部队去堵；若开打后不能补兵，战果在波次开始那一刻就已定死，
  "经济决定你能买多少、**战术决定你能不能守住**"的后半句随之落空。
- **锁石材也不必。** 担心的是玩家边打边把墙补回去、令「压制墙头 → 破墙 →
  缺口攻防」三阶段失效。但**建造耗时 + 工地暴露**已经解决了：在缺口原地重建，
  等于把工人和资源送进杀伤区。这还顺带给 AI 一个可学的目标（点杀施工中的工匠），
  比一条禁令有价值得多。

木材的独特性因此是**速度**而非权限：石材买永久结构、代价是时间；木材买立刻生效
的临时结构。木栅与部队补的是同一个缺口而代价不同——木栅便宜、立刻、但不会移动；
部队灵活、但会死、且要从别处调来（那边就空了）。这组取舍本身就是一条决策。

副产品：攻方的经济打击从"减少总收入"升级为"**打哪一种**"——拆采石场削弱防线的**上限**（建不了更多墙塔），拆伐木场让防线**修不动也建不起**（木材是所有建筑的配料），拆金矿场削弱**兵力**。这让"攻其必救"具备纹理，也为宏观层提供了值得学习的目标选择。空中单位正适合执行此类打击（点掉偏远矿场远比撞墙划算）。
- **攻方没有经济系统**，改为两个自动给定的预算（均为波数的函数）。攻方攻击玩家经济的动机是削弱其长期能力，不是喂养自己。

### 资源分布形态

**地形枚举、地图数据格式与全部几何约束见 `地图与场景设计.md`**，此处不重复
（同一件事写在两处必然漂移）。只记录一条属于设计契约层的分布形态：

- **地图中心是资源密集区**，位于玩家初始城区内，构成**保底收入**（线性增长那条），
  开局资金即来自此处
- **中心之外成小簇分布**，形态是「**大散居、小聚居、交错杂居**」：
  - **小聚居** —— 一簇若干个点，密到值得为它单独围一段墙
  - **大散居** —— 簇与簇隔得开，玩家不可能全部圈进城，**必须选**
  - **交错杂居** —— 一簇内混有石 / 木 / 金，因此每一簇都构成"必救"；
    但**各簇的混合比例不同**，攻方仍能通过选簇来选择打哪一种资源

这个形态服务两条既有设计：**拐点**（城内收入撑不住时被迫出城）与**攻其必救**。
若外围资源均匀铺开，"围哪一簇"就无从选择，两条一起失效。

「外部资源点必须结构性地不可围墙」这条要求见 `地图与场景设计.md` 2.1，
**其实现手段有两个候选、尚未定**（逐点禁建环 vs 城区 + 有界前哨点），详见该节。
无论采用哪个，本节的分布形态都成立；两者的差别在于「哪一簇值得围」这个决策
是否保留、以及规则对玩家是否可见。

## 架构：三层隔离

```
rts_core/     C++17/20，全部游戏逻辑在这
render/       raylib（或 SFML）+ Dear ImGui，正式前端，只读 rts_core 状态
game/         玩家输入、编队、指令、关卡加载
bindings/     pybind11 → Python
train/        PyTorch PPO（CleanRL 单文件风格）
tools/        Python 工具链：数值平衡、数据分析、绘图
tests/        Catch2 / GoogleTest
```

**项目主体是 C++，但不要求全栈纯 C++。** 训练、数据分析、绘图、平衡脚本用 Python 是既定方案；构建用 CMake；配置与数值表用 JSON/TOML 数据文件。这些都不影响"核心是 C++"这一课程要求。

真正需要守住的不变量只有三条，它们保护的是**训练吞吐量与可复现性**，不是语言纯净度：

1. **仿真 tick 的热路径内不得出现渲染器、引擎、或向上回调 Python。** Python 一次调用应推进 N 个 tick，绝不能每个单位、每个 tick 回调一次上层——那样吞吐量会塌掉一到两个数量级。`rts_core` 必须能在完全 headless 下跑完整局游戏。
2. **渲染层是 `rts_core` 的只读观察者。** 渲染代码不允许修改仿真状态；训练时 `render/` 整个不参与编译/链接。
3. **训练回路里没有渲染器、没有引擎。** Python 经 pybind11 **in-process** 直接调用 C++ 仿真，不走 IPC、不做状态序列化。这是训练吞吐量能达标的前提，不要为了方便引入进程间通信。

### 关于第三方库

`rts_core` 可以用第三方 C++ 库，约束是下面两条而非"零依赖"：

- **不得引入非确定性**。这是选库时唯一需要严格审查的点：迭代顺序不稳定的容器不能用来驱动仿真逻辑；会重排归约顺序的并行算法库会破坏浮点可复现性，从而毁掉回放测试。
- **优先 header-only，并用 FetchContent/vendored 方式引入**，不要求队友在系统里预装。原因是工期约束：四名成员全部在 **Windows（MSVC）** 上开发，训练在 **Linux GPU 服务器（GCC/Clang）** 上跑，每个需要独立构建链接的依赖都要在两套工具链上分别调通。MSVC 是三大编译器中与标准分歧最多的一个，这条摩擦比通常情况更明显。

**开发环境约定**：本地一律是**纯 Windows / MSVC，不使用 WSL2**；Linux 只存在于训练用的 GPU 服务器上。CMake 需同时支持 MSVC 与 GCC 两条路径；为 MSVC 开启 `/permissive-` 与 `/Zc:__cplusplus`，把大部分 MSVC 特有的宽松写法挡在本地。

代价是 GCC 侧的编译问题只能在推到服务器后才暴露，因此**要尽早、频繁地在服务器上跑构建**，不要攒到临近答辩才第一次编译 Linux 目标。上面"优先 header-only"与"训练时 `render/` 整个不参与编译"两条约定在此有额外价值：需要在服务器上编译的代码面越小，两套工具链的分歧就越少。

不建议在仿真里嵌入脚本语言（Lua 之类）——tick 内跑脚本 VM 会拖慢训练，而配置需求用 JSON/TOML 数据文件就够了。

### 前端：为什么当前不经引擎

**本节记录的是一项工程选择及其理由，不是硬性规则。** 硬性的只有上面三条不变量；「不用引擎」是当下满足它们的手段之一，写在这里是为了让后来者读到判断依据、而不是读到一条无从质疑的禁令——本项目已两次犯过把手段写成原则的错误（早先的「`rts_core` 零依赖」与「交付物必须纯 C++」），这条不应成为第三次。

因为 `rts_core` 对渲染器无感知，渲染器本就是可替换的。当前方案是 `render/` 直接链接 `rts_core`——同一个进程、同一套 C++ 类型，没有 C ABI、没有 marshalling、没有跨语言胶水。

**不选引擎的理由不是"引擎不好"，而是本项目的约束组合让引擎发挥不出优势、缺点却全留下：**

- **课程要求核心逻辑为 C++，这封死了引擎最省力的用法。** 引擎最顺畅的姿势是"用它的脚本语言写逻辑 + 用它的编辑器搭场景"，热重载丝滑。我们只能把 C++ 编译成原生插件（Godot GDExtension / Unity Native Plugin），那恰是引擎最别扭的用法。
- **Windows 独占锁定已加载的 DLL。** 编辑器进程加载原生插件后就无法重编（链接器报 `LNK1104`），必须关编辑器→重编→重开。这是操作系统语义而非某个引擎的 bug——Linux 上不存在，但全组都在 Windows。放弃 Unity 正是为了消除它，而该问题在 Godot GDExtension 上原样存在：官方的 `reloadable = true` 在 Windows 上不稳定（编辑器崩溃、扩展类从场景中消失、ClassDB 状态损坏），社区变通是构建后把 DLL 复制成唯一名并改写 `.gdextension`，而该文件需入库，四人并行会持续冲突。真正的代价还不是每次那十几秒，而是它诱使人"攒一批改动再编译"——对一个以确定性回放测试为主要防 bug 手段的项目，这个习惯的伤害更大。
- **仿真全部自研，引擎的核心能力用不上。** 物理、碰撞、动画状态机、场景图、内建 ECS 一概不用（用了反而破坏确定性与 headless 要求）。真正剩下的只有渲染、输入、UI 与打包，而 ImGui 已覆盖 UI，raylib 本身也不低级——纹理精灵、字体、2D 相机、shader、输入齐备。
- **前端改由编辑器搭建会削弱交付论据。** `交付要求的工程影响.md` 把"前端是常规 OOP"列为应对「类对象设计」要求的主要展示部分；若 UI 与场景变成 `.tscn` 之类的配置文件，这部分 C++ 代码就不存在了。

放弃 Unity 直接换来的：省下约 3–5 人日的 P/Invoke 胶水层；避开上述 DLL 锁定（前两周正是 `rts_core` 接口最不稳定的阶段）；前端不再有 C# 层，"C# 前端是否满足课程要求"这一风险项随之消失。

**何时值得重新评估**（满足任一即可重开讨论，不必视为翻案）：

- ImGui + raylib 明显撑不住界面复杂度，UI 工作量显著超出 5–8 人日预算
- 出现确实需要引擎级能力的需求（复杂粒子、光照、动画混合），且团队有对应人力
- "核心逻辑必须是 C++"这一解释被老师放宽，从而可以使用引擎的脚本语言

**另注意："成品质量"的瓶颈通常不是引擎，而是美术资源。** 团队无美术，换引擎之后画的仍是同样的方块。更有效的手段是走 `tools/sprite_gen/` 的精灵流水线：程序化占位图先让开发跑起来，再以 CC0 素材（Kenney.nl 一类，免费商用、风格统一）预渲染替换。raylib 直接加载即可，不需要引擎介入。

**因此 `render/` 不是"调试视图"，而是正式前端，工作量要按正式前端估。** 一个可用的 RTS 界面需要框选、编队、建造放置与预览、HUD、相机控制、血条等，**预算 5–8 人日**，不要按"画几个方块"来排期。

分工建议：**Dear ImGui 负责面板与 HUD**（开发速度最快，外观偏开发工具但课程项目完全够用），**raylib 负责世界渲染与场景内 UI**（选择框、血条、建造虚影）。有余力再美化。

前端从第 1 周就持续开发，同时兼任 RL 调试工具——调策略时必须可视化观测张量、奖励信号与目标选择，ImGui 面板是最快的手段。

**注意上面 5–8 人日的预算不含演示与可视化功能。** **进演示的只有两项**：「双视图（真实战场 vs AI 的侦查记忆图）」与「`AI vs AI` 观战模式」，它们是前端的增量需求，排期时应一并重估。清单、判据与逐项取舍见 **`演示与可视化清单.md`**。

其中**双视图不可砍**，它承担双份职责：演示玩家如何主动欺骗 AI，以及给「AI 是否发现并利用已有缺口」这一核心评估指标提供**现场**证据（AI 的记忆图里那段墙是破的 → 它调头去打那里）。

**「不进演示」不等于「不做」。** 「显著性热力图」与「策略池胜率矩阵 / 非传递性环」都改为**只用于分析与报告**：前者要先解释遮挡法与动作分布变化量、后者要先解释非传递性环，都违反「不需要解释术语就能看懂」这条演示判据；但前者同时是 `CLAUDE.md` 已要求的 RL 调试手段、后者是「评估指标」一节的第一条。两者都落在 `tools/`（离线脚本），**不占 `render/` 预算**，显著性图最终做到什么程度待评估。

## 确定性要求

仿真必须是确定性的，这既是 RL 训练可复现的前提，也是主要的防 bug 手段：

- 固定 tick（20 Hz），不使用可变时间步。
- 所有随机性走一个可播种的 PRNG，禁止 `rand()`、禁止依赖未定序的容器遍历（如 `unordered_map` 迭代顺序）影响仿真结果。
- 浮点只需保证**同平台同编译器可复现**（固定种子即可），不需要定点数或跨平台位级一致。

  **注意本项目天然跨了两套工具链**：游戏与演示在 Windows/MSVC，训练在 Linux/GCC。不同编译器的浮点优化与求值顺序不同，因此**回放文件在 Windows 与 Linux 之间不可复现**，不要建立任何依赖此前提的工作流（例如"在服务器上复现玩家在 Windows 上录的对局"）。训练可复现性只要求服务器上同平台一致，这是够用的。策略权重本身是跨平台通用的，不受影响。

  服务器侧编译**不要开 `-ffast-math`**，MSVC 侧保持默认 `/fp:precise`，不要用 `/fp:fast`。
- **确定性回放测试是核心测试形态**：存一串随机种子 + 指令流，重放必须逐 tick 状态一致。新增任何影响仿真的改动都要能通过回放测试。

## rts_core 实现约定

### 硬性约束：扁平数组、无逐单位堆分配、多态按兵种而不按实体

单位与建筑存放在**扁平的连续数组**中，通过整型 ID / 索引引用。**禁止**下面这种写法：

```cpp
class Unit { virtual void update() = 0; };   // ❌ 虚分派
class Archer : public Unit { ... };
std::vector<Unit*> units;                     // ❌ 每个单位一次堆分配
```

**主要理由是确定性，不是性能。** 确定性回放测试是本项目主要的防 bug 手段，而它最常见的破坏源是**拿地址当 key 或排序依据**——`unordered_map<Unit*, X>` 的迭代顺序随地址变化，按指针值排序同理。扁平数组 + 整型 ID 从结构上消除了这种可能：没有裸指针可用作 key，这类 bug 就写不出来。这是「结构封死，而非数值劝退」准则在架构层的应用，与上文「禁止依赖未定序的容器遍历影响仿真结果」是同一件事的两个层面。

两点澄清，免得推出错误结论：

- **堆分配本身不破坏确定性。** `std::vector<std::unique_ptr<Unit>>` 按索引遍历的顺序等于插入顺序，与地址无关。禁止它是因为它让上述错误**变得可写**，外加 cache 局部性与观测打包的附带收益。
- **虚函数分派本身也是确定的**（vtable 调用）。禁止它的理由是性能与内存布局——vptr、强制堆分配与指针间接、无法内联，与确定性无关。

反过来同样成立：**用了扁平数组也不等于自动确定**。若在别处用 `unordered_map<int, State>` 驱动仿真遍历，一样会坏。本条约束消除的是一类错误，不是全部。

#### 本节标题原写「无虚函数分派」，那个说法过窄，已订正

**真正要禁的是「逐实体的多态继承体系」（上面那段代码），而按兵种的多态是允许且推荐的。**

`rts_core/include/rts/unit_behavior.hpp` 用 `UnitBehavior` 基类 + 11 个派生类表达兵种之间的**结构性**差异（三条正交轴 + 能否对空 / 能否破坏结构），虚函数与 `override` 都真的用上了。它不触碰上面任何一条理由：

- **每个兵种一个静态存储期实例，共 11 个** —— 没有逐单位堆分配，`spawn()` 不分配任何东西
- **它不参与任何遍历或排序** —— 仿真遍历的仍是扁平数组 + `UnitId`，所以「拿地址当 key 或排序依据」这条破坏确定性的写法在那一层无从发生

**两者的界线是实例的数量与生命周期，不是「用不用虚函数」**：11 个长生命周期的兵种单例 ≠ 每个单位一次 `new`。

这一层有两个动机，都不是「为了写得更 OOP」：

1. **本文自己要求克制关系由三条正交轴「推导」而非平铺成 N×N 表**（见「游戏设计要点」）。「推导」是行为，而「基类给通则 + 派生类只 override 例外」正好让每条例外与它的理由待在同一处。`Phoenix` 对城墙破坏速率为 0 是全表唯一的 override，写在它自己的类里
2. **交付要求「描述软件的类对象设计」需要一张真实的类图**（`交付要求的工程影响.md`）

它同时**不**替换 `roster.hpp` 的 `constexpr` 函数（`side_of` / `is_aerial` / `is_combat`）——那些要留着，因为有 8 条 `static_assert` 用着它们，且无 `default` 的穷举 switch 由 `/w14062` 与 `-Wswitch` 保证完备。虚函数丢掉的正是后一条保证，由 `tests/unit_behavior_test.cpp` 把两边互相印证补回来（**已做破坏性验证：注册表下标对调一处，编译器零警告，测试红**）。

### 已经提供的基元：先看这几个头，别自己造

**写在这里是因为不知道它们存在的人会先自己造一份**——最典型的是去拿 `<random>`，
被确定性守卫拦下才回头找（守卫的报错信息里点了名，但那已经是撞上之后了）；
而 `TerrainMasks` 与 `enumerate_units()` 这两条没有守卫兜着，
各写一份不会红，只会在某次回放对不上时才显形。

| 头 | 提供什么 | 一句话 |
|---|---|---|
| `rts/types.hpp` | `Tick`、`kTicksPerSecond`、`Side`、`Vec2`、`GridPos`、`Handle<Tag>` → `UnitId` / `BldId` / `ObstacleId`、`grid_of` / `center_of` | **三种句柄两两不可互换**（Tag 模板挡住）——它们是三组独立的扁平数组。**格心是 `(i+0.5, j+0.5)` 不是 `(i, j)`**，弄错会让每个单位稳定偏半格、看起来像精灵锚点没对准 |
| `rts/rng.hpp` | `Rng`（xoshiro128\*\*） | **状态可存取**，因为回放要存档恢复；不要用 `<random>` 的分布适配器 |
| `rts/hash.hpp` | `StateHash`（FNV-1a 64） | 选它**只因为它顺序敏感**；浮点必须走 `feed_f32` / `feed_f64` |
| `rts/terrain.hpp` | 五种地形 + `TerrainMasks`（`passable` / `buildable` / `blocks_vision` 三张按格位图） | **`buildable` 已经与 `no_build` 合成过**（`地图与场景设计.md` 4.2 那条规则的唯一实现处），别在调用处再 AND 一遍 |
| `rts/world.hpp` | `World`、`SlotPool<Tag>`、`WorldInit`、`slot_of` / `pos_of_slot` | 机制不在里面（见上）。要遍历一侧的单位一律走 `enumerate_units()`——**那是唯一的规范顺序**，观测打包与 `submit_actions` 必须同序 |
| `rts/fog.hpp` | `FogLayer`（每侧一份）、`Vis` 三态、`vis_value` | **三态不是两态**，取值走 `vis_value()`；写 `vis != Unseen` 就把它压回两态了 |
| `rts/replay.hpp` | 回放格式、`ReplayRecorder`、`replay_verify` | 录回放**不要绕过 `ReplayRecorder` 直接调 `World`**——漏记一条输入的症状是回放对不上，一个假阳性。它只存**外生输入**，机制产物一律不存（理由见 `rts_core 接口契约.md` §4.6）。**改 `state_hash` 的喂入清单时把 `kWorldHashTag` 一起改** |

**方法名以头文件为准，此处不复制一份**（会漂移）。但有一条碰头时很难自查的要写下来：

> **`Vec2::operator==` 与 `StateHash` 对 ±0.0 的判断相反，这是有意分工**，
> 因此 **`a == b` 并不蕴含 `hash(a) == hash(b)`**。回放比对用哈希、单元测试比较位置用 `==`，
> 两者迟早在同一处碰头，看到的现象是「状态相等但哈希不等」。
> 理由见 `types.hpp` 该处注释——**只留指针，不复制内容。**

### AoS 还是 SoA：实现细节，不作硬性规定

这一层留给实现者判断，两种都可以：

```cpp
// AoS
struct Unit { float x, y; int hp; UnitType type; int level; };
std::vector<Unit> units;

// SoA
std::vector<float> x, y;  std::vector<int> hp;  std::vector<UnitType> type;
```

SoA 在本项目有一个具体好处值得知道：**观测张量的打包会退化成 memcpy**。RL 每步都要把所有单位的位置/血量/等级打包成连续数组交给 Python，SoA 下这些数组本就现成，AoS 下需要逐单位 gather。但 40 单位的规模下这不是瓶颈，不值得当成规则。

**注意：内存布局与"是否面向对象"是两个正交问题。** 一个类完全可以封装 SoA 数组并对外提供方法——那既是 SoA 也是标准的面向对象设计：

```cpp
class World {
public:
    UnitId spawn(UnitType t, Vec2 pos, int level);
    void   damage(UnitId id, int amount);
    void   step(Tick dt);
private:
    std::vector<float> x_, y_;      // 布局是私有实现细节
    CombatSystem  combat_;
    Pathfinder    pathfinder_;
};
```

因此本节的约束**不构成"不能写面向对象代码"**，它只排除了逐实体的多态继承体系。
**按兵种的多态继承体系是允许且已经在用的**，见上面「多态按兵种而不按实体」与
`rts_core/include/rts/unit_behavior.hpp`。

- 地图是 grid；寻路用 flow field 或格子 A*。**墙段按「高代价可通行」而非障碍处理**，代价 = 墙血量 ÷ 该单位破坏速率，于是"绕远走缺口"与"就近砸墙"自动比较，不需要专门写找缺口的逻辑。破坏速率随等级缩放（见「结构破坏规则」），所以 field 是**兵种 × 等级档**而非兵种尺度的常数——**等级要量化成少数几档破坏速率**，档数是接口契约里要定的形状，不能等寻路写到一半才发现（`无尽模式与地形分层.md` §7.7.5）。地图数据格式、地形枚举与结构约束见 **`地图与场景设计.md`**。
- 提供 `BatchedEnv`：用线程池一次并行 step N 局游戏，直接向 Python 返回打包好的 observation 张量。避免 Python 侧循环开销。

## 世界观与命名

奇幻设定：**人类王国（守方）vs 亡灵/魔物大军（攻方）**。该设定被选中的原因是它让机制上的不对称在叙事上自洽——攻方无经济（亡灵不事生产）、预算随波数增长（黑暗力量积蓄）、攻方有空军而守方没有（凡人不会飞）、攻方不在乎伤亡（亡灵本就是消耗品）。改动世界观前请确认这些机制解释仍然成立。

**命名纪律**：代码中一律使用英文标识符，中文名只出现在展示层。所有单位/建筑必须登记在命名对照表中（**中文展示名 ↔ 代码标识符 ↔ 三轴定位**），新增单位时先登记再写码。四人并行开发时若各自翻译中文名，同一单位会出现多个标识符。

**风味名不得遮蔽机制可读性**。玩家必须能快速读懂来袭编成才能应对，而"鬼域骑士团"这类名字不传达任何机制信息。因此机制信息由**视觉剪影与 UI** 承担：同类单位共享视觉语言（例如所有攻城器械均为笨重木质轮廓）。2D 俯视角下靠剪影与配色即可实现。

**器物用描述名，阵营单位用风味名。** 器械与建筑（攻城锤、城墙、木栅、采石场）直接以用途命名，玩家一看就懂；活的单位（鬼域骑士团、不死鸟、游猎斥候）用风味名承载身份，机制信息交给剪影与 UI。

**标识符一律 ≤7 字符。** 攻守双方阵营不重叠，因此不加 `Atk`/`Def` 前缀，直接用不同词区分（守方射手 `Archer`、攻方射手 `Shade`）。

**这条约束的对象是进入代码的标识符**——单位、建筑、地形枚举成员、**以及任何被仿真引用的实体**。**纯渲染层的素材键不受此限**：贴图变体（`PlainDirt`、`PlainRoad`、`ForestB` 等）只存在于 `assets.json` 与精灵文件名里，不进任何枚举、不被仿真引用，长度不影响代码可读性。写明这条豁免是因为规则原文是「一律」，不写就是静默违反。

但这条豁免有两个边界，都踩过：

- **不要把它扩大成"素材侧可以随便命名"**——素材键仍须与它所属的枚举成员**同前缀**（`Plain*` 属 `Plain`、`ForestB` 属 `Forest`），否则渲染层挑变体时无从归类。`ForestB` 曾叫 `TreeBig`，那时它是景物、不属于任何枚举成员，所以没有前缀可对；被改成 `Forest` 的贴图变体之后才越了线。
- **豁免的前提是「不被仿真引用」，而那个前提会被改动破坏。** `Sapling` / `Rubble` 曾叫 `TreeSmall` / `RockSmall`（均 9 字符），当时确实只是景物；后来它们改成了**有血量的可破坏障碍**（提案「无尽模式与地形分层」§6，2026-08-29 通过），「有血量」就是「被仿真引用」，前提一失效就得回落到 ≤7 字符。**改动一个实体的机制时，要回头检查它是否仍在豁免范围内。** 注意重命名是在 §6 表决**之前**做的（#30），依据是「豁免的前提正在被破坏」而不是「已经被破坏」——**这个提前是对的**：等到表决通过再改名，中间那段时间里已经会有人照旧名写码。

**动作、命令与观测通道的枚举成员同样不受 ≤7 字符之限**（`SelectForce`、`Composition`、`AllyDensity`、`ObstacleHp` 都超了）。理由与素材键那条一样：约束的对象是**实体**，而它们不是实体——它们是动作空间与张量通道的名字，且**要被 Python 侧当标识符读**（`obs.channels.ally_density`），缩写在那一侧只会变成一份要对照的密码本。写明这条是因为规则原文是「一律」，不写就是静默违反——与素材键那条同源。

但它继承同一个边界：**这三类的名字仍须成组同前缀**（`Move*` / `Atk*`、成对通道的 `ally_*` / `enemy_*`），因为前缀本身承载信息——`ally_` / `enemy_` 那一组的前缀就是「这两条要配对取」，而那条约定由 `tests/obs_test.cpp` 检查。

### 命名对照表：可破坏障碍

这是**第三类实体**——不是单位、不是建筑、也不是地形。上面三张花名册都没有它的位置，因此单列一张。

| 中文 | 标识符 | 视觉 | 破坏后产出 |
|---|---|---|---|
| 树桩 | `Stump` | 木质、贴地 | 木材 |
| 幼树 | `Sapling` | 单株细树，明显矮于密林 | 木材 |
| 碎石 | `Rubble` | 一小堆碎石，矮而散 | 石材 |

**它们不吃「三轴定位」**（交战距离 / 打击形态 / 机动性）——那三轴是为参与克制矩阵的战斗单位设的，而障碍物不移动、不攻击、不进克制矩阵。它们只有两条属性：**血量**与**破坏后的产出**（前者是数值、待定；后者是上表那一列，是结构）。因此本表不登记三轴。

视觉规则是一维的：**矮而单薄 = 可破坏；高而厚重 = 不可破坏**（`Rock` / `Forest` / `ForestB` 属后者）。等距下剪影高度是最稳的线索，而玩家把防线靠在一段其实能被打穿的障碍上是**一次不可挽回的误判**。曾用过「木质 / 矮 / 稀疏」三个维度，但它们**不共变**——`ForestB` 是木质 + 巨大 + 单株，三条里两条指向错的答案。

**产出跟着材质走，玩家不需要额外记一张表**：视觉规则已经把材质写进设计（「木质 = 可破坏」），于是木质的产木材、石质的产石材。唯一实现处是 `rts/roster.hpp` 的 `harvest_of()`。

#### 三条结构约束，改动前先读它们的理由

**这三条都是结构，不是数值旋钮**，完整论证见 `无尽模式与地形分层.md` 6.6：

- **没有任何障碍产金币。** 金币管人力而清野不产人力（三种资源各对应一条决策轴）；且 `Keep` 那条「兵力地板」是**唯一**的非资源点金币来源，再开口子等于悄悄削弱它的唯一性。
- **不可再生。** 可再生 + 产出资源 = 无限资源循环，属「结构封死，而非数值劝退」。
- **建筑摧毁不产生 `Rubble` 实体。** 本文另有「建筑摧毁后留下废墟/地基，原地重建打折」与「废墟回收比例：造价 ÷ 3」两条；若废墟是 `Rubble` 实体且拆它产石材，就有一条「建墙 → 被拆 → 拆废墟得石材 → 打折重建」的净赚回路，而那三个数值分别在三处定。**本文里的「废墟」始终是建筑自己的一个状态（一个折扣），不是实体**，回路因此不存在。

> **`Rubble` 的英文本义就是「废墟」，而「废墟折扣」是另一个机制。** 玩家侧不会混（中文展示名是「碎石」），但代码侧容易——那正是上面第三条要封的洞的入口。不改标识符（连带 476 张精灵文件名不值），把警告留在这里。

## 兵种与建筑花名册

### 守方（人类王国）

| 中文 | 标识符 | 定位 | 存在理由 |
|---|---|---|---|
| 戍卫弓手 | `Archer` | 中程单体 | 上墙吃高度加成，防线主力输出 |
| 铁壁枪卫 | `Spear` | 近战抗骑 | 堵缺口 + 克骑兵冲锋（开阔地克，巷战被反克） |
| 逐风猎骑 | `Ranger` | 快速近战 | **出城的执行手段**：摸攻城锤、争夺墙外矿场 |
| 游猎斥候 | `Scout` | 极快、大视野、无战力 | 侦查集结区。廉价可消耗 |
| 工匠 | `Mason` | 无战力 | 维修。脆，促使 AI 学出"优先点杀维修工" |

`Ranger` 不可与 `Scout` 合并：斥候必须**便宜到可以消耗**，情报交易才诚实。若侦查要动用主力机动部队，代价是"缺口没人堵"，玩家将永不侦查，该系统即失效。

`Ranger` 亦不可省略：`Spear` 太慢够不到墙外矿场，没有机动单位则"被迫出城争夺外部资源"缺乏执行工具。

### 攻方（亡灵/魔物）

| 中文 | 标识符 | 定位 | 存在理由 |
|---|---|---|---|
| 亡灵步兵 | `Ghoul` | 近战单体、耐打 | 主力肉盾，吸塔火抢缺口 |
| 亡灵弓手 | `Shade` | 中程单体 | **持续压制墙头守军**。无此单位则守方弓手可白嫖射死攻城锤，三阶段攻防不成立 |
| 鬼域骑士团 | `Knight` | 快速冲锋 | 冲锋伤害 ∝ 助跑距离。克远程与攻城锤，突袭墙外经济，**遏制守方出城** |
| 不死鸟 | `Phoenix` | 空中、高伤 | 手术刀。硬性数量上限、跨波留存、被击落后需 N 波重生 |
| 幽影窥使 | `Wraith` | **地面**、极快、大视野、无战力 | 侦查玩家防御布局。使"先侦查再决定主攻方向"成为可学的动作 |
| 攻城锤 | `Ram` | 贴身、AOE、对建筑特攻 | 高效破墙手段。慢、必须走到墙下，是玩家的头号集火目标 |

### 建筑

**所有永久建筑都同时消耗石材与木材**（配比不同：墙体偏石，门楼与楼板偏木）。
下表「资源」列只标其**偏重**。

| 中文 | 标识符 | 资源 | 说明 |
|---|---|---|---|
| 领主堡垒 | `Keep` | 石+木 | 核心，丢失即败。**产出极少量金币，作为兵力地板**（见下）。**可升级，且是守方唯一的升级轴**——抬高人口上限、建筑等级**上限**、兵种等级**上限**。**不改变任何已有建筑**，每座仍需单独升级 |
| 城墙 | `Wall` | 石材 | 可站人。**可升级，只加血量**（等级上限由 `Keep` 给出） |
| 城门 | `Gate` | 木材 | 木制，破坏速率高于城墙——结构上的既定薄弱点。**可升级，只加血量，与 `Wall` 同倍率**——同倍率是为了保住「门破得更快」这个**比例**，否则升级会把这个既定薄弱点抹平 |
| 镇野箭楼 | `Tower` | 石材 | 对地，**齐射覆盖**（克制步兵一拥而上啃墙） |
| 蔽空弩楼 | `Flak` | 石材 | **仅对空**，单体狙击；视野否定半径 > 伤害半径 |
| 瞭望塔 | `Watch` | 木材 | 大视野、**零战力**。与箭楼争同一个位置，构成"看得见 vs 打得着"的取舍。**视野不得覆盖集结区**，见下 |
| 兵营 | `Barrack` | 石+木 | 部队产出点。**离前线越近补员越快、也越容易被点掉**，位置即决策 |
| 木栅 | `Fence` | 木材 | 廉价应急工事，波次进行中可即时放置 |
| 采石场 / 伐木场 / 金矿场 | `Quarry` / `Lumber` / `Mine` | — | 建在资源点上。墙内为保底、墙外为增量 |

新增的三座各有非重复职能，若工期吃紧，**砍的顺序是 `Watch` → `Barrack`**
（前者可由斥候替代，后者可退化为"从堡垒出兵"）；`Mine` 不可砍，它是金币的唯一来源。

**不做「登墙阶梯」这类建筑。** 曾考虑过，理由不成立：它没有取舍——你总是要造、放哪儿
都行。这是杂活而非决策，而**「城墙可站人」是机制、「先造楼梯才能上墙」是杂活**，
两者不必绑在一起。实现代价也不小：墙顶会变成第二层可通行面，寻路要处理入口节点。

改用**驻守槽位**：直接命令单位占据某段墙，单位原地登上（短暂延迟），不需要楼梯、
也不需要墙顶寻路。上墙的代价是**机动性**——上去的单位被钉在那一段，
想换位置要先下来。这才是真正的取舍。

攻守双方各有**非战斗单位**（守方 `Scout`/`Mason`、攻方 `Wraith`）不进入克制矩阵，仅作为猎杀目标，因此实际的平衡负担小于花名册的单位总数。

### 结构破坏规则

**任何单位都可以破坏城墙**，但：

- **破坏速率随等级缩放，而城墙与城门也在同一条等级轴上（只加血量、两者同倍率）。这两条必须成对存在。**
  结构锁不再靠「速率恒定」来守，改为守一条更锐利的不变量：**破墙时间必须落在一个区间里，不趋于 0 也不趋于 ∞。**
  单独任何一条都比原状更糟——只让墙升级则破墙时间 → ∞（龟缩复活），只让速率随等级则 → 0（三阶段攻防的中段消失）。

  **改这条是为了修一处内部矛盾，不是为了加机制。** 原来的写法与「建筑与城墙走**与单位完全相同的战斗解算**」直接冲突：同一套解算 + 等级提升伤害 ⇒ 破坏速率必然随等级涨。原写法要成立，只能在「不要做成独立的特例系统」那条明令上挖一个洞——让建筑的承伤走一条与单位不同的路径。改完之后两条才真正一致。原来的理由（「满级步兵一拥而上瞬间破墙」）是手挥式的，已被上面那条区间不变量取代。推导见 `无尽模式与地形分层.md` §7.7。
- **`Ram` 的价值是效率而非安全。** 步兵砍墙慢，需长时间站在杀伤区内挨打，用尸体换缺口；攻城锤拆得快，总暴露时间短。攻方是亡灵、不在乎伤亡，因此"用命换"本就是正当打法，**攻方不需要绕开杀伤区的破墙手段**。
- **`Ram` 是贴身撞锤，不是抛射器械。** 它前摇长、一击 AOE：对**静止的建筑**全额命中且可波及相邻墙段与贴近的塔；对**能散开的单位**效率很差。因此它与 `Shade` 分工明确——攻城锤是爆发拆除，亡灵弓手才是持续压制。此规则复用既有的"溅射克密集、散开克溅射"机制，不需特例。

  本条原写「装填慢、弹道有飞行时间」，**那与花名册那一列写的「贴身」直接矛盾**，
  已按 2026-08-29 的口头定案改正。它误导过一次（提出重开投掷物贴图时），
  而精灵流水线其实一直按撞锤做（撞木滑动，`tools/sprite_gen/README.md` §2）——
  **实现比描述更早地做对了**，所以这处只是文字错。

  > **但改这一句时漏了它的另一个副本，因为那个副本换了个词。** 「游戏设计要点」的
  > 机制性克制清单里原写「**投石车**对静止密集目标高效，对散开移动目标大幅 miss」——
  > **花名册里没有投石车**，那一句写的就是 `Ram`，而「投石车」的定义正是抛射器械。
  > 于是它是本条刚删掉的那个错误的另一份，只是 `grep Ram` 搜不到它。已一并改正。
  >
  > 这不是措辞洁癖：本仓库里「投石车」**有所指**——`Flak` 塔顶装的就是投石机造型
  > （`tools/sprite_gen/README.md` §2.1 专门论证过为什么必须是它）。留着那一句，
  > 读者要么以为存在一个叫投石车的单位，要么以为 `Flak` 能对地。
  >
  > **它也是「同一件事写在两处然后漂移」的一个新变种**：这次漂的不是内容而是**词**，
  > 所以按标识符搜查不出来。订正一条机制时，除了搜标识符，还要搜它的**同义描述**。

  **删掉「飞行时间」之后，「对散开的单位效率很差」要靠另一条实现要求撑着，
  而它必须写下来**：**AOE 落点在前摇开始那一刻锁定成一个坐标**，伤害在前摇结束时
  按那个坐标结算。若落点跟着目标句柄走（每 tick 重算），散开就完全无效——
  撞锤永远打在人身上，而"溅射克密集、散开克溅射"这条机制性克制随之作废。

  **这条失效不会让任何测试变红**：它是机制正确性、不是确定性，回放照样逐 tick 一致。
  所以它不能留给 1c 自己悟，写在这里。它同时决定了「攻击目标」这条接口信息的形状——
  远程要的是**目标句柄**（箭朝一个单位去），`Ram` 要的是**锁定的落点坐标**
  （目标走了，AOE 还是砸原地），两者不是一种东西；统一存坐标即可，
  句柄能退化成坐标、反过来不行。
- **除 `Ram` 外，`Archer` / `Shade` / `Tower` / `Flak` 的攻击真有在途弹丸**，
  因此「承诺攻击的那一帧」与「伤害落地」在这四者身上也是两件事，只是成因不同
  （它们是飞行时间，`Ram` 是前摇）。这条是渲染层与精灵元数据的分界，
  见 `render/include/render/sprite_atlas.hpp` 的 `impact_frame_of()`。
- AOE + 对建筑特攻使**建筑间距成为决策**：塔挨着摆火力集中但会被一发端两座，摊开摆抗打击但覆盖变稀。

### 克制二部图

```
攻方           被克于
─────────────────────────────────────
Ram      ──►  Ranger 快速切入、出城强袭
Ghoul    ──►  Archer 拉扯 + Tower 齐射
Knight   ──►  Spear（开阔地）
Shade    ──►  Ranger 冲脸、城墙的高度惩罚
Phoenix  ──►  Flak（且仅此，纯位置性）
Wraith   ──►  任何机动单位（猎杀以否定情报）

守方           被克于
─────────────────────────────────────
Archer   ──►  Shade 压制、Phoenix 点杀
Spear    ──►  Shade（慢速近战被风筝）
Ranger   ──►  Knight（骑兵对冲，遏制出城）
Scout    ──►  Knight/Wraith 屏蔽与猎杀
Mason    ──►  Phoenix、Shade 狙杀
```

无孤立节点。`Knight` 遏制 `Ranger` 出城这一条尤其关键——它让攻方骑兵具备**防御性威慑**作用，而不只是进攻工具。

## 侦查系统

侦查与防空两难是同一机制的两半：玩家不知道这波有没有不死鸟，"造不造蔽空弩楼"就不是决策而是掷骰子。

### 情报划分：免费 vs 需侦查

| 免费可见 | 需要侦查 |
|---|---|
| 波数、大致到达时间 | **编成构成**（几架不死鸟？几台攻城锤？） |
| 本波总兵力（单个数字） | **精确的进攻方向与分兵情况** |

总兵力白给，玩家才能做规模上的准备；"这波是什么形状"必须自己去看。玩家的决策因此是：**花钱知道，还是把钱直接砸进防御赌一把。**

### 集结区

每波部队在建造阶段于地图边缘集结，之后才发起进攻。

- **固定候选点，AI 每波自行选择**，可同时使用多个（即佯攻）。全固定单点则无决策可言；允许 AI 自由选位则宏观层动作空间爆炸、玩家也无法侦查一条连续边界。固定候选点使其成为 bandit 尺度的小决策，正好挂在宏观层。**候选点的具体数量属待定数值**——它直接决定玩家每波要防几个方向，是平衡性与游戏体验的旋钮，须等框架完善后统一标定（见「关于数值」）。
- **每个集结点必须对应一条性质不同的进攻走廊**，否则 AI 选哪个都一样，"选集结点"没有可学的信号、分兵佯攻退化成随机分兵。**这是结构约束，与数量定没定无关**；它同时意味着集结点数量 = 走廊种类数，两者不是各自独立的旋钮。走廊性质的候选清单（开阔平原 / 隘口 / 林地 / 经济侧）及其对应机制见 `地图与场景设计.md`——**该清单是候选，不是定数**。
- 分兵有被各个击破的风险，因此佯攻是真决策而非白嫖。
- **免费方向提示 = 兵力最多的那个集结点。** 情报本身诚实，但 AI 控制它诚实的对象——把佯攻部队堆得比主攻更多，提示即成诱饵。玩家有侦查起点而非全盲乱找，AI 获得一个可学的欺骗手段。
- 集结区不设特殊保护，玩家可以强袭，但那是撞上满编部队，通常得不偿失——靠自然平衡即可，无需额外规则。
- **任何静态建筑的视野都不得覆盖集结区**（`Watch` 瞭望塔是最需要注意的那个，但这条对所有建筑成立）。**这是结构约束，不是数值。** 整个侦查博弈的前提是**情报要花钱买、且会过时**：斥候便宜到可以消耗，波次结构让记忆天然过时。一座永久建筑若能照亮集结区，「这波要不要花钱侦查」就被**一次性买断**——`Scout`、`Wraith` 屏蔽、佯攻诱饵会一起失效。这是把一条持续的经济交易换成一次性支出，**性质变了，不是强度问题**，所以不能靠"把视野半径调小一点"解决。可校验的形式是「集结点到最近的可建造格的距离 > 静态建筑视野半径上限」，属地图校验器条目，见 `地图与场景设计.md` 第 8 节。

### 双向欺骗

两侧都有斥候，博弈才闭环：

| 谁 | 手段 |
|---|---|
| AI | 分兵佯攻、藏起不死鸟不进侦查范围、屏蔽集结区、猎杀 `Scout` |
| 玩家 | **等 `Wraith` 走了再造蔽空弩楼**、故意露一段假薄弱墙、猎杀 `Wraith` 让 AI 带错误情报开打 |

玩家侧那条使**建造时机成为决策**：知道 AI 斥候何时来看，就可把关键建筑压到它离开之后再造。这是玩家主动欺骗 AI 的手段，而非被动挨打。

### RL 侧两条硬要求

1. **AI 的观测必须是它自己的迷雾状态，不得是 ground truth。** 不做这条则"欺骗"毫无意义——AI 看得见一切，玩家藏防空塔就是白藏。这是整个智斗设计的地基，实现上不可图省事。
2. **击杀 `Scout` 需给予即时奖励。** 与"摧毁经济建筑"同类：信息否定的收益无法在 episode 内衡量，不给奖励则 AI 永不会学习屏蔽集结区或猎杀斥候。权重只能手调（无法像重建成本那样有原则地推导），给一个适中常量即可。

**波次结构使 AI 的记忆天然过时**：玩家的新建筑正是在波次之间建造的。这既是玩家反制 AI 的天然手段，也是 `Wraith` 必须存在的理由——仅靠上一波的记忆，AI 在第 1 波全盲，且没有"侦查"这个**动作**可供学习，而"先派斥候再决定主攻方向"正是最能体现其智能的可演示行为。

## 波次结构与数值曲线

一波 = 一个 RL episode，波次之间是建造/准备阶段。这个结构同时承担三件事，改动前务必理解：

1. **短 episode** 让 credit assignment 链条足够短，是训练可行的关键。
2. **波数即难度轴**，天然构成课程学习，训练时按波次分层采样，不需要手工设计 curriculum。
3. **同一玩家连续打数十波**，才给了"AI 适应玩家风格"足够的观察样本。单场对局窗口太短，这个功能立不住。

### 攻方双预算：编成位 与 兵力

攻方的资源是**单一维度**的（与玩家的多资源相对），拆为两个自动给定的预算：

| 预算 | 增长曲线 | 用途 |
|---|---|---|
| **编成位** | 线性，**硬性封顶约 40** | 购买单位数量 |
| **兵力** | 超线性 | 购买单位等级 |

编成位耗尽后，多余兵力**只能**投入等级。这在结构上保证单位数永远落在 RL 可训练区间，同时波次强度的增长不设上限。

**单波单位数上限 20–40 是硬性约束**，源自 RL 现实：多智能体微操研究普遍限制在 20–30 单位以内，做得最好的 curriculum transfer 工作在 40v60 规模胜率已明显下滑。同类游戏（They Are Billions、Diplomacy is Not an Option）后期波次动辄数百单位，**本作必须刻意偏离这一类型惯例**。仅靠"数量不如质量划算"的数值劝退是不够的——按结构封死准则，必须是硬上限。附带收益是 20 个单位的战术意图玩家看得懂，数百单位的混战无法展示 AI 的智能。

**这条上限区分攻守，不是对称的。** 攻方仍是硬上限（上面那条 RL 现实）；**守方改为随堡垒等级增长的人口上限**（见「守方的一条升级轴」）。守方那一侧的上界不来自 RL——守方单兵是参数化脚本、不上 RL——而来自**演示可读性与 `render/` 预算**（`无尽模式与地形分层.md` §8.2）。

兵力**允许不均匀分配**，攻方由此可以选择编成形状（40 个低级杂兵铺开消耗 / 8 个高级精英硬凿一点 / 一名满级精英带一群炮灰）。该决策需要读取玩家的防御形态才能选对——防线均匀则宜集中凿点，防线有薄弱段则宜铺开找洞——因而是"AI 适应玩家"最直观的演示形式之一。RL 侧代价很小：观测中每个单位增加一个等级标量，宏观层动作空间仍是 bandit 尺度。

**这段现在双向成立。** 守方也在选编成形状——人口上限管数量、金币管质量（征兵时选等级），所以上面这套说法可以直接引来说明守方侧，不需要另立一套论证。

**等级只提升血量与伤害，绝不改变克制倍率、射程、速度或任何机制。** 否则高等级单位会靠属性压过其克制者，克制关系被淹没，智斗随之消失。所有机制性克制（冲锋助跑距离、溅射 vs 散开阵型、攻击前摇）必须**等级无关**。

此要求与"克制用乘法"是配套决定，**不要单独修改其中一项**：乘法克制在等级缩放下保持不变，加法克制（AoE2 的做法）会被等级淹没。

也不要设计"达到 N 级解锁某能力"——设计空间与观测复杂度都会膨胀。**机制多样性由兵种承担，等级只管数值。**

**但「哪几个数值」本身是一个待定的结构选择，不是已定。** 血量+伤害 / 只血量 / 只伤害
三种选法**都满足**上面那条原意（别让等级改变机制），而它们的有效战力指数**差一倍**：
两个都涨是 L²，只涨一个是 L。现行写法选中了最陡的那一档，**可能是无意的**。

这一条今天要定的是**归口与判据，不是答案**：三种各跑一遍成本产出矩阵，
看哪种下克制关系在高波次仍然决定胜负。**判据只能在野战里测**——
城墙攻防里几乎没有机动，冲锋助跑、枪阵克骑、风筝根本不发生
（`无尽模式与地形分层.md` §1.4 / §7.6.5）。

### 守方的一条升级轴：堡垒等级

**攻方有一条无界成长轴（兵力预算超线性），守方一条都没有——这个不对称就是「必败」的来源。**
无尽模式下玩家最终一定会输，这是设计内的（所以目标是分数、见「评估指标」）；
但输的方式不该是「到某一波之后你做什么都没用」。

因此守方也有**一条**成长轴，且只有一条：**领主堡垒的等级**。它有三个输出：

| 输出 | 管什么 |
|---|---|
| **人口上限** | 守方能同时养多少单位（取代原先那条对称的「20–40 硬上限」） |
| **建筑等级上限** | 每座建筑仍需**单独**花钱升级，堡垒只抬高天花板 |
| **兵种等级上限** | **征兵时选等级**（1..上限任选，越高越贵），已有部队可**批量升级**（取差价、需时间、须在 `Barrack` / `Keep`） |

外加一个用途：**「买活」**——金币可以买回**节奏**（在固定召唤点补一批兵），
但买不回**位置**（召唤点固定、不得突破人口上限、价格递增）。

四条必须一起认下的约束：

- **堡垒等级本身不设上限。** 早期经济复利由「**顶不满**」封住，而不是由一张档位表封住——
  钱再多也来不及把所有输出同时顶满，于是「先抬哪一项」成为决策。这比一个硬上限好，
  因为硬上限到了之后经济就再次变成纯粹的碾压
- **等级只提升血量与伤害**（同攻方那条，逐字适用，且在守方一侧理由更强：
  若满级 `Archer` 射程变远，玩家就不再需要另外四个兵种，**守方那半张克制二部图会直接塌掉**）
- **取兵种级，不取个体老兵制。** 老兵制让惨胜额外附带**战力永久倒退**，
  正是防死亡螺旋要防的形态，而现有那四条对策救的都是建筑、不是兵
- **无分支、无解锁、无科技树。** 一条轴、三个输出，仅此而已

**为什么兵种等级不是可选项**：城防有墙和塔兜底，**野战一样都没有**。
而野战是被「必须存在一个拐点使玩家被迫出城」与「资源点随波数解禁」**强制发生**的。
砍掉兵种等级等于承认「拐点之后玩家无法野战」，那会同时作废那条拐点要求、
`Ranger` 的存在理由、「部分资源点在墙外」这条不能砍的机制（`无尽模式与地形分层.md` §7.5）。

**RL 侧的代价是零新通道**：守方单位等级与建筑等级**共用**观测里已有的那条等级通道，
不新增通道数（`rts/obs.hpp` 的 `ally_level` / `enemy_level`）。

### 资源点随波数解禁

**外部资源点不是一开始就全部可用，而是随波数逐批解禁，且越晚解禁的越远。**

这条把「拐点」从一次性事件变成**反复出现的节奏**：每一批解禁都重新提出同一个问题——
去拿，还是守住现有的。它同时是上面那条升级轴的**资源来源**，两者是双向的：
解禁给钱，而新解禁的簇越远、去拿它越需要野战战力，那来自兵种等级。

它需要地图格式增加一个「起始波数」字段（**不能混进 `tier`**，因为它影响仿真），
以及一条「解禁序列按距离单调」的校验。见 `地图与场景设计.md` 与
`无尽模式与地形分层.md` §3。

**适应只改风格，绝不改强度。** 攻方预算严格是波数的函数，与玩家表现无关；风格适应只改变兵种配比与攻击目标选择，永不改变投入总量。若难度可因玩家表现下调，知情玩家会故意在前期打烂来压低后期难度——这会把本作的核心卖点变成可套利的漏洞。业界对自适应难度的共识也是只能单向上调。

**必须提供"提前召唤下一波"（附奖励）。** They Are Billions 的一个明确缺陷是玩家经济起飞后只能干等，既无加速也无法提前触发。成本极低的标准解法，不要遗漏。

调数值时的两条硬性曲线要求：

- **城内保底收入按线性增长，波次强度按超线性增长。** 必须存在一个拐点使玩家龟缩不再可行、被迫出城争夺外部资源。城内收入若能一路撑到通关，野战机制（骑兵冲锋助跑、枪阵、风筝）与"攻其必救"全部失效。**主要的难度旋钮是「解禁节奏与强度曲线之比」，不是单个拐点的位置**——资源点随波数解禁（见「资源点随波数解禁」）把拐点从一次性事件变成反复出现的节奏，所以要调的是两条曲线的相对斜率。
- **经济不得压倒战术。** They Are Billions 的玩家共识是"难度由早期经济决定，后期战术无力回天，再多微操也打不赢最后几波"。对本作而言这是致命的——经济若能覆盖所有战术失误，耗时数周训练的战术 AI 就被架空，玩家感受不到智斗。设计目标是：**经济决定你能买多少，战术决定你能不能守住**。
- **防御建筑造价要高到无法覆盖所有墙段。** 防御必然存在薄弱处，攻城器械专克建筑，缺口只能靠机动部队去堵。原则是**建筑定义防御的形状，部队负责补形状里的洞**。否则游戏退化为纯塔防，单位操作与战斗系统形同虚设。

## 防御建筑与城墙是消耗品

防御建筑和城墙**有血量、会被摧毁、需要花资源重建或维修**，不是一次性摆放的永久设施。防线的形状在一波之内会动态退化，玩家需要实时判断先补哪段、放弃哪段。这一点是多条设计的支点，不要简化掉。

由此产生的建造阶段决策是三方的：**修旧 / 建新 / 造兵**。

**必须防范死亡螺旋**——某一波惨胜、防御全毁、无力重建、之后每波更惨，玩家明知必输仍需拖很久。

> **这条标题此前 overshoot 了它自己的论证，已按 `无尽模式与地形分层.md` §1.3 重述。**
> 原文写的危害是「玩家明知必输仍需拖很久」——**那是节奏问题，不是公平问题**。
> 而无尽模式下**每一局都以螺旋收尾**，完全防止它等于让游戏永不结束；更要紧的是
> 完全防止会把「放弃哪段墙」的长期代价消掉，而那条代价是本文自己要求的。
>
> 所以要防的准确形态是**「慢性死亡」**，对策是两条：**快速结算**（局面已定时能迅速走完）
> 与**始终有决策**（哪怕在下坡路上，每一波仍有值得权衡的选择）。
> 下面四条仍然成立，它们服务的是后者。**还缺一条「投降 / 主动结算」**，尚未设计。

四条对策：

- 维修的成本显著低于重建，使"抢救残血建筑"成为有价值的选择
- 建筑摧毁后留下废墟/地基，原地重建打折
- 城内保底收入提供资源地板（此机制在防死亡螺旋上的作用比在防龟缩上更重要）
- **领主堡垒产出极少量金币，构成兵力地板。** 上面三条都要花资源，因此都救不了"金币断供"这一种死亡螺旋——金币管人力，断供意味着任何缺口都无法再堵。堡垒不可被摧毁（丢失即败），所以这条地板只要还没输就一定在。详见「守方多资源」一节

**实现要求**：建筑与城墙走**与单位完全相同的战斗解算**（血量、护甲类型、承伤计算），仅护甲类别不同且不可移动，存储上只是另一组扁平数组，额外带"建造进度/维修进度"状态。不要做成独立的特例系统，否则攻城器械的克制关系需要实现两遍。墙段同理：破损后留下缺口，缺口在修复前可通行。

参考 Stronghold 系列可直接采用的规则：

- **上墙途径**：守方把墙段当**驻守槽位**——直接命令单位占据某段墙，原地登上（短暂延迟），不设楼梯建筑、墙顶也不是可通行面。这样省掉一层寻路，也省掉一件无取舍的杂活；代价改由**机动性**承担：上去的单位被钉在那一段，换位要先下来。**攻方没有登墙手段**——花名册中不设云梯与攻城塔，破墙后从缺口进入是唯一路径。这与"没有兵种专属动作"配套：登墙需要一个专属动作，删掉它动作空间才能保持最小
- **高度优势**：从低处攻击墙上单位有 miss 概率且伤害降低；远程单位在墙上获得射程加成
- **局部破损**：墙段掉血不影响墙上单位，直到高度降至原高度一半
- **任何单位都能破坏城墙，破坏速率随等级缩放**（见下节）。Stronghold 长期被批评"手持武器步兵能砍穿石墙**导致攻城器械毫无意义**"；本作规避该问题的方式不是禁止步兵破墙（Stronghold 2 的做法），而是让攻城锤保有压倒性的拆除效率。**随等级缩放不会削弱这一点**：同一个乘数作用在所有兵种上，`Ram` 对步兵的**比例**优势不变（`无尽模式与地形分层.md` §7.7.3）
- **废墟回收比例**：以造价 ÷ 3 作为起点。比此更低会导致损失不可恢复
- **保留一个廉价应急防御层**（木栅栏、临时箭塔），可在波次进行中即时放置

## RL 设计决策（已定，勿擅自更改）

- **共享策略网络**（parameter sharing）：所有单位共用一个网络，兵种以 one-hot 输入区分。
- **观测**：以单位自身为中心的 K×K 局部视野多通道张量（我方/敌方单位密度与血量、墙、门、可通行地形、目标方向场）+ 自身状态向量 + 少量全局标量。
- **动作必须是离散的**：8 方向移动 + 停止、攻击最近/最弱敌人、攻击建筑/城墙。不要改成连续坐标动作。**没有兵种专属动作**——所有单位共用同一套动作枚举，差异体现在属性上（射程、破坏速率、能否对空），这样动作空间保持最小。
- **可破坏障碍不进战术动作枚举。** 加入 §6 之后曾考虑加一个 `AtkObst`，**否掉了**，两条独立的理由：
  - **攻方不需要它。** 寻路已经把障碍当「高代价可通行」，所以「绕远走走廊」与「就近打穿木桩」自动比较——单位往那个方向 `Move` 撞上去就开始破坏，这正是 §6 实现成本低的全部来源。给攻方一个它不需要的动作只是把动作空间从 13 变 14、浪费样本。
  - **守方清野是玩家级决策，不是逐 tick 微操。** 它因此走 `CommandKind::Clear`，与「驻守墙段归玩家级命令」完全同构（那条先例就在本文「不做登墙阶梯」一节）。
  于是**战术动作仍是 13 个**，这条已定项不受 §6 影响。
- **决策频率**：每 4–8 tick 决策一次，不是每 tick。
- **奖励**：以本波战果为主（突破与否、摧毁的建筑、造成的伤害，减去自身损失）；shaping 项**权重必须小**，否则会训出"在城外反复换血但永不推进"的退化策略。
- **跨波次信用分配靠重建成本解决。** 摧毁建筑的收益本来要许多波之后才体现，跨 episode 学不到。解决办法是把**摧毁建筑的即时奖励设为该建筑的重建成本**——这个权重等于给玩家造成的实际资源损失，是有原则的推导而非手工试凑，且经济建筑与防御建筑共用同一公式。不处理的话 AI 会永远只打城墙、从不骚扰经济，"攻其必救"这一核心战术不会涌现。
- 因此**一波未突破不等于失败**：拆掉数座防御建筑同样是有效战果。"消耗战"应当是一条 RL 可以学到的正当策略路径，奖励设计不要把它排除掉。
- **观测中必须包含波数与玩家当前的防御布局。** 玩家的建造选择在波次间不断变化，AI 读取并针对该布局，正是"适应玩家"的具体体现。
- **宏观层是 bandit 尺度的问题**：给定本波预算决定兵种配比，一波决策一次。可独立于战术层训练，前期甚至可先写死脚本。**战术层必须先跑通**，不要两层同时上。
- **训练时守方同样由 RL 控制，但只在决策层，且与攻方结构性不对称。** 正式游玩时守方仍是人类玩家；守方 AI 的用途是攻方的陪练、`AI vs AI` 演示、以及平衡测试。**攻方 RL 控制的是每一个单位，守方 RL 控制的是玩家会下的那些指令**（建造 / 经济 / 造兵 / 维修 / 调兵），**守方单兵由参数化脚本驱动，不上 RL**。理由是守方 AI 是人类玩家的替身，其动作空间应当等于玩家实际能下的指令；这同时从结构上封死了「守方打出人类做不到的逐 tick 微操、攻方过拟合到这些操作」这个坑，并使训练成本不再翻倍。守方决策层需把动作因子化到**地图给出的槽位列表**上（否则动作空间是整张地图，学不动），并做动作掩码。设计方案、脚本层的设计判据与排期见 **`守方AI与协同演化.md`**。
- **守方的脚本执行层不是「RL 做好之前的临时凑合」，它是最终形态。** 微操对 RL 并不难（我们本就指望攻方涌现集火与包夹），不用 RL 的理由是**攻方需要见到的是覆盖真人水平区间的一个分布，而 RL 只会收敛到一个点**——脚本的微操强度是可调参数，RL 的不是。脚本要写到什么程度有一条判据：**克制二部图里已写死的战术行为（如 `Ghoul ──► Archer 拉扯`）必须进脚本**，因为那是设计意图、不该指望涌现；图里没写的（包夹、佯攻、择机）才留给 RL。
- **上线顺序不可并行**：`rts_core` 与回放 → **一族参数化脚本守方** → 攻方战术层 vs 脚本守方轮换 → 守方决策层 → 行为克隆 → 协同演化 → 攻方宏观层。同时上则训练不收敛时无法定位来源。其中脚本守方**无论如何都要写**，且它不会被协同演化取代——攻方在全部阶段面对的执行层都是它。
- **不做在线 RL 微调**。"针对玩家风格调整"的实现方式是：离线训练 3–4 个风格化策略（速攻/消耗/骚扰经济/强攻城门），对局中统计玩家特征，用轻量元控制器（贝叶斯更新或 UCB bandit）在策略间切换。
- 已评估并放弃 Unity ML-Agents。

## 游戏设计要点

**攻守双方兵种表是差异化的**，不是同一套单位换个颜色。攻方单位围绕突破设计，守方单位围绕控场与补口设计。因此克制关系是一张**二部图**而非对称矩阵——设计时检查的是"攻方每个单位有没有对应的守方解法"以及反之，不需要考虑同阵营镜像对局的平衡。

克制关系由三条正交属性轴推导，**不要退化成平铺的 N×N 伤害倍率表**：交战距离（近战/中程/攻城）、打击形态（单体/溅射/破坏结构）、机动性（重甲慢速/轻甲快速/特殊位移）。

**克制用乘法加成，且必须在 UI 中完全透明。** AoE2 采用加法加成且刻意不透明（科技树不揭示护甲类归属，骆驼骑兵不属于骑兵类、鹰武士有隐藏的冲击步兵类等），这一点被社区长期批评。AoE3/神话时代改用乘法 + tag 系统。本作玩家必须在数秒内读懂来袭编成才能反应，不透明会让"被 AI 针对"退化为"被系统坑"；透明的克制关系同时让 RL 学到的策略人类可解释。

**克制的本质是资源效率，不是二元胜负**——克制单位应能以显著更少的资源投入击败目标。此定义可直接用于奖励函数与平衡工具。

**平衡工具：成本产出矩阵。** 每格记录 1v1 对抗的期望资源盈亏（而非谁胜），例如"防空单位对地面单位为净亏其全部造价，对空中单位即使自身战损仍净赚"。这张矩阵既是调数值的依据，也是答辩材料。

优先做**机制性克制**而非数值克制，因为后者会被 RL 一眼解出、缺乏深度：

- 骑兵冲锋伤害取决于助跑距离（开阔地怕枪阵，巷战反克）
- `Ram` 对静止密集目标高效，对散开移动目标大幅 miss（**AOE 落点在前摇开始时锁定成坐标**，
  见「结构破坏规则」；这一条的实现要求写在那里，这里只列它作为机制性克制的一例）
- 弓手有攻击前摇与转身速度，被贴脸即废
- 溅射单位被包夹时会误伤/无法释放

两个不能砍的机制：**城墙可站人**（压制墙头 → 破墙 → 缺口攻防的三阶段博弈），**部分资源点在墙外**（逼迫守方出城，杀死龟缩退化解，并让野战机制有用武之地）。

战争迷雾对玩家和 AI **对称生效**，AI 也必须侦查、也会被佯攻欺骗。

已知需要防范的退化策略：无限龟缩、单一兵种海、无成本风筝、早期无脑 rush。对应的机制对策见设计文档，改数值时注意别把这些对策改没了。

### 通用准则：结构封死，而非数值劝退

攻方预算随波数**超线性增长**，因此"低效"不等于"不可行"——任何仅靠高定价或低数值来抑制的策略，到高波次都会因规模而重新变得可行，RL 一定会找到。

**凡是不希望 AI 采用的策略，必须在结构上封死。** 具体表现为：用硬性数量上限而非高价来限制稀有单位；用伤害为 0 而非"伤害极低"来禁止某类打击；用机制不可能而非收益不划算来排除退化解。

### 空中单位

**`Phoenix` 是唯一的空中单位。`Wraith` 是地面单位——这是结构性决定，不要改。**

若 `Wraith` 会飞，防空建筑就同时具备了"否定侦查"这一**每波都稳定生效**的用途：
侦查每波都有价值，于是防空每波都不亏，玩家无脑造 AA 即可。这会连带摧毁三件事：

- AA 的机会成本（每座 AA 意味着该位置少一座对地火力）不再构成两难，而这是
  **本作"智斗"最可读的载体**——玩家预判 AI 的空军投入、AI 读取玩家的防空覆盖率
- "空军价值 = 迫使玩家投入的 AA 资源量"这一评估指标失效，因为玩家本来就要造
- 侦查单位必须**便宜到可以消耗**，情报交易才诚实（同 `Ranger` 不可与 `Scout` 合并
  那条）；而空中单位的硬性约束是高价、稀有、跨波留存、击落后需 N 波重生，正相反

这也是克制表里"`Wraith` ──► 任何机动单位"能够成立的前提：守方没有空军，
空中威胁只能被位置性否定，若 `Wraith` 会飞，能猎杀它的就只剩 `Flak` 一种。

视觉上 `Wraith` 仍是蝙蝠造型（贴地掠行、投影紧贴脚下），`Phoenix` 是展翼的鸟、
高悬且投影远在下方。**玩家靠本体与投影的间距判断能不能用防空塔招呼它**，
这条读不出来的话，上面的机制设计在玩家侧同样不成立。

定位是**手术刀，不是胜利条件**：点杀防御塔、清理城外资源点的工人，为地面突破清路。

结构性约束（硬性，不是待平衡的数值）：

- 对**城墙与城门破坏速率为 0**，无法攻击核心建筑。破墙只能靠地面部队，三阶段攻防因此完整保留。
- **硬性数量上限**，上限可随波数缓慢放开，但增速必须远低于预算增速。
- 高价、脆皮、**跨波次留存**：存活的空军下一波继续使用。观测中需包含当前存活空军数量。
- **被击落后不是永久损失，而是需经 N 波后重生**（贴合"不死鸟"命名）。"珍贵"的语义由此保留——击落一次意味着攻方数波内失去空中打击能力——同时损失惩罚真实但有界，比永久损失更易训练。对玩家而言这也是一次成功防空的可感知回报。

> 若改为每波重置空军预算，RL 会学到在波尾无成本自杀换伤害的退化策略。跨波留存 + 重生延迟是对此的结构性防范。

**守方没有空军，只有防空建筑。** 因此空中威胁只能被**位置性**地否定，无法通过机动拦截——玩家的决策是"AA 摆在哪、覆盖谁"这一空间问题，而非"造几座"这一数量问题。

**防空的要害：AA 建筑只能对空，不能兼顾对地。** 否则 AA 就是"更好的塔"，玩家无脑建造即可，两难消失。只有当每座 AA 都意味着该位置少一座对地火力时，玩家才被迫预判 AI 的空军投入，AI 也才需要读取玩家的防空覆盖率——这组双向预判是本作"智斗"最可读的载体，也是"AI 适应玩家风格"最容易被玩家直观感知的演示点。

**空军价值可量化为"迫使玩家投入的 AA 资源量"。** Beyond All Reason 的空战设计指出，空军战役的成败不只看是否重创敌人，还要看敌人为防空砸进去多少资源。因此"不死鸟一次未出击，但逼玩家造了六座防空塔"同样是有效战果——此指标可同时用于奖励项与评估指标。

**AA 只做单体狙击型，不做群伤型。** BAR 区分了远程单体导弹与短程高 AoE 高炮；本作空军数量少且珍贵，群伤型 AA 无意义。

**AA 同时承担情报否定职能**：AA 的**视野否定半径大于其伤害半径**，空军不敢接近，也就无法侦察其覆盖区域。此设计解决了一个否则会出现的漏洞——守方无空军意味着空军在 AA 射程外绝对安全，可沦为白嫖侦察机，使攻方战争迷雾形同虚设，而"AI 必须侦查、可被佯攻欺骗"是本作智斗设计的支柱之一。副作用是 AA 在无空袭波次中也不再是纯粹的死重（它在保护情报），而"每座 AA 少一座对地火力"的机会成本依然成立，两难不受影响。玩家由此还获得一种主动手段：**通过 AA 布局故意向 AI 暴露假的破绽。**

**排期**：空中单位属于正交叠加层，应在地面攻防核心循环跑通、战术策略训练收敛之后再加入（约第三周）。地面尚未收敛就引入第二个移动层，将无法区分训练不收敛的来源。

## 构建与测试

采用 CMake。骨架已落地，**两套工具链都实测过**（Windows/MSVC 与 Linux/GCC 各跑过
Release + Debug，7 条 ctest 全绿、零警告；服务器侧的记录与同步办法见
`训练服务器环境.md` 第 5 节）：

```bash
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release

ctest --test-dir build -C Release               # 全部测试
ctest --test-dir build -C Release -R <name>     # 按 ctest 条目挑
./build/tests/Release/rts_tests "<用例名>"       # 直接跑测试二进制中的单条用例
```

命令行下 PATH 里通常没有 cmake，VS2022 自带一份，完整路径见 `README.md`「本地环境」。

**ctest 条目按 Catch2 标签划分（`rts_tests_rng` 等），不是逐用例注册。**
用例名是中文，而逐用例注册要把用例名当命令行过滤器传回去，在 Windows 的中文
code page 下这条链路会把名字弄乱，结果是 ctest 报「No tests ran」——**测试被静默跳过，
比测试失败危险**。标签是 ASCII，不受影响。挑单条用例请直接用测试二进制。

条目按**族**列在下面。**权威清单是 `ctest --test-dir build -C Release -N`，不是这张表**——
条目每个 PR 都可能增加，此处刻意不写总数（原先 `tests/CMakeLists.txt` 里那句「现有 5 条」
已经过期成 7 条，就是这么来的；写死的数字对读者没用，只会漂移）。

| 族 | 条目 | 是什么 |
|---|---|---|
| Catch2 标签 | `rts_tests_<标签>` | 每个标签一条，标签清单在 `tests/CMakeLists.txt` |
| 兜底 | `rts_tests_all` | 不带过滤器，防标签漏登记，见下 |
| 确定性守卫 | `determinism_bans` | 扫 `rts_core/` 与 `game/`（扫哪些目录以 `tests/CMakeLists.txt` 为准） |
| | `determinism_bans_self_test` | 防守卫自己退化成永远绿 |
| | `determinism_bans_empty_is_red` | **`WILL_FAIL`**：扫空目录，期望脚本非零退出 |
| 地图校验器 | `map_gen_self_test` | `tools/map_gen/` 的自检 |
| | `map_gen_validate_fixture` | 拿 `game/testdata/fixture_min.json` **真的读一张真图**（全仓唯一一张，两侧共用） |
| | `map_gen_validate_empty_is_red` / `..._partial_path_is_red` | **`WILL_FAIL`**：一张都没读到 / 读到一些但另一些路径不存在 |
| 配置陈旧 | `cmake_configure_is_current` | **`ctest` 不会自己发现 `CMakeLists.txt` 变了**，见下 |
| | `cmake_freshness_missing_stamp_is_red` / `..._empty_root_is_red` | **`WILL_FAIL`**：上一条自己能不能变红 |
| 前端 | `render_*` | **只在 `RTS_BUILD_RENDER=ON` 时存在**，所以服务器上一条都不跑，见下 |

最后那类要单独说一句：**`WILL_FAIL` 条目在 ctest 里显示 `Passed`（结果已反转），
但单独跑脚本时退出码是 1。** 后者才是会被误判成「坏了」然后去「修」的地方。
**这类条目不止一条**——`ctest -N` 里名字以 `_is_red` 结尾的都是——
所以这不是一次性的注意事项。

> **这里原写「现在有五条（确定性 1 + 地图 2 + 配置陈旧 2）」，那个数字是错的**：
> `render/` 另有四条，实际是九条。**而它错在本节上面十几行刚立的规矩上**
> （「此处刻意不写总数……写死的数字对读者没用，只会漂移」）——
> 同一份文件里立完规矩就自己违反了一次。所以这次不是把 5 改成 9，是不写数字。

### `ctest` 单独跑会得到一个绿色的子集

**`ctest` 不会发现 `CMakeLists.txt` 变了。** 它读的是构建目录里生成好的
`CTestTestfile.cmake`，而那份文件只在**配置**时重写；只有 `cmake --build` 会顺带触发
重新配置，`ctest` 自己不会。于是：

```console
$ git pull                      # 别人加了几个测试文件与 ctest 条目
$ ctest --test-dir build -C Release
100% tests passed, 21 of 21     # ← 该有 29 条
```

这是真事：`build/` 是契约那个 PR 之前配置的，`roster` / `action` / `command` / `obs` /
`terrain` / `fog` / `world` / `replay` 八个标签的条目**根本不存在**，所以 ctest 当然全绿。

它与上面「标签清单是白名单」同源，但**更隐蔽一层**——标签写错会红（Catch2 非零退出），
而**条目不存在连红的机会都没有**：它不是一条失败的测试，是一条不存在的测试。

`cmake_configure_is_current` 把它变成会红的：配置时把每个 CMake 输入文件的 sha256
记进构建目录（`tools/check_cmake_freshness.py --stamp`），跑测试时重算一遍比对。
**按内容比而不是比 mtime**——CMake 只在内容变了时才重写生成物，所以一次什么都没改的
重新配置**不会**更新 `CMakeCache.txt` 的 mtime（实测），而 `git checkout` 会更新源文件
mtime，两者一凑那条守卫会永远红。

**它有一条绕不过去的边界：配置早于这条守卫存在的构建目录，不会注册它。** 自举问题，
一个不存在的条目没法报告自己不存在。代价一次性——重新配置一次就落进覆盖范围。

**所以习惯上就该 `cmake --build` 再 `ctest`，不要单跑 ctest。**

**但标签清单是白名单，所以另有一条不带过滤器的 `rts_tests_all`。** 标签写错能被抓到
（Catch2 匹配不到用例会非零退出），标签**没写进清单**抓不到——那条 ctest 条目根本不
存在，ctest 仍然全绿。新增测试文件时不必担心漏登记，但**仍应顺手把新标签加进清单**，
它是定位用的。

### 服务器上的绿也是一个子集，但那一个是**设计内**的

上一节那个子集是**过期的构建目录**给出的，是缺陷，已有守卫。
服务器上还有另一个子集，**表象一样（一个比预期小的绿色数字），性质完全相反**：

```console
本机（RTS_BUILD_RENDER=ON）：  35/35
服务器（默认 OFF）：            28/28      ← 差 7 条，全部是 render_*
```

**差集是 `render_smoke` / `render_verify_assets` / `render_utf8_path` 与四条
`render_*_is_red`。** 这不是谁漏了什么——`render/` 默认不配置正是不变量 2、3
在构建侧的落地（见下面「构建开关」）。**它不需要守卫，需要的是这句话**：
看到 28 而不是 35 的人不该去找哪里坏了。

**代价是真实的，要认下来：`render/` 的 C++ 永远只有 MSVC 一套工具链把关。**
这是「缩小需要在服务器上编译的代码面」这条约定的另一面，接受它。

**但另一半是回报，值得指出**：`rts_tests_{map,scene,iso,names,utf8path}`
**在服务器上是跑的**，因为 `game/` 无条件构建。当初把前端按「**能不能在默认构建里
被测**」切成 `game/` + `render/`（而不是按「渲染 / 非渲染」切），图的就是这个——
几条最易错的场景规则（一格画两张图、桥的底衬是 `Water`、叠加物与单位同一深度序列、
线性结构的 `d` 是走向、`rows[y][x]` 的坐标序）**全部落在 GCC 也验的那一侧**。

第三方依赖经 FetchContent 引入，**现在是三个**：Catch2、nlohmann/json（`game/` 用，
所以**服务器上也要下**）、raylib（只在 `RTS_BUILD_RENDER=ON` 时）。
这条原写「当前只有 Catch2」，已过期——`tools/server/build.sh` 也曾因为只认 Catch2
而卡在 configure（#63 的日志里那一段），**新增 FetchContent 依赖时那份脚本要跟着加**。
首次配置需联网；
之后可加 `-DFETCHCONTENT_FULLY_DISCONNECTED=ON` 离线复用。

构建开关：`RTS_BUILD_TESTS`（默认 ON）、`RTS_BUILD_RENDER`（默认 **OFF**，
训练构建因此根本不配置 `render/`，这是不变量 2、3 在构建侧的落地）、
`RTS_WARNINGS_AS_ERRORS`（默认 ON，仅作用于第一方代码）、
`RTS_REQUIRE_DETERMINISM_GUARD`（默认 ON：找不到 Python3 时**让配置失败**，
而不是把下面那条守卫静默摘掉；确实没有 Python 的环境显式关掉它）。

### 构建类型：那条守卫只在**单配置生成器**上生效

**Windows 上不给 `-DCMAKE_BUILD_TYPE` 不会落在 Release。** 本节此前的写法是错的，
而且错在全组每天走的那条路径上，已实测订正：

```console
$ cmake -B build                                  # 不给 -G，默认生成器
-- Building for: Visual Studio 17 2022            # ← 多配置
$ grep -c '^CMAKE_BUILD_TYPE' build/CMakeCache.txt
0                                                 # ← 缓存里一项都没有
```

顶层那条守卫的条件是 `if(NOT _rts_build_type_given AND NOT CMAKE_CONFIGURATION_TYPES)`。
VS 是多配置生成器，`CMAKE_CONFIGURATION_TYPES` 有值 ⇒ **守卫被跳过**，
配置改由构建期的 `--config` 决定，而 `vcxproj` 的首个配置是 `Debug`：

> **`cmake --build build` 不给 `--config` 构建的是 Debug。**
> Windows 上的 Release 靠 `cmake --build build --config Release`，不靠那条守卫。

**这条与本节末尾「性能相关的改动请在 Release 下测量」是连着读的**——原来那句转述会让人
以为 Windows 上不必操心配置，于是 `cmake -B build && cmake --build build` 拿到 Debug，
然后在 Debug 下量吞吐，**正好是那句要防的事**。

守卫真正生效的场合是**单配置生成器**：Linux/GCC，以及 Windows 上显式 `-G Ninja`。
它曾经写错过一次：判空放在 `project()` 之后，而 `Platform/Windows-MSVC.cmake` 那时
已经把平台默认值 `Debug` 写进缓存，于是那段代码只在 Linux/GCC 上有效——
**注意那次的 bug 与本次的转述错误不是同一件事**：那次坏的是单配置路径上的代码，
这次错的是对多配置路径的描述。**改动它之前先读 `CMakeLists.txt` 里那段注释。**

### 单配置生成器（Linux/GCC）的命令不一样

上面那组命令只对 MSVC 多配置成立。训练服务器是 Makefiles（单配置），而本节开头
那条「要尽早、频繁地在服务器上跑构建」是对**每个人**说的，所以这组命令是常用的、
不是一次性的：

```bash
cmake -B build -DCMAKE_BUILD_TYPE=Release   # 单配置必须在**配置期**给
cmake --build build                         # 无需 --config
ctest --test-dir build                      # 无需 -C
./build/tests/rts_tests "<用例名>"           # 注意不是 tests/Release/
```

单配置的另一个后果：Release 与 Debug **必须各占一个构建目录**（构建类型烤在缓存里），
而多配置是一个目录里两套。所以服务器上是 `build-Release/` 与 `build-Debug/` 两个。

**头文件必须自足**（能被单独 `#include` 而不依赖任何先行的头）。这条有结构性守卫：
`rts_core/CMakeLists.txt` 给 `include/rts/` 下每个头各自生成一个只包含它自己的 TU
编进 `rts_core`，用 GLOB 而非手写名单，所以新增的头自动纳入。曾经 `types.hpp` 用了
`operator<=>` 却漏了 `<compare>`，而它一直编得过——测试文件先 include 了 Catch2，
把 `<compare>` 顺带拉了进来；这类漏 include 还可能只在一套标准库上炸。

**确定性禁令有一条可执行的守卫**：`tools/check_determinism_bans.py` 扫
`rts_core/` 与 `game/` 里的 `unordered_*`、C 库随机函数、标准库分布适配器、挂钟、
以指针为 key 的有序容器，注册为 ctest。本节与「确定性要求」一节的禁令因此不只是
文字——违反会在测试里红。
确属误报时在该行末尾加 `// determinism-ok: <理由>` 放行（刻意要求写理由，使放行可评审）。

**改 `rts_core/` 时它是最快的反馈回路，不必走整个 ctest**（退出码 0 即通过；
**刻意不在这里写它扫了几个文件、自检有几条**，那种数字每个 PR 都会漂，
同本文「条目数不写死」那条）：

```bash
py tools/check_determinism_bans.py rts_core game
py tools/check_determinism_bans.py --self-test
```

两个与内容无关但会浪费时间的坑：**若 `python` 打不开**（某些机器上 PATH 里的
`python` / `python3` 是 Microsoft Store 的占位 alias），用 `py`；
**Git Bash 下中文输出会乱码**，加 `PYTHONIOENCODING=utf-8`。
后者与「ctest 按 ASCII 标签注册」是同一个原因的两个产物。
它自己也有两道防退化：`--self-test`（应报 / 不应报各十余条），以及**扫到 0 个文件即失败**
（路径写错时必须红，不能报「通过（0 个文件）」）。

> **那个 alias 的失败方式比原先写的更坏，实测订正。** 本节原写它「会直接退出并提示
> 去装商店版」——至少在一台队友机器上**它零输出、退出码 49**。差别是实质的：
> 有提示时你立刻知道该换 `py`，零输出时你只看到一个非零退出码，
> 而下一步很自然是去 `rts_core/` 里找那个不存在的违规。
>
> **它还会与一种常见写法组合出假绿**：`py ... | tail -3; echo $?` 取到的是 `tail`
> 的退出码，恒为 0。刚踩过一次——先得到「守卫退出码 0」，再发现那是管道的码，
> 真码是 49。**要判守卫的结果就别在它后面接管道**，或者用 `${PIPESTATUS[0]}`。
>
> **这段原先插在「后者与……」之前**，那有两个毛病：紧跟引用块的非空行会被
> Markdown 的 lazy continuation **吸进引用块**；而「后者」「它自己」的指代对象
> （上一段的两个坑、确定性守卫脚本）被中间八行隔断，读者会以为「后者」指
> `${PIPESTATUS[0]}`。**插入一段离题的补注时，要看它落在哪两句之间。**

训练侧从 `train/` 运行，依赖 `bindings/` 编译出的 Python 扩展模块。

**性能相关的改动请在 Release 下测量**，Debug 构建的仿真吞吐不具参考意义。

## 评估指标

**无尽模式下看「中位存活波数」，不看胜率。** 胜率在一个必败的模式里没有定义——
攻方兵力无界而玩家经济有界（地图资源点有限），所以每一局最终都会输，
「赢」这个事件不发生。分布的中位数才是可比较的量（`无尽模式与地形分层.md` §1）。

除此之外，以下三项是本项目的核心论证材料：

- **策略池两两对战胜率矩阵。** 若矩阵中出现非传递性环（A 克 B、B 克 C、C 克 A），即以实验数据证明了游戏策略空间具备深度，直接回应"是否足够偏向智斗"这一立项问题。**因守方也是 RL、双方协同演化，这张矩阵从"攻方策略之间"扩展为"攻守双方各代之间"**，证据强度显著提升——单一守方下测出的非传递性有可能只是那个守方的产物。
- **"AI 是否发现并利用已有缺口"。** 最直接的对标产品 Diplomacy is Not an Option 被评测明确批评为"敌人无视已有缺口、反复撞击加固过的城墙，明摆着的弱点被浪费"。这是脚本 AI 的典型失效，也正是 RL 的强项，宜作为显式指标并在答辩中与该批评对照呈现。
- **对不同玩家风格的适应曲线** + 涌现行为录像（包夹、佯攻调虎离山、集火、空军择机突袭、点杀维修工）。

  **上面这张清单是预期，不是上限。** 攻方全程由逐单位 RL 驱动，因此**打出我们没设计过的战术是可能的，而那是比清单内任何一项都更有力的材料**——它直接说明策略是学出来的、不是写死的。同理，「我们写的脚本守方被 AI 找出漏洞并反复利用」也算这一类。

  由此有一条团队约定：**训练期发现意料之外的涌现行为时，当场记录并同步给全组**，不要等整理答辩材料时再回忆。手段是现成的——确定性回放能把那一局固定下来，`演示与可视化清单.md` 第 3 节的显著性图能看出它当时在关注什么。

训练方法上参考 AlphaStar 的 league training：确保每个竞争者既能应对当前最强策略，又不遗忘如何击败早期策略，从而在策略空间中持续探索、避免套路化。波数本身即构成课程，可参考 curriculum transfer learning 在微操任务上从小规模向大规模迁移的做法。

## 参考资料

- [They Are Billions Wiki — Swarms](https://they-are-billions.fandom.com/wiki/Swarms)、[KeenGamer 生存指南](https://www.keengamer.com/articles/guides/they-are-billions-survival-guide/)、[Fantasy Warden 攻略](https://fantasywarden.com/games/they-are-billions-full-strategy-guide-tips) — 波次结构与经济曲线
- [TryHard Guides — Diplomacy is Not an Option 评测](https://tryhardguides.com/diplomacy-is-not-an-option-review/) — 最直接的对标产品及其 AI 缺陷
- [Stronghold Wiki — Wall](https://stronghold.fandom.com/wiki/Wall) — 城墙驻守、高度优势、破墙规则
- [AoE Wiki — Armor class (AoE2)](https://ageofempires.fandom.com/wiki/Armor_class_(Age_of_Empires_II))、[Counter](https://ageofempires.fandom.com/wiki/Counter) — 克制系统实现与其不透明性批评
- [Beyond All Reason — Basics of Air Warfare](https://www.beyondallreason.info/guide/basics-of-air-warfare) — 空军/防空的资源经济学
- [DeepMind AlphaStar](https://deepmind.google/blog/alphastar-mastering-the-real-time-strategy-game-starcraft-ii/)、[Nature 论文](https://www.nature.com/articles/s41586-019-1724-z) — league training 与涌现策略
- [arXiv 1804.00810](https://arxiv.org/abs/1804.00810) — 参数共享 + 课程迁移的微操 RL，含可参考的规模数据
- [arXiv 2509.03682](https://arxiv.org/pdf/2509.03682) — 2025 年游戏 MARL 综述

## 关于数值：一律保持待定，等框架完善后统一标定

**凡关系到游戏平衡性与玩家游戏体验的数值，现在一律不定。** 包括但不限于血量、伤害、
射程、速度、造价、重建成本、破坏速率、编成位占用、收入曲线、波次强度曲线、
地图尺寸、**集结点数量**、空军上限与重生延迟。

理由是这些数值互相耦合，孤立地定其中一个必然要返工：单位速度决定地图该多大，
地图大小决定集结点该有几个，集结点数量决定玩家每波要防几个方向，
而那又反过来决定防御建筑该多贵。**必须等机制框架跑通、能实际对局之后，
用成本产出矩阵与自动标定一次性倒推**，不要在文档里逐个拍脑袋。

因此写文档时请守住两条：

- **区分「结构约束」与「数值」。** 「每个集结点对应一条性质不同的走廊」是结构约束——
  它不随标定变化，现在就能定死；「集结点有几个」是数值——现在不定。
  前者是机制层面的，后者是旋钮。
- **需要举例说明时，明确标注为占位值。** 文档里出现的数字若不标注，
  后来者会当成已定值引用，然后在四个模块里各自固化一份。

这条是团队的明确要求，不是建议。已发生过一次违反：地图规范曾把集结点数量写成
「定为 4」，那是把一个平衡旋钮当成了结构结论。

## 待定事项

- 前端图形库选型：raylib 与 SFML 之间尚未拍板（raylib 上手更快，倾向 raylib）。**当前不引入引擎**属工程选择而非硬性规则，理由与重新评估的判据见「前端：为什么当前不经引擎」。
- `rts_core` 的公开接口**已全部定稿**（花名册与地形枚举、战术动作、玩家/宏观命令、观测通道注册表、`World` 数据布局、`submit`/`advance`、只读视图、迷雾三态、动作与命令掩码、状态哈希、回放格式与骨架），**因此它不再是并行开发的阻塞项，下一个最高优先级是机制（1c）**。清单、逐条理由与仍未决的项见 **`rts_core 接口契约.md`**。逐版答掉的问题：血量与伤害取 `int64`（附一条推广规则：**接口与回放格式的承诺不得依赖任何待定数值**），斜向穿角暂取「不能」并给出了可引用的常量名（仍需寻路那侧确认），`Command::slot` 取格线性下标（决策层那约 60 项的粗化归 `train/`），`visible` 通道改为三档（两档时「从未侦查过」与「记忆里那段墙是破的」不可区分），回放只存外生输入、并用 `kWorldHashTag` 把「布局改了」与「跑歪了」分开（**这条替掉了原先打算给在途弹丸预留一组空实体位的兜底**）。**仍未决**：硬要求 1（AI 只见自己的迷雾）的落点是观测打包那一层，而那一层还没有代码——`WorldView` 是 god 视角、实现的是不变量 2，这处要靠评审而非编译器把关。
- **地图尺寸与障碍物密度尚未标定。** 分布形态已定（见「资源分布形态」），具体格数、簇的数量与间距、隘口宽度都需实测——它们直接决定拐点位置与骑兵冲锋的可用空间。
- 经济系统的其余形态：是否有采集单位。~~是否有科技/升级。倾向于**保持精简**（无科技树）~~ **已定，见「守方的一条升级轴」：做一条升级轴（堡垒等级），三个输出，无分支无解锁。** 「保持精简」那句正式作废——但作废的只是「一条都不做」，不是「可以随便加」：仍然无科技树、无分支、无解锁。**资源种类数已定为石材 / 木材 / 金币三种**，主要来源一律是地图上的资源点——对应采集建筑 `Quarry` / `Lumber` / `Mine`（见「守方多资源」与建筑花名册）。唯一例外是 `Keep` 产出**极少量金币**作为兵力地板，理由见「领主堡垒的金币产出是兵力地板」一节；它是护栏而非收入，数额待定。不再列为待定。
- **全部数值尚未标定**：血量、伤害、射程、速度、造价、重建成本、破坏速率、编成位占用。应先建成本产出矩阵（每格填 1v1 对抗的期望资源盈亏）再倒推，不要拍脑袋定。数值以占位常量放在 JSON 数据文件中，调整不触碰代码。**地图边长、城区半径、走廊口宽度依赖速度标定**，`地图与场景设计.md` 第 3 节只锁了"`Ram` 行军时间占 episode 10–20%"这条不变量，未锁数字。
- 空中单位的数量上限曲线、重生延迟波数尚未标定。
- **守方 AI 仍有四项待定**（清单见 `守方AI与协同演化.md` 第 9 节）：守方奖励偏离零和到什么程度可接受（**已因守方只做决策层而降一档难度**——奖励是波次级的，没有逐单位的信用分配问题）；对手池规模与入池间隔；行为克隆需要多少局人类数据、谁来玩；「调兵」动作如何表达「调哪一支部队」（加第三个动作头还是拆成两步，倾向后者）。**其中「守方 RL 放在哪一层」与「守方战术层是否上逐单位 RL」已定，不再列为待定。** GPU 机时那条的题面已过期：训练成本不再翻倍，且 #22 实测出被串行化的是 CPU 不是 GPU，要估的是 `BatchedEnv` 吞吐。
- **演示已定只做两项**（双视图、`AI vs AI`）；显著性热力图与非传递性环矩阵改为**只用于分析与报告**，落在 `tools/`、不占前端预算。仍待定：显著性图最终做到什么程度、`AI vs AI` 进不进菜单、`render/` 预算重估到多少人日、答辩标准片段用哪几个、C++ 手写推理是否就此定为部署方案。见 `演示与可视化清单.md` 第 9 节。
- **集结区数量待定**（属平衡与体验数值，见上节）。已定的只有结构约束：每个集结点对应一条性质不同的走廊，故其数量 = 走廊种类数。具体位置由地图文件给出，演示地图的布局尚未确定。
- 是否允许玩家强袭集结区之外的特殊规则——目前决定靠自然平衡，无需额外机制，但需实测验证。
- ~~是否允许战斗中维修建筑/城墙~~ **已定：允许，且波次进行中不禁止任何资源的使用。** 不采用 Stronghold「敌人在附近时不可维修」的做法。限制器是时间与暴露：维修速度慢、维修单位脆弱，AI 因此会学出"优先点杀维修工"这一可演示的涌现行为。
- 波次强度曲线与经济收入曲线的具体参数尚未标定。建议参考 *They Are Billions* 的曲线设计以减少试错。
