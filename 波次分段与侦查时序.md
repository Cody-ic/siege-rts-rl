# 波次分段与侦查时序（2026-09-03，提案；代码未动）

> **定位。** 组长提出把建造期分段、并给玩家一段「只能侦查、不能动城防」的时间。
> 本文评估它、给出精确形状、并把落地清单列出来。**与 `CLAUDE.md`「侦查系统」
> 一节冲突处以本文为提案**，通过后合进去。
>
> 起点不是「侦查系统还缺功能」，而是**它现在没有内容**：`wave_scouted_` 是一个
> bool，唯一的消费者是 `Wraith` 自己的行为（没到手就前压、到手就掉头回集结点）。
> 攻方的编成由 `WaveCurve` 算、主攻方向是 `wave % n_spawn` 取余、目标选择是
> flow field 奔 `keep`——**侦查结果一个字都没进任何决策**。所以「杀掉 `Wraith`
> 让 AI 带错情报开打」现在零收益：AI 从来没用过情报。

---

## 0. 先记两条被推翻的论证（决策史，别再照它们推一遍）

讨论中我给过两条「玩家的延迟建造不足以作废 AI 情报」的辩护，**两条都不成立**：

| 我说的 | 为什么错 |
|---|---|
| 「建造耗时就是延迟的代价：塔 240 tick、建造期 260 tick，拖到后半段下单就完不了工」 | **对建筑成立，对部队完全不成立。** 弓手征兵 100 tick + 登墙 30 tick = 130 tick，只有塔的一半多。后期守方实力主要在兵上，那这条代价根本不适用 |
| 「防御是累积的，能藏的只有本波的边际增量，所以幅度有界」 | **逻辑倒了。** AI 每轮都查，它对第 1..N−1 波的布局本来就有记忆——**这一波侦查的全部价值恰恰就是那个增量**。「增量可藏」不是「幅度有界」，是「侦查价值归零」 |

所以在**无界**的延迟建造下，AI 情报确实会被免费作废，而 RL 会学会忽略它。
本提案要解决的就是这一条。

---

## 1. 提案：建造期分三段

```
波次
├─ 第一段「建设」    玩家可动城防；攻方在集结点待命，不侦查
├─ 第二段「窥探」    玩家仍可动城防；攻方**在本段内择时**派窥使
├─ 第三段「玩家侦查」玩家**不可动城防**，只能侦查 + 调兵 + 征兵
└─ 进攻期            计时器到点开打，与侦查成败无关
```

三段各自解决一件事：

- **第一段**给玩家一段不被观察的建设时间。它的作用是节奏与可读性——先让玩家做事，
  再开始互相算计；同时保证「攻方来看的时候有东西可看」。
- **第二段**是双方唯一交错的一段，**也是全部博弈发生的地方**。玩家「等它走了再造」
  仍然可行，但窗口 = 从攻方看的那一刻到本段结束，而**看的时机由攻方定**。
- **第三段**把情报冻住。它同时给玩家自己的侦查第一次找到用途：这一段拿到的编成情报
  没法再变成建筑，只能变成**部署**——而那正是机动部队该干的事。

### 1.1 这一段结构的核心是「承诺 → 揭示 → 部署」

三段读作一次完整的信息交换：

| 时刻 | 谁承诺了什么 |
|---|---|
| 第二段结束 | **玩家承诺城防**（此后不可改） |
| 攻方看的那一刻 | **攻方的情报定格**（此后不再更新） |
| 第三段 | 玩家拿到编成情报，只能用**部署**回应 |
| 开打前一刻 | 攻方按情报快照定主攻方向与目标 |

两侧各有一样东西要学、一样东西要藏，且不对称：

| | 学 | 藏 |
|---|---|---|
| 玩家 | 攻方**编成**（要花钱派 `Scout`） | 自己**这一波新加的城防**（靠在第二段里择时） |
| 攻方 | 玩家**城防布局**（要派 `Wraith`） | 自己的**主攻方向**（靠佯攻） |

**编成必须在波次开始时定死、不许等侦查回来再改**（现状已经如此，写下来防止以后
被「优化」成晚定）：否则玩家侦查到的编成会在她建造之后变掉，「花钱知道编成」
这件事直接失去意义。方向可以晚定，因为那本来就是攻方该藏的东西。

---

## 2. 这里用「禁令」是对的工具，尽管本仓库通常反对禁令

`CLAUDE.md`「波次进行中不禁止任何资源的使用」一节明确反对禁令：
「**限制器是时间与暴露，不是禁令**」，并逐条否掉了锁金币与锁石材。第三段是禁令，
所以要说清为什么它不违反那一条。

两条理由：

1. **那一节讲的是波次进行中**（有战斗），论证是「建造耗时 + 工地暴露已经解决了」。
   第三段在**开打之前**，没有战斗，暴露这条惩罚不存在，所以那个替代品在这里不可用。
2. **更要紧的：信息的时序没法定价。** 你没有办法让「压到情报截止之后再建」变贵，
   而不同时让「建造」本身变贵——延迟建造和正常建造花的是同一笔钱、同一段工期。
   一个不能被价格区分的东西，只能被结构区分。而这恰恰是 `CLAUDE.md`
   「**结构封死，而非数值劝退**」那条准则要求的形状：阶段边界是结构。

所以第三段不是那条纪律的例外，它是那条纪律的应用。

---

## 3. 第三段冻结的精确范围（这一节的依据是代码，不是习惯）

冻什么取决于**AI 的记忆里存了什么**。查了 `rts/fog.hpp`：

```cpp
struct RememberedBld {
    BldType type = BldType::Wall;
    std::uint16_t hp_permille = 0;   // 0 = 记忆里这段已经是缺口
};
```

记忆 = **类型 + 位置 + 血量千分比**。不含等级。据此：

| 动作 | 是否作废情报 | 提案 |
|---|---|---|
| 新建 | 是（位置上多出一个实体） | **冻结** |
| 拆除 | 是（记忆里那个实体不在了） | **冻结** |
| 维修 | **是**——记忆里 `hp_permille = 0` 的缺口被补上 | **冻结** |
| 升级 | 名义上不作废（等级不进记忆）…… | **仍冻结**，见下 |
| 征兵 | 否（部队不在建筑记忆里） | 放行 |
| 调兵 / 驻守 | 否（同上） | 放行 |

两处要专门说明：

- **维修必须一起冻，而这条比新建更要紧。** `CLAUDE.md` 的头号评估指标是
  「**AI 是否发现并利用已有缺口**」——它是对标产品 DiNaO 被差评的那一点，
  也是答辩要对照呈现的证据。而 AI 的记忆里缺口正是 `hp_permille = 0`。
  若玩家能在情报定格之后把缺口悄悄补上，那个指标的演示形态就从
  「AI 调头去打那段破墙」变成「AI 撞上一段刚修好的墙」——**看起来像 AI 蠢，
  而实际是情报被作废**。指标本身会失去意义。
- **升级也冻，理由是一个既有副作用**：`finish_upgrade` 把血量补到新上限
  （「完工即满血」，`CLAUDE.md` 记为待表态的副作用）。于是**升级 = 一次免费全额
  维修** ⇒ 放行升级就等于放行维修。与其留这个洞，不如统一冻结——而且对玩家更好解释。

**于是第三段的规则一句话说完：不能再动城防，只能调兵、征兵与侦查。**
这条规则可教、可显示，也不需要玩家记一张「什么算动城防」的表。

---

## 4. 攻方「什么时候看」是决策变量

现在 `Wraith` 在建造期**开头**生成、立刻出发——那是所有时机里**最差的一个**：
把整个建造期让给玩家去改。分段之后它成为一个真取舍：

| 攻方在第二段里 | 情报质量 | 拿到情报后可用的时间 |
|---|---|---|
| 早看 | 差（玩家后面还有大半段可以改） | 多 |
| 晚看 | **好**（玩家要么已经承诺，要么来不及完工/登墙） | 少 |

玩家的反制随之有内容：**她不知道这一波它什么时候来**，所以「一直不建等它走」是
一个有风险的赌——它可能压到第二段最后一刻才看，那时她既没城防也没来得及补兵。

对 RL 是顺的：宏观层本来就是 bandit 尺度、一波一次决策，「在第二段的 k 个时刻里
选一个派窥使」是一个很小的离散动作；而「按情报快照定主攻方向」挂在开打前一刻，
观测直接喂迷雾——`obs_pack` 那一侧现成（`visible` 通道三档，**0.5 就是「上一轮的
信息」**，硬要求 1 有测试钉着）。

### 4.1 派几只、派几轮，也都是攻方的预算决策

窥使从编成位里扣。于是自然产生几条真决策，一条机制都不用新加：

- 派两只 = 拿编成位买**冗余**（被杀一只还有一只）
- 第二段够长时派两轮 = 用编成位买**新鲜度**
- 一只都不派 = 省下编成位、靠上一轮的记忆开打

`WaveCurve::phoenix_*` 那一组已经是运行期参数了，窥使数量同理（现在恒 1）。

---

## 5. 玩家引导：两个倒计时，以及为什么名字很关键

组长指出的第二件事：**现在没有任何东西告诉玩家存在侦查系统。** HUD 只有
`地图 X   波 N   建造 N   tick N   主攻 方向`——有一个建造倒计时，但没有分段概念，
也没有「敌方斥候正在看」这类提示。玩家读不出机制，也就不会去用它。

提案：**同时给两个倒计时**

| 显示 | 含义 |
|---|---|
| **「玩家侦查阶段开启 N 秒」** | 到第三段开始（= 城防可动的截止时刻） |
| **「进攻开始 N 秒」** | 到开打 |

**名字刻意不叫「建造截止倒计时」。** 那个名字讲的是玩家**失去**什么；
「玩家侦查阶段开启」讲的是玩家**获得**什么，并且把机制的名字直接印在 HUD 上——
组长最初提的那个问题（玩家不知道有侦查系统）最便宜的解法就是让倒计时的标签自己去教。

两个都给的理由：它们管的是两件不同的事。前者是「我还能不能建完一座塔」
（塔 240 tick = 12 s，墙 100 = 5 s，木栅 30 = 1.5 s ⇒ 剩 8 秒时正确答案是砌墙不是造塔）；
后者是「我还有多久调兵」。少给任何一个，对应的那个决策就退化成盲赌。

还建议给一条**事件提示**：观察到攻方窥使时提示一次。玩家能不能观察到它取决于视野——
`Wraith` 改判空中后离墙约 9 格，而玩家视野最远的是 `Watch` 的 12（塔 8、防空 9、
堡垒 8）⇒ **要知道它何时来看就得专门造瞭望塔**。这让 `Watch` 第一次有了非重复职能，
也让 `CLAUDE.md` 给它设计的那个「看得见 vs 打得着」取舍第一次真的成立。

---

## 6. 连带效果（三条，都不是白拿的）

1. **波间隔的上界被解除。** 讨论中曾算出一条约束：建造阶段不能显著超过单座防御
   建筑的工期（塔 240 tick），否则「晚建」从战术选项变成支配策略。**第三段把那个洞
   结构性地封了**，于是波间隔恢复成只有下界（守方要来得及把收入转化成防御，实测
   6 秒太短、13 秒可以、26 秒之后饱和）。波间隔是组长点出的关键旋钮，这条让它松开一侧。
2. **玩家侦查第一次有用途。** 第三段拿到的编成情报只能变成部署，而这正好落在
   「机动部队不可被侦查、其应对发生在交战阶段」这条边界的正确一侧。
3. **`Scout` 与 `Watch` 都有了活。** 前者是第三段唯一能做的事之一，后者是知道
   窥使何时来看的唯一手段。两个此前都属「造了也没什么用」。

---

## 7. 要拍板的（本文不猜）

1. **三段的时长比例。** 提案不给数，只给约束：第二段要长于「玩家能建完一座塔」
   （否则攻方无论何时看都看得到全部），第三段要短（长了就是干等，正是 TAB 那个
   被批评的缺陷）。第一段是节奏。
2. **第三段能不能被「提前召唤下一波」跳过。** 倾向能——`CLAUDE.md` 要求这条机制
   正是为了修「只能干等」。但跳过第三段等于放弃自己的侦查，那本身是个取舍。
3. **征兵在第三段放行到什么程度。** 提案是全放行（部队不在建筑记忆里）。
   代价是「后期爆兵」那条：情报覆盖石材那一半、覆盖不了人口那一半，
   于是**守方实力越往兵上偏，AI 情报价值越低**。这条与「塔要不要是消耗品」
   「石材 vs 人口谁咬住实力」是同一个问题的两面，**不能分开定**
   （见 `攻守配平的数学模型.md` §4.2、§6）。
4. **驻守中的弓手要不要纳入侦查判据。** 现在判据只认塔/防空/堡垒。墙上弓手是
   **半静态**的（征兵 5 s 快，但下墙 + 走位 + 登墙 1.5 s 不便宜），所以这类情报
   有留存价值，值得考虑纳入。
5. **窥使数量上限**（现在恒 1；`WaveCurve` 里已是参数）。

---

## 8. 与既有文档的冲突点（通过后要一起改）

| 文件 | 冲突 |
|---|---|
| `CLAUDE.md`「侦查系统 / 双向欺骗」 | 「等 `Wraith` 走了再造蔽空弩楼」要改成**有界**版本：窗口在第二段内、由攻方的看时机决定，不是整个建造期 |
| `CLAUDE.md`「波次进行中不禁止任何资源的使用」 | 要补一句范围限定：那一节讲的是**进攻期**；建造期的分段冻结是另一件事，理由见本文 §2 |
| `CLAUDE.md`「波次结构与数值曲线」 | 波次结构从「建造 → 进攻」两段变三段 + 进攻 |
| `game/include/game/demo_driver.hpp` 的 `WaveTiming` | 现在只有 `build_ticks` / `first_build_ticks` / `assault_max_ticks`；要拆成三段。那里的注释目前只写了波间隔的**下界**理由，上界那条按 §6 第 1 条作废 |

---

## 9. 落地清单（依赖顺序）

1. **`WaveTiming` 拆三段** + `World` 侧暴露「当前处于哪一段」（`WavePhase` 现在只有
   `Build`/`Assault` 两态）。**这一步会动 `kWorldHashTag`**，旧回放要重录
2. **第三段的冻结**落在 `World::apply_one`：`Build`/`Demolish`/`Cancel`/`Repair`/
   `Upgrade` 在该段静默拒绝（同「满员时 `Train` 静默拒绝」的先例）。
   **在 `World` 里而不是在 UI 里**——UI 挡不住 RL 与回放
3. **情报快照语义**：截止于窥使撤回 / 死亡 / 第三段开始，三者取最早；此后该侧迷雾
   不再更新（现在是 bool 一置上永久有效）
4. **攻方看的时机**成为参数（先给 demo 一个固定占位，RL 阶段变成动作）
5. **方向决策移到开打前一刻**、读快照（现在是 `wave % n_spawn` 取余）
6. **HUD 两个倒计时 + 窥使提示**。⚠️ 新文案要登记进 `render/src/text.cpp` 的
   `ui_strings()`——缺一个码点是**画的时候抛**，而截图回归测试点不到那个分支
7. `CLAUDE.md` 与本文合并（§8 那张表）

第 1、2 两步是其余全部的前提；第 5 步之前，「先侦查再决定主攻方向」这条
`CLAUDE.md` 称作最能体现 AI 智能的可演示行为，在代码里不存在。
