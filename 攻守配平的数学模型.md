# 攻守配平的数学模型（2026-09-03）

> **定位。** 组长：「平衡没调好，涉及数学比较多，仔细研究怎么建立合适的模型，把两方弄均衡。」
> 本文给出配平所需的模型（§1–§3）、一条决定性的**不可能性结论**（§4）、按它推出的
> 四条缺陷与处方（§5–§6）、以及目标曲线与验证计划（§7–§8）。
>
> 脚本 `tools/balance/balance_solver.py`（联立约束优化 + 参数扫描），
> 依赖 `siege_race.py`（一波之内的入口攻防，需求侧唯一真相）与
> `economy_race.py`（地图资源分布 → 供给侧）。**本轮不动仿真代码、数值表与地图。**
>
> 前置：`攻守实力模型与平衡分析.md`（正确货币 Σ√B、入口赛跑）、
> `资源点分布与经济平衡.md`（分布实测与经济接线）。本文不重复它们的结论。

---

## 1. 配平的对象不是一个数，是一条曲线的形状

无尽模式下守方**必败**，所以「平衡」不能定义成胜率。可配的量是**余量曲线**

```
M(w) = 守方在第 w 波能调动的资源 ÷ 守住第 w 波最省的一套方案
```

`M(w) ≥ 1` 即守得住。一份**配好的** M(w) 要同时满足四条，缺一条都不叫平衡：

| # | 判据 | 为什么 |
|---|---|---|
| S1 | `M(早期) ≈ 1.2–1.5` | 开局要紧但不劝退 |
| S2 | **单调不升** | 无尽模式的难度必须递增；升就是「越玩越轻松」 |
| S3 | 在目标波 `W*` 跌破 1 | 中位存活波数 = 唯一的评估指标 |
| S4 | 跌幅有界（`|Δln M|` 每波 ≲ 0.15） | 悬崖会让「慢性死亡」变成「突然死亡」，两者都不要 |

S2 是本文的重点：**当前实现连符号都是反的**（§3）。

---

## 2. 需求侧：D(w, L_d, A)

一波之内的胜负判据取 `攻守实力模型与平衡分析.md` §1 的入口赛跑：主攻群走到门下，
守方要在门破之前把它打空。把它反解成「需要几座塔」：

```
D(w, L_d, A) = 门前拦住第 w 波主攻群所需的、L_d 级的塔数（门边有 A 名 1 级当量弓手）
```

由 `balance_solver.demand()` 对塔数二分实测（对塔数单调，二分合法）。实测值：

| 波 | A=0 | A=2 | A=4 | A=6 | A=8 | A=12 | A=16 |
|---|---|---|---|---|---|---|---|
| 3 | 7 | 6 | 3 | 1 | 0 | 0 | 0 |
| 5 | 9 | 9 | 7 | 3 | 2 | 0 | 0 |
| 8 | 12 | 12 | 12 | 8 | 5 | 2 | 0 |
| 10 | 15 | 15 | 15 | 12 | 8 | 4 | 2 |
| 15 | 29 | 29 | 29 | 23 | 16 | 9 | 6 |
| 20 | 38 | 37 | 37 | 32 | 26 | 16 | 11 |

标度：`D ∝ r(w)·P(w) / √B_d`，其中 `r` = 主攻群里的攻城锤数、`P` = 兵力预算、
`B_d` = 守方建筑等级倍率。推导来自 `T_破门 = 门血·√B_d /(r·d_ram·√B_a)` 与
`需杀伤 ∝ n·h̄·√B_a`，两式相除时 `n·B_a = P`。实测拟合 `A=0` 一列是 `D ∝ w^1.04`，
`A=6` 一列是 `w^1.71`（固定弓手数越来越不够用）。

**一条读法：弓手极其便宜地替代塔。** 第 20 波 16 名弓手顶掉 27 座塔——
每名弓手 ≈ 1.7 座塔 ≈ 135 石，而它只要 60 金。这条后面处处出现。

---

## 3. 供给侧：石材是存量，金币是流量

这不是选择，是实测逼出来的。在「刚好守住」的塔数上跑入口攻防，逐波记损失：

| 波 | 塔损 / 塔数 | 弓手损 / 弓手数 |
|---|---|---|
| 5 | **0 / 3** | **6 / 6** |
| 10 | **0 / 12** | **6 / 6** |
| 20 | **0 / 32** | **6 / 6** |

**塔一座都不掉，弓手每波全灭。** 于是两种资源的记账方式必须不同：

- **石材**：塔是永久存量，一次买断 ⇒ 与**累计**石材比
- **金币**：弓手每波全额重置 ⇒ 与**每波**金币收入比

用同一套记法会得出「守方永不缺资源」——那正是我第一版余量曲线单调上升的成因。

### 3.1 三个约束由堡垒等级 K 耦合在一起

组长指出的那条（升级消耗资源 vs 获取资源速度）在这里落地。**K 同时是三样东西的价格**，
而它们抢同一份石材：

```
K ──► 建筑等级上限 ceil(K/2)     （塔更强 ⇒ D 更小）
  ──► 人口上限 8 + 2K            （弓手更多 ⇒ D 更小）
  ──► 兵种等级上限 = K            （弓手更强 ⇒ 同样人口下当量更大）
```

于是「守住第 w 波」是一个三元可行性问题：

```
找 (K, L_d, A) 使
  石材存量：(K−1)·200 + 入口数 · D(w,L_d,A) · 塔造价(L_d)   ≤  累计石材 S(w)
  金币流量：60 · A/conc                                      ≤  每波金入 G(w)
  人口    ：A /(conc·√B(K))  + 4                             ≤  8 + 2K
M(w) = 1 / min over 可行组合 of max(石材占用, 金币占用)
```

`conc` = 门边弓手占全部弓手的比例，**是玩家水平在模型里的全部体现**：
脚本均匀登墙 `13/(8R) ≈ 0.16`，人类按免费方向提示调兵 0.5–0.8。

### 3.2 一条由 `c ∝ √B` 白送的简化

单位造价已于 #130 改成 `c ∝ √B(L)`，而战力也是 `√B(L)` ⇒ **每金买到的战力与等级无关**。
所以「多招几个」与「招高级的」在**金币**上等价、只在**人口**上不等价：一名 L 级弓手
占 1 人口却顶 `√B(L)` 名。**人口一咬住就该升级而不是加人**——这是一条能直接写进
守方脚本的策略，也让模型只需记「1 级当量」这一个量。

---

## 4. 中心结论：调价格改不了曲线的形状

把 §3 的可行性问题在「石材是唯一瓶颈」的区段上展开：

```
M(w) = S(w) / ( c_t · g · D(w) )
```

`c_t` = 一座塔的石材，`g` = 要设防的入口数，`S(w)` = 累计石材。

> **命题。** 把 `S` 乘以常数 σ（少几个城外石点、改产出倍率）、或把 `c_t` 乘以常数
> （改塔造价），都只把 `M(w)` 整体乘一个常数。**`d ln M / d ln w` 一点不变。**
>
> 证明是一行：`M` 对这两者都是零次齐次的，`ln M(w) = ln σ − ln c_t + ln S(w) − ln D(w)`，
> 后两项与 σ、`c_t` 无关。∎

所以 **S2（单调不升）不可能靠调价格得到**。这不是「还没调好」，是这一类旋钮的
能力边界。

**64 组参数扫描实测印证**（塔造价 ×{80,160,240,360} × 升级曲线 {线性, √B} ×
供给 ×{1.0, 0.5, 0.3, 0.2} × 等级上限除数 {1, 2}，波次 3/5/8/12/16/20/25，conc=0.5）：

| 结果 | 组数 |
|---|---|
| M 全程不升（满足 S2） | **0 / 64** |
| 上升段最少的一组 | 6 段里仍有 3 段上升 |

那一组是「塔 240 石 + 升级 ∝√B + 供给 ×1.0 + 上限除数 2」，
`M = 0.79 → 1.64 → 2.57 → 3.07 → 3.09 → 3.09 → 3.09`：起点在 1 以下、
中段涨到 3 倍、末段走平。**升级曲线与等级上限除数不是纯缩放**（它们改变 `D` 与
`c_t` 的关系），所以命题不覆盖它们——这次扫描是专门为这两个补的实测，
结论一致。

### 4.1 那么斜率由什么定

```
d ln M / d ln w = (累计石材的增长指数) − (需求 D 的增长指数)
```

- 需求：`D ∝ w^1.04`（门边无弓手）到 `w^1.71`（弓手数固定）
- 供给：城外簇逐波解禁时收入 `∝ w` ⇒ **累计 `∝ w²`**；簇开完（约第 10 波）之后
  收入恒定 ⇒ 累计 `∝ w`

于是指数差在饱和前是 `2 − 1.04 = +0.96`（**上升**），饱和后是 `1 − 1.04 = −0.04`
（几乎持平）。要从 `M ≈ 6.5` 跌到 1，需要 `w` 再涨 `6.5^{1/0.04} ≈ 10^20` 倍。
**守方事实上永不陷落。**

### 4.2 唯一能翻转符号的那一类改动

指数差里，需求那一侧被 RL 的可训练规模（单位数 20–40）与「等级不得淹没克制关系」
两条卡住，抬不了多少。**所以只能动供给那一侧的「累计」二字**——也就是让防御工事
**不再是永久存量**。一旦塔以每波比例 λ 损耗，石材那一侧从存量变流量：

```
稳态塔数 N* = S(w) / (λ · c_t)          （收入买的是「补损失」，不是「加库存」）
M_流(w)   = S(w) / (λ · c_t · g · D(w))
```

饱和后 `S` 恒定、`D ∝ w^1.04` ⇒ **M 单调下降，且跌破波对参数是线性敏感而非指数敏感**。
S2 与 S4 同时得到。

**这条恰好是 CLAUDE.md 自己写下的**：「防御建筑与城墙**有血量、会被摧毁**……
防线的形状在一波之内会动态退化，玩家需要实时判断先补哪段、放弃哪段。
**这一点是多条设计的支点，不要简化掉。**」§3 的实测说明，这个支点在塔这一侧
**事实上没有实现**。

---

## 5. 四条缺陷（各自都有实测）

### 5.1 塔不是消耗品 ⇒ 无尽模式的「必败」不成立

见 §3、§4。三处佐证：
- 入口攻防里塔零损失（守方在「刚好守住」的塔数上也一样）
- `Ram` 的 `aoe_radius = 1.3`，只溅到**紧贴城门**的一两座；引擎确实对建筑施加
  AOE（`mechanics.cpp` 的建筑循环），所以这是半径的量级问题，不是机制缺失
- `Phoenix` 是设计里点杀防御塔的手段，而编成写死 `wave >= 3 ? 1 : 0`。
  CLAUDE.md 说它的「硬性数量上限**可随波数缓慢放开**」——那条从未放开
- 实测把不死鸟加到 2/3/4/6/8 只，塔损是**阶跃**（0 或全灭）而不是渐进：
  塔打不到空中，防空一旦被清就无人能挡。**所以靠加不死鸟做渐进损耗行不通**，
  它只会把「必败」换成「一波突然全塌」，违反 S4

### 5.2 建筑升级定价错误 ⇒ 堡垒的一个输出是装饰品

塔造价 `80 + 32(L−1)`（**线性**），火力 `∝ √B(L)`（**开方**）。两者不同阶 ⇒
每石买到的火力 `∝ 1/√L`，**堆量严格占优**。实测（第 20 波，门边 6 弓手）：

| L_d | 需塔/门 | 单座石材 | 总石材 | 相对 1 级 |
|---|---|---|---|---|
| 1 | 32 | 80 | 5120 | 1.00× |
| 6 | 19 | 240 | 9120 | 1.78× |
| 12 | 14 | 432 | 12096 | **2.36×** |

单调上升 ⇒ **最优档恒为 1 级**，`K ──► 建筑等级上限` 这个输出从未被使用。

**这与 `数值设计与成本产出矩阵.md` §12 给单位修掉的是同一条**（造价 ∝ L、战力
仿射 ⇒ 堆量占优），当时只改了单位，建筑漏了。把塔造价改成 `∝ √B(L)` 后，
最优档移到 **9–11 级且总石材更低**（第 20 波 4005 vs 5120），升级第一次成为决策。

### 5.3 弓手集中度值 4–6 倍 ⇒ 整个早期挂在一个脚本不做的行为上

门边要 A 名当量就得全城有 `A/conc` 名。`conc` 从 0.16 到 0.8 是 5 倍，
而它同时压在**金币流量**（弓手每波全灭）与**人口上限**（要靠石材买 K）两条约束上。
实测余量曲线：

| conc | w=3 | w=8 | w=12 | w=20 | 结果 |
|---|---|---|---|---|---|
| 0.16（脚本均匀登墙） | **0.73** | 3.21 | 5.58 | 6.41 | 第 3 波陷落 |
| 0.40 | 1.03 | 3.28 | 5.89 | 6.58 | 守住 |
| 0.60 | 1.45 | 4.06 | 5.89 | 6.58 | 守住 |
| 0.80 | 1.77 | 5.06 | 6.52 | 7.32 | 守住 |

**「第 3 波陷落」与「永不陷落」之间只隔着一个行为**，而那个行为（把弓手调到
免费方向提示指的那座门）是操作杂活、不是战术判断。

### 5.4 难度曲线是倒的

上表每一行都在上升。当前版本**第 3 波最难，此后越玩越轻松**，与 S2、S3 全部相反。
第 5–12 波是一段没有任何张力的「难度洼地」。

> **一条统合的读法：现在的「平衡」靠的是守方玩不好。** 脚本守方（1 名工匠、
> 不铺城外、均匀登墙）第 3 波崩，看起来像「攻方偏强」；把守方换成会调兵、
> 会铺城外经济的玩家，同一套数值下守方永不陷落。两个结论不矛盾，
> 它们是同一条倒挂曲线的两端。

---

## 6. 处方：按「能不能改变曲线形状」分两类

### 第一类：改变形状（必须做，否则第二类白调）

**P1 塔要真的会被打掉（结构）。** 目标损耗率由稳态式反解：

```
λ = S / (g · D(W*) · c_t)
```

取 `S ≈ 2750 石/波`（内环带饱和后）、`W* = 20`、`g = 2`、`D(20) = 32`：

| 塔造价 c_t | 需要的每波塔损耗率 λ |
|---|---|
| 80（现值） | 0.54 |
| 160 | 0.27 |
| 240 | **0.18** |

**推荐 `c_t = 240、λ ≈ 0.18`**：λ 太高会让「修旧 vs 建新」退化成纯重建，
而 240 同时兑现 CLAUDE.md 那条「防御建筑造价高到无法覆盖所有墙段」——
现值 80 下 6 座塔即可覆盖 R=10 的整圈（480 石 = 不到 4 波的城内收入），
那条设计要求当前是**失效的**。

**怎么产生 λ**：不能靠加不死鸟（§5.1 实测是阶跃）。可选的两条，都要组里定：
① 攻方目标选择加一条「射程内的塔优先」——它同时是 RL 该学会的东西，
写进脚本只是给它一个下限；② `Ram` 对**结构**的 AOE 半径单独放大（对单位那一份
不动，避免破坏「散开克溅射」）。**①更好**，因为它不改任何机制、只改脚本的目标序。

**P2 守方脚本按免费方向提示集中弓手（游戏代码，不是数值）。** 把 `conc` 从 0.16
抬到 ≈ 0.5。理由不只是配平：`conc` 是 4–6 倍的杠杆，任何数值配平在它面前都是噪声，
**不先钉住它，后面所有数都调不准**。而「把弓手拖到主攻门」是杂活不是战术，
交给脚本正合「守方单兵全自主、玩家只下宏观命令」那条既定设计。

### 第二类：调整位置与斜率（P1/P2 之后才有意义）

**P3 塔升级改 `∝ √B(L)`**（§5.2）。它让堡垒的三个输出都真的在用，
且与单位那一侧同构——**同一条错误不要在两个地方各修一次**。

**P4 城外石点数量。** §4 的命题说它只平移不改形状，所以它**不是**主旋钮；
它的正确用途是在 P1 定下形状之后，把 M 的起点挪到 1.2–1.5。
与 `资源点分布与经济平衡.md` §4.2 的外环带处理一并做。

**P5 早期不需要单独压。** 实测第 3 波在 `conc ≥ 0.4` 时 M ≈ 1.0–1.5，
落在 S1 里。**P2 一旦落地，早期就不必再动攻方等级曲线**——
这与上一份文档「先压 `Ram` 等级曲线」的建议**不同**，改口的理由是那份文档
按 `conc = 0.16` 的脚本守方算，而 §5.3 说明那个前提本身才是要修的东西。

---

## 7. 目标曲线（假设，是旋钮不是结论）

中位存活波数从未定过。本文按「波长 60–70 秒 ⇒ 20 波约 25 分钟」取：

| 玩家 | conc | 目标跌破波 |
|---|---|---|
| 脚本 / 新手 | 0.3 | 10–12 |
| 一般玩家 | 0.5 | **20** |
| 熟练玩家 | 0.8 | 28–32 |

技术差约 3 倍。**这三个数是本文唯一没有推导的东西**，改它们只改 §6 里 λ 的取值
（`λ ∝ 1/D(W*)`），不改任何结构结论。

---

## 8. 验证计划

模型里每一个绝对波数都还没经过真机校准（同 `攻守实力模型与平衡分析.md` §7）。
按下面的顺序验，前一步不过不要做后一步：

1. **无渲染 runner**（§7 那条一直没做的）：多种子跑 20 波，量塔的实际损耗率 λ、
   门边弓手的实际比例 conc、塔的 AOE 命中数、`Phoenix` 的真实目标序列。
   **λ 与 conc 是本文两条处方的直接输入**，先量它们
2. P2 落地后重量 conc，确认它真到 0.5
3. P1 落地后重量 λ，用 §6 的式子回填 `c_t`
4. 用 `balance_solver.py --scan` 重跑，检查 S1–S4 四条
5. 12 张池图的方差：跌破波的四分位距应 ≤ 3 波（现在是 2 vs 3 那种一座塔的差别，
   见 `资源点分布与经济平衡.md` §3.2）

---

## 8.1 这份模型的定位，在校准 runner 落地之后要改（2026-09-03）

写完 §1–§8 之后拉到最新 main，发现队友已经把 §7 那个 runner 做出来了
（PR #135，`tools/calibration_runner/`）。**这改变了本文的定位，要写清楚。**

### 聚合模型盖不全变量，能盖全的只有仿真

组长指出本文的搜索空间太小（只扫了塔造价 / 升级曲线 / 供给缩放 / 等级上限除数
四项，攻方曲线的**函数形式**从头到尾锁死在 `6w^1.25`），而可调的还有兵力曲线、
等级战力曲线、升级消耗曲线、出矿速度、资源分布、逐兵种与逐建筑的血量攻击、
初始资源、城墙半径……并且没人规定曲线必须是幂律，它可以是对数、三角、任意单调
递增函数。**这条批评成立，而且它有一个比「多扫几个参数」更根本的版本**：

真实游戏里还有三轴克制、冲锋助跑、枪阵反冲锋、风筝、AOE 落点锁定、flow field
把墙当高代价可通行、迷雾与佯攻、林子与隘口、河桥、可破坏障碍、驻守与高度优势、
在途弹丸、工匠线性加速、维修与拆除退款、集结期与编队推进、不死鸟跨波留存……
**手写的聚合模型补到哪一步都能被正确地再问一次「其它的呢」。**

所以本文 §2、§3 的两张实测表与 §4 那条命题**仍然成立且有用**（它们是关于
「入口赛跑」这个子系统的、可复算的结论），但**「配平应当在什么模型上做」这个
问题的答案不是本文，是 runner**。本文自此定位为：给 runner 上的搜索提供
**判据（§1 的 S1–S4）、要看的量（余量、损耗率、集中度）与几条已证伪的方向**。

### runner 的首批数据比本文的模型更硬，且有两处直接对上

| 本文 | runner 实测（12 池图 × 3 种子 × 20 波） |
|---|---|
| §5.3：脚本守方（conc 0.16）第 3 波陷落 | **36/36 全陷落，中位第 2 波**（比模型早一拍） |
| §3：塔零损失 | 33/36 局**没有任何一段墙/门被打穿**——攻方直接从设计缺口进城 |
| §1.4 占位 `aoe_mult = 2.0` | 塔 AOE 逐发命中均值 **2.23**（1857 发），量级对上 |
| §5.3 的 `conc = 0.16` 档 | 门边 13 格墙弓手平均 **0.93 名/门**，低档得到支持 |
| §1.8「`Phoenix` 建模偏攻方」 | 更糟：唯一出场那局**出手 0 次**（飞到堡垒上空无目标） |

### 因此下一步不是继续扫参数，是补齐仿真里缺的那一半

runner 的 README 自己列了两条局限，它们其实是**同一件事**：
**无渲染跑的时候没有人替守方下宏观命令。** 全仓唯一产出
`Build`/`Train`/`Upgrade`/`Repair` 的地方是 `game/src/player_input.cpp`，
也就是人点鼠标。于是无渲染的守方**不封缺口、不建塔、不征兵、不升级**，
runner 量到的「中位第 2 波」测的是「守方什么都不做」。

补这一块（一个参数化的守方宏观决策层，即 `守方AI与协同演化.md` 里那个决策层的
脚本替身）之后，配平才能在**全变量**的仿真上做，而本文 §6 的处方要在那上面
重新验证一遍——**在此之前 §6 的取值（λ、`c_t`）只当方向读，不要照抄**。

---

## 8.2 真机基线（2026-09-03，合并后 main，36 局）

补齐 §8.1 那句「配平应当在 runner 上做」之后真的跑出来的第一组数。守方接
`game::DefenderMacro`，12 池图 × 3 种子 × 20 波上限：

| 量 | 值 |
|---|---|
| 截断 | **0**（两处波次死锁都不再发生） |
| 陷落波 | 中位 **4**、均值 4.2、分布 {3:3, 4:24, 5:6, 6:3} |
| 破口 | 墙 **63** / 门 **36** |

对照起点：队友 runner 首批（守方不作为）中位 **2**，且 33/36 局攻方直接从设计
缺口走进城、几乎零破墙。

**本文 §6 那两条处方在真机上的检验结果，一条对、一条错**：

- **错**：§6「先压 `Ram` 等级曲线」。实测把 `power_alpha` 从 1.25 压到 0.40
  （攻方大幅削弱），陷落波只从 `3 3 3 1` 动到 `3 5 4 1`——**几乎无效**。
  真正在第 3 波变化的是攻城锤与不死鸟**首次出场**，不是它们的等级。
  §5.2「B(w) 是主难度旋钮」因此要收回。
- **对，但成因不是本文说的那个**：§4.2 说「只能让防御工事不再是永久存量」。
  真机里起决定作用的是一个**几何洞**——`Tower` 射程 7 而城区半径 9–10 ⇒
  堡垒周围一整圈无火力覆盖，攻方破门后走到堡垒脚下零抵抗（6 只 1 级
  `Ghoul` 破 520 血门 24 s、砸 2400 血堡垒 112 s）。补上近卫塔后前两波从必死
  变完胜。这与「塔是不是消耗品」是两件事，本文当时没分开。

**§5.3「弓手集中度是唯一大杠杆（4–6 倍）」在真机里是中性的**：A/B 下来
`3 1 3 1 3 3` vs `3 1 3 1 3 2`。成因同上——它们下来也走不过去、也打不过。

**一条方法论教训**：本文 §1–§7 的绝对波数全部建立在「守方是一个合理但不操作的
玩家」这个假设上，而真机里**没有人替守方下宏观命令**（全仓只有
`player_input.cpp` 产出建造/征兵）。假设与实现之间这道缝，比模型里任何一个
参数的取值都重要。

---

## 9. 局限

- 需求侧继承 `siege_race` §1.8 的全部未建模项；本文另新增：塔的 AOE 命中数、
  攻方目标选择（P1 要改的正是它，所以现值下的 λ=0 有循环论证的成分——
  §8 第 1 步就是为了打断它）
- 供给侧是**上界**（内环带石点全铺、无袭扰），见 `资源点分布与经济平衡.md` §5
- `conc` 是把「玩家水平」压成一个标量的粗暴做法。它抓住了目前最大的那条杠杆，
  但抓不住择时、诱敌、出城摸攻城锤
- §4 的命题只在「石材是唯一瓶颈」的区段上严格成立；金币咬住时 `M` 是两个比值的
  下包络，命题对每一支各自成立、对下包络仍成立（两条都被同一个常数缩放）
- 目标曲线（§7）是假设
