# 交付要求的工程影响

本文件把 `课程设计最终交付项.md`（老师原文，**不得改动**）翻译成对架构、排期与协作纪律的具体约束，并记录我们的应对。

下面按影响程度排列。**注意区分「要求原文规定的」与「我们据此做的推断」**——前者不可协商，后者是可讨论的工程选择。

---

## 1.「软件可执行程序：可独立执行」→ 部署方式待定，不构成硬约束

**要求原文只有四个字：「可独立执行」。** 在课程语境下，它最可能的含义是：交付的应当是双击即可运行的 exe，而不是一个必须在 Visual Studio 里按 F5 才动、还要老师自己配环境的工程。

**它没有禁止运行时依赖。** 游戏带运行时依赖是常态（VC++ 运行库、DirectX、.NET）。把 Python 与 PyTorch 一并打包进交付目录，同样满足"双击即可运行"，因此**这不是一个合规问题**，三种部署方式都是合法的。

### 三种方案的实际权衡

| 方案 | 权衡 |
|---|---|
| 打包 Python + PyTorch | **零额外开发工作**；代价是交付包体积大、启动慢、Windows 上打包环境较脆，答辩换机器有一定风险 |
| 导出权重 + C++ 手写推理 | 体积最小、启动最快、无打包风险；代价是需**逐位对齐 PyTorch 的前向传播**（层序、padding、归一化任一处不符会导致策略静默变差），必须配"固定输入下 C++ 输出 vs PyTorch 输出"的比对测试 |
| 导出 ONNX + ONNX Runtime | 无对齐风险；**推理只需在 Windows 上运行，训练侧不需要**，故不增加 Linux 服务器的构建负担，仅是 exe 旁边多一个 DLL |

### 真正的成本不在包体积，而在运行时管线

若选择在 Python 中推理，游戏（C++）需要在运行时取得策略输出，这意味着要么**在 exe 中嵌入 CPython**（pybind11 支持，但需随包分发 Python 环境与 torch），要么**以独立进程 + IPC** 的方式通信。20 Hz、40 单位的规模下性能不是问题，但这层管线本身是要写的工作量，与手写推理的成本处于同一量级。

做决定时应比较的是这两者，而不是"包大小"。

### 结论

**此决定可推迟至第 3 周**，不阻塞任何当前工作。

「网络结构保持简单」这条建议仍然成立，但理由是**本项目规模本就不需要复杂网络**（不用 Transformer/LSTM），而非"要求禁止"。现有的"共享策略网络 + 局部观测 CNN"无论选哪种部署方式都合适。

---

## 2.「系统设计报告：描述软件的类对象设计」→ 张力比预想的小得多

本节早先的版本声称"SoA 布局与类对象设计存在冲突"。**该说法基于一个混淆，现已修正**：内存布局（AoS / SoA）与程序结构（面向对象 / 面向过程）是两个**正交**的问题。一个类完全可以封装扁平数组并对外提供方法——那既是数据导向布局，也是标准的面向对象设计。

`CLAUDE.md` 中真正的硬性约束只有一条：**不使用逐实体的多态继承体系**（不给每个单位单独堆分配、不用虚函数分派），理由是保护确定性回放测试。这一条并不妨碍写面向对象代码。

### 因此报告不缺内容可写

- **系统层是类**：`World` / `Sim` / `BatchedEnv` / `Pathfinder` / `VisionSystem` / `CombatSystem`，职责边界清晰，类图直接画得出来。
- **前端是常规 OOP**：`game/` 与 `render/` 层的输入处理、UI 组件、相机、建造放置等都是标准面向对象设计，可作为"类对象设计"的主要展示部分。
- 若希望进一步加分，可把"为何不采用逐实体多态继承"作为答辩 PPT 中「难点与解决方法」的一条，用确定性回放测试的需求来论证——这比堆砌继承层次更能体现工程判断。

### 待确认

**需向老师/助教确认**：上述类设计（系统层为类、实体为扁平数组）是否满足"类对象设计"的要求。只有当老师明确期待"Unit 基类 + Archer 派生"这类**实体继承体系**时才存在真正的冲突。应在第 2 周结束前有答案。

这已是**唯一**需要向老师确认的事项——原本还有一项"C++ 核心 + C# 前端是否满足课程要求"，在决定放弃 Unity、前端改用 C++ 图形库自绘之后已自动作废。

---

## 3.「人员分工单独一页，作为个人成绩重要依据」→ 提交纪律

**每名成员提交自己的代码，不要由一人代为 push。** git history 是分工最硬、最省事的证据，无需额外整理即可支撑答辩 PPT 中的分工页。

设计讨论阶段的文档提交由组长统一完成不影响此结论，但进入编码阶段后各人必须各自提交。

---

## 4.「课程设计日志：记录每次上机每名组员的进展情况」

这是最容易拖到最后现编的一项。约定：

- 在仓库内维护 `日志.md`，每次上机随手记录，最终转为 .doc
- `git log` 可自动补齐大部分内容，但**时间线与个人进展仍需人工记录**，不要指望完全由 git 生成

---

## 5. 交付文档的来源映射

四份 .doc 报告是实打实的工作量（估计 3–5 人日），**必须排进计划，不能留到最后一晚**。好消息是多数内容已存在于仓库文档中：

| 交付文档 | 主要来源 |
|---|---|
| 课程设计任务书 | `CLAUDE.md` 的「项目性质与硬约束」「游戏形态」 |
| 系统分析报告 | `CLAUDE.md` 的「架构：三层隔离」及各功能设计小节 |
| 系统设计报告 | 类图（需新绘）+ 架构章节 + 界面设计（需新写） |
| 程序说明书 | 需新写（操作说明、按键、玩法引导） |
| 课程设计日志 | `日志.md` + `git log` |
| 答辩 PPT | 全部文档 + `CLAUDE.md` 的「评估指标」章节 |

---

## 6. 打包注意事项

- 「软件源代码**包括输入文件和输出文件**」：需一并交付 JSON 数值表、地图数据、回放样例、训练曲线等产物
- 「删除 Debug/Release 文件夹中编译文件」：`.gitignore` 已排除 `build/`，但最终打包时需再次确认交付包内无编译中间产物
