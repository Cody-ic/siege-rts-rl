# 交付要求的工程影响

本文件把 `课程设计最终交付项.md`（老师原文，**不得改动**）翻译成对架构、排期与协作纪律的具体约束，并记录我们的应对。

三条交付要求会直接推翻或约束既有的架构决策，按影响程度排列如下。

---

## 1.「软件可执行程序：可独立执行」→ 推理必须脱离 Python

**约束**：交付的可执行程序不能依赖 Python / PyTorch 运行环境。而我们的策略网络在 PyTorch 中训练——因此**训练用 Python，推理必须回到 C++**。

这一条在原架构文档中完全没有考虑，属于新增的硬约束。

### 方案对比

| 方案 | 评价 |
|---|---|
| **导出权重 + C++ 手写极简推理** ← 推荐 | 小型 CNN + MLP 约两百行；零依赖、确定性好、无跨平台构建负担；本身也是 C++ 课程的加分内容 |
| 导出 ONNX + 链接 ONNX Runtime | 稳妥通用，但引入一个非 header-only 的重依赖，与 `CLAUDE.md` 中的第三方库约定冲突，且增加 MSVC/GCC 双工具链的构建成本 |

### 连带要求

选择手写推理意味着**网络结构必须保持简单**：不使用 Transformer、LSTM 或任何需要复杂算子的结构，否则手写推理不再划算。

这反过来约束了 RL 架构选型，**应在第 2 周之前定下来**，不要等到训练跑通再考虑部署。原本"共享策略网络 + 局部观测 CNN"的设计恰好满足此要求，不需要改，但今后若有人提议换更复杂的网络，必须先回答"手写推理还写不写得动"。

---

## 2.「系统设计报告：描述软件的类对象设计」→ 与 SoA 布局的张力

**冲突**：`CLAUDE.md` 明确规定"单位/建筑用 SoA 数组布局，**不是每个单位一个多态对象**"。而课程要求提交类对象设计说明，通常期待的是"Unit 基类 + Archer 派生"这类典型 OOP 结构。

### 应对：不改架构，改叙述方式

SoA 是性能与 RL 训练吞吐量的需要，不应为了写报告而推翻。三条应对：

1. **系统本身就是类。** 实体是数组，但 `World` / `Sim` / `BatchedEnv` / `PathFinder` / `VisionSystem` / `CombatResolver` 都是类，职责边界清晰，类图完全画得出来。报告不缺内容可写。
2. **前端是常规 OOP。** `game/` 与 `render/` 层的输入处理、UI 组件、相机、建造放置等都是标准面向对象设计，可作为"类对象设计"的主要展示部分。
3. **把这个取舍写进「难点与解决方法」。** 交付清单的答辩 PPT 恰好要求这一节。用实测吞吐量数据说明"为何放弃多态对象改用 SoA"，比堆砌继承层次更能体现工程判断，是加分项而非扣分项。

### 待确认

**需向老师/助教确认**：数据导向设计是否满足"类对象设计"的要求。建议与另一个待确认问题（C++ 核心 + C# 前端是否满足课程要求）**在同一次沟通中一并问掉**，两者都应在第 2 周结束前有答案。

---

## 3.「人员分工单独一页，作为个人成绩重要依据」→ 提交纪律

**每名成员提交自己的代码，不要由一人代为 push。** git history 是分工最硬、最省事的证据，无需额外整理即可支撑答辩 PPT 中的分工页。

设计讨论阶段的文档提交由组长统一完成不影响此结论，但进入编码阶段后各人必须各自提交。

---

## 4.「课程设计日志：记录每次上机每名组员的进展情况」

这是最容易拖到最后现编的一项。约定：

- 在仓库内维护 `日志.md`，每次上机随手记录，最终转为 .doc
- `git log` 可自动补齐大部分内容，但**时间线与个人进展仍需人工记录**，不要指望完全由 git 生成

---

## 5. 交付文档的来源映射

四份 .doc 报告是实打实的工作量（估计 3–5 人日），**必须排进计划，不能留到最后一晚**。好消息是多数内容已存在于仓库文档中：

| 交付文档 | 主要来源 |
|---|---|
| 课程设计任务书 | `CLAUDE.md` 的「项目性质与硬约束」「游戏形态」 |
| 系统分析报告 | `CLAUDE.md` 的「架构：三层隔离」及各功能设计小节 |
| 系统设计报告 | 类图（需新绘）+ 架构章节 + 界面设计（需新写） |
| 程序说明书 | 需新写（操作说明、按键、玩法引导） |
| 课程设计日志 | `日志.md` + `git log` |
| 答辩 PPT | 全部文档 + `CLAUDE.md` 的「评估指标」章节 |

---

## 6. 打包注意事项

- 「软件源代码**包括输入文件和输出文件**」：需一并交付 JSON 数值表、地图数据、回放样例、训练曲线等产物
- 「删除 Debug/Release 文件夹中编译文件」：`.gitignore` 已排除 `build/`，但最终打包时需再次确认交付包内无编译中间产物
