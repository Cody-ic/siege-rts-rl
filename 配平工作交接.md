# 配平工作交接（2026-09-03）

> **给接手攻守平衡这条线的人。** 本文只写「现在在哪、接下来做什么、哪些坑会再咬你」，
> 不复述结论——结论在下面那四份文档里，此处只给指针（同一件事写在两处必然漂移）。
>
> 读之前先知道一句话：**配平不要在手写模型上做，要在仿真上做。**
> 这条是这一轮最贵的教训，理由见 §4。

---

## 0. 三十秒版

| 量 | 现在 |
|---|---|
| 中位陷落波 | **4**（12 池图 × 3 种子，均值 4.2，分布 {3:3, 4:24, 5:6, 6:3}） |
| 目标 | **从未定过**。我提的占位是 20，没有被批准，别当结论用 |
| 破口分布 | 墙 63 / 门 36 |
| 波次死锁 | 已修两处，36 局无截断 |

**下一件该做的事**：`波次分段与侦查时序.md` §9 的第 1、2 步（`WaveTiming` 拆三段 +
第三段冻结落在 `World::apply_one`）。它们是其余全部的前置，而且**第 5 步之前，
「先侦查再决定主攻方向」这条 CLAUDE.md 称作最能体现 AI 智能的可演示行为，
在代码里不存在**。

---

## 1. 文档地图（按该读的顺序）

| 文件 | 读它干什么 |
|---|---|
| `攻守实力模型与平衡分析.md` | 正确的货币（Σ√B 不是 ΣB）与「攻城是入口处的赛跑」。**§8 是既有文档的误导清单**（已订正）；§7.1 是校准 runner 的首批数据 |
| `资源点分布与经济平衡.md` | 资源分布合不合理，按**环带**分开判。内环带对、外环带是个口子 |
| `攻守配平的数学模型.md` | 余量曲线 `M(w)` 与四条判据（S1–S4）。**先读 §8.1 与 §8.2**：前者是它的定位声明（聚合模型盖不全变量），后者是真机基线与两条被推翻的处方 |
| `波次分段与侦查时序.md` | **提案，代码未动。** 侦查时序的形状 + §7 五条待拍板 + §9 落地清单 |
| `tools/calibration_runner/README.md` | runner 怎么用、每波记什么、**「多种子」的实测语义**（见 §5 第一条坑） |
| `tools/balance/README.md` | 三份分析脚本各自算什么 |

---

## 2. 三条未解决的结构性问题（有先后）

### 2.1 塔打不掉 ⇒「防御是消耗品」这个支点没落地

实测：在「刚好守住」的塔数上跑入口攻防，**塔零损失、弓手每波全灭**。
`Ram` 的 `aoe_radius` 只有 1.3，只溅到紧贴城门的一两座；`Phoenix` 编成里恒 1 只，
而 CLAUDE.md 说它的上限「可随波数缓慢放开」——从未放开。

**为什么它排第一**：`CLAUDE.md`「防御建筑与城墙是消耗品」整节把它标为
「多条设计的支点，不要简化掉」，而 `攻守配平的数学模型.md` §4.2 证明了
**只有它能翻转余量曲线的斜率**——塔是永久存量时，累计收入相对任何多项式需求
都会赢，无尽模式的「必败」不成立。

**但注意 §8.2 的更正**：真机里起决定作用的是另一个东西（§2.2），
这两件事当时被我混在一起了，别照 §4.2 的处方直接调 λ。

### 2.2 建筑升级定价：造价线性、战力开方 ⇒ 最优档恒 1 级

塔造价 `80 + 32(L−1)` 是**线性**的，火力 `∝ √B(L)` 是**开方**的。两者不同阶 ⇒
每石买到的火力 `∝ 1/√L` ⇒ **堆量严格占优**，实测最优档恒为 1 级，
于是「堡垒等级 → 建筑等级上限」这个输出**从未被使用**。

**这与 `数值设计与成本产出矩阵.md` §12 给单位修掉的是同一条**（当时只改了单位，
建筑漏了）。把塔造价改成 `∝ √B(L)` 后最优档移到 9–11 级**且总石材更低**
（第 20 波 4005 vs 5120）。改法现成，`tools/balance/balance_solver.py --fix` 有对照表。

### 2.3 侦查没有内容 ⇒ 整个智斗层不存在

`wave_scouted_` 是一个 bool，唯一的消费者是 `Wraith` 自己的行为（没到手就前压、
到手就掉头）。**攻方的编成/主攻方向/目标选择一个字都不读它。**

分界要记清：**生产端完整**（`FogLayer` 三态 + 记忆建筑 + `obs_pack` 按硬要求 1
打包，`visible` 三档里 **0.5 就是「上一轮的信息」**），**消费端在 demo 脚本里不存在**
（方向是 `wave % n_spawn` 取余）。RL 宏观层一开工，消费端自动就有了。

形状见 `波次分段与侦查时序.md`，落地清单在它的 §9。

### 2.4 一件横在中间的：情报覆盖石材那一半、覆盖不了人口那一半

部队不在建筑记忆里，而弓手征兵 100 tick + 登墙 30 tick = 130，只有塔 240 的一半多。
所以**守方实力越往兵上偏，AI 情报价值越低**。

这条与 2.1 是同一个问题的两面：**塔要不要是消耗品、石材还是人口咬住实力，
决定了侦查有没有价值。不能分开定。**

---

## 3. 待拍板（不要替组里决定）

| # | 事 | 在哪 |
|---|---|---|
| 1 | **中位存活波数的目标值**。全仓从未定过；我占位 20，无依据 | `攻守配平的数学模型.md` §7 |
| 2 | 三段建造期的时长比例 | `波次分段与侦查时序.md` §7.1 |
| 3 | 第三段能否被「提前召唤下一波」跳过 | 同 §7.2 |
| 4 | 征兵在第三段放行到什么程度（与 2.4 耦合） | 同 §7.3 |
| 5 | 驻守中的弓手要不要纳入侦查判据 | 同 §7.4 |
| 6 | 窥使数量上限（`WaveCurve` 里已是参数，现恒 1） | 同 §7.5 |
| 7 | 外环带 37 个资源点的定位（砍数量 / 推迟解禁 / 承认是留白） | `资源点分布与经济平衡.md` §4.2 |

---

## 4. 别重做的事（我做过，是弯路）

### 4.1 不要在手写聚合模型上找配平参数

我写了两份（`siege_race.py` 的入口赛跑 + `balance_solver.py` 的联立优化），
它们**作为子系统分析仍然有用**（需求表 `D(w, L_d, A)`、石材存量 vs 金币流量的
记账、那条「改价格只平移、翻不了斜率」的命题），但**它们给出的处方在真机里
错了两条**：

| 模型说 | 真机 |
|---|---|
| 「先压 `Ram` 等级曲线，B(w) 是主难度旋钮」 | `power_alpha` 1.25 → 0.40 只把陷落波从 `3 3 3 1` 动到 `3 5 4 1`，**几乎无效** |
| 「弓手集中度是唯一大杠杆，值 4–6 倍」 | A/B 下来**中性**（`3 1 3 1 3 3` vs `3 1 3 1 3 2`） |

**根因**：模型假设「守方是一个合理但不操作的玩家」，而真机里**没有人替守方下宏观
命令**。假设与实现之间这道缝，比模型里任何一个参数的取值都重要。

**能覆盖全部变量的模型只有仿真本身。** 手写模型补到哪一步都能被正确地再问一次
「其它变量呢」——三轴克制、冲锋助跑、枪阵反冲锋、AOE 落点锁定、flow field 把墙当
高代价可通行、迷雾与佯攻、隘口、河桥、驻守与高度优势、在途弹丸、工匠线性加速、
维修与拆除退款、集结期与编队推进……

### 4.2 不要以为「几何」问题是「数值」问题

这一轮最大的单项改进不是任何一个数，是**发现塔射程 7 < 城区半径 9–10，
堡垒周围有一整圈没有火力覆盖**。攻方破门后走到堡垒脚下零抵抗
（6 只 1 级 `Ghoul`：破 520 血门 24 s、砸 2400 血堡垒 112 s）。

补上近卫塔后前两波从必死变**完胜**。而在那之前，「拉弓手下墙」中性、
「压平攻方曲线」无效——**堵不住的不是火力不够，是那一圈没有火力。**

**方法**：怀疑平衡问题时，先把射程、视野、速度、半径这几组数放在一张表上比一遍。
量纲不匹配的地方（射程 < 半径、视野 > 最大射程）往往就是病根。

---

## 5. 会再咬你的坑

### 5.1 `--seeds 1,2,3` 等于同一局跑三遍

**36/36 局：同一张图三个种子给出完全相同的陷落波，一局不差。** 攻方一条随机分支
都没有（编成按曲线、方向按取余），而 demo 的确定性本身是设计要求。

- 跑 12 局就是全部信息，别浪费三倍时间
- **「中位存活波数」这个评估指标目前测不出它该测的东西**：中位要有分布才有意义，
  而现在它只反映**地图池的构成**、不反映策略强弱。要方差得等 RL 策略，
  或给脚本攻方补随机化（`守方AI与协同演化.md` 那条「随机化那一半还没做」）

### 5.2 `World` 对下不了的命令不报错，只是不执行

所以「发了多少条命令」不能当「做成了多少件事」。我踩过：塔位候选是环内侧那一圈，
正是自家单位聚集处，而 `World::Build` 拒绝「有地面单位站着的格」
（`can_place_hint` **不查这条**）⇒ 每周期重发、一局刷 **2776 条**无效命令。

**写任何往 `World` 提交命令的代码时，先去 `world.cpp` 的 `apply_one` 把那一种命令
的拒绝条件读完，逐条镜像。**

### 5.3 大额低频支出不能按比例分成

按「库存的百分之几」给堡垒升级（200 石）或采集建筑（40 石）时，塔在每一轮把库存
抽干 ⇒ 那一份永远攒不到门槛。实测 `upg=0 gath=0`，两个输出全程是装饰品。
**用预留或限速，不要用比例。**

### 5.4 构建环境

```bash
# 必须用绝对路径 + 正斜杠（相对文件名在这台机器上报「不是内部或外部命令」）
cmd //c "$(pwd)/build_test.bat"        # Ninja Release，RENDER=OFF，出 build-Release2/
```

- **`python` 可能是商店占位 alias**（零输出、退出码 49）。一律用 `py`
- Git Bash 下中文输出乱码 ⇒ `PYTHONIOENCODING=utf-8`
- **不要在守卫脚本后面接管道**：`py ... | tail` 取到的是 `tail` 的退出码，恒 0
- **`git merge` / `rebase` / 切分支之后不要相信增量构建**（`CLAUDE.md` 有整节讲
  ODR 违反导致的假「栈损坏」）。`rm -rf build-Release2` 重配，几分钟换掉几轮误判
- **runner 正在跑时构建会 LNK1104**（Windows 锁定运行中的 exe）。先等它结束
- `/tmp/xxx.json` 这类路径**不能直接传给 exe**（Git Bash 路径，Windows 不认）。
  用 `build-Release2/xxx.json`

### 5.5 用 Python 补丁改文件时，heredoc 会吞反斜杠

`<<'PYEOF'` 里的 `"\\n"` 到 Python 手上可能已经变成真换行，于是 `assert old in s`
失败而你以为是找错了字符串。**要匹配 C++ 源码里的 `\n` 字面量就用 `chr(92)` 拼**。

### 5.6 新 UI 文案要登记进 `render/src/text.cpp` 的 `ui_strings()`

缺一个码点是**画的时候抛**，而截图回归测试点不到那个分支 ⇒ 不会有任何测试变红，
只会在试玩时崩。

---

## 6. 怎么跑

```bash
# 基线（守方接宏观层）。--limit N 只跑前 N 张图，本地快跑用
./build-Release2/tools/calibration_runner/calibration_runner.exe \
    --seeds 1 --max-waves 20 --max-ticks 60000 --macro \
    --out build-Release2/base.json

# A/B 一个旋钮（宏观层四个开关 + 曲线全套 + 波次节奏都有旗标）
... --macro --macro-recall 0        # 关掉「敌人进城拉弓手下墙」
... --macro --power-alpha 0.7       # 压平兵力预算
... --macro --power-form log        # 换曲线形式（power|linear|log|sat）
... --macro --build-ticks 520       # 建造阶段时长
--help 看全部

# 三份分析脚本（纯 Python，不进构建）
py tools/balance/siege_race.py towers      # 拦住 Ram 要几座塔
py tools/balance/economy_race.py dist      # 池图资源分布实测
py tools/balance/balance_solver.py upgrade # 建筑升级划不划算
```

---

## 7. 未合的 PR 与它们的耦合

| PR | 内容 | 注意 |
|---|---|---|
| **#139** | 本轮全部（守方宏观层、两处死锁、曲线与节奏运行期化、门血 75%、四份文档） | 待评审 |
| **#138** | 幽影窥使改判空中单位 | **与 #139 耦合**：一个杀不掉的窥使会把波次永远挂住，而 #139 的终止判据（改成「攻方**还能打的**归零」）正好兜住它。两个最好一起落，或 #138 以 #139 为基 |

关于 #138 的一个数值观察（不构成反对，但要知道）：窥使视野 **10**，而最大对空射程
是墙上弓手的 5.0 + 1.5 = **6.5**（`Flak` 5.0）。它在离堡垒约 18.5 格就看见塔并掉头，
比弓手极限还远 2.5 格 ⇒ **几何上谁都碰不到它**。

但按 `波次分段与侦查时序.md` 的排法，玩家的反制从「射死它」变成
「让它看到的东西过时」——那不需要够得着它。所以不必为此压视野；
真正该先做的是**消费端**（§2.3），在情报不被使用之前，调侦查单位的任何数值
都是在调一个没有后果的东西。
