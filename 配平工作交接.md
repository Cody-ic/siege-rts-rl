# 配平工作交接（2026-09-03，2026-09-04 大改）

> **给接手攻守平衡这条线的人。** 本文只写「现在在哪、接下来做什么、哪些坑会再咬你」，
> 不复述结论——结论在下面那四份文档里，此处只给指针（同一件事写在两处必然漂移）。
>
> 读之前先知道一句话：**配平不要在手写模型上做，要在仿真上做。**
> 这条是这一轮最贵的教训，理由见 §4。

---

## 0. 三十秒版（2026-09-04 收盘）

> ⚠️ **本文 09-03 那一版里凡是「实测出来」的数一律作废。** 它们测的是一个
> **只会招弓手、不建兵营、不建防空、只有一名工匠**的陪练。补齐之后同样的图
> 从第 3–5 波陷落变成 **60 波打不完**。作废清单：中位陷落波 4、塔的边际收益、
> 「人口上限没在咬」、「K 停在 1」、金币冗余量级。旧数留在 §2 各条里作决策史，
> 每条都标了「脚本读数」。

| 量 | 现在（2026-09-04） |
|---|---|
| 脚本守方 | ⚠️ **2026-09-05 起三张图全部陷落**（第 4 / 23 / 6 波）——见 §2.11。下面这一行是 09-04 的读数，保留作对照 |
| ~~脚本守方（09-04）~~ | ~~3 张池图 **全部撑满 60 波**~~ |
| ⇒ 结论 | **「无尽模式必败」2026-09-05 第一次在实测里成立**（此前 60 波内不成立）。但**幅度过头**：第 4 波陷落显然太早，而目标值仍未定 |
| 堡垒等级 K | 1 → **15**（60 波）。此前恒 1 |
| 建筑升级 | 45–928 次。此前 **0** |
| 资源 | 第 60 波 **金 33432 / 木 60496**，石材始终 0–50 ⇒ 三条决策轴仍只有石材是活的 |
| 人口 | 180 波里 **139 波顶格（77%）** ⇒ 人口上限现在是真的在咬 |
| 目标陷落波 | **仍然从未定过**。别把我占位的 20 当结论 |
| 试玩（人操守方） | 组长第 8 波陷落（09-03，旧脚本时代的数）；09-04 的四条体验反馈已全部落地 |

**为什么「60 波不陷落」不需要重跑就作数**：那组数据跑在建造期 520 拍时；
之后建造期涨到 660（收入按 tick 结算 ⇒ 每波更多资源、施工窗口更长、
`Keep` 升级第一次装得进一个建造期）。守方**只增不减**，所以「撑得住」这个
结论 a fortiori 成立。会变的只是绝对数字（更大）与曲线形状（可能更陡）。
**09-03 我曾写「那组数据不作数」，那是错的，组长纠正了：把「数字会变」
与「结论作不作数」混成了一件事。**

**下一件该做的事**：见 §3 的第 −1 与第 0 项（都已有算术依据，不是拍脑袋）。

## 1. 文档地图（按该读的顺序）

| 文件 | 读它干什么 |
|---|---|
| `攻守实力模型与平衡分析.md` | 正确的货币（Σ√B 不是 ΣB）与「攻城是入口处的赛跑」。**§8 是既有文档的误导清单**（已订正）；§7.1 是校准 runner 的首批数据 |
| `资源点分布与经济平衡.md` | 资源分布合不合理，按**环带**分开判。内环带对、外环带是个口子 |
| `攻守配平的数学模型.md` | 余量曲线 `M(w)` 与四条判据（S1–S4）。**先读 §8.1 与 §8.2**：前者是它的定位声明（聚合模型盖不全变量），后者是真机基线与两条被推翻的处方 |
| `波次分段与侦查时序.md` | **提案，第三段冻结仍未动**（守方侧的斥候侦查已按二元判定落地，见 §2.3）。 侦查时序的形状 + §7 五条待拍板 + §9 落地清单 |
| `tools/calibration_runner/README.md` | runner 怎么用、每波记什么、**「多种子」的实测语义**（见 §5 第一条坑） |
| `tools/balance/README.md` | 三份分析脚本各自算什么 |

---

## 2. 未解决的结构性问题（有先后）

### 2.1 塔打不掉 ⇒「防御是消耗品」这个支点没落地

实测：在「刚好守住」的塔数上跑入口攻防，**塔零损失、弓手每波全灭**。
`Ram` 的 `aoe_radius` 只有 1.3，只溅到紧贴城门的一两座；`Phoenix` 编成里恒 1 只，
而 CLAUDE.md 说它的上限「可随波数缓慢放开」——从未放开。

**为什么它排第一**：`CLAUDE.md`「防御建筑与城墙是消耗品」整节把它标为
「多条设计的支点，不要简化掉」，而 `攻守配平的数学模型.md` §4.2 证明了
**只有它能翻转余量曲线的斜率**——塔是永久存量时，累计收入相对任何多项式需求
都会赢，无尽模式的「必败」不成立。

**但注意 §8.2 的更正**：真机里起决定作用的是另一个东西（§2.2），
这两件事当时被我混在一起了，别照 §4.2 的处方直接调 λ。

### 2.2 ~~建筑升级定价~~ **已修（2026-09-03）**，但它治的病还在别处

塔造价曾是 `80 + 32(L−1)`（线性）而火力 `∝ √B(L)`（开方）⇒ 每石买到的火力
`∝ 1/√L` ⇒ 堆量严格占优、最优档恒 1 级、「堡垒等级 → 建筑等级上限」这个输出
从未被使用。**已改为累计 `∝ √B(L)`**（`World::bld_upgrade_cost_stone()`，
schema stats/11 → stats/12），与 `数值设计与成本产出矩阵.md` §12 给单位修掉的
那条同型。`py tools/balance/balance_solver.py upgrade` 并排打旧/新两张表。

**但实测它改不动陷落波，而那个「改不动」本身是下一条的证据**：12 张池图
`bupg=0`——`Keep` 一局只升 1 次 ⇒ `building_level_cap = ceil(2/2) = 1` ⇒
**没有任何非 `Keep` 建筑够格升级**。定价对了，而axis 根本没打开。见 2.5。

### 2.3 侦查：**守方那一半 2026-09-04 已落地**，攻方消费端仍然空着

**已做（守方侧，二元判定）**：斥候走到看得清集结点的距离 → 掷一次死活
（`DefenderSetup::scout_death_permille`，占位 400‰）→ **活着才拿到本波完整
编成快照，死了一个字都没有**。形状与攻方 `Wraith` 对称，组长定的。
面板读 `DemoBattle::scout_outcome()` / `scout_report()`，失败也报
（「派了没回来」与「根本没派」是两种处境）。

> **这里我先写错了一版，值得留着当反面教材。** 我第一版做成 `IntelLog`——
> 按帧累积「看见过什么」。那样**侦查从来不会失败**：斥候刚出城门瞟一眼就在
> 漏情报，猎杀它没有意义（它死之前已经漏了一路），`Scout` / `Wraith` 的整个
> 博弈被消掉。组长一句「它死了就是侦查失败，压根不应该有任何信息给玩家」
> 点掉了它。**侦查必须是二元事件，中间不能有渐进渗漏。**
>
> 实现上还踩了一个坑：`DefenderScript` 让斥候走向「最近的**当前不可见**的
> 集结点」，所以它一进视野就把目标看见了、目标随即被排除 ⇒ **永远走不到
> 那一格**。判据因此定为「走到看得清的距离」（默认取斥候视野，自维护），
> 而那也是更对的设计——风险由掷点承担，不由「站得多近」承担。

**仍然空着（攻方消费端）**：`wave_scouted_` 是个 bool，攻方的编成/主攻方向/
目标选择**一个字都不读它**（方向仍是 `wave % n_spawn` 取余）。所以
「杀掉 `Wraith` 让 AI 带错情报」仍然零收益。生产端（`FogLayer` 三态 +
记忆建筑 + `obs_pack` 按硬要求 1 打包）一直是完整的。
形状见 `波次分段与侦查时序.md`，落地清单在它的 §9。

`wave_scouted_` 是一个 bool，唯一的消费者是 `Wraith` 自己的行为（没到手就前压、
到手就掉头）。**攻方的编成/主攻方向/目标选择一个字都不读它。**

分界要记清：**生产端完整**（`FogLayer` 三态 + 记忆建筑 + `obs_pack` 按硬要求 1
打包，`visible` 三档里 **0.5 就是「上一轮的信息」**），**消费端在 demo 脚本里不存在**
（方向是 `wave % n_spawn` 取余）。RL 宏观层一开工，消费端自动就有了。

形状见 `波次分段与侦查时序.md`，落地清单在它的 §9。

### 2.4 一件横在中间的：情报覆盖石材那一半、覆盖不了人口那一半

部队不在建筑记忆里，而弓手征兵 100 tick + 登墙 30 tick = 130，只有塔 240 的一半多。
所以**守方实力越往兵上偏，AI 情报价值越低**。

这条与 2.1 是同一个问题的两面：**塔要不要是消耗品、石材还是人口咬住实力，
决定了侦查有没有价值。不能分开定。**

---

### 2.5 **`Keep` 的价 / 回报比 ⇒ 守方三个输出里两个是装饰品**（2026-09-03 新增）

起因是试玩反馈「第 8 波陷落，人口上限 2 级 12 太少了，比敌人还少」。
查下来**人口上限的公式不是病根**（完整推导 `波次预算曲线与堡垒等级曲线.md`
§2.2，可跑：`py tools/balance/pop_cap.py`）：

| 200 石 + 200 木 买到 | 火力 | 血量 | 附带 |
|---|---|---|---|
| 升 1 级堡垒 ⇒ +2 人口 = 2 名弓手 | 20.0 dps | 400 | **还要另付 120 金** |
| 2.5 座箭楼（只花 200 石 + 75 木） | **35.0 dps** | **1500** | 不占人口、不花金 |

⇒ **升堡垒的火力只有建塔的 1/1.8、血量 1/3.8。**「宏观层一局只升 1 次」
不是脚本写坏了，是这个比价下的正确答案。于是 `K` 实战中恒等于 2，
`cap(K) = 8+2K` 永远是 12、`ceil(K/2)` 永远是 1 ⇒ **人口上限与建筑等级上限
两个输出的曲线形状无关紧要**。

`8+2K` 当初只过了一条判据（「咬得住」），而那条**整个没有把攻方那条曲线
放进来**。§2.2 补了两条（C2 不是天花板 / C3 够得着），并给出：`K` 若能走到
`≈ w/2`，`Σ√B` 比值稳在 0.53–0.64（健康）；`K` 停在 2 则从 2.21x 塌到 0.16x。

**别单独抬人口上限**：它是三层天花板里最松的一层（金币 18.2 / 人口 12 /
等级上限 K=2），抬到 16 之后金币立刻接手，什么也没换到。

### 2.5b 三种资源里只有石材是活的（2026-09-03 实测）

`资源点分布与经济平衡.md` §4.5，可跑：
`py tools/balance/pop_cap.py --runner build-Release2/near.json`。

| 资源 | 波末中位 | 「这一波涨了」的波占比 | 判断 |
|---|---|---|---|
| 石 | 42 | 22/51 | **唯一真的紧的** |
| 木 | **413**（最大 3000） | **42/51** | **死的** |
| 金 | 40（拿外矿后最大 3387） | 29/51 | 被人口上限掐死 |

木材那条是纯比例错误、**与外矿无关**：`Lumber` 产 14 而 `Quarry` 产 9
（0.64:1），十座建筑造价却是 430:325（1.32:1）⇒ **每单位时间拿到 1.56 份
木/石，只花得掉 0.76 份**，一根不浪费也照堆。`CLAUDE.md`「守方多资源」
预言过这个失效（「木材永远花不完，『这波该攒哪种』就不成其为决策」）。

金币那条的 sink 就是人口上限（2.5），**在 K 动起来之前给金币加别的 sink
都是绕过病根**；顺带：尚未实现的「买活」正好是一个金币 sink，它现在有了
第二个存在理由。

### 2.5c 石材 → 塔 这条出口也被稀释：主攻方向每波轮换，而塔是永久的

`资源点分布与经济平衡.md` §4.6（2026-09-04 实测）。给守方多喂石材
（优先铺采石场）让塔数 **+45%**，中位陷落波只从 4.0 挪到 4.5。追下去：

| | 全场塔中位 | **打得到破口的塔**中位 | 覆盖率 |
|---|---|---|---|
| 基线 | 3.0 | **1.0** | 25% |
| 石材优先 | 4.5 | **1.0** | 20% |

塔多了 45%，「打得到破口的塔」**一座没多**。成因是
`demo_driver.cpp:368` 的 `main_spawn = wave % 集结点数`——**确定性轮询**，
而塔是永久的 ⇒ 一座塔平均只有 1/n 的波次正对主攻。

**分两半读**：塔覆盖不全是 `CLAUDE.md` 要的（「建筑定义形状、部队补洞」）；
但**强制轮换**是占位实现的副作用，是所有方向策略里对永久防御最不利的一个，
而设计要的是攻方读侦查结果再选（真实攻方会重复奏效的方向）。

⇒ **攻方方向策略定下来之前，「守方该在工事上投多少石材」问不出答案**——
现在量到的是面对轮询攻方的边际收益，那是下界不是设计意图。与
`波次分段与侦查时序.md`（侦查没消费端、方向不读情报）是同一件事的两端。

### 2.5d 闭环：守方被锁在 1 级，而解锁那条轴是错的花法（2026-09-04）

**这四条合起来是个闭环，而它不依赖脚本**（`波次预算曲线与堡垒等级曲线.md`
§2.3(1)）：

1. 攻方名义等级第 4 波就 14 级、第 8 波 23 级；守方 `unit_level_cap = K`，开局 1
2. ⇒ 守方开局**结构性地只能招 1 级兵**，单位战力差 1.96x（第 4 波）
3. 解锁它要升堡垒，而 200 石 + 200 木 只买到 +2 人口（20 dps）——同价 2.5 座塔
   是 35 dps ⇒ **理性玩家也不会先升它**
4. 就算想升，还要占一名工匠 400 tick，而工匠同时要砌墙、建塔、维修（三者互斥）

⇒ 守方唯一的成长轴从未启动（实测 12 局 **K 一次都没离开 1**）。
**这解释了为什么前面三轮实验（塔 +45% / 兵 ×4 / 金矿优先）全都只挪 0.2 波。**

⚠️ 上一段里只有 1–4 是算术，**「实测 12 局」那句是脚本读数**，见 §2.3(2)。

### 2.6 两侧的等级尺度不可比 ——**闭式解在这里，它把这条变成算术**

**攻方兑现战力 = √(编成位 × 兵力预算)。** 推导两行：名义等级是从预算**反解**
的（`L = 1 + (人均预算 − 1)/k`），于是 `B(L) = 1 + k(L−1) = 人均预算`，
`Σ√B = 编成位 × √(预算/编成位) = √(编成位 × 预算)`。
第 20 波验算：√(28 × 253.8) = 84.3，实测 84.7（差在等级取整）。

三条推论，都可以直接用：

1. **编成位与预算在式子里完全对称，且都是开方效率。** 编成位翻倍只给
   1.41 倍战力。所以「加单位」不是便宜的平衡杠杆。
2. **`k` 被约掉了 ⇒ 攻方战力与 `k` 完全无关。** 而守方的等级上限 `K` 是个
   **计数器**（不是反解出来的），它的战力 `√(1+k(K−1))` 随 `k` 增长。
   ⇒ **抬 `k` 是一次纯守方增益，攻方一分不涨。** 见 §3 第 0 项。
3. ⚠️ **CLAUDE.md 那条「p+q 太大则克制关系被等级淹没」假设等级是外生的。**
   固定等级差时 k 越大 B 比越大——那是对的；但攻方的等级是**内生**的
   （从预算反解），k 一涨等级自己就掉。**所以那条界在攻方一侧不适用**，
   这是「两侧尺度不可比」的又一个后果，要合进 CLAUDE.md。

原始症状（保留）：攻方名义等级第 4 波 14、第 8 波 23、第 20 波 38；
守方 `unit_level_cap = K` 到得了 2–15。两者喂进**同一条** `level_permille`。

后果是克制比例随等级差衰减 `∝ B^-1`：**第 8 波整张克制二部图被稀释到 21%**。

### 2.7 守方脚本此前是坏陪练，2026-09-04 重写（结论：前面大半读数作废）

组长原话「我感觉是你的守方脚本不会玩吧」。**是的**，而且它违反了
`CLAUDE.md` 对脚本守方的判据：

> 脚本要写到什么程度有一条判据：**克制二部图里已写死的战术行为必须进脚本**，
> 因为那是设计意图、不该指望涌现。

`DefenderMacro` 此前**只招弓手** ⇒ 守方那半张克制图（枪卫克骑兵、游骑摸
攻城锤）在训练里一次都没发生过。单兵层（`DefenderScript`）那两条行为
**早就有**，缺的是宏观层从来不招这两种兵。**这不只是配平读数偏低——
攻方 RL 的陪练里没有它们，学出来的策略会过拟合到「只会打弓手的守方」。**

补上的六项：按目标编成招兵（枪卫/游骑/工匠/斥候）、兵营、防空、破口木栅、
清野、过迷雾的敌情反应。**情报口径**（组内 2026-09-04 定）：只读免费情报
（波数/总兵力/主攻方向）+ **迷雾内可见的敌人**，不碰 god 视角——读了它
攻方 RL 会过拟合到一个「先知守方」。

**写的过程中被测试逼出来两个缺陷，都值得记住**：

1. **过迷雾反应写成相加** ⇒ 骑士与攻城锤几乎每波同时可见，两档反应叠起来
   把目标份额压成 A50/S500/R450，**弓手被压到 3 个**——而弓手是墙上唯一
   吃高度优势的输出。改成归一化。
2. **开局把空位全填成工匠斥候** ⇒ 人口上限 10、开局 7 人只空 3 格，
   `mason_target 3 + scout_target 1` 正好吃满，**一个战斗兵都招不上**。
   真机里因为每波伤亡腾位子而看不见，是那条「编成覆盖克制二部图」的测试
   第一次变红时报出来的（`A0 S0 R0 M2 Sc1`）。新增
   `noncombat_max_permille`（非战斗单位 ≤ 人口上限的 30%）。

**给后来者的一条通则**：这一层的每一项「不做什么」都是一次静默的能力缺失，
而它们**不会让任何测试变红**。加新机制时回头问一句「宏观层会用它吗」——
`Flak`、`Fence`、`Barrack`、`Clear` 四样都是机制齐备而宏观层不碰，
一样静默了几十次对局。

### 2.8 守方脚本还差第五件，而它是一整条战略轴：**扩张**（2026-09-04 组长指出）

§2.7 列了四件「机制齐备而宏观层不碰」（`Flak`/`Fence`/`Barrack`/`Clear`）。
第五件比那四件加起来都大：**`DefenderMacro` 从结构上不可能扩张城墙环，
也不重建被打掉的墙。**

代码写死的，两处：

```cpp
// 构造函数：环半径只算一次，此后是常量
if (cheb(c, keep_) != ring_r_) continue;   // gaps_ / wall_cells_ 只认这一圈
tower_spots_ 取 ring_r_ - 1                 // 塔位从此固定 8x9 = 72 个
```

而 `gaps_` 是**构造时**从 `MapData` 算出的**设计缺口**，不含战损 ⇒ 脚本只封开局
那几个口、只修没打死的墙，**被摧毁的墙一格都不重建**（60 波累计：建 2 格、
被打掉 50 格，`Wall` 76 -> 28），拿 `Fence` 在糊（建了 70 道）。
`CLAUDE.md` 要的「修旧 / 建新 / 造兵」三方决策，脚本只做了「修旧」与「建新（塔）」。

**这作废了我在同一轮里下的一个结论。** 我看到塔数在第 12-20 波饱和在 ~70、
之后在降（74 -> 69），写成「塔位耗尽，是几何问题不是经济问题」，并据此把
「让塔不再是永久存量」那条杠杆降级。**那是拿一个脚本缺陷论证了一条设计结论**
——实测塔位恰好是 72（8 x (R-1)，R=10），饱和量到的是 `ring_r_` 这个构造时
算死的常数。**杠杆降级撤回。**

**扩张划不划算：划算，而且是复利**（`gen_01001000`，R=10、周长 80）：

| R | 周长 | 塔位 | 圈进资源点 | 建环石材 |
|---|---|---|---|---|
| **10（现行）** | 80 | **72** | **3** | — |
| 20 | 160 | **152** | **14** | 4800 |
| 25 | 200 | 192 | 28 | 6000 |

**这张图 72 个资源点里只有 3 个在城内。** `R=10 -> 20` 花 4800 石 = 第 12 波
实测收入（5 座采石场 = 1242 石/波）的 **3.9 波**，换 +80 个塔位 + 11 个资源点
（约 +919 石/波，**5.2 波纯石材回本，此后复利**）。代价是周长翻倍、维护面翻倍，
所以它是一个**真取舍**、且窗口在早期（5-25 波守方富余）。

这正是 `CLAUDE.md`「围哪一簇」「拐点反复出现」「大散居/小聚居」整套设计的落点，
而脚本从结构上碰不到这个决策 ⇒ **它在仿真里一次都没发生过。**

**方向是单向的**：会扩张的守方严格更强 ⇒ §0 那条「60 波打不完」a fortiori 更成立，
而「塔火力增长指数 0.195」只是**下界**，不是实测值。

### 2.9 攻方的战果全是「路过顺手」，因为它只有一个目标

`demo_driver.cpp:594`：

```cpp
const rts::GridPos goal[1] = {w_.keep_pos()};   // 攻方 flow 的**唯一**目标
```

60 波累计（`gen_01001000`，摧毁 / 守方重建）：

| | 塔 | 采石场 | 金矿 | 伐木场 | 墙 |
|---|---|---|---|---|---|
| 被攻方摧毁 | **67** | 22 | 21 | 23 | 50 |
| 守方建造 | **135** | 28 | 25 | 27 | **2** |

两条读数：

1. **我上一轮说「攻方从来不打塔」是错的**，已撤回——60 波打掉 67 座。真实形状是
   **守方重建速率 > 攻方摧毁速率**（塔 135:67，采集 28:22）。
2. **采集建筑的 churn 接近 1:1** ⇒ 守方的采集建筑数被攻方**钉**在 6 座，
   不是它自己选的。所以攻方的经济打击**已经在起作用**，只是作为副作用。
   刻意打它 = 直接掐守方唯一的成长引擎（石材 -> K -> 三个输出 + 塔）。
   这就是 `CLAUDE.md` 的「攻其必救」，奖励函数也早已设计好（摧毁建筑的即时
   奖励 = 重建成本）。**是 RL 该学的东西，脚本版只需一个占位策略。**

**顺带订正我自己的「入口赛跑」提法**：第 60 波 `Wall` 只剩 28/80 ⇒
**墙根本不是障碍了，74 座塔的火力网才是。** 攻方对「塔海」的唯一设计答案是
`Phoenix`，见下。

### 2.10 `Phoenix` 的上限从未放开，而它几乎无人可挡

`WaveCurve`：`phoenix_base = 1`、`phoenix_cap = 1`、**`phoenix_per_waves = 0`**
（0 = 恒 1）。而 `CLAUDE.md` 明写「硬性数量上限，**上限可随波数缓慢放开**，
但增速必须远低于预算增速」——**参数早就在，一次都没开过。**

实测（60 波）：

- **58/60 波有 `Phoenix` 出场，只在 12 波里被打下来（存活 79%）**
- 它在打的正是设计要它打的：`phoenix_events` 里 `{kind: Bld, target: Mine}`
- 守方全程**只有 2 座 `Flak`**，对着 74 座 `Tower` ⇒ 防空形同虚设

⇒ 「AA 的机会成本」那个两难（`CLAUDE.md` 称「本作智斗最可读的载体」）
**在仿真里从未成立过**：1 只 `Phoenix` 不值一座 `Flak`，所以脚本不建、
人类玩家也不会建。放开上限是同时激活「攻方对塔海的答案」与「守方的防空两难」
两件事，而它只是把一个 0 改成正数。

### 2.11 编队制落地（2026-09-05）：`slots_cap` 第一次有依据，而守方**开始陷落了**

**这是 §0 那张表的第一次实质变化。** 同一批图从「60 波全部打不完」变成：

| 图 | 陷落波 | 关键读数 |
|---|---|---|
| `gen_01001000` | **第 4 波** | 塔只建到 8 座，第 3 波起每波都破口 |
| `gen_01002000` | **第 23 波** | 塔第 10 波饱和 74 座、撑了 13 波 |
| `gen_01003000` | **第 6 波** | 塔 19 座 |

⇒ **「无尽模式必败」第一次在实测里成立**，而且不是靠调数值调出来的。

**三条改动共同造成它，逐条的贡献没有分离**（要分离得各跑一轮 A/B）：

1. **`slots_cap = 27`，语义 = 编队数**。取值第一次有依据（SMAC 最大官方图
   `27m_vs_30m` 恰是 27 个 agent / 我们每 agent 的观测是它的 18–21 倍，
   所以取安全带下沿 / 40 在带外无先例）。**顺带订正了 `CLAUDE.md` 引错的
   一篇论文**：arXiv 1804.00810 那个「40v60 胜率 80.5%」是**零样本泛化**
   （只训练到 20v30 直接拿去跑），不是「训练 40 个失败」，且它用 93 维状态
   + Sarsa(λ) 不是 PPO。
2. **编队制**：一队 = 同兵种 1–3 个 ⇒ 27 个 agent 对应约 **70 个单位**，
   而第 1 波就是 **18 个 Ghoul**（此前 6 个）。这是陷落提前的主要嫌疑。
3. **`ram_permille` 100 → 250**（`Ram` 单独成队 ⇒ 台数 = 队数，旧值在
   27 编队下只给 2 台）+ **`Phoenix` 上限放开** + **分队打经济**。

**幅度过头了，但方向对。** 第 4 波陷落显然太早（目标值仍未定，见 §3 第 1 项）。
下一轮该做的是**分离贡献**而不是直接回调：runner 已有全部旗标
（`--slots-cap`、`--ram-permille` 未接、`--phoenix-*`），逐个关掉跑一轮。

**一条要注意的读数**：`gen_01002000` 第 10 波起塔就饱和在 74 座、此后 13 波
一直是 74 ⇒ **§2.8 那条「塔位是常量、脚本不会扩张」仍然是活的瓶颈**。
它现在更要紧了——守方真的会输之后，「扩张能不能救回来」变成一个有意义的问题。

### 2.12 编队动作的形状：**共享子目标，不是共享输出**（有文献背书）

落地时我先写错一版——**把队长选的动作原样抄给队员**，当场打红队形测试
（散布 11.86 > 允许的 11.72）。组长让我去查开源实现，而不是继续做实验。
查下来那个做法**有明确记录是错的**：

| 来源 | 说了什么 |
|---|---|
| **TStarBot2**（全局 SC2 bot） | 自己的 ablation 里写着：共享 group macro-action 会失败，因为「macro actions don't have control over individual units, which is inflexible」。解法是两层——上层发**意图**，下层**以意图为条件**各自算逐单位动作 |
| **ROMA / RODE** | role 是**条件输入的隐向量**，每个 agent 仍从自己的局部状态解码动作 |
| **SupCom2 的 flow field 原文**（Emerson, Game AI Pro Ch.23） | **每个 agent 永远在自己那一格采样**，整条流水线没有广播步骤 |
| **OpenRA** | **根本没有编队系统**——各自下令各自寻路，冲突靠 nudge |
| **0 A.D.** | `FormationController` 是一个**独立实体**供成员跟随 |
| **Spring / BAR** | formation-move 是**另一条命令**，给**每个单位算各自的目标点**、各自寻路；locked 档额外向最慢的限速 |

⇒ **group action 从不是单位的字面输出**，它是共享的子目标。于是编队落在两处：

1. **共享目标集**（意图）：`flow_` 缓存键多一维（打堡垒 / 打经济），同队读
   同一张 field，而每个成员在**自己那一格**采样方向。
2. **向最慢的成员限速**（凝聚）：任一队员越线则整队止步——BAR 的
   locked-formation 那条规则的离散时间版本，也是**唯一**与「各自采样」兼容
   且有出货先例的凝聚手段。

⚠️ **刻意没做「虚拟队心 + 成员偏移」**：那是唯一被明确报告在 flow field 上
**失败**的方案（有团队试过 formation forces 叠加在 flowfield 之上，最后整个
放弃了 flow field、退回 waypoint A*）。我们已经在 flow field 上，所以排除。
**代价认下来**：画面上不会出现整齐的三人小队，编队是「共享意图、各自走」。

## 3. 下一步与待拍板

**2026-09-04 晚重排。** 组长的判断：守方比我们以为的更强（§2.8）⇒ 该加强的是攻方。
而实测指出「加强攻方」的正确形状**不是调 Σ√B**——那条已经涨了 46 倍而破口反
而消失了（§2.9）。前四项都有算术或实测依据；其余是真待拍板。

| # | 事 | 依据 / 在哪 |
|---|---|---|
| **A** | **放开 `Phoenix` 上限**（`phoenix_per_waves` 由 0 改正数）。58/60 波出场、存活 79%、守方只有 2 座 `Flak`。`CLAUDE.md` 早写了「可随波数缓慢放开」，参数也早就在。**一个 0 改成正数**，同时激活「攻方对塔海的答案」与「守方的防空两难」 | §2.10 |
| **B** | **攻方目标选择**：现在 flow 的唯一目标是 `keep`，所有战果都是路过顺手；而顺手已经把守方采集建筑钉在 6 座。刻意打经济 = 掐掉守方唯一的成长引擎。**这是 RL 该学的，脚本版只要占位策略** | §2.9 |
| **C** | **接回 `slots_cap = 40`**。锤血增长指数 0.186 -> **0.480**，第 60 波 +38%；代价是总 Σ√B -28%，而那个量已证不绑定。同时它本就是 `CLAUDE.md` 的结构主张（`slots_at(60) = 77` 早已冲出 20-40 区间） | 本文 §2.6 + 本轮实测 |
| **D** | **给宏观层加扩张 + 重建战损墙**。不加的话后续任何攻方增强都是在打一个「不会扩张、墙塌了不补」的假守方——同 §2.7 那个教训 | §2.8 |
| **−1** | **木材产出比例**：`Lumber` 产 14 而 `Quarry` 产 9（0.64:1），十座建筑造价却是 430:325（1.32:1）⇒ **每单位时间拿到 1.56 份木/石，只花得掉 0.76 份**，木材以约 2 倍速率净堆积。改 `Lumber.income_amount` 14 → 9 一档。**纯算术，与陪练好坏无关。别动维修单价**——那是反死亡螺旋的规则不是旋钮 | `资源点分布与经济平衡.md` §4.5(2) |
| **0** | **抬 `k`（= `hp_permille_per_level`，现 220，就是 §1.4 那个待定的 p+q）**。由 §2.6 的闭式：**攻方战力与 k 无关**（等级是反解的、k 被约掉），而守方 `K` 是计数器、战力随 k 涨 ⇒ **纯守方增益**。连带：攻方名义等级从 38 掉到个位数、稀释大幅缓解、每级堡垒买到的东西翻倍（`Keep` 那条闭环跟着松）。`p−q=0` 不动 ⇒ TTK 与破墙时间不受影响 | §2.6 |
| 1 | **中位存活波数的目标值**。全仓从未定过；我占位 20，无依据 | `攻守配平的数学模型.md` §7 |
| 2 | **攻方曲线怎么抬**（`power_alpha` / `slots_per_wave`）。⚠️ **先读 §2.6 第 1 条**：两者对称且都是开方效率，而 `slots` 还要付 RL 的账——`slots_at(w)` 第 30 波就是 40、第 60 波 76，**已经超出 CLAUDE.md 引的 20–40 区间**（`slots_cap` 2026-09-01 置 None 等实测，那个实测从没做过）。所以「加单位」该是演示可读性的决定，不该当平衡杠杆 | §2.6 |
| 3 | 三段建造期的时长比例（第三段冻结） | `波次分段与侦查时序.md` §7.1 |
| 4 | 第三段能否被「提前召唤下一波」跳过 | 同 §7.2 |
| 5 | 征兵在第三段放行到什么程度（与 2.4 耦合） | 同 §7.3 |
| 6 | 驻守中的弓手要不要纳入侦查判据 | 同 §7.4 |
| 7 | 窥使数量上限（`WaveCurve` 里已是参数，现恒 1） | 同 §7.5 |
| 8 | 外环带 37 个资源点的定位 | `资源点分布与经济平衡.md` §4.2 |
| 9 | 斥候侦查的死亡概率 400‰、「派几只」的冗余成本 | `DefenderSetup::scout_death_permille`，占位 |
| 10 | 两个空军的「本体-投影间距」要不要严格一致（我判定**不需要**：那是二元信号、移动层只有两层；追求像素一致反而要牺牲 `lift` 的统一） | PR #138 评论 |

## 4. 别重做的事（我做过，是弯路）

### 4.1 不要在手写聚合模型上找配平参数

我写了两份（`siege_race.py` 的入口赛跑 + `balance_solver.py` 的联立优化），
它们**作为子系统分析仍然有用**（需求表 `D(w, L_d, A)`、石材存量 vs 金币流量的
记账、那条「改价格只平移、翻不了斜率」的命题），但**它们给出的处方在真机里
错了两条**：

| 模型说 | 真机 |
|---|---|
| 「先压 `Ram` 等级曲线，B(w) 是主难度旋钮」 | `power_alpha` 1.25 → 0.40 只把陷落波从 `3 3 3 1` 动到 `3 5 4 1`，**几乎无效** |
| 「弓手集中度是唯一大杠杆，值 4–6 倍」 | A/B 下来**中性**（`3 1 3 1 3 3` vs `3 1 3 1 3 2`） |

**根因**：模型假设「守方是一个合理但不操作的玩家」，而真机里**没有人替守方下宏观
命令**。假设与实现之间这道缝，比模型里任何一个参数的取值都重要。

**能覆盖全部变量的模型只有仿真本身。** 手写模型补到哪一步都能被正确地再问一次
「其它变量呢」——三轴克制、冲锋助跑、枪阵反冲锋、AOE 落点锁定、flow field 把墙当
高代价可通行、迷雾与佯攻、隘口、河桥、驻守与高度优势、在途弹丸、工匠线性加速、
维修与拆除退款、集结期与编队推进……

### 4.1b 不要把「结论」和「数字」绑在一起（我 2026-09-04 犯的）

守方脚本重写、建造期从 520 涨到 660 之后，我说「之前那组『60 波不陷落』
的数据不作数了」。**组长纠正：作数啊，现在不是加强了守方吗？**

他是对的。那两次改动**只增不减**（建造期变长 = 收入按 tick 结算拿得更多、
施工窗口更长），所以「守方撑得住 60 波」这个结论 **a fortiori 成立**，
单向推断、不需要重跑。会变的只有绝对数字（更大）和曲线形状（可能更陡）。

**通则**：改动之后先问「它是单向的吗」。单向增强/削弱下，不等式方向的结论
不需要重测；只有等式、比值、拐点位置才需要。把两者一律说成「作废」是浪费。

### 4.1c 别人写的验收判据也要先核一遍（同日）

队友在 #138 里给的重渲判据是「本体-投影间距与 `Phoenix` 一致」。我照它量、
量出 0.42 vs 0.30，报了「没达标」。组长一句「为什么要一致？」问到了点上：

那个间距承担的是**二元信号**（能不能用防空塔招呼它），而移动层只有两层
（`rts/terrain.hpp`：「只有两层」），不存在高度档位。所以要求只有
「地面 ≈ 0、空中明显 > 0」，实测 0.00 vs 0.25–0.42，**余量大到读混不可能**。
而且两者若要严格一致，就得放弃 `lift` 的统一（按本体高度反向折算），
与 `assets.json`「全体空军共用一个飞行高度」直接冲突——**追求像素一致会破坏
语义一致**。

**通则**：接下别人的验收任务时，先把判据对着设计要求核一遍再执行。
照着一条过度约束的判据报「没达标」，比不报更浪费。

### 4.1d 不要拿脚本的缺陷去论证设计结论（同日，第二次）

我看到塔数在第 12-20 波饱和在 ~70 且不再涨，写成「塔位耗尽，是**几何**问题
不是经济问题」，并据此把「让塔不再是永久存量」那条杠杆降级。**错。**
饱和量到的是 `DefenderMacro` 构造时算死的 `ring_r_`——脚本不会扩张，
所以塔位永远是 8x(R-1) = 72 个。组长一句「你没有考虑过玩家可以通过建城墙
扩展自己地盘的情况吗」点掉了它。

这与 §2.7 那次是**同一个错误的第二次**：那次是拿坏陪练量数值，这次是拿坏陪练
下**结构结论**——后者更贵，因为数值可以重测，而降级掉的杠杆不会有人回头再捡。

**通则**：写下「这是几何/结构/物理限制」之前，先去代码里确认那个限制**不是
某个常量**。判据很简单——**实测值恰好等于一个能从代码里读出来的整数**
（这次是 69 对 72），那多半是常量不是几何。

### 4.2 不要以为「几何」问题是「数值」问题

这一轮最大的单项改进不是任何一个数，是**发现塔射程 7 < 城区半径 9–10，
堡垒周围有一整圈没有火力覆盖**。攻方破门后走到堡垒脚下零抵抗
（6 只 1 级 `Ghoul`：破 520 血门 24 s、砸 2400 血堡垒 112 s）。

补上近卫塔后前两波从必死变**完胜**。而在那之前，「拉弓手下墙」中性、
「压平攻方曲线」无效——**堵不住的不是火力不够，是那一圈没有火力。**

**方法**：怀疑平衡问题时，先把射程、视野、速度、半径这几组数放在一张表上比一遍。
量纲不匹配的地方（射程 < 半径、视野 > 最大射程）往往就是病根。

---

## 5. 会再咬你的坑

### 5.1 `--seeds 1,2,3` 等于同一局跑三遍

**36/36 局：同一张图三个种子给出完全相同的陷落波，一局不差。** 攻方一条随机分支
都没有（编成按曲线、方向按取余），而 demo 的确定性本身是设计要求。

- 跑 12 局就是全部信息，别浪费三倍时间
- **「中位存活波数」这个评估指标目前测不出它该测的东西**：中位要有分布才有意义，
  而现在它只反映**地图池的构成**、不反映策略强弱。要方差得等 RL 策略，
  或给脚本攻方补随机化（`守方AI与协同演化.md` 那条「随机化那一半还没做」）

### 5.2 `World` 对下不了的命令不报错，只是不执行

所以「发了多少条命令」不能当「做成了多少件事」。我踩过：塔位候选是环内侧那一圈，
正是自家单位聚集处，而 `World::Build` 拒绝「有地面单位站着的格」
（`can_place_hint` **不查这条**）⇒ 每周期重发、一局刷 **2776 条**无效命令。

**写任何往 `World` 提交命令的代码时，先去 `world.cpp` 的 `apply_one` 把那一种命令
的拒绝条件读完，逐条镜像。**

### 5.3 大额低频支出不能按比例分成

按「库存的百分之几」给堡垒升级（200 石）或采集建筑（40 石）时，塔在每一轮把库存
抽干 ⇒ 那一份永远攒不到门槛。实测 `upg=0 gath=0`，两个输出全程是装饰品。
**用预留或限速，不要用比例。**

### 5.4 构建环境

```bash
# 必须用绝对路径 + 正斜杠（相对文件名在这台机器上报「不是内部或外部命令」）
cmd //c "$(pwd)/build_test.bat"        # Ninja Release，RENDER=OFF，出 build-Release2/
```

- **`python` 可能是商店占位 alias**（零输出、退出码 49）。一律用 `py`
- Git Bash 下中文输出乱码 ⇒ `PYTHONIOENCODING=utf-8`
- **不要在守卫脚本后面接管道**：`py ... | tail` 取到的是 `tail` 的退出码，恒 0
- **`git merge` / `rebase` / 切分支之后不要相信增量构建**（`CLAUDE.md` 有整节讲
  ODR 违反导致的假「栈损坏」）。`rm -rf build-Release2` 重配，几分钟换掉几轮误判
- **runner 正在跑时构建会 LNK1104**（Windows 锁定运行中的 exe）。先等它结束
- `/tmp/xxx.json` 这类路径**不能直接传给 exe**（Git Bash 路径，Windows 不认）。
  用 `build-Release2/xxx.json`

### 5.5 用 Python 补丁改文件时，heredoc 会吞反斜杠

`<<'PYEOF'` 里的 `"\\n"` 到 Python 手上可能已经变成真换行，于是 `assert old in s`
失败而你以为是找错了字符串。**要匹配 C++ 源码里的 `\n` 字面量就用 `chr(92)` 拼**。

### 5.6 新 UI 文案要登记进 `render/src/text.cpp` 的 `ui_strings()`

缺一个码点是**画的时候抛**，而截图回归测试点不到那个分支 ⇒ 不会有任何测试变红，
只会在试玩时崩。

---

## 6. 怎么跑

```bash
# 基线（守方接宏观层）。--limit N 只跑前 N 张图，本地快跑用
./build-Release2/tools/calibration_runner/calibration_runner.exe \
    --seeds 1 --max-waves 20 --max-ticks 60000 --macro \
    --out build-Release2/base.json

# A/B 一个旋钮（宏观层四个开关 + 曲线全套 + 波次节奏都有旗标）
... --macro --macro-recall 0        # 关掉「敌人进城拉弓手下墙」
... --macro --power-alpha 0.7       # 压平兵力预算
... --macro --power-form log        # 换曲线形式（power|linear|log|sat）
... --macro --build-ticks 520       # 建造阶段时长
--help 看全部

# 三份分析脚本（纯 Python，不进构建）
py tools/balance/siege_race.py towers      # 拦住 Ram 要几座塔
py tools/balance/economy_race.py dist      # 池图资源分布实测
py tools/balance/balance_solver.py upgrade # 建筑升级划不划算（旧/新定价并排）
py tools/balance/pop_cap.py                # 人口上限三条判据 + Keep 比价
py tools/balance/pop_cap.py --runner build-Release2/near.json   # 哪种资源咬得住
# 复现「金币剩几千」：让脚本守方够得到外环带（人类玩家会去拿，默认 40 格够不着）
... --macro --macro-gath-dist 75 --macro-gath 4

# 2026-09-04 新增的旗标
... --macro-gath-order <map|near|stone|gold>   # 先铺哪一种资源点（默认 map = 地图文件序）
... --pop-base <n> --pop-per-keep <n>          # 守方人口上限 = base + per×K（默认 8 2）
```

**跑长局找真正的陷落波**（守方补齐之后 20 波已经打不完）：

```bash
... --seeds 1 --max-waves 60 --max-ticks 400000 --macro --limit 3
```

⚠️ 一局 5–6 分钟，3 张图约 17 分钟。**跑之前先确认二进制是新的**——
`LNK1104` 就是上一次的 runner 还在跑、占着 exe（等它结束再编）。

## 6.1 构建：`ctest` 里有两条 135 秒的地图生成自检

`map_gen_self_test`（105 s）+ `map_gen_generate_smoke`（30 s）占了全量 ctest
的 87%。**没碰 `tools/map_gen/` 的时候可以跳掉**，一轮从 155 秒降到 53 秒：

```bash
ctest --test-dir build-Release2 -E "map_gen_self_test|map_gen_generate_smoke"
```

只改测试文件时更快的路径是**只编测试目标**（不重编 runner，也就不会被
它占着的 exe 挡住）：

```bash
cmake --build build-Release2 --target rts_tests
./build-Release2/tests/rts_tests.exe "[macro]" -s     # -s 打印 INFO
```

⚠️ **Catch2 的 `INFO` 是块作用域的**：写在 `{ }` 里，出了那个块就不会随失败
打印出来。我为此白瞎过一轮定位。

---

## 7. 未合的 PR 与它们的耦合（2026-09-04）

| PR | 内容 | 状态 |
|---|---|---|
| **#139** | 本轮全部（守方宏观层两版、两处死锁、门血 75%、建筑升级定价、斥候二元侦查、HUD 秒制、建造期 660、六份文档） | 待评审。**是合并顺序的头一个** |
| **#138** | 幽影窥使改判空中 | **我评审通过了**，实测合到 #139 HEAD 上 46/46 绿。托付我的三件：精灵重渲 ✅ 已做并推到该分支（`5db4c57`）；Flak 覆盖率核验 ⏳ 等 #139 落地；地图林子几何 ✅ 同意立案挂下轮 |
| **#140** | 侦查阶段提示（窥使入境横幅 / 斥候回报 / 召唤确认 + 阶段行二分 + Summon 护栏） | 已评。**有一处要改逻辑不只是解冲突**：它的「斥候回报」触发建在旧的「看见就算数」语义上，而 #139 改成了二元判定 ⇒ 要改读 `DemoBattle::scout_outcome() == Success`，并建议补一条「侦查失败」横幅 |

**合并顺序 #139 → #138 → #140**（队友定的，我同意）。#140 的 rebase 与解冲突
**队友 zhxxx233 已认领**，不用我们管。冲突面：`demo_driver.hpp/.cpp`（他的
Summon 护栏 vs 我的 `WaveTiming` 参数化）+ **新增 `render/src/main.cpp`**
（我改了 `draw_battle_hud` 的 map_id/tick→秒、`draw_intel_panel` 换了签名、
阶段行两边都在改同一个 `phase` 分支）。

**#139 落 main 之后要做的第一件事：通知 zhxxx233**，他等着动 #140。

---

## 8. 这一轮做完的事（给接手者对齐用）

- **守方宏观层重写**（两次）：从「只招弓手」补到按目标编成招兵 + 兵营 + 防空 +
  破口木栅 + 清野 + 过迷雾的敌情反应。情报口径限定在免费情报 + 迷雾内可见
- **斥候侦查改二元**：到达 → 掷死活 → 活着才给完整编成（组长定形状）
- **建筑升级定价** `∝ √B(L)`（stats/11 → stats/12，形状没变只改语义 ⇒ 必须进格）
- **一个真 bug**：堡垒被拆之后三个 cap 函数是**越界读**（`bld_at_[cell]` 是
  `uint16_t`，`0-1` → SIZE_MAX）。不崩、只返回随机数，没有任何测试会红
- **四条试玩反馈**：HUD 去掉 map_id、tick→秒、斥候判定后留反应时间、建造期 660
- **PR #138 的精灵重渲**

