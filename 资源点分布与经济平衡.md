# 资源点分布是否合理 + 经济接进实力模型后的平衡实验（2026-09-02/03）

> **定位。** 组长两问：现在的资源点分布合理不合理；两方平不平衡。并要求**先检查
> 上一版实力模型有没有把资源点分布算进去，再做实验**。本文按这个顺序：§1 检查
> （结论：没有，供给一侧被钉死成三个常数）、§2 分布实测、§3 实验、§4 判断与旋钮。
>
> 实验脚本 `tools/balance/economy_race.py`（读池图 + 复用 `siege_race` 的入口攻防，
> **只换经济、不换战斗**，两份结论才可比）。本轮**不动仿真代码、不动数值表、
> 不动地图**——`siege_race.py` 也一个字没改，它的表仍能逐行复算。

---

## 1. 先检查：上一版模型没有把资源分布算进去

`攻守实力模型与平衡分析.md` §1.6 的收入是三行常数：

```python
inc_stone = Bd['Quarry']['income_amount']                       #  9
inc_wood  = Bd['Lumber']['income_amount']                       # 14
inc_gold  = Bd['Mine']['income_amount'] + Bd['Keep']['income_amount']   # 11+2
stone_in  = inc_stone * periods * (1 + income_growth * (w - 2))
```

全文对 `unlock_wave`、`tier`、簇的引用数为 **0**（`grep` 实测）。于是四样东西没进模型：

| 没进去的 | 后果 |
|---|---|
| 采集建筑的**座数** | 收入被钉成「各一座」。而地图给了 53–84 个城外点，每点支持一座 |
| `tier` 产出倍率 | 城外 1.5×（`WorldInit::tier_income_permille` 占位）整个不存在 |
| 逐簇解禁表 | 供给的**时间形状**退化成一个手填的 `income_growth` 标量 |
| 采集建筑的造价 / 工时 / 工匠 / 人口上限 | 「铺开经济」的**代价**不存在，于是也没有投资期 |

**这不是遗漏一个参数，它反过来影响了那份模型的头号结论。** §3 的
「守方每门每波只建得起 0.85 座塔，从第 3 波起结构性追不上」里，那个 0.85 =
一座采石场的产出 ÷ 80。供给一侧换成地图的实际分布之后，这个数变成 **3.7–13.2**
（§3.1）。所以 §3 的**结论方向**要重估，不只是取值。

§4 那一行「收入每波 +15% ⇒ 陷落波零影响」**不能用来免掉这次检查**：它成立的理由
是「崩盘发生在收入生效之前」，而崩盘时刻本身是按被钉死的供给算出来的。本文 §3.2
的实验重新验了这一条——在现行等级曲线下它**仍然成立**，但那是重新验出来的，
不是原来那句话自动继承的。

---

## 2. 分布实测（12 张池图，重生成后的 170×170）

### 2.1 城内：3 个点，全部预置，收入恒定

12 张图**一张不差**：城内恰好 1 石 1 木 1 金，`unlock_wave` 全为 1，且
`buildings` 里开局就各预置了 `Quarry`/`Lumber`/`Mine` 一座踩在上面。

于是城内收入是**常数** 9 石 / 14 木 / 13 金（含 `Keep` 地板 2）每 5 秒，
**玩家在城内没有任何经济决策**——没有第四个点可建、也没有可升级的产量。

两处与文档不符，都属描述而非机制：

- CLAUDE.md「资源分布形态」写**「地图中心是资源密集区」**。3 个点不是密集区。
- CLAUDE.md「波次结构与数值曲线」写**「城内保底收入按线性增长」**。实测是恒定。
  **那条硬性曲线要求本身不受威胁**（恒定 < 超线性，拐点照样存在，且来得更早），
  受威胁的是「线性」这个说法——以及「修旧 / 建新 / 造兵」三方决策里的「建新」
  在城内无从发生。

### 2.2 城外：53–84 个点，1.5 倍产出，7–22 秒回本

| 量 | 实测（12 张，中位） |
|---|---|
| 城外点总数 | 70（53–84） |
| 其中石 | 23 |
| 簇数 | 15（12–18），每簇 4–5 点 |
| 解禁 | 第 2 波起，1 簇/波，第 13–19 波开完 |
| 内环带（距 keep ≤ 40） | 32 点，距离 11–36 |
| 外环带（距 keep ≥ 55） | 37 点，距离 59–82 |

一座采集建筑的回本期（自产自还，`income_period_ticks` = 100）：

| 建筑 | inner | outer |
|---|---|---|
| `Quarry` | 22.2 s | **14.8 s** |
| `Lumber` | 10.7 s | **7.1 s** |
| `Mine` | 18.2 s | **12.1 s** |

**外环带那 37 个点在当前工匠速度下是「名义可用」。** 工匠 0.08 格/tick，
到最近的外环点往返就要 **74–78 秒**，而一波只有 50–70 秒；且外环带
（59–82）在**集结点环之外**（集结点距 keep 45–54），工匠要穿过集结区。
所以真机里玩家吃得到的是内环带那 32 个，外环带是模型和任何不计风险的
算法才会去铺的东西（§3.3）。

### 2.3 一条做对了的：交错杂居

各簇混合比确实不同（同一张图上有 `{石1 金4}`、`{石4 金1}`、`{金1 木3}`），
所以「攻方选簇 = 选打哪一种资源」这条纹理是成立的。**这一项不用改。**

---

## 3. 实验

### 3.1 供给 vs 需求：内环带的形状其实是对的

需求取 §3 那张表（门前 6 名弓手时拦住 `Ram` 所需的塔数），供给取
「内环带已解禁的石点全铺、两座门平摊」：

| 波 | 内环已解禁点 | 其中石 | 石入/波 | 可建塔/门/波 | §3 需求 | 差 |
|---|---|---|---|---|---|---|
| 3 | 9 | 2.5 | 598 | 3.7 | 2 | +1.7 |
| 4 | 13 | 4.0 | 882 | 5.5 | 4 | +1.5 |
| 5 | 18 | 5.0 | 1071 | 6.7 | 8 | −1.3 |
| 6 | 22 | 7.0 | 1449 | 9.1 | 9 | +0.1 |
| 8 | 30 | 10.0 | 2016 | 12.6 | 11 | +1.6 |
| 10 | 32 | 10.5 | 2110 | 13.2 | 14 | −0.8 |
| 12 | 32 | 10.5 | 2110 | 13.2 | 16 | **−2.8** |

**这正是设计要的形状**：供给贴着需求走到第 10 波，内环带在第 10 波左右**开完
（32 点封顶）**，需求继续爬 ⇒ 从第 10–12 波起自然掉队。「拐点反复出现 + 最终必败」
由分布的**饱和**给出，而不是靠某个数值劝退。

对照上一版模型那个 0.85：**供给一侧差了一个数量级**，而这一栏正是 §3 结论的分母。

### 3.2 现行等级曲线下：经济来不及生效，结论不变

12 张池图逐张跑，`--waves 25`，中位陷落波：

| 情形 | 中位陷落波 |
|---|---|
| A 模型原样口径（只有城内三座、不铺城外） | 3 |
| B 铺城外、1 名工匠（现状 demo 守方脚本的能力） | 3 |
| C 铺城外、招到 4 名工匠 | 3 |
| D C + 弓手 60% 集中主攻门 | 3 |
| E C + 只铺内环带 | 3 |
| F C + 攻方分三成兵拆采集建筑 | 3 |
| G C + 攻方分五成 | 3 |
| H C + 先建塔后铺矿 | 3 |

**八种守方经济策略给出同一个陷落波。** 所以 §4 那条「中后期旋钮在早期赛跑修好
之前零影响」经这次独立重验**仍然成立**，且现在是用真实分布验的。

一处顺带查出来的：C/E/F/G 里有三张图掉到**第 2 波**，逐图核对后成因是
**两个条件同时成立**，缺一不掉：

| 图 | 预置塔 | R | 陷落波 |
|---|---|---|---|
| gen_01001000 / 01002000 / 01007000 | **2** | **10** | **2** |
| gen_01003000 / 01009000 | 2 | 9 | 3 |
| 其余七张 | 3 | 9 或 10 | 3 |

- **预置塔 2 座**：两门各摊 1 座，预置 3 座则各摊 1.5 座
- **R = 10**：脚本弓手均匀登墙，门边占 `13/(8R)`。R=9 时 3 名弓手里有 1 名
  落在门边，R=10 时是 0 名——周长 80 比 72 多出的那 8 格把最后一名弓手摊没了

第二条在模型里是一次取整，但它对应的是真效应：`日志.md` 09-02 那张「受攻面
每格墙摊几个人」的密度表说的就是这件事。**它同时给 §4「城区半径对陷落波零影响」
划了一个边界**——那一行是在 R=13 vs R=9、预置塔恒 3 的条件下测的；预置塔只有
2 座时，R 在 9 与 10 之间就能翻转陷落波。`地图生成器大改方案.md` §3.5 说
「预置塔数是难度量、不是装饰」，这里量出来了，而且它与 R 是耦合的。

### 3.3 把等级曲线这个压制项拿掉：城外经济会让守方过强

现行曲线下守方第 3 波就死，看不出经济的量级。换成 §4 里两个已知「守方不会
立刻死」的口径（「只买人」L≡1、以及 09-01 之前的旧曲线）再看：

| 情形 | 中位陷落波 | 末波塔数（中位） | 末波采集建筑（中位） |
|---|---|---|---|
| 只买人(L=1) + 不铺城外 | 守住 25 波 | 28 | 3 |
| 只买人(L=1) + 铺城外、4 工匠 | 8 | 5 | 23 |
| 旧曲线 + 不铺城外 | 19 | 23 | 3 |
| **旧曲线 + 铺城外、4 工匠** | **守住 25 波** | **697** | **67** |
| 旧曲线 + 铺城外 + 攻方分五成拆矿 | 守住 25 波 | 39 | 10 |

**「守方发展起来以后明显比攻方强很多」这条观察在这里被复现并量化了**：旧曲线下
铺开城外经济，第 20 波两门各 196 座塔、第 25 波合计 697 座。而同一条旧曲线不铺
城外时第 19 波就陷落。**差别整个来自城外那 60 多个点。**

三条必须一起认下的读法：

1. **那 697 里有模型 artifact。** 门守住而攻方又死不掉时 `fight_entry` 会跑到
   `t_end`（约 460 秒），一波白送 90 个入账周期。加一个 120 秒的入账上限重跑，
   第 20 波变成两门各 196 座（仍是 §3 需求的十几倍），中位陷落波变成 6–8。
   **量级结论不依赖这个 artifact，绝对数字依赖。** 顺带说明：这种长波在真机里
   **不完全是假的**——波次循环挂在「攻方存活数归零」上，`日志.md` 记过
   「第 2 波一万多 tick 不来」。
2. **「只买人 + 铺城外」比不铺更差（8 波 vs 守住）**，这是真的、也是好性质：
   铺矿要花石头、工匠要占人口上限，投资期里门前的塔与弓手都变少。
   **投资的脆弱窗口是设计想要的东西**，不是缺陷。
3. **攻方袭扰有效**：分五成兵去拆矿，末波采集建筑从 67 降到 10、塔从 697 降到 39。
   **但现行 demo 攻方脚本从不打经济**——`issue_actions` 的目标只有 `w_.keep_pos()`
   （实测全仓仅此一处），而「打哪一种资源」已由组长定为**留给 RL 涌现**。
   所以上表第四行（697 座塔）才是当前实现的真实形态，第五行是 RL 学会之后的形态。

### 3.4 反解：守住两门需要几个城外石点

判据取「第 w 波的累计石材刚好把 §3 的需求两门都建满」：

| 波 | §3 需求（两门） | 累计需石 | 需要的城外石点 | 地图已解禁的城外石点 |
|---|---|---|---|---|
| 5 | 16 | 1280 | 0.7 | 4 |
| 8 | 22 | 1760 | 0.5 | 11 |
| 10 | 28 | 2240 | 0.5 | 16 |
| 12 | 32 | 2560 | 0.5 | 20 |

**守住城门这件事只需要不到一个城外石点，地图给了 20 个（已解禁）、23 个（总数）。**
这个差距是**一到两个数量级**，是本文最锐利的一个数。

它与 §3.1 那张「内环带形状正好」不矛盾，两者算的是不同的东西：§3.1 问
「全铺内环带能建多少塔」，这里问「守住门最少要几个点」。合起来是：
**内环带的点数对「塔的供给曲线」是刚好的，对「守住门」是过剩的**——
也就是说，多出来的石材本该流去别处（堡垒等级、野战部队、买活），
而现在那些出口要么还没实现（买活），要么被别的东西压着（`Ram` 等级曲线）。

---

## 4. 结论：分布的四项判断

### 4.1 内环带（32 点、11–36 格、第 2–10 波解禁）——**合理，不要动**

供给曲线贴着塔需求曲线走、第 10 波饱和后自然掉队（§3.1）；簇的混合比不同，
「选簇 = 选资源」成立（§2.3）；离墙 1–26 格，工匠往返 20–60 秒，一波内铺得动。
**这一段是这次大改做对的部分。**

### 4.2 外环带（37 点、59–82 格）——**定位不成立，是本文查出的主要问题**

它现在既不是取舍、也不是留白，而是一个**「谁不计风险谁就白拿三倍收入」的口子**：

- 工匠往返 74–78 秒 > 一波波长，且要穿过集结点环 ⇒ **人类玩家实际吃不到**
- 而模型（以及任何不计风险的策略，包括将来的守方 RL 决策层）会去铺，
  一铺就是 §3.3 那个 697
- `地图生成器大改方案.md` §2.2 给它的设计理由是「后期扩张必须经过或绕过集结点环，
  让『强袭集结区通常得不偿失』成为真取舍」。**那个理由要成立，得先有人能走到那里**，
  而现在走过去的是一名 0.08 格/tick、120 血、无护送的工匠

三条候选，都不用改生成器的骨架：

1. **把外环带的点数砍到内环带的一半以下**（现在是 37 vs 32，比内环还多）。
   `outer_band_clusters` 现在是 [6,10] 且「远簇数 ≥ 近簇数」是强制抬齐的，
   那条强制正好把它顶成了多数——**先查那条约束的理由还成不成立**。
2. **给外环带一条护送的必要条件**：解禁波推到玩家真有野战部队之后
   （现在第 10 波起，而模型说第 10–12 波正是守方开始掉队的时候，等于
   「最需要它的时候拿不到」——这可能反而是对的，需要组里定）。
3. **承认它是留白**（大改方案 §2.2 的候选①），只放装饰地形与可破坏障碍。

### 4.3 回本期 7–22 秒——**太短，使「围哪一簇」不构成取舍**

CLAUDE.md 要的是「大散居 ⇒ 玩家不可能全部圈进城，**必须选**」。而一座外环
伐木场 7.1 秒回本、采石场 14.8 秒回本，且没有任何东西威胁它 ⇒ 正确答案永远是
「全都要」，不存在选。`数值设计` §11 早已量出内环 0.33–0.54 波回本并称之为
「无后果的 DoS」，**城外 1.5 倍倍率把它又缩短了三分之一**。
修法不在分布，在造价 / 产出比或在攻方袭扰，见 §4.5。

### 4.4 城内 3 点恒定——**机制无碍，但文档与「建新」这条决策要改一处**

拐点的存在性不受影响（§2.1）。要处理的是：CLAUDE.md 两句话（「资源密集区」、
「城内保底收入按线性增长」）与实测不符；以及城内没有任何经济决策，
「修旧 / 建新 / 造兵」的三方性在城内退化成两方。

### 4.5 两方平衡的判断

**当前实现是攻方远远过强，中位陷落波 3（八种守方经济策略无一改变它）。**
这与上一版模型的结论一致，成因也一致：等级曲线把 `Ram` 抬过塔的阈值。
**经济与资源分布不是当前的病因**，改分布对陷落波零影响。

**但分布是一个已经装好的雷。** 一旦 `Ram` 等级曲线按 §6 压平，
「守方发展起来后明显更强」会立刻回来，而这次的量级是 §3.3 那个 697 座塔。
所以顺序必须是：

1. 先压 `Ram` 等级曲线（§6 第一条，已是既有结论）
2. **同一轮里把外环带定位处理掉**（§4.2）——否则压完曲线立刻走到另一个极端，
   而那正是 §5.3「分配不是难度旋钮」那次教训的形状：两股相反的力必须一起看
3. 买活落地（§6.1 的金币出口），否则过剩的资源无处可去
4. 再谈解禁节奏与 `cluster_unlock_per_wave` 的取值

**另有一条要回填进 `攻守实力模型与平衡分析.md`**：该文 §4 与 §6 把「城区半径」
列为对陷落波零影响、不当难度旋钮。§3.2 给它划了边界——那是在预置塔恒 3 的
条件下测的，预置塔 2 座时 R 在 9 与 10 之间就翻转陷落波。**「R 不是难度旋钮」
要改成「R 在预置塔够多时不是难度旋钮」**，本轮只记，不改那份文档。

---

## 4.5 三种资源里只有石材是活的（2026-09-03 实测追加）

起因是一条试玩反馈：「堡垒陷落时金币还剩 2000 左右，攻方进攻时爆兵也爆不起来」，
外加组长自己的判断「当时把几个外金矿建起来了，可能是这个原因」——**那条判断是对的，
而且顺着它查出一个更大的**。

复现手段是给校准 runner 的逐波记录补上资源、人口与出兵建筑数
（此前**一个资源数都没有**，所以「金币冗余」这件事在 runner 的输出里完全不可见），
再跑一组 A/B：`--macro-gath-dist 40`（默认，工匠够不着外环带）vs `75`（吃得到）。

### (1) 外矿确实是金币冗余的开关

撑得最久那一局（`gen_01008000`，第 8 波陷落）：

| 波 | 金 起→末 | 木 起→末 | 石 起→末 | 人口/上限 |
|---|---|---|---|---|
| 3 | 358→34 | 386→908 | 153→27 | 9/10 |
| 4 | 34→**1720** | 908→2118 | 27→68 | **10/10** |
| 6 | 2671→2728 | 2869→3483 | 6→37 | **10/10** |
| 8 | 2959→**3387** | 4103→**4481** | 18→66 | **10/10** |

外矿一接上，金币一波内从 34 涨到 1720；而人口**从第 2 波起每一波都顶在 10/10**。
钱没处花，就堆着。组长看到的 2000 就是这个（本组最大 3387）。

**机制是「人口上限把金币的 sink 掐死」**，不是「收入太高」：金币只买兵，
而兵被 `cap(K)` 卡住（这一局 K 一次都没升，cap 恒 10）。这与
`波次预算曲线与堡垒等级曲线.md` §2.2 那条是同一件事的两端。

### (2) 但**木材在没有外矿时就已经堆了**，而它是个纯比例错误

`--macro-gath-dist 40`（完全不碰外环带）12 局：

| 资源 | 波末中位 | 波末最大 | 「这一波涨了」的波占比 |
|---|---|---|---|
| 石 | 42 | 198 | **22/51** |
| 木 | **413** | **3000** | **42/51** |
| 金 | 40 | 332 | 29/51 |

**木材在 82% 的波里净增长**，而石材只有 43%。成因是两个比例反着：

| | 石 | 木 | 石:木 |
|---|---|---|---|
| 采集建筑产出（每周期） | `Quarry` **9** | `Lumber` **14** | 0.64 : 1 |
| 十座建筑造价合计 | 430 | 325 | **1.32 : 1** |

⇒ **每单位时间拿到 1.56 份木材/石材，却只花得掉 0.76 份 ⇒ 木材以约 2 倍速率净堆积**，
一根都不浪费也一样。维修那条 sink 也小得可以忽略（`repair_wood_per_1000hp = 15`
⇒ 把一段满血 1200 的墙从 1 血修满只要 **18 木**）。

**这正是 `CLAUDE.md` 自己预言过的失效**：

> 「只让它管维修的话，开销被『维修成本显著低于重建』这条反死亡螺旋规则压住上限，
>  **木材永远花不完**，"这波该攒哪种"就不成其为决策」

那一段是为了论证「木材必须是所有永久建筑的**配料**」而写的。配料这条**已经落地**
（十座建筑都吃木），但**配比不够**：0.76 份木/石的消耗强度，撑不住 1.56 份木/石的产出。

### (3) 于是「三种资源 = 三条决策轴」现在只剩一条

| 资源 | 决策轴（设计） | 实测 |
|---|---|---|
| 石材 | 硬防御的上限 | **唯一真的紧的**（波末中位 42，且 57% 的波在净减少） |
| 木材 | 恢复速度与可维持性 | **死的**：82% 的波净增长，波末最多 3000–4481 |
| 金币 | 机动人力 | **被人口上限掐死**，拿到外矿后一路堆到 3387 |

`CLAUDE.md`「守方多资源」要求「每种资源对应一条**不同**的决策轴，且不可自由兑换」。
两条轴是死的时候，经济实际上是**单资源**的，而「这波该攒哪种」不成其为决策。

### (4) 三条修法方向（数值，归组里定）

按「改一处、影响面最小」排：

1. **木材：把产出/消耗两个比例掰回来。** 最省事的一处是 `Lumber` 的
   `income_amount`（14 → 与 `Quarry` 的 9 同阶甚至更低）；另一条是抬高建筑的木材
   配比（那更贴合「木材是配料」的原意，但要动十座）。**别去动维修单价**——
   「维修显著便宜于重建」是反死亡螺旋的规则，不是旋钮。
2. **金币：它的 sink 就是人口上限**，见 `波次预算曲线与堡垒等级曲线.md` §2.2。
   在 `K` 动起来之前，给金币加任何别的 sink 都是绕过病根。
   **注意「买活」那条尚未实现的机制正好是一个金币 sink**（`CLAUDE.md`
   「守方的一条升级轴」），它现在有了第二个存在理由。
3. **外环带那 37 个点**（本文 §4.2 早就标为「谁不计风险谁白拿三倍收入」的口子）
   现在有了实测后果：它把金币冗余从「小」变成「三千」。但**它不是病根**——
   在没有外矿的局里木材照堆不误。

## 4.6 「把外石矿优先建起来」实测：塔多了 45%，陷落波只挪 0.2（2026-09-04）

组长要求的实验。给守方脚本加了铺设顺序（`MacroParams::GatherOrder`，
默认仍是历史的地图文件序），跑四配置 × 12 张池图：

| 配置 | 陷落波中位 | 均值 | 波末塔数中位 | 波末石材中位 |
|---|---|---|---|---|
| a 地图序 / 够不着外环（基线） | 4.0 | 4.25 | 11 | 42 |
| b **石材优先** / 够不着外环 | **4.5** | 4.42 | **16** | 31 |
| c 地图序 / 够得到外环 | 4.0 | 4.42 | 15 | 43 |
| d **石材优先** / 够得到外环 | 4.0 | 4.42 | 16 | 48 |

**塔从 11 涨到 16（+45%），中位陷落波只从 4.0 挪到 4.5**（逐图 +3 / −2，
基本是噪声）。够得到外环带时石材优先**一点用都没有**（+1 / −1）。

### 「没效果」比「有效果」更值得追：多出来的塔不在打得着的地方

于是给 runner 补了一个埋点：**破口那一刻，全场塔数 vs 切比雪夫 ≤ `Tower.range`(7)
以内的塔数**（此前逐波记录里只有塔的**总数**，与战斗发生处没有任何关系）。

| 配置 | 破口样本 | 全场塔中位 | **打得到破口的塔** 中位 | 覆盖率 |
|---|---|---|---|---|
| 基线 | 33 | 3.0 | **1.0** | 25% |
| 石材优先 | 34 | 4.5 | **1.0** | **20%** |

**中位只有 1 座塔打得到破口，而且相当多的波是 0。** 塔数涨了 45%，
这个数**一点没动**——多出来的石材全变成了站在另一面的塔。

### 成因：主攻方向每波轮换，而塔是永久的

`demo_driver.cpp:368`：`main_spawn = wave % 集结点数`。集结点 3–5 个 ⇒
**一座永久的塔平均只有 1/n 的波次正对着主攻**。上一波为了挡 A 方向建的塔，
这一波攻方从 B 来，它们就全部站在场外看。

这解释了为什么「多给石材」几乎无效：石材的**唯一大宗出口**是塔，而塔的
边际收益被方向轮换稀释到 1/n。

**但这不是一个纯 bug，要分开两半看**：

- **一半是设计要的**。`CLAUDE.md`：「防御建筑造价高到无法覆盖所有墙段」
  「建筑定义防御的形状，**部队负责补形状里的洞**」。塔覆盖不全是**故意的**，
  机动部队才是补口的手段——所以「1 座塔覆盖破口」本身不是失效。
- **另一半是占位实现的副作用**。`wave % n` 是**确定性轮询**，那是所有方向
  策略里对永久防御**最不利**的一个：它保证你上一波学到的东西这一波一定用不上。
  而 `CLAUDE.md` 要的是攻方**读侦查结果再选方向**（RL 宏观层的 bandit 决策），
  一个真实的攻方（RL 或人）会**重复奏效的方向**，不会强制轮换。
  该函数的注释只说了「不掷点是为了 demo 的确定性」，**没有**说轮换是设计。

⇒ **在攻方方向策略定下来之前，「守方该在防御工事上投多少石材」这个配平问题
问不出答案**：现在测到的边际收益是「面对一个强制轮询的攻方」的边际收益，
而那是下界，不是设计意图。这条与 `波次分段与侦查时序.md`（侦查没有消费端、
方向不读情报）是同一件事的两端。

### 顺带：石材优先这个顺序本身该不该成为默认

倾向**该**，但不是因为它更强（实测几乎没变强），而是因为**历史默认「地图文件序」
既不看种类也不看距离，纯属没想过**——它会先去铺一个更远、更没用的点。
本轮**没有改默认**（改了旧测量就不可复现），旋钮是
`MacroParams::gather_order` / `--macro-gath-order map|near|stone`。

## 5. 本文的局限

- 沿用 `siege_race` §1.8 的全部未建模项；新增四项偏守方的乐观（工匠不被点杀、
  采集建筑不挡路不供视野、`Ranger` 不护矿、外环带只算距离不算穿越集结区的风险）
- 需求一侧直接引用 §3 那张塔数表，而那张表本身**尚未经无渲染 runner 校准**
  （§7），所以 §3.1 与 §3.4 的绝对数只作相对比较
- 守方的铺矿策略是「就近全铺、石材优先」一种；真人与 RL 都可能不同
- 攻方袭扰建模成「分一支去拆 500 hp 建筑」，没有算它路上被 `Ranger` 拦截
- 长波入账那个 artifact 见 §3.3 第 1 条；`--cap` 给了敏感性，两组数都列了
