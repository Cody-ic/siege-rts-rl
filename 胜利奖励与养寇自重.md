# 胜利奖励与终局测量（#147 的实现跟进）

本轮由 zhxxx233 接手实现，基于 #146。目标是让训练可以直接比较奖励方案，
并获得逐局胜负证据。此文替代 #147 原提案中的过强推论；不表示小组已表决
取消消耗战，也不把新模式的可训练性当作已验证的收敛结果。

## 已确认的问题

Keep 不可重建，资源成本为零是合法数值；`bld_value` 因而不能兼任胜利奖励。
2400 HP × 0.004 = 9.6 是打完整座堡垒的未折扣伤害项，不是最后一击奖励。
仅凭总步数/完成局数≈400，无法证明零胜率或策略故意避战：分子还包含未完成局
和课程重置片段。现在直接读取原生环境的终局原因，不再从均长反推。

## 已实现的接口与边界

- `BatchedEnv.episode_ends`：每局 `Running` / `KeepDestroyed` / `Timeout`，
  枚举由绑定导出；训练在 reset 前读取。一个决策步末既达到时限又无 Keep，按胜利计。
- 时限不整除决策间隔时，只推进剩余 tick。已经终局的环境冻结，重复 step
  保持 done、原因及世界状态，不追加战果；reset_one 清除原因并恢复完整时限。
- PPO 独立累计胜利、超时、升档中断，均长只使用完成局的步数；刚 reset、尚未
  推进一步的局不计中断。训练结束时仍在进行的片段另记在检查点元数据中。
- “打到建筑”的课程指标保留并明确标注；新增胜率窗口只包含完整终局批次。
  训练窗口仍是滚动策略统计，冻结评估见下。

## 两个可直接运行的奖励模式

默认 `--reward-mode economic --win-reward 2000`：原有战果权重 + 胜利奖励 +
#146 的 PBRS。保留项目关于消耗战和经济破坏的约定。2000 是队友建议的有限
起始超参，可通过命令行修改；不是“必然及时获胜”的数学下界，也不能保证不刷分。

对照 `--reward-mode victory --win-reward 1`：只有胜利任务奖励与相同 PBRS，
所有经济、伤害、击杀、损失 tally 都只作诊断，不参与此模式的任务奖励。
这不是 A 的槽位位图，也不是 B 的巨大 W；没有实体槽位复用、空格新建或
重复伤害绕过去重的问题，因为这些项在这个模式里均不付任务收益。

同一初始状态、有限完整 episode、终局势归零、相同 γ 下，此模式折扣回报为
`γ^(T-1) * W * won - 0.02 * Phi(initial)`。因此确定性可比轨迹中，重复拆建
不能增加回报，早胜优于晚胜，有胜优于超时。**这不保证 PPO 找得到胜利轨迹**；
随机环境中的优化目标是折扣胜利期望，仍会权衡胜率与时间，不是严格的胜率优先排序。
该模式会舍弃“本波未破城但已造成战略损失”的直接任务收益，是否采用它作为
最终玩法训练目标仍由小组评估。权重必须有限且为正；不使用正无穷。

## 为什么不直接实现原 A/B

实体槽位会复用，完整句柄重建又会换代；地图格则还需定义换类型与新建规则。
仅去重 bld_value、继续奖励重复伤害，不能声称回路已消失。
初始地图总值也不涵盖空格新建。若继续开发 A，必须先定义这些契约。

W 超过静态总额也不保证尽快结束：γ=.99 时，8000 < 110+.99×8000。
这只是推导反例，不声称当前地图必然有该一步路径。恒定流量的折扣估算不等于
实测轨迹折扣值，库存和初始建筑也使收入率不构成逐步奖励上界。
优势归一化对整个向量统一缩放不变；仅改胜利奖励通常不属于统一缩放。
单离群、其余相同的特例不能推出策略梯度占比固定，更不能推出其它样本白算。

## 运行与验收

在仓库根目录、已构建本分支绑定的环境执行（PYTHONPATH 指向绑定产物目录）：

```sh
python train/ppo.py --reward-mode economic --win-reward 2000 --total-steps 200000
python train/ppo.py --reward-mode victory --win-reward 1 --total-steps 200000
python train/evaluate.py --checkpoint train/ppo_attacker.pt --episodes 32 --seed 100001 --output train/evaluation.json
```

两次训练会写同一默认检查点路径，比较前先分别归档 `.pt` 与同名 `.json`。
JSON 记录奖励模式、W、γ、PBRS 权重、步数和终局计数。
冻结评估使用 argmax、无优化器、无课程升档；以 episode 编号采样固定种子，
输出逐局原因/步数、实际胜率及模型/地图/数值表 SHA256。默认满距离，
可用 `--frac` 单独评估各课程档位，不把伤害成功率冒充胜率。

自动回归覆盖击破与超时分类、同一步胜利优先、终局冻结、重置、非整除时限，
以及立即赢/延迟赢/重复战果/超时的回报比较；真实 PPO 冒烟覆盖两种模式、
权重更新、完成局统计、升档顺序、冻结评估与不同批大小的结果对齐。
此轮未接入真实守方脚本，仍是现有训练世界；接守方和 GPU 长跑继续独立验收。
