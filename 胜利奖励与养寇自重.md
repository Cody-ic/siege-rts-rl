# 胜利奖励与终局测量

> **本文有两半，作者不同，都别删。**
>
> | | 谁 | 是什么 |
> |---|---|---|
> | **§A 实现与边界** | zhxxx233（PR #148） | **权威**。两个奖励模式、终局分类、冻结评估 |
> | **§B 问题识别与订正** | Cody-ic（PR #147） | 病因、`0/32` 证据，以及**我初版四条推论的逐条订正** |
>
> §B 初版提的 A/B 两案**已作废**，不要再照它们实现——为什么错见 §B.4。

---

# §A 实现与边界（PR #148）
本轮由 zhxxx233 接手实现，基于 #146。目标是让训练可以直接比较奖励方案，
并获得逐局胜负证据。此文替代 #147 原提案中的过强推论；不表示小组已表决
取消消耗战，也不把新模式的可训练性当作已验证的收敛结果。

## 已确认的问题

Keep 不可重建，资源成本为零是合法数值；`bld_value` 因而不能兼任胜利奖励。
2400 HP × 0.004 = 9.6 是打完整座堡垒的未折扣伤害项，不是最后一击奖励。
仅凭总步数/完成局数≈400，无法证明零胜率或策略故意避战：分子还包含未完成局
和课程重置片段。现在直接读取原生环境的终局原因，不再从均长反推。

## 已实现的接口与边界

- `BatchedEnv.episode_ends`：每局 `Running` / `KeepDestroyed` / `Timeout`，
  枚举由绑定导出；训练在 reset 前读取。一个决策步末既达到时限又无 Keep，按胜利计。
- 时限不整除决策间隔时，只推进剩余 tick。已经终局的环境冻结，重复 step
  保持 done、原因及世界状态，不追加战果；reset_one 清除原因并恢复完整时限。
- PPO 独立累计胜利、超时、升档中断，均长只使用完成局的步数；刚 reset、尚未
  推进一步的局不计中断。训练结束时仍在进行的片段另记在检查点元数据中。
- “打到建筑”的课程指标保留并明确标注；新增胜率窗口只包含完整终局批次。
  训练窗口仍是滚动策略统计，冻结评估见下。

## 两个可直接运行的奖励模式

默认 `--reward-mode economic --win-reward 2000`：原有战果权重 + 胜利奖励 +
#146 的 PBRS。保留项目关于消耗战和经济破坏的约定。2000 是队友建议的有限
起始超参，可通过命令行修改；不是“必然及时获胜”的数学下界，也不能保证不刷分。

对照 `--reward-mode victory --win-reward 1`：只有胜利任务奖励与相同 PBRS，
所有经济、伤害、击杀、损失 tally 都只作诊断，不参与此模式的任务奖励。
这不是 A 的槽位位图，也不是 B 的巨大 W；没有实体槽位复用、空格新建或
重复伤害绕过去重的问题，因为这些项在这个模式里均不付任务收益。

同一初始状态、有限完整 episode、终局势归零、相同 γ 下，此模式折扣回报为
`γ^(T-1) * W * won - 0.02 * Phi(initial)`。因此确定性可比轨迹中，重复拆建
不能增加回报，早胜优于晚胜，有胜优于超时。**这不保证 PPO 找得到胜利轨迹**；
随机环境中的优化目标是折扣胜利期望，仍会权衡胜率与时间，不是严格的胜率优先排序。
该模式会舍弃“本波未破城但已造成战略损失”的直接任务收益，是否采用它作为
最终玩法训练目标仍由小组评估。权重必须有限且为正；不使用正无穷。

## 为什么不直接实现原 A/B

实体槽位会复用，完整句柄重建又会换代；地图格则还需定义换类型与新建规则。
仅去重 bld_value、继续奖励重复伤害，不能声称回路已消失。
初始地图总值也不涵盖空格新建。若继续开发 A，必须先定义这些契约。

W 超过静态总额也不保证尽快结束：γ=.99 时，8000 < 110+.99×8000。
这只是推导反例，不声称当前地图必然有该一步路径。恒定流量的折扣估算不等于
实测轨迹折扣值，库存和初始建筑也使收入率不构成逐步奖励上界。
优势归一化对整个向量统一缩放不变；仅改胜利奖励通常不属于统一缩放。
单离群、其余相同的特例不能推出策略梯度占比固定，更不能推出其它样本白算。

## 运行与验收

在仓库根目录、已构建本分支绑定的环境执行（PYTHONPATH 指向绑定产物目录）：

```sh
python train/ppo.py --reward-mode economic --win-reward 2000 --total-steps 200000
python train/ppo.py --reward-mode victory --win-reward 1 --total-steps 200000
python train/evaluate.py --checkpoint train/ppo_attacker.pt --episodes 32 --seed 100001 --output train/evaluation.json
```

两次训练会写同一默认检查点路径，比较前先分别归档 `.pt` 与同名 `.json`。
JSON 记录奖励模式、W、γ、PBRS 权重、步数和终局计数。
冻结评估使用 argmax、无优化器、无课程升档；以 episode 编号采样固定种子，
输出逐局原因/步数、实际胜率及模型/地图/数值表 SHA256。默认满距离，
可用 `--frac` 单独评估各课程档位，不把伤害成功率冒充胜率。

自动回归覆盖击破与超时分类、同一步胜利优先、终局冻结、重置、非整除时限，
以及立即赢/延迟赢/重复战果/超时的回报比较；真实 PPO 冒烟覆盖两种模式、
权重更新、完成局统计、升档顺序、冻结评估与不同批大小的结果对齐。
此轮未接入真实守方脚本，仍是现有训练世界；接守方和 GPU 长跑继续独立验收。

---

# §B 问题识别与我四条推论的订正（PR #147）

> **§A 是权威。** 本节只剩两件事：病因与证据（B.1–B.3），以及**我初版给的
> 四条推论全是错的**（B.4）——逐条留着，因为四条各自的错法不同，而
> **错法比结论有用**。

## B.1 病因：一个在别处正确的公式被复用到了终局奖励

摧毁建筑的奖励在 `rts_core/src/mechanics.cpp` 的 `deal_damage` 里算：

```cpp
const BldStats& bs = stats_.of(b_type_[t]);
tl.bld_value += bs.cost_stone + bs.cost_wood;
```

**这个公式在它原本的用途上完全正确**，`CLAUDE.md` 还专门论证过它是
「摧毁建筑的即时奖励 = 该建筑的重建成本……**有原则的推导而非手工试凑**」。

而 `Keep` 的两项造价**都是 0**，且那是**结构性的**、不是漏填——堡垒不可
重建，`game/data/stats_placeholder.json` 的 `_note_7` 明写「Keep 自己的 cost
都是 0（不可再建，结构）」。于是：

| 项 | 值 |
|---|---|
| `bld_value` | `0 + 0` = **0** |
| `blds_destroyed` | 权重是 **0**（「已由 bld_value 表达，别重复计一次」） |
| `dmg_to_blds` | 2400 血 × 0.004 = **9.6** |

**对照：拆一座箭楼（80 石 + 30 木）= 110 分。**

> ⚠️ 那 9.6 是**打完整座堡垒血量的累计伤害项**，不是「最后一击的奖励」
> ——§A 订正的措辞，比我原文准确。

## B.2 而且赢是被惩罚的

`done` 的判据含 `keep_alive` ⇒ **打掉堡垒终止 episode**，而继续拆墙拆塔还在
给分 ⇒ 提前赢等于主动放弃后面若干步的收益。

**这一条不依赖任何实测数字**：终止一个还在产出正奖励的 episode，本身就是
一笔机会成本。

## B.3 证据：冻结策略 0/32 胜

用 §A 的 `train/evaluate.py` 跑归档的 40M 权重（服务器 GPU，冻结 argmax、
无课程升档、无优化器）：

```
胜利 0 / 超时 32，冻结策略胜率 0.0%
步数中位 400.0（32 局全部 end=timeout）
sha256: checkpoint 00db577b… / map 60fe9df8… / stats e59a22dd…
```

而同一份权重一局能拆掉几千值的建筑、堡垒只有 2400 血 ⇒ **不是打不动，
是它学会了别去打。**

⇒ **`配平工作交接.md` §2.14.3c 那轮的回报 320 是「学会了别赢」**。拆建筑
那一半仍然是真的，但它与胜负无关。

> 带三份指纹这一点是 §A 做对的：以后引这个数不会引错权重或地图。

## B.4 我初版那四条推论，全部作废

### B.4.1 ☠️「平均每局 401.3 步」是无效推断，而且**自相矛盾**

我初版写：「156,288 步 ÷ 389.5 局 = 401.3，而超时上界是 400 ⇒ 几乎每局都
跑到超时」。§A 指出分子还含未完成局与课程重置片段。而做一次一致性检查
就看得出它坏了：

```
401.3 > 400 = 超时上界
```

**均长在数学上不可能超过上界** ⇒ 那个数本身就在报告分母漏了东西
（4 次升档 × 256 局 = 1024 个未计数片段）。

**教训不是「我算错了」，是我看到一个恰好符合预期的读数（401.3 ≈ 400）
就直接下了结论，没做这个一分钟的自查。**

### B.4.2 ☠️ A 案（按槽位去重）实现不了，而且方向错

我初版的落地形状是「位图记本局已付过的槽位，付款前查一次」。三条硬伤：

1. **槽位会复用。** `SlotPool::release()` 是 LIFO 回收 + 代数推进
   （`world.hpp`）⇒ 守方在别处新建一座建筑可能复用到同一槽位，攻方拆掉它
   **拿不到分**。那不是防刷，是**漏付**。
2. 句柄重建会换代 ⇒ 拿句柄当去重键同样失效。
3. 改按地图格记也不够：还要定义「同格换建筑类型」与「空格新建」的规则。

**而更根本的一条是 §A 指出的**：我只去重 `bld_value`，`dmg_to_blds`
**照旧重复付** ⇒ **回路没有消失，只是变窄了**。我引「结构封死，而非数值
劝退」这条准则，却给了一个只把回路缩小的方案。

⇒ **§A 的 `victory` 模式才是对的形状**：把**所有可重复项**一起排除，于是
回报是 `γ^(T-1)·W·won − 0.02·Φ(initial)` ⇒ 重复拆建不能增加回报、早胜优于
晚胜、有胜优于超时，**三条都是推出来的、不靠调数**。

### B.4.3 ☠️ B 案的界（`W > 全图可拆总值`）不成立

我初版写「W > 3,810 ⇒ 赢严格优于拆到极限」。**§A 的反例**：

```
现在就赢:              8000
先拆一座塔再赢: 110 + 0.99×8000 = 8030   ⇒ 更大
```

我那句只在**同一时刻**的二选一下成立，而 γ 折扣下「先拆一下再赢」是第三个
选项，**我没把它放进比较集**。

### B.4.4 ☠️ 优势归一化那个反例是**退化用例**的产物

我初版用一张表说「胜利奖励占 50% 梯度质量，2000 与一百万一模一样」。
**那张表的其余样本全部恰好等于 100（方差为 0）**，而真实分布下：

| W | 离群占 \|z\| 质量（其余 std ≈ 50） |
|---|---|
| 500 | 0.2% |
| 2,000 | 0.7% |
| 8,000 | 2.8% |
| 1,000,000 | **50.0%** |

**2000 与一百万差 70 倍，不是「一模一样」。**

**但「不用正无穷」这个结论仍然成立，只是理由换了**：不是「大小无关」，而是
极大的 `W` 会让 z **饱和到 √(n−1)** 并吃掉一半梯度质量；加上 critic 那半
（`vloss ∝ W²`，一百万时 5e11，配 `max_grad_norm = 0.5` ⇒ 整步被裁成只有
方向）——后者未被反驳。§A 也明写「不使用正无穷」。

### B.4.5 顺带一处口径错误

日志里那个 19,840 是**一个 rollout 里 256 局的总和**，不是一局。除回去是
77.5/局/rollout × 6.25 = **一局约 484**。我在 #145 的评论里读成「一局能刷
19,840」，**高报了 41 倍**。趋势结论（28 倍增长、两态分布）不受影响，
绝对值别引。

## B.5 这一批缺陷的共同形态

`cost_stone + cost_wood` 在它**原本的用途**上完全正确，被复用到**终局奖励**
时对**唯一真正重要的那座建筑**静静地给出 0，而**没有任何测试会红**——
因为那个 0 在它自己的语境里是对的。

它与 2026-09-06/07 那一批同类：

| 缺陷 | 坏的是什么 |
|---|---|
| 吞吐口径 | 拿仿真单跑速度当训练吞吐（差 60 倍） |
| 升档判据读总奖励 | shaping 一落地就把门废了 |
| 终局同步 / 窗口按 env 序截尾 | 「最近 N 局」不是 N 次独立采样 |
| 陪练是一座空城 | `enemy_*` 十万局零梯度 |
| **本文** | **打赢得 0 分** |

**五个都不会让任何测试变红，五个都得先加观测手段才看得见。** 而本文自己
又贡献了第六个变种：**我的诊断方法也坏了**（B.4.1 那个 401.3）。所以那条
纪律要补一句——

> **在 RL 上花时间之前，先问「我现在给的分到底在奖励什么」；
> 而在报出一个结论之前，先问「这个读数自己一致吗」。**
