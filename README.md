# siege-rts-rl

C++ 程序设计课程设计。**不对称波次生存 RTS**：玩家守城（建墙、造防御建筑、经营经济、
指挥部队），进攻方由**强化学习**驱动，一波一波来袭，无尽模式。

形态接近 *They Are Billions*，差异化卖点是**攻方由 RL 驱动而非脚本** ——
它会侦查、会佯攻、会挑你防线的薄弱段打，也会读你的防空布局决定要不要派空军。

> **本文只做导航与协作约定，不放设计决策。**
> 设计决策全部在 `CLAUDE.md`，那是唯一的设计契约。本文若和它冲突，以 `CLAUDE.md` 为准。
> 这条是刻意的：本项目吃过"同一件事写在两处、然后漂移"的亏。

---

## 仓库现在是什么状态

**设计文档相当完整；代码有了确定性地基、第一批接口契约、以及一个能跑的前端。仿真本体仍是空的。**

| | 状态 |
|---|---|
| 设计契约（`CLAUDE.md`） | ✅ 已定稿多轮，是四人对齐的依据 |
| 交付要求分析 | ✅ 完整 |
| 地图与场景规范 | ✅ 完整 |
| 精灵素材流水线 | ✅ 跑通，35 实体（11 单位 + 11 建筑 + 13 地形/景物）× 4 朝向，状态逐实体（`idle`/`move`/`attack`/`work`）= **476 张**成品已入库（20 MB）。**仍缺攻击表现的只有 `Tower`**（全是单网格塔块、无可动部件，它的攻击本就是垛口上射出的箭）；**在途弹丸的精灵也未做**，因为四个量化朝向表达不了连续的飞行方向，那是契约决定、不是数值。两者都留给 `render/`。**清单是三种（箭 / 影矢 / 狙击弹），不是四种**——`Ram` 是贴身撞锤、没有弹丸（#68 定案，本栏原写「带弧线的弹道」已随之撤掉：三种真有弹丸的都是直射）。见 `tools/sprite_gen/README.md` §8.1 / §8.2 |
| 守方 AI 与协同演化 | ✅ **结构已定**（#15 合入）：守方 RL **只在决策层**，单兵是参数化脚本。4 项细节待定 |
| 演示与可视化清单 | ✅ **清单已定**（#16 合入）：**演示**只做双视图 + `AI vs AI`；显著性图与胜率矩阵改为只用于分析。前端预算**基线已给**（见「未定」栏） |
| 无尽模式与地形分层 | ✅ **已定案并落地**（#23 合入）：守方一条升级轴（堡垒等级）、破坏速率随等级 + 墙可升级（成对）、2.1 改用森林带、资源点随波数解禁、无尽模式看中位存活波数。结论已进 `CLAUDE.md` 与 `地图与场景设计.md`；`§1.4`（等级提升哪几个数值）只定了归口与判据 |
| **`rts_core` 公开接口** | ✅ **全部已定。** 花名册与地形枚举、战术动作、玩家/宏观命令、观测通道注册表、`World` 数据布局、`submit`/`advance`、`WorldView`、迷雾三态、动作与命令掩码、状态哈希、**回放格式与骨架**（`rts_core 接口契约.md` §1）。剩下的是**机制**（1c）与两个下游模块（`obs_macro`、`BatchedEnv`） |
| 前端 | ✅ 能开窗看地图：raylib、精灵图集、等距投影、相机、中文文字、格点选、截图模式（#40 / #42 / #43）。**画实体现在不再被接口卡住**——`WorldView` 已经给出连续数组 |
| 代码骨架与 CMake | ✅ **双工具链均已实测**：Windows/MSVC 与 Linux/GCC 各跑过 Release + Debug，全绿、零警告（见 `训练服务器环境.md` 第 5 节）。**契约那批的 GCC 缺口已补**（#63：9 处 `-Werror=dangling-reference`，全在 `map_loader.cpp`、GCC 13 误报，`where` 取 `string_view` 一处签名修掉）。**GCC 侧验到的最新点是 `aaca89d`**（#67 上，Cody-ic 跑的：Release、0 error 0 warning、28/28）；此后 `main` 前进的都只是注释与 markdown。**那个 28 不是本机的 35**——差的 7 条全是 `render_*`，服务器上默认不配置 `render/`，**这是设计内的、不是遗漏**（不变量 2、3 在构建侧的落地），代价是 `render/` 的 C++ 永远只有 MSVC 一套工具链把关。逐条见 `训练服务器环境.md` 第 5 节 |
| 兵种多态层 | ✅ **已定并落地**：`UnitBehavior` 基类 + 11 个兵种派生类（`rts_core/include/rts/unit_behavior.hpp`），三条正交轴 + 两条结构性判定，**一个数值都不放**。中途有一版改成逐单位继承（#65，823 行）并**已撤回**——判据与那次曲折见 `创新点与答辩讲法.md` 2.1，它是答辩「难点与解决方法」那一页 |
| `tools/map_gen/` | 🟡 **进行中（@omg321-975）** —— 地图 I/O 与格子原语（#31）、第 8 节校验器 **22 条已实现 15 条**都已合入；**生成器（第 9 节）未开工**；**校验器真的读一张真图**（`game/testdata/fixture_min.json`，即 C++ 侧 `MapLoader` 那张，由 ctest `map_gen_validate_fixture` 钉住）。#29 那三条空缺**已全部答掉**（第 14 条取切比雪夫；「城圈」与第 7 条随 2.1 定案为森林带一并解决）。**剩 3 条阻塞**：第 9 条要城区（2.1 定案反把它变难了——城区 mask 本来由候选乙带来，乙已作废）、第 18 条缺解禁波数字段、第 20 条等生成器。第 19 条随 §6 表决解开、连同新加的第 22 条一并实现 |
| 全部数值 | ❌ 一律待定，见 `CLAUDE.md`「关于数值」 |

`rts_core/` 现在有确定性地基（可播种 PRNG、状态哈希、三组实体句柄）、
枚举与观测注册表（`roster.hpp` / `terrain.hpp` / `action.hpp` / `command.hpp` /
`obs.hpp`）、**世界状态那一层**（`world.hpp` / `world_view.hpp` / `fog.hpp`），
以及**确定性回放**（`replay.hpp`：格式 + 录制器 + 比对）。

**但机制一行都还没有**：战斗、寻路、视野、经济全在 1c。`World` 的分界是一条
明确的线——**凡是需要一个待标定数值才能推进的都没做**，逐项清单见
`rts_core 接口契约.md` §1.1。其中七种命令被**记账**而不是静默丢弃
（`deferred_command_count()`），所以「本版没做这一类」是可以被断言的事实。

回放只存**外生输入**（`submit` / `submit_actions`），不存机制产物
（`spawn_unit` / `kill_unit` 之类）。**这条界线是回放有没有用的分水岭**：
若把 `kill_unit` 也记进流里，重放时就照着流把那个单位杀掉，于是即便战斗解算
算出完全不同的结果，回放照样对得上——测的就变成了它自己的日志。理由与
字节布局见 `rts_core 接口契约.md` §4.6。

**两份原本待议的提案已合入并定稿**（#15 / #16），它们确实改变了范围，加入前请读：

- **`守方AI与协同演化.md`** —— 训练时守方也由 RL 控制，但**只在决策层**：攻方 RL
  控制每一个单位，守方 RL 只下玩家会下的那些指令，**单兵是参数化脚本**。
  合入时改了原稿一处结构性主张（原稿主张守方镜像攻方分两层），
  第 2 / 2.5 节讲清了为什么脚本层不是临时凑合
- **`演示与可视化清单.md`** —— **进演示的只有**「双视图」与「`AI vs AI`」；
  显著性热力图与非传递性环矩阵**改为只用于分析与报告**（落 `tools/`，不占前端预算）。
  「不进演示」不等于「不做」——前者兼任 RL 调试工具，后者是已定的核心评估指标。
  `render/` 的 5–8 人日预算仍不含进演示的那两项

### 已知但尚未处理的问题

| 问题 | 位置 |
|---|---|
| 占位产图路线与 3D 路线的元数据 schema 与文件名都已分叉，「无 Blender 也能跑前端」实际已失效。**去留待小组点头** | `地图与场景设计.md` 7.2 |
| `tools/sprite_gen/README.md` 顶部「两条路线产物格式相同」这句仍是错的 | 同上 |
| 服务器到 `github.com` 的连通性**间歇性中断**（`codeload` 与 `pypi` 同期正常），FetchContent 因此卡过一次。已加兜底 URL 与 `TIMEOUT`，但**根因在网络、不在仓库** | `训练服务器环境.md` 第 3、5 节 |
| 交付打包需按 `assets.json` 过滤 `models/`（103 个模型仅 38 个被引用） | `日志.md` 遗留问题 |
| **城区无法从地图现有字段推导** —— 一条不闭合的曲线不把平面分成内外，而第 8 条强制要求城圈留缺口。更尖锐的形式：`derive_city_area()` **当且仅当**在第 8 条该判失败的图上推导成功。**现在只挡校验器第 9 条**（「正面」的定义依赖它）——第 7 条已随 2.1 定案改用森林带、判据不经过城区，第 8 条早已换判据实现 | `tools/map_gen/README.md`、`地图与场景设计.md` 8.1 |

#19 修掉了原先四条精灵问题里的三条（`tile` 尺度差 2√2 倍、缺地形精灵、城墙占地不足一格），
只剩上面第一条。详情与「留下的规矩」见 `地图与场景设计.md` 第 7 节。

## 先读什么

按这个顺序读，不要一上来就读 `CLAUDE.md` 全文：

| 顺序 | 文件 | 是什么 | 能改吗 |
|:-:|---|---|---|
| 1 | 本文 | 导航与协作约定 | 可改 |
| 2 | `课程设计最终交付项.md` | **老师原文** | ❌ **一个字都不要动** |
| 3 | `交付要求的工程影响.md` | 我们对交付要求的应对与推断 | 可改，但要区分"原文规定的"与"我们推断的" |
| 4 | `CLAUDE.md` | **设计契约。四人对齐的依据** | 可改，但见下面「改 CLAUDE.md 的规矩」 |
| 5 | `地图与场景设计.md` | 地图数据格式与场景结构约束 | 可改，改字段定义前先讨论 |
| 6 | `日志.md` | 上机日志，**答辩「人员分工」页的直接依据** | **只改自己那一行** |
| 7 | `tools/sprite_gen/README.md` | 精灵流水线用法与素材坑 | 可改 |
| 8 | `训练服务器环境.md` | GPU 服务器的实测配置、环境与连法 | 可改，**改前请实测**——那台机器是共享的 |
| 9 | `守方AI与协同演化.md` | 训练时守方也是 RL（**只在决策层**），含脚本执行层的设计判据 | 可改，结构性主张改动前先讨论 |
| 10 | `演示与可视化清单.md` | 演示点清单（做两项），同时是 `render/` 的需求来源 | 可改 |
| 11 | `rts_core 接口契约.md` | 接口的**决定与理由**（形状看头文件）。含 PR #23 评论串里辩定的十三条 | 可改，推翻某条要在 PR 里说明 |
| 12 | `无尽模式与地形分层.md` | 无尽模式的目标、守方升级轴、2.1 森林带、资源解禁的**完整论证**。**结论已进 `CLAUDE.md`**，这份是「为什么」 | 可改，但结论已落地，改结论要同时改 `CLAUDE.md` |
| 13 | `创新点与答辩讲法.md` | **创新点怎么讲准**（同一个设计有三种讲法，两种会扣分）+ 可直接念的讲稿 + 高频追问答法 + **不要说的版本清单** + 答辩前复核表 | 可改。**加一条讲法就要同时加它的证据栏与状态标记**，否则会变成宣传稿 |

`CLAUDE.md` 有点长，第一遍只读这四节就够开工：
「项目性质与硬约束」→「架构：三层隔离」→「确定性要求」→「rts_core 实现约定」。
兵种花名册、克制关系、侦查系统这些等真要动到再回头查。

---

## 三条不变量

`CLAUDE.md` 里绝大多数内容是**有理由的工程选择**，理由不成立了就可以重开讨论。
**真正不可动摇的只有三条**，它们保护的是训练吞吐量与可复现性：

1. **仿真 tick 的热路径内不得出现渲染器、引擎、或向上回调 Python。**
   Python 一次调用推进 N 个 tick，绝不能每单位每 tick 回调一次上层。
   `rts_core` 必须能在完全 headless 下跑完整局。
2. **渲染层是 `rts_core` 的只读观察者。** 渲染代码不得修改仿真状态；
   训练时 `render/` 整个不参与编译链接。
3. **训练回路里没有渲染器、没有引擎。** Python 经 pybind11 **in-process** 直接调 C++，
   不走 IPC、不做状态序列化。

其余的（不用游戏引擎、用 raylib、扁平数组、五种地形……）都附了理由与重新评估的判据。

---

## 改 CLAUDE.md 的规矩

这个项目**已经三次把"手段"写成了"原则"**，每次都要专门开 PR 订正：

| 曾经写成硬性规则的 | 实际上只是手段 | 订正于 |
|---|---|---|
| 「`rts_core` 零依赖」 | 真正要守的是「不得引入非确定性」 | `f3ca7e6` |
| 「交付物必须是纯 C++」 | 课程要求的是**核心逻辑**为 C++ | #8 |
| 「不使用游戏引擎」 | 是满足三条不变量的当前手段之一 | #10 |

所以往 `CLAUDE.md` 写规则时：

- **写清楚理由，以及什么情况下该重新评估。** 一条没有理由的禁令，后来者只能盲从或盲改。
- **区分「硬性」与「当前选择」。** 前者标明是哪条不变量，后者附重评判据。
- **不要在两个地方写同一件事。** 需要交叉引用就写文件名，不要复制内容。

---

## 协作约定

### 分支与 PR

```bash
git checkout -b <类型>/<简短描述>     # docs/ design/ tools/ feat/ fix/
# ... 改动 ...
git commit                            # 提交信息见下
git push -u origin <分支名>
gh pr create --base main
```

不直接推 `main`。PR 描述要写清**为什么这么改**，不只是改了什么 ——
四个人都在用 Claude Code，读 PR 是同步认知最有效的途径。

### 提交信息

**写"为什么"，不只写"是什么"。** 标题一行，空行，正文说清动机与取舍。
本仓库现有的提交可以直接当模板抄。

### 每个人推自己的提交

`git log` 是答辩「人员分工」页的证据，而那页是**个人成绩的重要依据**。
不要代替别人提交。git 身份用 `--local` 配，不要动全局配置：

```bash
git config --local user.name  "<你的 GitHub 用户名>"
git config --local user.email "<你的 GitHub noreply 邮箱>"
```

### 日志纪律

**每次上机后当场补录 `日志.md` 自己那一行。**
`git log` 能补齐"改了什么"，但补不齐时间线、讨论结论与个人分工 —— 这三样只能人工记，
而且事后回忆一定会漏。这份日志要转成 `.doc` 交付，同时是答辩人员分工页的直接依据。

### 不要提交这些

`.gitignore` 已覆盖构建产物、训练权重、`__pycache__` 等。
另外**个人的 `.claude/` 目录不要提交** —— 它是每个人自己的配置，不该进团队仓库。
用 `.git/info/exclude` 排除（这个文件不入库，只对你本地生效）：

```bash
echo ".claude/" >> .git/info/exclude
```

---

## 本地环境

**全员 Windows + MSVC，不使用 WSL2。** Linux 只存在于训练用的 GPU 服务器上。

| 需要什么 | 说明 |
|---|---|
| Visual Studio 2022 | 自带 MSVC 与 CMake，够用，不必单独装 |
| Python 3.10+ | 跑 `tools/`。目前只有精灵流水线用到，只依赖 Pillow |
| Blender | **可选**。只有要重新渲染精灵时才需要 —— 成品已入库，前端可直接用 |
| GPU 服务器账号 | 训练用。配置、环境、连法与已知的坑见 **`训练服务器环境.md`**。**要尽早、频繁地在服务器上跑构建**，见下 |

克隆体积约 135 MB（`.git` 77 MB + 工作区 59 MB，其中 CC0 模型 42 MB、精灵成品 17 MB）。
素材必须入库：交付清单要求「软件源代码**包括输入文件和输出文件**」。

**关于跨工具链**：本地 MSVC、服务器 GCC。MSVC 是三大编译器里与标准分歧最多的一个，
GCC 侧的编译问题只有推到服务器才会暴露。**不要攒到临近答辩才第一次编译 Linux 目标。**
CMake 要同时支持两条路径，MSVC 侧开 `/permissive-` 与 `/Zc:__cplusplus`。

构建命令：

```bash
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release
ctest --test-dir build -C Release
```

**PATH 里通常没有 cmake。** VS2022 自带一份，不必单独安装 —— 两种用法：

- **在 VS 里**：「打开本地文件夹」选仓库根目录，VS 会自动识别 `CMakeLists.txt`
- **在命令行里**（Git Bash / PowerShell）用完整路径：

```bash
CM="/c/Program Files/Microsoft Visual Studio/2022/Community/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/cmake.exe"
"$CM" -G "Visual Studio 17 2022" -A x64 -B build
"$CM" --build build --config Release
```

**VS 18（2026）同样可用**（已实测，Release/Debug 双配置零警告、ctest 全绿）：
路径里的 `2022/Community` 换成 `18/Community`，生成器用 `-G "Visual Studio 18 2026"`。

首次配置会经 FetchContent 下载 Catch2（约 1 MB，**需要联网**）；之后可加
`-DFETCHCONTENT_FULLY_DISCONNECTED=ON` 离线复用。

挑单条用例跑（ctest 条目按标签划分，用例名本身是中文、不适合当命令行过滤器）：

```bash
ctest --test-dir build -C Release -R rts_tests_rng    # 按标签
./build/tests/Release/rts_tests.exe "<用例名>"         # 按用例名
```

几个构建开关：`-DRTS_BUILD_TESTS=OFF`（不建测试）、`-DRTS_BUILD_RENDER=ON`（建前端，
**目前 `render/` 还不存在**）、`-DRTS_WARNINGS_AS_ERRORS=OFF`（警告不当错误，默认当）、
`-DRTS_REQUIRE_DETERMINISM_GUARD=OFF`（**只在机器上确实没有 Python 时才用**：
默认情况下找不到 Python3 会让配置直接失败，因为确定性守卫是一条 ctest，
静默摘掉它的话 ctest 少两条、照样全绿，没有任何东西提示守卫没了）。

**性能相关的改动一律在 Release 下测量**，Debug 构建的仿真吞吐不具参考意义。

---

## 当前未认领的工作

按依赖排序。认领前在群里说一声，避免撞车。

> **活账在 issue 上，不在本表。** 本表给的是「有哪几块、依赖谁」这个骨架，
> 而**当前还没了的事逐条列在 #60（现存问题一览，含逐人「等你」清单）**。
> 三条需要独立动作的单开了：**#57**（1c，全项目唯一的关键路径）、
> **#58**（提案 §6 的机制那一半要表决）、**#59**（Linux/GCC 服务器构建）。
>
> 两处都记会漂移，所以**逐条进展记在 issue 上**，本表只在某一块整体完成或改变归属时更新。

| # | 工作 | 依赖 | 说明 |
|:-:|---|---|---|
| 1a | ~~接口契约：枚举与观测注册表~~ | — | ✅ **已完成**（@zhxxx233）：花名册、战术动作、玩家/宏观命令、观测通道注册表。理由与十三条见 `rts_core 接口契约.md` |
| 1b | ~~接口契约：`World` / 视图 / 迷雾 / 地形~~ | — | ✅ **已完成**（@zhxxx233）：三组扁平数组 + 槽位池、`submit`/`advance`、`WorldView`、迷雾三态、动作与命令掩码、状态哈希。地形枚举一并从 `game/` 搬进 `rts_core`（依赖方向本来是反的） |
| 1b-2 | ~~回放格式与骨架~~ | — | ✅ **已完成**（@zhxxx233）：`rts/replay.hpp` —— 格式（定长记录 + 全文校验和 + 平台指纹 + 哈希口径标签）、`ReplayRecorder`（包住 `World`，漏记一次输入写不出来）、`replay_verify`（区分四种「对不上」的成因）。**顺带补了 `WorldInit::units` 与 `state_hash` 漏掉的命令队列**，见 `rts_core 接口契约.md` §4.6 / §4.7 |
| **1c** | **战斗解算 / 寻路 / 视野 / 经济** | 1b（已就绪） | 由 @ArLiangz 认领（#28）。**刻意与 1b 切开，好让两边能并行**。枚举、注册表、`World` 的数据布局与迷雾原语都已经是它的输入，逐项待办见 `rts_core 接口契约.md` §1.1 那张表里的 ❌。**动 `state_hash` 的喂入清单时把 `kWorldHashTag` 一起改**——否则旧回放会报成「跑歪了」而不是「重录」 |
| ~~2~~ | ~~Linux/GCC 侧首次构建验证~~ | — | ✅ **已完成**（Cody-ic，2026-08-28）：Release + Debug 双配置 7/7 全绿、零警告。同步与构建脚本在 `tools/server/`，实测记录见 `训练服务器环境.md` 第 5 节。**「首次」做完了，「频繁」那一半是每个人的事**——动了 `rts_core` / `tests/` / CMake 之后请顺手 `tools/server/sync.sh && ssh gpu '…/build.sh Release'`，两分钟 |
| 3 | ~~`tools/map_gen/` 生成器 + 校验器~~ **已认领：@omg321-975** | 无 | 纯 Python。地基（#31）与校验器（#33）已合入，**22 条里 15 条已实现**（第 7 条随 2.1 定案落地；§8.1 的第 16、17、21 条一并落地，其中 **第 21 条是落地第 7 条时查出的洞**——没有它，一张把所有资源都放在城里的地图能通过全部检查。第 19 条随 §6 表决落地，**第 22 条是加 `obstacles` 字段时查出的洞**——新增一类点位实体就新增一片摆放冲突面，而那一片没有任何现存检查覆盖）。**剩下的：生成器（第 9 节）、4 条待阈值的校验、3 条阻塞**。三条阻塞各卡在不同的东西上，**不是一个批次**：第 9 条要城区（本来由候选乙带来，乙已作废，所以现在没有任何一处会顺带产出它）、第 18 条缺 `resources` 的解禁波数字段（**加 `obstacles` 时刻意没顺带加它**：字段名与取值形式还没定，捆在一起会让没定的拖住定了的）、第 20 条等生成器且大概不该留在那张表里。逐条理由见 `validate.py` 的 note。注意 6.3 的 `content_hash` 取的是**规范序列化后**的内容（定义见 6.3，唯一实现在 `tools/map_gen/mapfile.py`） |
| 4 | 占位产图路线：跟上契约，还是明确放弃 | 无 | 见 `地图与场景设计.md` 7.2。原四条精灵问题 #19 已修三条，只剩这条 schema 分叉，且已扩大到文件名 |
| ~~5~~ | ~~`render/` 前端起步~~ | — | ✅ **已认领并起步**（@zhxxx233）：#40 `game/` 地图加载与场景装配、#42 世界渲染（raylib、精灵图集、等距投影、相机、截图）、#43 中文文字与格点选。**剩下的增量是演示点两项与实体绘制**，后者等第 1 项 |
| **6** | **一族参数化脚本守方** | 1 | **地位在 #15 定稿后上升，不再是临时物**：它是守方执行层的**最终形态**，攻方在全部训练阶段面对的都是它，其质量直接决定攻方能学到什么。要写的行为不多（`Archer` 拉扯 / `Spear` 堵口不追 / `Ranger` 突袭后撤），但每条都要配参数并随机化——判据见 `守方AI与协同演化.md` 2.5 |
| 7 | 数值标定：成本产出矩阵 | 机制完善 | 每格填 1v1 对抗的期望资源盈亏，再倒推数值。**不要拍脑袋定** |
| 8 | 守方**决策层** RL 与协同演化 | 1、6 | 守方 RL 只在决策层（槽位列表 + 因子化动作头），**没有战术层**。训练成本不再翻倍 |
| 9 | 演示两项（双视图 / `AI vs AI`） | 5 起步完成 + 8 | `render/` 的增量需求。双视图**不可砍**，它是「缺口利用」这条核心指标的**现场**证据 |
| 10 | `tools/` 分析绘图三项 | 8 | 显著性热力图（离线、兼 RL 调试）、策略池胜率矩阵、元控制器后验。**不进演示、不占前端预算**，纯 Python |

第 1 项拆成 1a/1b/1b-2/1c 是有意的：**接口契约与它的实现被切开，好让两边能并行**。
1a 让 `train/` 能照 `rts/obs.hpp` 与 `rts/action.hpp` 定网络与动作头形状；
1b 让 `render/` 能画实体（`WorldView` 给的是连续数组）、也让 1c 有地方写机制；
1b-2 让「新增任何影响仿真的改动都要能通过回放测试」这句话有一个可执行的形式。

**1b-2 排在 1b 之后而不是并行**，理由是 `state_hash()` 的喂入顺序必须先定死——
事后改它等于让所有已录回放失效。这个顺序当场还了两次本：写迷雾时查出上一版
`visible` 通道的缺陷，写回放时查出 `state_hash` 漏了命令队列，
以及 `WorldInit` 表达不了「一个有单位的初始局面」。

**接下来最高优先级是 1c。** 契约那一侧已经没有阻塞项，`rts_core 接口契约.md`
§1.1 那张表里的每个 ❌ 都是一条待办。

第 4 项**现在就能独立开工**，不依赖 1b，且是纯 Python。
第 2、3、5 项已完成或已认领。
新加入的成员若想先熟悉项目再碰核心，第 4 项是合适的入口。

---

## 已定 / 未定速查

<table>
<tr><td valign="top" width="50%">

**已定**

- 游戏形态：不对称波次生存，一波 = 一 RL episode
- 架构：`rts_core` / `render` / `game` / `bindings` / `train` / `tools` / `tests`
- 技术栈：C++17/20、raylib + Dear ImGui、pybind11、PyTorch PPO、CMake
- 不用游戏引擎（**工程选择**，附重评判据）
- 平台：Windows/MSVC 本地 + Linux GPU 服务器训练，无 WSL2
- 确定性：固定 20 Hz tick、可播种 PRNG、回放测试是核心测试形态
- 实体存储：扁平数组 + 整型 ID。**多态按兵种（11 个静态单例），不按实体**
  ——界线是实例的数量与生命周期，不是「用不用虚函数」（#64 定，#65 撤回过一版）
- 花名册：11 单位 + 11 建筑，标识符定稿（≤7 字符；纯渲染层素材键不受此限）
- 守方资源三种：石材 / 木材 / 金币
- 攻方双预算：编成位（线性，**硬性封顶约 40**）+ 兵力（超线性）
- `Phoenix` 是唯一的空中单位
- RL：共享策略网络、离散动作、每 4–8 tick 决策、摧毁建筑的奖励 = 重建成本
- **战术动作 13 个**（停 + 8 方向 + 攻最近/最弱/建筑/城墙），**没有兵种专属动作**；
  「驻守墙段」与「清野」都归玩家级命令，不进战术枚举。
  **§6 通过后这个 13 没变**：攻方拆障碍是寻路的副产物（撞上去自动破坏），
  不需要 `AtkObst`
- **玩家指令集 = 守方 RL 决策层的动作空间**（守方 AI 是玩家的替身），一套枚举两用
- 观测：**带版本号与编译期布局指纹的注册表**，Python 侧一律从模块读、不硬编码；
  等级通道存**和**、血量存**比例**、分母一律用本波名义等级 `L(w)`
- 血量与伤害取 `int64`，附一条推广规则：**接口与回放格式的承诺不得依赖任何待定数值**
- **迷雾三态**（未探索 / 记忆 / 可见），`visible` 通道取 0 / 0.5 / 1。
  两态时「从未侦查过」与「记忆里那段墙是破的」在张量里是同一串字节，
  而后者正是「缺口利用」这条核心指标要的东西
- **迷雾记建筑、不记单位**（建筑不动，记住的单位位置主动误导）；迷雾**进状态哈希**
- `World`：三组扁平数组 + 代数句柄 + 空闲表（**空闲表也进哈希**，否则分叉会被推迟发现）；
  `submit` 一批是**全有或全无**；`submit_actions` 的长度必须**恰好**等于该侧活着的单位数
- **掩码里未定的位默认允许**——错误地允许只是浪费样本，错误地禁止会让 agent 永远学不到
- 地形枚举住在 `rts_core`（`game::Terrain` 是别名），因为仿真吃它
- **回放只存外生输入，不存机制产物**——存了 `kill_unit` 之类，回放测的就是它自己的日志
- 回放文件头记**平台指纹**与**哈希口径标签**，于是「跨平台」「布局改了」「文件坏了」
  三种预期情况各报自己那句，不再伪装成「第 N tick 状态不一致」
- **一条状态哈希都没有的回放判失败**，不判通过（同「守卫扫到 0 个文件即失败」）
- **训练时守方也是 RL，但只在决策层**——攻方 RL 控制每个单位，守方只下玩家会下的
  指令，**单兵是参数化脚本**（且脚本是最终形态，不是临时物）
- **演示**只做两项：双视图、`AI vs AI`。显著性热力图与非传递性环矩阵**改为只用于
  分析与报告**（落 `tools/`，不占前端预算）——「不进演示」不等于「不做」
- 意料之外的涌现行为**当场记录并同步全组**，它比预期清单内的任何一项都更有力
- 地图：grid、五种地形（`Plain`/`Rock`/`Forest`/`Water`/`Bridge`）、**不做地形高度**、初始给残破城圈并留缺口
- 一格画两张图：地形枚举 →（地砖，可选叠加物件），桥的底衬是水
- 守方三资源**全部来自地图资源点**（`Quarry`/`Lumber`/`Mine`）；`Keep` 只产极少量金币作「兵力地板」
- 波次进行中不禁止任何资源的使用——限制器是**时间与暴露**，不是禁令
- 不做登墙阶梯，改用**驻守槽位**（代价由机动性承担）
- 确定性禁令有可执行守卫（`tools/check_determinism_bans.py`，注册为 ctest）
- 每个集结点必须对应一条性质不同的走廊（**结构约束**；数量本身待定）
- 寻路把墙当「高代价可通行」，代价 = 血量 ÷ 破坏速率
- **守方有一条升级轴：堡垒等级**，三个输出（人口上限 / 建筑等级上限 / 兵种等级上限）
  + 「买活」。无分支、无解锁、无科技树。等级只提升血量与伤害，取兵种级而非老兵制
- **破坏速率随等级缩放，城墙与城门也可升级（只加血量、同倍率）——两条成对。**
  结构锁改为「破墙时间落在一个区间里」。它修的是一处内部矛盾，不是加机制
- **2.1「外部资源点不可围」用森林带**：外部簇周围铺 `Forest` + 到地图边界的森林连通条件。
  甲（`no_build` 环）与乙（城区 + 前哨点）都作废。零新机制、零新地图字段
- **资源点随波数解禁**，越晚解禁的越远。拐点从一次性事件变成反复出现的节奏
- **无尽模式看「中位存活波数」，不看胜率**——必败是设计内的，「赢」这个事件不发生
- **可破坏障碍已定案**（§6，2026-08-29）：`Stump` / `Sapling` / `Rubble` 有血量，
  **破坏后产出资源**（木质→木材、石质→石材、**没有一种产金币**）。
  四条结构决定：产出归最后一击方（攻方无经济⇒实际只有守方拿）、**不可再生**、
  **建筑摧毁不产生 `Rubble` 实体**（否则有一条「拆废墟得石材 + 打折重建」的净赚回路）
- **凡关系到平衡与体验的数值一律待定**，等框架完善后统一标定

</td><td valign="top" width="50%">

**未定**

- **`rts_core` 的机制（1c）** ← 最高优先级。公开接口已全部定稿，不再是阻塞项
- **硬要求 1（AI 只见自己的迷雾）的落点是观测打包那一层，而那一层还没有代码。**
  `WorldView` 是 god 视角、实现的是不变量 2；这是一处**要靠评审而非编译器**
  把关的地方（`rts_core 接口契约.md` §5.5）
- **flow field 要把等级量化成几档**（破坏速率随等级已定，见「已定」栏）。
  这是 `Pathfinder` 的内部形状、不在公开接口上，但**值得在实现前定**——
  它决定 field 的份数与内存（`地图与场景设计.md` 第 3 节那个 36 KB/张的估算要重算）
- 斜向穿角暂取「不能」（`kDiagonalNeedsBothOrthogonal`），**仍需寻路那侧确认**。
  它现在有了唯一的消费者 `World::action_mask()` 与一条会红的用例
- **地图格式缺一张「建造槽位列表」**：决策层动作头要的那约 60 项子集没有地方存，
  于是 `Command::slot` 现在取最粗的编码（格线性下标），粗化留给 `train/`（同文 §4.4）
- **全部数值**（血量、伤害、射程、速度、造价、重建成本、破坏速率、编成位占用）
- 地图边长、城区半径、走廊口宽度与数量、**集结点数量**
- **`resources` 缺一个「解禁波数」字段**（「资源点随波数解禁」已定，而 6.2 里没有这个
  字段）。**不能混进 `tier`**——`tier` 只区分城内 / 城外，解禁波数影响仿真。
  它是一次 6.2 契约变更，连带 `rts_core` 载入、`tools/map_gen/` 读写与第 18 条校验
- **`§1.4` 等级提升哪几个数值**（血量+伤害 / 只血量 / 只伤害）——**结构选择，控制曲线指数**
  （两个都涨是 L²，只涨一个是 L）。今天定的是**归口与判据，不是答案**：三种各跑一遍
  成本产出矩阵，而**判据只能在野战里测**（城墙攻防里冲锋助跑、枪阵克骑、风筝根本不发生）
- 第 8 节第 18、20 条校验尚未落地（第 16、17、19、21、22 条已落地）。**两条卡在不同的东西上**：缺解禁波数字段 / 等生成器，见该节 8.1
- 占位产图路线（`gen.py`）的去留（见 7.2）
- 演示地图的具体布局
- 守方 AI 的 4 项细节：奖励偏离零和多少、对手池规模与入池间隔、行为克隆要多少局
  人类数据、「调兵」动作怎么表达「调哪一支部队」（见 `守方AI与协同演化.md` 第 9 节）
- **显著性热力图最终做到什么程度**（先出一版 `tools/` 离线脚本，看它在调试与举证上
  实际有多少用，再谈要不要往前端做）、`AI vs AI` 进不进菜单、
  **`render/` 预算重估到多少人日**（**基线已给**：现有约 2.5–3 人日、到可玩前端
  估计总计 8–9 人日已超原 5–8 预算，演示两项另计 1.5–2 人日；要的是一次取舍而非一个数）、
  答辩标准片段用哪几个（「能不能用回放固定」不再是问题，`rts/replay.hpp` 已落地）。
  逐条见 `演示与可视化清单.md` 第 9 节
- C++ 手写推理是否定为部署方案 —— **不是悬着，是还没到时候**：`CLAUDE.md`
  已把「推理时是否随包分发 Python 运行时」定在**第 3 周**再决定
- 图形库：raylib 还是 SFML（倾向 raylib）
- 组长姓名与中文题目定名（**四份 `.doc` 交付文档的文件名依赖此项**）
- 待向老师确认：本项目的类设计是否满足「系统设计报告」中"类对象设计"的要求
  （**应在第 2 周结束前问**）
- 推理时是否随包分发 Python 运行时（第 3 周再定）
- 是否允许战斗中维修建筑

</td></tr>
</table>

---

## 交付物（老师要求，原文见 `课程设计最终交付项.md`）

四份 `.doc` + 源代码 + 可执行程序。其中：

- 「软件源代码**包括输入文件和输出文件**」—— 所以 JSON 数值表、地图数据、
  精灵成品、回放样例、训练曲线都要一并交付，这也是精灵与 CC0 模型入库的原因
- 「人员分工单独一页，作为个人成绩重要依据」—— 所以有上面那条提交纪律与日志纪律
- 「课程设计日志：记录每次上机每名组员的进展情况」—— 所以 `日志.md` 要当场补录

- 「系统设计报告：描述软件的**类对象设计**」—— 已由 `UnitBehavior`（基类 + 11 个兵种
  派生类）正面答掉，那条挂了两周的「需向老师确认」随之划掉。
  **但报告与答辩里怎么措辞是另一件事**：同一个设计有三种讲法，两种会让老师听到
  「他们在解释为什么没做到要求」。讲法、可直接念的讲稿、**不要说的版本清单**，
  以及「哪条现在成立、哪条还依赖 1c 或训练」的复核表，见 **`创新点与答辩讲法.md`**

详细的工程影响分析见 `交付要求的工程影响.md`。
