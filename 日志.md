# 课程设计日志

> 记录每次上机每名组员的进展情况，最终转为 `.doc` 交付（命名格式见 `课程设计最终交付项.md`）。
>
> **每次上机后当场补录。** `git log` 能补齐"改了什么"，但补不齐时间线、讨论结论与个人分工，这两样只能人工记。本日志同时是答辩 PPT 中「人员分工」页的直接依据，而该页是个人成绩的重要依据。

## 记录模板

复制下面这段追加到文末，新的记录放在最前面：

```markdown
## YYYY-MM-DD（第 N 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | | |
| ArLiangz | | |
| zhxxx233 | | |
| omg321-975 | | |

**本次决定**：

**遗留问题**：
```

---

## 2026-08-28（第 2 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | 骑兵重做：素材包无骑乘动作，新增 `pose`（在动作之上叠加骨骼角度）与 `attach`（真正的骨骼父子关系）两项能力，骑手不再双腿插进马腹、武器不再一奔跑就脱手；全部单位补 `idle` / `move` 动画状态，文件名与元数据改为逐状态；精度提至 256px/瓦片；补齐地形素材（5 种地面变体 + 岩壁 / 密林 / 水域 + 桥 + 景物）与 3 座新建筑；**订正全套尺寸比例**——尺寸约定曾以菱形屏幕宽度为 1 而非格边长，全体实体被放大 √2 倍，新增 `modules` 改为按 Kenney 模数统一定标；产出 `preview_map.py` 作为前端贴图与深度排序的可执行示例；订正资源模型三处（金币改由资源点产出、木材改为所有建筑的配料、波次进行中不禁止任何资源的使用）；删除「登墙阶梯」改用驻守槽位；提议地形枚举补 `Water` / `Bridge`；将 #13 的 2.1 实现手段列为两个候选待讨论；把素材与文档全面对齐 #13 契约。**实测训练服务器**（1×A800 80GB 空载、96 线程、503 GiB 内存、6.2 TB NVMe；gcc 13.3 / cmake 3.28.3 / python3 3.12.3 齐备，**缺 pip / PyTorch / conda / ninja**），据此订正「GPU 机时够不够」这个问法——**被串行化的是 CPU 不是 GPU**（A800 只用 14 MiB，而 `BatchedEnv` 吃的是那 96 线程），并把 `python3` 标成构建硬依赖（确定性守卫是一条 ctest，找不到 Python 会 `FATAL_ERROR`，下一台机器上看到它的人不该以为是配置写坏了）；容器重建导致 SSH 主机密钥变更，新旧两个指纹都记进文档供下一个人核对。**资源建筑三族重做**：起因是评审指出采石场与岩壁同为三堆灰石头而机制正好相反，改完发现**伐木场与木栅共用同一个模型**——同类错误第二次，于是写 `check_assets.py` 把它变成会红的检查（35 实体、67 处模型引用；「配件共用」只提示不失败，因为**一个总是红的检查等于没有检查**）。过程中连做废两版，教训是**先渲一张构件对照表再设计，不要照文件名猜**（`tower-slant-roof` 不是屋顶构件而是一整座小屋、`tower-square-base` 是没门没顶的塔地基）、以及**共用主体 + 小配件行不通，区分必须做在主体形体上**（等距下贴地配件根本看不见）。重写 `preview_map.py` 并拆成「契约」与「演示地图」两段，据此发现一处前端契约缺陷：**线性结构的 `d` 是走向不是朝向**（沿 `gi` 用 `SW`、沿 `gj` 用 `NW`，实测得出），照 4.2.1 写前端的人会得到一座墙线断开的城而不会怀疑朝向。评审 #20 与其复核 #24（逐条实测，含 Debug 侧那条他漏掉的配置），评审 #31 / #33 并论证**第 8 条不需要城区**（判据换成「存在集结点能不破墙走到 `keep`」，即第 4 条换一个障碍集）、**第 9 条的 min-cut 锚点必须是经济而非几何**（否则退化成 `keep` 周围一圈）、以及**第 16 条**（现有 15 条没有一条查那圈初始城墙存在，`initial_walls` 为空会空洞通过）。另：曾给训练截断波数 W 一个量级估计，**违反组内「数值一律待定」的明文规则并已公开收回**，由此提炼出一条建议进契约的规则——**回放格式承诺不得依赖任何待定数值**，字段宽度应选到「该数值的整个候选区间都不可能影响它」 | #19（含 `617e451` `9e9bc74` `243577d` `26df98c`）、#22、#27、#30；提案 #23 待议；#20 / #24 / #31 / #33 评审 |
| ArLiangz | 经 #25 接手 `rts_core`，任务范围是游戏核心部分：接口契约（`step()` 形状、观测通道注册表、动作/命令枚举、数据布局、回放格式）先行，战斗解算 / 寻路 / 视野 / 经济随后跟进（与交接说明第 1 节「后四块留作可认领」的划分不同，一并认领，异议请在 PR 里提）；按协作约定完成开工配置（`--local` 身份、`.claude/` 入 `.git/info/exclude`）；完成交接说明第 11 节第 1 步的构建验证——本机是 **VS 18（2026）/ MSVC 19.51** 而非 README 假设的 VS2022，实测生成器 `Visual Studio 18 2026` 可用并写回 README；Release 与 Debug 两配置均零警告、ctest 7/7 全绿（Debug 单验的原因见第 7 节第 8 条：它是 `assert` 唯一被编译的配置）；按验收标准第 3 条做破坏性测试——塞入 `unordered_map` 确认确定性守卫真的变红，还原后 `git diff` 为空、恢复全绿 | #25（接手方）、本 PR |
| zhxxx233 | **落下仓库第一行 C++ 代码**：`rts_core` 地基 —— 双工具链 CMake（MSVC `/permissive-` + GCC，第三方头标 SYSTEM 以免淹掉第一方 `/W4 /WX`）、自写 xoshiro128\*\* PRNG（不用 `<random>`：分布适配器的实现各标准库自定，同种子在 MSVC 与 libstdc++ 下数列不同；黄金向量由独立 Python 实现另算一遍，非从 C++ 输出反抄）、FNV-1a 状态哈希（选它是因为**顺序敏感**，顺序不敏感的哈希会对「内容相同、顺序不同」给出假绿灯，回放测试白做）、索引+代数的实体句柄（索引置高位使句柄序 = 下标序，`UnitId`/`BldId` 用 tag 做成不可互换）；把 CLAUDE.md 的三条确定性禁令变成构建时就红的 ctest 守卫，含自检以防检查器退化成永远绿；发现 ctest 按中文用例名注册会静默漏跑、改按 ASCII 标签注册；加 `.mailmap` 合并自己被拆成的两个作者名；三轮评审 #19 并指出采石场与岩壁撞视觉、日志「本次决定」自相矛盾、4.1/4.2 的「地形枚举 → 精灵」是一对多而文档写成一对一；论证出 2.1 候选甲有一个漏洞（只封死「围起来」、没封死「设防」——三面砌墙架塔即可让外部收入重新靠建筑守而非部队守，而这只是贵、不是不可能，落在「结构封死，而非数值劝退」上），据此两人一致改投候选乙；合入 #19 / #20 并做文档一致性结算（第 7 节按「已修/未修」重写、校验器表 12 → 15 条、新增 4.2.1「地形枚举 →（地砖，可选叠加物）」、6.3 的 `content_hash` 改为对规范化内容取、`.gitattributes`、`Watch` 视野不得覆盖集结区、标识符 ≤7 字符对纯渲染层素材键的豁免）；回应 #20 的事后复核，逐条独立复现其指出的五处缺陷（并把「构建类型默认给 Debug」定位到 CMake 自身的两行）、否掉其中一条修法建议（实测它在全新目录下会顶掉用户显式的 `-DCMAKE_BUILD_TYPE=Debug`）、另自查出两处同族问题（`types.hpp` 漏 `<compare>` 却被 Catch2 的包含顺序掩盖；ctest 标签清单是白名单、漏登记抓不到），全部修复并各加一道结构性守卫；审查 #23 的素材落地提交，查出四条规则合规问题（`TreeBig` 与 §6.4 三维可读性规则冲突且两条维度指错、它作为 `Forest` 变体却用 `Tree*` 前缀违反素材键归类规则、`TreeSmall`/`RockSmall` 的 ≤7 字符豁免因「有血量的实体」而前提失效、契约文档滞后于素材是 #19 那次的重演）并建议把素材提交从提案 PR 里摘出；**把 `rts_core` 交接给 ArLiangz**，产出交接说明——收拢十三条只存在于 PR 评论串里的接口设计点（评论串会被后续讨论淹没，而这是我自己在 #23 上说过的话）、十条已踩过的坑、以及「19 项待拍板中哪些其实不阻塞接口」的逐条结论；**把三份提案的待拍板事项合并成 issue #26 并逐条定性**——去重后 18 项，其中只有 13 项今天该拍：2 项按组内「数值一律待定」的明文规则今天**不能**定（训练截断波数 W、守方人数上限，前者的量级估计提出者本人已收回）、3 项没有训练回路就是猜（对手池规模、行为克隆数据量、协同演化周期），会上产出应是归口与触发条件而不是数字；#23 当天被重写后又对账一遍，指出原议程 13 项里 5 项已作废；**评审 #31 / #33（`tools/map_gen/`）并用它自己的原语实测出四处问题**——第 10 条（已实现、当时是绿的）对**斜向森林链**漏报（角上按「是不是森林」判而不是按「能不能走」判，而单位斜穿的两个角格是可通行的 `Plain`，于是一条真实的遮蔽通道查不出来），由此提炼出「保守 = 多报不漏报」这条安全性论证**只对「必须连通」型检查成立、对「必须存在通路」与「不得存在通路」两类极性是反的**；证出第 8 条其实不阻塞（换成「存在一条从集结点到 `keep`、全程不穿墙的通路」即可，原语对方已经写好），并实测出 `derive_city_area()` **当且仅当**在第 8 条该判失败的地图上成功——这条同时**削掉了他给候选乙的那条新论据，而我正是主张乙的那个**；另查出「多个路径参数里有一个写错仍然退出 0」与「地形调色板超过 10 项会静默失效（注释说会报错，实际没有）」；论证 §1.4「等级提升哪些属性」有一个**文档层面**的答案而不必只等实验（`CLAUDE.md` 已写死「破坏速率与等级无关……等级只让他们在火力下活得更久」，于是「只提升伤害」对破墙贡献为 `L⁰`、应直接排除，「只提升血量」正是那句话逐字读出来的那一个），并据此炸出 `CLAUDE.md` 一处内部矛盾（「建筑走与单位完全相同的战斗解算」与「破坏速率与等级无关」不能同时成立）；就 §4 第三案第二次改立场（见「本次决定」）；**把上述四处从推理升级为可运行的反例**——用对方自己的 `make_doc` 与 `grid` 原语构出斜向森林链（`flood` 看到的连通域只有起点一格，而单位三步全程站在 `Forest` 上斜穿到墙边）、构出「干净图 + 一个写错的路径」证明校验器**打印「全部检查通过」并退出 0**（他自己那道「一张地图都没读到必须红」的守卫只覆盖了全部目标都不存在的情形，不覆盖部分）、以及实测 palette 改成 11 项后允许字符集里出现 `'10'` 这个永不匹配单字符的两字符串而 `check_format` 一声不吭；**订正自己 #38 里的修法而非只订正数值**——原本按对方 #37 的指认把「现有 5 条」改成 7，但他同一条 issue 里写着「map_gen 合并后这里要改成 9 条」，那恰好说明改成 7 是错的做法：把一个必然漂移的数字改对只是把过期推迟一个 PR，于是两处（`tests/CMakeLists.txt` 与 `CLAUDE.md`）都改成不带数字、并把 ctest 表由逐条枚举改为按族列，写明**权威清单是 `ctest -N` 而不是那张表**；评审 #39（首次 Linux/GCC 构建）并**独立复现其唯一需要外部信任的那条论据**——实测 codeload 兜底归档的 SHA256 与 `URL_HASH` 声明值逐字符相同，确认兜底不靠信任域名；另实测全新目录配置 11.2 s（兜底 URL 与 `TIMEOUT` 未拖慢正常路径）、`git check-ignore` 命中 `build-*/`、两个 `.sh` 在索引里是 `100755` 且 `eol=lf`；结算 issue #29 的三处规范空缺（第一条：城区须显式给出成立，但「第 8、9 条都依赖它」只对第 9 条成立；第二条重分类为「不是空缺、是在等 A4」——先补 `layers.city` 字段等于先选乙；第三条保留，并指出第 14 条属「不得存在通路」型、保守方向与第 4 条相反）；逐条标出 #34 的六处改动里**有三处会回退 #39 刚修好的东西**；合入 #22 / #27 / #30 / #28 / #32 / #38 / #31 / #39 八个 PR | #20（`79fbb60`）、#21、#24、#25、#38、#19 五轮评审、#23 四轮评审、#22 / #27 / #30 / #31 / #33 / #39 评审、#26 议程、#29 结算 |
| omg321-975 | 环境搭建与全仓通读（本地 MSVC 侧复现构建，7 条 ctest 全绿）；认领 `tools/map_gen/`；读第 6、8 节后提 **#29**，列出三处「实现绕不开、而文档未定义」的空缺——「城圈」缺可操作定义（6.2 说缺口由校验器从城圈推导，但地图里没有城圈这个东西，而 `initial_walls` 本身带缺口、集合不闭合）、**第 7 条里候选乙需要的 `layers.city` 与 `outposts` 字段在 6.2 中根本不存在**（此前只记了「判据写不出」，未注意到采用乙还要连带改一次跨模块契约，且「外部资源簇」也缺一条聚类定义）、第 14 条的距离度量依赖尚未定稿的静态建筑视野形状；读 #26 后补充 A5 若通过（§2+§3）的连带影响：城区不再是一张静态 mask 而要随 `Keep` 等级分级、`resources` 需加解禁字段、第 9 条的「正面总长」从标量变成一条曲线，并建议把议程里「§2 必须配 §3，否则城区终将包住外部簇」这条口头约定做成校验器第 16 条（否则日后改一次城区半径曲线或解禁波数表，龟缩会**静默**复活）；**实现时自证 #29 第一条我自己给的 flood-fill 定义不成立**——两种障碍取法都会漏：取 `Rock∪Water` 则从走廊口漏（走廊口本就是 `Plain`，见 2.2），再加上墙则从第 8 条强制要求的缺口漏；根因是一条不闭合的曲线不把平面分成内外两部分，任何 flood-fill 变体都绕不过去，除非引入「能填多大缺口」这类形态学参数，而那又是一个待标定数值（用数值旋钮去定义结构约束，方向就错了），据此把结论更正为「城区必须由地图显式给出」，并给 A4 添一条此前没人提过的论据：**候选乙的城区 mask 是校验器第 8、9 条能够实现的前提，候选甲那一侧根本没有可实现的第 8、9 条**，甲乙之争由此从强度层面的比较变成能力层面的差别；落 **#31** `tools/map_gen/` 地基——`mapfile.py` 作为全仓唯一的地图 I/O 入口（6.3 的哈希若在生成端与校验端各写一遍，失败时的症状恰是最难查的那种）、`grid.py` 的格子解码与连通性原语、67 条断言的自检注册为第 8 条 ctest；三处规范留白一律取保守侧并写明理由与重评点：坐标 `[x,y]` 且 `rows[y][x]`（从示例中四个集结点的位置反推，用 7×5 非方形图钉住，因为弄反在方形图上不报错、只是整张图转置）、`content_hash` 改取**规范序列化**而非规范化文本（LF + 去尾随空白挡不住缩进宽度／键顺序／非 ASCII 转义这三种同样改变字节、症状却一模一样的差异，建议 6.3 相应改写）、斜向不穿角取最严的一种（多报不漏报）；`derive_city_area()` 泄漏即抛 `CityAreaUndecidable` 而不返回一个看起来像样的错答案，`layers` 用白名单使 `no_bulid` 这类拼写错误不会被静默当成全 0 | #29、#31 |

**本次决定**：

- **资源模型三处订正**（此前的写法都是错的）：
  - **金币的主要来源改为资源点。** 三种资源的主要来源一律是地图上的资源点，对应采集建筑 `Quarry` / `Lumber` / `Mine`；领主堡垒只保留**极少量**金币作为兵力地板（**见下一条**，该产出的定位在本次上机内改过两轮）。副产品是「攻其必救」成为三向：拆采石场削防线**上限**、拆伐木场令防线**修不动也建不起**、拆金矿场削**兵力**
  - **木材改为所有永久建筑的配料**，不只是维修材料。只管维修的话开销被「维修成本显著低于重建」这条反死亡螺旋规则压住上限，木材永远花不完，"这波该攒哪种"不成其为决策；连带攻方拆伐木场也不疼
  - **波次进行中不禁止任何资源的使用。** 曾写过「石材与金币只能在建造阶段花」，方向错了——那是靠削掉另外两种的权限来凑木材的独占性。限制器应是**时间与暴露**：石材建造慢且工地就在杀伤区内、金币的兵要训练要行军、木材快但只能补形状不能扩形状。锁金币会直接毁掉战术层（缺口本该由机动部队去堵，不能补兵则战果在开打那一刻已定死）；锁石材也不必，建造耗时 + 工地暴露已解决「边打边补墙」的顾虑，还顺带给 AI 一个可学的目标（点杀施工中的工匠）
- **领主堡垒恢复产出金币，但定位改为「兵力地板」而非收入。** 本次先把它的产出删掉了，随后发现这样会开一个死亡螺旋的口子：金币管人力，而人力是唯一能在波次进行中即时投入的手段；若金币完全依赖城内 `Mine`，金矿一旦被点掉玩家**连补兵都做不到**，任何缺口都无法再堵，一波之内被推平、此后每波更惨。而防死亡螺旋原有的三条对策（维修便宜、废墟折扣、城内保底）**本身都要花资源，因此都救不了这一种**。堡垒的产出额只保证能补最基本的兵、不足以支撑军队，且它天然不可被摧毁（丢失即败），这是"地板"最理想的性质，用别的建筑都做不到。这条是**有意的例外**而非历史遗留——若将来想删，须先为金币断供另找一条护栏
- **不做「登墙阶梯」这类建筑。** 它没有取舍——总是要造、放哪儿都行，是杂活不是决策；且会把墙顶变成第二层可通行面，寻路要处理入口节点。改用**驻守槽位**：直接命令单位占据墙段、原地登上，代价由**机动性**承担（上去的单位被钉在那一段）。「城墙可站人」是机制，「先造楼梯才能上墙」是杂活，两者不必绑在一起
- **地形枚举补 `Water` 与 `Bridge`**（提议，已写入 `地图与场景设计.md` 4.1）。原本只做三种、把 `Water` 判为与 `Rock` 职能重复。按标志位摊开看，**通行 × 视野的四种组合里「挡路但不挡视野」这一格只有 `Water` 能填**，而它给出的质感岩壁做不到——看得见对岸集结却打不着，直到他上桥。这一格直接落在「情报划分：免费可见 vs 需侦查」那张表上：水送免费情报、岩壁否定情报。`Bridge` 是走廊表里隘口一类的极端（正面最窄且全程可见）；河的长条形状也不同于岩壁的团块——岩壁做局部隘口，河把地图切成进攻扇区。按 4.2 的位图设计两者接近零成本。已封两个口子：`Bridge` 不可建造、且不做「桥可摧毁」
- **`Road` 仍然不做，但理由说清了**：反对的是**加速效果**而非路面美术。渲染层可把部分 `Plain` 画成路面，仿真里它仍是 `Plain`。同理草地深浅变体、焦土痕迹都只是 `Plain` 的贴图，**不占枚举位**，渲染层按坐标哈希选变体即可
- **素材尺寸约定订正为「一律以格子边长为 1」。** 原先以「菱形的屏幕宽度」为 1，而格边长只有 0.707——等于把所有实体放大了 √2 倍，于是单位比地砖还大、大树比战马还矮。新增 `modules`：1 个模型单位 = k 格边长、不做归一化。**Kenney 整套件都建在 1.0 模数上且内部自洽**（墙 1.31、塔身 1.01、大树 1.85、攻城锤 2.09、地砖 1.00），统一乘一个系数即让全套件与格子及彼此的比例自动正确；逐个手调高度反而破坏这份自洽，那正是上一版失控的原因
- **动画状态成为前端契约的一部分**：文件名 `<标识符>_<状态>_<朝向>[_<帧号>].png`，元数据按 `(标识符, 状态)` 读 `canvas` / `ground_anchor` / `frames`。建筑等静态实体也有 `idle` 状态，前端不必对它们特殊处理；不同状态画布大小不同但锚点对齐同一地面点，故 `idle` ↔ `move` 切换不会跳动

- **`rts_core` 接口的定稿方式改为分两层，而不是一次定完。** 此前四次记录都把「接口未定稿」列为最高优先级阻塞项，但朴素地现在定完是过早的：花名册（+`Watch`/`Barrack`/`Mine`）与地形枚举（+`Water`/`Bridge`）本次都在改、2.1 的甲乙也没拍板，现在写进代码则 #19 合入当天就过期。#20 的切法是**只落对这些零依赖的那一层**（tick、实体句柄、PRNG、状态哈希、CMake），把花名册 / 地形 / 动作枚举与 `step()` 签名留给下一个 PR。这样四人立刻有可编译的基础，而契约那一轮评审只讨论接口形状
- **确定性从「文档里的禁令」变成「会红的测试」。** CLAUDE.md 禁无序容器影响仿真遍历、禁 C 库随机、禁以地址当 key，此前**违反了不会有任何反应**，直到某次回放对不上——而那时人会先怀疑仿真逻辑，最后才想到是遍历顺序变了。这是「结构封死，而非数值劝退」用在代码库上：不指望每个人都记得规则，让违反规则的代码过不了测试。误报时须在行末写 `// determinism-ok: <理由>`，**刻意要求写理由**，使放行可评审而非悄悄绕过
- **资源建筑的旗色承载「产哪一种资源」。** 起因是评审指出采石场与碎石、岩壁同为三堆灰石头、只有大小之差，而三者机制正好相反。加旗（人造 = 玩家建筑）之后目视又发现只加旗不够：按「交错杂居」，一簇内本就混有石/木/金，**相邻的异种资源建筑是默认布局而非巧合**，两座并排仍读成一大堆石头。于是让旗色再给出第二个位（石灰白 / 木绿 / 金黄）——一个元素同时承载两个位，且位于实体最高处、逆着地面色，是画面上最易读的位置。「攻其必救」是头号演示点，观众必须看出 AI 在打哪一种
- **2.1「外部资源点不可围」改投候选乙，但记录在案的理由换掉了。** 原先提乙的理由是「`no_build` 盖在看起来完全正常的平地上、容易被读成 bug」，评审判定这**是渲染问题而非结构问题**——建造模式画一层放置遮罩是同类游戏标准做法，且本次刚入库的 `PlainDirt` 地砖变体正好能把禁建走廊铺成土路让规则可见，所以这条救得回来、不足以支撑改动一条硬性约束，已降级为附带好处。**真正的理由是候选甲有一个洞：它只封死「围起来」，没封死「设防」**——玩家可在外部资源簇三面砌墙 + 架塔、留着禁建走廊口不管，于是外部收入又变成靠建筑守而不是靠部队守，而「部分资源点在墙外」保护的东西是**野战必须发生**。三面设防只是贵、不是不可能，而攻方预算超线性、守方上 RL 之后那就是会被梯度找出来的策略。候选乙把洞从「可以靠钱扩大的旋钮」变成「地图作者写死的常数」。**仍未定案**：它使墙类决策只剩「修旧」（「建新」在墙这一类上被削掉），这处范围收缩需小组共同认，见 2.1.2
- **任何静态建筑的视野都不得覆盖集结区**（`Watch` 瞭望塔最需注意，但对所有建筑成立）。整个侦查博弈的前提是**情报要花钱买、且会过时**；一座永久建筑若能照亮集结区，「这波要不要花钱侦查」就被**一次性买断**，`Scout`、`Wraith` 屏蔽、佯攻诱饵一起失效。这是把持续的经济交易换成一次性支出，**性质变了而非强度问题**，所以不能靠调小视野半径解决。已写成校验器的距离条件（第 8 节第 14 条），随地图尺寸标定自动成立
- **「该红却绿」被当成一类缺陷来处理，而不是七个独立 bug（#24）。** #20 合入后的事后复核
  找出五处，复核那份复核又掉出两处，七处的形态完全一样：**失败时本该有反应，实际一声不吭。**
  `cmake -B build` 在 Windows 上其实给的是 Debug（守卫判空放在 `project()` 之后，
  而平台默认值那时已写进缓存，于是那段代码只在 Linux 上有效）；注释点名的 C4365 从来没开
  （off-by-default，于是 MSVC 侧比 GCC 侧松，本地干净、服务器会直接失败）；确定性守卫扫到
  0 个文件返回绿；找不到 Python3 时守卫整条静默消失、ctest 从 5 条变 3 条一样全绿；
  `1'000` 这种数字分隔符会让守卫吃掉整行、同一行里的 `rand()` 查不出来；`types.hpp` 漏
  `<compare>` 而测试文件先 include 了 Catch2 把它顺带拉进来；ctest 标签清单是白名单，
  标签写错抓得到、**漏登记抓不到**。
  **共同的成因不是判断力而是验证路径**：#20 的六项验证走的是 VS 多配置生成器 + Python 齐备
  + 头文件跟着 Catch2 一起编的那条 happy path，七处缺陷全在它旁边。因此每一处的修法都不止
  「改对」，而是**再加一道会红的守卫**：构建类型改成跨 `project()` 判断并实测四种场景、
  0 文件与路径写错一律判失败并加 `WILL_FAIL` 的 ctest、`RTS_REQUIRE_DETERMINISM_GUARD`
  默认 ON 使「守卫不在」成为需主动声明的决定、给 `include/rts/` 下每个头各生成一个只含
  它自己的 TU（GLOB 而非手写名单）、再加一条不带过滤器的 `rts_tests_all`。
  这是「结构封死，而非数值劝退」用在验证路径上：不指望下一个人记得走全部路径。
  **后续：#24 的复核抓出了这一类的第八例，而且就在修完前七例的那个 PR 里。**
  我的八项验证全部只跑 Release，于是新加的 `assert(index <= kMaxIndex)` 那行
  **在我的验证里一次都没编过**——`NDEBUG` 把它整行编掉了。复核补了 Debug 一列并真把它
  触发了一次。教训与前七例完全同源：修的时候盯着「守卫会不会红」，
  漏了「我这条验证路径覆盖了几个配置」。**凡新增仅 Debug 生效的检查
  （`assert`、仅调试期断言），只跑 Release 等于没验**，已写进交接说明的验收标准。
  **再后续：同一类的第九例，成因又换了一个**——@omg321-975 查出 `CLAUDE.md` 里
  「Windows 上 `cmake -B build` 不给 `-DCMAKE_BUILD_TYPE` 也会落在 Release」这句是**错的**
  （VS 是多配置生成器，`CMAKE_CONFIGURATION_TYPES` 有值 ⇒ 守卫被跳过 ⇒ 缓存里根本没有
  `CMAKE_BUILD_TYPE`，而 `vcxproj` 的首个配置是 Debug）。我复现了。
  **这次不是漏验，是「测对了、写错了」**：#24 当时四种场景确实都实测过（含 VS 多配置那条），
  落笔时却把「单配置下守卫生效」转述成了「Windows 上会落在 Release」。
  **验证路径与转述路径不是同一条**，于是文档独自跑偏，而代码没问题。
  后果不是学术性的：同一节紧接着要求「性能相关的改动在 Release 下测量」，
  两句连着读会让人 `cmake -B build && cmake --build build` 拿到 Debug 再去量吞吐——
  正好是那句要防的事，等于抵掉 #24 那次修复的一部分。已开 #38 订正，
  并把「#24 修的是单配置路径上的代码」与「本次错的是对多配置路径的描述」分开写明——
  不分开，读的人会以为同一个 bug 又犯了一次
- **`rts_core` 交接给 ArLiangz，并为此产出一份有时效性的交接文档。**
  交接的是**接口契约**（`step()` 形状、观测通道注册表、动作与命令枚举、数据布局、回放格式），
  不含战斗/寻路/视野/经济——那四块留作可认领任务，因为还有一名成员至今零提交，
  契约定了他才有活可领。**写这份文档的直接理由是：与 Cody-ic 辩定的十三条接口设计点
  目前只存在于 PR #23 的评论串里**，而「PR 评论会被后续讨论淹没」正是我在同一条串上
  对他说过的话——那句话对我自己同样成立。文档因此只放三类东西：别处没写的（十三条设计点、
  两条未决问题）、写在会被淹没的地方的（十条坑）、以及会被误读的（19 项待拍板里
  哪些其实不阻塞接口——结论是**一项都不阻塞**，前提是接口对「哪一侧由谁控制」保持中立）。
  **它有明确的时效性**：第 5 节两条问题一答完就过期、第 4 节合入契约文档后即冗余，
  所以 README 里写明「契约合入后即可删」——本项目吃过「同一件事写在两处然后漂移」的亏
- **`content_hash` 改取「规范序列化」，不再是「对文件文本做规范化」（6.3）。**
  原写法（LF + 去尾随空白）挡不住**缩进宽度、键的顺序、非 ASCII 转义**这三种同样会改变
  字节的差异，而它们的症状与 CRLF 那条**一模一样**——报「地图不匹配」，而看到它的人
  第一反应一定是去查仿真。改为「删掉该字段后按固定参数重新序列化再取哈希」，
  三种一次全消掉，LF 与无尾随空白成了它的副产品。**这条订正来自 @omg321-975 的实现**，
  规范跟着可执行的那一份走——它是我们第一次让文档去对齐代码而不是反过来
- **线性结构的 `d` 是「走向」不是「朝向」，并列进 4.2.1（新增 4.2.1.1）。**
  单位的 `d` 是面朝哪，墙 / 门 / 桥的 `d` 是**沿哪个方向铺**。这条不写下来，照 4.2.1 写
  前端的人会得到**一座墙线断开的城**，而且不会怀疑朝向——精灵没问题、锚点也对、
  每段单看都正常。城圈至少有两条互相垂直的边，所以「全部按同一朝向画」必然错一半。
  顺带写明两朝向素材的固有上限：拐角与孤立一格都没有正确答案
- **「保守 = 多报不漏报」不是一条全局性质，它取决于检查的极性。**
  评审 `tools/map_gen/` 时发现的：对「必须连通」型的检查（走廊可达、无孤岛、水不切断）
  少给通路 ⇒ 多报，安全；但对「**必须存在**一条通路」（初始城圈的缺口）与
  「**不得存在**一条通路」（森林遮蔽通道）两类，极性是反的——后者会**漏报**，
  而那条检查已经实现、当时是绿的。这条对 `rts_core` 同样成立：
  「斜向能不能穿角」这个移动模型约定现在已经有下游消费者在等
- **§4「任意位置建造」我第二次改立场：支持它，但必须补一条几何条件。**
  上一版我主张候选乙、并说 2.1.2 那条范围收缩用 §2 补——§2 已随 #23 重写作废，
  而第三案让 2.1.2 那个洞**根本不出现**（城外可建，「建新」原样保留）。
  但它在「封设防」那一格上仍是**部分**（森林压薄杀伤区，塔的数量仍随钱涨），
  按「结构封死，而非数值劝退」这条准则，「部分」就等于没封死——**那正是当初否掉候选甲的
  同一个洞**。补法用我们已经批准过的形式（第 8 节第 14 条那种）：
  「每个 `outer` 资源格到最近可建造格的距离 > 防御建筑射程上限」。
  补上之后第三案在每一行上都不劣于乙、在「墙类建新」那行严格更好。
  **「哪一簇值得围」这个决策在乙和补丁后的第三案下一样会消失**——只要「设防」被结构性
  封死它就保不住，而它换来的正是我们想要的：那个决策变成「哪一簇值得驻兵」，
  即「部分资源点在墙外」本来要保护的**野战必须发生**
- **三份提案的待拍板事项合并到 issue #26，那是唯一的表决场所。**
  分散在三个 PR 里讨论会散掉，而这个项目已经吃过「PR 评论被后续讨论淹没」的亏。
  议程做的最有用的一件事不是列清单，是**逐条定性**：18 项里只有 13 项今天该拍，
  2 项按组内明文规则今天不能定、3 项没有训练回路就是猜。对后 5 项，
  会上正确的产出是**负责人 + 触发条件**，不是数字
- **「一格画两张图」写进契约（4.2.1）。** 五种地形里只有 `Plain` 与 `Water` 是地砖（`kind:"tile"`、画布正好占满菱形、锚点在格心、不描边不投影），`Rock` / `Forest` / `Bridge` 都是立体物件、需要地砖底衬。**桥的底衬是 `Water` 而不是平地**，否则河在桥那一格视觉上是断的。叠加物件必须与单位混在同一深度序列排序（站在岩壁前的单位要遮住岩壁）。此前 4.2 写的是「渲染层读枚举决定画哪张地形精灵」——单数、一对一，照它写前端的人会去找 `Rock` 的地砖然后发现没有

**遗留问题**：

- ~~**`rts_core` 代码从未在 Linux/GCC 上编译过**~~ **本次已解决（#39，Cody-ic）**：Release + Debug 双配置 7/7 全绿、零警告，`-Werror` 全程开着、没降级任何开关，并验掉了 #24 里我靠推理消掉的两处跨工具链风险。**但这条不该就此划掉，而应改写成一条持续责任**——它暴露的四处**全部只在 Linux 侧可见，且其中三处的症状是「不报错，只是悄悄变差」**：Catch2 的 FetchContent 卡在 `github.com:443` 间歇不可达（已加 codeload 兜底 + `TIMEOUT 20`，安全性靠现成的 `URL_HASH`，我独立复现过两份归档 SHA256 逐字符相同）；`.gitignore` 漏 `build-*/`（Makefiles 是单配置生成器，Release 与 Debug 必须各占一个目录，而 VS 是单目录多配置，本地永不出现）；`*.sh` 被转成 CRLF 后 shebang 变成 `#!/usr/bin/env bash\r`，报错指向 `/usr/bin/env` 而那文件明明就在；Windows 上 `core.filemode` 默认 false 使本地 `chmod +x` 成为假动作。**「首次」做完了，「频繁」那一半从现在起是每个提交者的事**，`tools/server/` 两个脚本已入库、跑一次约两分钟。最该照做的是 ArLiangz——他要新增的代码面比谁都大，而攒一批再编正是 CLAUDE.md 明令要避免的
- ~~地图文件的换行会让 `content_hash` 跨平台不一致~~ **#21 已处理，两条都做了**：`地图与场景设计.md` 6.3 改为对规范化后的内容（LF、去尾随空白）取哈希（主，不依赖每个人的 git 配置），并加 `.gitattributes` 对数据与源码文件强制 `eol=lf`（副，零成本第二道防线，顺带解决 review 时整片显示为改动）。留下的规矩：**新增会被程序读取或参与哈希的文本格式时，要同时在 `.gitattributes` 里登记**
- **#13 遗留的四条精灵问题，三条已修、一条未修且被扩大**：
  - ~~`tile` 字段尺度差 2√2 倍~~ **已修**：现为 `[px_per_tile, px_per_tile/2]` = `[256, 128]`
  - ~~尚无任何地形精灵~~ **已补**：13 条地形条目（地面变体 / 障碍物 / 景物），标识符已对齐 4.1 枚举
  - ~~城墙水平占地约 0.60 瓦片、连续墙段留 40% 缝隙~~ **已修**：`modules: 1` 使 `wall-narrow` 沿长边正好占 1 格，相邻墙段无缝
  - **两条路线的元数据 schema 与文件名分叉——未修，且本次扩大了**。3D 路线本次又新增 `states` 与 `kind` 两层结构，占位路线 `gen.py` 仍是「全局单一 canvas + 全局 ground_anchor + 无状态」的老 schema，产物也仍是 `<标识符>_<朝向>.png`。占位路线的价值本是"无 Blender 也能跑前端"，**契约不通用则这个价值已实际失效**。要么让 `gen.py` 跟上（含地形与两种状态），要么明确宣布放弃占位路线、由已入库的 300 张成品承担该职责——**倾向后者**，但需小组确认
- **`地图与场景设计.md` 2.1 仍未定案，但已收窄到只剩一个问题。** 两人一致倾向候选乙（`buildable` 由城区定义 + 少量前哨点，`前哨点数 < 外部资源簇数`），理由见「本次决定」。已达成一致的：前哨点只许建采集与防御建筑、不许建墙与门；`Fence` 同样限制在城区与前哨内（理由是结构性的——木栅可在波次中即时放置且极廉价，能任意放就等于给了我们要封的那件事一个最廉价版本）；前哨点数量属待定数值。**剩下的唯一障碍是范围收缩**：城区固定 ⇒ 初始残破城圈必须落在城区边界上 ⇒ 「扩张防线」整个消失、墙类决策只剩「修旧」，而 CLAUDE.md 把建造阶段决策写成三方的「修旧 / 建新 / 造兵」。这条不该由两个人拍。另外若采用乙，校验器还缺一条「前哨点上摆不下足以围住该簇的塔」，判据要等前哨点与簇的形状定了才写得出
- ~~金币保底的单点脆弱性~~ **本次已处理**：领主堡垒恢复产出极少量金币作为兵力地板，见「本次决定」。数额属待定数值
- **本日志缺一名成员的行。** 仓库协作者为 4 人（Cody-ic / ArLiangz / zhxxx233 / omg321-975），而记录模板与首次记录只有 3 行，**omg321-975 此前从未出现在日志中**。本次已补上空行。日志是答辩「人员分工」页的直接依据，缺行等于该成员在个人成绩材料里不存在
- ~~**ArLiangz 与 omg321-975 仍无任何提交**~~ **本次已翻转，四人全部有提交与日志行。**
  ArLiangz 经 #25 接手 `rts_core`，**且范围扩为整个仿真本体**（契约 + 战斗 / 寻路 / 视野 /
  经济），理由是这四块与契约高度耦合、拆给不同人反而要多一轮契约谈判——这个理由成立，
  我不反对，但它带来一个后果：**仿真本体的整条关键路径串在一个人身上**，
  而 `bindings/` 等 `Sim` 能跑、`train/` 等 `bindings/`，RL 收敛要按周算。
  已在 #28 提出对策：**不拆人，拆 PR**——四块各自单独一个 PR、按
  `CombatSystem` → `Pathfinder` → 视野 → 经济 的顺序合，前两块一落地训练就能起步，
  且排期一旦吃紧只有已经拆开的那几块接得走。omg321-975 认领 `tools/map_gen/` 并已落
  #29 / #31 / #33 / #34。**分工由此变成：ArLiangz `rts_core` 全部、omg321-975 `tools/map_gen/`、
  Cody-ic 素材 / 设计 / 服务器、我倾向接 `bindings/` + `train/`——于是 `render/`
  成为最大的一块无人认领**（5–8 人日，且 #16 与 #23 §8 各要往里加一次），已进 #26 议程
- **未引用模型数量已变化**：`assets.json` 现引用 **38 / 103** 个模型（`9f8a65a` 给采石场加了 `stairs-stone.glb`），未引用 65 个（16.2 MB / 模型文件共 32.4 MB）。已逐个核对引用路径全部存在。打包时按 `assets.json` 过滤的要求不变，已写入 `tools/sprite_gen/README.md`
- 上次记录中「19 实体 × 4 朝向 × idle/move = 236 张」等表述已过期，现为 **35 实体、300 张**（含地形）
- 上次记录的两条本次已翻转，不再成立：**「仓库仍无 C++ 代码与 CMake 配置」**（#20 已落地基与双工具链 CMake）、**「`rts_core` 接口未定稿是后续一切工作的前置」**（改为分两层，与花名册/地形零依赖的那层已定死，见「本次决定」）。**接口契约那一层仍未定稿**——`step()` 签名、observation 通道、动作枚举、单位/建筑数据布局要等 #19 合入、花名册与地形枚举稳定后另开 PR
- **提案由两份变三份**（#15 / #16 / #23），待拍板事项已合并到 **issue #26** 并逐条定性。
  #23 本次被重写过一轮（原 §2 作废、原 §7 与 §8 合并成「堡垒等级」一条、新增 §1.4），
  我的议程随之对过账。**表决顺序的第 1 轮是 §1.4「等级提升哪些属性」**——它决定
  守方要不要一条等级轴，而我论证它有一个文档层面的答案（见「本次决定」）
- **`CLAUDE.md` 有一处内部矛盾要在契约那一版处理，而它当天就多出了第二个候选解**：
  「建筑与城墙走**与单位完全相同的战斗解算**」与「**破坏速率是兵种的固定属性，与等级
  无关**」不能同时成立——若同一套解算而等级又提升伤害，破坏速率就会随等级涨，
  「满级步兵一拥而上瞬间破墙」这条被明令封死的形态直接复活。**我主张破坏速率是独立于
  伤害的通道**（保住那把结构锁）。但 #23 当日第二次重写新增 §7.6，**给出了相反方向的
  解法且论证站得住**：墙可升级之后，只升墙 ⇒ 破墙时间 → ∞（龟缩复活）、只改速率 ⇒ → 0
  （三阶段中段消失），所以真正该守的不变量不是「速率恒定」而是「**破墙时间必须落在一个
  区间里**」——后者有标定靶子，前者在守方也有等级轴之后确实守不住原意。**两案的代价
  不在同一处**：我那案让 flow field 份数保持在兵种尺度的常数（`CLAUDE.md` 5.1 现行写法），
  §7.6 那案让它变成「兵种 × 等级档」，直接落在 ArLiangz 的寻路上。两条已一并挂进交接
  说明第 5 节与第 9 节，并注明**要一起答、不要分开**
- 其余遗留问题（待向老师确认类设计、组长姓名与中文题目）**均未变化**，见上次记录

**本次结算**：

| PR | 内容 | 状态 |
|---|---|---|
| #19 | 地形枚举补 `Water`/`Bridge`、资源模型三处订正、全套尺寸比例、资源建筑视觉分离 | 已合入 |
| #20 | `rts_core` 地基：双工具链 CMake、PRNG、状态哈希、确定性守卫、`.mailmap` | 已合入 |
| #21 | 文档一致性结算（#19 + #20 落地后的过期内容） | 已合入 |
| #24 | #20 复核的七处「该红却绿」修复 + 三道新守卫 | 已合入（复核十五项逐条实测通过） |
| #22 | 训练服务器环境实测，并订正「GPU 机时够不够」这个问法 | 已合入（我提的三处已改） |
| #27 | 素材三族重做、场景预览重写、`check_assets.py`，并修出两处前端契约缺陷 | 已合入 |
| #30 | 可破坏障碍改名（`Stump`/`Sapling`/`Rubble`）与独立成组、命名对照表 | 已合入 |
| #28 | ArLiangz 日志行 + VS18/2026 工具链补充 | 已合入 |
| #32 | omg321-975 日志行 | 已合入 |
| #35 | Cody-ic 日志行后半段 | 已合入 |
| #38 | 订正 `CLAUDE.md` 对构建类型的转述（#36），补四处开工必需的事实（#37） | 已合入，**连带关闭 #36 / #37** |
| #31 | `tools/map_gen/` 地基（地图 I/O、格子原语、67 条自检） | 已合入（我批准；两处订正归口到 #33） |
| #39 | 首次 Linux/GCC 构建（双配置 7/7）+ 服务器同步/构建脚本 | 已合入（我批准；独立复现了兜底归档的 SHA256） |
| #33 | `tools/map_gen/` 第 8 节校验器 8/15 条 | **我请求了改动**：第 10 条会漏报，已附可运行反例 |
| #34 | README 同步 map_gen 进度 | 需变基。**六处改动里三处会回退 #39**，已逐条标出 |
| #25 | `rts_core` 交接说明 + README / 日志 / 地图规范三处结算 | 随本 PR 合入 |
| #23 | 无尽模式与地形分层 | **提案仍待议**，8 个需拍板问题；本次被重写过**两轮**，第二轮提出城墙可升级 + 强制破坏速率随等级 |
| #15 | 守方 AI 与协同演化 | **提案仍待议**，6 个需拍板问题 |
| #16 | 演示与可视化清单 | **提案仍待议**，5 个需拍板问题 |
| #26 | 三份提案的表决议程（issue） | 开着，**唯一的表决场所** |

仓库现有根目录 **8 份文档** + 精灵流水线（Python）+ 确定性检查脚本 + `tools/map_gen/`
地基（#31 已合入）+ `tools/server/` 同步与构建脚本（#39 已合入）+ **654 行 C++**
（7 个文件）+ 约 330 行 CMake。**ctest 全绿，Windows/MSVC 与 Linux/GCC 各跑过
Release + Debug 四种组合。**

条数刻意不写——本次 #38 正是因为「现有 5 条」这个写死的数字过期而开的，
而它今天又从 7 变成了 8（#31 加了 `map_gen_self_test`）。**要数就 `ctest -N`。**

三份提案文档仍在未合并的分支上（#15 / #16 / #23），`tools/map_gen/` 的校验器
（#33）与 README 同步（#34）仍开着。

**本次上机后半段的形态变了**：上午还是「两个人在改文档」，下午四个人都有提交，
而且**同时开着的 PR 一度到 7 个**。由此显出两条协作上的教训，值得记下来：

- **栈式 PR（B 基于 A）合 A 时不要 `--delete-branch`。** 我合 #27 时带了它，
  而 #30 的 base 正是那个分支——base 一没，GitHub 直接把 #30 **关掉**了，
  且关掉之后连 base 都改不了。恢复办法是把提交推回去重建分支、reopen、改 base。
  **同一天我又踩了第二次**：合 #31 时照样带了 `--delete-branch`，而 #33 的 base
  正是 `tools/map-gen-foundation`，于是 #33 被关掉、CHANGES_REQUESTED 那条评审
  一起挂在一个已关闭的 PR 上。**教训不是「记住别带这个参数」——上面那行就是我自己
  两小时前写的，写下来并没有拦住我。** 真正的教训是它属于「**做之前查一下，
  而不是靠记得**」那一类：合并前一句 `gh pr list --json number,baseRefName` 就能看见
  谁以它为 base，而这件事**只有在合并前那一刻查才有用**。已改成合并前固定先查一次，
  本次合 #39 之前就是这么做的（确认全部 PR 的 base 都是 `main` 才动手）
- **同一份文档被三个 PR 同时改时，结算 PR 应当最后合。** 本次 `README.md` 被
  #22 / #25 / #28 / #34 四个 PR 同时改，`日志.md` 被 #25 / #28 / #32 三个改。
  我把自己的结算 PR 排在最后并承担变基，这样每个人只改自己那一行、冲突只由一个人解

下一件事仍是 `rts_core` 的**接口契约**：`step()` 签名、observation 通道注册表、
动作/命令分级、`WorldView`、回放格式。**它现在不被任何东西挡着**——花名册与地形枚举
随 #19 稳定、`ObstacleId` 的枚举成员名随 #30 定下、交接说明里那两条未决问题也消掉了一条。

**`render/` 现在是唯一一块无人认领的大活**——原先并列的 Linux/GCC 首次构建已由 #39
做掉。它 5–8 人日，且 #16 与 #23 §8 各要往里加一次，预算重估本身就在 #26 议程上。

它的麻烦之处在于**依赖接口、所以现在开不了工**，于是很容易被理解成「不着急」。
恰恰相反：接口一定，它立刻变成关键路径上最长的一条，而那时四个人都已各有在手的活。
所以**它该在接口定稿之前就有人认下来**，不是等能开工了再找人。

顺带一条与它相关的排期观察：今天四个人**全部有提交**了，但工作分布是
「三人做工具与文档、一人做仿真本体」。工具链这一侧现在相当扎实（双工具链实测、
地图校验器、素材流水线、服务器同步脚本），而**仿真本体一行都还没有**——
`rts_core/` 至今只有确定性地基。这个失衡不是谁的问题，是「接口未定稿」这一个瓶颈
造成的下游拥堵，也再次说明为什么接口契约是最高优先级。

---

## 2026-08-27（第 1 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | 确立项目选题与三层隔离架构；放宽第三方库约束，将关键不变量从"语言纯净度"重新定义为"热路径与确定性"；修正开发平台；产出兵种建筑花名册、克制二部图与侦查系统；分析交付要求对架构与排期的约束；评估后放弃 Unity，前端改为 C++ 图形库自绘；澄清内存布局与面向对象是正交问题，将实体存储约束的理由改挂在确定性上；搭建精灵生成流水线（程序化占位 + CC0 素材经 Blender 统一预渲染），并核实各素材站许可条款；据三轮评审完成 19 实体 × 4 朝向精灵成品并入库，建立前端尺寸契约 `_sprite_meta.json`，攻守双方视觉分离；全部单位补 idle/move 移动动画，蔽空弩楼与镇野箭楼剪影分离；据评审论证幽影窥使必须是地面单位并改素材落地 | `4c1de02` `f3ca7e6` `cd9d1b6` `d024ffc` #3 #4 #5 #7 #9 |
| ArLiangz | 待本人补充 | |
| zhxxx233 | 修正开发环境约定（本地纯 Windows/MSVC，不使用 WSL2）；补充老师要求的最终交付项清单并分析其对设计的约束；清理文档中登墙机制、单位计数、资源种类三处不一致；建立本日志；修正确定性论证中"堆分配即不确定"的因果错误；订正"交付物必须是纯 C++"这一过度推论；调研 Godot GDExtension / rbfx / Panda3D 等引擎方案后维持现有技术栈，并将"不使用游戏引擎"由硬性规则降级为带理由与重评判据的工程选择；三轮评审 #9 精灵流水线：实测出后处理脚本不幂等、锚点契约缺失、攻方剪影辨识度不足等问题，并订正自己上一轮不死鸟是唯一空中单位的错误判断；产出地图与场景设计规范（地形枚举、分层数据格式、走廊分类、寻路把墙当高代价可通行、校验器条目、训练图与演示图分工），并核实精灵侧四处影响地图渲染的契约问题（tile 尺度差 2√2 倍、两条路线元数据 schema 分叉、地形精灵缺失、城墙占地不足一格）；建立 README 作为新成员入场文档；结算全部待合并 PR；发现「训练时守方是谁」此前从未定义并产出守方 AI 与协同演化方案（分层镜像攻方、宏观层动作因子化到地图槽位、对手池、行为克隆做人类风格锚定、三个坑与排期）；产出演示与可视化清单（先定判据再排序，并明确列出不做的）；订正自己把集结点数量写成"定为 4"这一违反组内数值待定要求的错误，并把该要求写成 CLAUDE.md 的明文规则 | #1 #2 #6 #8 #10 #13 #14 #17、#15 #16（提案待议）、#9 五轮评审 |

**本次决定**：

- **游戏形态**：不对称波次生存，玩家为守方，RL 驱动攻方，无尽模式
- **架构**：`rts_core` / `render` / `game` / `bindings` / `train` / `tools` / `tests` 三层隔离，训练经 pybind11 in-process 调用 C++ 仿真
- **平台**：全员 Windows + MSVC 开发，训练在 Linux GPU 服务器；不使用 WSL2
- **前端**：**当前不引入游戏引擎**（Unity 方案当日提出后即放弃）——此为工程选择而非硬性规则，理由与重新评估的判据已写入 CLAUDE.md。`render/` 直接链接 `rts_core`，用 raylib + Dear ImGui 自绘，无 C ABI 与跨语言胶水。前端即正式交付界面，工作量按 5–8 人日估（**#16 指出该预算不含演示与可视化功能，需重估**）
- **实体存储**：扁平数组 + 整型 ID，禁止逐单位堆分配与虚函数分派；AoS / SoA 属实现细节不作规定。理由是从结构上消除"拿地址当 key"这类破坏确定性回放的写法
- **交付语言**：核心逻辑为 C++，但**不要求全栈纯 C++**。推理是否随包分发 Python 运行时属部署方式选择，第 3 周再定
- **命名**：单位与建筑花名册定稿，标识符规范为英文、≤7 字符、攻守不加前缀
- **资源**：守方三种资源（石材 / 木材 / 金币），各对应一条决策轴
- **地图与场景**：地图是 grid，地形只做三种（`Plain` / `Rock` / `Forest`），**不做地形高度**（高度只由城墙提供）。**每个集结点对应一条性质不同的走廊**（候选：开阔平原 / 隘口 / 林地 / 经济侧），否则选集结点没有可学信号——这是结构约束；**集结点数量本身属待定数值**，它等于走廊种类数，两者一起标定。**天然岩壁承担大部分周长**，只有走廊口需人工设防——这既让单位数上限下攻击密度足够，也实现了"防御建筑无法覆盖所有墙段"。地图**初始给一圈残破城墙并留缺口**，使"AI 是否利用已有缺口"这一核心指标第 1 波即可测。**外部资源点周围划禁建区并连通到地图边界**，从结构上封死"把外部矿场围进城"的龟缩解，而非靠墙造价劝退。尺寸不锁数字、只锁"`Ram` 行军占 episode 10–20%"。规范见 `地图与场景设计.md`
- **幽影窥使是地面单位，不死鸟是唯一空中单位**（结构性决定）。若幽影窥使会飞，防空建筑就多了"否定侦查"这一每波都稳定生效的用途，玩家无脑造 AA 即可——AA 的机会成本不再是两难（而这是本作智斗最可读的载体）、"空军价值 = 迫使玩家投入的 AA 资源量"这一评估指标失效、且侦查单位必须便宜到可消耗而空中单位必须高价稀有，两者正相反。视觉上幽影窥使贴地掠行、投影紧贴脚下，不死鸟高悬、投影远在下方，玩家靠间距判断能否用防空塔招呼
- **训练时守方也由 RL 控制**（方向已定，具体方案见 #15 待议）。此前文档从未规定训练时的守方是谁——若是固定脚本，攻方会过拟合到它的破绽，而头号指标"AI 是否发现并利用已有缺口"随之可疑：是学会了找缺口，还是背下了脚本总在哪留缺口。正式游玩时守方仍是人类玩家不变
- **凡关系到平衡性与玩家体验的数值一律保持待定**，等框架完善后用成本产出矩阵与自动标定一次性倒推。已写成 CLAUDE.md「关于数值」一节的明文规则，并给出两条守则：区分「结构约束」与「数值」；举例时明确标注占位值
- **美术路线**：不靠引擎提升观感，走**精灵预渲染**——3D 模型经统一相机与灯光渲成等距图，风格差异在渲染阶段压平（混用 2D 精灵做不到这点）。**只用 CC0 素材**，因交付要求源代码含输入文件、素材须随仓库提交。开发期先用程序化占位图解锁前端。精灵成品（19 实体 × 4 朝向 × idle/move，共 236 张）与尺寸契约 `_sprite_meta.json` 一并入库，避免每人各装一次 Blender；画布逐实体适配、世界→像素缩放固定，故相对大小始终正确

**遗留问题**：

- **待向老师确认（应在第 2 周结束前）**：本项目的类设计（系统层为类、实体为扁平数组）是否满足「系统设计报告」中"类对象设计"的要求。仅当老师明确期待"Unit 基类 + Archer 派生"这类实体继承体系时才存在真正冲突。原本还有一项"C++ 核心 + C# 前端是否满足课程要求"，随放弃 Unity 已作废
- `rts_core` 公开接口（`step()` 签名、observation 通道定义、动作枚举、数据布局）尚未定稿，是四人并行开发的关键契约，应最优先确定
- **凡关系到平衡性与玩家体验的数值一律保持待定**，等框架完善后用成本产出矩阵与自动标定一次性倒推，不逐个拍脑袋。含地图边长、城区半径、走廊口宽度与数量、**集结点数量**等。此条已写入 CLAUDE.md「关于数值」一节——地图规范曾把集结点数量写成"定为 4"，属把平衡旋钮当成结构结论，已订正
- **精灵元数据的不一致会影响地图渲染**（详见 `地图与场景设计.md` 第 7 节）：3D 路线的 `tile` 字段尺度差 2√2 倍（比例对、尺度错，肉眼看不出来，前端会把单位画大 2.83 倍）；两条路线的元数据 schema 与 PNG 文件名**都已分叉**——3D 路线的结构在三次提交里改了三次（全局 → 逐实体 → 逐实体逐状态 `states`），占位路线一次没跟。占位路线的价值是"无 Blender 也能跑前端"，契约不通用则这个价值实际已经没有了
- **尚无任何地形精灵**：19 个登记实体全是单位与建筑，地图目前无从渲染。`ground.glb` / `rocks-*.glb` / `tree-*.glb` 已在素材包内，加 3 条 `assets.json` 条目即可，不需改脚本
- **城墙精灵水平占地约 0.60 瓦片**（按取景公式反解，已用高度回算验证），仿真里占整格，连续墙段会留约 40% 缝隙。沿格子连续铺开的建筑应按占地宽度归一化，而非按高度
- **两份提案待小组讨论**：#15 守方 AI 与协同演化（6 个待拍板问题，其中"守方宏观层前期是否先写死脚本"与"GPU 机时够不够"影响排期）；#16 演示与可视化清单（5 个待拍板问题，其中"前端预算重估到多少人日"需要负责 `render/` 的人一起看）。两者都会实质改变范围，**讨论定了才好排后续的活**
- **ArLiangz 的日志行仍为「待本人补充」，且暂无提交记录。** 本日志是答辩「人员分工」页的直接依据，而该页是个人成绩的重要依据——这一行只能本人填，他人代填既不准确也失去意义。已建立 `README.md` 作为入场文档（导航、协作约定、环境准备、未认领工作清单），供其快速同步进度
- 组长姓名与中文题目定名尚未确定，四份 `.doc` 交付文档的文件名依赖此项
- 仓库尚未搭建代码骨架与 CMake 配置。已合入的 #9 是 Python 工具链，#13 / #14 / #17 是设计文档与协作约定，**仓库至今没有一行 C++**。现在就能独立开工、且不依赖 `rts_core` 的两块是 `tools/map_gen/` 与精灵元数据的四处修正
- **最终交付打包需按 `assets.json` 过滤 `models/`**：现有 103 个模型文件中仅 28 个被引用，未引用的 75 个（15.6 MB）不属于"输入文件"。仓库保留整包便于换模型，但交付包应精简。`交付要求的工程影响.md` 第 6 节尚未覆盖素材筛选

**本次结算**：

| PR | 内容 | 状态 |
|---|---|---|
| #9 | 精灵流水线（19 实体 × 4 朝向 × idle/move = 236 张） | 已合入 |
| #13 | 地图与场景设计规范 | 已合入 |
| #14 | README 入场文档 + 日志结算 | 已合入 |
| #17 | 集结点数量改回待定 + 「关于数值」明文规则 | 已合入 |
| #15 | 守方 AI 与协同演化 | **提案待议**，含 6 个需拍板问题 |
| #16 | 演示与可视化清单 | **提案待议**，含 5 个需拍板问题 |

仓库共 6 份文档 + 1 条 Python 工具链，**仍无任何 C++ 代码与 CMake 配置**。
`rts_core` 公开接口未定稿，是后续一切工作的前置。

现在就能独立开工、且不依赖 `rts_core` 的有两块：`tools/map_gen/`（生成器 + 校验器，
12 条校验里 7 条不含数值）与精灵元数据的四处修正。
