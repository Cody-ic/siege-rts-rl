# 课程设计日志

> 记录每次上机每名组员的进展情况，最终转为 `.doc` 交付（命名格式见 `课程设计最终交付项.md`）。
>
> **每次上机后当场补录。** `git log` 能补齐"改了什么"，但补不齐时间线、讨论结论与个人分工，这两样只能人工记。本日志同时是答辩 PPT 中「人员分工」页的直接依据，而该页是个人成绩的重要依据。

## 记录模板

复制下面这段追加到文末，新的记录放在最前面：

```markdown
## YYYY-MM-DD（第 N 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | | |
| ArLiangz | | |
| zhxxx233 | | |
| omg321-975 | | |

**本次决定**：

**遗留问题**：
```

---

## 2026-09-06（第 11 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | **新开 #142：攻方宏观层 + 编队制**（+1308 行，四提交）。`slots_cap` = 27 第一次有依据（SMAC 27m_vs_30m 对标 + 观测维度 18–21 倍 + 文献带外论证）；编队 = 「共享子目标」非「共享输出」（TStarBot2 ablation 等四个来源佐证）；编成第一次过攻方自己的迷雾看布防；顺手修掉 runner `build_ticks` 260/660 不一致——**此前所有跨日期 A/B 都被它污染过**。⚠️ 其陷落波读数（4/23/6）后被证实受预算 bug 污染，**正确值是 36/31/29（见 #143）**。<br><br>**同日续：新开 #143（#142 的续，13 提交）：PPO 首版 + 奖励管道 + 编队号进 World（World/13→14）+ 课程学习。** 先修 #142 的预算 bug（编队语义下一支编队的预算发给了队里每个成员，攻方前期凭空强 2.5 倍）；`kMaxUnitsPerEnv = 40` 过期两次 ⇒ 攻方约 70 个单位里 30 个存在、会挨打、但完全不受控且不被观测，编队号进 World 解决（散兵各自成队，守方侧退化为逐单位）；`World::Tally` 奖励埋点（`bld_value` 用重建成本、`scouts_killed` 单列、不进 state_hash）；吞吐实测 512 局 96 线程 **64 万 agent-step/s**（1e8 步约 43 分钟，机时不是瓶颈）；PPO 跑通前修掉四个静默缺陷（编成摆在空角落 / 无 episode 上界 / observe 传 nullptr 只 4 条通道非零 / rollout 缓冲常驻显存 3.1 GiB）；探索问题有算术（400 决策随机游走期望位移 9.6 格 vs 实际距离 50 格）⇒ **课程学习**按到 keep 距离五档、成功率升档（第 1 档 93%、第 2 档 2–4% 迁移中）。**训练还没收敛** | #142、#143 |
| ArLiangz | | |
| zhxxx233 | **跟进并合并 #142**：分支已含最新 main（零冲突），本地全量重建 + Release 59/59 绿；与 #140 交叉检查无语义碰撞（他动攻方窥使侧，与守方斥候「所见即报」独立）。评审意见落在评论。**#141 修订两票**：`slots_cap` 40→27（他的依据比我引的 CLAUDE.md 区间硬）、`phoenix_per_waves` 20→接受 10（均占位，归 §7 校准）；其余三票不变。<br><br>**同日续：跟进并合并 #143**：MSVC Release 全量重建零错误、ctest 59/59 绿；抽验两处关键声明属实（`wave_level` 单位数显式传入、`kWorldHashTag = "World/14"`——旧回放响亮报错而非静默错位）。**在评审评论里广播陷落波数订正**：旧数 4/23/6 是预算 bug 版读数，正确值 36/31/29（中位 31）——结论方向不变（无尽必败成立），但引用过旧数字的地方以此为准 | #142 已合并（`ba3fbb9`）；#143 已合并（`5551bd0`）；#141 评论 |
| omg321-975 | | |

**本次决定**：

- **#142 合并**（merge commit `ba3fbb9`）：攻方宏观层 + 编队制落地，「无尽模式必败」第一次实测成立（幅度过头，目标陷落波仍未定，下一轮分离三条改动各自的贡献再调）
- **#143 合并**（merge commit `5551bd0`）：PPO 首版 + 奖励管道 + 编队号进 World（World/13→14，旧回放不兼容）+ 课程学习。**陷落波数订正：36/31/29（中位 31），非 #142 正文的 4/23/6**；训练未收敛，第 2 档迁移中

**遗留问题**：

- **#141 尚待 Cody-ic / ArLiangz / omg321-975 三票**（zhxxx233 已投并修订）
- **#141-D（守方扩张 + 重建战损墙）未开工**：#142 实测最耐打的图第 10 波起塔饱和在 74 座——「塔位是构造时算死的常量」现在是活瓶颈，守方真的会输之后，扩张能不能救回来才是个有意义的问题
- 目标陷落波数全仓从未定过（交接 §3 第 1 项），#142 的幅度回调依赖它
- **PPO 训练未收敛**：课程第 1 档成功率 93%、第 2 档 2–4% 迁移中，下次跟进看曲线（#143）
- 两条斥候闪修复后版本仍待真人试玩目检（截图模式不可达）

---

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | | |
| ArLiangz | | |
| zhxxx233 | **仓库进度盘点与三 PR 合并协调。** 盘点：#138（Wraith 空中）、#140（侦查提示 + 召唤护栏）仍零评审；**组长新开 #139**（守方宏观决策层 `DefenderMacro`、两处波次死锁修复、波次曲线参数化、Gate 520→900，+4859 行），与我们的两个 PR 大面积同文件。merge-tree 实测冲突面：#138、#139 各自对 main 干净，#138 × #139 干净，**唯一冲突在 #140 × #139 的 `demo_driver.hpp/.cpp`**（#140 的 Summon 护栏撞 #139 的 `WaveTiming` 参数化 + `is_combat()` 清波判据）。**提议并按用户拍板执行：合并顺序 #139 → #138 → #140**，#139 落 main 后 #140 的 rebase 与解冲突由本行认领；评论里同时确认两处协同（#139 的死锁修复正是 #138 改空中后更突出的形状；Gate 900 恰好销掉 #138 注记里 Gate/Wall 比 0.43 的开放问题）。**三项核验任务托付组长**（评论已 @）：①Flak 视野否定覆盖率交 §7 校准 runner 在真实布局上量；②Wraith 精灵重渲需 Blender 环境（命令在 #138 评论），问他能不能跑；③地图 §4 林子几何只对地面单位有意义这条挂到下一轮地图复核。<br><br>**同日续：#139 落 main，按约定执行合并与 rebase。** 组长日间回话极快：#138 评审通过（他实测 #138 合到 #139 HEAD 46/46 绿）、托付的 ②精灵重渲他本机有 Blender 直接做完推到本分支（`5db4c57`，四朝向本体-投影间距 0.40–0.42 格自洽，他还自我纠正了「与 Phoenix 一致」那条过度约束的判据）、③同意立案；①Flak 覆盖率量出**结论级发现**：「视野否定半径」在代码里不存在（只有 `roster.hpp:86` 一句注释），且几何上 Flak 够不着窥使（差 8 格），真正病根是 `Phoenix` 上限从未放开——据此他开了提案 **#141**（攻方加强，主结论「别调 Σ√B」，待表决）。**本行执行**：① 顺手清掉组长报的 3 张孤儿贴图告警（`decal_*` 是手工素材、非流水线精灵，在 `check_orphans` 收集处豁免，与 `_` 前缀同例）；② **合并 #138**（merge commit `83ab525`）；③ **#140 rebase 到新 main**——组长预警得对：不只是文本冲突。文本面按「两边都留」合（阶段行「敌袭迫近/交战」+「建造 N 秒」并存、侦查面板用 #139 新签名、护栏撞 `WaveTiming` 参数化处取 `timing_.build_ticks` 并保留 `build_start_`）；**语义面：「斥候回报」触发改读 `scout_outcome()` 的 None→Success 边沿**（#139 把侦查改成二元死活判定，旧「第一次看见敌人」语义已不成立），并**新增第四种横幅「斥候阵亡：侦查失败」**（None→Killed 边沿；占位死亡率 400‰，失败是常态分支，「派了没回来」与「根本没派」必须可区分）；操作说明补两条（斥候掷死活规则、窥使「只在你的视野里现形，瞭望塔看得最远」——组长实测窥使停在 cheb 19 而守方静态视野最远 17，横幅不亮不是 bug 是 Watch 的报酬，但玩家得知道为什么）。**验收**：Release 全量 **59/59 绿**（#139 新增 `[macro]` 一条 66s；Debug 全量会超 10 分钟，逐标签排查确认是慢不是挂）；字形登记 395→415 码点；截图模式三轮扫描 + 临时探针确认**demo 对局不会自发产生斥候判定**（初始 7 兵无 Scout、交互局没有 DefenderMacro——侦查在交互局是玩家行为），两条斥候闪在截图模式不可达，靠 `[macro]` 测试 + 共享绘制路径兜底，真人试玩（训练斥候）目检；探针已撤净。<br><br>**同日二续：试玩反馈「斥候没看见敌军，面板却报出敌军编制」——定位 + 修复 + 推送（#140 第 4 个提交 `9ca38be`）。** 病根是 #139 的「快照取全波编成」在 09-02 分兵（主攻一路 + 佯攻一路）之后前提不再成立：「站在集结点上 = 整波都在」没了，斥候走到空集结点或佯攻路掷出成功，面板照样报全波。**修法两条**：①`tick_scout_recon` 改**所见即报**——到达后先数斥候看见圈内的攻方单位（chebyshev ≤ 视野**+3**，+3 是 `spawn_wave` 7×7 落位环半径；不加它斥候停在远侧时整支驻军都在圈外，探针第一轮就是这么抓到的），只报看见的那部分；**集结点空无一人时不掷点、不记账**（空营没人会发现斥候），继续往下一路走。②横幅「已看清敌袭编成」→「探到敌军集结」（部分情报下不 overclaim），`text.cpp` 登记串同步。**连带抓到并修掉一个脚本层真 bug**：探针实测波2 斥候全程钉在已探空的集结点旁原地踏步——集结点恰在视野圈沿上时迷雾 V/R 逐拍闪，「最近的不可见集结点」每拍重选会在脚边与下一站之间来回换；改为**巡逻承诺**（`PatrolGoal`，目标选定走到底、优先从未看过的集结点、世代纪律同 `ManualOrder`）。**必活用例改逐拍轮询到第一次 Success**（`scout_outcome_` 逐波重置，固定拍数终点会撞上波次边界——这次红就是这么红的，不是侦查失败）。**验收**：探针实测波1 t=649 探到主攻路 5 人、波2 斥候自行转移到 (35,44) 于 t=4017 探到；Release 全量 **59/59 绿**；字形登记 415→416 码点；截图冒烟正常（两条斥候闪仍需真人试玩目检，截图模式不可达这点不变）。这是**改动 #139 已合并的机制**，已在 #140 评论 @Cody-ic 报备并给依据（他自己的 CLAUDE.md「精确分兵是要花钱侦查的」条款：全波快照让佯攻零情报价值），请他事后评审，有异议可单独回退这一个提交。<br><br>**次日（09-05）续：#140 合并 + #141 表决。** 合并 #140（merge commit `034c057`；合并后 main 全量重建 + Release 59/59 绿复验）。**#141 五项表决本行已投**（评论有完整表）：①`phoenix_per_waves` = 20 且 `phoenix_cap` 1→4（线性 +1/20 波，远低于预算 46 倍增速的约束）；②`flak_target` 随**已观测**空军数走（记忆口径——真实数量口径是作弊陪练，与「所见即报」同一条原则）；③`slots_cap` = 40；④B 的占位策略放**宏观层**（波次级意图，与主攻方向同层）；⑤D 的触发 = 内环采集点全占且存量 ≥ 下一件计划支出时扩张、建造期检测到缺口且石材 ≥ 墙价 + 一座塔余量时重建。**D 视为前提项**（不先补扩张与重建，A/B/C 量的还是假守方，§2.7 的教训）。#141 尚待其余三名组员投票。 | 见 #139 评论；#138、#140 已合并（`83ab525` / `034c057`）；#141 已投本行一票 |
| omg321-975 | | |

**本次决定**：

- **合并顺序 #139 → #138 → #140**：冲突全部集中在 #140 × #139 的两个文件且全是 #140 自己的改动，殿后解冲突成本最低、且不占用组长时间

**遗留问题**：

- **#141（攻方加强提案）待组内表决**：五个参数（`phoenix_per_waves`、`flak_target` 情报口径、`slots_cap`、B 的占位策略层级、D 的扩张触发条件），组内成员各投一票——**zhxxx233 已投**（五项全投 + 四项提案整体赞成，D 视为前提项），待 Cody-ic / ArLiangz / omg321-975
- **窥使入境横幅可能整局不亮**（几何：窥使停在 cheb 19，守方静态视野最远 17）：组长建议接受为「瞭望塔的报酬」，操作说明已补一句话；试玩若觉得机制太隐，下一轮再议
- **两条斥候闪（回报/阵亡）在截图模式不可达**（demo 初始兵力无 Scout、交互局无 DefenderMacro）：靠 `[macro]` 测试与共享绘制路径兜底，**真人试玩时训练一只斥候目检**
- Debug 全量 ctest 会超 10 分钟（`[macro]` 60 波仿真在 Debug 下太慢，Release 66s）：验收一律跑 Release
- 地图 §4 林子几何条目挂下一轮地图复核（组长已立案）

---

## 2026-09-03（第 9 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| zhxxx233 | **试玩反馈两条：城外森林过密、木资源点贴图躺倒。两条都先定位再改，其中第一条定位的结果与直觉相反。**<br>①**「城外 forest 过于密集」的旋钮不在 `forest_patches` 上。** 先分阶段量：把 `paint` 拆开逐步统计争夺带（墙外 2 格到集结点环）里的阻挡格，`scatter_wild_terrain`（三形态：小撮 20–30 簇 + 林子 4–6 团 + 岩脊）在带内只落 **0.5–1.0%**，`place_water` 再加 0.2–2.1%，**剩下的 7–9% 全是 `topup_wild_terrain` 画的**——林子只放内环带与外环（大半落在带外），小撮铺满整张 170 格图（带只占其中一小块）。而补量的停止条件是「达到 `wild_blocked_fraction_min` 就停」，所以那个**下界不是一条「不低于」的护栏、它就是实际密度本身**：12 张池图的阻挡率一律落在 **10.0–10.1%**，一张不差。改 `forest_patches` 只会被补量原样补回来。⇒ 只改下界 **0.10 → 0.06**（strict 与 reference 两档同步，理由与「同步」这件事本身都写进 `_note_wild_blocked`/`_note_same_as_strict`：两档若分家，参考图与池图的城外观感会再次分道扬镳，同 `wall_hp_frac_range` 那次）。**上界不动**——它管的是「行军被地形主导」那一侧，从来没被碰到过。实测带内森林 7.5–9.5% → **3.3–5.5%**，阻挡率 6.0–6.1%。**丢弃率零代价**：同一批种子（`-n 30 --seed 501`）改前改后都是 **18.9%（37 次尝试）**、逐条否决计数逐位相同（第 27 条 5 / 第 30 条 1 / 第 31 条 1）——补量只画 `Forest`，而它可通行，`_min_path_width` 的口径是 `ground_passable`，所以隘口/连通那批检查根本看不见这次改动。**顺手把三处写死「10–20%」的转述改成指向 `thresholds.json`**（`generate.py` 的 `scatter_wild_terrain` docstring、`地图与场景设计.md` §2.2、`selftest.py` 第 26 条那组的 docstring），并在 `_note_wild` 与那条 docstring 里写明「实际密度来自补量、不是本组四键」——这次的定位过程本身就是一条会被重新踩的坑。`selftest.py` 那一组**不需要改判据**：三个铺法（0% / 16.7% / 50%）离两条边界都远，本来就是为了「调占位值时这一组不跟着红」这样设计的，只把 docstring 里那句复制来的数字换掉。<br>②**木资源点贴图躺倒 = 本地一份未提交的旋转版本。** `git status` 里 `decal_WoodPt.png` 一直是 `M`，`git show HEAD:` 取出来一比：HEAD 版 176×200（树立着），工作区版 200×176。把工作区版逆时针转 90° 与 HEAD 版逐像素比 `ImageChops.difference(...).getbbox() is None`——**零差异**，所以它不是另一张素材、就是同一张被顺时针转了 90°，`git checkout --` 即可。**顺带记一件事**：`build/render/中文路径测试/` 下那份拷贝也是转过的（`render/CMakeLists.txt` 的复制规则从工作区拿），而 `build-render/` 下那份是正的——两个构建目录不同步这件事本身不会让任何测试变红。<br>**已验证**：`selftest.py` 3217 条断言 59 组全过；12 张池图重生成（14 次尝试、丢弃 14.3%，文件名集合与改前**完全相同**，所以点名具体文件名的文档不用动）+ 参考图重跑，strict/reference 两档校验全过；MSVC Release 全量重编、`ctest -C Release` **58/58 全绿**；离屏截图逐张核对——城墙外那圈贴身「树墙」变通透、城圈轮廓能看清了，`decal_WoodPt` 树干朝下且与地形 `Forest` 的针叶林明显区分（PIL 7 倍放大确认）。<br><br>**同日续：幽影窥使（Wraith）改判空中单位（PR #138，组内决定）。** 移动机制与不死鸟同一套。「会不会飞」在代码里只有两处权威来源（`roster.hpp` 的 `is_aerial()` 与 `unit_behavior.cpp` 的 `mobility()`），寻路/实体占位/视野遮挡/防空选目标/AOE 豁免/观测打包全部泛型读取、自动生效——真改动只有两行；连带更新克制提示（地面单位已够不着它，克星改与 Phoenix 同组的「Flak + 墙上 Archer」）、obs_pack 注释（`AerialAliveFrac` 不再恒 0）、测试 6 文件（原拿 Wraith 当「破不了结构的地面载具」「会被森林挡视线的观察者」的用例由测试表伤害为 0 的 `Shade` 接任，数值逐项照抄；`flow_test` 新增一节反向钉住：同一条封死墙线 Shade 不可达而 Wraith 直线照穿）。**平衡复核**：成本产出矩阵无回归；新克制是位置性的（矩阵测不了），改跑闭式暴露窗口——单座 Flak 全直径飞越的生死临界速度 = 10 格 / 52 tick = **0.1923**，0.12 下一座 Flak 就是硬禁飞区（滞留 4.17s > 击落 2.60s），恰是原论证最担心的「无脑造 AA 每波稳赚」；故 **speed 0.12 → 0.20**（= Phoenix，单座只能重创剩 15 HP，硬否定需 ≥2 座重叠，AA 两难保留；附带建造期侦查行程 31.2 → 52.0 格）。血量 60 与造价 0 不动（2 发 Flak 即落保住「可消耗侦查」；加血会让单座 Flak 连重创都做不到，废掉新克制）。**视觉**：`assets.json` 登记 `lift: 0.8`（与 Phoenix 同一飞行高度——本体与投影的间距是玩家判断能否用防空打的唯一视觉依据，全体空军共用一个高度信号才不含混）；本机无 Blender，`out_3d/` 精灵待重渲（命令已留在 PR）。**文档**：CLAUDE.md「空中单位」整节、克制二部图、README、实力模型 §10、数值设计 §8、地图方案 §4 全部按日期补注、不改写历史结论；其中地图方案一条是新事实——Wraith 会飞后不吃林子遮挡，「看到布局前必须进入某种射程」对地面射程（Tower 7 < 视野 10）**结构上不可达**，「可被猎」自此只由 Flak 视野否定承担。**已验证**：MSVC Debug 全量重编、`ctest` **58/58 全绿**、`check_assets.py` 通过。<br><br>**同日再续：侦查阶段专属提示（PR #140，叠在 #138 上）。** 试玩反馈「前期侦查阶段仍会直接显示『进攻中』，感知不明显」——先定位再改：侦查博弈（`Wraith` 来探、`Scout` 去看）发生在建造期与开打后的行军窗口，而阶段行只有三态（建造 / 进攻中 / 败），唯一的侦查读数（右上「已侦查」面板）是**被动的**——它等你去看，不提醒你。方案先给四选一（阶段行文案细分 / 警报横幅 / 波次阶段条 / 屏外指引），拍板**警报横幅为主 + 阶段行二分**。落法：判定全在 `game/` 纯逻辑层——`enemy_wraith_sighted` **复用** `sighted_composition`，与侦查面板同一迷雾判据、不可能各自漂移；`combat_engaged` = 任一侧任一单位攻击四位亮起，拿 `World&` 但**不过迷雾不泄露**（攻击位亮着说明你的东西已在对手射程内，接触本就在你视野所及）。渲染侧：顶中两条横幅（窥使入境常驻、离开视野或被击落即灭；斥候回报每波首见闪 4 秒；窥使优先并吃掉回报闪）+「敌袭迫近 / 交战」替代「进攻中」（开打 ≠ 接战）。**两个坑都是目视验收抓的、测试一个都抓不到**：①警报状态最初绑在绘制上推进，而截图模式一步推 N 个 tick ⇒ tick 3200/4000 的截图里仍挂着早该灭掉的回报闪——状态推进拆成 `advance_recon_alert` 逐 tick 调，窗口与截图两条路径看到同一状态；②横幅首版与 HUD 同高，正好盖住第一行的 tick 与第二行的「主攻」方向提示（免费情报被遮是事故）——下移到 HUD 四行之下。**已验证**：`[input]` 新增两用例（警报三态：未探索 / 看见步兵 / 窥使入镜；交战远近两世界、单侧亮也算），全量 **58/58 全绿**；离屏截图逐态核对（t1000 回报闪、t1800 窥使入境且与面板同名单、t2000 敌袭迫近、t1500 交战、t1300 与 t3200 闪已灭），命令在 PR #140 描述里可复跑。**顺手补了 #138 两处被速度改动落下的注释**（`demo_driver` 的「0.12、能被追上」与 `demo_test` 的行程估算，纯注释），作 #138 第 6 个提交。<br><br>**同日三续：「第 2 波起建造时间直接没了」疑案——仿真无罪，N 键提前召唤是唯一直接路径；补上确认闪与 2 tick 护栏（#140 第 3 个提交）。** 试玩报告从第 2 波起建造期消失、直接「敌袭迫近」。**先做无罪推定式排查**：密集截图实测（波 1→2 每 10 tick 一张）显示波间有完整 260 tick 建造倒计时（「建造 237→37」逐帧递减），仿真与 UI 都正常。通读代码后确认：能让建造期瞬间结束的**唯一**路径是 `CommandKind::Summon`——`World::submit` 在 Build 期收到它直接 `begin_assault()`（`world.cpp:316-320`），而这是**设计如此**（CLAUDE.md 明文「必须提供提前召唤下一波」），绑定在 N 键上。旧 UI 两条路都显示「进攻中」无法分辨自然到点与提前召唤，新文案只是让这个隐形操作显形。用户确认没按住 N——而 raylib `IsKeyPressed` 是**边沿触发**（一次按压一发），但**上一波末尾连打 N 的最后一发可以溢出到下一波建造期的第 0~1 tick**，这正是「每波都秒没」且玩家无感的形状。**两个修补**（用户拍板都做）：① **确认闪**——N 被受理时顶中闪 4 秒「已提前召唤下一波 / 敌军即刻开拔」（`draw_alert_banner` 第三种样式，优先级最高；反馈与受理用同一条判据 `summon_accepted_now()`，不骗玩家）；② **护栏**——demo 层在建造期前 2 tick 丢弃 Summon（只挡连打溢出，正常按压不受影响；护栏在 demo 层而非 `World`，因为 RL 侧直接驱动 `World`、提前召唤是它的合法动作）。**确认闪同时是自诊断**：此后再遇到建造期消失——闪了 = 确有 Summon 提交（查键盘/输入源）；没闪 = 另有 bug 再深挖（届时嫌疑方向：暂停菜单里仿真是否照走等未读过的路径）。**已验证**：旧「提前召唤」测试在 tick 0 提交会被护栏吃掉，改为先 `update(3)` 越过护栏窗再提交（原断言全保留）；新增护栏测试（窗内丢弃 / 窗后受理，`summon_accepted_now()` 两端）；全量 **58/58 全绿**；字形登记经截图模式实跑验证（字体载入即校验，缺字会抛）。Summon 横幅无法截图验收（截图模式没有按键输入），靠与已验证两态共享同一绘制路径兜底，已在 PR 注明。<br><br>**同日四续：「建造期消失」结案——误诊为误触 N。** 用户复玩仍报「第 2 波起无建造显示」。静态穷举：`phase_` 全部三个写入点逐一核对，Build→Assault 仍只有超时与 Summon 两条路；窗口模式补 tick 每帧最多 5 tick、暂停清零，不存在快燃；play.bat 首选 exe 的二进制里查实含确认闪代码（排除跑错版本）。**遂加临时落盘诊断**（本地未提交，用后已撤）：每次阶段/波次迁移与每次 N 键写 `battle_trace.log`。用户再玩一局——**日志显示整局零次 N 键、每波建造期完整 260 tick**，问题未复现。结论按「此前局有零星 N 被受理（旧 UI 无反馈所以无感），本局知情后未误触」结案；黄色确认闪即为长期兜底，再复现时一眼可辨。诊断代码（`trace_tick`/N 键落盘，约 60 行）已 `git checkout` 撤掉、Release 重编回净。 | 见本次提交；PR #138（6 个提交）、#140（3 个提交） |
| Cody-ic | | |
| ArLiangz | | |
| omg321-975 | | |

**本次决定**：

- **争夺带阻挡率下界 0.10 → 0.06**（strict 与 reference 同步）。仍是占位值、仍待标定；上界 0.20 不动
- **两档的 `wild_blocked_fraction_min` 必须同步改**，写进两处注记：分家的后果是参考图与池图的城外观感不一致，而那本身就是 bug 级的误导（`wall_hp_frac_range` 那次的先例）
- **幽影窥使改判空中单位**（PR #138）：移动机制同不死鸟；CLAUDE.md 原「必须地面」论证作为已知接受的代价保留，换「侦查机可被位置性防空屏蔽」的直观克制读法。速度 0.12 → 0.20（单座 Flak 飞越生死临界 0.1923 的闭式依据），血量 60 与造价 0 不动；精灵登记 `lift` 0.8 与 Phoenix 同高
- **侦查阶段提示的形态：警报横幅 + 阶段行二分**（PR #140，叠在 #138 上）：窥使入境常驻警报 + 斥候回报每波首见短闪，判定落 `game/` 且与侦查面板同一迷雾判据；「进攻中」拆为「敌袭迫近 / 交战」（开打 ≠ 接战）。屏外指引（箭头指向屏外窥使）留作后续，试玩觉得还不够醒目再做
- **Summon 护栏 2 tick + 提前召唤确认闪**（#140 第 3 个提交）：护栏落在 demo 层而非 `World`（RL 侧直接驱动 `World`，提前召唤是它的合法动作）；确认闪的反馈与受理用同一条判据 `summon_accepted_now()`，避免「闪了没开打 / 开打了没闪」骗玩家

**遗留问题**：

- **`topup_wild_terrain` 是城外森林密度的实际来源，而这件事从代码结构上看不出来**：三形态那四个键（`forest_patches` 等）读起来像是密度旋钮，实际只贡献约 1%。已在两处 docstring/注记里写明，但**没有守卫**——若哪天补量被删或三形态被大改，那两句注记会静默过期
- 补量画的是 4–10 格的小团块且团间只留一格，所以低密度下形态偏「散点」而不是「连片」。第 23 条（4 连通块 ≥ 3 格）拦得住粉尘，但拦不住「一堆刚好 3–4 格的小簇」——森林「遮视野」那条职责主要靠林子（`woods_*`）承担，补量只是凑占比。若之后觉得掩体不够连片，应该调的是林子的数量与大小，不是再抬补量下界
- 上一轮（09-02）的遗留问题全部仍然有效，未处理
- **`out_3d/` 的 Wraith 精灵仍是贴地旧图**（`assets.json` 已登记 `lift` 0.8，但本机无 Blender）：与旧 `_sprite_meta.json` 自洽、游戏照常运行，只是「会飞」在画面上读不出来。需有 Blender 的环境重渲，命令在 PR #138 评论里；重渲后按「四朝向各帧本体-投影间距与 Phoenix 一致」目视核对
- **Flak 视野否定的实际覆盖率未验证**：Wraith 视野 10 > Flak 射程 5 ⇒ 单座 Flak 在几何上无法完全否定紧贴资产的侦查（全否定一个点是圆盘覆盖问题，约需 7 座环列），实战是走廊级部分否定。量级是否合理交 §7 校准 runner 在真实布局上验证，不是靠闭式拍脑袋
- **地图方案 §4 的林子几何要求自此只对地面单位有意义**：「侦查有价值、可被猎」那一行的地图侧论证（集结点到城墙直线上至少一片 Forest ≥ 6 格）管不住会飞的 Wraith 了——还要不要为 `Scout` 保留、要不要为 Flak 覆盖加地图侧条目，下一轮复核
- **「斥候回报」在一波内会因「视野清空 → 再看见」重复触发**（PR #140）：当前语义是「重新接触」，保留观察；试玩若觉得吵，收紧为每波只闪第一次（记一个 wave 号即可，改动在 `advance_recon_alert` 一处）
- **「建造期消失」已结案（同日四续）**：落盘诊断实跑一局，零次 N 键、每波建造期完整 260 tick，未复现。按「此前局零星误触 N、旧 UI 无反馈」定论；确认闪是长期兜底——再复现时**有闪 = 误触，无闪 = 重开此案**，届时先怀疑暂停菜单里仿真是否照走等未读过的路径

---

## 2026-09-02（第 8 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | **地图几何：边长与行军距离解耦（只落设计，代码未动）。** 试玩反馈两条——「初始城墙范围稍稍有点大了」、「整个地图对于无尽明显小了」。<br>**先量再改，而量出来的诊断与直觉不同**：12 张池图实测边长 144（半宽 72）、城区半径 12–15（中位 14），而**最远的城外资源只到离堡垒 28 格**（半宽的 39%），集结点在 70 格外 ⇒ **中间 42 格是纯空地**。所以「地图小」的体感来自**内容只铺在中间四成**，单纯把 `size` 调大只会让那条空带更宽、行军更久。<br>**随后算出一个「天花板」并向组长报告**：`Ram` 速度 0.045 格/tick 与 episode [1200, 2400] 一钉死，第 5 条（行军占比 ≤ 0.75）把边长封在**约 188** 以内——200 时占比区间 [0.79, 1.57] 与判据无交集、每张图都会被否决。<br>**组长当场指出那个天花板是假的**：「没人规定集结点一定要在地图边缘，也没人规定敌军意图只能是直奔堡垒而去」。核实后确认这话是对的——钉着它的是校验器**第 2 条**（集结点距地图边界 ≤ 4 格）与**第 1 条**的文案（「§3 的边长由『城区半径 + 集结点到城墙的距离』推出」），而第 2 条自己写的理由是「集结点跑到地图中间意味着攻方在城边上凭空出现，**侦查与行军时间窗口**一起失效」——**那两个窗口分别已经由第 5 条（行军占比）与第 14 条（禁建环 13 格）直接管着**。「贴地图边」只是在旧的「边长 = 走廊 × 2」模型下与「离城够远」恰好等价的一个**代理**，边长一旦独立两者就分开，而代理那一侧没有任何设计理由。按第 5 条反解，集结点到城墙的距离合法区间是 **[≈15, ≈81] 格**（下界由禁建环 13 与占比下限 0.10 兜着），现在是 55——**中间还有很大余量，天花板是自己造出来的**。<br>**定版为方案 A（组长选定）**：集结点内移（不再贴地图边）、地图往外放大，**行军距离与训练侧一元都不动**（episode 长度、`Ram` 速度、第 5 条区间、第 14 条全不改），改的只有第 2 条的判据。多出来的环带是**争夺带**——外部资源簇、野战、玩家前哨，正是「部分资源点在墙外」与「拐点反复出现」两条既有设计需要的空间。<br>**目标取值（三个数一起改，缺一个都会让另外两个变差）**：`size` 144→208、`city_radius_range` [12,15]→[10,13]、`outer_cluster_span` 13→42（内容铺到离堡垒约 55 格）、`size_max` 160→224、`spawn_edge_distance_max` 作废换成「到城墙距离」的上下界。集结点到城墙保持 55 ⇒ 第 5 条占比 [0.51, 1.02]，与今天**逐位相同**。<br>**文档落点**：`地图与场景设计.md` 新增 **§3.0**（解耦的完整推导 + 动/不动对照表 + 迁移清单）、§3 量级表末行那条推导标为作废、§2.2 集结点那条、§8 第 1/2 条判据与理由、§9 新增「下一轮的目标取值」（含实测诊断数字）；`CLAUDE.md`「集结区」开头那句与走廊作废那条。**按组长要求本轮只写文档、不写代码。**<br><br>**同日续：资源经济体检（`数值设计与成本产出矩阵.md` §11）。** 组长问「现在三个采矿建筑能建在资源点上、资源点不能被破坏、建筑可以被破坏，这套机制离智斗更近还是更远」，并要求「仔细算算资源点应该怎么分布」。**结论：机制的形状是对的，病根在它周围的三个数字。**<br>**先把对的部分和错的部分分开**：不可破坏的资源点 = 一个双方都知道的**稳定目标**，而智斗的前提是可预判（AA 摆哪、要不要出城都要求能预判攻方会去哪）；若资源点能被永久摧毁，地图单调退化、同一个点不会被争第二次，「攻其必救」失去承载物。拆建筑 = 效率交换而非二元胜负，正对上「一波未突破不等于失败」。**这两条要留。**<br>**但三个数字把它压成了无后果的 DoS**：① 采集建筑回本期只有 **0.33–0.54 波**（`Lumber` 40石30木 / 产 14/周期 ⇒ 0.33 波），被拆一座玩家全部损失只有 **88–116**（重建 + 停产），半波挣回来 ⇒ 「被拆」不构成需要提前防范的后果，而智斗的载体恰恰是「因为怕后果所以提前做布局承诺」；② 攻方拆一座 500hp 采集建筑：`Ghoul` 115s、`Knight` 71s、`Phoenix` 26s、`Ram` **11s** ⇒ 只有 `Ram` 划算，而它是全场最慢 + 头号集火目标，派它去远处矿场等于放弃本波破墙 ⇒ **理性攻方不该骚扰经济**；③ **城外产出与城内完全相同**——`tier` 只影响 `unlock_wave`、不进产出（产出来自 `BldStats.income_amount` 这个逐建筑常数），于是城外那一座风险更高、回报相同，**是严格劣势的选择**，「被迫出城」纯粹是被「城内钉在 1 石 1 木 1 金」推出去的，不是被城外拉出去的——一个选项严格更差的「决策」不是决策。再叠上 #122 移除「不可围墙」后**围一簇一次性买断**（实测 32 段墙 = 960 石），长期最优解是「围一次从此不用再想」，那会把一个反复出现的决策**永久删除**。<br>**顺带核实出三处其它机制与智斗不符**（都查到代码行）：`phoenixes = wave >= 3 ? 1 : 0` **空军恒 1 只** ⇒ CLAUDE.md 称作「本作智斗最可读的载体」的防空两难**无可预判**，造够 1 座 `Flak` 即完事；`spawn_wave` 按 `spawns[n % spawns.size()]` **轮转**派点 ⇒ 各集结点兵力≈相等，**分兵佯攻结构上不可能**，而我 09-01 才加的「免费方向提示」因此由整数取余决定（**实现了消费侧、没检查生产侧**，记作一例）；全仓搜索**没有任何 UI 展示克制表或来袭编成**，而 CLAUDE.md 明写「克制必须在 UI 中完全透明」「玩家必须能快速读懂来袭编成」——迷雾落地后玩家侦查到的编成也没有面板可读，只能数精灵。<br>**分布算法（把「簇数」算成主难度旋钮）**：一切折成「1 级 `Archer` 当量」（`cost ∝ 战力` 已是结构结论）。城内保底 = **9.1 当量/波恒定**、每簇 ≈ **11.4 当量/波**、攻方 = `6w^1.25` ⇒ 「第 w 波要几簇才平手」= `(6w^1.25 − 9.1)/11.4`：第 10 波要 8.6、第 15 波 14.8、第 20 波 21.5。**现有 7–8 簇 ⇒ 玩家最多跟到第 10 波**——而 7–8 这个数是被 `place_outer_clusters` 的「8 方位各一簇」实现上限钉住的、不是标出来的。反解出需求曲线近似**每波多要一簇**，而**解禁必须慢于这条线**（否则场场全胜就能永远跟上、必败没了），快慢之差就是中位存活波数——CLAUDE.md 那句「主要难度旋钮是解禁节奏与强度曲线之比」由此变成两条可比的曲线。取「第 3 波起每波开 1 簇」⇒ 缓慢掉队，**地图要备 16–20 簇**。几何可行性：208 格地图上争夺带（离堡垒 17–55 格方环）面积 10944 格，18 簇占 8.1%，簇心间距约 32 格 ⇒ 装得下且余量很大，**瓶颈从来不是面积**。<br><br>**同日三续：侦查面板落地（写代码）+ 人口上限严肃推导（只写文档）。**<br>**面板**：组长指「迷雾把编成藏起来了、侦查到之后没有面板可读，这个现在就改」。新增 `game::sighted_composition`（**过迷雾，判据与 `BattleScene::sorted` 逐字相同**——两处若不一致，面板就把「编成构成」从「需侦查」偷偷挪回「免费」那一列）与 `game::counters_of`（`CLAUDE.md`「克制二部图」的玩家侧读数，**不是 N×N 倍率表**，倍率仍只从三条正交轴的机制来），`render::draw_intel_panel` 画在右上角、**看不见就整块不画**（空面板会读作「确认无敌人」）。守卫做成**与机制交叉核对**而不只是「填了字」：「`Spear` 克 `Knight`」要求 `anti_charge_permille > 1000`、「只有 `Flak` 与墙上 `Archer` 能对付 `Phoenix`」要求它真是空中单位且 `Archer` 真是远程、「`Wraith` 被机动单位猎杀」要求列出的单位速度 ≥ 它的 0.8 倍；另有一条 `[demo]` 把**面板报出的与绘制列表画出的逐类对齐**，且在两个时点各比一次（只测「看不见」那一头会让「面板永远返回空」也通过）。截图验收：tick 850 出现「已侦查 / 亡灵步兵 ×6　克：戍卫弓手/镇野箭楼」，tick 1400（第 2 波刚生、全在迷雾里）整块不画。<br>**人口上限**：组长要求「严肃研究」。**先定它的设计职责**——CLAUDE.md 说「人口上限管数量、金币管质量」，所以它不是防超载的护栏、是**制造取舍**的装置，于是唯一的结构要求是「**上限必须咬得住**」（低于「全部金币买 1 级兵能养的数量」）。算金币流量：只有 `Mine`(11/周期) + `Keep` 地板(2/周期)，城内恒 1 座 ⇒ 196 金/波 ⇒ 留存 0.75 下**稳态只能养 13 个**。而原占位 `20+4K` 在 K=1 时是 24 ⇒ **城外拿到 4 座 `Mine` 之前一次都碰不到**，是装饰品——与「建筑等级上限」当初 hp 系数为 0 时空转**同型**。对比四个候选后取 **`8+2K`**：每一档都咬得住、K=12 时 32 仍低于攻方 20–40 那条可读上限、开局 7 兵 + 上限 10 还能加 3（这个紧是故意的，让堡垒轴从第 1 波就有压力）、斜率比金币流量慢所以取舍不会中期自己消失。<br>**推导过程中查出一个必须先修的前提**：`World::train_cost_gold` 是 `cost_gold × level`（**造价 ∝ L**），而战力是 `1 + 0.22(L−1)`（**仿射、不过原点**）⇒ 每金买到的战力从 L=1 的 1.00 掉到 L=12 的 0.29，**堆量严格占优、优势渐近 4.5 倍**——正是 `budget_curves.py`「结构结论一」定义的「宏观层决策塌到角上」。根因是曲线文档 §3 把**「线性」与「正比」混为一谈**（与 §1.4 那次翻车同一类错误，都是「线性」用松了）。**而 `budget_curves.py` 的结构检查没抓到它，因为那条检查里写的是 `cost = base × (1 + k(L−1))`——它在检验自己的假设、不是游戏的实现**，与第 14 条那次「手写 8 而表里是 12、结构约束绿着失效」同型。不修这条，人口上限会从「取舍」变成「战力天花板」（顶住后升级效率只有 1/4.5），守方战力会撞墙而不是继续爬坡。<br><br>**同日四续：组长要求把造价那条「严肃探索一下」——探索的结果推翻了我自己上一轮给的修法。**<br>①**先把「战力」这个词查清楚**：我上一轮用的 `战力 = 血×伤 = B(L)` 是**单个单位**的强度，而「一支部队多强」取决于**有多少人能同时交战**（兰彻斯特：隘口 α=1 ⇒ 强度 ∝ N¹·(血×伤)、开阔地远程 α=2 ⇒ ∝ N²·(血×伤)）。等价条件因此是 **造价 ∝ B(L)^(1/α)**，而不是我说的「造价 ∝ 战力」——**那条只在 α=1 成立**。⇒ `budget_curves.py`「结构结论一」隐含假设了 α=1 且没写出来，这与 §1.4（隐含「每属性线性增长」）、§3（「线性」当「正比」）是**同一族**错误，**三次了**。<br>②**用真实公式跑等成本对拼**（同兵种镜像、集火、width = 每拍能同时开火的人数）：现行 `c∝L` 三种形态里杂兵**全胜且碾压**（20 打剩 13–20）；`c∝B`（我上一轮的修法）杂兵**仍然全胜**，只是隘口那档收窄到 +2 ⇒ **它只在隘口附近接近中性、开阔地照样一边倒**；`c∝√B` 是**唯一一条「谁赢取决于形态」**的曲线（隘口精英 +4、开阔地杂兵 +7）。<br>③**攻城场景（把 AOE 这个「量」的天敌加进去）**：24 份预算砸 1200hp 墙、守方 1 箭楼齐射 + 2 墙上弓手。**现行曲线不是「偏向量」，是一道悬崖——L≥3 的编成根本打不破墙**（L=1 破墙 287t，L=3/5/8 全部失败）。玩家但凡买过一次高级兵就会发现它连墙都打不动，此后永远不碰等级 ⇒ 「兵种等级上限」这个输出不是弱，是**不可用**。<br>④**指数扫描**（造价 = base·B^p）：中性点在 **p≈0.70**（L=1/3/5 都是 287t）；p≥0.85 量占优、p≤0.60 质占优。<br>⑤**建议取 p=0.5（造价 ∝ √B）**，理由不是「中性」而是**它最贴合设计原话**：CLAUDE.md 要的是「防线均匀则宜集中凿点、防线有薄弱段则宜铺开找洞」——要求的是**答案随局面翻转**，而不是永远等效。三条曲线里只有 √B 做到（凿点用质、铺开用量）。代价是等级变便宜（L=20 只要 2.28 倍造价却有 5.18 倍战力），挡住它的是**另外两条上限**⇒ **造价曲线 / 人口上限 / 兵种等级上限三件必须一起落地**，只改其一会走到另一个角上。<br>⑥**顺带查出攻守两侧跑着两个不同的造价模型**：攻方 `wave_level` 反解假设人均造价 = base·B(L)（p=1），守方 `train_cost_gold` 实际按 `× level` 收钱。两侧本来就是不同货币、不要求数值相同，但「cost ∝ 战力」这条结构结论**只在攻方那侧成立**，且没有任何检查在管。改守方造价时要一并决定攻方那条反解跟不跟着改——若攻方也走 p=0.5，`总战力 ≡ 兵力预算` 这条 PR #124 特意建立的构造性等式会不再成立，**不能顺手改**。<br><br>**同日五续：「敌人从四面八方来、一点压迫感都没有」——量化后主因不是城太大。**<br>把「压迫感」精确成**受攻面上每格墙摊几个攻方单位**（受攻面 = 被压的那条边，2R+1 格）：现状第 5 波 **0.09 人/格**，即每 11 格墙才站 1 个敌人。拆开看两个成因的贡献：**集中兵力 ×4、缩小城区 ×1.5** ⇒ **主因是 `spawn_wave` 的轮转派点，不是城太大**（虽然两条都真）。<br>①**已改代码：兵力集中**。`spawns[n % spawns.size()]` 轮转 ⇒ 3–4 路恒等，改为**主攻一路拿七成 + 佯攻一路**，主攻方向按波数轮换（不掷点，demo 的确定性不依赖随机；轮换保证玩家不能靠「永远守北面」蒙混）。**逐兵种分配而不是按 roster 下标切一刀**——切一刀会把排在后面的 `Ram` 整体推给佯攻，而它是破墙主力必须随主攻；`Wraith` 反而走另一路（它的活是看清防线，挤在主攻里看不到别处）。落位偏移环从 5×5 扩到 7×7（集中后单点要摆下几乎整波人）。**顺带修好两件本来就该成立的事**：分兵佯攻此前结构上不可能、以及 09-01 加的「免费方向提示」此前由整数取余决定。判据取「主攻占过半」而不是「恰好七成」（比例是旋钮、有没有主攻是结构）；**破坏性验证**：改回轮转后当场红在 `top*2 > total`（3 vs 6）。<br>②**正面总长超出本仓库自己的上限 1.5–1.9 倍**。第 9 条要求正面 ∈ [24,64]，而 2026-08-31 重构后整圈都是 `Wall` 实体 ⇒ 正面 = 周长 = 8R：R=12–15 给出 96–120。手写参考图用 R=8（周长 64，正好压线），**池图全部超标**——**而第 9 条是阻塞状态、从来没跑过**（「正面」依赖城区、城区无法从现有字段推导），所以这个违反一直是隐形的。⇒ §9 的目标取值从 [10,13] **再下调到 [8,10]**。CLAUDE.md §1 末行其实早就预言了这个形态：「一座周长 150 格的城对 40 个攻方单位而言太稀，攻防会退化成散兵游勇」。<br>③**「巷战反克枪阵」在地图上没有任何触发条件**。CLAUDE.md §1 把它列为「只能靠地图几何满足或违背」的要求，需要「开阔地与狭窄地形同时存在」；实测 12 张池图**城内空地占比 98%**（中位只有 13 格阻挡物）⇒ 骑士在城里照样跑满 4 格助跑拿 1.7x，枪阵那 3.2x 反冲锋永远等不到前提。**机制在、数值在，缺的是几何**，而成因正是 §2.2 自己那条「城内不生成 Forest/Rock」。三个候选（城内撒阻挡物 / 靠玩家建筑形成街巷 / 承认巷战发生在缺口）**尚未定**，倾向后两条（都不动生成器）。 <br><br>**同日续：攻守实力模型重建（`攻守实力模型与平衡分析.md`，只落文档）。** 组长要求做攻守平衡审查；我先按「HP×DPS 当战力」给出了一版结论（攻方人均战力第 2 波 2.0×、克制第 2 波反转），组长两次追问后查出**货币错了**：等级各开一份平方根 ⇒ 一波兑现到场上的血池与火力是 Σ√B、不是 ΣB（现行曲线第 5 波兑现率 43%、第 20 波 32%）；攻城不是消耗战而是入口处的赛跑（`Ram` 活着到门下 → 塔火下撑过破门 → 进城砸堡）；两侧兰彻斯特指数还不一样。按正确货币重建模型（§1，聚合量、0.25 s 步进、照 demo 双方脚本的行为规则）后：① 拦住 `Ram` 所需塔数 ∝ 它的 B(w)，现行曲线第 3 波 7 座、第 10 波 15 座，而守方每门每波只能加 0.85 座——**旧曲线复原表 6 弓手 + 5 塔守到 20 波以上，正好复现「守方发展后太强」那条观察**，新旧两条曲线在这个货币里各在一个极端；② 多波敏感性：弓手集中到主攻门是唯一的大杠杆（陷落波 3 → 6 → 10），分配甲/乙 +0/+1，地图与节奏项（集结点距离、城区半径、编队、AOE 倍率）与中后期旋钮（维修价、人口上限、收入增长）**零影响**；③ 「只买人」永不破门 ⇒ 现行难度全部来自等级曲线把 `Ram` 抬过塔的阈值；④ 门一开这一波就输——内城无第二道线，「缺口攻防」阶段不存在。**既有文档里由旧货币推出的疑似误导内容列在该文 §8（十条，含 `budget_curves.py`「总战力 ≡ 兵力预算」、CLAUDE.md「硬顶只改分配不改难度」、§11.5 的簇数量级），按组长要求本轮只记不改。**模型脚本已入库 `tools/balance/siege_race.py`（四种模式复算全部表格）；无渲染 runner 校准是下一轮（§7）。 <br><br>**同日再续（PR #127）：地图生成器大改方案 + 金币冗余 + 事实分类。** 组长两条：地图生成器要大改（既含 §3.0 的尺寸解耦，也要解决「地图只是攻城战、不体现智斗」）；买活要求金币有冗余，实际的实力约束应当是石材 vs 人口上限。落成：① `地图生成器大改方案.md`（提案）——五条实测事实合成「一切在门前 5 格决定」的诊断，把 CLAUDE.md 里靠地图承载的设计要求翻译成可生成可校验的几何（路线性质可量出、隘口与开阔并存、林子遮视野、逐簇解禁、路过约束、入口异面），四个环的骨架（size 由 R/D/W_far 推导），逐项改动与 7 条新校验、键变更清单、四条配套代码项、七条待拍板决定项——**D 与集结期绑在一起是最大待定项**，实力模型说 D 对陷落波零影响但它决定弓手调兵窗口；② `攻守实力模型与平衡分析.md` §6.1 金币冗余：ρ = 金币收入 ÷ 稳态补员开销，城内 ρ≈0.65–1.1 只够不缩编、一座城外金矿 ρ≈1.75 正好够买活（买活受人口上限约束 ⇒ 一次最多补阵亡数），上界 ρ≈2.5 由人口上限 + 买活递增价封而不是压收入；造价 ∝√B 是金币不绑手的前提；③ 同文 §10 重写：把此前的事实清单按「与实力模型的关系」分 A/B/C 三类——侦查不可猎杀、集结期不存在、金≥石是 A（独立、模型反而抬高优先级）；编成位不等价、解禁逐点、簇数上限、到达离散、河桥、维修价、塔覆盖、金币无处可花是 B（事实留、取值或方向重评——其中「55 应缩到 30」方向收回，「金币无处可花」按买活框架不是缺陷）；野战几何整块转到大改方案。**全部只落文档。** | 见本次提交 |
| ArLiangz | | |
| zhxxx233 | | |
| omg321-975 | | |

**本次决定**：

- **地图边长与行军距离解耦（方案 A）**：集结点从「贴地图边缘」改为「离城墙一个规定距离」，边长成为独立的一元。**校验器第 2 条的判据随之改写**（「距地图边界 ≤ N 格」→「到城墙的距离落在区间内」），第 1 条的理由文案跟着换。**训练侧一元不动**：episode 长度、`Ram` 速度、第 5 条占比区间、第 14 条全不改——这是选方案 A 而不是「放松第 5 条」的全部理由（那条区间已经因为 72→144 被放宽过两次）
- **目标取值**：`size` 208、`city_radius_range` [10,13]、`outer_cluster_span` 42、`size_max` 224；集结点到城墙保持 55 格
- **多出来的环带定位为「争夺带」**，不是留白：外部资源簇 + 野战 + 玩家前哨
- **集结点环之外那一圈本轮按「留白」办**，不铺远簇（另一个候选是放最晚解禁的远簇、让玩家后期必须穿过或绕过集结区，更有嚼头但容易做成「必须打一场打不赢的仗」，需实测，记进 §10）
- **攻方「打外围经济」不写脚本，留给 RL 涌现**（详见下方遗留问题第 2 条，它已从「遗留」改记为「决定」）
- **「资源点不可破坏 + 采集建筑可破坏」这条机制保留**：它给出一个双方都知道的稳定目标，而可预判是智斗的前提。要改的是它周围的数字，不是它本身（`数值设计与成本产出矩阵.md` §11.1 / §11.3）
- **簇数是主难度旋钮，且当前被实现上限钉死**：`place_outer_clusters` 的「8 方位各一簇」把上界锁在 8，而算下来要 16–20 簇才够撑到第 20 波。解禁节奏必须**慢于**「每波多要一簇」这条需求曲线——快慢之差就是中位存活波数

**遗留问题**：

- **代码一行没动**，上面全部是设计。迁移清单写在 `地图与场景设计.md` §3.0 末段：校验器第 1、2 条的判据与文案、`place_spawns` 的摆位、`thresholds.json` 五个键、12 张池图与参考图重生成、`selftest.py` 相关断言、README 速查表
- ~~**「敌军意图不只是直奔堡垒」这一半没做**~~ **已定：不写脚本，留给 RL 涌现**（组长当日决定）。现状是 `demo_driver.cpp` 的 `issue_actions` 让所有攻方单位走 flow field 奔 `keep_pos()`，外围经济从来不是目标；而 CLAUDE.md 明写「攻其必救」「打哪一种资源」「空军点掉偏远矿场比撞墙划算」。**组长定为不在占位脚本里补它**——那本来就是「图里没写的（包夹、佯攻、择机）才留给 RL」那条判据的适用面：「打哪一种资源」是目标选择，正是 RL 该学的东西，手写脚本反而会把它固化成套路。**这条从「遗留」改记为「决定」**：它不是漏了，是选了让它涌现。<br>**但有一个前提要一起认下**：奖励侧必须给得出信号，否则涌现不了——CLAUDE.md 已写「摧毁建筑的即时奖励 = 该建筑的重建成本」，而本日 §11 算出那个重建成本只有 88–116（半波就回本），**信号强度可能不足**，见下条
- **`outer_cluster_span` 42 是从「争夺带该有多深」倒推的占位值**，没有实测过簇的实际观感；且它与「小聚居偏松」那条（`_OUTER_CLUSTER_RADIUS=4` 允许同簇两点相距 8 格）耦合——争夺带一变深，簇的紧密度更值得一起复核
- **§11 查出的五条都只提出、没实施**，其中三条动的是契约面、必须团队一起过：
  1. **城外产出应当 > 城内**（现在完全相同 ⇒ 冒险无回报）。要么给地图格式的资源点加一个倍率字段、要么在 `World` 里按到 `keep` 的距离算——**两条都动跨模块契约**
  2. **采集建筑回本期该从 0.33–0.54 波拉长到 2–3 波**（提造价或降产出）。它同时决定「摧毁建筑的即时奖励 = 重建成本」这条 RL 信号的强度，所以不只是手感问题
  3. **攻方需要一个「造成持久后果」的出口**（现在只能打断、不能否定）。最小候选是被拆后原地留需清理的废墟、把停产时间变成可被攻方拉长的量
  4. **空军恒 1 只** ⇒ 防空两难无可预判。CLAUDE.md 已写「上限可随波数缓慢放开」，只是 demo 曲线没实现
  5. ~~**克制关系与来袭编成在 UI 里完全不透明**~~ ✅ **同日已做**（侦查面板，见上方 Cody-ic 三续）
- **人口上限的落地被一个前提卡着，不是被数值卡着**：`8+2K` 已推导定版，但造价曲线必须先修（`数值设计与成本产出矩阵.md` **§12**），否则上限会从「取舍」变成「战力天花板」。**修法取 `cost_gold(L) = base × √(1+k(L−1))`（p=0.5）而不是我最初说的 `× (1+k(L−1))`（p=1）**——后者只在隘口附近接近中性、开阔地照样一边倒，是同日模拟推翻的。**动仿真行为，要团队过**；且**造价曲线 / 人口上限 / 兵种等级上限三件必须一起落地**，只改其一会走到另一个角上
- **`budget_curves.py` 那条「检验自己的假设」的结构检查要修**（它写死 `cost = base×(1+k(L−1))` 再验证「堆量堆级等效」，而游戏实现是 `× level`）。照第 14 条那次的先例：从唯一真相来源读，不复述它
- **攻守两侧的造价模型不一致，且没有检查在管**：攻方 `wave_level` 反解假设人均造价 = base·B(L)，守方 `train_cost_gold` 按 `× level` 收钱。改守方时要一并决定攻方跟不跟——若攻方也走 p=0.5，`总战力 ≡ 兵力预算`（PR #124 特意建立的构造性等式）会不再成立
- **顶住人口上限之后没有「把数量换成质量」的出口**：`UpgradeForce` 已随编队移除删掉、「解散」从来没有 ⇒ 只能等有人阵亡才能招更高级的。波次游戏里损耗每波都有所以不是死锁，但「换装」有一波延迟；要么接受，要么把尚未实现的「买活」一并设计成出口
- **一处自我批评**：09-01 加的「免费方向提示」是消费侧，而生产侧（`spawn_wave` 轮转派点）让攻方从不集中兵力 ⇒ 那行提示当前由整数取余决定、佯攻无从发生。**加消费侧时没有回头检查生产侧**
- 上一轮（09-01）的遗留问题全部仍然有效，未处理

---

## 2026-09-01（第 7 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | **数值审计（六路并行）查出等级差侵蚀是结构性缺口，落地兵种等级上限补上这条根因。** 用户要求"整体评估一下数值,感觉还是不太合理"，六个维度并行审计（经济时间尺度、等级差克制侵蚀、地图规模自洽、防御造价量级、升级轴投入产出比、冲锋/溅射系数耦合）之外，自己先确认了一条：`building_level_cap()` 的奇偶台阶——`Keep` 要升到 3 级才有任何其它建筑能升第一级，而 `Keep` 每次升级固定 200 石+200 木+400 tick。审计结果与这条相互印证：`level-gap-erosion` 与 `charge-splash-coupling` 两个独立维度都独立算出 `Spear`/`Knight` 约第 10 波、`Ranger`/`Shade` 约第 19 波永久反转——根因是守方单位出生等级后永远不变（全仓搜索确认 `u_level_` 只在开局配置与征兵出兵两处写入，征兵恒用 `kMinUnitLevel`），攻方等级随波次无上限。用户随即指令：**兵种升级必须做**。<br>**落地形状**：`Command` 追加 `level` 字段（征兵选级用；`_reserved0` 一起补，凑够 8 字节防止编译器插填充——`sizeof(Command)` 6→8，回放载荷 6→7 字节/条，`kReplayFormatVersion` 2→3）；新增 `CommandKind::UpgradeForce`（批量升级，追加在 `Upgrade` 之后，`kCommandKindCount` 13→14）；`World::unit_level_cap()`——直接等于堡垒等级、**没有除数**，与 `building_level_cap()` 公式来源本来就不同；`World::train_cost_gold()`/`train_ticks_at()`——`Train` 与 `UpgradeForce` 唯一共用的造价/耗时公式（造价纯线性，耗时走新增的 `train_ticks_permille_per_level` 千分比系数、**不开方**——训练耗时不参与 `p−q=0` 那组不变量）；`World::barrack_near()`——`mason_near` 的反过来版本。`GlobalStats` 两个新字段令 `Stats/8→9`，两个新数组（`u_upgrade_left_`/`b_train_level_`）令 `World/10→11`。**一处刻意偏离建筑升级先例**：单位升级按血量比例换算、不补满——建筑那边"完工即满血"是有记录的副作用，半血的兵不该靠"正好在升级"变成免费回满。UI 复用 `train_force_sel` 那个旋钮（不新开框选流程），新增 `train_level_sel`（`[`/`]` 调），Train 弹窗多一行"升级编队"，同"升级"那条既有纪律——没有够格单位时也照常出现、灰着。<br>**过程中的一处环境坎坷，记下来防止下次重踩**：这台机器的 bash 工具把 `/Users/…/计算机程序设计大作业` 解释成一个与 `mount` 表无关的虚拟路径（`stat`/`readlink` 都说它不存在，但 `cd`/`pwd`/文件 I/O 一路正常），而 `cmd.exe` 走这条路径时**必须用绝对路径 + 正斜杠**，相对文件名（哪怉是 `dir` 能看到的）会以"不是内部或外部命令"报错，反复试了带反斜杠加引号、`call`、去引号等写法都不行，唯一稳定的是 `cmd //c /绝对/路径/foo.bat`。**已验证**：Windows/MSVC，Debug+Release 双配置零警告，`ctest` 44/44（RENDER=OFF）与 55/55（RENDER=ON，raylib 首次配置遇到一次 502，直接 `curl` 下载校验 SHA256 后用 `-DFETCHCONTENT_SOURCE_DIR_RAYLIB` 绕过网络）全绿；截图确认新 HUD 行（`[/] 征兵等级`）正常渲染、未缺码点。 | 见本次提交 |
| ArLiangz | **同步仓库到最新 main（#112~#115，池图 72→144、兵种等级上限、驻守修复等）后，把用户直接给的三张真实素材接进资源点标记。** 资源点（`StonePt`/`WoodPt`/`GoldPt`）此前是 zhxxx233 那次试玩反馈里加的程序化生成菱形徽章（本机无 Blender，精灵流水线出不了图的权宜做法）；用户给了三张裁好的真实图（树/石堆/矿场，按文件名对应资源种类），要求换成真实贴图。**处理**：先用 PIL 把三张源图裁到内容边界（去掉多余透明留白）、按长边 200px 等比缩放，存进 `tools/sprite_gen/out_3d/decal_*.png`（与花名册精灵**同一目录**，`decal_` 前缀区分，不新开一层目录——理由见下）；`SpriteAtlas::register_decal`（程序化生成）换成 `register_decal_image`（从磁盘读真实 PNG），地面锚点取**画布底边中点**（这批素材已裁到内容边界，物体视觉底部贴着画布下沿，不是 Blender 渲染那批需要 `stand_lift_px` 目视留白系数的情形）。**踩了两个坑，都是真的构建/测试当场抓出来的**：①最初图片加载直接用 `LoadImage(path)`，而 `render_utf8_path` 这条 ctest 专门钉的就是「raylib 全部走窄 `fopen`、路径含非 ASCII 会失败」那个坑——虽然这次的具体失败原因是路径本身找不到（见下一条），但既然要新开一条图片加载路径，就该照 `get()` 里已经踩过的先例走「自己读字节 + `LoadImageFromMemory`」，不能重新绕回那个坑，已改正。②最初把 decals 存成 `sprite_dir` 的**同级**目录（`tools/sprite_gen/decals/`），想当然以为 `sprite_dir` 永远是 `.../tools/sprite_gen/out_3d`——`ctest` 立刻用 `render_utf8_path` 打脸：那条测试把精灵复制到一个临时的中文路径目录再指给 `--sprites`，同级目录假设在那种场景下不成立。改法是把三张 decal 挪进 `out_3d/` 内部（与流水线精灵同目录，`decal_` 前缀），`register_resource_decals` 直接从调用方传入的 `sprite_dir` 拼路径，不再对目录结构做任何额外假设；`render/CMakeLists.txt` 里那条测试的文件复制段落相应补一条 `decal_*.png` 的 glob。**渲染截图 + PIL 逐像素放大核对过三种标记都正确显示**：独立单株小树（区别于成簇的 `Forest` 地形）、小石堆（区别于更大更灰的 `Rock` 地形）、带矮轨小车的矿场，锚点落在地面、没有悬浮或陷进地面。**已验证**：Windows/MSVC Release 全量重编零警告，`ctest -C Release` 55/55 全绿（含改动后的 `render_utf8_path`）。**同日再续：用户测试后反馈「城墙内资源点太多、城外太少且太远」，调城内外资源点的数量与分布**。①**城内三种资源各恰好 1 处**（`inner_resources_range` 从 `{stone:[2,2],wood:[1,2],gold:[2,2]}` 收紧到三种恒 `[1,1]`，参考图与池图 `thresholds.json` 两处同步改）——城内资源点定位是保底收入的地板，不是决策点，多了只是拥挤，一石一木一金各配一座对应的采集建筑不变。②**城外簇数与簇内点数上调**（池图 `outer_clusters_range` [4,6]→[7,8]、`outer_cluster_size` [3,4]→[4,5]，上界 8 是方位数上限，写更大的数字会被 `min(cfg.outer_clusters, len(directions))` 静默钉住，之前一版写了 [7,10] 已收窄改正）。③**簇距上界与地图边长解耦，改跟城区半径走**——`place_outer_clusters` 原来的 `max_d = 地图边长//2-4` 在池图 144 之后把簇距跨度被动拉到贴地图边缘（实测城外 50+ 格），新增 `outer_cluster_span` 配置项，`max_d` 改成 `min(地图给的上界, city_radius+4+outer_cluster_span)`——地图再放大也不会把簇自动推得更远。`thresholds.py` 的 `Generator` 白名单类同步加这个必填键（含 ≥1 校验），`build_reference_map.py`/`thresholds.json` 两处都要给值。④**簇心卡住了，资源点仍能跑远——追加一次真调试**：光改 `outer_cluster_span` 之后实测参考图最远资源点仍在城墙外 19 格，比预期的簇距上界还远——查出 `place_outer_clusters` 给资源点找候选格时遍历的是**整条**森林带（`belt`，从簇心一路挖到地图边界，第 7 条要求这条带必须通边界，不能缩短），资源点只要贴着带子任意一格就能落地，等于借着带子"搭车"跑到远超簇心限制的地方。加一个新常量 `_BELT_POINT_WINDOW`（簇心往边界方向数 6 格以内才允许落点，其余带子纯森林、只负责连到边界），两条要求互不影响后城外最远点缩到城墙外 9–17 格（参考图 9、池图池均值 4–17）。参数是拿参考图（56 格、`city_radius=8`）反复实测调出来的比例（`span=7`、窗口=6），按 `city_radius` 12–15 折算成池图的 `span=13`，**未在 144 池图上逐一复核，仍是占位**，已在 `thresholds.json` 的 `_note_outer` 写明。⑤参考图改参数过程中原固定种子 `20260831` 一度因为改动挪动了 RNG 消费序列而变得不合法（第 11 条：一格孤立 Plain），调完最终参数后重新验证过仍然合法，未换种子；过程记录留在 `SEED` 旁的注释里，防止以后改参数时想当然「种子还能用」。⑥`selftest.py` 里两处硬编码的「城内金/石矿必须恒 2」断言按新数字改成恰好 1；另有一处 `_ref_cfg` 的 docstring 曾自称「参考图同款形状」，现在两边参数已经分道扬镳，订正掉这句过期的声明（该夹具本来就该被当成独立于参考图/池图之外的第三份配置，不是同一份的另一份拷贝）。地图池 12 张全部重新生成（`generate.py -n 12 --seed 1001`，2 张需要二次尝试，否决面同样是第 11 条——与 `_FINDING_discard_rate_after_loosening` 里记录的既有模式一致，不是新问题）。**已验证**：`selftest.py` 777 条断言全过、参考图与池图 12 张 `validate.py` 全部通过、Windows/MSVC Release 全量重编零警告、`ctest -C Release` 55/55 全绿、渲染截图 + PIL 放大确认城外资源点密集分布在贴近城墙的森林带起点附近。 | #116；地图与场景设计.md §2.2 / tools/map_gen/{build_reference_map,generate,selftest,thresholds}.py / tools/map_gen/thresholds.json / game/data/maps/reference_border_keep_01.json / game/data/maps/pool/*.json（本续，见 #119）。**同日再续：用户发现森林带总是一条直线，指出这是"玩家不能把资源点围起来"这条被团队否掉的设计没删干净，要求删掉——先核实再动手**：查 `地图与场景设计.md` 2.1 发现这个理解不对，"不可围墙"不是被否掉的设计，是团队 2026-08-31 经候选甲/候选乙比较后**主动采纳**的反龟缩结构约束（对应校验器第 7 条），且团队已经为"森林带看起来是直线"这个问题修过两轮（#117 加折步、#121 因带子被 #119 拉长后再加密折步）——不是没删干净的残留，是一个已知问题的现有修复不够。把这个事实核实清楚后向用户说明，用户在了解代价（会拆掉一条反龟缩结构保护、需要连带删校验器第 7 条）后仍选择彻底删除，**这是一次经过说明的主动决定，记录在案**。**落地**：`generate.py` 删掉 `_carve_forest_belt` 与 `_BELT_POINT_WINDOW`，`place_outer_clusters` 改用 `cv.free_plain_cells(center, radius=_OUTER_CLUSTER_RADIUS)` 直接在簇心周围找候选格，不再要求连到地图边界的森林通道；`validate.py` 删 `check_outer_unenclosable`，第 7 条与随之失去前提的第 20 条（"第7与第10条的联合可满足性"）一起移进 `REMOVED`（`MOVED_TO_GENERATOR` 因此清空）；`地图与场景设计.md` 2.1 整节改写为"原「结构性不可围墙」，2026-09-01 移除"，保留候选甲/乙/森林带的历史脉络与移除理由，**明确写出认下的代价**——玩家现在理论上能砌墙圈住外部资源点，"部分资源点在墙外"这条更基本的要求改由第 21 条独自守住；`CLAUDE.md`「资源分布形态」同步订正。**清理面比预想的大**：`selftest.py` 删掉两个整测试组（`check_v7_outer_unenclosable`、`check_v7_v10_tension_is_real`）与森林带自身的测试（`check_generator_forest_belts_meander`），补一处硬编码的条目状态计数（22→21）；`validate.py`/`selftest.py`/`generate.py`/`thresholds.json` 里另有十余处散在各条检查（#10/#14/#17/#19/#23/#9 阻塞说明）docstring 或注释里提及"第7条"/"2.1.1"/"森林带"作对照或引用理由的地方，逐处订正而不是只删主体——这批文档密度高，遗漏一处就是新的漂移。`README.md`/`tools/map_gen/README.md` 的状态清单也同步。**移除森林带候选格窗口后的副产品**：城外资源点总数不降反升（12 张池图从 13–24 涨到 29–38），因为候选格不再局限于狭窄的带子，簇心周围整片空地都能用。**已验证**：`selftest.py` 913 条断言全过、参考图与池图 12 张 `validate.py` 全部通过（12 张零丢弃重生成）、Windows/MSVC Release 全量重编零警告、`ctest -C Release` 55/55 全绿、渲染截图确认森林与岩壁现在是自然的小簇分布，没有任何直线痕迹。见 #122 |
| zhxxx233 | **试玩反馈五条一次收掉（地图侧四条 + HUD 一条）。** ①**池图城墙非满血**：#112 只修了两张手写图，池图的残血来自 `thresholds.json` 的 `wall_hp_frac_range` [0.35,1.0]——钉死 [1.0,1.0]，理由与训练侧影响记进 `_note_hp`，`hp_frac` 字段本身保留（格式能力不删）。②**池图 72 → 144**：第 5 条「Ram 行军占比 [0.10,0.20]」在 144 下结构性不可达（实测行军 54–58 格 ≈ 1250 tick，占 episode [1200,2400] 的 50%–107%）；episode 长度是训练侧 credit assignment 前提不能翻倍、Ram 不能提速（`笨重攻城器械` 定位），照 `_FINDING_size_vs_ram_speed`「反过来求解」的先例改占比区间这一元为 [0.10, 0.75]，`-n 30` 实测 107 个集结点全部通过且 lo 离新上界还有约 20 个百分点余量。③**「金矿完全不刷新」实为可见性问题**：金矿数据里一直在（城内恒 2、城外 ≥2 有校验器第 25 条守着），是渲染层从不画资源点 + 预置建筑没有 Mine 当地标。双线修：生成器预置一座 `Mine` 精确踩城内金点（与 Lumber/Quarry 同法）；渲染层新增 `SpriteAtlas::register_decal`——程序化生成菱形徽章（本机无 Blender，精灵流水线无法重渲），通过预填元数据+缓存接入既有 `get()`/`preload_idle()` 路径、零特例分支；`SceneModel::build` 与 `BattleScene::sorted` 各摆一遍（同层/插序，采集建筑落成后正好盖住脚下标记）。④**城外散布美观化 + Water/Bridge 落地**：森林与岩壁从随机游走团块改**圆形斑块**（半径 isqrt、概率随到圆心距离平方衰减、贴已长格生长）；`place_water` 新画湖泊（圆瓣）与一条边到边的河（2–3 格宽）+ 垂直贯穿水宽的桥——当年「不画水」的留白理由（画错一格切断通路、丢弃率飙升而原因难查）由「逐水体落地后自查连通性、失败整片撤销」消解。途中修掉一个真 bug：相邻两条桥带互为水邻，逐带检查通过、联合落地后四邻无水（第 13 条红 2 次），`pick_bridges` 末尾加联合复验后归零。⑤**HUD 文字看不清**（144 下地图一拉大，浅色字压在草地上）：两个 HUD 函数加半透明深底板（模式照 `draw_cell_readout` 那块底），提示行提亮；顺手把调用点单独画的「选中集」第四行收进 `draw_battle_hud`——底板按行数现算高度，不收进来就盖不住它。文档联动：`地图与场景设计.md` §2.2/§2.3（标题「残破城墙」改为「城墙」、满血开局是当前产物状态、`hp_frac` 保留为格式能力）/§4.4.0（预置构成加金矿场）/§9（布局段、留白从三处变两处、池图 144）、CLAUDE.md（待定事项的水系留白条目与两处「72」）、README（速查表）、`map_data.hpp`/`generate.py` 注释。池图 12 张重生成（删旧 5 张），参考图重跑（布局因新散布算法变化，逐字节复现性由「零配置不消费 rng」+ selftest 钉住的部分不受影响）。**已验证**：`selftest.py` 867 断言 51 组全绿、`-n 30 --seed 501` 丢弃率 18.9%（第 5 条零否决）、MSVC Release 重编零警告、`ctest -C Release` 55/55 全绿、确定性守卫通过、离线截图 + PIL 放大核对过 HUD 底板与资源标记。 | 见提交 |
| Cody-ic（同日四续） | **侦查系统对玩家生效 + 三处视觉/摆位缺陷 + 代审代合 #122。** 用户报四条：侦查形同虚设、城墙四角没包起来、城门与城墙没对齐、Tower 位置奇怪。**先把根因定位到具体行与具体数字，再动手**。<br>①**侦查形同虚设的根因不在 `rts_core`**：那一半本来是完整的（`FogLayer` 三态、记忆建筑、进 `state_hash`、`obs_pack` 按硬要求 1 打包、`DefenderScript` 的 `Scout` 巡逻真的读 `view.fog()`）。断点三处全在 `World` 之外：`render/` 对迷雾**零引用**（全目录 grep 无命中）⇒ `BattleScene::sorted` 把 god 视角原样画出 ⇒ 「编成构成」与「精确进攻方向」这两项 CLAUDE.md 明列为**需侦查**的情报全部免费、`Scout`（25 金）与 `Watch` 没有任何理由被造；**攻方从来没生成过 `Wraith`**（全仓 grep，`game/` 里它只出现在中文展示名表）⇒ 「双向欺骗」只有守方一向；没有免费方向提示。落地按组内定版取「只藏敌军、地形恒可见」：`sorted` 按 `view.fog()` 过滤敌方单位与弹丸（判据与来源逐字照 `obs_pack.cpp`，全仓唯一先例），己方/地形/中立障碍/资源标记照画，`DrawItem` 不加字段、`render/` 一行没改；`spawn_wave` 第 2 波起恒 1 只 `Wraith`（从编成位里扣）+ `issue_actions` 单独一档「看到守方建筑就掉头回集结点」；免费方向提示走新增的 `game::strongest_spawn` + `display_name(Compass)`，方位名吃 `all_display_strings()` 的机械遍历、不手抄进 `ui_strings()`。**过程中防住一个会卡死的坑**：下一波挂在「攻方存活数归零」上，而撤回集结点的 `Wraith` 不会死——一只就能让游戏永远停在这一波，所以它落单时改为前压。<br>②**「城门没对齐」是素材锚点，量出来的**：四个朝向共用一个 `ground_anchor`，实测 `Wall` SE 0.0 / NE 0.0 / **SW +34.5（右侧被画布切掉 228 px）** / NW +34.5，`Gate` SE +5.5 / NE +5.5 / **SW −32.5** / NW −32.5 ⇒ 干净的是 `{SE, NE}`、脏的是 `{SW, NW}`。而 `scene_model.hpp` 原先把脏的那组**记成了 `NE`/`NW`——那句话是错的**，代价是上一轮只修了竖直走向（`NW`→`SE`）、水平走向仍留在 `SW`：于是**东西门看着是对的、南北门始终错开 67 px≈0.26 格**，池图 12 张恰好一半一半，读起来像「时好时坏」。改 `run_direction` 的 gi 档 `SW`→`NE`（同轴 180° 孪生、拼接无缝），错位降到 5.5 px≈0.02 格，顺带解掉那 228 px 裁切。**前后截图并排对比确认**：修复前城门孤零零立着、右侧那段墙明显错位下移、中间留着可见断口；修复后门两侧的墙都接上、整条线是直的。<br>③**四角有两个独立成因**：生成器 `_ring_cells` 四个角本来就是真的 `Wall` 实体（纯视觉），少一块竖板（上一个 PR 已补）+ 横板被裁 228 px，而**裁掉的正是它该与竖板接上的那一端**——两条一起才闭合，截图确认已闭。<br>④**Tower 位置奇怪 = 单张固定地图时期的写法**（同 #110 那个「驻守目标写成堡垒正东三格」）：`demo_init` 用固定偏移摆 `Tower`(keep+(1,−2))/`Flak`(keep+(1,2))，而池图 `city_radius` 是 12–15 ⇒ 两座离墙线 10–13 格，`Tower.range=7`、`Flak.range=5`，**永远打不到墙线**，敌人进城五格才开火。删掉 `Tower`（地图预置的 2–3 座位置本来就对），`Flak` 改为从墙线推：按 `(是不是门, 到堡垒的切比雪夫距离)` 排序取城门内侧。**门优先不是审美**——方环上每格到堡垒的距离**完全相同**，只按距离排等于「取文件里第一个」，实测落在拐角上。<br>**守卫都做了破坏性验证**：新增 `SpriteAtlas::verify_linear_anchor()`（把「线性结构用到的朝向内容确实落在锚点上」变成会红的检查：内容中心偏离容差 + 不得压画布边缘），临时指向 `SW` 后如实报出 `Wall_SW` 偏 34 px/被切、`Gate_SW` 偏 −33 px；`game/` 侧那条枚举断言临时改回 `SW` 后 `rts_tests_scene` 与 `rts_tests_all` 当即变红。迷雾两条**互为反面**（迷雾里的敌人不进列表 + 走进视野的必须重新出现——只有前者的话「永远不画敌方」也能通过，那是把白给改成全瞎）。<br>**一处自己的读数错误，当场纠正**：先前判断「建筑视野全是 0，迷雾一开玩家连自己城墙被打都看不见，必须先补数值」——那是查 JSON 时用错了键名（`vision_radius`/`attack_range`，表里是 `vision`/`range`）打出的一整列 0。真实值 `Keep` 8 / `Wall` 2 / `Gate` 2 / `Barrack` 5 / 采集三座 4 / `Watch` 12，迷雾直接可玩，**那条数值前置整条取消**，也不必再动一次数值指纹。<br>**代审代合 #122**（ArLiangz，移除森林带）：本机复跑 `selftest.py` 913 条断言、全池图 + 参考图 strict 档校验、逐处核对十余个交叉引用都已订正、`MOVED_TO_GENERATOR` 清空后覆盖性断言仍成立，无异议后合并。**顺手修掉它带进来的一处漂移**：`generate.py` 的批量报告仍自称「规范第 20 条」，而那条编号刚随第 7 条废除——报告机制本身没变（服务当前表里全部检查），把名字摘掉即可，留着旧编号会让读者去 `--list` 里找一条不存在的条目。<br>**地图生成复查（合并后实测）**：`-n 30` 丢弃率 **3.2%**（此前 18.9%——森林带这个失败源没了），唯一否决面是第 11 条 Plain 孤岛 1 次；30 张图 986 个城外资源点聚成 228 簇，簇大小中位 4、**簇内直径中位 7**、簇心间距中位 19.3 ⇒ 「大散居」成立、「小聚居」偏松（`_OUTER_CLUSTER_RADIUS=4` 配「点距 ≥2」允许同簇两点相距 8 格，读起来接近散点）。**顺带量化 #122 认下的那个代价**：把一簇整圈围起来中位需 **32 段墙 = 960 石**，约等于 24 座采集建筑的石材，且还要派兵守——「被迫出城」现在只由数值劝退撑着，而这个数字目前相当陡。**已验证**：MSVC Release 零警告，`ctest` 44/44（RENDER=OFF，含 `rm -rf` 后的全量重建——合并带进了 `world.hpp`/`stats.hpp`，按 CLAUDE.md 那条规矩不信增量）与 55/55（RENDER=ON）全绿，确定性守卫通过，`selftest.py` 913 条断言全过，截图人工验收。 | 见本次提交 |
| omg321-975 | | |

**本次决定**：

- **兵种等级上限（堡垒等级轴第三个输出）已落地**，含征兵选级与已有部队批量升级两半；三个输出里只剩人口上限（含"买活"）未做
- **单位升级按血量比例换算，不补满**——与建筑升级"完工即满血"刻意不同，是本次新立的一条约定，理由见 `CLAUDE.md`「守方的一条升级轴」新增段
- 池图尺寸 72 → 144（第 5 条占比区间随之放宽为 [0.10, 0.75]，episode 长度与 Ram 速度不动）；所有产物城墙满血开局（`wall_hp_frac_range` 钉死 [1.0,1.0]，字段保留）；生成器开始画 Water/Bridge（留白 1 作废）；资源点进渲染（程序化 decal，不走精灵流水线）。
- **玩家侧的迷雾定为「只藏敌军、地形恒可见」**（组内定版）：敌方单位与弹丸只在 `Vis::Visible` 的格上出现，地形/己方/中立障碍/资源标记恒可见。未探索地形**不压黑**——城区半径 12–15 而建筑视野零散（`Wall` 2、`Keep` 8），压黑会让自家城内出现环状暗带；要压黑就得整体抬高建筑视野，而那会顶到「静态建筑视野不得覆盖集结区」那条结构约束的上限（第 14 条与生成器的禁建环都从数值表推导）
- **墙与门的走向只许取 `{SE, NE}`**（内容落在锚点上的那两个朝向）。这不是审美选择：另两个朝向的内容偏离共用锚点 32–34 px、`Wall` 还被画布切掉 228 px，而**那种错画面照样出、只是画错了**。像素侧由 `SpriteAtlas::verify_linear_anchor()` 守、枚举侧由 `[scene]` 一条断言守，两边合起来才是完整判据（§7 不许 `game/` 读像素几何）
- **`demo_init` 不再用固定偏移摆建筑**：那是单张固定地图时期的写法，随机地图池下必然错位。`Tower` 删掉（地图预置的位置本来就对），`Flak` 从墙线推

**遗留问题**：

- **人口上限与"买活"仍未做**，牵连的是"当前活着的守方单位总数上限"这个新概念，独立工作
- **批量升级没有取消入口**：金已预付，升级中只能等完，`Cancel` 未扩展到单位——刻意留的小缺口，不是遗漏
- `train_ticks_permille_per_level`（150）与 `unit_upgrade_radius`（2.0）都是占位数值，未跑批量仿真标定
- 数值审计六路里 `economy-scale`（建造阶段与采集建筑建造时间的耦合问题）、`defense-affordability`（防线可全覆盖）、`map-scale-consistency`（`Flak` 射程与外部资源簇间距）三项发现仍未处理，见对话记录，未开新 issue
- `Gate.max_hp` 未随 `Wall` 同步那一条仍在等组内拍板（#57 已点名）；集结点数量、episode 长度与 144 的终极标定关系仍待训练侧定（README 速查表已注明「是自洽不是最终标定」）。
- **`outer_cluster_span=13` 是从参考图（56 格）按 `city_radius` 比例折算到池图（144 格）的占位值，未在 144 池图上逐一复核**——12 张池图实测城外资源点在城墙外 1–16 格之间，落在「不太远」的目标区间内，但没有像参考图那样反复调参数，之后若试玩觉得偏近/偏远，从这个数字开始查（`_BELT_POINT_WINDOW` 已随森林带机制一起删除，不再存在）
- **新增风险，需要团队知悉：外部资源点「不可围墙」这条结构性反龟缩保护已于本次移除**（`地图与场景设计.md` 2.1 的订正，理由是承载它的森林带即便两轮加固仍读成直线，视觉代价被判定大于结构收益）。玩家现在理论上可以砌一整圈墙把外部资源点圈进城内，龟缩退化解从"结构上不可行"退化为"数值劝退"（墙的造价与长度）。这是经过说明、用户主动选择的取舍，不是疏漏，但值得在后续试玩里留意龟缩是否真的重新变得可行——如果变得可行，需要团队重新讨论是否要换一种不依赖森林带的反龟缩手段。**同日已量化这条数值劝退现在有多陡**（合并后 30 张图实测）：把一个城外资源簇整圈围起来中位需 **32 段墙 = 960 石**，约等于 24 座采集建筑的石材，且围完还要派兵守。所以短期内它撑得住；要留意的是**长期**——CLAUDE.md「结构封死，而非数值劝退」那条讲的正是「低效不等于不可行」，玩家石材收入会随波数涨，这个倍数会被稀释
- **「小聚居」现在偏松，是数值不是结构**：30 张图 986 个城外资源点聚成 228 簇，簇大小中位 4、**簇内直径中位 7**（`_OUTER_CLUSTER_RADIUS=4` 配「簇内点距 ≥2」允许同簇两点相距 8 格），读起来接近散点而不是 CLAUDE.md 要的「密到值得为它单独围一段墙」。「大散居」那一半成立（簇心间距中位 19.3）。**没有单方面改**——它是一个占位数值，且与上面那条围墙代价直接耦合（簇越紧，围它越便宜），两个数应该一起定
- **`AI vs AI` / 双视图用到攻方视角时，记忆态建筑还不会画**：`FogLayer::remembered_bld` 有数据，但守方视角下攻方没有建筑，今天写了就是死代码。`BattleScene::sorted` 里那一句按侧过滤已经留好位置（守方视角下它不挡任何东西，但它是「本函数尊重迷雾」与「碰巧安全」的差别）
- **弹窗那一层仍然没有自动化守卫**（同上一轮）：本轮新增的 HUD 文案「主攻 + 方位名」走的是 `all_display_strings()` 的机械遍历，所以不会漏码点；但弹窗里的文案仍然只能靠试玩

---


| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | **复核 #96 / #97 并把数值那一侧的四处问题收掉。** 起点是「同步队友进度，尤其看数值的合理性平衡性」。#97（框选取代键位）复核无异议——两处真实回归都是被既有测试抓出来的，`help_entries()` 与 `CLAUDE.md` 同步了。#96 方法论扎实（只改 11 个数、§7 自己立了「无理由的改动一律撤回」并真的执行），但有四处要收：<br>① **第 14 条「静态建筑视野不得覆盖集结区」绿着失效**。`thresholds.json` 的 `static_vision_radius_max` 是手写的 8，而表里 `Watch` 是 12——**实测**（用仓库真实的 `generate.py`）生成图的四个集结点到最近可建造格都是 10 格，`Watch(12)` 全部够得着，**16/16 而校验器全绿**。这条 CLAUDE.md 明写是「结构约束，不是数值」。改法走第 5 条取 `Ram` 速度那条现成先例：上限改由 `max_building_vision()` 从数值表推导，手写的键退化成**被核对的声明**，不一致时第 14 条自己报。声明抬到 12（环 9→13）**实测免费**：`size=72` 仍 20/20 零丢弃，距离 10→14。两条破坏性验证进了自检（232→238 条）<br>② **§1.4 重开**。#96 那个三选一是「每属性线性增长」这个隐含假设的产物：写成 `血 = B(L)^p`、`伤 = B(L)^q` 之后，「克制比例衰减」只吃 `p+q`、「TTK」与「破墙时间」只吃 `p−q`，**两者正交**（p、q 限在 {0,1} 时它们才被锁死在一起）。`p=q=0.5` 与 #96 的选择**战力曲线逐点相同**，而 TTK 从 ×0.19 变 ×1.00、破墙时间从 ×0.20 变 ×1.03——**没有一项更差**。破墙那一条是真的违反了「结构破坏规则」的区间不变量（`hp` 系数为 0 ⇒ 墙升级空操作 ⇒ 破墙时间 ∝ 1/L → 0），而 #96 用的闭式 1v1 模型**没覆盖单位对城墙**那一侧，所以它没被发现。**没有单方面改数值**：拆成「`p−q = 0` 是结构、`p+q` 是数值」记进 CLAUDE.md 待表决，并写下实现两个坑（不能用浮点 `pow`；**不能只改语义不动形状**，否则 `fingerprint()` 不变、旧回放静默算错）<br>③ **撤回两处无理由的数值改动**（`Spear` damage/max_hp、`high_ground_dmg_permille`），依据是 #96 §7 自己的判据——那一轮撤回了四处、漏了这两处。**撤回前量过：四条结论一条都不翻**。顺带查实 `Shade` 那条注释里的「15s → 约31s」是用**改动前**的 `damage=11` 量的（13.64/30.61），当前值是 10.71/21.43——记录改动的注释引了改动前的数<br>④ **矩阵判据从「谁胜」改成三档**。原判据是 `对方用时 > 自己用时`，而 CLAUDE.md 明写要「期望资源盈亏（**而非谁胜**）」且克制的定义是「**显著**更少的资源投入」——`Shade` 克 `Archer` 以 1.05x 记成「通过」，而那个幅度下两个 Archer 就翻盘。改成 通过/幅度薄/未通过，照 `validate.py` 那条纪律（三种状态都进报告），只有方向反了才非零退出。同时查出**「编成位占用」这个字段整个仓库都不存在**（`combat_math.cost_of()` 正是为它留的位置，至今无调用方），所以成本产出矩阵**算不出成本**——README 把它列进已落地清单是一处 overclaim，已订正<br>另新增 `tools/balance/budget_curves.py`：把队友那份只能被读的公式变成可跑的，含四条会红的结构断言与三条结构结论（`cost(等级)` 必须与 `战力(等级)` 同阶，否则宏观层「40 杂兵 vs 8 精英」那个决策塌到角上；`cost ∝ 战力` 一旦成立则「波次强度曲线」与「兵力预算曲线」是同一条；波间隔 × 收入率、出矿速率 × 密度在预算上只有乘积进去，各自不是两个自由度）。建模上修了自己一处错：初版拿守方**每波收入**比攻方每波预算，低估了守方——攻方是流量、守方是存量，改成累积递推。**已验证**：本机 MSVC Release 200/200 零警告、`ctest` **44/44 全绿**（RENDER=OFF；44 + 11 条 `render_*` = 队友那个 55）。**另订正自己一处判断错误**：先前对组里说「这台机器没有 VS/CMake，C++ 那半验不了」，实际装的是 Build Tools 且在 D 盘（`D:/tools/BuildTools/`），`C:\Program Files\Microsoft Visual Studio\` 整个目录不存在——查法（问 `CMakeCache.txt` 而不是猜路径）与 Git Bash 下要导 vcvars 环境这两条已写进 README。**同日续：按线索去查队友的「Ram 削弱」（#102，标题「Ram 改为单体伤害，不再有 AOE 范围伤害」），带出组长当场指出的另一处真问题**。#102 本身：PR 正文与提交信息都没写「为什么」，代码侧 AOE 解算与 CLAUDE.md 文档逐字对应，不是 bug；量出**单体化之后 8 个 Ghoul 在两个破墙尺度上都比 1 台 Ram 快**，直接顶到 `CLAUDE.md:735`「`Ram` 必须保有压倒性拆除效率」那句——该句用的是同义词「压倒性的拆除效率」而不是「AOE」，PR 逐处搜 AOE 时漏掉，**与「投石车」那次同一种漂移，第二次发生**。已发评论列出数字与这条漂移，等作者回应，**没有动它的代码**。**真正落地的是组长直接指出的另一处、与 #102 无关**：AOE 圈内此前不分主副，承诺锁定的目标与被溅射到的其余单位吃同一份 `damage`——「点一个等于点一片」，与花名册「对能散开的单位效率很差」自相矛盾（散开只要还在半径内就照样满伤，散开的惩罚名不副实）。新增 `UnitStats::splash_dmg_permille`（`Stats/5 → Stats/6`）：承诺时锁定的主目标恒满伤，圈内其余单位（不分敌我——「溅射误伤」是 CLAUDE.md 已列的机制性克制，组长追问过这条，答复见 CLAUDE.md 新增段）按这个千分比打折；**只管单位不管建筑**，「对静止的建筑全额命中」那半原样保留。`StatsLoader` 新拦一条不变量：开了 `aoe_radius` 却把这个数留在 1000 直接拒绝载入——「主攻击的单位和溅射伤害不能一样」现在是结构不是靠人记得调。`Ram` 取 500‰占位，等 `bindings/`+`train/` 长出真训练循环再标定。补两条测试（fingerprint 覆盖该字段；破坏性验证主副伤害真的不同：主目标 18→8，旁观者 18→14）。**已验证**：Windows/MSVC Release 200/200 零警告、`ctest` 44/44 全绿；`py tools/balance/cost_output_matrix.py --scenarios` 复核过，四个场景数字不受影响（工具是闭式 1v1，圈里从来只有一个目标，主副之分没机会体现）。**同日再续：组长问「防御塔攻击是不是有点低」，查出真正的缺口是建筑完全没有等级机制**（`b_level_` 全仓不存在，攻方伤害每级 +22% 而塔的伤害永远是表里那个固定数，波次越推进相对越弱）。**这不是新决定**——组长直接指出 `CLAUDE.md`「守方的一条升级轴：堡垒等级」早写好但没实现：Keep 等级不设上限，三个输出之一是「建筑等级上限」，公式在 `波次预算曲线与堡垒等级曲线.md` §2（`ceil(K/2)`）。走了完整的 Plan Mode：先问了升级白名单（用户选 Wall/Gate/Tower/Flak），随后自己查出 `hp_permille_per_level=0`（§1.4 待表决）会让 Wall/Gate 的升级「花钱不涨血」，正要再问处理方式时被用户打断更正范围——「所有建筑都要能升级，以堡垒等级为上限」，直接对应文档已写的三输出设计，不是待商榷的新范围。**本次只落地三个输出里的一个**（建筑等级上限），人口上限与兵种等级上限（征兵选级/批量升级/买活）明确排除在外，牵连 `Train` 命令语义是更大的独立工作。落地内容：`CommandKind::Upgrade`（12→13，追加在末尾）；`World` 新增 `b_level_`/`b_upgrade_left_`（`World/9→10`）与 `building_level_cap()`（**唯一算 ceil 公式的地方**，`apply_one` 与 `WorldView` 的 UI 提示都调它，不各自重推——同 `repair_wood_cost` 那条纪律）；`BldStats` 新增三个升级字段、`GlobalStats` 新增 `building_level_cap_divisor`（`Stats/6→7`）；升级与施工/维修互斥（同一时刻一件工程）、工匠在场才推进（点杀维匠一样管用）、`Cancel` 收得掉在途升级但不退款；`land_bld_attack` 补上 `p.lvl_pm`（此前恒等 1000，注释明写「建筑没有等级轴」，现在真的读 `b_level_`）；`player_input.hpp`/`.cpp` 补齐 UI 三层（`can_upgrade_hint`/`can_afford_upgrade`/`upgrade_command`，镜 `Repair` 那组写法）；11 座建筑的 JSON 占位造价——10 座直接抄各自 `cost_stone`/`cost_wood`/`build_ticks`（「升一级约等于再建一次」的最简起点），`Keep` 因为自己的造价是 0（不可再建）另给一组占位（全场最贵最慢）。**已知空转、落地时就写明不是后来发现**：hp 系数是 0，任何建筑靠升级涨的血量数值上是 0，但升 Keep 本身仍有实质效果（抬高其他建筑上限），Tower/Flak 的伤害缩放立刻见效（吃非零的 `dmg_permille_per_level=220`）。补的测试：`economy_test.cpp` 三条（上限门槛 + 双资源 + 工匠在场；与 Repair 的互斥；Cancel 不退款）、`mechanics_test.cpp` 一条（Tower 升级后单发伤害按等级系数缩放，手算比对不硬编码）、`stats_test.cpp` 补 fingerprint 覆盖与 schema 版本号。**过程中揪出两处不相关的既有小问题**：`replay_test.cpp` 那条防「哈希口径变了」的用例用的 `patch_ascii` 要求等长替换，`kWorldHashTag` 从 7 字节的 `World/9` 进到 8 字节的 `World/10` 后，原来等长的哨兵串 `World/X` 差一字节——这与它自己注释里记录的上一次翻车（`World/9` 撞上真版本号）是同一类教训的第三次重演，只是这次过期的是「等长」这个隐含前提，不是版本号本身；另外 `stats_placeholder.json` 里 `_note_1b_wall_upgrade` 那条注释写着「建筑还没有等级字段」，这次顺手订正。**已验证**：Windows/MSVC Release 全量重编零警告，`ctest` 44/44 全绿，`check_determinism_bans.py` 通过。**同日三续（一）：组长核对「实现 vs 文档」，第一条就是「玩家点不到升级」。** 上一轮把机制、命令、测试全做了，却只做到 `game::can_upgrade_hint` 那一层就收工——`render/` 的弹窗一个字没动，于是**这次改动的动机（塔不随波次变强）原封不动留在原地**。「机制落地」与「玩家用得上」是两件事，只有后者兑现动机。补的时候三处不是照抄维修就能得到：①**升级行对己方任何完工建筑都出现，即使当前升不了**（维修行只在掉血时出现）——满血在血条上看得见，而「这座建筑几级/上限几级」画面上一个字都没有，而占位系数 `divisor=2` 下 1 级堡垒的上限就是 1 级，**开局每座建筑都升不了**，那一行若不显示玩家既不知道有升级、也不会想到该先升堡垒。这就是 `game::upgrade_block()` 分四档而不返回 bool 的唯一理由，`can_upgrade_hint` 退化成它 `== None` 的别名（一份判定，不是两份）；②**拾取判据刻意不是`can_upgrade_hint`**——它顶到上限时是 false，用它决定弹不弹菜单等于让菜单在最需要解释时恰好不出现；③**升级与维修、征兵三者都不互斥**，同「受损兵营的练兵入口不该消失」那条先例。**险些漏掉一个会当场崩的缺陷**：弹窗新文案的字要登记进 `render/src/text.cpp` 的 `ui_strings()`，缺码点是**画的时候抛**而不是渲错，而三种文案里有两种只在特定局面出现（顶到上限/有工程在进行）、截图测试点不开那个弹窗——**漏登记不会让任何测试变红，只会在试玩时点一下就崩**；最后是写了个一次性脚本机械比对码点才确认覆盖（那个脚本没入库，同类改动下次仍只能靠人记得）。顺带把 `help_entries()` 补上升级那两行（按键绑定与说明是同一份表）。**同日三续（二）：组长指出 README 的 `§1.4` 还写着「只涨伤害已拍板」，与 `CLAUDE.md` 直接矛盾，并给出正确结论「攻击和血量均 sqrt(L)」——于是把它真的落地了。** 漂移的一侧是**过期的那个结论**（同日早先被推翻），不是措辞。落地：`level_permille` 从 `1000 + k(L−1)` 改成 `isqrt(1000 × (1000 + k(L−1)))`，血量与伤害**调同一个函数、同一个系数** ⇒ `p−q = 0` 由构造成立；两个系数必须相等由 `StatsLoader` 拒绝（填不一样不会让任何仿真报错，只会让 TTK 悄悄发散）；`hp_permille_per_level` 0 → 220，`kStatsShapeTag` Stats/7 → Stats/8、schema stats/7 → stats/8。**当初写在 CLAUDE.md 里的两个实现坑都真的踩到了**：用整数牛顿法而非浮点 `pow`（那个值进 `state_hash`）、形状没变也必须进格。**两条测试因此变红，都是真信号**：`level_permille(3,150)` 从 1300 变 1140；`flow_test` 那条「高档就近破墙」失效——系数是**开方前**的，1000 只给 2 倍而不是 4 倍，破墙 20 tick、直穿 52 > 绕缺口 49.9，field 改选绕路，改成 5000 才回到 4 倍（**想要 N 倍，系数给 N² 量级**）。连带解掉三件：十一座建筑的升级真的涨血量了（那条「已知空转」当天自己消解，一行代码没改）；「城墙/城门可升级只加血量」与「破坏速率随等级」两条终于成对成立；`flow.cpp` 的 `break_ticks` 分子分母同源。`cost_output_matrix.py` 复核**四条结论一条没变**——同级 1v1 时等级系数在比值里抵消，这正印证了 §1.4 的论证（改动只在等级差拉开时显现）。**过程中查出一个比本次改动更重要的构建缺陷**：`build/` 那个目录**静默不跟踪头文件依赖**（`ninja -t deps` 显示 193 个 obj 全是 `#deps 0`），成因是中文 Windows 上 MSVC 的 `/showIncludes` 输出「注意: 包含文件:」而 ninja 按英文前缀匹配、CMake 的探测发生在配置那一刻。后果是**改 `.hpp` 后 `cmake --build` 什么都不做、`ctest` 全绿但跑的是旧 obj**——我在那个目录上跑出的每一次「44/44 全绿」都不可信，两条本该红的断言在那里是绿的。修法是删掉重配（重配后 `#deps 174`，44/44 真绿）。**已验证**：`build-render`（Release + RENDER=ON）**55/55 全绿**、`build`（Debug + RENDER=OFF 重配后）**44/44 全绿**，零警告，`check_determinism_bans.py` 通过，`cost_output_matrix.py` 复核过。 | 见本次结算表 |
| ArLiangz | **同步仓库进度，确认 1c 已由 @zhxxx233 接手**：拉取 main 后发现自己在 #57 上被点名三条无回音、`main` 已推进到 #95，1c 六批机制全部落地、可玩层四件与主菜单也已完成——**#57/#60 上"还等你"的部分已经清零，唯一仍等自己的只有日志空行**（本行即补录）。**转而认领 README 未认领工作表第 7 项：数值标定（成本产出矩阵），此前完全空白**。方法论上没有走"拍脑袋"：`bindings/`/`train/` 都不存在、无法跑批量仿真，改用 Python 逐字复现 `combat_math.hpp` 的公式（`apply_permille`/`level_permille`/`charge_permille`/`anti_charge_permille`）做闭式 1v1 验证，工具留在 `tools/balance/` 供复用。**产出三项结构决定**：①`§1.4`（等级涨哪个属性）用数学证明拍板——同级对拼时任何缩放方案的克制比例都精确不受等级影响（等级系数在比值里抵消），真正的分水岭在"等级差拉开"的场景，两个都涨(L²)让克制比例随等级差**平方**衰减、只涨一个是**线性**衰减，两害相权先排除L²；血量方案又会让 TTK 随等级发散（20级单挑耗时是1级的4.8倍，威胁"短episode"这条训练前提），故选"只涨伤害"；②验证"开阔地怕枪阵，巷战被反克"这条花名册原文点名的机制时，发现占位的 `anti_charge_permille=1800` 根本不够——骑士自身冲锋倍率(2.0x)压过枪阵反冲锋倍率(1.8x)，克制没有兑现，改为 250→180（自身冲锋）与 1800→3200（反冲锋）后两个场景（开阔地/巷战）都验证通过；同一轮验证还发现 `Shade` 压制 `Archer` 不成立，伤害 11→14 修正；③`tools/map_gen/thresholds.json` 自己记录的"Ram 速度与地图尺寸不自洽"——没有选"把 Ram 提速去凑 §3"（会让 Ram 不再是全场最慢，"笨重攻城器械"定位与"Ram 决定行军时间上限"一起垮），改为保持 Ram 慢速、用仓库**真实的** `generate.py`+`validate.py`（不是手算近似）遍历地图边长找自洽点：size=64/68/72 全部 20/20 通过，74 起 60/60 全被第 5 条否决——**size 定为 72**。另差异化了三座资源建筑的产出（此前 12/12/10 几乎相同，未体现三条决策轴的性质差异）。**过程中自己犯过一次错并当场自查改正**：草稿把 `Phoenix.vs_structure_permille` 设成 0，理由是"对城墙城门破坏速率恒为0"——查 `unit_behavior.cpp` 才发现墙/门免疫是 `PhoenixBehavior::can_break_structure()` 的虚函数 override，与这个数值字段无关，字段管的是 Phoenix 打"非墙非门"建筑（Tower/Flak）的倍率，清零等于让它连"点杀防御塔"这个本职都做不到；由此触发一轮系统性自查——把设计稿逐字段与最新占位表 diff，凡无法追溯到已写明理由的差异（`Ranger`/`Ghoul` 的血量伤害、`Phoenix`/`Ram` 的伤害）一律撤回原值。**波次预算曲线与堡垒等级曲线**（`composition_` 数组从未被任何代码消费、`demo_driver.cpp` 的波次生成是作者自陈"演示占位无平衡含义"）只给了公式与推导，**明确标为设计建议非契约**，schema 放哪需要团队一起过。**已验证**：Windows/MSVC，Release 配置 `/W4 /WX` 零警告，`ctest -C Release` 55/55 全绿（含用新 `Ram` 速度与 `size=72` 实测通过的 `map_gen_generate_smoke`），`map_gen/selftest.py` 232 条断言全过，`tools/balance/cost_output_matrix.py` 4 条可数值验证的克制关系全部通过。**同日续：#102（Ram 单体伤害）确认被否，接受组内结论**——细读 splash_dmg_permille 那版修法与 Cody-ic 量出的数字（单体化后 8 个 Ghoul 破墙反比 1 台 Ram 快，正撞 CLAUDE.md「压倒性拆除效率」那句），认同这是更贴近问题本身的修法，PR 已关闭不再纠结。**同日再续：按用户具体要求重新设计《边境要塞》参考地图**（堡垒居中、切比雪夫距离 8 的方形城墙环 + 两个相对城门、内部两片森林/两处石矿/一处金矿/两箭塔/一兵营/一伐木场/一采石场、外部资源随距离递减分散，参考 AoE4 1v1 布局）。**发现并补齐一处地图 schema 的真实空白**：地图文件此前完全没有"玩家开局拥有除 Keep/Wall/Gate 外的其余建筑"这个表达能力（`demo_driver.cpp` 写死的占位编成是唯一途径），新增 `buildings` 字段（`mapfile.py`/`map_data.hpp`/`map_loader`/`world_builder` 五处 + 校验器第 24 条摆放冲突），过程中自己先写错一版——把"采集建筑必须踩在资源点上产出"这个真实游戏机制误判成摆放冲突，验证时被真实校验器当场抓出来，修成「类型匹配则不算冲突，只有建筑与资源类型不符时才报」。**手写地图的权威来源是新脚本** `tools/map_gen/build_reference_map.py`（不是直接改 JSON），用真实 `validate.py` 反复迭代到 22 条已实现检查全部通过（第 9 条阻塞、与本次改动无关）。**Ram 行军占比这条又撞上一次地图尺寸自洽问题**：用户要的城墙半径（8）远小于训练地图集的假设（12-15），56 格地图在当前 `strict.size_min=64` 下过不了第 1 条——**没有再去改 Ram 速度**，而是照上一轮同样的方法（真跑校验器，不手算）把 `size_min` 降到 56，两个集结点实测行军占比 16.7%-33.3% 与 15.7%-31.5%，与目标区间有交集，且与训练地图集的 `size=72` 互不冲突（两套城区半径假设各自内部自洽）。渲染截图核对过整体布局符合预期。**已验证**：Windows/MSVC，Release 配置零警告，`ctest -C Release` 55/55 全绿，`map_gen/selftest.py` 448 条断言全过。**同日三续：用户试玩后反馈数值问题较大，四处一次改完**。①**守方五个移动单位血量翻倍**（`Archer` 100→200、`Spear` 180→360、`Ranger` 130→260、`Scout` 50→100、`Mason` 60→120）；②**`Wall.max_hp` +50%**（800→1200）——**`Gate` 未同步**，用户原话点名的是「城墙」而花名册里 `Wall`/`Gate` 是两个独立条目，按字面只改了前者，副作用是两者血量比从 0.65 被动拉到约 0.43（城门这个既定薄弱点相对更脆），已在 `stats_placeholder.json` 新增的 `_note_8` 里写明，若非预期效果需团队另定是否让 `Gate` 同步；③**波次间隔 +5s、首波延后 10s**——`demo_driver.cpp` 原来用同一个 `kBuildTicksPlaceholder`（160 tick/8s）同时管「首波前」与「每波清完后到下一波」两处倒计时，这次拆成两个常量：`kBuildTicksPlaceholder` 改 260（13s，间隔 +5s）、新增 `kFirstBuildTicksPlaceholder` = 360（18s，首波单独再 +10s）；④**两张手写地图（`reference_border_keep_01.json`、`demo_skirmish.json`）的 `initial_walls` 改成一律满血**——**这不是推翻 2.3「初始墙段允许带残血」那条通用设计**，`generate.py` 训练地图集仍走 `thresholds.json` 的 `wall_hp_frac_range` 保留残血多样性（训练侧的「修旧/建新/造兵」三方决策与本次演示手感调整是两件事）；也不影响「AI 利用已有缺口」评估指标——那条指标测的是 `initial_breaches` 留的完整缺口，与 `hp_frac` 是两个独立字段，`地图与场景设计.md` §2.3 已补一段说明两者的区别与本次改动范围。**改完踩了一处测试假设**：`demo_test.cpp` 里两条 `update(200)` 硬编码「越过占位建造时长 160」来触发首波，首波改到 360 后不再够，改成 `update(400)`；另一条「demo 真的在打」的测试原来 `update(1200)` 一分钟，双方都更抗打 + 首波延后后一分钟内可能一次伤亡都还没发生，假阳性通过的风险变成真的会失败——实测改成 `update(3000)` 后能稳定触发攻防；「无尽模式最终必败」那条测试的 `a.update(6000)` 预算也相应放宽到 20000，避免血量翻倍后来不及分出胜负。**已验证**：Windows/MSVC Release 全量重编零警告，`ctest -C Release` 55/55 全绿（含 `map_gen_generate_smoke`/`map_gen_self_test`），`validate.py` 对重新生成的参考地图 22 条已实现检查全部通过（第 9 条阻塞与本次无关）。 | 数值设计与成本产出矩阵.md / 波次预算曲线与堡垒等级曲线.md / tools/balance/ / stats_placeholder.json / thresholds.json（第一续）；地图与场景设计.md §4.4.0/§6.2/第8节 / game/data/map_data.hpp / game/src/{map_loader,world_builder}.cpp / tools/map_gen/{mapfile,validate,selftest,generate,build_reference_map}.py / game/data/maps/reference_border_keep_01.json（本续）；game/data/stats_placeholder.json / game/src/demo_driver.cpp / tools/map_gen/build_reference_map.py / game/data/demo_skirmish.json / game/data/maps/reference_border_keep_01.json / tests/demo_test.cpp / 地图与场景设计.md §2.3（三续，见 #112） |
| zhxxx233 | **同步队友进度、代审代合并 #108/#109，随后按组长拍板完成地图生成系统重构。** 上午两条试玩 bug（城墙口两侧免费缺口、攻方波次强度）已修合并（#106/#107）。同步发现并代审合并：**#108（Cody-ic）建筑等级上限**（`CommandKind::Upgrade` 12→13、`World/9→10`、`Stats/6→7`，审过命令枚举/哈希口径/指纹/唯一公式点/互斥与退款规则，无问题；与 #109 在 README/日志上冲突，在队友分支上解掉、保住作者名）。**#109（ArLiangz）参考图重设计 + `buildings` schema**（审过三侧必填校验与第 24 条摆放冲突，无问题）。**同日续：组长试玩反馈三件事，扩成一次生成器重构（走 Plan Mode，组长逐条拍板）**：① 参考图设计语言应适用于**所有**随机图（城内森林/金矿、预置建筑）；② 两个数据级 bug——城外金矿不保证存在（实测 `gen_01006000` 城外零金矿）、簇种类纯随机可全石贴成一坨（「大片石头集中在一整块」）；③ **取缔「走廊」概念**（组内建议，城外地形随机散布参考 AoE4），且**城圈方环应为城墙实体而非 Rock 岩壁**——旧「天然岩壁承担大部分周长」是文档错误表述，一并订正。落地：`generate.py` 重写（`place_ring_walls` 整环墙 + 一对相对城门 + 设计缺口、`place_inner_content` 城内 2 片 L 森林 + 2石/2金/1-2木 + 预置建筑微随机 + 堡垒 3 格净空、`place_outer_clusters` 簇内点距≥2/每簇≥2种/全局城外金≥2/簇距递增 + 返回簇记录供自检、`scatter_wild_terrain` 随机游走团块散布、`paint` 共享给参考图）；`validate.py` 第 3 条废除留号（`REMOVED`）、新增第 25 条城外金矿、第 12 条改语义；schema 删 `spawns[].corridor`（Python + C++ 五处 + 全部地图重序列化）；参考图脚本改共享步骤函数的薄壳（固定种子 20260831、56×56、半径 8）；selftest 重写/新增六组生成器断言（427 断言 50 组）；文档五份同步。**重标定实测**：size 64–76 全部 20/20 通过（旧「74 起全否」结论随墙变整环失效，新上限 76）、80 起全否；`-n 30` 丢弃率 31.8%（第 5 条 13 次 + 第 11 条 1 次，新否决面偶发且重试即消）；12 张池图重生成全过 strict、参考图重生成全过。**已验证**：MSVC Release 零警告、`ctest` 55/55、selftest 427 断言、确定性守卫通过、渲染截图三张核对。 | 见本次结算表 |
| omg321-975 | **修「城墙上站人，人会站在墙外」——试玩报告的一条，查出来是两处独立缺陷叠在一起，而两处都不会让任何测试变红。**<br>① **抬升的量纲错了**：`battle_scene.cpp` 给驻守单位填 `lift = 0.75`，`scene_renderer.cpp` 把它乘 `tile_h()`——而 `tile_h` 是**菱形半高**（128 px，地面两轴的换算），竖直方向的一格边长是 `sin(60°)/√2 × px_per_tile ≈ 156.8 px`（由 `blender_render.py` 的相机俯角与 `TILE_SIDE` 定死）。混用让任何抬升都系统性偏小 22%，弹丸那条路径也一样中招（只是那个偏差没人看得出来）。而 `0.75` 本身还远远不够：墙精灵顶端在格心地面点上方 258.7 px，`0.75 × 128 = 96` 只把脚抬到墙高的 **37%** 处，人于是贴在墙的外侧面上。补 `IsoProjection::tile_z()`，注释写清它不是 `tile_h`<br>② **抬多少刻意不在 `game/` 定**：`DrawItem` 新增 `stand_on`（脚下那座建筑的标识符），像素归 `render/` 按那座建筑的精灵几何算（`SpriteAtlas::stand_lift_px`）。城墙 309.7 / 门楼 313.7 各不相同，在 `game/` 写一个数必有一处错，且那会是「地图与场景设计.md」第 7 节禁止的**第二个像素真相来源**。不用 alpha 包围盒反解顶端——这套墙板逐朝向不对称（`Wall` 的 SE 顶端 258.7、NE 只有 205.7），照它抬会让同一段墙上的人按走向高低不一。系数 `kStandFrac = 0.67` 是**目视标定的占位视觉值**（实测 0.58 脚陷进墙里、0.75 悬空），已按组内纪律标注为占位并写下重新标定的办法<br>③ **顺带查出 demo 里其实从来没有人站上墙**：驻守目标写成「堡垒正东三格」这样的固定偏移，那是单张固定地图时期的写法；换成随机地图池（`city_radius` 也随机）之后墙离堡垒有远有近——实测 `keep=(36,36)` 而最近的墙在 `i=50`，指令落在空地上被 `apply_one` 直接丢掉。改为找离堡垒最近的那段初始墙，并**沿墙的走向**取三格（原先固定沿 `j`，三格里有两格不是墙，只有中间一个人登得上，看起来像「登墙只支持一个人」）；`garrison_wall()` 入参取 `MapData` 而不是建好的 `World`，让摆兵与下令两处从同一个函数拿答案<br>④ **三条守卫都做了破坏性验证**：`verify_stand_geometry()`（挂进既有的 `render_verify_assets`，系数改 0.10 / 1.20 各退出码 4，它挡量级错、不替代目视）、`[iso]` 钉 `tile_z()`（期望值独立重算，刻意不引用被测常量）、`[demo]` 钉「三名弓手登上真墙 + `stand_on` 与脚下建筑一致」。**最后这条第一版是假绿的**——用 `demo_skirmish.json` 跑时 `keep=(4,6)`、墙在 `i=7`，被换掉的那个固定偏移**恰好命中真墙**，把驻守目标改回旧写法照样全绿；换成实战尺度的 `reference_border_keep_01.json` 之后三组破坏各红在该红处。**这不是挑一张更难的图，是挑一张判据不退化的图** | `d8c70e7` |

**本次决定**：

- **数值设计不再是空白**：`game/data/stats_placeholder.json` 从"一个都没标定"换成一版有方法论、逐条验证过关键克制关系的 v1，具体见「相关提交」栏的文档
- ~~**`§1.4`（等级提升哪几个数值）拍板为"只涨伤害"**~~ ~~**当日复核后重开，待表决。**~~ **同日落地：血量与伤害各开一份平方根（`p=q=0.5`，两个系数都取 220），`kStatsShapeTag` 进到 Stats/8。** 原论证（`数值设计与成本产出矩阵.md` §2）本身没错，但它假设了「每属性线性增长」；放开成 `血 = B(L)^p` / `伤 = B(L)^q` 之后，「克制比例衰减」只吃 `p+q`、「TTK」与「破墙时间」只吃 `p−q`，两者正交，于是 `p=q=0.5` 在战力曲线**逐点相同**的前提下把 TTK 与破墙时间从 ×0.19 / ×0.20 修成 ×1.00 / ×1.03。**拆法已定并已落地**：`p−q = 0` 是结构约束，由 `StatsLoader` 强制「两个系数必须相等」；`p+q` 是数值，**取 220 作占位**（那个值使战力曲线与 #96 那版逐点相同），仍要与波次预算、堡垒等级两条曲线一起标。实现走整数牛顿法 `isqrt_permille`（不用浮点 `pow`——那个值进 `state_hash`），且**形状没变也进了格**（Stats/7 → Stats/8）：一张 stats/7 的表在新公式下每个数都还合法，不进格它会载入成功并算出一整局不同的仗。见 `CLAUDE.md` §1.4 段
- **第 14 条那条上限不再手写，改由数值表推导。** 它此前让一条结构约束「绿着被违反」（`Watch` 视野 12 > 声明的 8，实测 16/16 个集结点被照亮而校验器全绿）。同类耦合以后按同一条先例办：判据留在校验器、数值只从 `stats_placeholder.json` 取一份
- **地图生成的随机种子不是待定项**——`generate.py --seed` 早在用,`generator` 块的「固定项」也有明文理由（固定项若随机,训练数据混进与策略无关的方差)。而当前风险与直觉相反:**不是奇形怪状,是过于同质**(丢弃率 0%,且「合法但走廊性质没体现在地形上」不会让任何检查变红)。新增第 23 条(Forest 不得碎成粉尘)补掉森林那一半——森林的三项职责都依赖连片而总面积一模一样,此前无人管
- **倾向不做「拖动建筑」这个动作,改为补一条 `Demolish`,前端把拖动手势翻译成「拆+建」。** 「某波次后允许」是数值劝退而非结构封死;自由挪动会打掉「AA 摆在哪」的机会成本(整个防空两难靠布局是不确定下的**承诺**)与「防御必然有薄弱处」。决定性的是命令面:拖动要两个槽位 ⇒ 又撞上「调兵怎么表达调哪一支」那个待定项,而 `Demolish` 只要一个、与 Build/Repair/Cancel/Clear 同构、不新增动作头。**它动命令枚举(回放的线路编码),所以待表决而非直接做**,改动清单已写进 CLAUDE.md
- **平衡工具的「通过」不再等于「谁胜」。** CLAUDE.md 要的是「显著更少的资源投入」，所以加了「幅度薄」这一档；而真正的资源盈亏**算不出来**，因为「编成位占用」这个字段不存在——那是下一步的前置条件，不是「以后优化」
- **地图边长的自洽值定为 72**（与 `Ram` 速度 0.045 格/tick 精确对应），但这是"自洽"不是"最终标定"——真正的地图尺寸仍要等 episode 长度与训练截断波数定了复核

**遗留问题**：

- **波次预算曲线（编成位/兵力预算的具体公式）与堡垒等级曲线仍是设计建议，没有 schema 承载**——`composition_` 数组没有消费代码，需要团队一起定是否新增契约、字段放哪
- **闭式 TTK 近似不是真实批量仿真**，只验证了 Spear/Knight/Archer/Ghoul/Shade/Ranger 几对旗舰级克制关系；Scout/Mason/Wraith 的猎杀关系、Phoenix/Flak 的位置性克制这类靠地图与 AI 行为体现的关系，留给协同演化阶段用真实对局验收
- **重建成本沿用占位值未单独复核**（废墟回收比例=造价÷3 这条结构已定，具体数字随 v1 一起改了但没有针对性验证）
- **新增三项要组里定的**（都在 README 未认领工作表第 7 项里）：`§1.4` 的 `p+q` 取值与是否改 `combat_math.hpp`（要动形状、旧回放重录）；**编成位占用**这个字段加不加、加在哪；波次预算 / 堡垒等级曲线的 schema 放哪
- 组内待表决的"要不要让机器跑 GCC 那一轮"（GitHub Actions）与其余历史遗留问题（组长姓名、四人日志空行）均未变化，见前几次记录
- **#102 仍开着，等作者回应**：评论已指出「没写为什么」「与 `CLAUDE.md:735` 冲突」「与本次改动有三文件合并冲突」三点，未合并、未关闭
- **`Ram.splash_dmg_permille = 500‰` 是占位比例**——闭式 1v1 工具测不出「几个目标同时挨打该怎么分」，真正的数字要等批量仿真或至少一次多目标场景的手动验证
- **建筑等级上限落地了，但堡垒轴另两个输出（人口上限、兵种等级上限/征兵选级/批量升级/买活）仍未做**——牵连 `Train` 命令语义，是独立的更大工作
- **「升级完成即满血」顺带成了一次免费全额维修**（`finish_upgrade` 把血补到新上限）。对一段残血的墙，「升一级」可能比「修满」划算——升级要石+木、维修只要木，但维修单价随缺口走。它受等级上限约束、不能无限用，所以不是漏洞，但它给「修旧/建新/造兵」那个三方决策加了一条斜边，**要不要留是设计层的事，留给组里定**
- **弹窗那一层仍然没有自动化守卫**：`render_battle_shot` 截的是对局画面、点不开任何弹窗，所以「升级行显示对不对」只能靠试玩。这次险些因此漏掉一个会当场崩的缺陷（新文案的字没登记进 `ui_strings()`，缺码点是**画的时候抛**而不是渲错），最后是靠一个一次性脚本机械比对码点才发现的——**那个脚本没有入库，同类改动下次仍然只能靠人记得**
- **建筑等级没有进观测**：`CLAUDE.md` 说「守方单位等级与建筑等级**共用**已有那条等级通道」，但 `obs_pack.cpp` 的建筑那一段只喂 hp/max_hp，`ally_level`/`enemy_level` 是纯单位通道。攻方 RL 现在看不见塔升到几级——落地时明确排除的范围，但文档读起来像已经接上了
- **守方脚本与 demo 波次循环都不会下 `Upgrade`**，所以 `AI vs AI` 与自动 demo 里防御建筑永远 1 级
- **`p + q` 仍是占位 220**——它管「一级值多少战力」，要与波次预算、堡垒等级两条曲线一起标。`p − q = 0` 那一半是结构约束、已由 `StatsLoader` 强制（两个系数必须相等），不是旋钮
- **中文 Windows 上 Ninja 构建目录可能静默不跟踪头文件依赖**（`/showIncludes` 前缀对不上）。这次实测撞到：`build/` 里 193 个 obj 的 deps 全是 0，改 `.hpp` 后 `cmake --build` 什么都不做而 `ctest` 全绿。**没有守卫**——那要在 ctest 里质询构建系统自己那份（正是坏了的）元数据；防线只有 `CLAUDE.md` 新增那一节 + 「注意: 包含文件」刷屏这个可见症状
- ~~**`hp_permille_per_level` 仍是 0**~~ **已解决**：`§1.4` 同日定为「各开一份平方根」，两个系数都成 220，十一座建筑的升级全部真的涨血量，**一行代码没改**——那条「认下已知空转」的判断兑现了。连带「城墙/城门可升级只加血量」与「破坏速率随等级」两条终于成对成立
- **11 座建筑的升级造价全是「抄一遍建造造价」的占位**，不是标定值；`Keep` 那组（200/200/400）更是纯拍的
- **`building_level_cap_divisor=2` 抄自 `波次预算曲线与堡垒等级曲线.md` §2 的占位系数**，那份文档整体仍标注「设计建议，非实现」——这次只落地了它的一条公式，不代表整份文档已获团队批准
- **`Gate.max_hp` 在这轮「城墙 +50%」里未同步调整，仍是 520**——按用户原话字面只改了 `Wall`，需团队确认这是否是预期效果（城门相对更脆的比例被动拉大），若不是需要另外决定 `Gate` 是否也 +50%
- **训练地图集（`gen_*.json` 12 张）与生成器的 `wall_hp_frac_range` 本轮未动**——「所有城墙一开始都是满血」只应用到两张手写演示地图，训练侧的残血墙多样性判断为与本次演示手感调整无关，未经团队确认，如认为应当同步需另开工作

---

**本次结算**：

| PR / 提交 | 内容 | 状态 |
|---|---|---|
| #96 | 数值设计 v1——成本产出矩阵工具 + 逐条验证的数值表 | 已合入 |
| #97 | 简化守方交互：框选取代键位模式 | 已合入 |
| `05f97ce` | 第 14 条的上限与数值表脱钩，让「绿着的违反」变红 | 复核产出 |
| `cd2723e` | 撤回两处无理由的数值改动 + 矩阵判据改成三档 | 复核产出 |
| `8a160de` | 等级曲线拆成两个正交自由度 + 预算节奏模型 | 复核产出 |
| `4a8d8cc` | 把四处订正写进契约层（CLAUDE.md 那节与 README 曾互相矛盾） | 复核产出 |
| `ab510b4` | 第 23 条:Forest 连通块不得碎成粉尘 | 组内提问查出 |
| `1a270a6` | AOE 溅射折扣：`UnitStats::splash_dmg_permille`，`Stats/5 → Stats/6` | 组长当场指出并要求「先改完」，已推 #103 |
| #108 | 建筑等级上限：`CommandKind::Upgrade`，`World/9→10`、`Stats/6→7` | @zhxxx233 代审代合（无问题，API 直接合并） |
| #109 | 参考图重设计 + `buildings` schema + 校验器第 24 条 | @zhxxx233 代审代合（与 #108 冲突在其分支上解掉，保住作者名） |
| #113 | **地图生成系统重构**：取缔走廊、城圈改城墙实体、城外随机散布（AoE4 式）、校验器第 25 条、`spawns[].corridor` 废除 | 组长拍板后落地，@zhxxx233 提交并自合 |
| #111 | 升级的玩家入口 + `upgrade_block` 四档理由 + 弹窗文案登记 | 上一条只做到 `game/` 那一层就收工，**玩家点不到升级**，等于把动机原封留在原地；组长核对时点出来 |
| #111 | `§1.4` 落地：等级缩放改成各开一份平方根，`Stats/7 → Stats/8` | 组长核对文档时点出 README 那条还写着「只涨伤害已拍板」，与 `CLAUDE.md` 直接矛盾——**漂移的一侧是过期的那个结论**，不是措辞 |
| #112 | **试玩反馈数值再平衡**：守方五移动单位血量翻倍、`Wall.max_hp` +50%（`Gate` 未动，见 `_note_8`）、波次节奏放缓（间隔 +5s / 首波 +10s）、两张手写地图初始墙满血 | @zhxxx233 代审代合 |

---

## 2026-08-30（第 5 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | **补上队友做不到的那一半验证：Linux/GCC 侧两轮，覆盖今天全部五个 PR**。他在 #57 标了 ⚠️「服务器 GCC 侧欠了今天全部五个 PR，不在校园网内连不上」，而 `game/` 新增四个 `.cpp` 与 `tests/economy_test.cpp` 都在 GCC 编译面内。两轮结果：`940133d`（#71–#74）**0 warning、31/31**；`53be3b3`（加 #75）**0 warning、32/32**，差的一条正是新增的 `rts_tests_econ`。**顺带定了一个报告口径**：服务器 32 与本机 39 差的 7 条是 `render_*`（`RENDER=OFF` 是训练环境的定义），所以该写「GCC/`RENDER=OFF`：32/32」而不是「32/32 全绿」——**同一个数字在两台机器上指不同的集合，而看报告的人只会记住数字**。并建议把这一轮归口给我，理由是它卡在校园网而不是能力。**否证了一条被诚实标注为「待验」的推测**：他判断 `SpriteAtlas::dirs_of()` 是 #63 那 9 处 `-Werror=dangling-reference` 的形状，我在服务器上实测——真实文件 0 条，改成引用参数**仍然** 0 条，而同一套选项在还原后的 `map_loader.cpp` 上报 9 条（自证检查生效）。最小分离实验钉出触发条件：**决定性变量只有「引用参数是否绑定到临时量」，`range-for` 不是保护**（引用参数 + range-for 照样炸）——所以「用了 range-for 所以安全」是个错的安全理由。**方法论上的教训**：第一版复现零警告，但那时对照组也没炸，所以那个结果什么都证明不了；是补上「让对照组真的炸」才让整件事可信。**另**：整理本地（main 落后 68 个提交、20 个分支、一个遗留 worktree），删分支的判据用 `git cherry` 而非 `--merged`——有三个分支在 `--merged` 里没列出来但内容其实全在 main（squash 痕迹）。 | #74 / #57 的 GCC 验证评论、#70 的实测否证 |
| ArLiangz | | |
| zhxxx233 | **1c 正式开工，三个叠放的 PR 打到「有一个能看的 demo」**：#72 数值表进入路径与指纹（`rts/stats.hpp` + `StatsLoader` + 占位表，`kWorldHashTag` → `World/3`、回放格式 → 2、新判决 `StatsMismatch`，顺带收编 `InitialHp`）；#73 机制第一批（承伤 / 目标选择 / 移动 / 建筑攻击 / 视野，`World/4`，乙的掩码精确化落地，AOE 落点锁定有专门用例）；#74 演示对局（`BattleScene` + `DemoBattle` + 手写演示图，`rts_render --battle`，截图模式可带 `--ticks` 离线验收）。**同日续：#71–#74 按序合入 main（自合是既有惯例），随后开机制第二批**——经济闭环与六种命令解算（`Build`/`Repair`/`Cancel`/`Train`/`MoveForce`/`Clear`），施工与维修由 `Mason` 在场推进（点杀工匠真的能停工期），采集建筑踩点产出 + `Keep` 金币地板，`Stats/2`、`World/5`，新增 `[econ]` 一族用例含两条哈希喂入探针（破坏性验证过：注释掉一条喂入，探针当场红）。**同日再接手 `tools/map_gen/` 的最后一大件并做完**：阈值归口 `thresholds.json`（§10 早写了「不要硬编码」，**待定的从来是值不是归口**，归口一有含数值的第 1、2、5、14 条就都实现了）、生成器 `generate.py`（约束式：固定项读配置、随机项按种子、每张跑校验器、不过就丢弃重生成、`map_id` 编码种子），第 20 条按 §8.1 自己写的那句移出校验表落到生成器的批量报告。**第 5 条落地当天抓到一处占位值不自洽**：数值表里 `Ram` 速度 0.8 格/s 与 §3 量级表假设的 1.5–2 格/s 差一倍多，按前者算 80 格的图行军占 25%–50%、超出 §3 锁死的 10–20%——自洽的是 §3、偏离的是那个速度，刻意不改数值表（会动 fingerprint、且该由仿真侧定），生成器暂取 size=64 并把矛盾记进配置。**两处 bug 是靠看产出发现的、不会让任何检查变红**（林地走廊一株森林都没撒、`defile` 夹壁起点错了一个半径），因此加了 `--preview` 与一条「林地走廊沿途必须真有森林」的自检。**同日三续：机制第三批——驻守与高度优势**（`world.hpp` 文件头明写这对要成对、单独一批）：`Garrison` 解算成**按格**的常设驻守指令（按格不按编队——一个编队要能同时驻守多格，且与 Build/Repair/Clear 同构）、登墙在 `World` 内解算（延迟查表、一格一人、钉住、`MoveForce` 即召回、墙塌人落缺口站着），高度优势三条幅度全查表（miss / 减伤 / 远程射程加成，`EngageRange::Ranged` 是「远程才吃加成」的结构谓词）而**墙血 < 一半三条一起消失**（局部破损的二值实现，不引入新数值）；**12 种命令自此全部解算**，`deferred_command_count()` 按当初的约定删除，「漏写解算分支」改由无 default 的穷举 switch 在编译期兜住。`Stats/3`、`World/6`；`UnitInit` 补 `force` 字段且**攻方带编队在建局时被拒**（「编队是守方概念」由此是结构不是约定）。新增 `[garrison]` 10 用例：miss 用 0‰/1000‰ 两端做**无偶发**的确定性验证；哈希探针**逐字段隔离**（笼统的「两世界不同 ⇒ 哈希不同」会假绿——旧字段本来就在哈希里；破坏性验证过：摘掉任一新喂入，对应隔离段当场红；把「墙血减半」判据改恒真，两条局部破损段当场红）。演示对局顺带升级：三名弓手开局受命驻守墙线含门楼，`BattleScene` 给登顶单位加 `lift`（像素换算留在渲染侧，§7 分界），截图实测弓手真的站上墙顶、门楼后被 Ram 撞开。**同日四续：机制第四批——冲锋、克制倍率、齐射**（#80）：冲锋动量逐 tick 累实际位移（站停/撞停归零、前摇冻结、落地耗尽，「冲完就是冲完了」），「伤害 ∝ 助跑距离」机制等级无关；克制倍率**由三轴推导不平铺 N×N**——唯一的数值克制关系是 `counters_charge()`（Melee × HeavySlow，轴组合而非点名兵种）打有动量的 `Charge`，**幅度随对方动量线性放大：同一份动量既给冲锋加成也给枪阵加成，「开阔地克、巷战被反克」因此不需要读地形**；`Tower` 齐射 = 建筑 AOE、落点承诺即锁定（与 Ram 同一条规则），**对空建筑结构上忽略 AOE**（「AA 只做单体狙击型」，同「表不能把瞭望塔配成印钞机」）。`Stats/4`、`World/7`（+`u_charge_`/`b_aim_`）。`[charge]` 7 用例：反冲锋用**前摇冻结动量**造出确定的判定窗口（不赌 tick）；破坏性验证两组——齐射落点改成跟着目标走 ⇒「散开躲齐射」当场红；轴推导放宽成「所有近战都克冲锋」⇒ Ranger 反例当场红。demo 攻方补一名 Knight（开阔走廊直线满动量，首击明显重于互殴）。**同日五续：机制第五批——在途弹丸**（第四组实体）：谁放弹丸是结构（单位侧 `launches_projectile()` = Ranged × 非空中，恰好 Archer/Shade——Phoenix 是 Ranged × Aerial、俯冲直击；建筑侧凡开火皆弹丸），单体弹丸**追踪目标句柄**（目标死了箭落空、不转火）而齐射弹雨**飞向锁定落点**（散开的躲闪窗口从「前摇」延长为「前摇 + 飞行」），高度 miss / 减伤挪到**命中那一刻**按目标当时的状态结算（飞行中登墙真的能挡箭，有专门用例）；弹速查表、**0 = 瞬时**——§1.1.1 那条「落地帧瞬时结算」的书面近似不是删掉而是降级成 0 速度的退化形态，第五批之前的每条测试因此一字不动。弹丸**无句柄**（没人引用它，SoA + 稳定压实，与前三组刻意不同），全部字段进哈希。`Stats/5`、`World/8`（预演过的「加一组实体就进格」这次真的按预演兑现，零预留空位）。`[proj]` 9 用例：追踪的主断言是**命中时机**（单看最终掉血区分不了追踪与锁点——锁点的箭按句柄结算照样扣血）；弹丸字段的逐字段哈希探针**不可构造**（同 u_charge_，诚实记录），覆盖由「回放跨越放箭/飞行/命中窗口」承担。破坏性验证四组全红过（追踪退化成锁点 ⇒ 时机断言红；miss 不在命中掷 ⇒ 登墙挡箭红；谓词放宽成凡 Ranged 皆放箭 ⇒ 结构锁 + Phoenix 用例红；0 速不再瞬时 ⇒ 新旧 7 条用例齐红）。渲染侧按 §8.3 走「按飞行角旋转」路径（角度从**投影后**的方向算——等距不保角；旋转中心用 `pivot` 不用 `ground_anchor`），`Flak` 的弩矢是 `Bolt`、其余是 `Arrow`，截图实测箭在墙线两侧沿飞行方向横飞。**同日六续：机制第六批——flow field 与出城（1c 收官）**：档数**不按预告自取默认**——把 1 / 2 / 3 / 动态四案的得失整理成决策简报发 #57 交组内定夺（选 C：3 档；2 档的表达力在固定边界下会随波数塌缩，动态档把契约形状从常数变变量），组内拍板后才动工。field 是 `WorldView` 之上的**纯函数**（`rts/flow.hpp`）：不进世界状态、不进哈希、不进回放（回放存的是脚本按 field 选出的动作——§4.6 的界线再次生效，改代价模型不触碰口径）；**档界刻意不进 `StatsTable`**（表指纹的职责是区分「布局改了」与「跑歪了」，field 两者都不改，收进表会让旧回放谎报 `StatsMismatch`）；档的代表等级取**档下界**（「多绕一段」远比「站杀伤区啃啃不穿的墙」便宜，取下界把后者结构性砍掉）。代价 = 行军 + 破坏，破坏时间与机制走同一对 combat_math 函数；通行规则与 `cell_open` 逐条对应（斜向穿角对实体同样成立、平局归 (代价, 格下标) 全序的先到者）。**顺带修一处可玩性硬伤：城门此前连自己人都拦**——守方地面单位现可穿行完工的 `Gate`（没有这条「被迫出城」在结构上不可能发生），攻方照旧要砸开。`World/9`——**第一次为行为变更而非布局变更进格**：喂入清单没动但相同输入的推演结果变了，不进格旧录像报 `Diverged`（一次注定失败的排查），进格报「口径变了，重录」。进格当天顺带抓到 replay 测试里一处「写死必然过期」的新变种：「另一个口径」硬编码成 `World/9`，真口径走到 9 的那天补丁失效、测试红——已改成永不为真版本号的 `World/X`。`[flow]` 8 用例：档数改变路线（同图同兵种，低档绕缺口高档破墙，代价可手算）、穿角 + 平局归属（一条用例双职责）、城门三兵种三种代价、目标格敞开、回放跨越穿门窗口。破坏性验证**五组**全红过（穿门谓词阵营写反 ⇒ 机制侧红；平局改写放宽 <= ⇒ 归属翻转红；穿角弱化成单角 ⇒ 代价红；档代表等级恒取低档 ⇒ 路线红；flow 侧城门例外摘除 ⇒ 守方代价红——第 1 与第 5 组合起来锁「机制与 field 两处同真值」）。demo 攻方推进从贪心方向换成 flow field，按（兵种 × 档）缓存、每个决策拍作废重算，截图实测攻方沿代价最低的门楼一线收敛。**同日七续：守方单兵执行层脚本（可玩层第一件，README 第 6 项落地）**：`game::DefenderScript`，按 2.5 的判据只写克制表里写死的三条战术（`Archer` 拉扯——威胁进圈后撤、出圈回头放箭，「打一下退一步」由距离震荡自然给出；`Spear` 堵缺口**不追击**——被风筝出阵位正是 Shade 克它的机制，脚本不能亲手送；`Ranger` 避骑士摸攻城锤——不与 Knight 对冲、没命令时主动出城摸 Ram），图里没写的（包夹佯攻择机）一概不写、留给攻方 RL。它同时是玩家命令的执行手（World 记账、脚本走路）：`MoveForce` 开拔按 flow field 走（**穿城门出城靠它**，贪心会怼在自家墙上）、`Garrison` 各奔**最近**指令格（`force_target_` 只指最后一格，多格驻守时全员挤一格、其余格永远空着）、`Clear` 走过去撞上自动破坏、`Mason` 没命令自动找活。参数在 `ScriptParams`（全占位），随机化范围等训练侧开工再定——现在定就是拍数值。`WorldView` 补两个只读直通（`stats()` / `action_mask()`），理由记在契约。`[script]` 8 用例、每条战术配「关掉参数就退化」的对照；破坏性验证三轮全红过，**其中两轮先抓出了测试自己的假绿**：穿门用例原先把门摆在直线上（贪心碰巧也能过，锁不住 flow 的载重——门挪到墙线顶端才红）；驻守用例原先只下一格指令（`Garrison` 解算顺手设的 `force_target_` 把脚本的「走向最近指令格」完全遮蔽——改成两格相隔的指令才红）。**破坏性验证的价值这回不在证明代码对，在证明测试真的在锁**。demo 守方切到脚本。**同日八续：波次循环与胜负判定（可玩层第二件）**：`DemoBattle` 从「摆好两队打一场」改成无尽模式的完整形状——建造倒计时（占位 160 tick）→ 按占位编成曲线在集结点生波 → 攻方清空进下一波（名义等级随波缓涨）→ **Keep 陷落即败且败局定格**（再 update 不推进，好看清最后一帧）。编成落位用固定偏移环不掷点（demo 的确定性不该依赖生成散布）。HUD 补阶段与败局（「建造 N / 进攻中 / 堡垒陷落·败」——新文案要登记进 `ui_strings()`，漏登记会当场抛，这个刻意的坑这回真踩了一次并当场修）。占位数值下实测：守方清掉第一波、撑到波 4、Keep 于 t≈3700 陷落——**必败是结构不是数值碰巧**（demo 守方无补员、攻方编成随波涨），测试据此断言「波数 ≥ 2 且最终必败」而不钉具体波数。**修一处自己刚写出的假绿**：旧断言拿单位总数判战损，波 2 生兵把总数抬回去、假绿——改成只看守方与建筑（守方无补员，单调）。破坏性验证两轮红过（败局不定格 / 清波不进下一波）。**同日九续：交互层首版（可玩层第三件，「不考虑 RL 的基本可玩」自此成立）**：命令的**语义**归 game/（`player_input.hpp`，默认构建可测）、拾取与按键归 render/——右键一个键按格上的东西分三种命令（完工墙/门=驻守、活障碍=清野、其余=开拔），数字键 1–4 选编队（demo 开局全员有编队——没编队的单位在「按编队下令」的命令面里指挥不动），B 建造模式（TAB 换型、左键放置、虚影描边绿/红提示——**真正的裁决在 World 的 Build 解算**，提示只是颜色）、N 提前召唤。生波从「倒计时走完」改挂「进攻阶段的第一拍」，Summon 与倒计时两条路在那里汇合——否则玩家召出的是一个空波。DemoBattle 加 `submit_defender`（写入面收在一处）与占位开局资源（石木全零则 Build 永拒，建造没法演示）。`[input]` 3 用例含「三种命令真提交一遍 World 都受理」；破坏性验证三轮红过（工地墙也给驻守 / 提示无视障碍 / Summon 召出空波）。**同日十续：外壳与双击启动（可玩层第四件）**：起因是「启动方式对用户太不友好」——三个必填路径参数对开发者是好的，对双击 exe 的人不可用（资源管理器不传参数）。分两层做：`game/` 侧是**素材自动发现**（三个标记文件同时命中才算仓库根，从「工作目录」与「exe 所在目录」逐级向上；`args[0]` 在 Windows 换成 `GetModuleFileNameW` 的绝对路径——CRT 给的那个从 PATH 调起来时只是个名字）与**屏幕状态机 + 菜单模型**（`GameShell` / `MenuModel`，主菜单 / 对局 / 暂停 / 说明 / 败局五屏，都在默认构建里被测）；`render/` 侧只剩像素（`MenuView`，`draw*` 与 `hit_test*` 共用同一份版面函数并收同一个 `Chrome`——两处各算一遍的症状是「看着点在这一项、选中的却是上一项」，还随窗口尺寸变化）。**两条定法**：按键绑定与游戏内「操作说明」是同一份表（`help_entries()`，于是「改了绑定忘了改说明」不可能发生）；丢进度的入口只有「开始新对局」一个（「返回主菜单」只换屏——**先写成顺手丢对局，那会让主菜单的「继续对局」永远是灰的**，一个恒不可用的选项比没有更糟，已改并补测试钉住）。平台侧三件收在 `cli_entry.cpp` 一处：双击时藏控制台（判据是「控制台上挂着的进程数 == 1 且没有参数」；**不改成 WINDOWS 子系统**——那样命令行输出与几条 ctest 一起瞎）、失败弹对话框（藏了控制台等于藏了 stderr，否则失败的表现是「双击之后什么都没发生」）、`SetExitKey(KEY_NULL)`（raylib 默认把 Esc 当关窗，不改则玩家想开菜单却把程序关了、在他看来就是崩溃）。字体烘焙字号 32 → 48（菜单标题 44 px，画得比烘焙字号大会糊）。新增 `[menu]` 9 用例与 `[assets]` 5 用例，破坏性验证三轮红过（导航不跳过禁用项 / 返回主菜单丢对局 / 发现不再逐级向上——**其中一轮先撞上「构建失败却拿旧二进制跑出结果」那个坑，第二次改成能编译的破坏才算数**）；`render_*` 新增三张截图 ctest（主菜单**且一个路径参数都不给**——素材发现那条路径只有双击的人会走，也就是最不会有人发现它坏了的一条；说明屏是版面最容易崩的一屏；对局屏）与一条 `render_bad_screen_is_red`（屏名拼错会安静地拍一张主菜单，而那张图看起来完全正常）。`play.bat` 落在仓库根，CRLF 由 `.gitattributes` 保证。顺带把 `CLAUDE.md` 里那对写死的 ctest 条目数（`35/35` 与 `28/28`）**删掉而不是改成新数字**——同一份文件里立的「条目数不写死」那条规矩，已经被写死的数字破过三次。**同日十一续：同步队友进度，复核并代合 #86（魔法弹精灵）**：它自称「不碰 C++，因此不需要 GCC 轮」——对 GCC 成立，但 `_sprite_meta.json` 有两个**只在 `RTS_BUILD_RENDER=ON` 时存在**的 C++ 消费者（`render_verify_assets` 按 C++ 读取器的文件名规则把声明的每张精灵载一遍；`render_utf8_path` 的 `GLOB *_idle_*.png` 会把新增那张一起复制进中文目录），也就是**只有 MSVC 侧跑得到**，所以先把 `main` 合进他的分支、在合并后的形状上验（38 实体 / 487 张 / 弹丸 3 张；53/53 全绿）。推在他分支上两处小修：删掉一张全仓无人引用的临时渲图 `tools/sprite_gen/_tower.png`（**趁合并前删，进了 `main` 就去不掉历史里那 73 KB**）；对齐 README 一行里「弹丸横躺一张」与三句之后「魔法弹是球」的自相矛盾（`CLAUDE.md` 那份副本他已改对，README 这份没跟上——同一件事写在两处然后漂移，这次两处相距三句话）。**并答掉他挂给我的两条契约级问题**：弹丸来源字段先答了「取甲」（新增 `p_src_unit_`），判据是「写错时的症状指向哪里」——乙把一个**已经在状态哈希里**的字节做成 tagged union，编码写错报 `Diverged`（读作「确定性坏了」）而真因是语义，且乙同样要重录、只省一个 `std::vector`。**但这条答案三分钟后就被推翻，而推翻它的是我自己漏读的一个字段**：`World` 的弹丸组里本来就有 `p_side_`（发射方阵营，放箭那刻定格、随压实搬移、进哈希），只是 `WorldView` 没暴露；而 `launches_projectile()`（Ranged × 非空中）**每侧恰好命中一个兵种**（守 `Archer` / 攻 `Shade`），所以「单位射的」那一档按阵营一分就够——**不动布局、不动 `state_hash`、不动 `kWorldHashTag`、旧回放不受影响**，只加一个只读访问器。教训很具体：我读到 `p_src_bld_`（注释写着「渲染挑精灵用」）就停了，没把那组字段读完，于是把一个「暴露已有状态」的问题答成了「加字段」；`SelectForce` 那条**更正了他的两个前提**（它有解算——写 `selected_force_` 且进哈希，准确说法是「没有任何机制读它」；而「两条并存、回放里出现哪一种取决于是谁录的」这个担心**目前不成立**——`MoveForce` 在 `force == kNoForce` 时直接 `ContractError`，所以「先选中、再下一条不带 force 的令」提交不进去），因此结论不是「宣布甲、标废弃」，而是甲定为命令面唯一表达 + 成员不删（删中间成员会让后面全部前移）+ 废弃与否留给动守方 RL 的人拍；但那段基于错误前提的注释现在就改，并补一条测试钉住「不带 `force` 的 `MoveForce` 必须抛」（现在没有）。第 13 条命令**两票不加、维持 12 种**，采用他那条更强的理由（编队是**常备建制**而非选择组，加它是把动作面从「编队 × 槽位」换成「任意单位子集」），我原来那条「征兵时定编队够用」撑不住——编队会随阵亡畸形。**同日十二续：接住队友的接线（#89）**：他在我合掉 #86 之后三分钟把接线推上同一分支，因此另开 PR 收。我补两件：一是**他跑不成的那一轮**（他在 Git Bash 下过不了转义与中文路径的 8.3 短名，所以这批 C++ 此前只有 GCC 一侧验过）——MSVC / `RENDER=ON` 53/53 零警告，并**真的看了一次画面**（`--battle --ticks 520`：法师身前飞的是青色球、守方射出的仍是箭，接线在画面上成立而不只在类型上成立）；二是修掉一处**同一 PR 内两处相反的注释**：`world_view.hpp` 说那条前提由 `battle_scene.cpp` 里的 `static_assert` 钉住，而 `battle_scene.cpp` 明写着不能用 `static_assert`（虚函数 + `behavior_of()` 非 constexpr）——**指错的那一处更危险**：照它去找会找到一个不存在的保证，然后合理地推断「那条保证没了」。**同日十三续：按用户试玩报告修四件，其中两件是「测试全绿但玩起来是坏的」那一类**。（一）**经济与补员的链路本来是断的**：交互层只让造墙 / 门 / 木栅 / 箭楼 / 弩楼五种，而三种资源的**流量只来自采集建筑**，于是石木永远停在开局那个数、金币每 5 秒 +2 而一名弓手 60 金——用户的原话是「只能新建造建筑，不能招兵，兵被打死了就没了」。加三个「点一格生效」的模式（B 建造扩到十种含采集三座 / T 征兵 / R 维修），三条 hint 各自查解算真会查的结构性规则（**资源点归属两个方向都拦**——绿框点了没反应比红框更难懂），并补一条**端到端**测试钉住整条链路（建金矿 → 收入涨 → 征兵 → 新兵进指定编队）。（二）**血条在整张图入画时只有两像素高**（56×8 世界像素 × zoom 0.25）：改成按屏幕尺寸画（同 `SceneOverlay` 那条描边宽度的做法），加实心空槽底。（三）**「城门缩在城内」与「桥没在水上」是同一处**：线性结构的走向表。重测后改了两点——判据从「接不接得上」加上「贴得正不正」（墙板 180° 对称，`SE` 与 `NW` 拼起来都无缝，而这套素材的 `NE`/`NW` 内容偏离锚点、`Wall` 还被画布切掉，于是门与墙错开约 0.9 格），且**墙与桥的表是反的**（两个模型长轴不同轴，原来共用一份）；桥的走向另改为看**相邻水格**（过河方向）——一格宽的河上并排两格桥时，按相邻桥格推会把走向答成「沿着河」。（四）**「骷髅兵横着走」是两个朝向错叠在一起**：`DIR_YAW` 的 `SW`/`NE` 写反（从相机姿态推导 + 撞锤头目视印证），外加 10 个 Quaternius 模型的 `yaw` 都是 0 而它们基础朝向是世界 `−y`。**两处都不用 Blender 就能修**：四个朝向的图都在，错的只是标签——改 `DIR_YAW`、给那 10 个实体写上 `yaw: 90`、把磁盘文件名按同一套换过来，于是重渲一遍会得到同样的名字（只改文件名不改那两处，下一次重渲会把问题原样带回来）。**留给精灵流水线那侧的一件**：`NE`/`NW` 部分实体内容偏离锚点且被画布切掉（`Wall`/`Bridge`/`Tower`/`Quarry`/`Lumber` 实测），那要重渲才能修，无 Blender 做不了。**同日十四续：同步队友进度，复核并代合 #93（观测打包 + `BatchedEnv`，两件在一个 PR 里）**：那批此前只有 GCC 一侧验过（他 MSVC 那轮在 Git Bash 下跑不成），所以我这一轮是 MSVC / `RENDER=ON` **55/55 零警告** + 确定性守卫退出码 0。复核补三处，都不改机制。（一）**`FlowDi` / `FlowDj` 是这批唯一没有任何调用方喂过的一段代码**：`BatchedEnv::observe` 与全部用例都传 `nullptr`，于是那两条通道在训练真正会走的那条路上恒为 0，而写错了不会有任何测试红。它与 `AerialAliveFrac` 那条恒 0 **不同类**——后者是世界里还没有空军（接线是对的，放一只不死鸟进去就有值），前者是**接线还不存在**，所以后者写注释就够、前者要一条用例：补的 `[obspack]` 绕过 `BatchedEnv` 直接喂一张真 field，逐格断言两条通道等于 `move_delta(step_of(...))` 并要求斜向真的出现过（**斜向两个分量都是 ±1 不是 0.707**，把那句「刻意不做单位化」钉住），破坏性验证两轮各红在该红处（两条通道对调 / 斜向单位化成 ±0.70710678）。写它时先踩了那份文件自己已经记过两次的同一个陷阱的**第三例**：占位表没设 `speed`（默认 0）⇒ 每格代价 `1/0 = +inf` ⇒ field 整片不可达 ⇒ `step_of` 处处 `Stop` ⇒ **那圈等式全部成立、用例全绿**（前两例是 `range` 为 0 永不出手、漏 `submit_actions`，症状都是同一个 0），因此加了一条 `reachable` 前置断言。（二）**一条用例的名字没兑现它自己**：「工作线程里的异常要捎回调用线程」——`observe` 的长度检查在 `for_each_env` 之前、在调用线程上就抛了，那两行一个工作线程都没进过；而那层 `try/catch` 确实需要，只是**目前通过公开 API 到不了**（`pack_unit_obs` 的三种抛出条件在 `observe` 里都不可能成立、`step` 的切片被补齐到恰好等于活单位数），即防御性代码。按原名字留着比改掉更坏：读到的人会以为这条路有常驻覆盖，于是哪天把捎回删了、看见全绿就以为没事——而症状要到从 Python 调时才出现，且是进程直接死。改成如实的名字并写明它哪天能变成真覆盖（出现「逐局才知道合不合法」的输入时）。（三）恒 0 那件事记进 `batched_env.hpp` 的 `observe`，连带写明**为什么不顺手接上** field（按 `(mover, tier)` 缓存与失效是 `flow.hpp` 划给调用方的，一批里每局墙血各自在变，重算节律该等 `train/` 定形）。**并答掉他挂给我的三件**：两个归一化分母不进 `StatsTable` —— 同意，且理由不是「省一次重录」而是 **`cell_capacity` 在仿真里根本没有对应规则**（单位互不阻挡，一格能站多少人无界），它是观测尺度、不是待标定的平衡数值，本来就不属于那张表；PyTorch 装（各自 venv 无共享风险），但**四个人各一份就是 10 GB**，先看 `df -h ~` 与 `nvidia-smi` 给的 CUDA 上界；**`python3-dev` 不必找管理员**——缺的只是「一个自带开发头的 Python」，而那不必是系统 Python：`uv python install`（python-build-standalone 的 `install_only` 包带 `include/pythonX.Y/`）或 micromamba 都装在用户目录、不需要 root，而 CMake 的 `FindPython` 是问解释器要 include 路径的，所以「`bindings/` 在服务器上暂时编不了」这条可以不接受（已知坑：头文件在托管安装目录、**不在 venv 里**，别照 venv 判断）。**顺带给那条组内待定（碰 `rts_core`/`game`/`tests`/CMake 要不要强制一轮 GCC）补一个实测事实**：#91 那两类 MSVC **都抓不住**——`/w14010`（注释里的行尾续行）对那个形状不报（实测 `-W4 -w14010 -WX` 退出码 0），而 `uint64_t` 与 `...ull` 混算在 LLP64 上**根本不存在**，没有类型分歧可报。也就是说这个洞靠本地纪律与编译器开关都补不上，于是题面是「每次合并前有一轮 GCC」还是「接受短暂坏窗口」——我的意见是两条都不选，改成让机器跑那一轮（GitHub Actions 一个 `ubuntu-latest` 的 `RENDER=OFF` 构建 + ctest，不占任何人时间、不阻塞谁），已问 @Cody-ic 与组内 | #72 / #73 / #74（已合）；#86（复核并代合，两处小修推在其分支上）；#89（接住队友接线，补 MSVC 轮）；#94（按试玩报告修四件）；#93（复核并代合，补 MSVC 轮与三处修，逐条理由在 #93 评论）；第二批至第六批与可玩层四件见 #57 |
| omg321-975 | | |

**demo 里的一切战术都是机制涌现的，脚本一行战术都没写**——脚本只有「守方有敌就打、
攻方向堡垒推进」两句。攻方自己找到桥（贪心方向被水挡出来的绕行）、自己开始啃墙
（movement 撞上可破坏阻挡物自动承诺破坏）、Ram 的 AOE 把门连同相邻墙段一起砸开、
Flak 击落 Phoenix 而 Tower 从不对空。三张 `--ticks 0/400/1200` 的截图对照：
过河 → 压到墙线 → 破口后双方拼到全灭。**这正是要给全组看的东西：机制层能撑起
一场完整攻防了。**

**过程里修了一个自己当场写出来的 bug**：Bresenham 视线的 y 分支 `err += dy` 应为
`+= dx`——纯垂直的视线第二步开始横移，水平与对角全对。抓到它的是「隔两格正上方的
墙进不了记忆图」这条用例，回归断言已留。

**本次决定**：数值表指纹由 `World` 自己算（调用方给就是第二个真相来源）；
`StatsMismatch` 当场拦（契约同 `MapMismatch`）；表的形状留 §1.4 的余地
（血量与伤害两个缩放系数都存）；掩码攻击 4 位从「默认允许」升为精确
（那不是推翻默认允许，是它管的范围缩小了）；AOE 圈内不分敌我（溅射误伤是
CLAUDE.md 明写的机制性克制）；`bld_at_` / `obstacle_at_` 占位格是派生缓存、
刻意不进哈希。

**遗留问题**：~~三个 PR 叠放要按序合~~（同日已按序合入）；`land_attack` 单体落地
不追射程（无弹丸实体时期的近似，弹丸做成第四组实体时一并改，届时 `kWorldHashTag`
再进一格）；~~`Clear` 等七种命令仍在记账~~（第二批解算掉六种，只剩 `Garrison`，
与驻守解算 + 高度优势成对做）；征兵还选不了等级（`Command` 缺等级字段，
归升级轴那一批，加字段走追加纪律）；工地挨打不延长工期、多工匠不加速、
放弃维修不退款——三条都是占位机制决定，注释就写在解算处，标定时再议；
服务器侧 GCC 构建**还没跑这五个 PR**（`game/` 新增五个 `.cpp` 都在 GCC 编译面内，
**要尽快 sync 上去跑一轮**）；ArLiangz 的日志行仍空着（8-29 起三次）。**不代填**。

---

## 2026-08-30（第 4 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | **定案一处 `CLAUDE.md` 内部矛盾，并补上它删掉的那条机制所需的实现要求**。花名册写 `Ram`「贴身」、结构破坏规则写它「装填慢、弹道有飞行时间」，两句互斥；定案取撞锤。**要紧的不是订正而是订正的后果**：「对能散开的单位效率很差」原先靠飞行时间解释，删掉之后必须换一个支点，否则那条机制性克制静默作废 —— 于是补进一条 **「AOE 落点在前摇开始那一刻锁定成一个坐标」**，并写明**它错了不会让任何测试变红**（机制正确性，不是确定性；回放照样逐 tick 一致）。同一处顺带定了 #42 那条接口缺失的形状：远程要目标句柄、`Ram` 要锁定落点，两者不是一种东西。**做出「投掷物贴图」并把撤退理由重新审了一遍**：§8 当初撤掉 `projectiles` 分组时记的两条出路，第一条的代价已随「元数据只剩单一读取点」下降，且 2D 旋转**不依赖弹道形状**（多档俯仰恰恰违反「承诺不得依赖待定数值」）。清单缩过两次，**第二次是自己推翻自己**：曾按阵营分「守方箭 / 攻方影矢」，但阵营不是机制信息（两者机制相同、飞行方向已区分它们），真正该由剪影承担的是轻箭 vs 重狙击弹。 | #68、#69（`9a9ed35` / `dfd9404`） |
| ArLiangz | | |
| zhxxx233 | **接手 1c（`rts_core` 的机制本体），并先把前置做完而不是直接写机制**：结算 #70（队列归零）；在 #57 公开接手 —— 四块（战斗 / 寻路 / 视野 / 经济）全做、**不预先切一半留着**；闭合两条挂在他人名下的悬项；查出一处**排在机制之前**的格式缺口。 | #70 合入（`e99d160`）、本 PR、#57 接手声明 |
| omg321-975 | | |

**「不预留切分」是一个判断，不是省事**：ArLiangz 自 8-28 认领 `rts_core` 至今
`rts_core/src/` 仍是 5 个契约层 `.cpp`、机制零行，#57 上两条问题（`Ram` 的 AOE 锁定、
「先落哪一块」）都无回音。预留切分要付的成本是真的——接口要提前对齐、`world.hpp`
的方法签名要一次性铺满、每个 PR 都要考虑另一半会怎么写。**为一个短期不会发生的协作
预付这笔成本，代价会落在关键路径上**（1c 挡着未认领表的第 6 → 8 → 7 → 9 → 10 项全部
五项）。所以改为**进度可见**：进度滚动更新在 #57，他任何时候回来看一眼就知道做到哪、
不会重复劳动，要接进来直接说一句、不需要等谁先划范围。

**闭合两条悬项，其中一条的理由是「依赖方向」而不是「哪个更好」。**
`kDiagonalNeedsBothOrthogonal` 从「暂取、仍需 ArLiangz 确认」改为已定（仍是 `true`，
改动量与两天前一样，变的只是**默认值有主**）。另一条是 `AtkNear` 选中的目标不在射程内
怎么办：**取「打不到就空转」+ 掩码精确**，而不是「选中即走过去」。取舍的决定性理由是
后者会让 `Atk*` 隐含寻路，于是**把一块本来可以立刻开工的东西（目标选择）绑到一块卡住
的东西（flow field）上**——而 §1.1.1 刚论证的正是这两块数值依赖不同。**一个决定若把
「不卡」拖成「卡」，它就自己否掉了。** 附带理由是保住动作正交：`Atk*` 自带接近的话，
「先走近再打」这个可演示的战术就被烤进动作里、看不见了。

**查出一处该排在机制之前的格式缺口：数值表是外生输入，而没有任何字段核对它。**
全仓库有七处注释把它称作「那份**还不存在的** JSON 数值表」，每处都在解释「所以这个
数值由调用方给」，但**没有一处规定它怎么进来**。而它一进来就有一个问题：改一个数字
⇒ 回放报「第 N tick 状态不一致」，那句话的意思是**跑歪了**，于是有人会去查一个不存在
的确定性缺陷。现有三个诊断字段一个都抓不到——`platform_fp` 管浮点、`file_hash` 管
文件完整性，而 **`hash_tag` 也抓不到**：状态布局没变、喂入清单没变，只是数值不同。

**解法照抄 `map_content_hash`，而照抄要抄全——它有两个落点，各担一职。**
查代码时才看清这一点：它既进 `state_hash()`（`world.cpp:582`，于是「地图变了」在
**第 0 tick** 就分歧）**又**进回放头（`replay.cpp:213`，于是诊断能说出是地图变了）。
只做前者会让诊断退化，且退化得不显眼：**第 0 tick 哈希不一致有三个成因**
（地图变了 / 数值表变了 / 初始单位不同），少一个字段，三者就并回同一句话。
一处不对称是有理由的：`map_content_hash` **必须**由调用方给（`rts_core` 不读地图文件、
算不出），而数值表指纹**应由 `World` 自己从表算**——表就在 `WorldInit` 里、就在手上，
让调用方另给一个等于给同一件事两个真相来源。

**它排在机制之前，理由是成本随时间涨**：改 `state_hash()` 的喂入清单 ⇒ 必须动
`kWorldHashTag`（`World/2` → `World/3`）⇒ 已录回放全部作废。现在已录的只有 `tests/`
里那几份、都是现生成的；而往后每实现一条机制就会多一批夹具回放（#57 那条「每实现一条
机制补一份夹具」）。**越晚改越贵，所以它不是机制的一部分，是机制的前置。**

**本次决定**：

- **1c 由 @zhxxx233 全权推进，四块全做，不预先切一半。** 进度滚动更新在 #57；
  ArLiangz 要接进来直接说一句，从当时的进度里挑一块——**不需要等谁先划范围**。
  「不预留」的理由是**不为一个短期不会发生的协作预付接口成本**，而代价会落在关键路径上
- **`kDiagonalNeedsBothOrthogonal` 取 `true`，从「仍需确认」闭合为已定。**
  改动量不变（一个常量 + `tools/map_gen/` 校验器对齐），变的是默认值有主
- **`AtkNear` 打不到就空转，攻击 4 位掩码精确到「射程内有没有合法目标」，`Atk*` 不隐含
  移动。** 这不推翻「掩码里未定的位默认允许」——那条管的是**还判不出来**的位，这四位
  现在判得出来了，所以是它管的范围缩小，不是方向反转
- **数值表的形状只定三条**：进 `WorldInit`（不逐单位进 `spawn_unit`——射程是兵种级的，
  逐单位存是冗余 + 第二个真相来源；也不留在 1c 内部私有——`advance()` 要用它）、
  `game/` 读 JSON（`rts_core` 不读文件，沿用 `MapData` 那条纪律）、按四组分
  （兵种 / 建筑 / 障碍 / 全局）。**字段清单刻意不定**，它随机制展开，现在定就是猜
- **但要现在留出一条余地**：`§1.4`（等级提升哪几个数值）还没定，所以表里存的应是
  「基础值 + 缩放参数」，**不要把缩放规则烤进表的形状**。这是 §4.1 那条规则
  （承诺不得依赖任何待定数值）的又一次应用——若表的形状假定了「血量和伤害都随等级涨」，
  §1.4 定成「只涨一个」时要改的就不止一个数字
- **代码注释不记人事。** `world.hpp:8` 写的是「tracker 是 #57」而不是谁在做，
  所以这次接手**没有改它**——归属会变，而注释跟着人事变动漂移是新造一处漂移源；
  想知道谁在做就去 #57

**遗留问题**：

- **数值表的前置 PR 还没做**（本 PR 只是把决定与理由落进文档）。它含三件：
  `WorldInit` 加表、`World` 从表算指纹并喂进 `state_hash`、回放头加字段 +
  `format_version` 与 `kWorldHashTag` 各进一格。**做完它才开机制**
- **`AtkNear` 那条决定尚无代码，因此也尚无测试。** 它现在只活在文档里，
  而「打不到就空转」与「掩码精确」是两个可以各自写歪的东西——落地时要有一条用例
  钉住「射程外时该位为 0」，否则默认允许那条会把它悄悄盖回去
- ArLiangz 的日志行仍是空的（8-29、8-30 两次）。**不代填**，那是他自己那一行

---

## 2026-08-29（第 3 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | **瞭望塔视觉重做（#45）**：起因是评审指出它该有木质感——查下去发现根因不是「染得不够」而是**染错了东西**：它与箭楼/弩楼/兵营共用同一套砌体构件（`tower-square-base/mid/top`），只靠 3 处 `tint` 染棕，渲出来是**一根刷了棕漆的石柱**。染色只改色相，改不掉「实心砌体」这个形体。这是「共用主体 + 配件区分」在本仓库**第三次**失败，前两次是三座资源建筑共用塔基、采石场撞岩壁，而 `sprite_gen/README.md` 早写着「区分必须做在主体形体上」——这次只是把「配件」换成了「染色」。改法：主体换成未被引用的 `tower-square-mid-open-simple`（四柱斜撑镂空架）叠三节 + 方形开放平台，**剪影透光、看得见背景**，与四座实心塔不同族。**新条目一处 `tint` 都没有**——木色是构件本色，而四种染法（`COLOR`+压暗 / `MIX` / `MIX`+压暗 / 只压暗）实测**全都比不染更差**：把浅色砌体压暗只会从「石板」变成「泥板」。由此提炼一条进 README 的规律：**材质感属于形体与本色，染色只该用来分阵营**；发现自己在用 `tint` 表达「这是什么材料造的」，就说明选错了构件。另两处也是实测才知道：三节（2.94）而非两节（2.06）——后者读作脚手架不是塔，而高度是「大视野」唯一的视觉表达；顶层用方形而非六角——`tower-hexagon-top-wood` 名字带 `wood` 且确为木色，但 0.86 宽配 0.93 的方架会向外挑出、垛口角与立柱错开，读作「架子上扣了顶帽子」。**落地 #15 / #16 的讨论结论（#46）**：两份提案原样合入以保留提案人的作者身份，结论由我另开 PR 落地（照 #21 的先例）。#15 改了原稿一处**结构性主张**——原稿主张守方镜像攻方分两层、战术层与攻方同构，现改为**守方 RL 只在决策层、单兵是参数化脚本**，三条理由：守方 AI 是人类玩家的替身故动作空间应等于玩家能下的指令；它从结构上消除了原稿自列的「三个坑里最要命的一个」（攻方过拟合到超人类微操）；训练成本不再翻倍。**这与议程 #26 议题 A6 的建议方向相反**——A6 建议「守方先做战术层 RL」，而议程默认了原稿的分层结构、没问过「守方 RL 该放哪一层」。新增 2.5 节专门讲清**脚本层不是 RL 的廉价替代品**，因为若把它理解成临时凑合，第 2 节的取舍就完全讲不通：先否掉一个误解——**微操对 RL 一点也不难**（SMAC 整套 benchmark 做的就是微操，风筝是最经典的涌现行为，而我们本就指望攻方涌现集火与包夹），所以理由从来不是「学不会」；真正的理由是**攻方要打的是人，我们需要覆盖真人水平区间的一个分布，而 RL 只会收敛到一个点**——脚本的微操强度是可调参数，RL 的不是，多个脚本轮换即 domain randomization，**这是脚本能做而 RL 做不到的事**。并给出「脚本要写到什么程度」的判据（A6 里一直没人问的那个问题）：**克制二部图里已写死的行为进脚本**——`Ghoul ──► Archer 拉扯` 里「拉扯」这个词就在克制表里，它是设计意图不是涌现目标，若脚本不会拉扯这条克制关系在训练中根本不存在。#16 侧：**演示只做双视图 + `AI vs AI`**，判据是本文第 0 节自己那条「不需要解释术语就能看懂」——**原稿给了判据却没有拿判据筛自己的清单**；但「不上台」不等于「不做」，显著性热力图与非传递性环矩阵**改用途**（后者是 `CLAUDE.md`「评估指标」的第一条，按字面砍掉会误删一条已定的核心论证），且「只用于分析」把显著性图从前端需求降为 `tools/` 离线脚本、**成本降一个量级**。**首次修掉 `main` 上一个真 bug（#63）**：应队友在 #59 的请求跑服务器构建，发现 **`main` 在 Linux/GCC 下编不过**——9 处 `-Werror=dangling-reference`，全在 `game/src/map_loader.cpp`。是 GCC 13 的**误报**（`need()` 返回的引用来自第一个参数 `obj`，不可能指向第四个参数 `where`；GCC 不做别名分析，只看「返回引用 + 参数含临时量」就报），但 `-Werror` 下误报也是硬失败，而 **MSVC 没有这条警告、本地一条都看不到**。这是 CLAUDE.md「GCC 侧的编译问题只有推到服务器才会暴露」**第二次兑现**（第一次是 #39 那四处）。修法是把 `where` 取 `std::string_view`，**一处签名、23 个调用点一行不改**。过程本身还暴露了我自己写的脚本的一个缺口：`tools/server/build.sh` 只认 Catch2，而 `nlohmann_json` 是 #40 才引入的，于是构建卡在 configure、报 `Build step for nlohmann_json_single failed`——**看起来像依赖坏了，实际是脚本没跟上**。改成一张 `名字:目录` 的依赖表，并把「每新增一个 FetchContent 依赖，这里要跟着加一条、`~/shared/deps/` 里也要放一份」写在表旁边。顺带补了共用缓存里的 `json.hpp`（SHA256 与 `ThirdParty.cmake` 声明一致），此后两次服务器构建都没等过一次网络超时。另实测到一次 `curl` 直接把 SSH 会话 reset 掉、连带把前台构建带走，所以用法注释里加了 screen 的跑法。**本次最值钱的是一次失败的重构（#64 提案 → #65 实现 → 撤回）**，四条教训都不是关于 C++ 的：①**收益可以被一个「看起来纯属清理」的手法砸掉。** 为让「类对象设计」这条交付要求有一张继承图，我把单位改成 `Unit` 基类 + 派生类 + `vector<unique_ptr<Unit>>`，并用 `TypedUnit<>` 模板别名收掉四个 `override` 的样板——动机（别让真正的差异淹没在样板里）单独看完全正当，但它**连派生类的名字都收掉了**：类图画出来是 `Unit → TypedUnit<Archer, Ranged, Single, LightFast>` 而不是 `Unit → Archer`，**而「老师一眼就懂那张继承图」是我为这个 PR 陈述的唯一收益**。这一条是队友指出来的，我完全没看到——它比「代价太高」更值得记：**局部合理、全局失效**。②**两次尝试都薄，根因是时机不是方向。** 先做的是「按兵种」那版（`UnitBehavior`，11 个静态单例），被评为「只是套了层皮」而这个批评成立——11 个类约 200 行换 **1 个 override**，三轴各返回一个常量、三个委托方法纯转发、`can_engage()` 十一个派生类零 override；我据此推出「那就把继承落到单位上」，但**真正会厚的那些**（冲锋助跑加成、溅射对散开阵型的衰减、攻击前摇与转身、对建筑特攻）**全在 1c、一行都还没写**，所以换落点也只能拿到 1 个 override。正确的推论是**等 1c 落地再做**，而我推出的是「现在就去碰 1c」——差别在于前者只要排队、后者要抢别人的分片。③**把自我暴露的失败换成了静默失败。** 我把 `WorldView` 的九个 `std::span` 改成逐单位访问器后，空槽从「读到被清成规范值的数据」（症状是偶尔画出一个血量 0 的幽灵单位，**看得见、无害**）变成解空指针（UB）。我在注释里警告了「先过滤」，但**把它写成调用方的责任是不够的**——方向正好与本仓库一直在防的相反。④**`kWorldHashTag` 撞号是一次真实的协作失误**：我的 PR 与队友的 #62 都把它推到 `World/2`，两边逐字相同所以 git 判无冲突，于是**两个不同口径共用一个标签，正好废掉这个标签存在的唯一理由**（把「口径变了、重录」与「第 N tick 跑歪了」分开），而冲突恰落在标签上一行——最坏的位置。提炼出的规矩：**改 `kWorldHashTag` 的 PR 要在标题里写明它占了哪个号**，因为标题在 PR 列表里躲不开、不依赖任何人记得看 diff。另外还犯了一处**时态错误**并已订正四处：我把「观测打包退化为逐单位 gather」写成既成事实，而 `obs.hpp` 目前只有通道注册表、**打包实现一行都还没写**——按「退化」这个词去找那段变慢的代码会找不到，而找不到之后最可能的结论是「夸大了代价」或「已经改好了」，两个都不对。撤回后的结论：**扁平数组保留，多态就落在兵种上**，那层 `UnitBehavior` 因为「薄且便宜」（不动任何已合并的代码、不碰确定性护栏、不改回放口径）留在 `main` 上，并由它关掉了挂两周的「需向老师确认类对象设计」——队友给的答法比我原来想的好：老师问「这个继承体系解决了什么问题」，答**「让每条结构性例外与它的理由待在同一处，而不是散在一张 N×N 表里」**，那是 `CLAUDE.md` 自己的要求；按这个答法 `UnitBehavior` 本来就够，我不必去改存储层。**最后一条自我批评**：我一开始把「好汇报」这条权重给低了——这是课程设计，评分来自报告与答辩，而我按工程优雅度排序。另：作为 §6 的提案人补投 #58 表决（**通过，且赞成「打掉掉落少量资源」**这条原提案没有的附加机制，理由是只有血量没有产出的话清野对守方是净负收益——拆掉自家防线前的天然减速带，于是城外障碍没人会去清、§6.2 想要的「景物第一次有机制」只兑现一半），并建议把 #62 自己点名的那处钝角（城内非走廊上的障碍清掉是纯赚）写进文档而不只留在 PR 描述里 | #45、#46、#63 合入；**#65 撤回**（提案 #64，分支保留供答辩引用）；#58 表决、#59 完成 |
| ArLiangz | | |
| zhxxx233 | **接手 `game/` + `render/` 并落地玩家侧从零到能开窗**：`game/` 的地图加载与场景装配（#40）、世界渲染（#42：raylib 经 FetchContent 取源码而非预编译包以避开跨 VS 版本的 ABI 风险、精灵图集、等距投影、相机、**截图模式**——存在的理由不是省事，而是让渲染结果不必靠人盯着看才能验，于是回归能进 ctest）、中文文字与格点选（#43）。**按「能不能在默认构建里被测」切模块，而不是按「渲染 / 非渲染」切**：`render/` 默认不配置，挂在它下面的测试在默认构建里根本不存在、ctest 照样全绿，所以那几条易错规则（桥的底衬是水不是平地、叠加物与实体同一深度序列、线性结构的 `d` 是走向）一律放在 `game/`，投影反向也因此从 `render/` 下移。

字体那一段是先写一次性 spike 探出来的，**三种失败形态各配一道检查**（#43）：码点要了而字体没有该字形 → 零宽的什么都没有，连豆腐块都不给；字体有该字形而码点没要 → 渲成 `?`（集合含 ASCII 时）或**图集里的第一个字形**（不含时，实测「镇野箭楼」渲成「平平平平」，四个看着完全合理的汉字）；给一个 `.ttc` 字体集合 → raylib **静默退回内置字体**且 `texture.id != 0`，所以「载入成功」这条判断为真、中文全变 `?`（靠「字形数必须正好等于要的个数」检出）。码点集合因此**机械地从「我们到底要显示哪些串」推导**，不手抄 Unicode 区段；输入含**地图文件里的 `name`**——它是来自数据的任意中文，不在任何清单上，最容易漏。顺带把「空白码点可以没有墨、但不能没有宽度」这条豁免收紧：实测全角空格 U+3000 在 simhei 下宽度为 0，用它当分隔符各项会糊成一片而三道检查全绿。

**查出一处项目级的编译器警告不对称**：我曾断言「switch 漏枚举值在 MSVC 上编不过」，破坏性验证（删掉 `case Terrain::Bridge`）证明**它编得过、零警告**——C4062 在 MSVC 上是 off-by-default，而 GCC 的 `-Wswitch` 含在 `-Wall` 里。加 `/w14062` 补齐（与此前记录的 C4365 同族）。这条不是风格问题：`display_names.cpp` 与 `roster.hpp` 的完备性保证**全靠「switch 没有 default」**，没有那个开关，「加了枚举值忘了加名字」在本地一条警告都没有、到服务器上才编译失败。

**复现队友报的 4 条 ctest 全红并订正其根因**（#47）。他归因为 MSYS 虚拟路径；真因是**编码**——MSVC 的窄路径按 ANSI 代码页（cp936）解释，而 CMake 烤进宏里的是 UTF-8，**路径怎么规范化都修不掉**，按 MSYS 那条思路去修会得到「看起来修了但没修」。修完第一条边界又撞上第二条：**Windows 上的 `argv` 也是 cp936**（打出原始字节才看清：「中文路径测试」6 个汉字只占 12 字节，UTF-8 会是 18），于是程序里同时存在两种编码——这也解释了当天一整天的乱码输出，而**报错信息是这类问题唯一的线索**，糊掉它等于线索也没了。两条边界各修在边界上：`rts/utf8_path.hpp` 收口「怎么正确打开一个文件」（放 `rts_core` 是因为**回放将来也要读写文件**，三份拷贝里漏一份就在他机器上原样再坏一次）；入口拆成不含 raylib 的独立 TU 用 `wmain`（`windows.h` 与 `raylib.h` 撞 `Rectangle`/`CloseWindow`/`ShowCursor`/`DrawText`，靠宏压是在维护一份会漂移的清单，拆 TU 之后冲突**不存在**而非被压制）。raylib 那侧必须绕开：它的文件读写全部走窄 `fopen`（核过源码，零处 `_wfopen`），所以改走「自己读字节 + 内存版 API」。回归测试在**构建目录**里造中文目录（不依赖仓库路径），含一条「宏里真的带非 ASCII 字节」的自检——否则它在纯 ASCII 路径上会全绿，变成永远绿的测试。

**接下 `rts_core` 接口契约的第一半**（本 PR）。它此前已由 @ArLiangz 认领（#28）但下一步的契约草案 PR 未发出，而它卡着 `render/` 画实体、`bindings/`、`train/` 三处；接的范围刻意只到**契约与回放**，战斗 / 寻路 / 视野 / 经济仍留给他，并把 README 第 1 项拆成 1a/1b/1c 让两边能并行。落地：花名册进代码（11 单位 + 11 建筑 + 3 障碍）、战术动作 13 个、玩家/宏观命令 11 个、观测通道注册表。**最要紧的一件事是把 PR #23 评论串里辩定的十三条搬进一份合并了的文件**——它们此前只活在评论串与**未合并且已 CONFLICTING** 的 #25 分支上，而「PR 评论会被后续讨论淹没」这句话是我自己在同一条串上对别人说过的。答掉两个球在我这边的问题：血量与伤害取 `int64`（采纳对方论证：我算的 `int32` 溢出点依赖两个**都还没标定**的数，两个未知数相乘算出来的余量不是余量），并把它推广成一条契约规则——**接口与回放格式的承诺不得依赖任何待定数值**（本版已用它两次：波数取 `log2(1+wave)` 而非 `wave / 训练截断波数`、存活空军存比例而非绝对数）；斜向穿角暂取「不能」并给出可引用的常量名，解掉 #33 校验器那条「等接口定稿回来对齐」。

契约层新加的检查里有三条值得记：**观测通道的编译期布局指纹**（版本号靠人记得改、指纹不靠；分工是指纹管布局、版本号管语义），**成对通道必须是前缀且严格 (ally, enemy) 相邻**（Python 侧按 `obs[...,2k]`/`obs[...,2k+1]` 取一对，插一条不成对的进去会让后面所有对错位一位而不报错），以及**方向名与等距投影的一致性拿真的 `IsoProjection` 印证**——破坏性验证对调 `MoveE`/`MoveW` 的格增量后，只有这一条红，「反向 = (k+4)%8」照样绿。顺带写下一条容易搞反的事实：**格坐标的斜向，是屏幕上的正北东南西**，而那才是「穿角」规则的适用对象。

**接着落契约的第二半：世界状态**（1b）。`World`（三组扁平数组 + 代数句柄 + 空闲表）、`submit`/`advance`、`WorldView`、`FogLayer`、地形三张位图、动作与命令掩码、状态哈希。**边界画在「凡是需要一个待标定数值才能推进的都不做」**，于是战斗/寻路/视野/经济仍整块留给 1c。两条与「不做」有关的判决：`advance()` **不是空操作**（一个什么都不做的 `advance()` 会让回放测试变成永远绿的摆设），而六种要造价与耗时的命令**记账而非静默丢弃**，所以「本版没做这一类」是可以被断言的事实。

**写迷雾时查出前一版观测的一处缺陷**：`visible` 原是布尔，于是「从未侦查过」与「侦查过、记忆里那段墙是破的」在张量里是**同一串字节**（两者的 `visible` 与 `wall_hp` 全为 0）——而后者正是核心评估指标「AI 是否发现并利用已有缺口」要的东西，也正是对标产品 DiNaO 被批评的那一点。改成三档（0 / 0.5 / 1）。**它顺带成了「为什么指纹不够、还要一个版本号」的现成例子**：通道名与归一化方式一个字没动，所以布局指纹**完全不变**，只有语义变了，`kObsVersion` 因此 1 → 2；只靠指纹的话旧 checkpoint 会被判为兼容，然后那份权重里的「0 = 看不见」在新语义下是「从未见过」，不报错、不崩、只是策略是错的。同一条 §4.1 规则（承诺不得依赖待定数值）又用了两次：迷雾记忆的血量存**千分比**而非绝对值（绝对值要连 `max_hp` 一起存，而那在还不存在的数值表里）、「记忆新鲜度」**不做**连续衰减（分母是待定的预期波长）。

**地形枚举从 `game/` 搬进 `rts_core`**：依赖方向本来是反的（`地图与场景设计.md` 第一句就是「`rts_core` 要吃它」、4.2 明写展开成三张位图在这一侧，而 `game` 链接 `rts_core`、反过来不行）。用别名保住 `game::Terrain`，五十余处调用点一个字没改。同时写了 `game::make_world_init()` 作为 `MapData` → `WorldInit` 的唯一接缝——它的第二个用途是**证明契约装得下真实消费者**：接口目前全部由我一个人写、也全部由我一个人消费，独立校验缺失，而一个能跑通的适配层是这个问题唯一能自己动手补的部分。

这一批 10 条破坏性验证全部变红。其中两条第一版是**靠编译失败**红的（`if (false && …)` 撞 /WX 的恒假条件、删掉 `blocked` 撞未使用局部变量），那证明不了**测试**能抓到它，换成语义改变但编得过的探针重验才算数。另有一条测试写错了并被自己抓出来：「非方形网格钉住行主序」原本把探针格取在 (2,1)，而 3×2 网格上 `y*w+x` 与转置的 `x*h+y` 在那一格**都算出 5**——那条用例会对一个转置的实现放绿灯，改取 (1,0) 才有效。

**最后落契约的第三块：确定性回放**（1b-2）。格式（定长记录 + 全文校验和 + 平台指纹 + 哈希口径标签）、`ReplayRecorder`、`replay_verify`。这一条唯一真正需要论证的判决是**回放里存什么**：只存**外生输入**（`submit` / `submit_actions`），机制产物（`spawn_unit` / `kill_unit` / `place_bld`）一律不存。理由不是省字节，而是**存了会让回放变成假绿灯**——若把 `kill_unit` 也记进流里，重放时就照着流把那个单位杀掉，于是即便战斗解算算出完全不同的结果，回放照样对得上，测的就变成了它自己的日志。这条界线同时逼出一个缺口：不存机制产物 ⇒ 初始状态只能由 `WorldInit` 表达，而它当时描述不了「有单位的局面」，于是 `submit_actions`（两条输入通道里字节量占九成的一条）**在任何回放里都走不到**——补了 `WorldInit::units`。

**回放头里三个字段各消掉一种误判**，因为对不上时最贵的不是修 bug，是分辨这到底是不是 bug：平台指纹（本项目**天生跨两套工具链**，而 `CLAUDE.md` 明写回放在两者之间不可复现，没有这个字段，跨平台比对会表现为「第 N tick 状态不一致」）、哈希口径标签（`kWorldHashTag`，把「状态布局改了」与「仿真跑歪了」分成两条诊断）、全文校验和（「文件坏了」不报成「第 N tick 不一致」）。三者的**处理方式刻意不同**：前者只管浮点可复现性，而本版仿真一次浮点运算都没有、跨平台照样对得上，所以它只在真的对不上时才升格成成因；后两者不同则意味着**比对本身没有意义**（两边对「状态是什么」的定义都不一样），所以当场拦，哈希碰巧相等也不构成证据。

**哈希口径标签顺带答掉了在途弹丸那个悬而未决的问题**，而且比我原先给 @ArLiangz 的兜底更好：原打算「在回放格式里给弹丸预留一组空的实体位」，现在不需要了——他若把弹丸做成第四组实体，改这个标签即可，失效会以「口径变了、重录」的形式报出来。预留空位反而是一个必须被后人维护、且永远不知道能不能删的坑。

**写回放时又查出 `state_hash` 漏了一项**：待排空的命令队列。它跨 `submit` / `advance` 边界存活，所以是仿真状态。这不是一个「会漏掉 bug」的洞（分叉在下一个哈希点仍会被发现），而是一个**会指错位置**的洞——报出来的 tick 比真正出问题的地方晚，而回放测试唯一的定位能力恰好是「它指的那个 tick 就是第一个错的 tick」，所以这个洞正好打在它的价值上。

这一批 15 条破坏性验证，14 条直接变红，**第 15 条变绿并因此查出我自己一处「通过的理由是错的」测试**：那条 `catch (ContractError)` 换成一个接不住它的类型，全套测试照样全绿——因为原本盯它的用例是「初始单位少一个」，而单位数进哈希，**tick 0 的哈希就先对不上**，比对在到达那批动作之前已经返回。改成手工造一份自相矛盾的回放（哈希点是对的、那批动作的条数不对）才真正走到那条路径，重验变红。这一条比其余十四条都有价值：它证明了「测试跑绿」与「测试盯住了那段代码」是两件事。

清 PR 队列 11 → 4（合入 #39 / #41 / #44 / #45 / #46 / #47 等，两次处理 squash 合并打断堆叠 PR 的问题：main 拿到新的 squash 提交而子分支仍带原提交，需 `rebase --onto` 加手动改 base——GitHub 只在基分支被删时才自动改指向）。评审 #45（交叉核对元数据 diff 恰好一处、`--verify-assets` 在其分支上 476/476、前端无需改动）与 #46（同意守方 RL 只在决策层，并指出它反而简化前端：玩家指令与策略命令成了同一套，观战视图与正常游玩共用一条渲染路径；也指出 #16 第 4 项被「改用途」而非砍掉，暴露我原清单把 `tools/` 预算与前端预算混在一张优先级表里的结构缺陷）。

**收尾时又清算了一轮 issue 与 PR（7 条开放 PR → 5 条，且含更多实际内容），最要紧的一条是收拾自己的判断失误**：把契约切成三条堆叠 PR，本意是缩小每条的评审面，但三条都没有评审进来，成本因此反过来——任何一条 squash 合并都要我手动 `rebase --onto` 两次加改两次 base，而每次 rebase 都让评审意见挂在旧行号上；**更要紧的是单独评审第一条不解锁任何东西**，`main` 上的 `rts_core` 一直只有四个头，**契约躺在三条没人看的 PR 里等于契约没落地**，而那是全项目唯一的关键路径。改成一条对 `main` 的 PR（三个提交原样保留，人员分工那页的证据不变）：一次评审 = 整个契约上 main = 1c 可以开工。**教训是「减小评审面」与「减小评审总量」不是一回事**，前者在没人评审时会放大后者。

同一轮里关掉 #25（710 行、CONFLICTING、开了 27 小时）：它的主体是一份 `rts_core 交接说明.md`，而**一份交接文档的保质期按小时算**——接手人已经开工一天，这份写给他的「第一天动作顺序」现在只会让人读到一份过期的仓库状态。逐文件交代了内容去哪（十三条 → 契约文档 §3、血量宽度 → §4.1、破坏速率甲乙 → §5.1、十条踩过的坑 → 已分别落在 `CLAUDE.md` 与 `训练服务器环境.md`），并从中**捞出唯一没被取代的 58 行**单独开 PR（#51）——两处都是「已合并的代码指向一份说着另一件事的规范」：`game/` 与 `tests/` 里三处注释引用 §4.2.1.1 而那一节不存在；6.3 的 `content_hash` 规范化不够，而 #31 已按更强的规范序列化实现了并写明「未定之前以本模块为准」，#31 合入了、6.3 没跟着改。**这个 PR 的错不在内容而在载体**——「把结论从评论串里搬出来」是对的，但我搬进了一种天生会过期的文档。

帮队友推进的三件：给 #33 划出**最小可合并集**（六条评审意见里只有一条真的挡着合并，其余两条是一行的顺手、三条不必他做），因为六条读起来像一堵墙而实际不是；答掉 #29 三条空缺里的第三条（第 14 条的距离度量**取切比雪夫**，理由是它对「静态建筑视野是圆还是方」这个未决问题单调保守——方形球包含同半径的圆形球，所以 `cheb > R` 对两种形状都够；欧氏会在 `dx=dy=R` 时**假通过**，正是第 14 条要防的那件事），并指出他记的三条阻塞其实是两条、且那两条是**同一个**阻塞；给 #34 逐 hunk 对了一遍（六个 hunk 三个已被后续 PR 改到别处，剩下三个给了可直接粘的文本，其中一条的论据要收窄——第 8 条其实有一个不经过城区的等价判据）。结算 #26 议程（18 项已定 4 项，**剩下 14 项收敛到 #23 一个瓶颈**；R 那条「必须由认领 `render/` 的人在场才谈」的前置已满足，给出基线：现有约 2700 行合 2.5–3 人日，基础设施那一半做完、交互那一半一件没做，诚实的估计是总计 8–9 人日，已越出原 5–8 预算，另加双视图 1.5–2 人日） | #40、#42、#43、#47、#50（原 #48/#49/#50 三条合一）、#51

**随后小组明确「只剩一个人在持续推进」，于是把 #33 / #34 从「给建议」改成代为落地。** 两条都推在**他们自己的分支**上：原提交连同作者名原样保留在历史里、我的修复另开提交，`git log` 仍然分得清谁做了什么 —— 那正是「每个人推自己的提交」这条规矩要保护的东西，而在无人推进时，代填日志才是它真正禁止的事。合并一律用 merge commit 而非 squash，否则两个作者会被压成一个。

**#33 的六条评审意见里只有第一条真挡着合并**（第 10 条漏报），其余是一行的顺手。落地时把三条阻塞减到两条：**第 8 条其实不需要城区**，判据换成「存在一条从任一集结点到 `keep`、全程不穿墙的通路」即可，而且它比字面判据更贴合意图（缺口被第二道墙堵在后面时，字面判据放行、这条判它没缺口）。还把 #29 第三条一并答掉：**第 14 条的度量取切比雪夫，理由是它对「视野是圆还是方」这个未决问题单调保守**（方形球包含同半径圆形球），所以那个问题不必先答；刻意**不留 `vision_metric` 旋钮**，因为拨错的症状是「校验通过」。自检 116 → 133 条断言。

**过程中撞出一个还没现形的真 bug**：✓ / ✗ / ⊘ / … 都不在 cp936 里，所以 `py validate.py maps/demo.json` 会抛 `UnicodeEncodeError` 而**报告一个字都印不出来**。现在看不见只因为仓库里还没有地图文件 —— 唯一走到打印路径的 ctest 是「0 张地图即失败」，那条分支不打记号，**第一张地图入库当天它就会现形**。修在边界上（编码不了就退回 ASCII 记号），与 #47 那两条同源。

**自己也踩了一次「白名单挡不住没登记的那一项」**：临时开的 `build33/` 不匹配 `.gitignore` 的 `build/` 与 `build-*/`，`git add -A` 一次带进去 902 个文件（已撤销、未推送），补了 `build*/` 兜底。这与 `tests/CMakeLists.txt` 里「ctest 标签清单是白名单」是同一种失败，而我当天早些时候刚在别处引用过那条。

**破坏性验证六条，其中一条第一次「变绿」是因为探针没生效**（perl 正则没匹配上，文件根本没被改）—— 换 `sed` 删行重做才真的红。**探针无效与修复无效看起来一模一样**，这与本批回放那条「第 15 条变绿」是同一类教训的另一面。

**最后把两个开放 issue 也清了（#29 / #26），open issue 归零。** 关之前逐条核落点——**只有每一条都在长期文档里有位置，关掉才是清算而不是丢掉**。核下来只有三处没有落点，用 #52 补上：`render/` 预算基线（#26 议题 R 一直卡在议程自己写的前置「必须由认领 `render/` 的人在场才谈」，而认领人已就位）、问 4 的前提已从承诺变成实物（`rts/replay.hpp` 落地，所以只剩「用哪几个片段」）、问 5 不是悬着而是排在第 3 周。A1–A5 只活在 #26 与 #23，所以整个连锁搬到 #23 上再关。

**过程中更正了我自己前一天的两处判断。** 一是上一条评论里写「#26 不关，因为 A9/A10/A11/R 需要落点」——**没查就下的结论**，那四条一直都在 `演示与可视化清单.md` 第 9 节里。二是我曾支持队友给 A4 的那条论据「候选乙是校验器第 8、9 条能实现的前提」，**它对第 8 条不成立**（第 8 条有不经过城区的等价判据，而且当天已经实现并合入），所以 A4 现在只剩第 9 条支持它，应回到强度层面比较——这条要主动说，否则那个论据会在会上被反驳掉、反而削弱一个本来站得住的结论。

`render/` 那份基线值得单记，因为它的结论不好听：**基础设施那一半做完了（约 2.5–3 人日），交互那一半一件没做**（框选、编队、建造放置与预览、HUD、实体绘制、血条），**总计估到 8–9 人日，已越出原 5–8 预算**，演示两项另计 1.5–2。所以那一条议题的产出**不是一个数、是一次取舍**，给了三个具体主张。双视图的成本分布容易估错：数据侧几乎免费（`FogLayer` 本来就必须存在），**贵在版面**。

**最后小组定了两条：#23 照 #15 / #16 的先例合并，#50 按我的建议先合。于是 open PR 与 open issue 全部清零，`main` 上的 `rts_core` 从 4 个头变成 13 个——1c 不再被卡。**

#23 的先例后半段（落结论）是本次上机最重的一块。提案里 §10 有八项待拍板，其中五项我能直接落（归口类、已被 #46 / #52 答掉的、素材已落地的），**三项是范围决定、提案自己写着「不宜由两三个人拍」，交给小组定，三项都按我的推荐通过**：完整接受守方升级轴（含兵种等级）、接受 4a 那对成对条款、2.1 改用森林带。

**最值得记的是第二项，因为它不是「加机制」而是「修矛盾」。** `CLAUDE.md` 原写「破坏速率是兵种的固定属性、与等级无关」，而它与「建筑与城墙走**与单位完全相同的战斗解算**」直接冲突——同一套解算 + 等级提升伤害 ⇒ 速率必然随等级涨。**原写法要成立，只能在「不要做成独立的特例系统」那条明令上挖一个洞。** 所以它从来不是「甲乙两个都行、挑一个」，是原写法自身不自洽。结构锁换成一条更锐利的不变量：**破墙时间必须落在一个区间里，不趋于 0 也不趋于 ∞**。这同时结算了 `rts_core 接口契约.md` §5.1 那个悬了两天的甲/乙，而 §5.1.1 当初写的「差别整个落在 1c 内部、一个字节的接口都不用改」现在成了**已验证的事实**——当初那条推理是为了论证「不阻塞契约」，定案反过来印证了它。

**2.1 那一项我要改口，而理由值得写清楚。** 我此前在四处都主张候选乙，这次改投森林带，依据是两条新事实、不是重新权衡：一、乙那个已知的洞（城区固定 ⇒ 墙类决策只剩「修旧」，而 `CLAUDE.md` 明写建造阶段决策是三方的）原打算用「`Keep` 升级扩城区」补，**而那条已随提案 §4 一同作废**——它本来只为补这个洞而存在；二、森林带封住了当初否掉甲的那个洞，**且封的方式很好：森林挡视野把杀伤区压成薄带，薄带的深度是几何上界、钱买不到**。附带好处是零新机制、零新地图字段，**校验器第 7 条今天就能写出判据**（乙要先给 6.2 加两个字段，那是一次跨模块契约变更）。

**同时认下两条不好听的后果**：它与校验器第 10 条**正面对立**（可满足，但生成器可行域变窄，而生成器是「不通过就丢弃重生成」）；**每一条被强制要求的森林通道本身就是一条通到玩家外围经济的遮蔽潜行道，AI 会学会用它**——这是机制后果、调覆盖率调不掉，所以必须是明写的设计决定，不能是事后被人发现的副作用。另加 2.1.5：**玩家不得有清除地形的手段**，否则上面整节退化成数值劝退。

顺带修的两处 off-by-one：防死亡螺旋那节写「三条对策」而实际列了四条；**「维持费」这个全仓库只出现一次、从未有过定义的空词**随资源表改动一并消失。还有一条自查：这一批新增了两处交叉引用，而 #51 刚修过一次「已合并的代码指向不存在的章节」，所以**写了个脚本扫全部「见「X」」引用**确认没有悬空的。

**清完队列之后又做了一轮盘点，查出 #53 自己留下的漂移：它把结论落进了三份文档，却没回头改「引用那些结论的地方」。** 四处：`无尽模式与地形分层.md` §10 还写着「需要小组拍板」8 项而其中 7 项已拍完、§11 还写着「若本文通过」、`validate.py` 第 7 条还标着 `BLOCKED` 且 note 引的是已作废的候选乙、`地图与场景设计.md` §8 那张表里第 7 条那一行还写着「取决于 2.1 采用甲还是乙」（同节的散文被 #53 改过，**表格行漏了**）。这与 #51 修的是同一类问题，而**我在同一天引用过那条教训**。

**顺手把校验器第 7 条落了下来**（阻塞 2 → 1）。判据逐字取自 2.1.1，零新字段。真正需要论证的是**连通性取 4 邻接，而第 10 条取 8 邻接——两条都对**：第 10 条问「单位能不能沿森林走到墙边」（**移动**），本条问「任何闭合墙线能不能绕开这条带子」（**拓扑**）。斜向森林链撑不起后者，墙线能从两格之间那个对角缺口穿过去、一格森林都不碰。这个取法同时**绕开一个还没定的问题**：「一圈只靠斜向相连的墙算不算闭合防线」取决于 1c 怎么实现墙（5.1 说墙是高代价可通行，「闭合」是防御语义、还没有代码）——不必等它，因为 4 连通的森林路径会被 4 连通与 8 连通的闭合墙线**同时**跨过。手法与第 14 条取切比雪夫相同。

**过程中实测出一条反直觉的事实并写进了 docstring：真正会把第 7 条写坏的是 `corner=`，不是 `diagonal=`。** `neighbors()` 默认 `corner = passable`，于是一步斜向要求两个正交角格都在集合里，而那两格都在的话 4 连通本来就走到了——**所以「8 连通 + 同一个谓词判角」恒等于 4 连通**（这也解释了第 10 条为什么必须显式给 `corner=` 才修得好那次漏报）。我第一次的破坏性探针正是翻了 `diagonal`，得到一个**骗人的绿**；换成加 `corner=grid.is_passable` 才红 2 条。

**然后查出「仓库一张地图文件都没有」这个前提一直是错的。** `game/testdata/fixture_min.json` 完全符合 6.2 格式、一直都在，只是没人把 Python 校验器指向 C++ 侧的测试数据。指过去当场三个结果：10 条里 **9 条过、第 7 条不过**（它写在 2.1 定案之前）；**cp936 的 ASCII 退回第一次真的被执行**；那两条此前被判为「在 CLI 层构造不出来」的 ctest，现在都构造得出来。**这两种说法在处置上不同**——「等第一张地图入库就会现形」可以等，「已经坏着、只是没人踩到」不能等。

改夹具合规只动三处，但**位置不是随手挑的**：7×5 下右侧墙 `(6,0..2)` 的八邻覆盖了 `(5,0..3)`，森林一碰到集结点 `(6,4)` 的八邻就会被第 10 条判成遮蔽通道，所以第 7 与第 10 条能同时满足的位置很少，可行解是把森林压在 `x=5, y=0..1`。这条约束写进了夹具的 `_note`。新增两条 ctest（29 → 31），其中 `map_gen_validate_fixture` 是那个 cp936 修法**唯一的回归**。

**原计划是手写 §9 的演示地图，写不了，而且理由属于组内明文规则**：演示地图要定 `size`（`CLAUDE.md` 明写地图边长依赖速度标定）与集结点数量（明列为待定数值，而地图规范曾把它写成「定为 4」，正是那条规则记下的唯一一次违反）。手写一张就是把两个平衡旋钮当成结构结论。夹具没有这个问题——它的 `_note` 已声明数值是占位、非 §9 演示地图，这是 `game/testdata/` 立下的先例。于是「校验器跑在真图上」这个价值拿到了，「定死待定数值」这个代价没有付。

**这一轮的破坏性验证五条，三条第一版无效或指错，比通过的那两条更值得记**：翻 `diagonal` 是空探针（见上）；把 outer 资源点挪回 `[5,0]` 而森林不动照样通过——因为 `(5,0)` 自己就是 `Forest` 且在边界上，第 7 条本来就满足；余下三条（加 `corner=`、起点改八邻、把第 7 条摘回 `BLOCKED`）都红且原因正确。**探针无效与修复无效长得一模一样**，这句话本次上机应验了三遍（另一遍是 #33 那次 perl 正则没匹配上）。

另记一件与本轮改动无关但会浪费别人时间的事：开工前跑 `ctest --test-dir build` 报 **21/21 全绿**，而标签清单算下来该有 29 条——`build/` 是 #50 之前配置的，契约那 8 个标签的**条目根本不存在**，ctest 当然绿。**`ctest` 不触发重新配置，只有 `cmake --build` 会**，所以 `git pull` 之后只跑 ctest 会得到一个绿色的子集。这与「ctest 标签清单是白名单」同形，只是白名单在**构建目录的缓存**里，而且比标签那条更隐蔽（标签写错会红，条目不存在连红的机会都没有）。目前没有守卫。

**落地组内口头表决的 §6：三种景物变成有血量的可破坏障碍，并附一条原提案没有的机制——破坏后产出资源。** 表决只给了结论，而「产出资源」不在 §6 里，所以要单独论证：它先过了本仓库对新机制的第一道测试（**清野是决策而非杂活**——收益是一次性资源与腾出的建造格，代价是**拆掉自己防线前面的一道天然减速带**，因为障碍本就是「高代价可通行」；「登墙阶梯」正是死在这道测试上）。四条**结构**决定（不是数值，所以现在定死）：产出跟着材质走（木质→木材、石质→石材）且**没有一种产金币**；产出归**造成最后一击的一方**——刻意不写成「只有守方能拿」，因为攻方没有经济系统，所以那是既有规则的**推论**而非特例，附带得到一条真实后果（攻方顺路拆掉障碍，守方永久失去那份产出，于是清野的时机有了代价）；**不可再生**；以及**建筑摧毁不产生 `Rubble` 实体**。最后一条是**封一个具体的洞而不是澄清措辞**：`CLAUDE.md` 已有「废墟折扣」与「废墟回收 = 造价 ÷ 3」两条，若废墟是 `Rubble` 实体且拆它产石材，就有一条「建墙 → 被拆 → 拆废墟得石材 → 打折重建」的净赚回路，而那三个数值分别在三处定、由不同的人定——每一处单独看都合理。封法零成本：本文里的「废墟」本来就是建筑自己的一个状态（一个折扣）、不是实体。同时把「不可再生」做成**结构而非约定**：`World` 根本没有「跑动中生成一个障碍」的入口，障碍只能经 `ObstacleInit` 从建局进来，于是「可再生」写不出来。

**清野否掉了 `AtkObst`，走 `CommandKind::Clear`**，两条独立理由：攻方**不需要**那个动作（寻路已把障碍当高代价可通行，撞上去自动破坏——这正是 §6 实现成本低的全部来源，给攻方加一个动作只是把空间从 13 变 14、浪费样本）；守方清野是**玩家级决策**，与「驻守墙段归玩家级命令」完全同构。于是「战术动作 13 个」这条已定项不受 §6 影响。新成员**追加在枚举末尾而不是按语义插在 `Cancel` 旁边**：枚举值就是回放的线路编码，插在中间会让旧回放里 `Train`..`PickSpawn` 的字节被重新解释成别的命令（`Garrison` 变 `Composition` 之类），而那种失效不报错、只是回放出一局不同的仗。

**`kWorldHashTag` 因此 `World/1` → `World/2`，而触发它的改动一点都不显眼。** `deferred_` 是 `std::array<..., kCommandKindCount>` 且整块进 `state_hash`，所以往枚举末尾加一个成员就改了哈希口径。这条标签当初是为「1c 加第四组实体」预备的，第一次真正用上却是这么一次小改动——**教训是改哈希口径的不一定是「加了一组实体」这种看得见的事**，动枚举前该先搜一遍它有没有出现在喂入路径上。

**同一处查出一条「该红却绿」：那条名叫「六种需要数值的命令被记账」的用例，在我加了第七种之后照旧全绿。** 它把六种硬编码成 `array<..., 6>`，第七种进不了它的视野。改成「两份清单（记账 / 已应用）必须正好铺满整个枚举」，并去掉标题里的数字（同 `tests/CMakeLists.txt`「刻意不写条目总数」）。**破坏性验证在这里也纠了我自己一次**：`STATIC_REQUIRE` 那半条是编译期的，所以「漏一个」撞的是编译器不是测试；真正需要运行期那条覆盖断言的场景是**重复登记 + 漏一个**（长度和仍然对），照此重做探针才见到 `11 == 12`。另一次同类：`harvest_of` 的「没有一种产金币」与三条具体映射原写在**同一个 TEST_CASE** 里，而 `REQUIRE` 一失败就中止用例——探针「把 `Rubble` 改成产金币」先撞上具体映射那一行，**那条循环一次都没跑到**。拆成两个用例后同一个探针让两条各自红。

**6.2 加 `obstacles` 字段（跨模块契约变更），并刻意没有顺带加第 18 条要的解禁波数字段**：后者的字段名与取值形式还没定，捆在一次变更里会让没定的那件拖住定了的。字段**必填、空数组也要写出来**（缺失与刻意为空不可区分是个洞，同 `Command::None` 不当哨兵那条）、**不带血量也不带残血比例**（城墙的残血来自 2.3 的设计要求，障碍没有对应的要求）。全链路打通到 `World`：`MapData::ObstacleNode` → `MapLoader` → `game::InitialHp::obstacle` → `ObstacleInit`，血量仍由调用方给、默认刻意是 1。

**校验器第 19 条的两个阻塞一起解开，并查出第 22 条这个洞。** 第 19 条此前双重阻塞（等 §6 表决 + 地图格式没有那份列表），两条都解了，实现时取**白名单**而非「不是 Forest / Rock」——白名单同时挡住拼写错误与将来新增的地形名。第 22 条是加字段时顺带查出来的：**新增一类点位实体就新增一片摆放冲突面，而那一片没有任何现存检查覆盖**（摆在水里的树桩、同格两处障碍、与 `keep` / 资源点 / 墙段 / 集结点同格），性质与第 16、17 条完全相同。**刻意与第 17 条分开编号而不是并进去**：第 17 条已实现、已有测试，把新实体塞进它等于让一条绿着的检查悄悄扩大职责，而 review 时看不出覆盖面变了。校验器 21 → 22 条、已实现 13 → 15。

**另修一处与本轮无关、但会让「修复无效」伪装成「探针无效」的坑**：`tests/` 与 `render/` 里那三处 `file(COPY)` 把夹具地图复制进中文目录，而 `file(COPY)` **不把输入登记进重新配置的依赖表**——改完夹具只跑 `cmake --build` 时副本不会更新。这次它红了（新字段必填），但危险的是反方向：改夹具去修一条失败的用例，用例照旧红，因为它读的是旧副本。换成 `configure_file(... COPYONLY)`，并实测确认（改夹具后只跑 `--build`，副本从 3 个障碍跟到 2 个）。

**当天晚间：执行「多态落点」那次方向变更的全面回撤，并把「创新点怎么讲」落成文档。** 组内讨论定下的是回到按兵种那版（`UnitBehavior`），同时**保留队友在 Linux/GCC 上的调试与修复**——这两件事恰好在同一个分支上（#63 = GCC 修复 + `build.sh` + `UnitBehavior`），而 #65 是它的后继分支、加一层再把它换掉，所以**回撤路径是「#63 按现状合、#65 关掉」，一个提交都不用拆**。合并前在本机重验 #63（35/35 全绿、零警告、确定性守卫过），审了它对 `CLAUDE.md` 的措辞订正（「无虚函数分派」→「多态按兵种而不按实体」，**改得对**：该节自己的澄清早就写着虚函数分派本身是确定的，标题与正文原本不自洽）。

**回撤的依据是三条可检验的事实，不是口味**，全部实测过：①#65 宣称的唯一收益是「一张 `Unit → Archer / Spear / …` 的继承图」，而落地版 11 个兵种里 **10 个是 `using ArcherUnit = TypedUnit<…>` 模板别名**，画出来是 `Unit → TypedUnit<Archer, Ranged, Single, LightFast>`；②它的多态面与被它替换的那版**逐格相同**（三轴返常量、`can_engage` 零 override、`can_break_structure` 仅 `Phoenix` 一个）；③根因是**时机**——会厚的那些（冲锋助跑、溅射衰减、前摇、对建筑特攻）全在 1c，一行未写。另外它指名要我拍的吞吐那条**当时无法拍**：`bindings/` 与 `train/` 目录不存在，`BatchedEnv` 一行没有，而 `CLAUDE.md` 明令性能改动一律 Release 实测、不许靠推理。

**顺带实测出一条会静默发生的缺陷并留档**：#62 与 #65 各自把 `World/1` 改成 `World/2`，而 `git merge-tree --write-tree` 显示**全仓只有 `world.hpp` 一处冲突、且冲突块在标签上面那段注释（311–324 行），标签本身第 325 行自动合成 `World/2`**——两边做了逐字相同的修改，git 视为无冲突。于是两份不同口径共用一个标签，**正好废掉这个标签存在的唯一理由**。冲突恰在标签上一行是最坏的位置：解冲突的人在两段解释里挑一段，下面那行「两边都同意」根本不引起注意。撤回 #65 后撞车不再发生，但规矩留下了：**改 `kWorldHashTag` 的 PR 要在标题里写明它占了哪个号。**

**写讲法文档时核对自己的两处断言，各查出一个问题。** ①我在 #65 评论里写过「`range()` 不接受 level 参数，所以『射程随等级变』在类型层面写不出来」——**核代码发现 `UnitBehavior` 里根本没有 `range()`**，那句来自 issue #64 的提案草图，而落地版刻意一个数值都不放。已在 #65 更正，**若不核就写进讲稿，答辩时被要求指出那个签名会当场落空**。②`ObstacleInit` 的注释写着「`World` 没有任何造障碍的入口，于是『可再生』写不出来」，**而 `place_obstacle()` 是公开的**——那条保证当时只是约定，注释读起来却像结构保证。**修的是代码不是注释**：改成私有（只有构造函数调它），受影响的那条用例改走 `WorldInit`，并做破坏性验证确认外部调用报 `C2248`。这两处正是那份文档要防的形态，所以写进了它的附录。

**次日凌晨：评审并结算队友的 #68（`Ram` 的矛盾定案），它本身自洽，但揭示了两处 diff 之外的漏改，其中一处让那个 PR 内部自相矛盾。** #68 修的是 `CLAUDE.md` 里两句关于 `Ram` 的话冲突（花名册写「贴身」、结构破坏规则写「装填慢、弹道有飞行时间」），定案取撞锤——这个判断有三条独立证据支持（标识符本身就是 battering ram、花名册那一列、精灵流水线 §2 一直按撞木滑动做），且它最有价值的部分不是订正而是**顺手给那条失去支点的机制另找了支点**（「AOE 落点在前摇开始那一刻锁定成坐标」），因为删掉「飞行时间」会带走「对散开的单位效率很差」的全部解释。**漏改第一处是「游戏设计要点」里那句「投石车对静止密集目标高效，对散开移动目标大幅 miss」**——花名册里没有投石车，那句写的就是 `Ram`，而「投石车」的定义正是抛射器械，**所以它是刚被删掉的那个错误的另一份，只是换了个词，`grep Ram` 搜不到**。要紧的是 #68 新写的 `sprite_atlas.hpp` 论据把那一句当权威引文引了进去，于是**新论据引的是旧错误**。由此提炼一条：**「同一件事写在两处然后漂移」还有一个变种是漂词而不漂内容**，订正一条机制时除了搜标识符还要搜它的同义描述。第二处是根 `README.md` 那一栏还写着「带弧线的弹道」，而 §8.1 已明确撤掉弧线（三种真有弹丸的都是直射）。

**同时认下我自己在 `sprite_atlas.hpp` 里那段论据的错**（是队友查出来的）：我把攻击分成近战 / 远程两类并引 `Ram` 当论据，**而 `Ram` 恰恰是三类里唯一没有弹丸的那个**。他的定性准确——把一条更一般的要求（承诺与落地之间有时间差）窄化成了一个特例（飞行时间）。但我自己再往下看一层：**那句话真正的毛病是让一条注释的正确性依赖于一个数值机制的具体形态**，而那正是本仓库明写过的规则的另一面（「接口与回放格式的承诺不得依赖任何待定数值」）——改成「承诺与落地之间有时间差」之后它依赖的是结构，标定改不动它。

**核他那条「28 与 35 不是同一个集合」的观察，顺着它查出 `CLAUDE.md` 一处自我违反。** 差集实测正好是 7 条 `render_*`（服务器默认 `RTS_BUILD_RENDER=OFF`），而**其中 4 条是 `WILL_FAIL`**——于是全仓的 `WILL_FAIL` 实际是 9 条，而 `CLAUDE.md` 写着「这类条目现在有五条」。那是我加 `render/` 时留下的漂移，**但它错在同一节上面十几行刚立的规矩上**（「此处刻意不写总数……写死的数字对读者没用，只会漂移」）——立完规矩就自己违反了一次。所以修法是**不写数字**（「`ctest -N` 里名字以 `_is_red` 结尾的都是」），不是把 5 改成 9。另落一条区分：**「该修的绿色子集」与「设计内的绿色子集」表象相同（一个比预期小的绿数字）、性质相反**——前者是过期构建目录给的、已有 `cmake_configure_is_current` 守着；后者是配置给的、**不需要守卫，需要的是一句话**，免得下一个人看到 28 去找哪里坏了。代价那一面也写下来了：`render/` 的 C++ 永远只有 MSVC 一套工具链把关。回报那一面是当初「按能不能在默认构建里被测切模块」的兑现：`rts_tests_{map,scene,iso,names,utf8path}` 在服务器上是跑的。

**另修一处 Markdown 结构缺陷**：#68 那段关于 python alias 的补注插在「后者与……是同一个原因的两个产物」之前，于是紧跟引用块的非空行被 lazy continuation 吸进引用块，而「后者」「它自己」的指代对象被中间八行隔断（读者会以为「后者」指 `${PIPESTATUS[0]}`）。**插入一段离题的补注时要看它落在哪两句之间**，不只看它自己写得对不对。 | 本 PR；#45 / #46 评审；**代为落地并合入 #33 / #34**；**#52 落 `render/` 预算基线并关掉 #29 / #26**；**合入 #50（契约上 main）与 #23（提案），并以 #53 落 #23 的结论**；关 #25；清队列 **11 → 0 个 PR + 0 个 issue**；**收尾修 #53 自己留下的四处漂移 + 落校验器第 7 条 + 让校验器真的读一张真图**；**执行回撤：审并合 #63、关 #65、合 #62、更新 #59**；`创新点与答辩讲法.md`；**审并合 #68 / #66 / #67，队列再清零** |
| omg321-975 | | |

**本次决定**：

- **`rts_core` 的接口契约拆成 1a / 1b / 1b-2 / 1c，前三块是我的、1c 仍是 @ArLiangz 的。** 三块**均已落地**，公开接口至此全部定稿，**不再是并行开发的阻塞项**。拆的理由是各块不依赖后面的：`train/` 照 1a 就能定网络与动作头形状，`render/` 照 1b 就能画实体
- **回放排在 1b 之后而不是与它并行**，因为 `state_hash()` 的喂入顺序必须先定死——事后改它等于让所有已录回放失效。这个顺序当场还了三次本：写迷雾时查出 `visible` 通道的缺陷、写回放时查出 `state_hash` 漏了命令队列、以及 `WorldInit` 表达不了「有单位的初始局面」
- **回放只存外生输入，不存机制产物。** 存了 `kill_unit` 之类，回放测的就是它自己的日志而不是仿真
- **回放文件头存平台指纹与哈希口径标签**（`kWorldHashTag`），并对两者**区别对待**：指纹只管浮点可复现性，所以只在真的对不上时才升格成成因；口径不同则意味着比对本身没有意义，当场拦。**这条替掉了原先打算给在途弹丸预留一组空实体位的兜底**——改 `state_hash` 的喂入清单时把标签一起改即可
- **一条状态哈希都没有的回放判失败，不判通过**（同「确定性守卫扫到 0 个文件即失败」）
- **`-ffast-math` / `/fp:fast` 变成编译错误**，落在 `rts/replay.hpp`（头文件自足性守卫会把它编成一个 TU，所以每次构建都检查）。`CLAUDE.md` 原有的禁令此前只是文字
- **`visible` 观测通道改为三档，`kObsVersion` 1 → 2。** 两档时「从未侦查过」与「记忆里那段墙是破的」在张量里不可区分，而后者是核心评估指标要的东西
- **迷雾记建筑、不记单位**（建筑不动，记住的单位位置主动误导），且**迷雾进状态哈希**（记忆是累积状态，不是每 tick 重算的）
- **地形与资源枚举归 `rts_core`**，`game::Terrain` / `game::ResourceType` 是别名。「`rows` 逐字符解码 ⇒ palette ≤ 10 项」那条断言**留在 `game/`**——它约束的是地图文件的行编码，`rts_core` 不读文件
- **`Command::slot` 取格线性下标 `y*w+x`**，因为仿真接受的必须是完备信息（玩家能点任意一格）；决策层那约 60 项的槽位子集属地图未定字段，粗化归 `train/`。格数上界（< 65535）是一条**运行期**检查，不是注释
- **掩码里未定的位一律默认允许**：错误地允许只是浪费样本，错误地禁止会让 agent 永远学不到那个动作
- **玩家指令集与守方 RL 决策层的动作空间是同一套枚举。** 这是 #46「守方 AI 是人类玩家的替身」的直接后果，推论是 `Command` 必须是玩家指令集的**完备**表达——`守方AI与协同演化.md` 第 3 节那个「建墙 / 建塔 / 建防空 / 建采集」的四组粗化**不能烤进契约**，否则人类玩家没法指定造哪一座建筑。粗化属 `train/`
- **接口与回放格式的承诺不得依赖任何待定数值**（由血量宽度那个问题推广而来，见上）
- **血量与伤害取 `int64`**
- **动作、命令与观测通道的枚举成员不受「标识符 ≤7 字符」之限**，但仍须成组同前缀。写明这条豁免是因为规则原文是「一律」，不写就是静默违反——与素材键那条同源
- **组内只剩一人持续推进时，「每个人推自己的提交」按其目的执行而不是按字面执行。** 那条规矩保护的是人员分工页的证据，所以代为落地队友的 PR 要满足两条：**推在他们自己的分支上**（原提交连同作者名留在历史里、我的修复另开提交），且**用 merge commit 而非 squash**（squash 会把两个作者压成一个）。**代填 `日志.md` 仍然禁止** —— 那才是这条规矩真正不能让步的部分
- **守方有一条升级轴：堡垒等级**，三个输出（人口上限 / 建筑等级上限 / 兵种等级上限）+ 「买活」。堡垒等级**本身不设上限**，早期经济复利由「顶不满」封住而非由档位表封住。`CLAUDE.md`「倾向保持精简（无科技树）」正式作废——**但作废的只是「一条都不做」，不是「可以随便加」**
- **破坏速率随等级缩放，且城墙城门也可升级（只加血量、同倍率）——两条成对。** 结构锁改为「**破墙时间落在一个区间里**」。**这一条修的是矛盾不是加机制**：原写法与「建筑与城墙走与单位完全相同的战斗解算」冲突，要成立只能在「不要做成独立的特例系统」那条明令上挖洞。契约 §5.1 的甲/乙因此结算为乙，1c 唯一要定的是**等级量化成几档**
- **校验器第 7 条的森林路径取 4 连通，第 10 条取 8 连通——两条相反且都对。** 前者问拓扑（闭合墙线能不能绕开），后者问移动（单位能不能沿森林走到墙边）。4 连通同时**对「一圈斜向相连的墙算不算闭合防线」这个未决问题保守**，所以不必等它，同第 14 条取切比雪夫。刻意不留连通性旋钮：**拨错的症状是「校验通过」**
- **`game/testdata/fixture_min.json` 升格为全仓唯一的真地图，两侧共用。** C++ 侧 `MapLoader` 与 Python 侧校验器读同一张图，因为 `mapfile.py` 是全仓唯一的地图 I/O 入口——两侧各用一张图，就会各自漂移出一套「合法地图」的理解。代价是**它的三处几何受校验器约束**（森林带位置），已写进它的 `_note`
- **`§9` 的演示地图现在不能写。** 它要定 `size` 与集结点数量，而两者都是明列的待定数值（地图规范曾把集结点数量写成「定为 4」，是那条规则记下的唯一一次违反）。**「让校验器跑在真图上」不需要它**——用声明了占位性质的夹具即可，先例是 `game/testdata/`
- **2.1「外部资源点不可围」改用森林带**（外部簇周围铺 `Forest` + 到地图边界的森林连通条件），甲与乙都作废。**后半句不是可选的**——少了它玩家绕着森林带在外面画更大的一圈就行，纯数值劝退。连带 2.1.5：**玩家不得有清除地形的手段**
- **无尽模式看「中位存活波数」，不看胜率**——必败是设计内的，「赢」这个事件不发生
- **防死亡螺旋那节 overshoot 了自己的论证。** 原文写的危害是「玩家明知必输仍需拖很久」，**那是节奏问题不是公平问题**；而无尽下每一局都以螺旋收尾，完全防止它等于让游戏永不结束。准确形态是「慢性死亡」，对策是快速结算 + 始终有决策。**还缺一条「投降 / 主动结算」**
- **「等级提升哪几个数值」只定归口与判据，不定答案。** 血量+伤害是 L²、只涨一个是 L，三种都满足「别让等级改变机制」这条原意，而**现行写法选中了最陡的一档、可能是无意的**。判据**只能在野战里测**——城墙攻防里冲锋助跑、枪阵克骑、风筝根本不发生
- **关 issue 的前置是「逐条核落点」，不是「看起来没人管了」。** 一条内容若只活在 issue 里，关掉就是丢掉；只有它在长期文档里有位置，关掉才是去重。#29 / #26 按这条核过，核出三处没落点的，先用 #52 补上再关
- **`render/` 预算：现有约 2.5–3 人日，到可玩前端估计总计 8–9 人日，已越出原 5–8。** 演示两项另计 1.5–2（双视图数据侧几乎免费、贵在版面）。**这一条的产出是一次取舍而非一个数**，三个主张见 `演示与可视化清单.md` 第 9 节问 3。人日是排期估计，不受「关于数值」约束
- **`.gitignore` 的 `build/` 与 `build-*/` 是白名单，补 `build*/` 兜底。** 白名单挡不住没登记的那一项，与「ctest 标签清单漏登记抓不到」同源
- **第 14 条的距离度量取切比雪夫**，理由是它对「静态建筑视野是圆还是方」这个未决问题**单调保守**（方形球包含同半径的圆形球），所以那个问题不必先答。**刻意不留 `vision_metric` 配置旋钮**：拨错的症状是「校验通过」，没有任何东西会提示
- **校验器第 8 条不依赖城区**，判据换成「存在一条从任一集结点到 `keep`、全程不穿墙的通路」。它比字面判据更贴合意图：缺口被第二道墙堵在后面时，字面判据放行而这条判它没缺口
- **契约那三条堆叠 PR 合成一条对 `main` 的 PR**（提交原样保留）。**「减小单条 PR 的评审面」与「减小评审总量」不是一回事**——前者在没人评审时会放大后者：三条堆叠意味着任何一条 squash 合并都要手动 `rebase --onto` 两次、每次 rebase 让评审意见挂在旧行号上，**而单独评审第一条不解锁任何东西**。此后再切分 PR 要先问「单独合掉最前面那条，有没有人能因此开工」，答案是否就不该切

- **提案 §6 的机制那一半通过，并附一条原提案没有的机制：破坏后产出资源。** 四条结构决定见 `无尽模式与地形分层.md` 6.6.2。要留意的是**第四条是在封洞而不是在澄清措辞**——「建筑摧毁不产生 `Rubble` 实体」，否则「废墟折扣」与「废墟回收 = 造价 ÷ 3」两条既有规则会与「拆碎石得石材」合成一条净赚回路，而那三个数值分别在三处定。
- **清野走 `CommandKind::Clear` 而不是 `UnitAction::AtkObst`，战术动作仍是 13 个。** 攻方不需要那个动作（寻路已把障碍当高代价可通行），守方清野是玩家级决策——与「驻守墙段」同构。
- **命令枚举的新成员一律追加在末尾，不按语义分组插入。** 枚举值就是回放的线路编码，插在中间会让旧回放里后面那些命令的字节被重新解释成别的命令，而那种失效不报错、只是回放出一局不同的仗。
- **`kWorldHashTag` `World/1` → `World/2`，旧回放要重录。** 触发它的只是往枚举末尾加了一个成员——而那个枚举恰好是一个进哈希的数组的长度。
- **6.2 加 `obstacles`（必填，空数组也要写），但刻意不顺带加第 18 条要的解禁波数字段。** 后者的字段名与取值形式还没定，捆在一次跨模块契约变更里会让没定的那件拖住定了的。
- **第 22 条与第 17 条分开编号，不并进去。** 第 17 条已实现、已有测试，把新的实体类别塞进它等于让一条绿着的检查悄悄扩大职责，而 review 时看不出覆盖面变了。
- **`Ram` 是贴身撞锤，不是抛射器械**（#68，口头定案）。`CLAUDE.md` 原写的「装填慢、弹道有飞行时间」与花名册那一列的「贴身」矛盾，取后者。**连带一条必须写下来的实现要求**：AOE 落点在前摇开始那一刻锁定成一个坐标，伤害在前摇结束时按那个坐标结算——若落点跟着目标句柄走，「散开克溅射」这条机制性克制直接作废，**而这种失效不会让任何测试变红**。在途弹丸的清单因此从四种缩到三种（箭 / 影矢 / 狙击弹）
- **订正一条机制时，除了搜它的标识符，还要搜它的同义描述。** 上面那条改完之后，「游戏设计要点」里那句「**投石车**对静止密集目标高效」还留着——花名册里没有投石车，那句写的就是 `Ram`，而「投石车」正是抛射器械。**「同一件事写在两处然后漂移」的这个变种漂的是词而不是内容**，所以 `grep` 标识符查不出来。它已经让 `sprite_atlas.hpp` 的新论据引了一句旧错误
- **`WILL_FAIL` 条目数不写死。** 原写「现在有五条」，实际九条（`render/` 另有四条），**而它错在同一节上面十几行刚立的规矩上**（「刻意不写总数……写死的数字只会漂移」）。改成「`ctest -N` 里名字以 `_is_red` 结尾的都是」
- **「设计内的绿色子集」与「该修的绿色子集」要分开写，前者不需要守卫。** 两者表象相同（一个比预期小的绿数字）：过期构建目录给出的子集是缺陷、已有 `cmake_configure_is_current` 守着；服务器上 `RTS_BUILD_RENDER=OFF` 给出的 28/35 是**设计内的**（不变量 2、3 在构建侧的落地），它需要的是一句话而不是一道检查——免得下一个人看到 28 去找哪里坏了。**要判断自己是不是拿到了子集，比对 `ctest -N` 的清单，不要记数字**
- **插入一段离题的补注时，要看它落在哪两句之间。** 不只看补注自己写得对不对：紧跟引用块的非空行会被 Markdown 的 lazy continuation 吸进引用块，而被隔断的「后者」「它自己」会指向错的对象

**遗留问题**：

- **本节另外三行待各自本人补录。** 本次上机内 @Cody-ic 有 #45 / #46 合入，我不代填——日志是个人成绩材料，替人写等于替人认领
- **本批契约尚未在 Linux/GCC 上编过，而它现在已经在 `main` 上了。** `/w14062` 的作用正是把 MSVC 对齐到 GCC 的既有行为，所以这一批尤其值得在服务器上过一遍（`tools/server/sync.sh && ssh gpu '…/build.sh Release'`）。**这是本次上机唯一一件我做不到的事**——本机只有 Windows
- **`main` 上有约 7800 行 C++ 从未被第二个人读过。** #50 是按「先合、评审改在后面提」合的，那是权衡后的选择（拖着的代价是整条关键路径停摆），但代价要记在明处：**这一批的「该红却绿」类问题会晚一轮才被发现**。缓解手段是那 15 条破坏性验证，不是评审
- ~~**校验器第 7 条的判据定了但代码没写。**~~ **已做**（4 连通、起点取四邻，见上）。**校验器现在只剩第 9 条阻塞，而 2.1 定案把它变难了**：它要的城区 mask 本来由候选乙带来，乙已作废，所以现在没有任何一处会顺带产出城区，要它就得为它单独改一次 6.2
- ~~**第 8 节第 16..20 条一条都没落地**，且「第 16、17、20 条现在就能写」~~ **已部分落地并更正**：第 16、17 条 + 新增的第 21 条已实现（#56）；**「第 20 条现在就能写」是错的**——它的产出是丢弃率（生成器报告），逐图看没有新东西可查。剩下四条各卡在不同的东西上，逐条见下与 #60
- ~~**提案 §6 有两半，只有一半落了地，而这个分裂此前没人写下来。**~~ **已解决**：机制那一半于本日组内口头表决通过（#58），连结论一起落地了，另附「破坏后产出资源」这一半的论证。原文保留在下： `Forest` / `Rock` 不可破坏那一半已随 §4 落地（它是 §4 的**前置**，见 2.1.5）；`Stump` / `Sapling` / `Rubble` 变成有血量那一半**仍待议**，`CLAUDE.md` 那张「可破坏障碍」命名表至今写着「随 §6 一同待议」。§6 从来没进过 §10 的表决清单，因为 §9 把它算作「素材部分已经落地」
- **一张把所有资源都放在城里的地图能通过全部检查。** 第 7 条在没有 `outer` 资源点时空过、第 6 条只查 `inner` 侧三种齐全，于是没有任何一条查「必须有 `outer` 资源点」——而「部分资源点在墙外」是 `CLAUDE.md` 列为不能砍的两个机制之一。数量下界属数值、现在不定，但「≥ 1」是结构性的，可以现在就查。已记进 `地图与场景设计.md` 第 10 节
- ~~**`ctest` 单独跑不会发现 `CMakeLists.txt` 变了，目前没有守卫。**~~ **已补上守卫**（`cmake_configure_is_current`，见下）。原始症状：开工前 `ctest --test-dir build` 报 21/21 全绿而该有 29 条——`build/` 是 #50 之前配置的，契约那 8 个标签的条目根本不存在。**它比「标签清单是白名单」更隐蔽一层：标签写错会红，条目不存在连红的机会都没有**——它不是一条失败的测试，是一条不存在的测试。**守卫有一条绕不过去的边界**：配置早于它存在的构建目录不会注册它（自举问题），代价一次性
- ~~**一张把所有资源都放在城里的地图能通过全部检查。**~~ **已补上第 21 条。**
- ~~**第 16、17 条**~~ **已实现。** 第 19 条要保护的 2.1.5 也补了能查的那一半（`tests/roster_test.cpp`：可破坏障碍标识符不得与地形枚举重名）
- **可破坏障碍现在有血量，但没有任何地方画它们。** `SceneModel` 是 `obstacles` 字段之前写的，所以障碍不进绘制列表。映射与两条坑（一格可能有两个叠加物；障碍会消失所以不能和地形一起缓存）已写进 `地图与场景设计.md` 4.2.1.2，属 `render/` 的增量、不阻塞任何人。
- **清野是否腾出可建造格，需要 1c 在建造合法性里加一次实体占用检查。** `TerrainMasks::buildable` 只来自地形、不含实体占用，所以 6.6.1 那条收益目前没有落点。
- **6.6.1 里承认了一处钝角，不要粉饰**：城内、且不在任何走廊上的障碍，清掉是纯赚，那部分确实退化成杂活。它有界（存量有限、一次性），可以靠把数额压小使它不值得操心，但**那是数值劝退而不是结构封死**。
- **`.gitattributes` 漏了 `*.md`，已补。** 本次用 Python 批量改文档（写时给了 `newline="
"`）之后 `git add` 打出「LF will be replaced by CRLF」，而 `git ls-files --eol` 显示工作区**已经混着**了。危害比源码那条重：**`.md` 是评审面最大的文件**，两个人的 `core.autocrlf` 不一致就会让一行的改动显示成整份文件重写。加它零风险——索引里全部已经是 `i/lf`
- **仍未补：提案 §6 的机制那一半需要表决**（三种景物变成有血量的可破坏障碍）。它卡着校验器第 19 条的另一半，以及 `地图与场景设计.md` 4.1「可破坏」一列 / 4.2.1 第三类实体映射 / 5.1 代价公式三处
- **仍未补：第 18 条缺 `resources` 的解禁波数字段**（一次 6.2 契约变更），**第 20 条等生成器**且它大概不该留在校验条目表里（产出是丢弃率，属生成器报告）
- **仍未补：第 9 条**（唯一一条与 2.1 无关的阻塞）要城区，而城区的来源随候选乙一起作废了
- **Python 侧 ctest 条目的中文输出在 ctest 里会乱码**（写的是 cp936 字节，Git Bash 按 UTF-8 解）。**这是既有性质、`CLAUDE.md` 已记，不是本次引入的**，但它削弱了所有 Python 守卫的报错价值——包括新加的这条配置陈旧守卫，而那条守卫的全部价值就是它的报错文本。要修得统一修（给这批条目设 `ENVIRONMENT PYTHONIOENCODING=utf-8`），而那要先定「哪个终端为准」，所以没有顺手做
- **「投降 / 主动结算」尚未设计。** 它是防死亡螺旋重述之后新出现的缺口——要防的是「慢性死亡」，而「快速结算」这半条没有实现手段
- **回放测试目前只能测「同一份输入流重放一致」，测不到机制。** 因为机制还不存在。它现在锁住的是格式、录制器与比对器本身；等 1c 落地，那批 ❌ 每实现一条都应当补一份回放夹具（存进 `tests/` 的一小段对局，而不是每次现录——现录出来的回放与当前代码同源，改坏了两边一起改，等于没测）
- **硬要求 1（AI 只见自己的迷雾）的落点是观测打包那一层，而那一层还没有代码。** `WorldView` 是 god 视角、实现的是不变量 2；这处**要靠评审而非编译器**把关，写进契约文档 §5.5 以免它被读成已经解决。若打包器写出来后发现 `FoggedView` 那层成本可接受，值得回来把它变成编译期的
- **`map_content_hash` 现在一律是全零**：`MapLoader` 刻意不算它（要先证明 C++ 的规范序列化与 `mapfile.py` 逐字节一致）。`WorldInit` 已经留好那个字段，所以那条跨语言比对测试写好之后接口不必改。**它现在有了第二个后果**：回放头里那 32 字节也是全零，于是「同名地图被改过」这条检查暂时形同虚设——`replay_verify` 的 `MapMismatch` 分支已经写好并有用例，只是真实数据填不进去
- ~~**`rts_core 交接说明.md` 第 4 节已被本 PR 的契约文档取代**，可随 #25 一并删。而 #25 本身已 CONFLICTING 且内容过期，需重写~~ **已处理：#25 整条关掉**（不是重写）。它的四个文件里只有 `地图与场景设计.md` 那 58 行没被取代，捞进 #51；其余逐文件在 #25 上交代了去处
- ~~**#51 与 #50 都还没人评审**~~ **两条都已合并**（#50 按「先合、评审改在后面提」，代价见上面那条）。**但「没人评审」这件事本身没有解决**，只是从 PR 挪到了 `main` 上
- **「调哪一支部队」定了之后，`SelectForce` 与 `MoveForce.force` 其中一条会变成死代码，该删。**两条并存意味着同一件事有两种字节表达，而回放里出现哪一种取决于是谁录的
- **本节这份清单已统筹进 issue，供全组同步**：**#60 是总览**（含逐人「等你」清单、各模块空缺、两条会白花时间的坑），三条需要独立动作的单开了——**#57**（1c，全项目唯一的关键路径，`rts_core/src/` 里机制一行都没有）、**#58**（提案 §6 的机制那一半要表决，从来没进过任何表决清单）、**#59**（Linux/GCC 服务器构建，我做不到）。**日志仍是上机记录，issue 才是待办的活账**——两处都写会漂移，所以此后逐条的进展记在 issue 上
- ~~**#58 提案 §6 的机制那一半要表决**~~ **已表决通过并落地**（#62 合入）。**#64 也已定案**（多态按兵种）、**#65 已撤回**、**#63 已合入**（GCC 修复 + 按兵种那层）
- ~~**`main` 前进到 `669a51e` 之后，无人在 Linux/GCC 上验过。**~~ **已验**：@Cody-ic 在 #67 上跑了 `aaca89d`（g++ 13.3.0，Release，0 error 0 warning），此后 `main` 前进的都只是注释与 markdown。**这一次是别人跑、验别人的 PR**，正是「频繁那一半是每个人的事」在兑现——那个 PR 动了 `rts_core/`，而我本机只有 Windows。
- **但那次验证顺带暴露一条永久的代价：`render/` 的 C++ 永远只有 MSVC 一套工具链把关。** 服务器跑 28 条、本机 35 条，差的 7 条全是 `render_*`，因为 `render/` 默认不配置——那是不变量 2、3 在构建侧的落地，**不是遗漏，所以不该「修」**。已把它与「回报那一半」（`game/` 无条件构建，于是 `map`/`scene`/`iso`/`names`/`utf8path` 在服务器上是跑的）一起写进 `CLAUDE.md` 与 `训练服务器环境.md` 第 5 节。**这条列在这里是因为它是一个要长期知道的风险，不是一个待办**
- **答辩讲稿里有 7 条现在还不成立**（`创新点与答辩讲法.md` 第 6 节那张表：3 条 ⏳ 依赖 1c、4 条 ⚠️ 依赖训练跑起来）。**其中两条最容易被误讲**：一是「战斗解算走 `can_engage`」——1c 一行没写，只能说「它是唯一的目标合法性判定入口」；二是「回放测试是主要防 bug 手段」——它现在验的是「输入 → 状态」的确定性，**还没有战斗解算可验**。答辩前必须照那张表逐条核，核不过的降级为「设计目标」，不要说成成果
- **非传递性环那一条是实验结果，不是设计承诺。** 矩阵跑出来若没有环，讲法必须改成「我们建了这个工具，目前观察到的是……」。这条现在就该记住，因为它是最容易在 PPT 里被写成结论的一条

---

## 2026-08-28（第 2 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | 骑兵重做：素材包无骑乘动作，新增 `pose`（在动作之上叠加骨骼角度）与 `attach`（真正的骨骼父子关系）两项能力，骑手不再双腿插进马腹、武器不再一奔跑就脱手；全部单位补 `idle` / `move` 动画状态，文件名与元数据改为逐状态；精度提至 256px/瓦片；补齐地形素材（5 种地面变体 + 岩壁 / 密林 / 水域 + 桥 + 景物）与 3 座新建筑；**订正全套尺寸比例**——尺寸约定曾以菱形屏幕宽度为 1 而非格边长，全体实体被放大 √2 倍，新增 `modules` 改为按 Kenney 模数统一定标；产出 `preview_map.py` 作为前端贴图与深度排序的可执行示例；订正资源模型三处（金币改由资源点产出、木材改为所有建筑的配料、波次进行中不禁止任何资源的使用）；删除「登墙阶梯」改用驻守槽位；提议地形枚举补 `Water` / `Bridge`；将 #13 的 2.1 实现手段列为两个候选待讨论；把素材与文档全面对齐 #13 契约。**实测训练服务器**（1×A800 80GB 空载、96 线程、503 GiB 内存、6.2 TB NVMe；gcc 13.3 / cmake 3.28.3 / python3 3.12.3 齐备，**缺 pip / PyTorch / conda / ninja**），据此订正「GPU 机时够不够」这个问法——**被串行化的是 CPU 不是 GPU**（A800 只用 14 MiB，而 `BatchedEnv` 吃的是那 96 线程），并把 `python3` 标成构建硬依赖（确定性守卫是一条 ctest，找不到 Python 会 `FATAL_ERROR`，下一台机器上看到它的人不该以为是配置写坏了）；容器重建导致 SSH 主机密钥变更，新旧两个指纹都记进文档供下一个人核对。**资源建筑三族重做**：起因是评审指出采石场与岩壁同为三堆灰石头而机制正好相反，改完发现**伐木场与木栅共用同一个模型**——同类错误第二次，于是写 `check_assets.py` 把它变成会红的检查（35 实体、67 处模型引用；「配件共用」只提示不失败，因为**一个总是红的检查等于没有检查**）。过程中连做废两版，教训是**先渲一张构件对照表再设计，不要照文件名猜**（`tower-slant-roof` 不是屋顶构件而是一整座小屋、`tower-square-base` 是没门没顶的塔地基）、以及**共用主体 + 小配件行不通，区分必须做在主体形体上**（等距下贴地配件根本看不见）。重写 `preview_map.py` 并拆成「契约」与「演示地图」两段，据此发现一处前端契约缺陷：**线性结构的 `d` 是走向不是朝向**（沿 `gi` 用 `SW`、沿 `gj` 用 `NW`，实测得出），照 4.2.1 写前端的人会得到一座墙线断开的城而不会怀疑朝向。评审 #20 与其复核 #24（逐条实测，含 Debug 侧那条他漏掉的配置），评审 #31 / #33 并论证**第 8 条不需要城区**（判据换成「存在集结点能不破墙走到 `keep`」，即第 4 条换一个障碍集）、**第 9 条的 min-cut 锚点必须是经济而非几何**（否则退化成 `keep` 周围一圈）、以及**第 16 条**（现有 15 条没有一条查那圈初始城墙存在，`initial_walls` 为空会空洞通过）。另：曾给训练截断波数 W 一个量级估计，**违反组内「数值一律待定」的明文规则并已公开收回**，由此提炼出一条建议进契约的规则——**回放格式承诺不得依赖任何待定数值**，字段宽度应选到「该数值的整个候选区间都不可能影响它」。**做掉 `README` 挂了两天的第 2 项：`rts_core` 的首次 Linux/GCC 构建**（Release + Debug 双配置 7/7 全绿、零警告、`-Werror` 全程开着），并顺带验掉三处此前只在 Windows 侧验过或标着「尚未实测」的（#24 的自足头 TU 在 Makefiles 下、构建类型守卫在 GCC 侧的四种场景、`find_package(Python3)`）。收获不是「过了」而是暴露的四处，**四处全都只在 Linux 侧看得见、且三处的症状是「不报错，只是悄悄变差」**：服务器到 `github.com` 的连通性**间歇性中断**（DNS 正常、TCP 不可达，而同期 `codeload` 与 `pypi` 都通），FetchContent 卡住——黑洞地址对照实验量出「不给 `TIMEOUT` 要等 133 秒才轮到第二个 URL，给 20 秒则 23 秒完成」，兜底 URL 的安全性由现成的 `URL_HASH` 保证（两份归档 SHA256 逐字节相同，不靠信任域名）；`.gitignore` 漏 `build-*/`（Makefiles 是**单配置**生成器，两个配置必须各占一个目录，而 VS 生成器是单目录多配置，所以这条在本地永远不会出现）；`.gitattributes` 漏 `*.sh`（CRLF 的 shebang 报 `bad interpreter: /usr/bin/env`，**而那个文件明明就在**）；Windows 上 `chmod +x` 不进 git 索引（`core.filemode` 默认 false），脚本到服务器上是 644。写了 `tools/server/{sync.sh,build.sh}`：**走 `git bundle` 而不在服务器上放凭据**——仓库私有而账号四人共用，放 token 等于把自己的凭据交给另外三个人；零状态实测全量 102 MB → 增量 **20 KB** → 幂等重跑零传输。自己写的脚本又跑出三个 bug，其中两个同样是静默变差（`--git-dir=~/x` 的 `~` 不展开；增量前置只看同名分支，于是同步任何特性分支都退化成全量；`while read` 漏掉最后一行，于是前一条修好了也没用）。**补齐攻击动画**（`attack` / 工匠的 `work`，476 张精灵）：提炼出 `impact_frame`——`CLAUDE.md` 有「弓手有攻击前摇」而**前摇时长是待定数值**，若前端按固定帧数播完，改前摇就要重渲精灵，所以给出「打出去的那一帧」让前端保持命中前的帧、把帧数与 tick 解耦（同上面那条回放规则的资产版）。这一轮最主要的教训是**「零动作」不等于「做不了动画」**：第一版把 `Shade` / `Phoenix` / `Ram` / `Tower` / `Flak` 五个都判成做不了，逐个追问后四个被做出来——`Ram` 与 `Flak` 有**可分离的子网格**（撞木、抛臂）、`Phoenix` 的俯冲**本该由姿态而非动作表达**、`Shade` 的可读性**可以由武器承担而不必动身体**，只有 `Tower` 真的没有可动部件。还有两处自己的错值得记：不死鸟的俯仰**符号搞反了**（渲出来是抬头爬升），而上一轮的验收判据「与平飞是否区分得开」**两种符号都通过**，正确判据要看头尾相对高度；以及差点推翻 `Flak` 一个**记录在案且正确**的旧决定（弩炮在这个尺寸下读不出「朝天」，而「仅对空」才是玩家要读的那一位）——教训是**改一个记录在案的决定之前，先把它的 `_note` 读完** | #19（含 `617e451` `9e9bc74` `243577d` `26df98c`）、#22、#27、#30、**#39**；提案 #23 待议、**#41 待评审**；#20 / #24 / #31 / #33 评审 |
| ArLiangz | 经 #25 接手 `rts_core`，任务范围是游戏核心部分：接口契约（`step()` 形状、观测通道注册表、动作/命令枚举、数据布局、回放格式）先行，战斗解算 / 寻路 / 视野 / 经济随后跟进（与交接说明第 1 节「后四块留作可认领」的划分不同，一并认领，异议请在 PR 里提）；按协作约定完成开工配置（`--local` 身份、`.claude/` 入 `.git/info/exclude`）；完成交接说明第 11 节第 1 步的构建验证——本机是 **VS 18（2026）/ MSVC 19.51** 而非 README 假设的 VS2022，实测生成器 `Visual Studio 18 2026` 可用并写回 README；Release 与 Debug 两配置均零警告、ctest 7/7 全绿（Debug 单验的原因见第 7 节第 8 条：它是 `assert` 唯一被编译的配置）；按验收标准第 3 条做破坏性测试——塞入 `unordered_map` 确认确定性守卫真的变红，还原后 `git diff` 为空、恢复全绿 | #25（接手方）、本 PR |
| zhxxx233 | **落下仓库第一行 C++ 代码**：`rts_core` 地基 —— 双工具链 CMake（MSVC `/permissive-` + GCC，第三方头标 SYSTEM 以免淹掉第一方 `/W4 /WX`）、自写 xoshiro128\*\* PRNG（不用 `<random>`：分布适配器的实现各标准库自定，同种子在 MSVC 与 libstdc++ 下数列不同；黄金向量由独立 Python 实现另算一遍，非从 C++ 输出反抄）、FNV-1a 状态哈希（选它是因为**顺序敏感**，顺序不敏感的哈希会对「内容相同、顺序不同」给出假绿灯，回放测试白做）、索引+代数的实体句柄（索引置高位使句柄序 = 下标序，`UnitId`/`BldId` 用 tag 做成不可互换）；把 CLAUDE.md 的三条确定性禁令变成构建时就红的 ctest 守卫，含自检以防检查器退化成永远绿；发现 ctest 按中文用例名注册会静默漏跑、改按 ASCII 标签注册；加 `.mailmap` 合并自己被拆成的两个作者名；三轮评审 #19 并指出采石场与岩壁撞视觉、日志「本次决定」自相矛盾、4.1/4.2 的「地形枚举 → 精灵」是一对多而文档写成一对一；论证出 2.1 候选甲有一个漏洞（只封死「围起来」、没封死「设防」——三面砌墙架塔即可让外部收入重新靠建筑守而非部队守，而这只是贵、不是不可能，落在「结构封死，而非数值劝退」上），据此两人一致改投候选乙；合入 #19 / #20 并做文档一致性结算（第 7 节按「已修/未修」重写、校验器表 12 → 15 条、新增 4.2.1「地形枚举 →（地砖，可选叠加物）」、6.3 的 `content_hash` 改为对规范化内容取、`.gitattributes`、`Watch` 视野不得覆盖集结区、标识符 ≤7 字符对纯渲染层素材键的豁免）；回应 #20 的事后复核，逐条独立复现其指出的五处缺陷（并把「构建类型默认给 Debug」定位到 CMake 自身的两行）、否掉其中一条修法建议（实测它在全新目录下会顶掉用户显式的 `-DCMAKE_BUILD_TYPE=Debug`）、另自查出两处同族问题（`types.hpp` 漏 `<compare>` 却被 Catch2 的包含顺序掩盖；ctest 标签清单是白名单、漏登记抓不到），全部修复并各加一道结构性守卫 | #20（`79fbb60`）、#21、#24、#19 五轮评审 |
| omg321-975 | 环境搭建与全仓通读（本地 MSVC 侧复现构建，7 条 ctest 全绿）；认领 `tools/map_gen/`；读第 6、8 节后提 **#29**，列出三处「实现绕不开、而文档未定义」的空缺——「城圈」缺可操作定义（6.2 说缺口由校验器从城圈推导，但地图里没有城圈这个东西，而 `initial_walls` 本身带缺口、集合不闭合）、**第 7 条里候选乙需要的 `layers.city` 与 `outposts` 字段在 6.2 中根本不存在**（此前只记了「判据写不出」，未注意到采用乙还要连带改一次跨模块契约，且「外部资源簇」也缺一条聚类定义）、第 14 条的距离度量依赖尚未定稿的静态建筑视野形状；读 #26 后补充 A5 若通过（§2+§3）的连带影响：城区不再是一张静态 mask 而要随 `Keep` 等级分级、`resources` 需加解禁字段、第 9 条的「正面总长」从标量变成一条曲线，并建议把议程里「§2 必须配 §3，否则城区终将包住外部簇」这条口头约定做成校验器第 16 条（否则日后改一次城区半径曲线或解禁波数表，龟缩会**静默**复活）；**实现时自证 #29 第一条我自己给的 flood-fill 定义不成立**——两种障碍取法都会漏：取 `Rock∪Water` 则从走廊口漏（走廊口本就是 `Plain`，见 2.2），再加上墙则从第 8 条强制要求的缺口漏；根因是一条不闭合的曲线不把平面分成内外两部分，任何 flood-fill 变体都绕不过去，除非引入「能填多大缺口」这类形态学参数，而那又是一个待标定数值（用数值旋钮去定义结构约束，方向就错了），据此把结论更正为「城区必须由地图显式给出」，并给 A4 添一条此前没人提过的论据：**候选乙的城区 mask 是校验器第 8、9 条能够实现的前提，候选甲那一侧根本没有可实现的第 8、9 条**，甲乙之争由此从强度层面的比较变成能力层面的差别；落 **#31** `tools/map_gen/` 地基——`mapfile.py` 作为全仓唯一的地图 I/O 入口（6.3 的哈希若在生成端与校验端各写一遍，失败时的症状恰是最难查的那种）、`grid.py` 的格子解码与连通性原语、67 条断言的自检注册为第 8 条 ctest；三处规范留白一律取保守侧并写明理由与重评点：坐标 `[x,y]` 且 `rows[y][x]`（从示例中四个集结点的位置反推，用 7×5 非方形图钉住，因为弄反在方形图上不报错、只是整张图转置）、`content_hash` 改取**规范序列化**而非规范化文本（LF + 去尾随空白挡不住缩进宽度／键顺序／非 ASCII 转义这三种同样改变字节、症状却一模一样的差异，建议 6.3 相应改写）、斜向不穿角取最严的一种（多报不漏报）；`derive_city_area()` 泄漏即抛 `CityAreaUndecidable` 而不返回一个看起来像样的错答案，`layers` 用白名单使 `no_bulid` 这类拼写错误不会被静默当成全 0 | #29、#31 |

**本次决定**：

- **资源模型三处订正**（此前的写法都是错的）：
  - **金币的主要来源改为资源点。** 三种资源的主要来源一律是地图上的资源点，对应采集建筑 `Quarry` / `Lumber` / `Mine`；领主堡垒只保留**极少量**金币作为兵力地板（**见下一条**，该产出的定位在本次上机内改过两轮）。副产品是「攻其必救」成为三向：拆采石场削防线**上限**、拆伐木场令防线**修不动也建不起**、拆金矿场削**兵力**
  - **木材改为所有永久建筑的配料**，不只是维修材料。只管维修的话开销被「维修成本显著低于重建」这条反死亡螺旋规则压住上限，木材永远花不完，"这波该攒哪种"不成其为决策；连带攻方拆伐木场也不疼
  - **波次进行中不禁止任何资源的使用。** 曾写过「石材与金币只能在建造阶段花」，方向错了——那是靠削掉另外两种的权限来凑木材的独占性。限制器应是**时间与暴露**：石材建造慢且工地就在杀伤区内、金币的兵要训练要行军、木材快但只能补形状不能扩形状。锁金币会直接毁掉战术层（缺口本该由机动部队去堵，不能补兵则战果在开打那一刻已定死）；锁石材也不必，建造耗时 + 工地暴露已解决「边打边补墙」的顾虑，还顺带给 AI 一个可学的目标（点杀施工中的工匠）
- **领主堡垒恢复产出金币，但定位改为「兵力地板」而非收入。** 本次先把它的产出删掉了，随后发现这样会开一个死亡螺旋的口子：金币管人力，而人力是唯一能在波次进行中即时投入的手段；若金币完全依赖城内 `Mine`，金矿一旦被点掉玩家**连补兵都做不到**，任何缺口都无法再堵，一波之内被推平、此后每波更惨。而防死亡螺旋原有的三条对策（维修便宜、废墟折扣、城内保底）**本身都要花资源，因此都救不了这一种**。堡垒的产出额只保证能补最基本的兵、不足以支撑军队，且它天然不可被摧毁（丢失即败），这是"地板"最理想的性质，用别的建筑都做不到。这条是**有意的例外**而非历史遗留——若将来想删，须先为金币断供另找一条护栏
- **不做「登墙阶梯」这类建筑。** 它没有取舍——总是要造、放哪儿都行，是杂活不是决策；且会把墙顶变成第二层可通行面，寻路要处理入口节点。改用**驻守槽位**：直接命令单位占据墙段、原地登上，代价由**机动性**承担（上去的单位被钉在那一段）。「城墙可站人」是机制，「先造楼梯才能上墙」是杂活，两者不必绑在一起
- **地形枚举补 `Water` 与 `Bridge`**（提议，已写入 `地图与场景设计.md` 4.1）。原本只做三种、把 `Water` 判为与 `Rock` 职能重复。按标志位摊开看，**通行 × 视野的四种组合里「挡路但不挡视野」这一格只有 `Water` 能填**，而它给出的质感岩壁做不到——看得见对岸集结却打不着，直到他上桥。这一格直接落在「情报划分：免费可见 vs 需侦查」那张表上：水送免费情报、岩壁否定情报。`Bridge` 是走廊表里隘口一类的极端（正面最窄且全程可见）；河的长条形状也不同于岩壁的团块——岩壁做局部隘口，河把地图切成进攻扇区。按 4.2 的位图设计两者接近零成本。已封两个口子：`Bridge` 不可建造、且不做「桥可摧毁」
- **`Road` 仍然不做，但理由说清了**：反对的是**加速效果**而非路面美术。渲染层可把部分 `Plain` 画成路面，仿真里它仍是 `Plain`。同理草地深浅变体、焦土痕迹都只是 `Plain` 的贴图，**不占枚举位**，渲染层按坐标哈希选变体即可
- **素材尺寸约定订正为「一律以格子边长为 1」。** 原先以「菱形的屏幕宽度」为 1，而格边长只有 0.707——等于把所有实体放大了 √2 倍，于是单位比地砖还大、大树比战马还矮。新增 `modules`：1 个模型单位 = k 格边长、不做归一化。**Kenney 整套件都建在 1.0 模数上且内部自洽**（墙 1.31、塔身 1.01、大树 1.85、攻城锤 2.09、地砖 1.00），统一乘一个系数即让全套件与格子及彼此的比例自动正确；逐个手调高度反而破坏这份自洽，那正是上一版失控的原因
- **动画状态成为前端契约的一部分**：文件名 `<标识符>_<状态>_<朝向>[_<帧号>].png`，元数据按 `(标识符, 状态)` 读 `canvas` / `ground_anchor` / `frames`。建筑等静态实体也有 `idle` 状态，前端不必对它们特殊处理；不同状态画布大小不同但锚点对齐同一地面点，故 `idle` ↔ `move` 切换不会跳动

- **`rts_core` 接口的定稿方式改为分两层，而不是一次定完。** 此前四次记录都把「接口未定稿」列为最高优先级阻塞项，但朴素地现在定完是过早的：花名册（+`Watch`/`Barrack`/`Mine`）与地形枚举（+`Water`/`Bridge`）本次都在改、2.1 的甲乙也没拍板，现在写进代码则 #19 合入当天就过期。#20 的切法是**只落对这些零依赖的那一层**（tick、实体句柄、PRNG、状态哈希、CMake），把花名册 / 地形 / 动作枚举与 `step()` 签名留给下一个 PR。这样四人立刻有可编译的基础，而契约那一轮评审只讨论接口形状
- **确定性从「文档里的禁令」变成「会红的测试」。** CLAUDE.md 禁无序容器影响仿真遍历、禁 C 库随机、禁以地址当 key，此前**违反了不会有任何反应**，直到某次回放对不上——而那时人会先怀疑仿真逻辑，最后才想到是遍历顺序变了。这是「结构封死，而非数值劝退」用在代码库上：不指望每个人都记得规则，让违反规则的代码过不了测试。误报时须在行末写 `// determinism-ok: <理由>`，**刻意要求写理由**，使放行可评审而非悄悄绕过
- **资源建筑的旗色承载「产哪一种资源」。** 起因是评审指出采石场与碎石、岩壁同为三堆灰石头、只有大小之差，而三者机制正好相反。加旗（人造 = 玩家建筑）之后目视又发现只加旗不够：按「交错杂居」，一簇内本就混有石/木/金，**相邻的异种资源建筑是默认布局而非巧合**，两座并排仍读成一大堆石头。于是让旗色再给出第二个位（石灰白 / 木绿 / 金黄）——一个元素同时承载两个位，且位于实体最高处、逆着地面色，是画面上最易读的位置。「攻其必救」是头号演示点，观众必须看出 AI 在打哪一种
- **2.1「外部资源点不可围」改投候选乙，但记录在案的理由换掉了。** 原先提乙的理由是「`no_build` 盖在看起来完全正常的平地上、容易被读成 bug」，评审判定这**是渲染问题而非结构问题**——建造模式画一层放置遮罩是同类游戏标准做法，且本次刚入库的 `PlainDirt` 地砖变体正好能把禁建走廊铺成土路让规则可见，所以这条救得回来、不足以支撑改动一条硬性约束，已降级为附带好处。**真正的理由是候选甲有一个洞：它只封死「围起来」，没封死「设防」**——玩家可在外部资源簇三面砌墙 + 架塔、留着禁建走廊口不管，于是外部收入又变成靠建筑守而不是靠部队守，而「部分资源点在墙外」保护的东西是**野战必须发生**。三面设防只是贵、不是不可能，而攻方预算超线性、守方上 RL 之后那就是会被梯度找出来的策略。候选乙把洞从「可以靠钱扩大的旋钮」变成「地图作者写死的常数」。**仍未定案**：它使墙类决策只剩「修旧」（「建新」在墙这一类上被削掉），这处范围收缩需小组共同认，见 2.1.2
- **任何静态建筑的视野都不得覆盖集结区**（`Watch` 瞭望塔最需注意，但对所有建筑成立）。整个侦查博弈的前提是**情报要花钱买、且会过时**；一座永久建筑若能照亮集结区，「这波要不要花钱侦查」就被**一次性买断**，`Scout`、`Wraith` 屏蔽、佯攻诱饵一起失效。这是把持续的经济交易换成一次性支出，**性质变了而非强度问题**，所以不能靠调小视野半径解决。已写成校验器的距离条件（第 8 节第 14 条），随地图尺寸标定自动成立
- **「该红却绿」被当成一类缺陷来处理，而不是七个独立 bug（#24）。** #20 合入后的事后复核
  找出五处，复核那份复核又掉出两处，七处的形态完全一样：**失败时本该有反应，实际一声不吭。**
  `cmake -B build` 在 Windows 上其实给的是 Debug（守卫判空放在 `project()` 之后，
  而平台默认值那时已写进缓存，于是那段代码只在 Linux 上有效）；注释点名的 C4365 从来没开
  （off-by-default，于是 MSVC 侧比 GCC 侧松，本地干净、服务器会直接失败）；确定性守卫扫到
  0 个文件返回绿；找不到 Python3 时守卫整条静默消失、ctest 从 5 条变 3 条一样全绿；
  `1'000` 这种数字分隔符会让守卫吃掉整行、同一行里的 `rand()` 查不出来；`types.hpp` 漏
  `<compare>` 而测试文件先 include 了 Catch2 把它顺带拉进来；ctest 标签清单是白名单，
  标签写错抓得到、**漏登记抓不到**。
  **共同的成因不是判断力而是验证路径**：#20 的六项验证走的是 VS 多配置生成器 + Python 齐备
  + 头文件跟着 Catch2 一起编的那条 happy path，七处缺陷全在它旁边。因此每一处的修法都不止
  「改对」，而是**再加一道会红的守卫**：构建类型改成跨 `project()` 判断并实测四种场景、
  0 文件与路径写错一律判失败并加 `WILL_FAIL` 的 ctest、`RTS_REQUIRE_DETERMINISM_GUARD`
  默认 ON 使「守卫不在」成为需主动声明的决定、给 `include/rts/` 下每个头各生成一个只含
  它自己的 TU（GLOB 而非手写名单）、再加一条不带过滤器的 `rts_tests_all`。
  这是「结构封死，而非数值劝退」用在验证路径上：不指望下一个人记得走全部路径
- **「一格画两张图」写进契约（4.2.1）。** 五种地形里只有 `Plain` 与 `Water` 是地砖（`kind:"tile"`、画布正好占满菱形、锚点在格心、不描边不投影），`Rock` / `Forest` / `Bridge` 都是立体物件、需要地砖底衬。**桥的底衬是 `Water` 而不是平地**，否则河在桥那一格视觉上是断的。叠加物件必须与单位混在同一深度序列排序（站在岩壁前的单位要遮住岩壁）。此前 4.2 写的是「渲染层读枚举决定画哪张地形精灵」——单数、一对一，照它写前端的人会去找 `Rock` 的地砖然后发现没有
- **多态的落点定在兵种，不在每个实体**（#64，晚间讨论）。`UnitBehavior` 基类 + 11 个兵种派生类，每个兵种一个静态存储期实例。**界线是实例的数量与生命周期，不是「用不用虚函数」**——`CLAUDE.md` 那节标题原写「无虚函数分派」，说法过窄（该节自己的澄清就写着虚函数分派本身是确定的），已随 #63 订正。同时**撤回 #65 那版逐单位继承**（823 行），依据是三条可检验的事实而非口味：宣称的收益（一张 `Unit → Archer` 的图）在落地版里没拿到（11 个兵种 10 个是模板别名）、多态面与被替换那版逐格相同、根因是**当时还没有可多态的行为**（会厚的那些全在 1c）。**队友在 Linux/GCC 上的调试与修复保留并已合入**，两件事恰在同一分支上，所以回撤不需要拆任何提交
- **「攻方 RL 逐单兵」与「多态按兵种」不冲突，而且前者是支持后者的。** 三条：RL 侧的分解本来就是「一套共享网络 + 兵种 one-hot」（已落成 `ObsSelfField::TypeOneHot`），与「基类通则 + 11 个兵种单例」是同一个分解；逐单位多态唯一能承载的「同兵种个体行为不同」被三条硬约束刻意封死（等级只改数值、无兵种专属动作、机制性克制等级无关），所以它拿到的是空盒子；而攻方是唯一每 4–8 tick 要把全部单位打包成张量的一侧，**逐单兵 = 打包频率最高 = 最需要连续数组**
- **兵种 behavior 那 11 个单例必须无状态，且这条已做成编译期的**（本 PR）。它们被所有单位共用，往里存一个字段（如「本波已出手几次」）立刻是跨单位隐式耦合，且进不进 `state_hash` 两条路都错（不进就是一份不被记录的仿真状态；进了就得给一个「按兵种」的东西找槽位顺序，而它不在任何实体数组里）。**检查是 `sizeof(派生) == sizeof(UnitBehavior)` 而不是 `std::is_empty_v`** ——我起初两处都写了后者，而 `is_empty_v` 对**任何**多态类恒为 false（虚函数带来 vptr），照那么写会得到一条**永远失败**的断言，是「该红却绿」的镜像。基类正好只有一个 vptr、无尾部填充，所以 `sizeof` 比对是紧的。破坏性验证：给 `ArcherBehavior` 加一个 `int` 字段 → `C2338` 并打出那条中文说明
- **改 `kWorldHashTag` 的 PR 要在标题里写明它占了哪个号。** 两个 PR 各自把 `World/1` 改成 `World/2` 时 git 不会拦（逐字相同的修改视为无冲突），实测冲突只落在标签**上面**那段注释里，标签本身自动合成
- **「创新点」的讲法单独落一份文档**（`创新点与答辩讲法.md`）。判据是：一条创新点讲准了要同时满足「说得出防住了什么具体的坏事」「说得出没有它会怎样」「当场能指出证据」，三条缺一条就还没讲准；而**主线是一句话——把不希望发生的事变成写不出来**，代码层的实例是数据布局、游戏设计层的实例是「结构封死而非数值劝退」，两者是同一个方法论的两次应用。文档里每条讲法都带状态标记（✅ 已在库 / ⏳ 依赖 1c / ⚠️ 依赖训练），并有一张答辩前必须复核的表——**讲一条当场被追问穿的创新点比不讲它更糟**

**遗留问题**：

- **`rts_core` 代码从未在 Linux/GCC 上编译过**，本机无 GCC。CLAUDE.md 明写「要尽早、频繁地在服务器上跑构建，不要攒到临近答辩才第一次编译 Linux 目标」，而现在已经有 C++ 代码却一次都没编过。**这是当前最该有人认领的一项**，已进 `README.md` 未认领工作表。#24 顺带消掉了两个候选失败点（MSVC 补开 C4365 后与 GCC 的 `-Wsign-conversion` 对称、`types.hpp` 补上 `<compare>`），但**一点也没降低这条的紧迫性**——两个都是靠推理找出来的，真正有几个只有编过才知道，而且越往后拖、堆在上面的代码越多
- ~~地图文件的换行会让 `content_hash` 跨平台不一致~~ **#21 已处理，两条都做了**：`地图与场景设计.md` 6.3 改为对规范化后的内容（LF、去尾随空白）取哈希（主，不依赖每个人的 git 配置），并加 `.gitattributes` 对数据与源码文件强制 `eol=lf`（副，零成本第二道防线，顺带解决 review 时整片显示为改动）。留下的规矩：**新增会被程序读取或参与哈希的文本格式时，要同时在 `.gitattributes` 里登记**
- **#13 遗留的四条精灵问题，三条已修、一条未修且被扩大**：
  - ~~`tile` 字段尺度差 2√2 倍~~ **已修**：现为 `[px_per_tile, px_per_tile/2]` = `[256, 128]`
  - ~~尚无任何地形精灵~~ **已补**：13 条地形条目（地面变体 / 障碍物 / 景物），标识符已对齐 4.1 枚举
  - ~~城墙水平占地约 0.60 瓦片、连续墙段留 40% 缝隙~~ **已修**：`modules: 1` 使 `wall-narrow` 沿长边正好占 1 格，相邻墙段无缝
  - **两条路线的元数据 schema 与文件名分叉——未修，且本次扩大了**。3D 路线本次又新增 `states` 与 `kind` 两层结构，占位路线 `gen.py` 仍是「全局单一 canvas + 全局 ground_anchor + 无状态」的老 schema，产物也仍是 `<标识符>_<朝向>.png`。占位路线的价值本是"无 Blender 也能跑前端"，**契约不通用则这个价值已实际失效**。要么让 `gen.py` 跟上（含地形与两种状态），要么明确宣布放弃占位路线、由已入库的 300 张成品承担该职责——**倾向后者**，但需小组确认
- **`地图与场景设计.md` 2.1 仍未定案，但已收窄到只剩一个问题。** 两人一致倾向候选乙（`buildable` 由城区定义 + 少量前哨点，`前哨点数 < 外部资源簇数`），理由见「本次决定」。已达成一致的：前哨点只许建采集与防御建筑、不许建墙与门；`Fence` 同样限制在城区与前哨内（理由是结构性的——木栅可在波次中即时放置且极廉价，能任意放就等于给了我们要封的那件事一个最廉价版本）；前哨点数量属待定数值。**剩下的唯一障碍是范围收缩**：城区固定 ⇒ 初始残破城圈必须落在城区边界上 ⇒ 「扩张防线」整个消失、墙类决策只剩「修旧」，而 CLAUDE.md 把建造阶段决策写成三方的「修旧 / 建新 / 造兵」。这条不该由两个人拍。另外若采用乙，校验器还缺一条「前哨点上摆不下足以围住该簇的塔」，判据要等前哨点与簇的形状定了才写得出
- ~~金币保底的单点脆弱性~~ **本次已处理**：领主堡垒恢复产出极少量金币作为兵力地板，见「本次决定」。数额属待定数值
- **本日志缺一名成员的行。** 仓库协作者为 4 人（Cody-ic / ArLiangz / zhxxx233 / omg321-975），而记录模板与首次记录只有 3 行，**omg321-975 此前从未出现在日志中**。本次已补上空行。日志是答辩「人员分工」页的直接依据，缺行等于该成员在个人成绩材料里不存在
- **ArLiangz 与 omg321-975 仍无任何提交，两人的日志行均为「待本人补充」。** 这两行只能本人填。当前可独立开工且不依赖 `rts_core` 的有：`tools/map_gen/`（生成器 + 校验器）、占位路线的 schema 对齐、前端的地图渲染（素材与契约已就绪）
- **未引用模型数量已变化**：`assets.json` 现引用 **38 / 103** 个模型（`9f8a65a` 给采石场加了 `stairs-stone.glb`），未引用 65 个（16.2 MB / 模型文件共 32.4 MB）。已逐个核对引用路径全部存在。打包时按 `assets.json` 过滤的要求不变，已写入 `tools/sprite_gen/README.md`
- 上次记录中「19 实体 × 4 朝向 × idle/move = 236 张」等表述已过期，现为 **35 实体、300 张**（含地形）
- 上次记录的两条本次已翻转，不再成立：**「仓库仍无 C++ 代码与 CMake 配置」**（#20 已落地基与双工具链 CMake）、**「`rts_core` 接口未定稿是后续一切工作的前置」**（改为分两层，与花名册/地形零依赖的那层已定死，见「本次决定」）。**接口契约那一层仍未定稿**——`step()` 签名、observation 通道、动作枚举、单位/建筑数据布局要等 #19 合入、花名册与地形枚举稳定后另开 PR
- 其余遗留问题（待向老师确认类设计、#15 / #16 两份提案待议、组长姓名与中文题目）**均未变化**，见上次记录

**本次结算**：

| PR | 内容 | 状态 |
|---|---|---|
| #19 | 地形枚举补 `Water`/`Bridge`、资源模型三处订正、全套尺寸比例、资源建筑视觉分离 | 已合入 |
| #20 | `rts_core` 地基：双工具链 CMake、PRNG、状态哈希、确定性守卫、`.mailmap` | 已合入 |
| #21 | 文档一致性结算（#19 + #20 落地后的过期内容） | 已合入 |
| #24 | #20 复核的七处「该红却绿」修复 + 三道新守卫 | 随本 PR 合入 |
| #15 | 守方 AI 与协同演化 | **提案仍待议**，6 个需拍板问题 |
| #16 | 演示与可视化清单 | **提案仍待议**，5 个需拍板问题 |

仓库现有根目录 6 份文档 + 精灵流水线（Python）+ 确定性检查脚本 + **654 行 C++**
（7 个文件）+ 302 行 CMake，**7 条 ctest 全绿**。另有两份提案文档在未合并的分支上（#15 / #16）。
下一件事是 `rts_core` 的**接口契约**：`step()` 签名、observation 通道注册表、
动作/命令分级、`WorldView`、回放格式。花名册与地形枚举已随 #19 稳定，不再阻塞它。

**当前最该有人认领的三项**（都不等接口定稿）：Linux/GCC 侧首次构建、
`tools/map_gen/`、占位产图路线的去留。

---

## 2026-08-27（第 1 次上机）

| 成员 | 本次进展 | 相关提交 / PR |
|---|---|---|
| Cody-ic | 确立项目选题与三层隔离架构；放宽第三方库约束，将关键不变量从"语言纯净度"重新定义为"热路径与确定性"；修正开发平台；产出兵种建筑花名册、克制二部图与侦查系统；分析交付要求对架构与排期的约束；评估后放弃 Unity，前端改为 C++ 图形库自绘；澄清内存布局与面向对象是正交问题，将实体存储约束的理由改挂在确定性上；搭建精灵生成流水线（程序化占位 + CC0 素材经 Blender 统一预渲染），并核实各素材站许可条款；据三轮评审完成 19 实体 × 4 朝向精灵成品并入库，建立前端尺寸契约 `_sprite_meta.json`，攻守双方视觉分离；全部单位补 idle/move 移动动画，蔽空弩楼与镇野箭楼剪影分离；据评审论证幽影窥使必须是地面单位并改素材落地 | `4c1de02` `f3ca7e6` `cd9d1b6` `d024ffc` #3 #4 #5 #7 #9 |
| ArLiangz | 待本人补充 | |
| zhxxx233 | 修正开发环境约定（本地纯 Windows/MSVC，不使用 WSL2）；补充老师要求的最终交付项清单并分析其对设计的约束；清理文档中登墙机制、单位计数、资源种类三处不一致；建立本日志；修正确定性论证中"堆分配即不确定"的因果错误；订正"交付物必须是纯 C++"这一过度推论；调研 Godot GDExtension / rbfx / Panda3D 等引擎方案后维持现有技术栈，并将"不使用游戏引擎"由硬性规则降级为带理由与重评判据的工程选择；三轮评审 #9 精灵流水线：实测出后处理脚本不幂等、锚点契约缺失、攻方剪影辨识度不足等问题，并订正自己上一轮不死鸟是唯一空中单位的错误判断；产出地图与场景设计规范（地形枚举、分层数据格式、走廊分类、寻路把墙当高代价可通行、校验器条目、训练图与演示图分工），并核实精灵侧四处影响地图渲染的契约问题（tile 尺度差 2√2 倍、两条路线元数据 schema 分叉、地形精灵缺失、城墙占地不足一格）；建立 README 作为新成员入场文档；结算全部待合并 PR；发现「训练时守方是谁」此前从未定义并产出守方 AI 与协同演化方案（分层镜像攻方、宏观层动作因子化到地图槽位、对手池、行为克隆做人类风格锚定、三个坑与排期）；产出演示与可视化清单（先定判据再排序，并明确列出不做的）；订正自己把集结点数量写成"定为 4"这一违反组内数值待定要求的错误，并把该要求写成 CLAUDE.md 的明文规则 | #1 #2 #6 #8 #10 #13 #14 #17、#15 #16（提案待议）、#9 五轮评审 |

**本次决定**：

- **游戏形态**：不对称波次生存，玩家为守方，RL 驱动攻方，无尽模式
- **架构**：`rts_core` / `render` / `game` / `bindings` / `train` / `tools` / `tests` 三层隔离，训练经 pybind11 in-process 调用 C++ 仿真
- **平台**：全员 Windows + MSVC 开发，训练在 Linux GPU 服务器；不使用 WSL2
- **前端**：**当前不引入游戏引擎**（Unity 方案当日提出后即放弃）——此为工程选择而非硬性规则，理由与重新评估的判据已写入 CLAUDE.md。`render/` 直接链接 `rts_core`，用 raylib + Dear ImGui 自绘，无 C ABI 与跨语言胶水。前端即正式交付界面，工作量按 5–8 人日估（**#16 指出该预算不含演示与可视化功能，需重估**）
- **实体存储**：扁平数组 + 整型 ID，禁止逐单位堆分配与虚函数分派；AoS / SoA 属实现细节不作规定。理由是从结构上消除"拿地址当 key"这类破坏确定性回放的写法
- **交付语言**：核心逻辑为 C++，但**不要求全栈纯 C++**。推理是否随包分发 Python 运行时属部署方式选择，第 3 周再定
- **命名**：单位与建筑花名册定稿，标识符规范为英文、≤7 字符、攻守不加前缀
- **资源**：守方三种资源（石材 / 木材 / 金币），各对应一条决策轴
- **地图与场景**：地图是 grid，地形只做三种（`Plain` / `Rock` / `Forest`），**不做地形高度**（高度只由城墙提供）。**每个集结点对应一条性质不同的走廊**（候选：开阔平原 / 隘口 / 林地 / 经济侧），否则选集结点没有可学信号——这是结构约束；**集结点数量本身属待定数值**，它等于走廊种类数，两者一起标定。**天然岩壁承担大部分周长**，只有走廊口需人工设防——这既让单位数上限下攻击密度足够，也实现了"防御建筑无法覆盖所有墙段"。地图**初始给一圈残破城墙并留缺口**，使"AI 是否利用已有缺口"这一核心指标第 1 波即可测。**外部资源点周围划禁建区并连通到地图边界**，从结构上封死"把外部矿场围进城"的龟缩解，而非靠墙造价劝退。尺寸不锁数字、只锁"`Ram` 行军占 episode 10–20%"。规范见 `地图与场景设计.md`
- **幽影窥使是地面单位，不死鸟是唯一空中单位**（结构性决定）。若幽影窥使会飞，防空建筑就多了"否定侦查"这一每波都稳定生效的用途，玩家无脑造 AA 即可——AA 的机会成本不再是两难（而这是本作智斗最可读的载体）、"空军价值 = 迫使玩家投入的 AA 资源量"这一评估指标失效、且侦查单位必须便宜到可消耗而空中单位必须高价稀有，两者正相反。视觉上幽影窥使贴地掠行、投影紧贴脚下，不死鸟高悬、投影远在下方，玩家靠间距判断能否用防空塔招呼
- **训练时守方也由 RL 控制**（方向已定，具体方案见 #15 待议）。此前文档从未规定训练时的守方是谁——若是固定脚本，攻方会过拟合到它的破绽，而头号指标"AI 是否发现并利用已有缺口"随之可疑：是学会了找缺口，还是背下了脚本总在哪留缺口。正式游玩时守方仍是人类玩家不变
- **凡关系到平衡性与玩家体验的数值一律保持待定**，等框架完善后用成本产出矩阵与自动标定一次性倒推。已写成 CLAUDE.md「关于数值」一节的明文规则，并给出两条守则：区分「结构约束」与「数值」；举例时明确标注占位值
- **美术路线**：不靠引擎提升观感，走**精灵预渲染**——3D 模型经统一相机与灯光渲成等距图，风格差异在渲染阶段压平（混用 2D 精灵做不到这点）。**只用 CC0 素材**，因交付要求源代码含输入文件、素材须随仓库提交。开发期先用程序化占位图解锁前端。精灵成品（19 实体 × 4 朝向 × idle/move，共 236 张）与尺寸契约 `_sprite_meta.json` 一并入库，避免每人各装一次 Blender；画布逐实体适配、世界→像素缩放固定，故相对大小始终正确

**遗留问题**：

- **待向老师确认（应在第 2 周结束前）**：本项目的类设计（系统层为类、实体为扁平数组）是否满足「系统设计报告」中"类对象设计"的要求。仅当老师明确期待"Unit 基类 + Archer 派生"这类实体继承体系时才存在真正冲突。原本还有一项"C++ 核心 + C# 前端是否满足课程要求"，随放弃 Unity 已作废
- `rts_core` 公开接口（`step()` 签名、observation 通道定义、动作枚举、数据布局）尚未定稿，是四人并行开发的关键契约，应最优先确定
- **凡关系到平衡性与玩家体验的数值一律保持待定**，等框架完善后用成本产出矩阵与自动标定一次性倒推，不逐个拍脑袋。含地图边长、城区半径、走廊口宽度与数量、**集结点数量**等。此条已写入 CLAUDE.md「关于数值」一节——地图规范曾把集结点数量写成"定为 4"，属把平衡旋钮当成结构结论，已订正
- **精灵元数据的不一致会影响地图渲染**（详见 `地图与场景设计.md` 第 7 节）：3D 路线的 `tile` 字段尺度差 2√2 倍（比例对、尺度错，肉眼看不出来，前端会把单位画大 2.83 倍）；两条路线的元数据 schema 与 PNG 文件名**都已分叉**——3D 路线的结构在三次提交里改了三次（全局 → 逐实体 → 逐实体逐状态 `states`），占位路线一次没跟。占位路线的价值是"无 Blender 也能跑前端"，契约不通用则这个价值实际已经没有了
- **尚无任何地形精灵**：19 个登记实体全是单位与建筑，地图目前无从渲染。`ground.glb` / `rocks-*.glb` / `tree-*.glb` 已在素材包内，加 3 条 `assets.json` 条目即可，不需改脚本
- **城墙精灵水平占地约 0.60 瓦片**（按取景公式反解，已用高度回算验证），仿真里占整格，连续墙段会留约 40% 缝隙。沿格子连续铺开的建筑应按占地宽度归一化，而非按高度
- **两份提案待小组讨论**：#15 守方 AI 与协同演化（6 个待拍板问题，其中"守方宏观层前期是否先写死脚本"与"GPU 机时够不够"影响排期）；#16 演示与可视化清单（5 个待拍板问题，其中"前端预算重估到多少人日"需要负责 `render/` 的人一起看）。两者都会实质改变范围，**讨论定了才好排后续的活**
- **ArLiangz 的日志行仍为「待本人补充」，且暂无提交记录。** 本日志是答辩「人员分工」页的直接依据，而该页是个人成绩的重要依据——这一行只能本人填，他人代填既不准确也失去意义。已建立 `README.md` 作为入场文档（导航、协作约定、环境准备、未认领工作清单），供其快速同步进度
- 组长姓名与中文题目定名尚未确定，四份 `.doc` 交付文档的文件名依赖此项
- 仓库尚未搭建代码骨架与 CMake 配置。已合入的 #9 是 Python 工具链，#13 / #14 / #17 是设计文档与协作约定，**仓库至今没有一行 C++**。现在就能独立开工、且不依赖 `rts_core` 的两块是 `tools/map_gen/` 与精灵元数据的四处修正
- **最终交付打包需按 `assets.json` 过滤 `models/`**：现有 103 个模型文件中仅 28 个被引用，未引用的 75 个（15.6 MB）不属于"输入文件"。仓库保留整包便于换模型，但交付包应精简。`交付要求的工程影响.md` 第 6 节尚未覆盖素材筛选

**本次结算**：

| PR | 内容 | 状态 |
|---|---|---|
| #9 | 精灵流水线（19 实体 × 4 朝向 × idle/move = 236 张） | 已合入 |
| #13 | 地图与场景设计规范 | 已合入 |
| #14 | README 入场文档 + 日志结算 | 已合入 |
| #17 | 集结点数量改回待定 + 「关于数值」明文规则 | 已合入 |
| #15 | 守方 AI 与协同演化 | **提案待议**，含 6 个需拍板问题 |
| #16 | 演示与可视化清单 | **提案待议**，含 5 个需拍板问题 |

仓库共 6 份文档 + 1 条 Python 工具链，**仍无任何 C++ 代码与 CMake 配置**。
`rts_core` 公开接口未定稿，是后续一切工作的前置。

现在就能独立开工、且不依赖 `rts_core` 的有两块：`tools/map_gen/`（生成器 + 校验器，
12 条校验里 7 条不含数值）与精灵元数据的四处修正。
