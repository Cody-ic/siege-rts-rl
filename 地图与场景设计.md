# 地图与场景设计

> **本文是地图数据的规范，不是实现。**
>
> 地图格式是四人并行开发的**第二个跨模块契约**（第一个是 `rts_core` 公开接口）：
> `rts_core` 要吃它、`render/` 要画它、`train/` 要按波次采样它、`tools/` 要生成与校验它。
> 改动本文的字段定义等于同时改动四个模块，请先讨论再改。
>
> **本文中没有任何已标定的数值。**
>
> 出现的一切数字（地图边长、城区半径、行军时间占比、**集结点数量**、
> 校验器的区间上下界、JSON 示例里的坐标与尺寸）都只用于**验证量级可行**，一律待定，
> 须等基本机制完善、速度与造价标定之后再定，届时按本文的结构约束倒推。
> 引用本文时**不要把任何数字当作已定值**。
>
> 真正锁定的只有各节标为「硬性」的**结构约束**——它们是机制层面的，
> 不随数值标定变化，也正是数值标定时必须满足的边界条件。

## 0. 为什么地图应当先做，以及它和 `rts_core` 接口的关系

CLAUDE.md 把 `rts_core` 公开接口列为最优先项。地图不与之竞争，而是**从数据侧逼近它**：

观测张量的通道定义里有「墙、门、可通行地形、目标方向场」，这四项里有三项是地图数据。
地形枚举、可通行 / 可建造标志、资源点类型一旦定下来，`step()` 签名剩下的自由度就小得多。
所以先定地图不是绕开最优先项，是把它拆出一块能独立定稿的部分。

反过来也成立：**地图规范不能等数值标定**。下面第 1 节列的那批要求只能靠几何满足或违背，
几何一旦定错，再怎么调血量伤害都救不回来。

## 1. 地图承担的设计约束

CLAUDE.md 里有一批要求，其成立与否**由地图几何决定，数值改不动**：

| 文档中的要求 | 实际是什么 |
|---|---|
| 防御建筑造价高到无法覆盖所有墙段 | 需设防正面总长 / 经济产能之比 |
| 必须存在拐点使龟缩不再可行 | 城内资源点数量决定保底收入的天花板 |
| 侦查有价值 | 集结点必须在玩家初始视野之外 |
| 冲锋伤害 ∝ 助跑距离；巷战反克枪阵 | 需要开阔地**与**狭窄地形同时存在 |
| `Tower` 齐射克制「步兵一拥而上啃墙」 | 需要隘口把攻方挤成密集队列 |
| 空军点掉偏远矿场比撞墙划算 | 外部资源点必须远到 `Flak` 覆盖不过来 |
| `Ram` 慢、必须走到墙下，是头号集火目标 | 集结点到城墙的行军距离决定它的暴露时间 |
| 单波单位数硬上限 20–40 | 需设防正面必须窄到让 40 个单位形成压力 |

最后一条最容易被忽略：一座周长 150 格的城对 40 个攻方单位而言太稀，攻防会退化成散兵游勇。
解决办法见 2.2 —— **城圈收在方环上（周长 8R），攻击密度由城区半径调节**。

## 2. 硬性结构约束

以下四条不是待平衡的数值，改动它们等于改动游戏的核心循环。

### 2.1 外部资源点分布形态（原「结构性不可围墙」，2026-09-01 移除）

**2026-09-01 前，本节标题是「外部资源点必须结构性地不可围墙」**：若玩家可
自由建墙，最优解是把外部资源点也圈进城内，龟缩退化解就复活，而「部分资源
点在墙外」是 CLAUDE.md 明确列的两个不能砍的机制之一。「墙太长、造价太高」
是数值劝退，不够用——攻方预算超线性增长、玩家经济同步增长，某个波次一定
会变得划算，而 RL 一定会找到那个波次；按「结构封死，而非数值劝退」准则，
必须在结构上禁止。

承载这条结构约束的手段是**森林带**（`无尽模式与地形分层.md` §4，随 #23
定案；此前另有候选甲/候选乙两个方案，权衡后否掉）：每个外部资源簇周围铺
一条 `Forest` 4 连通到地图边界的通道，`Forest` 不可建造也不可破坏，任何
想围住资源点的闭合墙线都必须跨过它——跨过就得在森林上建墙，建不了 ⇒
围不起来。校验器第 7 条是这条约束的可执行判据。

**2026-09-01 团队决定移除这条结构约束**，理由是纯视觉：承载它的森林带
即便经过两轮加固（#117 加折步、#121 因带子被拉长后再加密折步、加大偏移
上限）实测在几十格长的距离上侧向偏移仍只有 3–5 格——不管怎么调折步参数，
玩家看到的基本还是一条直线，试玩反馈直接指出了这一点。团队认为一条看起来
还是直线的森林带，换来的反龟缩保护没有价值到值得保留，情愿放弃这条防线
换取更自然、可控的城外散布效果（`scatter_wild_terrain` 的纯装饰性小撮团块，
见 §2.2）。

**认下的代价，不是没想到的副作用**：玩家现在理论上可以砌一整圈墙把外部
资源点圈进城内，本节原文论证的「龟缩退化解复活」风险失去了结构性屏障，
退化为**纯数值劝退**（墙的造价与长度）——而本节原文正是在论证「数值劝退
不够用」。这是一次明知有此风险仍主动接受的取舍：一条实现两轮仍看起来像
直线的森林带，换来的保护被判定为不值得留。如果将来试玩发现龟缩重新变得
可行，回头看的应当是这一段。

**保留的部分：**

- 「部分资源点在墙外」这条**分布决策**本身不受影响——外部资源簇依旧存在，
  数量、簇内点数与位置的生成逻辑见 §2.2，与本节讨论的「能不能被围住」是
  两件事，没有一起被移除
- `Forest`/`Rock` 仍然不可破坏、玩家没有伐林/平地的手段——理由不再是「唯一
  承载结构约束」（那条约束已经不存在），而是下面这条独立收益仍然成立：
  森林挡视野是**双向**的，玩家也看不见有人来摸自己的外围矿场，外围经济的
  防守因此仍是「**要么驻兵，要么认损**」而不是「架几座塔覆盖住」——这正是
  「部分资源点在墙外」真正要保护的东西（野战必须发生），也给 `Ranger`
  （出城的执行手段）与 `Scout`（外围侦查）各自一个明确职能。这条论证本身
  不依赖森林带是否承载结构约束，是森林地形本身的性质，原样保留

**已删除、不再适用**：候选甲/候选乙的详细比较、森林带与校验器第 10 条的
对立分析——两者都以「森林带是强制结构约束」为前提，前提没了，这些论证
本身也失去意义，不再复述；查历史版本可见完整原文。

### 2.2 城圈 = 一整圈城墙实体；城外地形随机散布（2026-08-31 重写）

**本节原文是「每个集结点对应一条性质不同的走廊」，2026-08-31 组长拍板取缔
「走廊」概念，整节作废重写如下。** 作废理由：走廊性质把城外地形成型为四条
规则画的通道，而实战对局要的是每次不一样的布局——城外地形改为**随机散布**
（森林斑块、岩壁团块、资源簇，参考 AoE4 1v1 对战地图的原理），宏观层
「选集结点」的信号来源从「走廊性质标签」改为「地形与资源的**实际分布**」。

**城圈是方环上的一整圈 `initial_walls` 实体，不是 `Rock` 地形。**
旧文「天然岩壁承担大部分周长、只有走廊口需要人工墙」是**错误表述**，
生成器曾照它实现过一版（`Rock` 环 + 口上人工墙），已随本次一并改正。
`Rock` 降级为城外随机散布的团块。

结构上定死的形状（其余全是待定数值）：

- **一对相对方向的城门**（随机挑一对相对边，各一座 `Gate`，恒满血——
  城门是结构上的既定薄弱点；Wall 侧 2026-09-01 起也恒满血，见 2.3）
  + **1–2 个设计缺口**（该格不摆墙，2.3 那条「初始城圈留缺口」的落点）
- **集结点 = 固定边缘候选点**，随机抽 3–4 条边（占位）。数量与「走廊种类数」
  不再有任何绑定——「集结点数量 = 走廊种类数」那条旧结构约束随走廊概念一起作废
  （校验器第 3 条废除、编号留空）
- **城内**：两片 3 格 L 形森林；资源构成**恰好 1 石 + 1 金 + 1 木**（2026-09-01
  第二次调整——试玩反馈「城墙内资源点太多」，从「2 石 2 金 1–2 木」收紧；
  三种各配一座采石场/金矿场/伐木场，精确踩对应资源点）；预置建筑
  （箭塔 2–3 座贴城门/缺口内侧、兵营 1–2 座）；
  **堡垒周围 3 格净空**（demo 的预置内容占用 keep 邻格，地图内容不得与之撞车）
- **城外资源簇**：7–8 簇、每簇 4–5 点、簇内点间距 ≥2、每簇 ≥2 种类型、
  **全局城外金 ≥2**、簇距随离城距离递增但**上界与城区半径挂钩、不随地图边长
  一起涨**（`outer_cluster_span`，2026-09-01 第二次调整——试玩反馈「城外资源点
  太少且离城墙太远」；旧算法把簇距跨度绑在地图边长上，144 格地图下最远一簇
  能摆到贴地图边缘）。**点直接落在簇心切比雪夫半径内的空闲 Plain 格上**
  （`_OUTER_CLUSTER_RADIUS`，2026-09-01 第三次调整）——**不再要求每簇有一条
  通到地图边界的 `Forest` 带**：那是 2.1 原先「不可围墙」结构约束的实现手段，
  即便两轮加固（#117 加折步、#121 加密折步）仍在长距离上明显读成直线，团队
  决定移除这条约束、换取更自然的地形，理由与取舍见 2.1 的订正
- **城外散布**：森林斑块与 `Rock` 团块随机撒（避开墙/门八邻与集结点八邻，
  团块是 4 连通随机游走集——散点抽样会被第 23 条全否）。
  **2026-09-01 美观化**：团块改为**圆形斑块**（半径随目标格数取 isqrt、
  格被选中的概率随到圆心距离平方衰减、须贴着已长出的格生长），
  且间距拉开——试玩反馈「一坨一坨贴在一起」的形态正是旧随机游走画的。
  **2026-09-01 第三次调整（恢复 #117 定版）**：「大散居、小聚居」——
  森林每簇 3–5 格、岩壁 3–6 格，簇数上调（森林 24–36 簇、岩壁 12–20 簇），
  各簇中心有最小间距且不得贴着已有 Forest/Rock 生长（含先落地的团块），
  形态是各处一小撮而不是少数大团；可破坏障碍同样只在城外、按同类 1–3 个
  组成彼此分离的小团。（#117 与 #119 曾就「小撮 vs 大团」给出相反参数，
  合 #119 时误以 #119 整体覆盖 #117，本次按 #117 恢复）
- **水域**：湖泊（圆瓣团块）+ 一条边到边的河（2–3 格宽、随机游走主干），
  河上架 `Bridge`（垂直贯穿整段水宽的一排，两岸必须是岸）。
  **2026-09-01 前这是刻意留白**——当时担心画错一格切断集结点通路会让
  丢弃率飙升而原因难查；解法是**逐水体落地后自查连通性，失败整片撤销**
  （keep 可达性 + 无 Plain 孤岛），那条担心因此不再成立

顺带一提量级：候选点数落在个位数时，分兵组合仍是 bandit 尺度
（n 个点最多 2ⁿ−1 种非空子集），不构成动作空间膨胀。这条对整个候选区间都成立，
所以数量怎么定都不会在这里出问题。

### 2.3 地图初始给一圈城墙，并留缺口

**给初始城圈**的三个理由：

1. 「城内 / 城外」由地图定义，保底收入才稳定；否则它取决于玩家怎么画墙，
   保底收入无法作为难度曲线的地板（而 CLAUDE.md 把它列为防死亡螺旋的主要手段之一）。
2. 4 周内做不出好用的建墙 UX，第 1 波让玩家从零画一圈墙是劝退设计。
3. 答辩演示时画面第一眼就像一座城。

**留缺口**还有一个额外好处：CLAUDE.md 的核心评估指标之一是「AI 是否发现并利用已有缺口」，
对标产品 *Diplomacy is Not an Option* 正是被批评「敌人无视已有缺口、反复撞击加固过的城墙」。
地图初始就带缺口的话，**这个指标第 1 波就能测**，不必等玩家自己打出缺口。

初始墙段允许带残血（`hp_frac < 1`），使第 1 波就存在「修旧 / 建新 / 造兵」的三方决策。
这是格式与生成器的能力，不是每张地图的强制要求——**且当前所有产物都是满血开局**：
两张手写演示地图（`reference_border_keep_01.json`、`demo_skirmish.json`）2026-08-31 试玩反馈后
改为初始满血（当时守方单位血量偏低、城墙偏脆，叠加残血墙后开局手感过难）；**生成池图也在
2026-09-01 跟进**（同一反馈的第二次试玩：「新开局城墙仍然不是满血，血量各有不同」——此前
只修了两张手写图，池图仍按 `wall_hp_frac_range` 撒残血；现已把该区间钉死 [1.0, 1.0]，
理由与训练侧影响见 `thresholds.json` 的 `_note_hp`）。「修旧」只在开局不可用（后续
波次战斗损耗后仍会出现），不影响「建新」（填补下面这条缺口）与「造兵」两个选项，**也不影响上面那条
「AI 是否利用已有缺口」评估指标**——那条指标测的是 `initial_breaches` 留出的完整缺口（本节上一段），
与这里的 `hp_frac` 是两个独立字段，互不影响。`hp_frac` 字段本身保留：它是格式能力，
哪天训练侧要残血墙多样性，把 `wall_hp_frac_range` 松开即可，地图格式不用动。

### 2.4 不做地形高度

CLAUDE.md 的高度优势规则全部挂在**城墙**上（「墙段掉血到原高度一半才失去优势」）。
引入地形高度的代价是：高度规则要实现两遍、等距渲染要多一层精灵、寻路要处理坡度可达性，
而收益只是观感。**不做**，纯收益。高度只由建造物提供。

## 3. 尺寸：锁不变量，不锁数字

尺寸依赖单位速度，而速度未标定，现在拍数字必然返工。**本节不定任何数字**，
只锁一条结构关系：

> **`Ram` 从集结点走到城墙外沿的时间，应占一个 episode 的一小段固定比例。**
>
> 初步取 **10–20%**，但这个区间本身也待定——它依赖 episode 实际长度，
> 须等战术层跑通、看到真实的对局节奏后复核。

`Ram` 是最慢的单位，它决定上限。低于 10% 则玩家来不及反应，侦查与「攻其必救」都没有
时间窗口；高于 20% 则 episode 大半花在行军上，RL 的有效决策步数被浪费。

量级校验。**下表除前两行外全部是待定值**，列在这里只为证明「地图能同时满足行军占比、
攻击密度与 flow field 成本」这件事在量级上成立，不构成对任何数字的承诺：

| 量 | 取值 | 状态 | 由来 |
|---|---|---|---|
| tick | 20 Hz | **已定** | CLAUDE.md 固定 |
| 决策频率 | 每 4–8 tick | **已定** | CLAUDE.md 固定 |
| episode 长度 | 60–120 s = 1200–2400 tick | 待定 | 一波 = 一 episode |
| 每单位决策步数 | 150–600 | 推出量 | 上两行相除 |
| 行军预算 | 6–24 s | 待定 | episode 的 10–20% |
| `Ram` 速度 | 1.5–2 格/s | 待定 | 纯假设，用于走通下面的推导 |
| 集结点到城墙 | 约 30 格 | 待定 | 上两行相乘 |
| 城区半径 | 12–15 格 | 待定 | 容纳 `Keep`、城内资源点与建造空间 |
| 地图边长 | 约 80–96 格 | 待定 | (城区半径 + 30) × 2 |

成本核对（取上表的上限做最坏估计）：96×96 = 9216 格，flow field 每格一字节方向 = 9 KB/张；
按 5.2 节每种破坏速率一张、约 4 张 = 36 KB；并行 64 局约 2.3 MB。可忽略，不是瓶颈。
**这一条的结论对整个待定区间都成立**，所以尺寸最终定在区间内哪里都不会撞上性能问题。

观测的 K×K 局部视野与地图尺寸无关，地图变大不会让观测变大。

## 4. 地形与分层

### 4.1 地形枚举：五种，每种是一个互不相同的标志位组合

| 地形 | `passable` | `buildable` | `blocks_vision` | 可破坏 | 作用 |
|---|:-:|:-:|:-:|:-:|---|
| `Plain` | ✓ | ✓ | ✗ | — | 默认地形 |
| `Rock` | ✗ | ✗ | ✓ | ✗ | 城外随机散布的岩壁团块（见 2.2，不再承担城圈） |
| `Forest` | ✓ | ✗ | ✓ | ✗ | 侦查博弈的地形载体 |
| `Water` | ✗ | ✗ | ✗ | — | **可见但不可达**：唯一「挡路不挡视野」的地形 |
| `Bridge` | ✓ | ✗ | ✗ | — | 跨水的唯一通路，最窄的隘口 |

**「可破坏」一列全是 ✗ 或 —，这不是占位，是结论：地形一律不可破坏。**
可破坏的东西是**第三类实体**（`Stump` / `Sapling` / `Rubble`，见 4.4），它们不在
地形层——地形是静态枚举、展开成三张位图，而带血量的东西必须进实体扁平数组。
这与 4.3「`no_build` 是独立的一层 mask，不是地形类型」是同一种切分理由。

`Rock` 与 `Forest` 那两个 ✗ 是**硬性的**，尤其 `Forest`：2.1 那条挡视野是双向的
独立收益仍然成立——玩家若能清掉森林，外围经济的「要么驻兵、要么认损」这条
野战压力就没了（见 2.1）。`Plain` / `Water` / `Bridge` 那三个写 — 而不是 ✗，
因为「破坏一片平地」本身没有意义，不需要一条禁令。

**玩家不得拥有任何清除地形的手段**（无论叫「开荒」还是「整地」）。
按「结构封死，而非数值劝退」，这不能靠「清地很贵」来防。

**五种恰好对应五个互不相同的标志位组合，没有冗余项。** 此前一版只做前三种、
把 `Water` 判为与 `Rock` 职能重复，那是错的：通行 × 视野的四种组合里，
「**挡路但不挡视野**」这一格只有 `Water` 能填，而它带来的战术质感岩壁给不了——

> 你**看得见**他在对岸集结，但打不着，他也过不来，直到他上桥。

这一格直接落在 CLAUDE.md「情报划分：免费可见 vs 需侦查」那张表上：
水送免费情报、岩壁否定情报，地图因此多出一个调节「多少情报是免费的」的旋钮。
`Bridge` 是隘口地形的极端——正面最窄、且全程可见。河的**长条形状**也不同于
岩壁的团块：岩壁做局部隘口，河把地图切成若干进攻扇区（2026-08-31 前这里写
「正好服务『每个集结点对应一条走廊』」，随走廊概念取缔一并删除）。

两者在仿真侧**接近零成本**：按 4.2 枚举本就要展开成三张位图，它们只是新的组合，
不引入新代码路径。空中单位的位图里水是可通行的，这与「空军无视墙」用的是
同一套按兵种类别的地形掩码。

**`Bridge` 不可建造**，玩家因此无法在桥上砌墙把它封死——要守只能在桥头设防，
这正是想要的攻防形态。**也不做「桥可被摧毁」**：那会给守方一个过强的开关，
且与「攻方的唯一通路是破墙」这一既有形态冲突。

**`Forest` 不设移动减速。** 加了就需要逐格速度倍率，而它的设计职责是遮视野，不是拖慢。

**`Forest` 的覆盖率必须受限**：若森林能连成从任一集结点直达城墙的连续遮蔽通道，
AI 会学到永远走林地潜行，城外地形博弈作废。这是校验器必须查的（见第 8 节）。
**`Water` 同理要查连通性**：河若把某条集结点通路完全切断而不设桥，那条通路等于不存在。

**`Road` 仍然不做**（加速会诱导所有路径收敛到路上，反而减少路径多样性）。
注意这条反对的是**加速效果**，不是路面美术：渲染层可以把部分 `Plain` 画成路面
以引导视觉，只要仿真里它仍是 `Plain`。同理，草地的深浅变体、焦土痕迹都只是
`Plain` 的不同贴图，**不占枚举位**——渲染层按坐标哈希选变体即可，不必写进地图文件。

### 4.2 `rts_core` 内部展开成三张 bitmask

地图文件里地形是一个枚举，`rts_core` 载入时展开成 `passable` / `buildable` /
`blocks_vision` 三张按格的位图。这样：

- 仿真热路径查的是位图，不是枚举 + 查表；
- **观测打包退化成 memcpy** —— 正好用上 CLAUDE.md 提到的 SoA 好处；
- 渲染层单独读原始枚举决定画什么，不侵入仿真数据（但**不是一格一张图**，见 4.2.1）。

**`buildable` 的组合规则**：格子可建造 ⇔ `terrain.buildable AND NOT no_build`。
现在有两套机制都能表达「不可建造」——地形枚举里的 `buildable` 位，与 4.3 的 `no_build`
mask——不明写规则的话，`terrain=Plain + no_build=1` 与 `terrain=Bridge` 谁管谁只能靠猜。

### 4.2.1 地形枚举 → （地砖精灵，可选叠加物件）

**一格要画两张图，不是一张。** 这一条容易写错，且错了才发现的成本不小：

五种地形里**只有 `Plain`（含其贴图变体）与 `Water` 是地砖**——元数据里 `kind == "tile"`、
画布正好等于一格菱形、锚点在菱形中心、不描边不画投影，因此可无缝平铺。
`Rock` / `Forest` / `Bridge` 都是**立体物件**：锚点在脚底、带描边与地面投影、
画布远大于一格，单独贴上去底下是透明的。所以它们各自需要一张地砖作底衬：

| 地形枚举 | 地砖底衬 | 叠加物件 |
|---|---|---|
| `Plain` | `Plain`（变体按坐标哈希挑） | 无 |
| `Water` | `Water` | 无 |
| `Rock` | `Plain` | `Rock` |
| `Forest` | `Plain` | `Forest` |
| `Bridge` | **`Water`** | `Bridge` |

**桥的底衬是 `Water` 而不是平地**——否则河在桥这一格视觉上是断的，读作「水断了一格换成桥」
而不是「桥架在水上」。这条只值一行代码，但不写下来，第一个照抄示例的人就会把河画断。

**叠加物件必须与单位混在同一个深度序列里排序**（按 `gi + gj` 的画家算法），
不能先画完全部地形再画单位：站在岩壁前面的单位要遮住岩壁，站在后面的要被岩壁遮住。
地砖可以单独先铺完，因为它是平的、永远不遮挡任何东西。

可执行的参考实现见 `tools/sprite_gen/preview_map.py`（`OVERLAY` 表、`expand()`、
两遍渲染）。**那份代码是本节的规范来源**，本节与它不一致时以它为准并回来修本节。

前端已按本节落地，见 `game/src/scene_model.cpp` 的 `SceneModel::expand()` 与
`build()`（#40）。**两份实现之间以 `preview_map.py` 为准**——它先写、且是本节点名的
那一份；但真正跑在交付物里的是 C++ 那份，所以两者分叉时要在同一个 PR 里一起改。

#### 4.2.1.2 第三类实体（可破坏障碍）的映射

**随 §6 于 2026-08-29 通过。** 它不在上面那张表里，因为那张表是「地形枚举 → 精灵」，
而障碍**不是地形**（见 4.1）：它是一份独立的点位列表（`obstacles`，见 4.4.1），
一格上可以既有地形又有一处障碍。

| | 取什么 |
|---|---|
| 地砖底衬 | **由该格的地形决定，与障碍无关**——照上面那张表取 |
| 叠加物件 | 障碍的标识符本身（`Stump` / `Sapling` / `Rubble`，素材已入库） |
| 深度 | 与单位、与 `Rock` / `Forest` **同一个序列**，按 `gi + gj` |

两条踩得到的：

- **一格可能有两个叠加物**：地形自带的（如 `Forest`）+ 一处障碍。
  4.4.1 没有禁止这种摆法，而第 22 条也不查它（它只查 `Rock` / `Water`）——
  一株幼树长在密林格上是合理的。所以 `expand()` 那一层**不能假设每格至多一个叠加物**。
- **障碍会消失**（被打掉），地形不会。所以障碍的绘制项**不能和地形一起缓存**
  ——地形展开一次就够，障碍要跟着世界状态走。这是它与 `Rock` / `Forest`
  在渲染层唯一的实质差别。

`tools/sprite_gen/preview_map.py` 里那三处（`Stump` / `Rubble` / `Sapling`）
一直是当景物画的，与本节一致——**它当时不知道它们会有血量，但画法不受影响**。

#### 4.2.1.1 线性结构的 `d` 是**走向**，不是朝向

与上面并列的第二条规则。**这一节是补写的**：规则本身在 #40 就已落地并有测试钉住，
但当时漏了写进本文，于是 `game/include/game/map_data.hpp`、
`game/include/game/scene_model.hpp` 与 `tests/scene_model_test.cpp` 三处注释
指向了一个不存在的节号。

单位的 `d` 是它**面朝**哪；**墙、门、桥的 `d` 是它沿哪个方向铺**。同一段墙沿 `gi` 铺和
沿 `gj` 铺要取不同的精灵，弄错的后果是**相邻墙段接不上**——一条城墙在画面上读作一排
分开的板子，而城圈至少有两条互相垂直的边，所以「全部按同一个朝向画」必然错一半。

哪个朝向对是**实测出来的**（四个朝向各排三格看），换素材后要重测。
**2026-08-30 重测过一次，判据多了一条、结论因此变了，而且两种结构的表是反的**：

| 走向 | 墙 / 门 | 桥 | 上一版（两者共用） |
|---|---|---|---|
| 沿 `gi` | `SW` | **`SE`** | `SW` |
| 沿 `gj` | **`SE`** | `SW` | `NW` |

**多出来的判据是「贴得正不正」，不只是「接不接得上」。** 墙板 180° 对称，
所以 `SE` 与 `NW` 拼起来都无缝，上一版只测无缝、两个都过，选了 `NW`；
但这套素材的 `NE` / `NW` 两张图**内容偏离锚点**（实测 `Wall` 偏 +34 px 且右侧被画布
切掉、`Gate` 偏 −33 px），于是墙整条浮在格子外，而**门与墙错开约 0.9 格**——
玩家看到的就是「城门缩在城里、没和城墙连上」。素材那一侧的偏移与裁切是另一件事，
要靠重渲修（已挂给精灵流水线那侧）；本表只是在两个**本来等价**的朝向里挑了正的那个。

**两张表反过来不是笔误**：`wall-narrow.glb` 与 `bridge-draw.glb` 的长轴不是同一个轴，
而 `tools/sprite_gen/README.md` 那节自己写着「这取决于模型自身的长轴朝向」——
所以这张表只能**逐模型**测，不能全局共用一份（上一版共用一份正是它错的根源之一）。

**桥另有一条**：桥的走向是**过河的方向**（河的垂直方向），不是「相邻桥格的方向」。
一格宽的河上并排两格桥时（演示地图就是），后者会把走向答成「沿着河」，
于是桥板顺着水流铺——玩家的原话是「bridge 没在水上」。判据改成看相邻的**水格**。

**这条必须写下来的理由**：照 4.2.1 写前端的人得到的是一座**墙线断开的城**，而他不会
怀疑朝向——精灵没问题、锚点也对、每一段单看都正常。

走向必须按**邻居**推导，不能按建造时记下的方向存（与自动拼接地砖同一套做法）：
墙段被打断之后走向会变，而 6.2 的 `initial_walls` 里也没有这个字段。
实现见 `SceneModel::run_direction()`。

**一处固有上限**：两朝向素材下，**拐角**（两个方向都有邻居）与**孤立一格**（两个方向
都没有）都没有正确答案，实现按上表当作「沿 `gi`」与「沿 `gj`」处理。城圈的四个拐角因此必然有一处
接不齐。这不是 bug，是两朝向素材的上限；要消掉它需要补拐角件，属素材范围。

### 4.3 区域层

`no_build` 是**独立的一层 mask**，不是地形类型。

> **注意它不承担 2.1。** 2.1 当时定为森林带（`Forest` 本身不可建造），
> 所以 `no_build` 从来不是「外部资源点周围那圈禁建区」；2.1 那条结构约束
> 现已整节移除（2026-09-01），这句话因此更加成立而不是过期。`no_build`
> 只是一个**通用的局部禁建覆盖**，留给地图作者做局部微调（例如演示地图上
> 想留出一条空地）。
> **它不再承载 2.1，但「不承载任何结构约束」那句是错的，已订正**——
> 集结点周围那圈 `no_build` 环正是第 14 条那条结构约束（「任何静态建筑的视野
> 都不得覆盖集结区」）的**唯一**承载者，见 §9 布局段与 `generate.py:place_spawns`。
> 那一条没法用地形表达：集结点是攻方的出兵口，铺 `Forest` 会把走廊本身堵上，
> 而地形也无法表达「这里不许建瞭望塔但可以走兵」。
>
> 留着这句订正而不是直接删掉原话，是因为原话更容易被读到并据此把那圈环删掉
> ——而它一删，第 14 条就变成一条**判定合法却被违反**的检查（那正好发生过，
> 见 `thresholds.json` 的 `_FINDING_static_vision_was_8`）。
>
> 下面这句仍然成立，只是不再是「一律」——凡是结构性的禁建都**应当尽量**由地形承担，
> 因为地形是玩家看得见的，而 mask 不是。

> 注意 `Bridge` 看起来是这条规则的例外：它的标志位三元组正好等于 `Plain` 去掉 `buildable`，
> 也就是本节说「不该进枚举、该做成 mask」的那个东西。保留它占一个枚举位是有意的
> ——仿真侧它确实是一种独立地形（跨水的唯一通路），这个语义值得占一个名字，
> 且渲染侧它需要一个不同的底衬（见 4.2.1）。代价就是上面 4.2 那条组合规则必须明写。
>
不设 `initial_city` mask：「城内 / 城外」不需要用区域表示，见 4.4。

### 4.4 点位实体

不落在格子层的东西：`keep` 位置、集结点、资源点、初始墙段、**可破坏障碍**、
**玩家开局已拥有的其余建筑**。

#### 4.4.0 玩家开局已拥有的其余建筑

**2026-08-31 新增，随大地图重设计一起补的空白。** 此前地图 schema 完全没有
承载"玩家开局有一座兵营/箭塔"这件事的地方——`keep` 恒一座、有专门字段；
`Wall`/`Gate` 带残血比例、走 `initial_walls`；除此之外玩家开局拥有什么，
只能靠 `demo_driver.cpp` 里写死的占位编成表达，没有一张地图文件能自己定义它。

在地图文件里是一份点位列表（6.2 的 `buildings`），字段只有 `type` 与 `pos`：

- `type` 取花名册标识符，允许**除 `Keep`/`Wall`/`Gate` 外**的全部建筑
  （`Tower`/`Flak`/`Watch`/`Barrack`/`Fence`/`Quarry`/`Lumber`/`Mine`）。
  那三个不在这份白名单里——它们已经各有专门字段，重复表达同一件事只会制造
  「两个字段互相不一致」的新洞
- **不带血量、也不带残血比例**，同 `obstacles`：满血进场，没有设计要求说
  玩家的初始建筑开局就该带伤（2.3 的「城圈可以带伤」只对城圈成立）
- **必填，空数组也要写出来**，同一条纪律：缺失与刻意为空不可区分，
  会让第 24 条那类校验空过

**第 24 条查它的摆放冲突**（同 16/17/22 条的模式）：必须落在可通行且可建造的格上、
不得落在 `Rock`/`Water` 上、不得与其余任何点位实体（`keep`/资源点/墙段/障碍/
集结点）同格、同格不得两座建筑。**采集建筑踩在类型匹配的资源点上不算冲突**——
`Quarry`/`Lumber`/`Mine` 就该踩在对应资源点上，那是它们生效的唯一摆法；
只有类型不匹配（或建筑本就不是采集建筑）才报。

**生成器预置的构成**（2026-08-31 重构，微随机化，落在 `place_inner_content`）：
箭塔 2–3 座贴城门/缺口内侧、兵营 1–2 座在堡垒附近、伐木场/采石场/**金矿场**恒各一座
精确踩对应资源点（金矿场 2026-09-01 加：试玩反馈「不会刷新金矿」的落点之一是
没有一座 `Mine` 当地标——三种采集建筑自此齐）。**堡垒周围 3 格净空**是另一条硬规则——`demo_init` 在
keep+(1,±2) 预置 `Tower`/`Flak`、keep+(2,·) 撒 7 个单位，地图内容不得与它们
撞车（净空区 cheb ≤ 3 内不得有资源/森林/建筑格）。城内其余区域同样不生成
`Forest` / `Rock`，树石景观统一放在城墙之外。

#### 4.4.1 可破坏障碍是第三类实体

**随提案「无尽模式与地形分层」§6 于 2026-08-29 通过。** 三种：`Stump`（树桩）、
`Sapling`（幼树）、`Rubble`（碎石），登记在 `CLAUDE.md`「命名对照表：可破坏障碍」。

它们既不是单位也不是建筑，也**不是地形**（理由见 4.1 那段）。在地图文件里
是一份点位列表（6.2 的 `obstacles`），字段只有 `type` 与 `pos`：

- **不带血量。** 绝对血量是待标定数值，在那份还不存在的 JSON 数值表里
- **也不带残血比例**，与 `initial_walls` 不同。城墙的残血是 2.3 表达的一条设计能力
  （初始城圈可以带伤——尽管当前全部产物都满血开局，见 2.3），而没有任何设计要求说障碍开局就该带伤。满血就是它的初始状态
- **必填，空数组也要写出来。** 缺失与刻意为空不可区分是个洞——
  一张没有这个键的图，读者分不清「这张图没有障碍」与「写图的人不知道有这个字段」，
  而后者会让第 19、22 条那类校验空过

**破坏后产出资源**：`Stump` / `Sapling` → 木材，`Rubble` → 石材，**没有一种产金币**。
映射的唯一实现处是 `rts/roster.hpp` 的 `harvest_of()`；四条结构决定（含
「不可再生」与「建筑摧毁不产生 `Rubble` 实体」）见 `无尽模式与地形分层.md` 6.6.2。

**注意这一条对地图生成器有约束**：产出资源意味着障碍的分布不只是装饰，
它是一份**一次性的资源存量**。撒得越多，早期压力被抹得越平——
而「城内保底收入按线性增长」那条拐点要求是按资源点算的，不含清野。
当前生成器把障碍限制在城外，并按同类 1–3 个组成相互分离的小撮；单体和小团都会出现。

**资源点的 `tier`（`inner` / `outer`）是平衡标注，不影响仿真。** 两者机制完全相同
（在上面建 `Quarry` / `Lumber` / `Mine` 产出资源），差别纯粹是位置。写进文件是给校验器和
平衡工具用的，**不要在仿真里给 `inner` 加任何加成** —— 那会变成一条隐藏机制。

**城内保底必须同时覆盖石材、木材与金币**（各至少一个 `inner` 点）。若某一种保底为零，
玩家在龟缩状态下对应的整条决策轴直接消失，而保底收入的职责是防死亡螺旋、
不是只防龟缩。

金币也要有资源点：**三种资源的主要来源一律是资源点**，对应的采集建筑是 `Mine`（金矿场）。
唯一例外是领主堡垒产出的**极少量**金币，那是防死亡螺旋的护栏（"兵力地板"，见 CLAUDE.md），
数额不足以替代 `Mine`，因此 `inner` 仍必须有金矿点——校验器照查三种全覆盖。
这一改动使「攻其必救」变成三向的：拆采石场削防线**上限**、拆伐木场令防线
**修不动也建不起**（木材是所有建筑的配料）、拆金矿场削**兵力**。

**外部资源点偏向木材。** 木材管维修与应急工事，切断它会让防线**持续退化**而非一次性削弱，
这正是 CLAUDE.md 描述的「拆伐木场使玩家无法挽救残血建筑」。石材偏城内则保证玩家
总能重建最基本的硬防御。配比是数值旋钮，倾向如上。

## 5. 寻路

### 5.1 墙是「高代价可通行」，不是障碍

这一点容易踩坑，且两种错法都会直接复现对标产品的缺陷：

- 若 flow field 把墙当**不可通行**：城圈一旦补完整，field 无解，AI 卡在墙外转圈。
- 若只让 AI 就近砸墙：**AI 永远不会绕到缺口** —— 正好是 DiNaO 被批评的那个失效。

正确做法是把墙段的通行代价设为**破墙耗时**：

```
cost(墙段) = 墙段当前血量 / 该单位的破坏速率
```

于是「绕远走缺口」和「就近砸墙」在同一个尺度上自动比较，不需要专门写「寻找缺口」的逻辑。
缺口（血量 0）代价为 0，自然被优先；加固过的厚墙代价高，自然被绕开。

**可破坏障碍（4.4.1）复用这一条，一字不改。** 它就是一段没有归属、不参与战斗的
「墙」，所以「绕远走通路」与「就近打穿木桩」同样自动比较。三处零改动：

| | 为什么不用改 |
|---|---|
| 寻路 | 同一个代价公式，只是被代入的实体换了一组 |
| flow field 份数 | 份数 = 破坏速率的种类数（见 5.2），而破坏速率是**兵种 × 等级档**的属性，与被破坏的对象无关 |
| 战斗解算 | `CLAUDE.md` 已要求「建筑与城墙走与单位完全相同的战斗解算」，障碍是同一条路径上的第三组实体 |

**这也是攻方为什么不需要一个「攻击障碍」的战术动作**：单位往那个方向走、撞上去
就开始破坏。给攻方加 `AtkObst` 只会把动作空间从 13 变 14 而不带来任何新行为
（见 `CLAUDE.md`「RL 设计决策」）。守方清野是另一回事——它是玩家级决策，
走 `CommandKind::Clear`。

### 5.2 flow field 的数量是常数，不随单位数增长

因为 CLAUDE.md 规定**破坏速率是兵种固定属性、与等级无关**，上式的分母只有几种取值，
因此 field 数量 = 不同破坏速率的种类数（`Ram` / 近战 / 远程 / 空中为 0），
是个小常数，不随单位数、等级、波数增长。

那条规则本来是为平衡设的（防止满级步兵一拥而上瞬间破墙），在寻路上顺带把成本从
O(单位数) 压到 O(兵种数)。**修改「破坏速率与等级无关」会同时破坏平衡与寻路成本**，
不要单独动它。

空中单位对墙的破坏速率为 0，其 field 直接把墙当不可通行 —— 这自动实现了
「空军无法破墙」，不需要特例。

## 6. 数据格式

### 6.1 硬性要求：文本、逐行可 diff

地图要入库、要评审、要和回放哈希对应。二进制地图无法 review，四人并行时也无法合并。
因此格式是 JSON，**格子层按行编码成字符串**，一行地图 = 一行文本。

### 6.2 结构

```json
{
  "format": 1,
  "map_id": "border_keep_01",
  "name": "王国边境",
  "size": [96, 96],
  "content_hash": "sha256:0f3a...",

  "layers": {
    "terrain":  { "palette": ["Plain", "Rock", "Forest", "Water", "Bridge"],
                  "rows": ["0000111000...", "0001110000...", "..."] },
    "no_build": { "rows": ["0000000000...", "0000011000...", "..."] }
  },

  "keep": [48, 48],

  "spawns": [
    { "id": 0, "pos": [48,  2] },
    { "id": 1, "pos": [94, 48] },
    { "id": 2, "pos": [48, 94] },
    { "id": 3, "pos": [ 2, 48] }
  ],

  "resources": [
    { "type": "stone", "pos": [42, 45], "tier": "inner", "unlock_wave": 1 },
    { "type": "wood",  "pos": [53, 44], "tier": "inner", "unlock_wave": 1 },
    { "type": "gold",  "pos": [46, 51], "tier": "inner", "unlock_wave": 1 },
    { "type": "wood",  "pos": [70, 30], "tier": "outer", "unlock_wave": 2 },
    { "type": "gold",  "pos": [72, 33], "tier": "outer", "unlock_wave": 2 }
  ],

  "initial_walls": [
    { "kind": "Wall", "pos": [40, 33], "hp_frac": 1.0  },
    { "kind": "Wall", "pos": [41, 33], "hp_frac": 0.45 },
    { "kind": "Gate", "pos": [48, 33], "hp_frac": 1.0  }
  ],

  "obstacles": [
    { "type": "Stump",   "pos": [44, 38] },
    { "type": "Sapling", "pos": [45, 38] },
    { "type": "Rubble",  "pos": [61, 29] }
  ],

  "buildings": [
    { "type": "Tower",   "pos": [44, 40] },
    { "type": "Barrack", "pos": [50, 44] }
  ]
}
```

说明：

- **上面示例中的一切数字（`size`、坐标、`hp_frac`、乃至 `spawns` 的条目数）都是编造的占位值**，只用于说明字段形状。
  真实地图的这些值待定。字段名与结构是规范，数字不是。
- **地图文件中不出现任何像素单位**，只有格子坐标。像素几何是渲染层的事，见第 7 节。
- 初始缺口**不显式存储** —— 缺口就是城圈上没有墙段的位置，由校验器推导。
  显式存一份会和 `initial_walls` 冗余，两者一旦不一致就是静默 bug。
- `spawns` 只有 `id` 与 `pos`（**2026-08-31 起 `corridor` 字段已废除**——
  「走廊」概念取缔，集结点是固定边缘候选点、不带性质标签，见 2.2）。
- `type` 是 `stone` / `wood` / `gold` 三种，对应采集建筑 `Quarry` / `Lumber` / `Mine`。
  **`inner` 侧三种都必须有**（见 4.4）。
- **`unlock_wave`（2026-08-31 新增）**：这一点从第几波起才产出（CLAUDE.md「资源点
  随波数解禁」）。**必填、整数、下界 1**（`rts::World::wave()` 从 1 起，`inner`
  侧照惯例填 1，即「从第一波就有」）。**与 `tier` 不是同一件事**——`tier` 只是
  描述、不进 `rts_core`（哪些资源点在墙内取决于玩家把墙建在哪，仿真不消费地图
  作者当初的标注）；`unlock_wave` **影响仿真**（`World::tick_economy` 按
  `wave() >= unlock_wave` 判是否入账），必须传进去。第 18 条校验它随距离单调：
  解禁越晚的簇，离 `keep` 越远。
- **`obstacles` 随提案 §6 于 2026-08-29 新增**，见 4.4.1。字段只有 `type` 与 `pos`
  ——**不带血量、也不带残血比例**，理由在那一节。
  - `type` 取值是 `Stump` / `Sapling` / `Rubble`，**大小写与花名册标识符一字不差**。
    注意这与 `resources` 的 `type`（小写的 `stone` / `wood` / `gold`）风格不同，
    而这个不一致是**有依据的**：`initial_walls` 的 `kind` 也是 `Wall` / `Gate`。
    分界是「值是不是一个花名册标识符」——是的用原样大小写，是类别名的用小写。
  - **必填。** 没有障碍的图写 `"obstacles": []`，不要省略这个键。
    缺失与刻意为空不可区分会让第 19、22 条校验空过。
  - **它是一份一次性资源存量**（破坏后产出木材 / 石材），所以撒多少不是纯装饰问题，
    见 4.4.1 末尾那条对生成器的约束。

### 6.3 回放必须记录 `map_id` 与 `content_hash`

确定性回放测试是本项目主要的防 bug 手段。地图改一个格子，旧回放就会静默偏离，
而「静默」正是最坏的情况 —— 回放跑不过时，人会先怀疑仿真逻辑，不会想到是地图变了。

因此回放文件头记 `map_id` + `content_hash`，载入时不匹配直接报错。

**`content_hash` 取的是除该字段本身以外全部内容、经「规范序列化」之后的 SHA-256。**

**规范序列化**（注意不是对文件文本做规范化——理由见下）定义为：删掉 `content_hash`
键之后把整个文档重新序列化一遍，参数固定为

```python
json.dumps(doc, sort_keys=True, ensure_ascii=False, indent=2, separators=(",", ": ")) + "\n"
```

再取其 UTF-8 字节的 SHA-256。**改动这四个参数中的任何一个，等于改动全部已入库地图的
哈希**，属跨模块契约变更（回放文件头依赖它），不要顺手动。
唯一实现在 `tools/map_gen/mapfile.py` 的 `_CANONICAL_KWARGS`，**那份是规范来源**。

> **本条原来写的是「换行统一为 LF + 去掉每行尾随空白」，那不够，这是一次订正。**
> 文本级规范化挡不住另外三种同样会改变字节的差异：**缩进宽度**（有人顺手格式化了
> 一下）、**键的顺序**（不同 json 库 / 版本的序列化顺序）、**非 ASCII 转义**
> （`ensure_ascii` 开或关，`"王国边境"` 与 `"王国..."`）。这三种的症状与
> 下面 CRLF 那条**一模一样**，而看到它的人第一反应一定是去查仿真。规范序列化把三种
> 一次全消掉，且 LF 与无尾随空白是它的**副产品**——序列化本来就不产生这两样，
> 所以本条原来要的东西自动满足。
>
> 订正来源：@omg321-975 在 #31 里按更强的定义实现了它，并在 `mapfile.py` 的模块
> docstring 里写明「建议 6.3 改写为『对规范序列化后的内容取哈希』……未定之前以本模块
> 为准——因为它是可执行的那一份」。**#31 已合入，本条却没跟着改**，于是仓库里出现了
> 一个自称临时、实际权威的实现和一份说着另一件事的规范。规范跟着可执行的那一份走。

这一步不是洁癖，是必需的：仓库启用了 `core.autocrlf`，同一份地图文件在 Windows 工作区里是
`CRLF`、在 Linux 上是 `LF`，**字节不同，哈希也就不同**。不做规范化的话，
Windows 上生成并写入哈希的地图，在训练服务器上会因为哈希对不上而直接报「地图不匹配」
——一个看起来毫无道理、且第一反应一定是去查仿真的错误。

注意 CLAUDE.md 那条「回放文件在 Windows 与 Linux 之间不可复现」**不覆盖这一条**：
那说的是浮点，这里坏掉的是文件字节，而且它会让**地图本身**在两个平台上被判为两张不同的图。

`.gitattributes` 已对数据文件强制 `eol=lf` 作为第二道防线，但规范序列化仍是主要手段
——它不依赖每个人的 git 配置是否正确。

## 7. 与渲染的耦合：像素几何只有一个来源

**像素几何的唯一来源是 `tools/sprite_gen/out_3d/_sprite_meta.json`。**
地图文件、地图规范、`rts_core` 都不复述任何像素数字 —— 复述就会漂移。

核实精灵流水线时曾发现四处影响地图渲染的契约问题。**#19 已修掉三处**，
下面按现状记录：修掉的只留一句结论（论证已无价值，但结论要留着，免得有人再改回去），
未修的那一处保留完整论证。

### 7.1 ~~`tile` 字段的尺度错了 2√2 倍~~ 已修

曾经 `_sprite_meta.json` 写 `"tile": [128, 64]` 而实际投影出的菱形是 `362 × 181`
——比例对、尺度差 2√2 ≈ 2.83 倍，肉眼看不出来，前端会把所有单位画大 2.83 倍。
根因是那两个数字是从**占位路线**的投影常量原样抄过来的，在占位路线下它是对的。

现在 `blender_render.py` 引入了 `TILE_SIDE = 1/√2`（边长为 s 的正方形投影后横跨 `√2·s`,
取 `s = 1/√2` 使菱形正好占满 `px_per_tile × px_per_tile/2`），`tile` 随 `px_per_tile` 导出，
不再是手写常量。已核对：`Plain` 的 `canvas` 是 `[256, 128]`、`ground_anchor` 是 `[128, 64]`。

**留下的规矩**：`tile` 必须从 `px_per_tile` 推导，不得手写。

### 7.2 两条路线的元数据 schema 与文件名已经分叉（**未修，且已扩大**）

这是四处里唯一还在的，而且比原先更严重。

| | `gen.py`（占位） | `blender_render.py`（3D） |
|---|---|---|
| 画布 | 全局 `"canvas": 256` | 逐实体逐状态 `sprites[id].states[state].canvas: [w,h]` |
| 锚点 | 全局 `"ground_anchor": [128,184]` | 同上路径下的 `ground_anchor` |
| 动作状态 | 无 | `idle` / `move` |
| `px_per_tile` / `frames` | 无 | 有 |
| 地砖标记 | 无 | `kind: "tile"` |
| 文件名 | `<标识符>_<朝向>.png` | `<标识符>_<状态>_<朝向>[_<帧号>].png` |

两份 JSON **结构不兼容**，读得了一份的前端代码读不了另一份。3D 路线的结构至今改了四次
（全局 → 逐实体 → 逐实体逐状态 → 加 `kind`），占位路线一次没跟。

占位路线的价值本是「无 Blender 也能跑前端」。**契约不通用则这个价值已经实际失效**
——前端只能对着 3D 那份写，占位图用不上。

出路有两条，**需要小组点头**：让 `gen.py` 跟上（含地形与两种状态），
或明确宣布放弃占位路线、由已入库的 300 张成品承担该职责。
`tools/sprite_gen/` 的负责人倾向后者。

### 7.3 ~~缺地形精灵~~ 已补

现已登记 13 条地形素材（5 种 `Plain` 贴图变体 + `Rock` / `Forest` / `Water` / `Bridge`
+ 4 件景物），标识符与 4.1 的枚举一致。**贴图变体不占枚举位**，由渲染层按坐标哈希挑，
见 4.2.1。

### 7.4 ~~城墙精灵水平占地不足一格~~ 已修

曾经 `Wall` 的水平占地约 0.60 瓦片，而墙段在仿真里占整格，连续墙段之间会留约 40% 的缝。
根因是尺寸约定以「菱形的屏幕宽度」为 1，而格边长只有 0.707，等于把所有实体放大了 √2 倍。

现在改为一律以**格边长**为 1，并对 Kenney 套件用 `modules`（1 个模型单位 = k 格边长，
不做逐实体归一化）。`wall-narrow` 在 `modules: 1` 下沿长边正好占 1 格，相邻墙段无缝。

**留下的规矩**：沿格子连续铺开的建筑（墙 / 门 / 木栅）按**占地宽度**定标，不按高度。

## 8. 校验器必须查的条目

`tools/map_gen/` 的校验器是地图规范的可执行部分。手写地图与生成地图都必须通过。

**下表中出现的一切阈值与区间都是待定的占位值**，应写在校验器的一份配置里、
不要硬编码进代码，等数值标定后统一填。

> **那份配置已经落地：`tools/map_gen/thresholds.json`**（读取层 `thresholds.py`）。
> **待定的从来是值，不是归口**——归口一旦有了，含数值的第 1、2、5、14 条就能实现，
> 占位值随时改而代码不动。四条**已实现**。
>
> 它分两档 profile（`strict` / `fixture`），而**profile 只改数值、不改「哪些条目跑」**。
> `fixture` 只服务 `game/testdata/fixture_min.json`：那是一张 7×5 的最小夹具、
> 不是一张游戏地图，对它套用「边长 ≥ 64」或「行军占 10–20%」没有意义，
> 而格式与拓扑检查对它完全适用。**防止 profile 退化成后门的手段是两条对照守卫**：
> ctest 的 `map_gen_validate_fixture_strict_is_red`（`WILL_FAIL`，走真的 CLI）
> 与 `selftest.py` 的「profile 不是无操作」一组（进程内，能比对具体哪几条红）。
>
> **`Ram` 速度刻意不在那份配置里**，从 `game/data/stats_placeholder.json` 读——
> 那是数值进仿真的唯一路径，抄一份就是第二个真相来源，而两份数值分叉时
> 第 5 条会拿一个仿真里根本不存在的速度算行军时间，且没有任何东西会红。
>
> **第 5 条落地当天就抓到一处占位值不自洽**，记在这里因为它是标定的输入：
> 数值表里 `Ram` 的占位速度是 0.04 格/tick = 0.8 格/s，而本文 §3 量级表假设
> 1.5–2 格/s（并据此推出「边长约 80–96」）。按 0.04 算，80 格的图上行军占
> 25%–50%，超出 §3 锁死的 10–20%。**自洽的那一份是 §3，偏离的是那个速度**
> （取 0.0875 时 size=80 给出 11.4%–22.9%，正好落在区间里）。
> 生成器因此暂取 size=64，理由与算式在 `thresholds.json` 的
> `_FINDING_size_vs_ram_speed`。

| # | 检查 | 不通过的后果 |
|:-:|---|---|
| 1 | `size` 的**两条边分别**落在给定区间 | 行军时间失控（第 3 节） |
| 2 | 集结点数量落在给定**区间**内，且均距地图边界不超过给定格数 | CLAUDE.md 要求「地图边缘集结」 |
| 3 | ~~每种 `corridor` 恰好出现一次~~ ➜ **已废除**（2026-08-31 走廊概念取缔，编号留空） | — |
| 4 | 每个集结点到 `keep` 有通路（墙按高代价可通行） | AI 卡死 |
| 5 | 集结点到城墙外沿的 `Ram` 行军时间占 episode 的比例落在区间（占位 10–20%） | 第 3 节的不变量 |
| 6 | `inner` 资源点中石材、木材、金币**三种各 ≥ 1** | 保底缺一条决策轴（4.4） |
| 7 | ~~外部资源点结构性不可围~~ ➜ **已废除**（2026-09-01 移除森林带机制，编号留空，见 2.1） | — |
| 8 | 初始城圈至少一处缺口 | 「AI 利用已有缺口」指标第 1 波测不了（2.3） |
| 9 | 需人工设防的正面总长落在给定区间 | 40 单位上限下攻击密度失衡（2.2） |
| 10 | `Forest` 不构成任一集结点直达城墙的连续遮蔽通道 | 城外地形博弈作废（4.1） |
| 11 | 无不可达的 `Plain` 孤岛 | flow field 出现洞 |
| 12 | **`Water` 不得在无桥的情况下完全切断任一集结点到 `keep` 的通路** | 那条通路等于不存在（4.1） |
| 13 | **每个 `Bridge` 格的四邻中至少有一格是 `Water`** | 桥不在水上，底衬映射（4.2.1）会渲出一块孤立的水 |
| 14 | **任一集结点到最近的可建造格的距离 > 静态建筑视野半径上限** | 侦查被一次性买断，见下 |
| 15 | `content_hash` 与**规范序列化后**的内容一致（定义见 6.3） | 回放静默偏离 / 跨平台误判为两张图（6.3） |

**表里刻意不再写占位区间**（原先第 1 条写着「占位 [64, 128]」、第 5 条写着
「占位 10–20%」）。区间的唯一归口是 `thresholds.json`，在这里复制一份就是
**两个真相来源**，而它已经漂了一次：那个 `[64, 128]` 与配置里的 `[64, 96]`
不一致，而没有任何东西会因此变红。**第 5 条那个 10–20% 是例外，保留**——
它不是阈值而是 §3 锁死的**不变量**，本文是它的出处。

**第 2 条取区间而不是「等于配置给定值」，是一处有意的改动。** 集结点数量是
待定数值（CLAUDE.md「关于数值」），而本文曾把它写成「定为 4」——那是把平衡
旋钮当成结构结论，是那条规则记下的唯一一次违反。**一个数会被下一个读者当成
已定值，区间表达「还没定」。**（2026-08-31：「集结点数 = 走廊种类数」那条
旧结构约束随走廊概念取缔一起作废，第 3 条废除、编号留空。）

**第 7 条已随 2.1 的订正一并废除（2026-09-01）**，原实现是「每个 `outer`
资源点必须有一条到地图边界、全程位于 `Forest` 的 4 连通路径」，判据本身
没有任何问题（不含数值、不需要新字段），废除的原因是承载它的森林带在
视觉上仍读成直线，团队认为收益不值——理由与取舍见 2.1。**「部分资源点
在墙外」这条更基本的要求不受影响**，它现在完全由第 21 条守住（见 8.1）。

第 14 条不是数值检查而是**结构约束的可校验形式**，理由值得写清楚：
`Watch`（瞭望塔）是大视野零战力的静态建筑。而 CLAUDE.md 的「情报划分」把**编成构成**
划在「需要侦查」一侧，整个侦查博弈的前提是**情报要花钱买、且会过时**
（斥候便宜到可消耗、波次结构让记忆天然过时）。一座永久建筑若能照亮集结区，
「这波要不要花钱侦查」就被**一次性买断**了——`Scout`、`Wraith` 屏蔽、佯攻诱饵会一起失效。

这是把一条持续的经济交易换成一次性支出，**性质变了，不是强度问题**，所以不能靠调小视野
半径解决。写成「集结点必须落在任何静态建筑的视野之外」这个距离条件，则它随地图尺寸标定
自动成立，不必等数值定完再回头查。

### 8.1 编号 16 起的条目

**编号已经排定，落地时按这个顺序**（每加一条都要同步 `tools/map_gen/validate.py`
的注册表与它那条「注册表必须正好覆盖 1..N 不重不漏」的断言）：

| # | 检查 | 不满足的后果 | 来源 | 状态 |
|:-:|---|---|---|---|
| 16 | `resources` 与 `keep` 必须落在**可通行且可建造**的格上 | 手画的图渲出来一切正常，直到 `rts_core` 发现采石场盖不上去 | 实测 | ✅ 已实现 |
| 17 | `initial_walls` 不得落在 `Rock` / `Water` 上、同格不得有两条；`spawns` 的 `pos` 不得重合（现在只查了 `id`） | 同上 | 实测 | ✅ 已实现 |
| 18 | **`resources` 的解禁波数序列按距离单调**：解禁越晚的簇，离 `keep` 越远 | 「越往后越要往外走」这条节奏失效，解禁退化成随机给钱 | 2.1 定案 | ✅ **已实现**（2026-08-31，6.2 新增 `unlock_wave` 字段） |
| 19 | **`Forest` 与 `Rock` 不得出现在可破坏障碍列表里** | 2.1——玩家能清掉森林，视野双向遮蔽那条收益就没了 | 同上 | 🟡 **不再阻塞**，只等实现 |
| 20 | ~~第 7 与第 10 条的联合可满足性~~ ➜ **已随第 7 条一起废除**（2026-09-01，编号留空——本条本身就是「第 7 条存在」这一前提的产物，前提没了就不再有「联合」可言，见下） | — | — | — |
| 21 | **至少有一个 `outer` 资源点** | 龟缩退化解不必绕过 2.2——它压根不用出城 | 落地第 7 条时查出，第 7 条废除后独立扛住「部分资源点在墙外」这条要求 | ✅ 已实现 |
| 22 | **`obstacles` 的摆放冲突**：不得落在 `Rock` / `Water` 上、同格不得有两处、不得与 `keep` / `resources` / `initial_walls` / `spawns` 同格 | 一处摆在水里的树桩渲出来正常，直到有人去打它；摆在墙上的障碍让那段墙是死是活取决于遍历顺序 | 加 `obstacles` 时查出 | ✅ 已实现 |
| 23 | **`Forest` 4 连通块的格数 ≥ 阈值**（不得碎成粉尘） | 森林的两项职责（§2.2「看不清来了什么」、把杀伤区压成薄带）**都依赖连片**，而**总面积一模一样**：100 格撒成 50 对散点与聚成几片，遮蔽效果完全不同。第 10 条只禁止**过度**连通，此前没有条目管下界 | 组内提出「一片密林不能太分散」时查出 | ✅ 已实现（阈值在 `thresholds.json`） |
| 24 | **`buildings` 的摆放冲突**：必须落在可通行且可建造的格上、不得落在 `Rock` / `Water` 上、同格不得有两座、不得与 `keep` / `initial_walls` / `obstacles` / `spawns` 同格。**与 `resources` 同格不算冲突**——`Quarry`/`Lumber`/`Mine` 就该踩在类型匹配的资源点上（那是它们生效的唯一摆法），只有建筑与资源类型不匹配（或建筑本就不是采集建筑）时才报 | 一座箭塔盖在岩壁上，地图渲出来正常，直到 `rts_core` 发现它盖不上去；一座 `Quarry` 踩在 `wood` 点上，玩家造出来才发现它产不出石材 | 6.2 新增 `buildings` 字段时查出 | ✅ 已实现 |
| 25 | **`outer` 资源点里金矿数 ≥ 阈值**（占位 2，在 `thresholds.json`） | 攻其必救在城外少一条轴：「打哪一种资源」的纹理被削掉（2026-08-31 实测：`gen_01006000` 城外零金矿、多张图只有 1 个） | 同上 | ✅ 已实现 |

第 16、17 条来自实测：把资源点摆进水里、把另一个摆进岩壁里、把墙摆在水上、
同一格叠两条墙——**格式层与那 15 条全部放行**。

**第 23 条有两点容易写错，都做过破坏性验证：**

- **判据取「块格数」而不是「块厚度」。** 城外散布的森林小簇可以窄到 1 格宽
  的连接处，所以任何厚度判据都会把合法的形态判红。这不是理论风险：把判据
  改成「至少 2 格宽」之后，`generate.py` 立刻**60/60 全被否决**——生成图里
  真的有 1 格宽的窄连接。
- **取 4 连通**，落在保守那一侧：4 连通把块切得更碎 ⇒ 报得更多，而本条是
  一条**下界**约束，多报是安全的那一侧。

**它今天会平凡通过，那不是不加的理由。** 实测 6 张生成图共 33 个 4 连通块，
最小 4 格、粉尘块 0 个——因为生成器画的是半径 1–3 的圆盘。但那是**生成策略**
保证的，不是约束保证的（同第 10 条那句「这不等于第 10 条无用——它保护的是
手写地图」）。而 §9 那张手写演示地图**还没做**，正是最容易点几格森林当装饰的地方。

**第 21 条本是落地第 7 条时查出来的洞**：第 7 条在一个 `outer` 都没有时空过，
第 6 条只查 `inner` 侧三种齐全，于是一张把所有资源都放在城里的地图能通过全部检查——
而那等于砍掉「部分资源点在墙外」，`CLAUDE.md` 把它列为**不能砍的两个机制之一**。
**2026-09-01 第 7 条随 2.1 的订正废除后，第 21 条独自扛起这条要求**——「部分
资源点在墙外」现在只靠这一条守住，不再有第 7 条的「不可围墙」做后盾。
只查「≥ 1」：具体几个、内外配比多少全是数值，而 0 与 1 的差别不是强度差别，
是那条机制在不在。

**第 22 条是加 `obstacles` 字段时顺带查出来的**，性质与第 16、17 条完全相同——
新增一类点位实体就新增一片摆放冲突面，而那一片没有任何现存检查覆盖。
它**刻意与第 17 条分开编号而不是并进去**：第 17 条已实现、已有测试，
把新的实体类别塞进它等于让一条绿着的检查悄悄扩大职责，
而 review 时看不出覆盖面变了。

**剩下三条各自卡在不同的东西上，不要当成一个批次：**

- ~~**第 18 条缺的是字段，不是判据。**~~ **已实现（2026-08-31）**：6.2 新增
  `resources[].unlock_wave`（必填、整数、下界 1，`inner` 侧惯例填 1），连带
  `rts::ResourceSite::unlock_wave`、`World::tick_economy` 的入账判断、
  `game::ResourceNode`/`make_world_init` 的载入管线、`mapfile.py` 的格式校验、
  `validate.py` 的 `check_resource_unlock_wave`、`generate.py` 按每个点到
  `keep` 的实际距离严格排名自动生成都已跟上（**按点，不按簇**——按簇心的
  名义距离分配可能被簇内随机取点的顺序小幅打乱，按点严格排名不依赖「簇内
  顺序也单调」这条更强的假设，见 `generate.py` 的 `_resource_unlock_waves`）。
- **第 19 条此前是双重阻塞，两条现在都解了。** (a) §6 的机制那一半已于
  2026-08-29 表决通过；(b) 6.2 随之新增了 `obstacles` 字段，本条终于有东西可查。
  所以它现在**只等实现**，判据是现成的：`obstacles[].type` 只能取
  `Stump` / `Sapling` / `Rubble`，出现任何地形名即失败。
  **代码那一侧一直有一半**：`rts::ObstacleType` 三个成员由
  `tests/roster_test.cpp` 钉住不与地形枚举重名，往里加 `Forest` 会红。
- ~~**第 20 条（第 7 与第 10 条的联合可满足性）曾移出 `CHECKS`，落到生成器
  的批量报告。**~~ **2026-09-01 第 7 条随 2.1 的订正废除后，第 20 条也一并
  废除**——它原本问的是「森林通道必须存在」与「森林不得构成遮蔽道」两条
  合起来是否还有可行解，前一半没了，「联合」这件事本身就不再成立，不是
  移走后又要移回来，是这条编号从此不对应任何检查。`validate.py` 的
  `REMOVED` 字典把 7、20 两个编号一起记着理由。

  **批量报告这个机制本身没有被拆掉，只是不再叫"第 20 条"。**
  `tools/map_gen/generate.py` 仍然在跑：丢弃率 + 逐条否决计数 +
  「最常否决的是哪一条」（约束互相挤压时，那一行指出该先松哪一边）——
  这是一个**通用**的诊断能力，服务的是当前表里剩下的全部检查，不专属于
  某一条编号，原表述把它与 #7/#10 绑定得太紧，也一并订正。

  **报告的价值取决于它真的被走过。** `selftest.py` 有一条把阈值收紧到必然
  否决的用例，断言失败**有界**（不挂住）且否决计数**指向真正否决它的那
  一条**（报告指错条目比不报更坏，它会让人去松错的那一边）。

## 9. 训练地图与演示地图

**单张地图训练不够。** 策略会记住地图而非学会战术，而这直接威胁核心论证材料：
「AI 是否发现并利用已有缺口」在指标上无法区分「学会了找缺口」与「记住了那个位置有缺口」。

但答辩需要一张精心设计、反复打磨的地图。所以两者都要：

| | 演示地图 | 训练地图集 |
|---|---|---|
| 数量 | 1 张，手写 | 数十张，生成 |
| 来源 | 人工调整，入库 | `tools/map_gen/` 按种子生成 |
| 用途 | 答辩、录像、回放样例 | 训练与评估 |

生成器必须是**约束式的，不是自由噪声**：

- **固定**：地图尺寸（`size`）
- **随机**：城区半径、集结点数量与选哪几条边、城内资源构成、外部资源簇数量与
  大小、野外散布（森林斑块 / `Rock` 团块 / 水域与桥）、预置建筑、初始缺口的位置与数量
  （「初始墙段的残血分布」曾在此列，2026-09-01 起区间钉死 [1.0, 1.0]，见 2.3）
- 每张生成结果跑第 8 节校验器，不通过就丢弃重生成
- `map_id` 编码生成种子，使任何一局训练都可复现

固定项若也随机，训练数据里就混入了「这张图的城墙特别短」这类与策略无关的方差，
波次课程的信号会被淹没。

### 9.1 生成器已落地：`tools/map_gen/generate.py`

上面五条要求逐条对应到实现：固定项读 `thresholds.json` 的 `generator` 段、
随机项走 `random.Random(seed)`、每张图跑完整校验器、不通过换种子重试
（上限 `max_attempts`，使失败**有界且可报告**）、`map_id` 编码种子（`gen_XXXXXXXX`）。

```bash
py tools/map_gen/generate.py -n 12 --seed 1            # 干跑，只校验不落盘
py tools/map_gen/generate.py -n 12 -o maps/train       # 落盘
py tools/map_gen/generate.py -n 1 --preview            # 打成 ASCII
```

**布局（2026-08-31 重构：取缔走廊、城圈改城墙实体；2026-09-01 续：
池图 72 → 144、水域落地、散布改圆形斑块；2026-09-01 第三次调整：
恢复 #117 的小撮散布与折线森林带——合 #119 解决冲突时被其直线版/
大团参数整体覆盖过一次）**：城圈是方环上的一整圈
`initial_walls` 实体——一对相对城门 + 1–2 个设计缺口（§2.3），其余
环格全是 `Wall`（§2.2；**当前全部满血**，见 2.3）；`Rock` 是城外随机散布的
**圆形小团块**（3–6 格），与森林小簇（3–5 格）一起撒在城圈之外（避开墙/门
八邻与集结点八邻，半径随格数取 isqrt、选中概率随到圆心距离平方衰减，
各簇中心有最小间距且不得贴着已有 Forest/Rock 生长）；
城内放两片 3 格 L 形森林、资源**恰好 1 石/1 金/1 木**（2026-09-01 第二次调整，
从「2 石/2 金/1–2 木」收紧，试玩反馈「城墙内资源点太多」）、预置建筑
（塔贴城门/缺口内侧、兵营在堡垒附近、**伐木场/采石场/金矿场各一、精确踩匹配
资源点**——金矿场是 2026-09-01 加的：试玩反馈「完全不会刷新金矿」，数据里
金矿一直在，是没有任何地标把它标出来，见 4.4.0），堡垒周围 3 格净空——见
4.4.0）；城外资源簇 7–8 个、每簇 4–5 点、簇距上界随城区半径走（不随地图
边长走，见 §2.2「城外资源簇」一条）；点直接落在簇心切比雪夫半径内的空闲
`Plain` 格上（不再要求森林带连通到地图边界，2.1 那条结构约束已移除，见该节），
簇内点间距 ≥2、每簇 ≥2 种类型、全局城外金 ≥2、
簇距随离城距离递增；**水域**：湖泊（圆瓣团块，2–4 片）+ 一条边到边的河
（2–3 格宽）+ 垂直贯穿水宽的 `Bridge` 若干，每个水体落地后自查连通性、
失败整片撤销；集结点周围铺 `no_build` 环——**那是第 14 条那条结构约束的
直接实现手段**，用禁建区表达「静态建筑的视野不得覆盖集结区」，而不是靠调小视野半径。

**两处刻意的留白**，都在 `generate.py` 的模块 docstring 里逐条写着理由：
集结点八邻不放 `Forest` / `Rock`（于是第 10 条在生成图上恒过）；
城圈是方环所以集结点最多四条边各一个。
（原先还有第三处「不生成 `Water` / `Bridge`」，**2026-09-01 作废并落地**——
当时的担心是画错一格切断通路会让丢弃率飙升而原因难查；现在每个水体落地后
自查「keep 可达 + 无 Plain 孤岛」、失败整片撤销，那条担心不再成立。
第 12、13 条自此在生成图上做实事。）

**`--preview` 不是玩具。** 生成器的真实 bug（重构前：林地走廊一株森林都没撒、
`defile` 的夹壁起点错了一个半径）都**不会让任何检查变红**：校验器查的是「地图
合不合法」，而它们是「设计语言没体现出来」。合法但无趣的图会静默进池子。
`selftest.py` 因此有 doc 层的结构断言（城圈完整性、城门相对、城外金矿、
资源簇不变量）——「看产出」被做成了断言就不会漏。

**§9 那张手写演示地图已做（2026-08-31）**，但它的角色比本节原先设想的窄：
`Ram` 速度不自洽那处矛盾已在 `_FINDING_size_vs_ram_speed` 里解决（size 曾取 72，
**2026-09-01 用户拍板池图放大到 144**——72 相对城内大小太小；第 5 条占比区间
随之放宽为 [0.10, 0.75]，推导与实测都记在那条 `_FINDING` 的续篇里），
不再是阻碍；而**实战默认地图不再是"一张固定图"**——组内讨论后认为，玩家真正打开
游戏时应当每次不一样才有持久可玩性，「给答辩/录像用的一张固定图」是另一个目标，
两者不该混在一起，见下面「两份产物，两个用途」。

### 两份产物，两个用途

| | `game/data/maps/pool/`（12 张，**144×144**，2026-09-01 起） | `game/data/maps/reference_border_keep_01.json`（1 张，56×56） |
|---|---|---|
| 用途 | **实战默认地图来源**——不给 `--map` 时随机选一张 | 答辩/录像用的固定参考图，与池子同一套生成逻辑、种子固定 |
| 生成方式 | `generate.py`，种子段 1001 起 | `tools/map_gen/build_reference_map.py`（**权威来源脚本，不手改 JSON**），56×56、城圈半径 8、固定种子 `20260831` |
| 是否被随机选中 | 是 | **不会**——不在 `pool/` 目录下 |
| 通过校验器 | 全部已实现检查（第 9 条阻塞，见下），已实测 | 同上 |

**随机地图池比原设想的更"随机"**：2026-08-31 那轮把 `city_radius`、走廊数量与
种类、走廊口宽度、城内资源点配比、外部资源簇数量都从固定值松开成了区间/候选域；
同月晚些时候的重构**进一步取缔了走廊概念本身**——城外地形改为完全随机散布，
参考图的设计语言（城内森林/金矿、预置建筑、资源簇硬保证）适用到**所有**随机图
（见 `tools/map_gen/thresholds.json` 的 `generator` 段与其 `_note_*`）。
"实战地图"与"训练地图集"从此是两个不同的消费者，前者要的是玩家层面的持久可玩性，
原先"固定项不能随机，否则训练信号被污染"这条顾虑只对后者成立。`size` 仍保持固定，
理由见 `generate.py` 模块 docstring 与 `thresholds.json` 的
`_FINDING_size_vs_ram_speed`。

**C++ 侧的接线**：`game::asset_paths.hpp` 的 `kDefaultMapRel`（单文件）换成了
`kMapPoolDirRel`（目录），`assets_under()`/`discover_assets()` 各加一个
`rts::Rng&` 参数、从池子里随机选一张。唯一的真随机源（挂钟播种）在
`render/src/main.cpp` 里，理由是 `tools/check_determinism_bans.py` 只扫
`rts_core/` 与 `game/`——把随机源放在扫描范围之外、且这本来就是外壳层的选择，
不是仿真的一部分。

## 10. 尚未决定

**全部数值继续保持待定**，等基本机制完善、速度与造价标定后再定。本文的职责是给出
它们必须满足的结构约束（第 2 节）与校验条件（第 8 节），而不是给值。具体地：

- **集结点数量**（生成器占位 3–4；2026-08-31 起与走廊种类数解耦——走廊概念
  已取缔，见 2.2）—— 待定
- 城区半径、走廊口宽度与数量、地图边长、`Forest` 覆盖率上限、初始缺口数量 —— 全部待定
- 第 3 节的行军占比区间（占位 10–20%）本身也待定，须按战术层跑通后的真实 episode 长度复核
- 第 8 节校验器的一切阈值待定，应集中在一份配置里，不要硬编码。
  **那份配置已经落地**（`tools/map_gen/thresholds.json`），**值仍然全部待定**——
  这一条从「没有归口」变成「归口有了、等标定」，而含数值的第 1、2、5、14 条
  因此都已实现。见第 8 节开头那段。**标定时第一件要解决的是那处
  `Ram` 速度与 §3 量级表差一倍多的矛盾**（同上）
- ~~**2.1 采用候选甲还是候选乙**~~ **曾定：两个都不采用，改用森林带 + 到边界的森林
  连通条件**（随 #23 定案）。**2026-09-01 这条结构约束整节移除**（森林带在长
  距离上仍读成直线，团队认为反龟缩收益不值这个视觉代价），第 8 节第 7 条随之
  废除，详见 2.1 的订正——这一项因此不再是「待定→已定→已实现」，是
  「待定→已定→实现过→主动撤回」，记在这里防止有人只读到中间那句
- ~~**缺一条「地图必须有 `outer` 资源点」的检查**~~ **已定并已实现，即第 21 条**——
  当时第 7 条在一个 `outer` 都没有的地图上空过，第 6 条只查 `inner` 侧三种齐全，
  于是一张把所有资源都放在城里的地图能通过全部检查，而那等于砍掉「部分资源点
  在墙外」（CLAUDE.md 不能砍的两个机制之一）。**第 7 条 2026-09-01 废除后，
  第 21 条独自扛起这条要求**，不受影响，仍然实现且仍然生效
- ~~若采用候选乙：校验器缺一条「前哨点上摆不下足以围住该簇的塔」~~ **不再需要**——
  没有前哨点这个概念了
- 石材 / 木材 / 金币在城内外两层的具体配比（倾向见 4.4）
- 演示地图的具体布局（本文只规定它必须满足第 8 节）
- ~~**第 8 节第 16..20 条尚未落地**~~ ~~第 16、17 条已落地……仍未落地三条~~
  ~~第 16、17、19、21、22 条都已落地……仍未落地的只剩两条~~
  **第 16 到 22 条（除已移出本表的第 20 条）全部落地，包括 2026-08-31 补上
  `unlock_wave` 字段后落地的第 18 条。仅剩第 9 条**：
  - **第 9 条卡在城区**。它有两个阻塞，`thresholds.json` **只解开了一个**
    （阈值 `front_length_min/max` 已经有归口）；另一个是「正面」的定义依赖城区，
    而 `grid.derive_city_area()` 对**任何留了缺口的合法地图**都抛
    `CityAreaUndecidable`——第 8 条恰恰强制要求留缺口。所以它不是「实现没写」，
    是**规范缺一个字段**（城区 mask），与第 18 条同类

  ~~第 19 条等 §6 表决~~ 已表决通过并落地（issue #58 / #62）。
- **`keep` 与资源点重合值不值得单开一条检查，未定。** 第 16 条只查各自落在可建造格上，
  第 17 条按 §8.1 字面只管墙与集结点，所以「堡垒盖在金矿上」现在没人查。
  它是不是错的取决于 1c 怎么处理「同一格上两座建筑」，所以**先不查**——
  按第 11 条那条纪律，扩大检查范围会拒掉今天合法的地图，属规范该不该改的问题
- **第 17 条只查墙不在 `Rock` / `Water` 上，没有扩大成「墙必须落在可建造格上」。**
  后者更严（会连 `Forest` 一起拒）。**原有一条支持扩大的理由**——2.1 的森林带靠
  「森林不可建造」封住围墙，地图作者若把 `initial_walls` 直接摆在带子上，
  那条保证就被绕过去了——**随 2.1 那条结构约束移除（2026-09-01）一并作废**，
  现在没有任何理由要求扩大，维持原样即可，不再是「未定」
- ~~**`resources` 缺一个「解禁波数」字段。**~~ **已实现（2026-08-31）**：字段名
  定为 `unlock_wave`，见 6.2 说明与第 18 条。`game/data/demo_skirmish.json`
  与 `game/testdata/fixture_min.json` 已补上（inner=1，outer 按到 keep 的
  切比雪夫距离排序给 2 起递增的值）
- **占位产图路线（`gen.py`）的去留** —— 它与 3D 路线的元数据 schema 与文件名都已分叉
  （见 7.2），「无 Blender 也能跑前端」这个价值实际已失效。要么让它跟上，
  要么明确宣布放弃。属 `tools/sprite_gen/` 范围，需小组点头
