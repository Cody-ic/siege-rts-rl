# ---- C++ / CMake ----
build/
# Makefiles / Ninja 是**单配置**生成器，构建类型烤在缓存里，所以 Linux 上想同时留
# Release 与 Debug 只能一个配置一个目录，`build-Release/` 是最自然的命名。
# Windows 用 VS 生成器是单目录多配置，因此这个需求在本地开发时不会出现——
# 这一条是首次 Linux/GCC 构建时才暴露的。
build-*/
# 兜底：**任何** build 开头的目录。上面两条是白名单，而白名单只挡住了想得到的
# 命名。真事：临时开了一个 `build33/` 来验证另一条 PR 的 ctest，`build/` 与
# `build-*/` 都不匹配，`git add -A` 一次就带进去 902 个文件。
# 与「ctest 标签清单是白名单，写错抓得到、漏登记抓不到」是同一种失败。
build*/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
CMakeUserPresets.json
compile_commands.json
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# ---- 训练产物（体积大，不入库）----
runs/
logs/
checkpoints/
wandb/
*.pt
*.pth
*.onnx
*.tfevents.*

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
.DS_Store
*.swp

# ---- 精灵生成 ----
# out_3d/ 的成品精灵**入库**：交付清单要求「源代码包括输入文件和输出文件」，
# 且"可重建所以不入库"在此不成立——重建需要装 Blender 并配环境，成本不低。
# 体积也可忽略（236 张共约 14 MB，与已入库的 .glb 素材同量级）。
tools/sprite_gen/out/
tools/sprite_gen/dl/
