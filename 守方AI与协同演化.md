# 守方 AI 与协同演化

> **状态：方向已通过，结构已定；第 9 节仍有 4 项待定。**
>
> 提案经小组讨论通过（#15 合入，讨论见 #26 议题 A6 / A7）。**通过时改了一处结构性主张**：
> 原稿主张守方镜像攻方、同样分两层且战术层与攻方同构，现改为
> **守方只在决策层上 RL，执行层是参数化脚本**——第 2 节整节重写，
> 第 3、4、6、7、10 节随之调整，改动处均标注了原稿写法与推翻它的理由。
>
> 生效的结论（第 2、2.5、3、6 节）已并入 `CLAUDE.md`；实现细节留在本文。
>
> **本文不含任何已标定数值**，出现的数字都是量级估计。

## 1. 起因：训练时的守方是谁，此前没有定义

`CLAUDE.md` 写的是「玩家为守方，RL 控制的 AI 为攻方」。但训练时没有玩家——
那几百万局里，墙是谁建的、部队是谁调的、经济是谁经营的，**文档中从未规定**。

若守方是一个固定脚本，有两个后果：

- **攻方会过拟合到该脚本的破绽。** 头号评估指标「AI 是否发现并利用已有缺口」随之可疑：
  它是学会了找缺口，还是背下了那个脚本总在哪留缺口？
- **「策略池两两对战胜率矩阵」的证据强度下降**，因为整个矩阵都在同一个守方下测得，
  非传递性可能只是该脚本的产物。

已决定：**守方在训练时同样由 RL 控制**。这不只是补洞——它把项目从"一个智能体"
变成"一对协同演化的智能体"，评估材料与演示形态都随之变化（见 `演示与可视化清单.md`）。

**注意这不改变游戏形态**：正式游玩时守方仍是人类玩家。守方 AI 的用途有三个——
攻方的陪练、`AI vs AI` 演示模式、以及作为平衡测试工具（第 8 节）。

### 但「守方由 RL 控制」不等于「守方每个单位都由 RL 控制」

这个区分是第 2 节的全部内容，**必须在这里就讲清楚，否则本节读起来会与它矛盾**：
上面刚说固定脚本会让攻方过拟合，而第 2 节却让守方的执行层是脚本。

两者不矛盾，因为**过拟合的风险在于对手的决策模式固定，不在于执行细节固定**：

| | 谁来定 | 会不会导致攻方过拟合 |
|---|---|---|
| 建哪、修哪、兵派去哪（**决策**） | **RL，会演化** | 不会——这一半是对抗性的 |
| 射程内自动开火、被贴身后撤（**执行**） | 脚本，固定 | 不会——**真人玩家的单位执行同样由规则决定** |

本节担心的「固定脚本守方」指的是**整个守方都写死**，那样攻方面对的局面分布是静止的。
而新方案里局面分布仍由一个会演化的 RL 决策层生成。

反过来，让执行层也变成 RL 会引入一个**新的**问题（超人类微操 → 第 6.2 节），
它比这里担心的问题更难对付。两端的取舍见第 2.5 节。

## 2. 守方只做决策层：与攻方结构性不对称

> **本节在小组讨论后整节重写。** 原稿主张「守方镜像攻方、同样分宏观 / 战术两层、
> 战术层观测与动作与攻方同构」，理由是工程复用。**这一条已被推翻**，
> 讨论记录见 #15 / #26 议题 A6、A7。原稿的排期（第 10 节）也随之改写。

守方的决策同样分两个时间尺度，但**只有上面那一层上 RL**：

| | 攻方 | 守方 |
|---|---|---|
| **决策层**（一波若干次） | 兵种配比、集结点选择 | 建造 / 经济 / 造兵 / 维修 / 部队调度 |
| **执行层**（每 4–8 tick） | **逐单位 RL**（共享策略网络） | **参数化脚本**（见第 2.5 节） |

也就是说，攻方 RL 控制的是**每一个单位**，守方 RL 控制的是**玩家会下的那些指令**，
守方单兵不由 RL 驱动。两侧结构不对称，而这是有意的。

### 为什么不对称才是对的

**一、它匹配守方 AI 的定位。** `CLAUDE.md` 写死「正式游玩时守方是人类玩家」，
所以守方 AI 是**人类的替身**。而人类守城做的是「建什么、放哪、兵调到哪段墙」，
不是逐 tick 决定每个弓手往哪走。**守方 AI 的动作空间应当等于玩家实际能下的指令**，
多出来的那部分能力没有对应的真人来源。

**二、它从结构上消除了第 6.2 节那个坑**——而那是本方案三个坑里最要命的一个。
原稿担心守方 AI 打出人类做不到的操作（完美补口、逐 tick 微操）、
攻方过拟合到这些操作、真人上手时反而显得笨；原稿的对策是行为克隆（第 7 节），
成本不低。**现在守方打不出逐 tick 微操，因为根本没有那个动作空间。**
这是 `CLAUDE.md`「结构封死，而非数值劝退」用在训练设计上。

**三、训练成本不再翻倍。** 原稿第 6.3 节把「两倍采样」列为实打实的代价。
守方一波决策若干次，与逐单位每 4–8 tick 相比，样本量差一到两个数量级。
这直接缓解第 9 节问题 6（GPU 机时够不够），而那是被列为「影响排期」的两项之一。

### 要认下的代价

**原稿「复用同一套动作枚举与观测打包」的便宜没有了。** 守方需要一套独立的动作空间
（第 3 节的槽位列表 + 因子化动作头）与独立的观测（全局摘要，而非 K×K 局部视野）。

好消息是**这份设计原稿第 3 节已经做出来了**，所以它是已知成本而非未知风险。
真正的代价在排期：原稿把宏观层放在最后一个阶段，**现在它是守方唯一的 RL 层，
必须提前**，见改写后的第 10 节。

同时失去的还有那条消融实验（「用同一个网络同时控制攻守双方」）——
两侧不再同构，这个实验做不了了。它本来也只是附带好处。

### 这不是「守方战术层永不上 RL」

按 #26 议题 A7 的措辞，这条记作**「本学期不做」而非「永不」**。区别是实质的：
守方战术层若日后要加，架构与攻方同构、是增量改动，没有必要现在就立一条
日后要专门开 PR 撤销的规则。**现在不做的理由是上面三条，不是它不可行。**

## 2.5 脚本守方不是 RL 的廉价替代品

这一节是上一节的支柱。**若把脚本理解成「RL 还没做好之前的临时凑合」，
那么第 2 节的取舍就完全讲不通了**——所以它的职责要单独讲清楚。

### 先澄清一个误解：微操对 RL 一点也不难

微操恰恰是多智能体 RL 最成熟的领域。SMAC 那一整套 benchmark 做的就是微操，
**风筝是其中最经典的涌现行为**——射程差场景下，标准算法都能自己学出「打一下退一步」。
`CLAUDE.md` 参考资料里的 arXiv 1804.00810 正是参数共享 + 课程迁移的微操 RL。

更直接的反证在本项目内部：**我们本来就指望攻方涌现出集火、包夹、散开躲 AOE**
（`CLAUDE.md`「涌现行为录像」）。既然相信攻方能涌现微操，就没有理由认为守方 RL 学不会。

**所以「守方不上战术层 RL」的理由从来不是它学不会，而是下面这条。**

### 真正的理由：我们要的是一个分布，而 RL 只给一个点

攻方最终要打的是**人**，而人的微操强度有限、且不稳定。目标因此不在两端：

| 守方执行层 | 微操强度 | 可控性 |
|---|:-:|---|
| 无微操脚本 | 0 | —— |
| **参数化脚本** | **可调**（后撤概率、反应延迟 tick 数、集火阈值） | **可控** |
| RL 守方 | 一路优化到超人类 | **不可控** |

RL 只会收敛到一个点——最优点。而攻方需要见到的是**覆盖真人从生疏到熟练的整个区间**。
把脚本参数随机化、多个脚本轮换，给出的正是这个分布，这就是 domain randomization
的标准用法。**这是脚本能做而 RL 做不到的事，不是反过来。**

反过来说，若守方用 RL 学出完美风筝，攻方就会去学「如何应对完美风筝」——
那正好又落回第 6.2 节那个坑。**两端都是错的，中间那一段只有脚本给得出。**

### 脚本要写到什么程度：一条可操作的判据

`CLAUDE.md` 的克制二部图里写着：

> `Ghoul ──► Archer 拉扯 + Tower 齐射`

**「拉扯」这个词就在克制表里。** 它不是我们希望涌现的东西，是**已经写死的设计意图**。
守方脚本若不会拉扯，这条克制关系在训练中根本不存在，
攻方也就永远学不到「`Ghoul` 不该单独去追 `Archer`」。

由此得到判据，省掉「脚本该写多复杂」这个凭感觉的问题：

> **克制二部图里已写死的战术行为 → 进脚本**（它们是设计意图，不该指望涌现）。
> **图里没写的（包夹、佯攻调虎离山、择机突袭）→ 留给 RL 涌现。**

按这条数，要实现的其实很少——守方战斗单位只有三种（`Scout` / `Mason` 无战力）：

| 单位 | 脚本行为 | 来源 |
|---|---|---|
| `Archer` | 被近战贴近时后撤保持射程（拉扯） | 克制表 `Ghoul ──► Archer 拉扯` |
| `Spear` | 堵在缺口不追击，保持枪阵密度 | 克制表 `Knight ──► Spear（开阔地）` |
| `Ranger` | 突袭目标后撤离，不与 `Knight` 对冲 | 克制表 `Ranger ──► Knight` |

每一条都配一组参数（反应延迟、触发距离、执行概率），随机化后即得到一族风格不同的守方。

### 残留的劣势，不要说成没有

脚本控得住微操的**强度**，控不住**种类**——它只会我们写进去的那几种，
而 RL 可能涌现出我们没想到的打法。

对本项目这个代价有限：守方战斗单位三种、各自的战术模式 `CLAUDE.md` 已经写死，
而真人玩家同样不会发明出只有网络才找得到的打法。
**但这是一个真实的取舍**，若日后发现攻方明显吃亏于某类没被脚本覆盖的守方行为，
那就是重开第 2 节讨论的信号。

#### 但这一条**不涉及攻方的涌现**，那才是卖点，而它一点没损失

这个区分要写下来，因为上面那段单独读会让人以为「我们为了省事放弃了涌现」：

| | 谁驱动 | 会不会涌现出我们没想到的打法 |
|---|---|---|
| **攻方**（每一个单位） | **逐单位 RL，全程如此** | **会，而且我们指望它会** |
| 守方执行层 | 参数化脚本 | 不会——这是上面那条劣势 |

`CLAUDE.md` 列为核心论证材料的「涌现行为录像（包夹、佯攻调虎离山、集火、
空军择机突袭、点杀维修工）」**全部在攻方一侧**，因此完全不受第 2 节影响。
本节的取舍只让**守方**的行为种类受限于我们的想象力。

而**「AI 打出了我们没想到的打法」本身就是最好的答辩素材之一**——它比任何胜率曲线
都更能说明「这是学出来的，不是我们写死的」。所以：

> **约定：训练期发现意料之外的涌现行为时，当场记录并同步给全组**，
> 不要等到整理答辩材料时再回忆。手段是现成的——确定性回放可以把那一局固定下来
> （`演示与可视化清单.md` 第 9 节问 4 建议标准片段全部用回放固定），
> 而 `演示与可视化清单.md` 第 3 节的显著性图可以看出它当时在关注什么。

**顺带：脚本被攻方 RL 找到破绽，同样属于这一类发现。** 它表面上是上面那条劣势的
兑现，但「我们写的守方脚本被 AI 找出了漏洞并反复利用」讲出来是有说服力的——
前提是**发现得到**，所以它也归上面那条约定。

## 3. 守方决策层的动作空间必须裁剪

（**术语**：守方的「决策层」对应攻方术语里的「宏观层」。第 2 节改用「决策层 / 执行层」
是因为守方两层的驱动方式不同——只有上层是 RL——而攻方两层都是 RL。）

**这一节的分量在第 2 节改写后上升了：它不再是「本方案里最难的那部分」，
而是守方 RL 的全部。** 守方策略的好坏完全取决于这个动作空间设计得对不对，
没有一个战术层可以兜底。

难点未变：攻方宏观层是「给定预算选兵种配比」，bandit 尺度；守方决策层要**在地图上选位置**。

朴素做法是「地图上任选一格」，96×96 = 9216 个动作，学不动。

### 做法：槽位列表 + 因子化动作头

地图预先给出**可建造槽位列表**（见 `地图与场景设计.md`）：城圈上的墙段位、
塔位、资源点位。动作因子化成两个头：

| 头 | 取值 | 说明 |
|---|---|---|
| 操作类型 | 建墙 / 建塔 / 建防空 / 建采集 / 维修 / 造兵 / **调兵** / 跳过 | ~8 |
| 目标 | 槽位 index；「造兵」时复用为兵种 index | ~60 |

两个头相加约 68 个输出，而不是相乘的 400+。一波之内守方会连续做多次决策，
因此决策层是一个**短序列**，而非单次决策。

**「调兵」是第 2 节改写后新加的**，原稿把部队调度放在战术层，而现在没有战术层了。
它同时也是波次进行中唯一的动作来源：建造类操作在波次中受「时间与暴露」限制
（`CLAUDE.md`「波次进行中不禁止任何资源的使用」），而调兵是即时生效的。

它带来一个原稿没有的维度——**调哪一支部队**。两个候选，**尚未定**：

| 候选 | 做法 | 代价 |
|---|---|---|
| 甲 | 加第三个头「编队 index」 | 动作头 +1，编队数是待定数值 |
| 乙 | 复用目标头：先选编队、再选去处，两步动作 | 序列变长，但动作空间不变 |

倾向乙——它与「决策层本就是短序列」一致，且不引入新的待定数值。
定这条之前不影响本文其余部分。

### 与地图设计的耦合，是个便宜

`地图与场景设计.md` 里「天然岩壁承担大部分周长，只有走廊口需人工设防」
这条约束，本来是为了在单位数上限下保证攻击密度而设的。
它**顺带把守方的槽位数压下来一个量级**，直接决定了上面这个动作空间可不可学。

两个独立的设计目标在此收敛，这一点值得写进报告。反过来也要留意：
**若日后有人放宽岩壁比例、加长人工设防正面，会同时破坏攻击密度与守方动作空间**，
这两件事是一起的。

（走廊口的数量与宽度、以及由此决定的槽位数，均属待定数值，
见 `CLAUDE.md`「关于数值」。本节论证不依赖它们的具体取值。）

### 必须做动作掩码

买不起的选项、已占用的槽位、无需维修的完好墙段，一律在 logits 上屏蔽。
不做掩码的话，agent 会把大量样本浪费在学习"什么买得起"这件规则本身就能给出的事上。

## 4. 守方观测

守方**没有逐单位观测**（第 2 节：执行层是脚本，脚本直接读世界状态、不经网络）。
所以这一节只剩决策层需要的全局摘要，建议：

- 城圈各槽位的状态（有无建筑、类型、血量比例）——即上一节槽位列表的逐项状态
- 三种资源的当前存量与上一波收入
- 波数、上一波的战损与破口位置
- **自己的侦查记忆图**（下一节强调）
- **各编队的位置与兵力**——「调兵」动作（第 3 节）没有它就无从决策。
  粒度是编队而非单位，与动作空间的粒度一致

**守方同样只能看到自己的迷雾状态。** `CLAUDE.md` 已经规定「战争迷雾对玩家和 AI
对称生效」，守方 AI 不得读取攻方的真实编成——否则「攻方分兵佯攻」这一整套机制
在守方 AI 身上失效，而 `AI vs AI` 演示的说服力也随之归零。

## 5. 协同演化：对手池，而不是只打当前版本

若两侧只与对方的最新版本对练，会出现**策略循环**：A 克 B、B 克 C、C 克 A，
两边相对水平互有胜负，绝对水平原地踏步，且不断遗忘如何击败早期策略。

对策是 **league / 对手池**，即 `CLAUDE.md` 参考资料里已经引用的 AlphaStar 做法：

- 两侧各自保留历史快照，按固定间隔入池
- 训练时对手从池中采样，而非固定为最新版本
- 采样分布偏向"能打赢当前版本"的历史对手，以保持压力

这套机制同时是评估材料的来源：池内两两对战的胜率矩阵，正是 `CLAUDE.md`
列为核心论证材料的「非传递性环」那张图。**协同演化让这张图从"攻方策略之间"
扩展为"攻守双方各代之间"，证据强度显著提升。**

## 6. 三个坑与对策 —— 第 2 节改写后只剩一个半

原稿列了三个坑。**6.1 不变；6.2 的大部分与 6.3 因守方不再有逐单位动作空间而消失。**
两条都保留题面，因为「为什么它不再是问题」本身就是第 2 节那个取舍的证据，
删掉之后后来者会以为它从未被考虑过。

### 6.1 难度错配：守方太强会饿死攻方的奖励信号

攻方预算严格是波数的函数（`CLAUDE.md` 硬性规定，不随表现调整）。
若守方过早变强，攻方每局都被打光、拿不到任何正奖励，训练直接失败。

对策是现成的：**波数即课程**。按波次分层采样，从低波次（守方弱、攻方预算低）
逐步放开。这条 `CLAUDE.md` 已经写了，只是此前只用于攻方。

### 6.2 非人类打法 —— **已被第 2 节结构性消除，保留此条是为了记录理由**

原文：守方 AI 的存在意义是陪练攻方，而**攻方最终要打的是人**。
若守方 AI 打出人类做不到的操作（完美补口、逐 tick 微操、反直觉的建筑摆法），
攻方就学会了针对这些操作，真人上手时反而显得笨。原稿称它是三个坑里最要命的一个。

**它现在大部分不成立了**，因为守方没有逐单位动作空间——「逐 tick 微操」不是被劝退，
是**做不到**。这正是第 2 节理由二。

剩下的一小半仍然成立，**不要连带认为整条已解决**：

- 决策层本身仍可能打出非人类节奏（例如每波都把预算花到最后一个铜板、
  在人类反应不过来的时刻精确调兵）。**对策仍是第 7 节的行为克隆**，
  它没有因此变得不必要，只是从「主要对策」降为「补充」
- 脚本执行层的参数若调得过强（反应延迟设为 0），一样会打出超人类微操。
  这条归第 2.5 节：**强度是参数，责任在标定，不在结构**

### 6.3 训练成本 —— **不再翻倍**

原稿：两个策略、两个对手池、两倍的采样。

第 2 节改写后守方只有决策层，一波决策若干次，与逐单位每 4–8 tick 相比
**样本量差一到两个数量级**。仍有两个对手池、两套快照，但采样成本远不到两倍。

这条连带影响第 9 节问题 6（GPU 机时够不够）——它是被 #26 列为「影响排期」的两项之一，
现在的量级估计需要按新结构重做。**注意 #22 已实测出被串行化的是 CPU 不是 GPU**
（A800 只用 14 MiB，吃满的是那 96 线程），所以这条要估的是 `BatchedEnv` 的吞吐，
不是显存。

## 7. 行为克隆：给守方一个"人类风格锚"

人类不玩攻方，但**人类恰好玩守方**。所以：

1. 队友玩若干局，记录下来
2. 用这些数据监督预训练一个守方策略（行为克隆）
3. 该策略作为**对手池中的固定成员**参与协同演化

这样攻方的对手池里始终有一个"打得像人"的守方，6.2 剩下的那一半被稀释——
而不是靠"希望协同演化自然收敛到人类打法"。

### 数据管线是免费的，而且第 2 节改写后**更便宜了**

**确定性回放本来就是硬性要求**——`CLAUDE.md` 把「确定性回放测试」列为核心测试形态，
回放文件本身就是「状态 + 指令流」的序列，正是模仿学习需要的 `(observation, action)` 对。
不需要额外做数据采集管线，只需要一个从回放重放观测的转换脚本。

**原稿最麻烦的那一步直接消失了。** 它写的是：

> 人类玩的是**整个守方**（同时指挥多单位），而策略网络是逐单位的。转换时要把
> 编队指令拆解到单位级，**这一步的正确性需要用回放比对验证**。

守方策略现在就是决策层，而**人类的操作本来就是决策层的**——玩家点的就是「建这里」
「修那段」「这队去堵缺口」。`(observation, action)` 对**直接对应**，
不需要拆解、不需要额外验证。原稿整条管线里唯一需要专门验证正确性的步骤没有了。

这也顺带说明为什么行为克隆与第 2 节是配套的：**守方 RL 的动作空间等于玩家的指令集，
所以人类数据可以直接喂**。若守方是逐单位 RL，人类根本没有对应的数据可产。

仍需注意的一点：

- 人类的操作频率低且不均匀。转换时需要把"无操作"也作为合法动作填充，
  否则数据分布与在线交互时不一致。决策层本就有「跳过」这个动作（第 3 节），
  正好承接它——这在原稿的逐单位设定下是没有的。

### 附带的演示价值

"这个守方 AI 是学我们组长的打法学出来的"——见 `演示与可视化清单.md`。

## 8. 协同演化顺带就是自动平衡测试

若某个单位数值超模，两侧在均衡状态下都会收敛到刷它。因此**观察均衡时的兵种配比分布，
就能读出哪里不平衡**，比人工对着数值表推演有效得多。

这与"用黑箱优化标定数值"是互补而非重复的两步：

| | 职责 |
|---|---|
| 黑箱优化（CMA-ES / 贝叶斯优化） | 在 1v1 成本产出矩阵上搜出候选数值 |
| 协同演化 | 验证该组数值在完整博弈的均衡下站不站得住 |

前者快但只看局部对抗，后者慢但看全局。先用前者收窄范围，再用后者验收。

## 9. 需要小组拍板的问题

**1 与 7 已定（#26 议题 A6 / A7），下面保留题面与结论；2 因它们而变简单；
3–6 仍待定。**

1. ~~**守方宏观层是否也上 RL，还是前期先写死脚本？**~~
   **已定：第一阶段用多个脚本轮换，第二阶段决策层上 RL。**
   注意结论与原稿的倾向**不同**——原稿建议「守方先只做战术层 RL，宏观层用脚本」，
   现在是反过来的：**RL 上的是决策层，战术层永远是脚本**（第 2 节）。
   「多个轮换」比原稿的「随机化参数」更强，理由见第 2.5 节：
   我们要的是覆盖真人水平区间的一个**分布**，而不是一个点。
7. ~~**守方战术层是否承诺永不采用逐单位 RL？**~~（#26 由 #23-6 并入本文）
   **已定：本学期不做，但不写成「永不」。** 措辞取自 #26 议题 A7——
   区别是实质的，见第 2 节末。
2. **守方奖励如何设计？** 倾向近似零和（守方奖励 ≈ 攻方奖励取负），
   偏离处需写明理由。但攻方有"击杀 `Scout` 的即时奖励"这类无对称项，
   完全零和做不到，需要讨论偏离到什么程度可接受。

   **这条因第 2 节而降了一档难度**：守方只有波次级的决策，
   奖励就是波次级的（守住多少、损失多少），**不需要把功劳分配到单个单位**。
   逐单位 RL 才有 credit assignment 问题。
3. **对手池的规模与入池间隔**，以及采样分布。
4. **行为克隆需要多少局人类数据**才够，谁来玩、玩多久。
5. **`AI vs AI` 模式是否算正式功能**（进交付的可执行程序），还是仅用于答辩演示。
   → **归 `演示与可视化清单.md` 第 9 节问 2 回答**（#26 已判为与 #16-2 重复）。
6. 训练成本翻倍后，**GPU 服务器的机时够不够**，需要先估。
   → 题面已过期：**成本不再翻倍**（第 6.3 节），且 #22 实测出被串行化的是
   CPU 不是 GPU。重估的对象是 `BatchedEnv` 吞吐。

**另新增一条待定**（第 3 节引入）：**「调兵」动作如何表达「调哪一支部队」**——
加第三个动作头，还是拆成两步动作。倾向后者。

## 10. 排期与前置条件

**必须按顺序上，不要并行。** `CLAUDE.md` 已有的纪律「战术层必须先跑通，
不要两层同时上」在这里仍然适用——同时上的话训练不收敛时将无法定位来源。

**本节随第 2 节整体改写：原稿的阶段 2 是「守方战术层」，那一层不做了；
原稿放在最后的宏观层现在提前，因为它是守方 RL 的全部。**

| 阶段 | 内容 | 前置 |
|:-:|---|---|
| 0 | `rts_core` 接口 + 代码骨架 + 确定性回放 | 无。**仍是当前的瓶颈**（代码骨架已由 #20 落地，接口未定稿） |
| 1 | **一族参数化脚本守方**（第 2.5 节的三条行为 + 参数随机化） | 0。纯 C++，可与接口并行准备 |
| 2 | 攻方战术层 vs **脚本守方轮换** 跑通 | 1 |
| 3 | 守方**决策层**跑通（对手为阶段 2 的攻方；执行层仍是脚本） | 2 |
| 4 | 人类对局录制 → 行为克隆守方决策层 | 0（回放）+ 有可玩前端 |
| 5 | 协同演化 + 对手池 | 3、4 |
| 6 | 攻方宏观层 | 5 |

与原稿的差别有三处，都是第 2 节的直接后果：

- **脚本守方从「阶段 1 的一部分」升为独立的阶段 1。** 它不再只是首个对手，
  而是**执行层的最终形态**——攻方在全部阶段面对的都是它。
  它的质量因此直接决定攻方能学到什么，不能当临时物草草写完（第 2.5 节）
- **原阶段 2「守方战术层」删除**
- **末阶段只剩攻方宏观层。** 守方没有第二层要上

阶段 1 的脚本守方**无论如何都要写**这一点比原稿更强了：原稿里它是"会被协同演化
取代的临时物"的反面，现在它根本不会被取代。它同时也在 `README.md` 未认领工作表第 6 项，
且**不依赖 `rts_core` 接口定稿之外的任何东西**——接口一定，它就能写。

**空中单位仍按 `CLAUDE.md` 的原定安排，在地面核心循环收敛之后再加入。**
不要让"守方也是 RL"和"引入空中层"这两件事撞在一起。
