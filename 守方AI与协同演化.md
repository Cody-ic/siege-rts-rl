# 守方 AI 与协同演化

> **状态：提案，待小组讨论后再定稿。**
>
> 本文不是已生效的设计契约。它把「训练时守方也是 RL」这个方向展开成可讨论的具体方案，
> 并把其中的坑与代价一并列出。第 9 节是明确需要小组拍板的问题清单。
>
> 讨论通过后，第 2、3、6 节的结论应并入 `CLAUDE.md`；其余留在本文。
>
> **本文不含任何已标定数值**，出现的数字都是量级估计。

## 1. 起因：训练时的守方是谁，此前没有定义

`CLAUDE.md` 写的是「玩家为守方，RL 控制的 AI 为攻方」。但训练时没有玩家——
那几百万局里，墙是谁建的、部队是谁调的、经济是谁经营的，**文档中从未规定**。

若守方是一个固定脚本，有两个后果：

- **攻方会过拟合到该脚本的破绽。** 头号评估指标「AI 是否发现并利用已有缺口」随之可疑：
  它是学会了找缺口，还是背下了那个脚本总在哪留缺口？
- **「策略池两两对战胜率矩阵」的证据强度下降**，因为整个矩阵都在同一个守方下测得，
  非传递性可能只是该脚本的产物。

已决定：**守方在训练时同样由 RL 控制**。这不只是补洞——它把项目从"一个智能体"
变成"一对协同演化的智能体"，评估材料与演示形态都随之变化（见 `演示与可视化清单.md`）。

**注意这不改变游戏形态**：正式游玩时守方仍是人类玩家。守方 AI 的用途有三个——
攻方的陪练、`AI vs AI` 演示模式、以及作为平衡测试工具（第 8 节）。

## 2. 守方的分层结构：镜像攻方，不另起一套

守方的决策天然分两个时间尺度，与攻方的宏观 / 战术分层正好对应：

| | 攻方 | 守方 |
|---|---|---|
| **宏观**（一波一次） | 兵种配比、集结点选择 | 建造 / 经济 / 造兵 / 维修 |
| **战术**（每 4–8 tick） | 移动与攻击目标 | 部队调度、应急工事放置 |

**这个对应关系是刻意的**：动作枚举、观测打包、`BatchedEnv`、训练回路都能复用同一套，
而不是写两遍。守方战术单位的观测与动作**与攻方同构**（同样的 K×K 局部视野、
同样的 8 方向 + 攻击目标枚举），差异只体现在兵种 one-hot 与阵营 one-hot 上。

**但权重分开，不共享。** 架构相同、参数独立，理由是协同演化需要为两侧各自维护
历史快照池（第 5 节），共享权重会让快照失去意义。

> 附带好处：因为架构同构，「用同一个网络同时控制攻守双方」可以作为一次消融实验，
> 几乎不需要额外代码。

## 3. 守方宏观层的动作空间必须裁剪

守方宏观层是本方案里唯一比攻方难的部分：攻方宏观层是「给定预算选兵种配比」，
bandit 尺度；守方宏观层要**在地图上选位置**。

朴素做法是「地图上任选一格」，96×96 = 9216 个动作，学不动。

### 做法：槽位列表 + 因子化动作头

地图预先给出**可建造槽位列表**（见 `地图与场景设计.md`）：城圈上的墙段位、
塔位、资源点位。动作因子化成两个头：

| 头 | 取值 | 说明 |
|---|---|---|
| 操作类型 | 建墙 / 建塔 / 建防空 / 建采集 / 维修 / 造兵 / 跳过 | ~7 |
| 目标 | 槽位 index；操作为「造兵」时复用为兵种 index | ~60 |

两个头相加约 67 个输出，而不是相乘的 400+。一波之内守方会连续做多次决策
（花光预算为止），因此宏观层是一个短序列，而非单次决策。

### 与地图设计的耦合，是个便宜

`地图与场景设计.md` 里「天然岩壁承担大部分周长，只有四条走廊口需人工设防」
这条约束，本来是为了让 40 个攻方单位形成足够攻击密度而设的。
它**顺带把守方的槽位数从上百压到几十**，直接决定了上面这个动作空间可不可学。

两个独立的设计目标在此收敛，这一点值得写进报告。反过来也要留意：
**若日后有人放宽岩壁比例、加长人工设防正面，会同时破坏攻击密度与守方动作空间**，
这两件事是一起的。

### 必须做动作掩码

买不起的选项、已占用的槽位、无需维修的完好墙段，一律在 logits 上屏蔽。
不做掩码的话，agent 会把大量样本浪费在学习"什么买得起"这件规则本身就能给出的事上。

## 4. 守方观测

战术层与攻方同构，不再赘述。宏观层需要的是全局摘要，建议：

- 城圈各槽位的状态（有无建筑、类型、血量比例）——即上一节槽位列表的逐项状态
- 三种资源的当前存量与上一波收入
- 波数、上一波的战损与破口位置
- **自己的侦查记忆图**（下一节强调）

**守方同样只能看到自己的迷雾状态。** `CLAUDE.md` 已经规定「战争迷雾对玩家和 AI
对称生效」，守方 AI 不得读取攻方的真实编成——否则「攻方分兵佯攻」这一整套机制
在守方 AI 身上失效，而 `AI vs AI` 演示的说服力也随之归零。

## 5. 协同演化：对手池，而不是只打当前版本

若两侧只与对方的最新版本对练，会出现**策略循环**：A 克 B、B 克 C、C 克 A，
两边相对水平互有胜负，绝对水平原地踏步，且不断遗忘如何击败早期策略。

对策是 **league / 对手池**，即 `CLAUDE.md` 参考资料里已经引用的 AlphaStar 做法：

- 两侧各自保留历史快照，按固定间隔入池
- 训练时对手从池中采样，而非固定为最新版本
- 采样分布偏向"能打赢当前版本"的历史对手，以保持压力

这套机制同时是评估材料的来源：池内两两对战的胜率矩阵，正是 `CLAUDE.md`
列为核心论证材料的「非传递性环」那张图。**协同演化让这张图从"攻方策略之间"
扩展为"攻守双方各代之间"，证据强度显著提升。**

## 6. 三个坑与对策

### 6.1 难度错配：守方太强会饿死攻方的奖励信号

攻方预算严格是波数的函数（`CLAUDE.md` 硬性规定，不随表现调整）。
若守方过早变强，攻方每局都被打光、拿不到任何正奖励，训练直接失败。

对策是现成的：**波数即课程**。按波次分层采样，从低波次（守方弱、攻方预算低）
逐步放开。这条 `CLAUDE.md` 已经写了，只是此前只用于攻方。

### 6.2 非人类打法：攻方可能过拟合到"人类不会那么打"的守方

这是三个坑里最要命的一个。守方 AI 的存在意义是陪练攻方，而**攻方最终要打的是人**。
若守方 AI 打出人类做不到的操作（完美补口、逐 tick 微操、反直觉的建筑摆法），
攻方就学会了针对这些操作，真人上手时反而显得笨。

对策见第 7 节。

### 6.3 训练成本翻倍

两个策略、两个对手池、两倍的采样。这是实打实的代价，排期上必须承认（第 10 节）。

## 7. 行为克隆：给守方一个"人类风格锚"

人类不玩攻方，但**人类恰好玩守方**。所以：

1. 队友玩若干局，记录下来
2. 用这些数据监督预训练一个守方策略（行为克隆）
3. 该策略作为**对手池中的固定成员**参与协同演化

这样攻方的对手池里始终有一个"打得像人"的守方，6.2 的过拟合被结构性地稀释——
而不是靠"希望协同演化自然收敛到人类打法"。

### 数据管线是免费的

**确定性回放本来就是硬性要求**——`CLAUDE.md` 把「确定性回放测试」列为核心测试形态，
回放文件本身就是「状态 + 指令流」的序列，正是模仿学习需要的 `(observation, action)` 对。
不需要额外做数据采集管线，只需要一个从回放重放观测的转换脚本。

需要注意的两点：

- 人类的操作频率远低于每 4–8 tick 一次。转换时需要把"无操作"也作为合法动作填充，
  否则数据分布与在线交互时不一致。
- 人类玩的是**整个守方**（同时指挥多单位），而策略网络是逐单位的。转换时要把
  编队指令拆解到单位级，这一步的正确性需要用回放比对验证。

### 附带的演示价值

"这个守方 AI 是学我们组长的打法学出来的"——见 `演示与可视化清单.md`。

## 8. 协同演化顺带就是自动平衡测试

若某个单位数值超模，两侧在均衡状态下都会收敛到刷它。因此**观察均衡时的兵种配比分布，
就能读出哪里不平衡**，比人工对着数值表推演有效得多。

这与"用黑箱优化标定数值"是互补而非重复的两步：

| | 职责 |
|---|---|
| 黑箱优化（CMA-ES / 贝叶斯优化） | 在 1v1 成本产出矩阵上搜出候选数值 |
| 协同演化 | 验证该组数值在完整博弈的均衡下站不站得住 |

前者快但只看局部对抗，后者慢但看全局。先用前者收窄范围，再用后者验收。

## 9. 需要小组拍板的问题

1. **守方宏观层是否也上 RL，还是前期先写死脚本？**
   本文倾向：**先脚本**。守方宏观层是本方案里最难的部分（第 3 节），
   而 `CLAUDE.md` 已有的纪律是「战术层必须先跑通，不要两层同时上」。
   建议守方先只做战术层 RL（部队调度），宏观层用参数化脚本并随机化参数。
2. **守方奖励如何设计？** 倾向近似零和（守方奖励 ≈ 攻方奖励取负），
   偏离处需写明理由。但攻方有"击杀 `Scout` 的即时奖励"这类无对称项，
   完全零和做不到，需要讨论偏离到什么程度可接受。
3. **对手池的规模与入池间隔**，以及采样分布。
4. **行为克隆需要多少局人类数据**才够，谁来玩、玩多久。
5. **`AI vs AI` 模式是否算正式功能**（进交付的可执行程序），还是仅用于答辩演示。
6. 训练成本翻倍后，**GPU 服务器的机时够不够**，需要先估。

## 10. 排期与前置条件

**必须按顺序上，不要并行。** `CLAUDE.md` 已有的纪律「战术层必须先跑通，
不要两层同时上」在这里要加强一档——现在有两个智能体、各两层，
同时上的话训练不收敛时将完全无法定位来源。

建议顺序：

| 阶段 | 内容 | 前置 |
|:-:|---|---|
| 0 | `rts_core` 接口 + 代码骨架 + 确定性回放 | 无。**当前的瓶颈** |
| 1 | 攻方战术层 vs **参数化脚本守方** 跑通 | 0 |
| 2 | 守方战术层跑通（对手为阶段 1 的攻方） | 1 |
| 3 | 人类对局录制 → 行为克隆守方 | 0（回放）+ 有可玩前端 |
| 4 | 协同演化 + 对手池 | 2、3 |
| 5 | 双方宏观层 | 4 |

阶段 1 的"参数化脚本守方"**无论如何都要写**——它既是阶段 1 的对手，
也是后续对手池里的多样性来源，还是行为克隆之前的兜底。这不是被协同演化取代的临时物。

**空中单位仍按 `CLAUDE.md` 的原定安排，在地面核心循环收敛之后再加入。**
不要让"守方也是 RL"和"引入空中层"这两件事撞在一起。
