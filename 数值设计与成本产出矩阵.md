# 数值设计与成本产出矩阵（v1，ArLiangz，2026-08-30/31）

> 目标：把 `game/data/stats_placeholder.json` 里"一个都没标定过"的占位数值，
> 换成一版**有方法论支撑、逐条验证过关键克制关系**的数值。参考帝国时代4与
> 亿万僵尸的相对比例关系，但优先服从本作自己的机制（三资源决策轴、乘法千分比
> 克制、超线性波次预算、结构封死准则）。
>
> 本文只放**决定与理由**，具体数字看 `game/data/stats_placeholder.json`
> 的最终版本（下称"数值表"）。与既有文档冲突时，`CLAUDE.md` 优先。

## 0. 方法论：为什么不是拍脑袋

`CLAUDE.md`「平衡工具：成本产出矩阵」把要求写得很明确：每格记录 1v1 对抗的
期望资源盈亏，作为调数值的依据与答辩材料。但调研确认：**这个工具此前不存在**，
`bindings/`/`train/` 目录也不存在，连"能跑批量对局"这个前提都还没有。

本次的做法：**用 `rts_core/include/rts/combat_math.hpp` 与
`mechanics.cpp` 里逐字复现的战斗公式**（不是凭感觉估算的近似），在 Python 里
重建了 `apply_permille` / `level_permille` / `charge_permille` /
`anti_charge_permille` 这几个真实函数，对 CLAUDE.md 明文点名的每一条克制关系
跑闭式 1v1 交换比（TTK、资源效率），验不过就调参数、不是验完就当过了。
这是"批量仿真"落地前的最小可行替代品，不是长期方案——一旦
`bindings/`+`train/` 落地，应该换成团队自己方法论里写的两步法（黑箱优化在
矩阵上搜候选 → 协同演化在完整博弈均衡下验收）。工具留在 `tools/balance/`
供后续复用，见该目录 `README.md`。

## 1. 参考 AoE4 / TAB，但优先服从本作机制——具体怎么取舍

两款参考游戏的战斗解算方式都和本作不同（AoE4 是护甲减免、TAB 接近纯数值
对拼），能直接借用的只有**比例关系**，不是具体数字：

| 借用的比例关系 | 来源 | 本作的取法 |
|---|---|---|
| 远程射程与近战射程的比例（约4-8倍） | AoE4 弓手 vs 民兵 | `Archer.range=5.0` vs `Spear.range=1.2`，比例 ≈4.2，量级一致 |
| 建筑血量比单位血量高一个数量级 | AoE4 城墙(~1800) vs 步兵(~60) | `Wall.max_hp=800` vs `Archer.max_hp=100`，量级一致（本作单位普遍更肉，比例略低但方向一致） |
| 攻城器械慢、贵、专精拆building | AoE4 Ram：420HP/-3甲/200攻城伤，TAB 后期机甲单位慢重贵 | `Ram` 保持全场最慢、`vs_structure_permille=4000` 远超其他单位（约10倍于Ghoul，占位表原值即已体现这条设计意图，本次核实保留） |
| 波次/天数强度呈超线性增长 | TAB 出怪潮天数曲线 | 兵力预算曲线取 `w^1.6`（见「波次预算」文档），不是线性 |

**没有借用的地方**：AoE4 的护甲减免制（本作是乘法千分比，理由已写在接口契约
§3.4：矩阵要能在服务器和演示机上给出一致结果）；TAB 的资源体系（本作三种
资源各对应一条不可兑换的决策轴，TAB 是可兑换的多资源经济）。

## 2. 结构性决策一：等级涨哪个属性（**本节结论已被同日推翻，见节末订正**）

> **本节的结论已被一次后续测量重开，现在待表决。** 下面的推导本身没有错，
> 但它有一个隐含假设——**每个属性线性增长**——而放开之后三选一不再成立：
> 把倍率写成 `血 = B(L)^p`、`伤 = B(L)^q`（`B(L) = 1 + k(L-1)`），则
> 「克制比例衰减」只吃 `p+q`、「TTK」与「破墙时间」只吃 `p−q`，**两者正交**。
> `p = q = 0.5`（√L 各一份）在 `p+q = 1` 下与本节的选择**战力曲线逐点相同**，
> 而 TTK 从 ×0.19 变 ×1.00、破墙时间从 ×0.20 变 ×1.03。没有一项更差。
>
> 完整的表、正交性的验证、以及本节没覆盖到的那条（单位对城墙，
> 即 `hp_permille_per_level = 0` 与「城墙可升级」互斥）见 `CLAUDE.md`
> 「波次结构与数值曲线」那一节的 `§1.4` 段。**下面保留原文不改**——
> 它是当时的依据，改掉就看不出重开的理由了。

`CLAUDE.md`/`无尽模式与地形分层.md` §1.4 把"哪几个数值随等级涨"列为
**尚未拍板的结构选择**（血量+伤害两者都涨 = 有效战力 L²，只涨一个 = L），
要求"三种各跑一遍成本产出矩阵，判据只能在野战里测"。本次做了这个实验。

**结论（已被推翻，见下）：`hp_permille_per_level = 0`，`dmg_permille_per_level = 220`（每级+22%伤害）。**

> **订正（2026-08-31 同日）：改为血量与伤害各开一份平方根，两个系数都是 220。**
> 本节的论证没有算错，但它假设了「每属性线性增长」。放开成 `血 = B(L)^p` /
> `伤 = B(L)^q` 之后，三个判据各吃一个**互相正交**的组合：「克制比例衰减」
> 只吃 `p+q`、「TTK」与「破墙时间」只吃 `p−q`。p、q 限在 {0,1} 时这两者被
> 锁死在一起，「三选一」这个框架本身就是那个限制的产物。
>
> `p = q = 0.5` 的战力曲线与本节的选择**逐点相同**（1.22 / 1.44 / 1.88 /
> 2.98 / 5.18 / 9.58），而 TTK 从 ×0.19 回到 ×1.00、破墙时间从 ×0.20 回到
> ×1.03——**没有一项更差**，所以不是权衡而是纯改进。
>
> **本节漏掉的那一半判据正是破墙**：本节自己写「判据只能在野战里测」，用的
> 闭式 1v1 模型确实是野战，但**没有覆盖单位对城墙**——而 `hp` 系数归零 ⇒
> 墙升级空操作 ⇒ 破墙时间 ∝ 1/L → 0，恰好落在没测到的那一侧。
>
> 落地与实现坑见 `CLAUDE.md` §1.4 段与 `rts/combat_math.hpp` 的
> `level_permille`。**本节以下按写作时的逻辑保留，不逐条重写**——「三选一」
> 那个框架是这次要防的错误，只留结论下次还会照它重推一遍。

### 实验与数学证明

用 Spear(反冲锋) vs Knight(冲锋) 这对旗舰级克制关系做同级/等级差两种场景：

- **同级对拼**：无论哪种缩放方案，克制比例（谁更快解决对方）在数学上完全
  不受等级影响——两个单位的 HP×DPS 乘积比值里，等级缩放因子会精确抵消。
  这是一条通用结论，不是这对单位的特例：只要两个单位用**同一套全局等级系数**，
  同级 1v1 的胜负结构在任何缩放方案下都不随等级改变。
- **真正的分水岭在"等级差拉开"的场景**（这才是波次推进中会真实发生的情形：
  攻方兵力超线性增长、守方堡垒等级"顶不满"，几十波之后攻方单位等级会明显
  领先守方）：
  - 只涨一个属性（不论血量还是伤害）：克制比例随等级差**线性**衰减
  - 两个都涨：克制比例随等级差**平方**衰减——这正是文档担心的"克制关系
    被等级压垃圾"，且是可以量化证明的，不是猜测
  - 因此"两个都涨"两害相权取其重，首先排除

再在"只涨一个"的两个候选里选：血量方案会让 TTK 随等级发散（20级单挑耗时
是1级的4.8倍，实测数据见提交历史），直接威胁 CLAUDE.md「短
episode 是训练可行的关键」这条前提；伤害方案下 TTK 随等级反而缩短，不
制造这个风险。故选伤害。

**副作用（记录在案，不是意外）**：伤害单方向缩放意味着高等级单位是"效率
更高的杀手，但本身不会更肉"——集中火力/驻守/维修工这类战术手段
对高等级单位依然管用，不会因为对面练级就失效。这与"经济决定你能买多少，
战术决定你能不能守住"这条设计目标方向一致。

## 3. 结构性决策二：反冲锋倍率必须显著提高才能兑现"枪阵克骑"

用同一套实验脚本验证"开阔地怕枪阵，巷战被反克"这条 CLAUDE.md 花名册原文
点名的机制时，发现**继承自占位表的 `anti_charge_permille=1800` 不够用**：
骑士自身的冲锋倍率（`charge_bonus_permille_per_cell=250`×4格=+1000‰=2.0倍）
比枪阵的反冲锋倍率（1.8倍）还高，纯出手对拼骑士反而更快解决枪兵——克制
关系没有兑现，只是文档写了但数值没跟上。

**修正**：`charge_bonus_permille_per_cell` 250→180（骑士自身冲锋加成减弱），
`anti_charge_permille` 1800→3200（枪阵反冲锋加成显著提高）。Knight 基础
伤害保留占位表原值 18（核实后发现原值已经足够，不需要再改）。

修正后验证结果（`tools/balance/cost_output_matrix.py` 实测）：

| 场景 | Knight杀Spear用时 | Spear杀Knight用时 | 结果 |
|---|---|---|---|
| 开阔地满助跑(4格) | 6.97s | 4.27s | Spear(枪阵)胜 ✓ |
| 巷战/无助跑(0格) | 12.00s | 13.71s | Knight(骑兵)胜 ✓ |

> **这四个数原写 7.35 / 4.57 / 12.67 / 14.77，是在 `Spear` 被顺手改成
> damage 13 / max_hp 190 的状态下量的，而那处改动没有记录理由。**
> 按本文 §7 自己立的判据（「凡是无法追溯到某条已写明理由的改动，一律撤回到
> 占位表原值」）已撤回成 14 / 180，表里的数随之更新。`high_ground_dmg_permille`
> 700→650 同属此类，也已撤回。
>
> **撤回之前量过：四条结论一条都不翻**（开阔地 Spear 胜 / 巷战 Knight 胜 /
> 平地 Shade 胜 / 墙上 Archer 胜），只有绝对秒数变。所以撤回是安全的，
> 而补一个事后编的理由不是。现在这四个数可以用
> `py tools/balance/cost_output_matrix.py --scenarios` 直接复现。

两个场景都符合花名册原文，这条机制性克制现在是真的成立，不是写在文档里
没有数值支撑。同一轮验证也发现 `Shade` 压制 `Archer`（花名册被克表：
"Archer被克于Shade压制"）在占位数值下不成立（Archer反而更强），已把
`Shade.damage` 从 11 提到 14 修正。

## 4. 结构性决策三：Ram 速度与地图尺寸的自洽——用真实生成器验证，不是估算

`tools/map_gen/thresholds.json` 自己记录了一处已知矛盾：占位 `Ram` 速度
（0.04格/tick=0.8格/s）与 `地图与场景设计.md` §3 假设的速度（1.5-2格/s）
不自洽，按§3推出的地图边长(80-96)会让 Ram 行军占比超出锁死的10-20%区间，
生成器因此临时把 size 降到64作为权宜，注释明写"标定时第一件要解决的是这处
矛盾"。

**没有选择"把 Ram 提速去凑地图尺寸"**：Ram 是攻城器械，`地图与场景设计.md`
§3 原文写"Ram 是最慢的单位，它决定[行军时间]上限"——本作除 Ram 外全部单位
的速度都落在1.4-4.0格/s区间（Spear最慢1.4），若把 Ram 提到1.5-2格/s，Ram
就不再是全场最慢，"笨重攻城器械"这个定位与 §3 那句原文一起垮掉。

**改为反过来求解**：保持 Ram 速度慢速(0.045格/tick≈0.9格/s，基本沿用占位值），
用仓库里**真实的** `tools/map_gen/generate.py` + `validate.py`（不是手算近似
公式）遍历地图边长，找出与这个速度自洽、且被 strict 档 22 条校验全部接受
的最大边长：

```
size=64,68,72: 20/20 种子全部通过，丢弃率0%
size=74,76,78,80: 60/60 次尝试全部被第5条(Ram行军占比)否决
```

**结论：地图边长取 72**（比当前权宜值64略宽，仍在 strict 档 [64,96] 区间
内，且是与当前 Ram 速度精确自洽的上限，不是又一个未经验证的猜测）。
已把这个结论与验证方法写回 `tools/map_gen/thresholds.json` 的
`generator.size` 与对应 `_FINDING_size_vs_ram_speed` 注释。

## 5. 三种资源产出的差异化（此前是占位对称，未体现三条决策轴）

调研发现 `Quarry`/`Lumber`/`Mine` 三座采集建筑此前产出几乎相同(12/12/10)，
没有体现 CLAUDE.md「守方多资源」一节里"每种资源对应一条不同的决策轴"这条
设计意图。本次按三条决策轴各自的性质拉开差距：

- **石材（硬防御）产出最慢**（9/周期）——石材决策轴要求"防御造价高到无法
  覆盖所有墙段"，产出慢是让这条要求自然成立的手段之一，不是唯一手段（造价
  本身仍然是主要杠杆），但至少不该用"和木材金币一样快"去反向抵消这条要求
- **木材（恢复/应急）产出最快**（14/周期）——「维修显著低于重建」这条
  反死亡螺旋对策要靠木材供给跟得上才成立，木材慢了会让"抢救残血建筑"变成
  一句空话
- **金币（机动人力）产出居中**（11/周期）——人力要能持续补，但仍受训练
  时间与人口上限约束，不该无限快

## 6. 波次预算曲线与堡垒等级曲线：设计而非实现

这两块在 `StatsTable` 里完全没有 schema 承载——`composition_` 数组写入后
从未被任何代码消费，`demo_driver.cpp` 的波次生成是作者自陈的"演示占位，
无平衡含义"。这不是"数值没填"，是"转换逻辑本身还没写"。

详细公式、推导依据与"为什么不做成 L²"的呼应论证，见
`波次预算曲线与堡垒等级曲线.md`。这一节**明确是设计建议，不是可以直接消费
的契约**——是否新增 schema、字段放哪，需要团队一起过。

## 7. 一处需要记录的自查错误：Phoenix 的对结构倍率

草稿阶段一度把 `Phoenix.vs_structure_permille` 设成了 0，理由是"Phoenix 对城墙
城门破坏速率恒为0"——把**结构约束**和**这个数值字段**搞混了。查
`rts_core/src/unit_behavior.cpp` 才确认：墙/城门免疫是
`PhoenixBehavior::can_break_structure()` 的虚函数 override，纯结构判定，
与 `vs_structure_permille` 无关；`mechanics.cpp` 里 `is_wall` 目标专门走
`can_break_structure()` 这条门，非墙非门的普通建筑（`Tower`/`Flak`等）走的是
`vs_structure_permille` 这个数值。清零它只会让 Phoenix 连"点杀防御塔"这个
本职都做不到，与它的定位（"手术刀，不是胜利条件"）正面冲突。已改回符合
其定位的 800‰，并在数值表里把这条区分写成注释，防止下一个人踩同一个坑。

**这也是本次交付前做的一轮系统性自查的产物**：把设计稿逐字段与最新
`game/data/stats_placeholder.json` 做过 diff，凡是无法追溯到某条已写明
理由的改动，一律撤回到占位表原值（`Ranger`/`Ghoul` 的血量伤害、`Phoenix`/
`Ram` 的伤害都属此类，已撤回）。留下的改动全部能在本文档里找到对应理由。

## 8. 已知局限（不回避）

- **闭式 TTK 近似 ≠ 真实批量仿真**：忽略了接敌时间、路径绕行、混战中的
  目标切换、AOE 命中密集度随实际站位变化等因素。这些因素会让真实对局的
  资源效率与本文的闭式估算有偏差，但不改变"克制方向对不对"这个一阶结论。
  一旦 `bindings/`+`train/` 落地，应换成团队方法论里的真实两步法验收。
- **除 Spear/Knight/Archer/Ghoul/Shade/Ranger 这几对旗舰级克制关系外**，
  其余单位（Scout/Mason/Wraith 的猎杀关系、Phoenix/Flak 的位置性克制）
  只做了结构层面确认（伤害是否恰好为0、vs_structure是否正确排序等），
  没有逐一跑闭式验证——这些关系本身也不是纯数值能验证的（侵查猎杀、
  位置性否定都要靠地图与AI行为体现），留给协同演化阶段用真实对局验收。
- **波次预算与堡垒等级的具体系数全部是占位旋钮**，只验证了"曲线形状对不对"
  （编成位打满在哪一波、名义等级怎么随波数走），不是最终标定值。

## 9. 已验证

Windows/MSVC，Release 配置：`/W4 /WX` 零警告，`ctest -C Release` **55/55
全绿**（含 `map_gen_generate_smoke`、`map_gen_self_test` 用新 `Ram` 速度与
`size=72` 跑通）。`py tools/balance/cost_output_matrix.py` 4 条可数值验证的
克制关系全部通过。

## 10. 追加（2026-08-31）：Ram 的 AOE 溅射折扣不是本文档的范围，但会动 §4 的前提

试玩反馈发现 `Ram` 的 AOE 圈内不分主副、一律满伤——「点一个等于点一片」，
太离谱。修法与理由记在 `CLAUDE.md`「结构破坏规则」（新增
`UnitStats::splash_dmg_permille`，`Stats/5 → Stats/6`），不在此重复。

**这里只记一条与本文档相关的边界**：`tools/balance/cost_output_matrix.py`
与 `combat_math.py` 建的是**闭式 1v1 TTK 模型**，不模拟"一次命中打中几个目标"，
所以本文档 §3 的四个场景数字（Spear/Knight/Shade/Archer）**不受这次改动影响**
——它们从来只有一个目标在 AOE 圈里，主副之分没有机会体现。改完后原样跑过
`--scenarios` 复核，四个数字逐位不变。真正需要"多目标同时挨打"的场景（比如
"3 个弓手挤在缺口，Ram 一锤下去谁疼多少"）这个工具测不出来，见 `tools/balance/
README.md`「已知局限」那条"AOE 命中密集度随实际站位变化"——这次的改动没有
缩小那条局限，只是把"主目标该比溅射疼多少"这个此前不存在的旋钮显式化了。

---

## 11. 追加（2026-09-02）：资源经济体检 —— 它离「智斗」是更近还是更远

起因是组长的两句话：「我感觉现在地图生成还有不少问题，可能会影响平衡性」、
「现在三个采矿建筑能建在资源点上，资源点不能被破坏，建筑可以被破坏，
你思考一下这套机制离智斗的目标更近了还是更远了」。

**本节全部结论由现有数值算出，不是评价。** 复算方式写在每小节里。

### 11.1 「资源点不可破坏 + 建筑可破坏」这条**本身是对的**

先把它和「更远」的部分分开，否则容易连对的一起改掉：

- **不可破坏的资源点 = 一个稳定的、双方都知道的目标。** 智斗的前提是
  「可预判」——AA 摆哪、兵放哪、要不要出城，都要求玩家能预判**攻方会去哪**。
  若资源点本身能被永久摧毁，地图会单调退化，同一个点不会被争第二次，
  「攻其必救」失去承载物。这条要留。
- **拆建筑 = 节奏/资源效率交换，不是二元胜负**，正好对上本文 §0 与
  `CLAUDE.md`「克制的本质是资源效率」，也让「一波未突破不等于失败」
  成为真的策略路径。这条也要留。

### 11.2 但**周围的数字**让这条机制的智斗价值基本归零

#### (a) 采集建筑回本期只有 **0.33–0.54 波**

| 建筑 | 造价（石/木） | 产出/周期 | 产出/波 | 回本 |
|---|---|---|---|---|
| `Quarry` | 40 / 30 | 9 | 136 | 0.52 波 |
| `Lumber` | 40 / 30 | 14 | 211 | 0.33 波 |
| `Mine` | 50 / 40 | 11 | 166 | 0.54 波 |

（一波按 1510 tick 粗算 = 建造阶段 260 + 行军约 1250；`income_period_ticks` = 100）

被拆一座，玩家的**全部**损失 = 重建 70–90 + 停产 18–26 ≈ **88–116 资源**，
而它半波就把自己挣回来。**于是「被拆」不构成需要提前防范的后果**，
而智斗的载体恰恰是「因为怕后果，所以提前做布局承诺」。

#### (b) 攻方要拆它，只有 `Ram` 划算，而 `Ram` 不该去

| 单位 | 拆 500 hp 采集建筑 | 对结构倍率 |
|---|---|---|
| `Ghoul` | 2292 tick = 115 s | 0.4 |
| `Knight` | 1429 tick = 71 s | 0.2 |
| `Phoenix` | 526 tick = 26 s | 0.8 |
| `Ram` | **221 tick = 11 s** | 4.0 |

`Ram` 是全场最慢、且是玩家的头号集火目标，派它去远处矿场等于放弃本波破墙。
**所以理性攻方不该骚扰经济**——`CLAUDE.md` 要的「攻其必救」在当前数值下
没有牙齿。（`Phoenix` 26 s 是唯一可行的那条，正对上「空军点掉偏远矿场比
撞墙划算」——但 demo 里空军恒 1 只，见 11.4。）

#### (c) 城外资源点的产出与城内**完全相同** ⇒ 冒险无回报

`CLAUDE.md`：「城内资源点提供**保底**收入，城墙之外的资源点提供**增量**收入
但需派兵争夺」。而机制里 `tier` 只影响 `unlock_wave`，**不进产出**：产出来自
`BldStats.income_amount`，是逐建筑的常数。于是城外那一座**风险更高、回报相同**，
是严格劣势的选择。

**「被迫出城」因此纯粹是「推」而不是「拉」**——玩家出城只因为城内被钉在
1 石 1 木 1 金，而不是因为城外更香。一个选项严格更差的「决策」不是决策。

> **修法（需团队定，动的是契约面）**：产出按距离/tier 给倍率。
> 那要么在地图格式给资源点加一个倍率字段，要么在 `World` 里按到 `keep` 的
> 距离算——两条都动跨模块契约，不能由做数值的人单方面改（同 §6 那条纪律）。

### 11.3 综合判断：**离智斗更远，但病根不在那条机制**

把 11.1 与 11.2 合起来：机制的**形状**是对的（稳定目标 + 效率交换），
但三个数字把它压成了一个**无后果的 DoS**：拆了半波就回本、只有不该来的单位
划算、城外冒险还不加钱。再叠上 #122 移除「不可围墙」之后**围一簇一次性买断**
（实测中位 32 段墙 = 960 石，见 `日志.md` 09-01 条），长期最优解是
「围一次，从此不用再想」——那会把一个反复出现的决策**永久删除**，
正是 `CLAUDE.md`「经济不得压倒战术」点名的 TAB 失效形态。

**优先级建议**（都只提出、不实施）：

1. **拉长回本期**（提造价或降产出）到 2–3 波 —— 一次被拆真的疼，威胁才能塑造布局
2. **城外产出 > 城内**（11.2c）—— 让「出城」从被推变成被拉，同时给
   「打哪一种资源」以纹理（远簇更肥 ⇒ 更值得被袭 ⇒ 攻其必救有牙齿）
3. 给攻方一个**造成持久后果**的出口（例如被拆后原地留需清理的废墟、拉长停产），
   否则攻方永远只能「打断」不能「否定」

### 11.4 顺带查出的三处**其它**机制与智斗不符（都已核实到代码）

1. **空军恒 1 只 ⇒ 防空两难不存在。** `demo_driver.cpp:260`
   `phoenixes = wave >= 3 ? 1 : 0`，永远 1。而 `CLAUDE.md` 把
   「AA 摆在哪」的机会成本称作「**本作智斗最可读的载体**」，它要求玩家
   **预判攻方的空军投入**——投入恒为 1 时无可预判，造够 1 座 `Flak` 即完事。
2. **分兵佯攻在 demo 里结构上不可能。** `spawn_wave` 按
   `spawns[n % spawns.size()]` **轮转**派点，于是每个集结点分到的兵力
   ≈ 相等。CLAUDE.md 的「免费方向提示 = 兵力最多的集结点」因此由整数取余
   决定，而「把佯攻堆得比主攻更多、让提示变成诱饵」这条**欺骗手段无从发生**。
   （那行 HUD 提示是 2026-09-01 才加的，**加的时候没发现攻方从不集中**
   ——记在这里作为「实现了消费侧、没检查生产侧」的一例。）
3. **克制关系在 UI 里完全不透明。** `CLAUDE.md`：「克制用乘法加成，
   且**必须在 UI 中完全透明**」「玩家必须能快速读懂来袭编成才能应对」。
   全仓搜索：没有任何一处 UI 展示克制表或来袭编成构成（操作说明那一屏
   全是按键）。而迷雾落地之后，玩家侦查到的编成**也没有面板可读**，
   只能靠数精灵——「不透明会让『被 AI 针对』退化为『被系统坑』」正是
   CLAUDE.md 引 AoE2 那条批评时要避免的。

### 11.5 资源点该怎么分布：把「簇数」算成主难度旋钮

**折算口径**：一切换算成「1 级 `Archer` 当量」（60 金），
因为 `cost ∝ 战力` 已是结构结论（`budget_curves.py` 结构结论一）。

- 城内保底（1 石 + 1 木 + 1 金 + `Keep` 地板）= 36/周期 ⇒ **9.1 当量/波，恒定**
- 每 securing 一簇（4 点 × 平均 11.3/周期）≈ **11.4 当量/波**
- 攻方战力 = `6·w^1.25`（`demo_driver.cpp` 与 `budget_curves.py` 同源）

于是「第 w 波要几簇才平手」= `(6w^1.25 − 9.1) / 11.4`：

| 波 | 3 | 5 | 8 | 10 | 12 | 15 | 20 | 25 |
|---|---|---|---|---|---|---|---|---|
| 需要簇数 | 1.3 | 3.1 | 6.3 | **8.6** | 11.0 | 14.8 | 21.5 | 28.7 |

**现有池图 7–8 簇 ⇒ 玩家最多跟到第 10 波，此后无论怎么打都掉队。**
方向对（无尽模式必败是设计），但 **7–8 这个数是被实现钉住的、不是标出来的**：
`place_outer_clusters` 走 8 个方位、每个方位一簇，`min(cfg.outer_clusters,
len(directions))` 把上界锁死在 8。

反解「第 k 簇该在第几波解禁」（需求曲线近似**每波多要一簇**）：

| 第 k 簇 | 1 | 2 | 3 | 4 | 6 | 8 | 12 | 16 | 20 |
|---|---|---|---|---|---|---|---|---|---|
| 刚好需要它的波次 | 2.7 | 3.8 | 4.9 | 5.8 | 7.7 | 9.5 | 12.8 | 15.9 | 18.9 |

**解禁必须慢于这条线**，否则「场场全胜就能永远跟上」⇒ 必败没了。
快慢之差就是中位存活波数——`CLAUDE.md` 那句「主要的难度旋钮是**解禁节奏与
强度曲线之比**」，这里把它算成了两条可比的曲线。

**取「第 3 波起每波开 1 簇」**：第 10 波 8 簇（刚好平手）、第 15 波 13 簇 vs
需要 14.8、第 20 波 18 簇 vs 需要 21.5 ⇒ **缓慢掉队**，正是要的形状。
⇒ **地图上要备 16–20 簇，而不是 7–8。**

几何可行性（按 §3.0 解耦后的 208 格地图、城区半径 13）：争夺带是离堡垒
17–55 格的方环，面积约 **10944 格**；18 簇 × 每簇约 7×7 = 882 格 = 带面积的
**8.1%**；分两圈各 9 簇、平均半径 36 ⇒ 簇心间距约 **32 格** ⇒ 「大散居」成立。
**装得下，且余量很大**——瓶颈从来不是面积，是那个「每方位一簇」的实现上限。

### 11.6 本节的局限

- 「一波 1510 tick」是从 demo 的建造阶段 + 行军时间凑的**占位**，不是标定值；
  回本期与「当量/波」都随它线性缩放，但**结论的方向不随它变**
  （回本期 0.33–0.54 波要变成「疼」得慢 4–6 倍才行）
- 三种资源被**等权相加**折成「当量」。真实决策里它们不可兑换（三条决策轴），
  所以这个折算只用于比较**量级**，不能用来定单种资源的价格
- 「每簇 4 点、平均产出 11.3」取的是当前生成器配置；簇大小一改要重算
- 守方是**存量**、攻方是**流量**（`budget_curves.py` 的 `defender_stock_power`
  记过这条），所以「要几簇才平手」是一个**偏保守**的估计：墙塔与活兵留到
  下一波，实际能撑得比表上久。表的用途是给簇数**定量级**，不是预测胜负

---

## 12. 追加（2026-09-02）：造价曲线的严肃探索 —— 并订正 §11 之后我自己给错的修法

组长要求把「造价 ∝ L 而战力 ∝ 1+k(L−1)」这条**严肃探索一下**。探索的结果是：
**问题确实存在，但我上一轮给的修法（「造价应 ∝ 战力」）自己也带着一个没写出来
的假设，而且被模拟推翻了。**

### 12.1 先把「战力」这个词查清楚——它不是「单位战力之和」

上一轮我用的是 `战力 = 血 × 伤 = √B · √B = B(L)`。那是**单个单位**的强度。
但「一支部队有多强」不等于把它加起来——**取决于有多少人能同时交战**：

| 战斗形态 | 兰彻斯特 | 部队强度 |
|---|---|---|
| 隘口 / 城门 / 贴身（只有前排打得着） | 线性律 | ∝ N¹ · (血×伤) |
| 开阔地 / 远程（全员可射） | 平方律 | ∝ N² · (血×伤) |

等价条件（花同样的钱，选哪个等级都一样强）：

```
强度 = (预算 / 造价(L))^α · B(L)   与 L 无关   ⇒   造价(L) ∝ B(L)^(1/α)
```

- α = 1（线性律）⇒ 造价 ∝ B —— **这正是我上一轮给的修法**
- α = 2（平方律）⇒ 造价 ∝ √B

⇒ **`budget_curves.py` 的「结构结论一」隐含假设了 α = 1，而它没写出来。**
这与 §1.4（隐含「每属性线性增长」）、§3（「线性」当「正比」）是**同一族**
错误：一个没写下来的前提。三次了。

### 12.2 等成本对拼（同兵种镜像，集火，真实公式）

预算 1200 金、`Archer`、`width` = 每拍能同时开火的人数上限
（1 ≈ 隘口/线性律，∞ ≈ 开阔地/平方律）。表内是**谁赢、还剩几个**：

| 造价曲线 | 对阵 | width=1 | width=4 | width=∞ |
|---|---|---|---|---|
| **现行 c ∝ L** | 20×L1 vs 4×L5 | 杂兵 +13 | 杂兵 +16 | 杂兵 +20 |
| 候选 c ∝ B | 20×L1 vs 10×L5 | 杂兵 +2 | 杂兵 +5 | 杂兵 +15 |
| 候选 c ∝ √B | 20×L1 vs 14×L5 | **精英 +4** | **精英 +4** | 杂兵 +7 |

- **现行 c ∝ L：三种形态里杂兵全胜，而且是碾压**（20 打剩 13–20）
- **c ∝ B：杂兵仍然全胜**，只是隘口那一档收窄到 +2 ⇒ 我上一轮的修法
  **只在隘口附近接近中性，开阔地照样一边倒**
- **c ∝ √B：唯一一条「谁赢取决于形态」的曲线**

### 12.3 攻城场景（把 AOE 这个「量」的天敌加进去）

24 份预算砸 1200 hp 城墙，守方 1 座箭楼（齐射覆盖 4 人）+ 2 名墙上弓手。
判据是**破墙耗时**（越短越好）：

| 造价曲线 | L=1 | L=3 | L=5 | L=8 | 最优 |
|---|---|---|---|---|---|
| **现行 c ∝ L** | 24 兵 287t | 8 兵 **打不破** | 4 兵 **打不破** | 3 兵 **打不破** | L=1 |
| c ∝ B | 24 兵 287t | 16 兵 353t | 12 兵 397t | 9 兵 485t | L=1 |
| c ∝ √B | 24 兵 287t | 20 兵 265t | 17 兵 265t | 15 兵 243t | L=8 |

**现行曲线不是「偏向量」，是一道悬崖**：L ≥ 3 的编成**根本破不了墙**。
玩家/AI 但凡买过一次高级兵就会发现它连墙都打不动，此后永远不再碰等级
——「兵种等级上限」那个输出因此不只是弱，是**不可用**。

### 12.4 指数扫描：中性点在 p ≈ 0.7

造价 = `base · B(L)^p`，同一攻城场景：

| p | L=1 | L=3 | L=5 | L=8 | 最优 | |
|---|---|---|---|---|---|---|
| 1.00 | 287t | 353t | 397t | 485t | L=1 | 量占优 |
| 0.85 | 287t | 309t | 331t | 397t | L=1 | 量占优 |
| **0.70** | 287t | 287t | 287t | 309t | L=1 | **接近中性** |
| 0.60 | 287t | 287t | 265t | 287t | L=5 | 质占优 |
| 0.50 | 287t | 265t | 265t | 243t | L=8 | 质占优 |

### 12.5 建议取 p = 0.5，理由是它**最贴合设计原话**

`CLAUDE.md`：「该决策需要读取玩家的防御形态才能选对——**防线均匀则宜集中
凿点，防线有薄弱段则宜铺开找洞**」。这句话要求的**不是**「两者永远等效」，
而是「答案随局面翻转」。把 §12.2 与 §12.3 合起来看：

| | 隘口 / 攻城（凿点） | 开阔地铺开（找洞） |
|---|---|---|
| c ∝ L | 量 | 量 |
| c ∝ B | 量 | 量 |
| **c ∝ √B** | **质** | **量** |

**只有 √B 让那句话字面成立。** p ≈ 0.7 是「攻城中性」，但开阔地仍偏量，
所以它是「不偏不倚 + 一边倒」的组合，而不是一个会翻转的决策。

```
cost_gold(L) = round(base_cost_gold * sqrt(1 + k*(L-1)))     # k 与 level_permille 同源
```

**代价要认下来**：等级变得很便宜（L=20 只要 2.28 倍造价、却有 5.18 倍战力）。
挡住它的是**另外两条已有的上限**——兵种等级上限（= 堡垒等级 K）与人口上限
（§2.1 定的 `8+2K`）。**所以这三件必须一起落地**：只改造价而不上人口上限，
后期会变成「少数满级精英」一种解。

### 12.6 顺带查出：攻守两侧跑着**两个不同的造价模型**

- 攻方 `demo_driver.cpp::wave_level` 反解名义等级用
  `L = 1 + (人均预算/base − 1)/k`，即**假设人均造价 = base·B(L)**（p = 1）
- 守方 `World::train_cost_gold` 实际收钱是 `cost_gold × level`（p 相当于 ~1
  但**过原点**，即 §11 说的不同阶）

两侧本来就是不同的货币（攻方无经济，「兵力预算」按定义就是战力预算），
所以**不要求数值相同**；但它们对「一个 L 级单位值多少」这件事给出的**形状**
不同，而 `cost ∝ 战力` 这条结构结论只在攻方那侧成立、守方那侧不成立，
**且没有任何检查在管**。改守方造价时要一并决定攻方那条反解跟不跟着改
（若攻方也走 p = 0.5，`总战力 ≡ 兵力预算` 这条构造性等式会不再成立，
那是 PR #124 特意建立的性质——**不能顺手改，要单独想清楚**）。

### 12.7 `budget_curves.py` 的结构检查为什么全绿

那条检查里写的是 `cost_L = base_cost * (1 + level_k*(L-1))` ——
**它在检验自己的假设，不是游戏的实现**。与第 14 条那次
（`static_vision_radius_max` 手写成 8、而表里 `Watch` 是 12，结构约束绿着失效）
同型。修法照那条先例：**从唯一真相来源读，不要复述它**——让它读
`train_cost_gold` 的公式，或至少断言两处同阶。

### 12.8 本节的局限（结论方向稳，具体 p 不稳）

- 模拟是**同兵种镜像 + 集火**，没有克制倍率、冲锋助跑、走位、撤退、
  目标选择；AOE 用「齐射覆盖固定人数」粗略建模，没有真实半径与散开
- 只测了一种守方配置（1 箭楼 + 2 弓手）与一种墙血
- **所以 p 的具体取值必须等 `bindings/`+`train/` 能跑批量对局后再定**；
  本节能立住的是三条**方向性**结论：① 现行 c ∝ L 使高等级编成**打不破墙**，
  是悬崖不是斜坡；② 「造价 ∝ 战力」只在隘口附近接近中性，不是普适修法；
  ③ 只有 p ≈ 0.5 能让「凿点用质、铺开用量」这条设计原话字面成立
