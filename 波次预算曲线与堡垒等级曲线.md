# 波次预算曲线与堡垒等级曲线（设计，非实现）

> 这两块在 `StatsTable` 里完全没有 schema 承载（三个研究代理交叉确认）。
> `composition_` 数组写入后从未被消费，`demo_driver.cpp` 的波次生成是作者自陈的
> "演示占位，无平衡含义"。这里只给出**公式与推导依据**，不新增 JSON 字段或改
> C++ 代码——是否要开一张新表、字段叫什么，应该走团队一起定契约的流程，
> 不该由做数值设计的人单方面决定 schema。

## 1. 攻方双预算

```
编成位(w)   = 6 + 1.2 * (w - 1)                  # 线性；硬顶暂不设（见下）
兵力预算(w) = 6 * w^1.25                         # 超线性，无上限
人均预算(w) = 兵力预算(w) / 编成位(w)
本波名义等级 L(w) = 1 + (人均预算(w)/base − 1) / k
                  # base = 人均预算(1)（锚定 L(1)=1）
                  # k = 数值表的等级系数（hp/dmg_permille_per_level ÷ 1000）
```

> **第三行原写 `L(w) = 兵力预算(w) / 编成位(w)`，2026-09-01 订正为由
> `cost ∝ 战力` 反解。** 战力(L) = 1 + k(L−1) 是仿射、不过原点，把预算商
> 直接当等级会让「攻方总战力 = 编成位 × 战力(L)」低于兵力预算一大截、
> 且随波数打的折还在变——超线性那条结构承诺被静默削弱。反解之后
> **总战力 ≡ 兵力预算由构造成立**（推导见 `tools/balance/budget_curves.py`
> 文件头「结构结论一/二」，那里同时把这条做成了会红的结构检查）。
> 代价是名义等级的数字比旧公式大得多（同一份预算，等级要除以 k≈0.22 才
> 折成战力）——那只是单位换算，不是变强了。

验证关键点：
- `L(1) = 1` **由构造成立**（base 锚定；`budget_curves.py` 与
  `demo_driver.cpp` 两处实现都显式除 base，改系数不丢锚点）
- **编成位的硬顶 2026-09-01 撤下（用户定版）**：原来的 40 是随手标的，
  RL 实际能支持多少单位没人测过——「必须有硬上限」这一结构主张（CLAUDE.md
  「攻方双预算」）**不撤**，撤的是没有依据的取值。撤下它不改难度曲线：
  名义等级按预算反解 ⇒ 总战力 ≡ 兵力预算，上限只改「人数 × 等级」的分配。
  等 `bindings/`+`train/` 实测出可训练规模后把数填回 `budget_curves.py` 的
  `slots_cap`（现为 None，第三条结构检查随之挂起）与 `demo_driver.cpp`
- 逐波的数**刻意不写在这里**（上一版写了三个，公式一订正全部作废——
  正是 CLAUDE.md「条目数不写死」那条纪律说的漂移），要看就跑
  `py tools/balance/budget_curves.py`
- 编成位系数(6起步、每波+1.2)与兵力预算系数(6*w^1.25)都是**占位旋钮**，
  不是最终值——它们只是用来验证"曲线形状对不对"，真实系数要等
  `bindings/`+`train/` 落地、能跑批量对局后用黑箱优化标定
  （α 原取 1.6，2026-09-01 回调到 1.25：1.6 在低波段太陡——第 3 波人均
  战力已是首波的 4.3 倍，玩家没起经济就被数值碾过——而「超线性」这条
  结构只要求 α > 1，改的是旋钮、不是结构）

## 2. 守方堡垒等级轴

```
人口上限(K)     = 8 + 2*K             # 2026-09-02 定版；原占位 20+4K 不生效，见 §2.1
建筑等级上限(K)  = ceil(K / 2)          # 每2级堡垒开1级建筑上限（已落地）
兵种等级上限(K)  = K                    # 直接等于堡垒等级（已落地）
```

三者都不设上限（堡垒等级本身不设顶，靠"经济顶不满"防复利，见
`无尽模式与地形分层.md` §7.2）。

### 2.1 人口上限：严肃推导（2026-09-02，组长要求「严肃研究一下」）

**结论先写**：原占位 `20 + 4K` **在城外拿到 4 座 `Mine` 之前一次都不会被碰到**
⇒ 那是个装饰品。改为 `8 + 2K`，理由是下面那条**唯一的**结构要求。

#### (1) 上限的设计职责：它是「数量 vs 质量」那个决策的一半

`CLAUDE.md`：「守方也在选编成形状——**人口上限管数量、金币管质量**（征兵时
选等级）」。所以人口上限**不是**一个防超载的护栏，它是一个**制造取舍**的装置：

> **上限必须咬得住**——即「上限 < 玩家把全部金币买 1 级兵能养的数量」。
> 咬不住时金币是唯一约束，玩家永远只买 1 级兵，
> **「人口上限」与「兵种等级上限」两个输出会一起变成装饰品**。

这两个输出是**互相支撑**的：上限逼你把余钱投进等级，等级上限（= K）
限制你能投多深。少了任一个，另一个失去意义。

#### (2) 算「能养多少」：守方的金币流量很窄

金币**只**来自 `Mine`（11/周期）与 `Keep` 地板（2/周期），城内恒 1 座 `Mine`。
稳态军队规模 = 每波可买数 ÷ (1 − 留存率)，留存取 0.75（`budget_curves.py` 的占位）：

| 城外 `Mine` 数 | 金币/波 | 每波买 1 级弓手 | 稳态可养 |
|---|---|---|---|
| 0（只有城内） | 196 | 3.3 | **13.1** |
| 1 | 362 | 6.0 | 24.2 |
| 2 | 528 | 8.8 | 35.2 |
| 4 | 861 | 14.3 | 57.4 |
| 6 | 1193 | 19.9 | 79.5 |

#### (3) 候选公式对比：只看「咬不咬得住」

| K | 城外 `Mine` | 稳态可养 | `20+4K`（原占位） | `8+2K` | `6+3K` | `10+2K` |
|---|---|---|---|---|---|---|
| 1 | 0 | 13 | 24 ✗松 | **10 ✓咬** | 9 ✓咬 | 12 ✓咬 |
| 2 | 0 | 13 | 28 ✗松 | **12 ✓咬** | 12 ✓咬 | 14 ✗松 |
| 3 | 1 | 24 | 32 ✗松 | **14 ✓咬** | 15 ✓咬 | 16 ✓咬 |
| 5 | 2 | 35 | 40 ✗松 | **18 ✓咬** | 21 ✓咬 | 20 ✓咬 |
| 8 | 4 | 57 | 52 ✓咬 | **24 ✓咬** | 30 ✓咬 | 26 ✓咬 |
| 12 | 6 | 80 | 68 ✓咬 | **32 ✓咬** | 42 ✓咬 | 34 ✓咬 |

取 **`8 + 2K`**，四条理由：

- **每一档都咬得住**（`10+2K` 在 K=2 松掉、`20+4K` 到 K=8 才开始咬）
- **上界落在可读区间内**：K=12 时 32，仍低于攻方那条 20–40 硬上限
  （守方的上界不来自 RL，来自**演示可读性与 `render/` 预算**，
  见 `无尽模式与地形分层.md` §8.2）
- **开局不越界且余量很小**：demo 开局 7 名守方单位，K=1 时上限 10 ⇒ 还能加 3。
  这个紧是**故意的**——它让堡垒等级轴从第 1 波就有压力，而不是二十波后才想起来
- **斜率比金币流量慢**：金币随「securing 了几簇」增长（§11 那条分析里每簇
  ≈ 11.4 当量/波），而上限只随 K 走，而 K 要花石+木 ⇒ 上限会**持续**咬住，
  取舍不会在中期自己消失

#### (4) K 涨得多快（上限的自变量）

`Keep` 每级 200 石 + 200 木 + 400 tick；城内石+木流量 = 136 + 211 = 347/波：

| 投入石木的比例 | 每级耗时 | 第 20 波约 K= |
|---|---|---|
| 20% | 5.8 波 | 3 |
| 33% | 3.5 波 | 6 |
| 50% | 2.3 波 | 9 |

⇒ `8+2K` 在第 20 波给出上限 **14–26**。这个量级与「演示可读性」相容，
也与守方是**存量**（墙塔与活兵留到下一波）这条相容。

#### (5) ⚠️ 一个**必须先修**的前提：造价与战力不同阶

**这条不修，人口上限会从「取舍」变成「战力天花板」。**

`§3` 写着「战力近似与 L 线性 ⇒ 造价也应该近似线性」，落地成
`World::train_cost_gold` = `cost_gold × level`（**造价 ∝ L**）。
但战力是 `B(L) = 1 + k(L−1)`，k = 0.22 —— **仿射，不过原点**。两者不同阶：

| L | 造价（× base） | 战力（× base） | 每金买到的战力 |
|---|---|---|---|
| 1 | 1.0 | 1.00 | 1.00 |
| 2 | 2.0 | 1.22 | 0.61 |
| 5 | 5.0 | 1.88 | 0.38 |
| 12 | 12.0 | 3.42 | 0.29 |

⇒ **堆量严格占优，且优势随等级拉大（渐近 1/k = 4.5 倍）**。而
`budget_curves.py` 的「结构结论一」明写：「`C` 比 `P` 涨得快 ⇒ 堆量严格占优
⇒ 永远买一堆 1 级兵」，那正是宏观层决策**塌到角上**的定义。

> **`§3` 那句话把「线性」和「正比」混为一谈了。** 战力是一条直线，但它的截距
> 不是 0；造价是过原点的正比。要满足「两者等效」，造价必须 ∝ **战力**：
> ```
> cost_gold(L) = base_cost_gold * (1 + k*(L-1))     # 与 level_permille 同一个 k
> ```
> 这与 `§1.4` 那次翻车是**同一类错误**（那次的隐含假设是「每属性线性增长」）
> ——都是「线性」这个词用松了。
>
> **`budget_curves.py` 的结构检查为什么没抓到它**：那条检查里写的是
> `cost_L = base_cost * (1 + level_k*(L-1))`，即它**假设**造价 ∝ 战力，
> 然后验证「堆量堆级等效」——**它在检验自己的假设，而不是游戏的实现**。
> 这与第 14 条那次「`static_vision_radius_max` 手写成 8、而表里 `Watch` 是 12，
> 结构约束绿着失效」是同型的：**工具应当从唯一真相来源读，而不是复述它**。
> 修法照那条先例：让检查读 `train_cost_gold` 的公式（或至少断言两处同阶）。

修了这条之后，人口上限才是「取舍」——被顶住时把余钱换成等级，
每金买到的战力不变，选哪个由战术决定（集火脆弱性、AOE 吃亏程度、目标选择）。
不修的话，顶住之后升级的效率只有 1/4.5，守方战力会**撞上一堵墙**而不是继续
爬坡，`必败` 会来得又早又突然。

#### (6) 落地时要注意的两点（不是数值）

- **顶住之后没有「把数量换成质量」的出口。** 「批量升级」那条命令
  （`UpgradeForce`）已随 2026-09 编队移除删掉，而「解散」从来没有；
  于是玩家在顶格时只能**等有人阵亡**才能招一个更高级的。波次游戏里损耗每波
  都有，所以这不是死锁，但它意味着「换装」有一波的延迟——**要么接受，
  要么把 `CLAUDE.md` 里那条尚未实现的「买活」一并设计成出口**
- **`Keep` 自己不占人口**，否则 K=1 时上限 10 会被开局 7 兵 + 堡垒挤到只剩 2

#### (7) 本节的局限

- 留存率 0.75、一波 1510 tick 都是占位；**结论的方向不随它们变**
  （`20+4K` 要变成「咬得住」，得让稳态可养掉到 24 以下，那需要金币流量减半）
- 三种资源被等权折算（同 `数值设计与成本产出矩阵.md` §11.6），只用于比量级
- **`8+2K` 的两个系数仍是占位**：它们满足的是「咬得住」这条**结构**要求，
  而具体取值要等 `bindings/`+`train/` 能跑批量对局后与波次预算一起标

## 3. 士兵造价曲线（兑现"k个1级兵≈1个k级兵"这条标定靶子）

因为一个L级单位的"战力"近似与L线性——**这条在 §1.4 改成「血量与伤害各开一份
平方根」之后仍然成立、结论不用改**：战力 = 血 × 伤 = `√B × √B` = `B(L)`，正是
开方前那个线性底。（原文写的理由是「等级只涨伤害(线性)、不涨血量」，那是被推翻的
那一版；换成平方根之后线性性来自两边相乘而不是来自单边。）
所以造价也应该近似线性，否则会出现"堆量"或"堆级"其中一个策略碾压另一个：

> **⚠️ 下面那行 `cost_gold(L) = base × L` 是错的，2026-09-02 查出，详见 §2.1(5)。**
> 本节把「线性」与「正比」混为一谈了：战力 `B(L) = 1 + k(L−1)` 是一条直线，
> 但**截距不是 0**；而 `base × L` 是过原点的正比。两者**不同阶**，于是
> 「每金买到的战力」从 L=1 的 1.00 掉到 L=12 的 0.29 ⇒ **堆量严格占优、
> 且优势随等级拉大（渐近 1/k = 4.5 倍）**，正是本节这句话要避免的结果。
> 应为 `cost_gold(L) = base_cost_gold * (1 + k*(L-1))`（与 `level_permille`
> 同一个 k）。**代码尚未改**（`World::train_cost_gold` 仍是 `× level`），
> 因为它动的是仿真行为、要团队过一下。

```
cost_gold(L)   = base_cost_gold * L        # ← 见上，应改为 base * (1 + k(L-1))
train_ticks(L) = base_train_ticks * (1 + 0.15*(L-1))   # 训练耗时温和增长，
                                                          # 不线性——否则批量升级
                                                          # 撤防线的代价会随等级
                                                          # 发散，堡垒等级曲线里
                                                          # "建筑/兵种等级上限"
                                                          # 前期就失去意义
```

## 4. 为什么不做成 L²（呼应 stats 设计里的选择）

若造价也搭配"两个都涨"(L²)的战力模型，会同时踩两个雷：
1. 造价必须跟着涨 L²才能保持效率不变，但"半价买两个1级"vs"全价买一个2级"
   这类比较会因为二次项而对整数取值极度敏感，调数值的容错空间很小
2. 与 §1.4 的选择自相矛盾——那一条取 `p + q = 1`（各开一份平方根），不是 2；
   两处若不一致，答辩时会被问出破绽

## 5. 这部分交付的定位

这一节**不是**最终契约，是"下一步该长什么样"的具体建议，附带论证。
真正要落地需要：
1. 团队讨论是否新增 schema（放 `StatsTable.global` 还是单开一张
   `wave_curve.json`/`keep_curve.json`）
2. 把 `composition_` 数组接上真正的消费逻辑（当前完全没有代码读它）
3. ~~`demo_driver.cpp` 的波次生成从"占位曲线"换成这套公式~~
   ✅ **已做（2026-09-01，起因是试玩反馈「20 分钟后玩家显著强过敌人」）**：
   `demo_driver.cpp` 的 `wave_slots`/`wave_level` 就是 §1 那两条曲线（含上面
   订正后的仿射反解），系数与 `budget_curves.py` 的 `PLACEHOLDER` 同源、
   等级系数 k 从数值表读。它是这套公式的**无决策退化消费者**（比例固定、
   预算全花）——正式形态里编成由攻方宏观层按同一对预算**决策**，所以
   第 1 条（schema 归属）不因此关闭
