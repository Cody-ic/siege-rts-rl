# 波次预算曲线与堡垒等级曲线（设计，非实现）

> 这两块在 `StatsTable` 里完全没有 schema 承载（三个研究代理交叉确认）。
> `composition_` 数组写入后从未被消费，`demo_driver.cpp` 的波次生成是作者自陈的
> "演示占位，无平衡含义"。这里只给出**公式与推导依据**，不新增 JSON 字段或改
> C++ 代码——是否要开一张新表、字段叫什么，应该走团队一起定契约的流程，
> 不该由做数值设计的人单方面决定 schema。

## 1. 攻方双预算

```
编成位(w)   = 6 + 1.2 * (w - 1)                  # 线性；硬顶暂不设（见下）
兵力预算(w) = 6 * w^1.25                         # 超线性，无上限
人均预算(w) = 兵力预算(w) / 编成位(w)
本波名义等级 L(w) = 1 + (人均预算(w)/base − 1) / k
                  # base = 人均预算(1)（锚定 L(1)=1）
                  # k = 数值表的等级系数（hp/dmg_permille_per_level ÷ 1000）
```

> **第三行原写 `L(w) = 兵力预算(w) / 编成位(w)`，2026-09-01 订正为由
> 造价曲线反解。** 战力(L) = 1 + k(L−1) 是仿射、不过原点，把预算商
> 直接当等级会让「攻方总战力 = 编成位 × 战力(L)」低于兵力预算一大截、
> 且随波数打的折还在变——超线性那条结构承诺被静默削弱。
> 代价是名义等级的数字比旧公式大得多（同一份预算，等级要除以 k≈0.22 才
> 折成战力）——那只是单位换算，不是变强了。
>
> **订正（2026-09-02，详见 攻守实力模型与平衡分析.md §8 第 1、3 条）**：
> 反解公式**保留**，但理由改——原注写的「反解之后**总战力 ≡ 兵力预算由构造
> 成立**」是旧货币（ΣB = HP×DPS 之和）下的读法：没有一场仗按 ΣB 打，兑现到
> 场上的是 Σ√B（攻守实力模型 §2），「总战力 ≡ 预算」并不成立。公式现在的
> 定位见 `tools/balance/budget_curves.py::nominal_level` 的 docstring——
> 攻方「兵力预算」按定义就是战力预算（`数值设计与成本产出矩阵.md` §12.6：
> 攻方无经济、反解不是真扣钱），本式只是把人均预算折成等级；守方造价改
> `c ∝ √B` 后攻方反解**不跟**（§12.6 已决），两侧差异归标定时校准。
> 原推导见 `tools/balance/budget_curves.py` 文件头「结构结论一/二」
> （结论二同日已订正，保留作决策史）。

验证关键点：
- `L(1) = 1` **由构造成立**（base 锚定；`budget_curves.py` 与
  `demo_driver.cpp` 两处实现都显式除 base，改系数不丢锚点）
- **编成位的硬顶 2026-09-01 撤下（用户定版）**：原来的 40 是随手标的，
  RL 实际能支持多少单位没人测过——「必须有硬上限」这一结构主张（CLAUDE.md
  「攻方双预算」）**不撤**，撤的是没有依据的取值。~~撤下它不改难度曲线：
  名义等级按预算反解 ⇒ 总战力 ≡ 兵力预算，上限只改「人数 × 等级」的分配。~~
  **订正（2026-09-02，详见 攻守实力模型与平衡分析.md §8 第 2 条）**：
  被划掉的这句是旧货币（ΣB）下的读法——硬顶改变兑现率（Σ√B，
  攻守实力模型 §2）与 `Ram` 阈值（同文档 §3），**改**难度。
  等 `bindings/`+`train/` 实测出可训练规模后把数填回 `budget_curves.py` 的
  `slots_cap`（现为 None，第三条结构检查随之挂起）与 `demo_driver.cpp`
- 逐波的数**刻意不写在这里**（上一版写了三个，公式一订正全部作废——
  正是 CLAUDE.md「条目数不写死」那条纪律说的漂移），要看就跑
  `py tools/balance/budget_curves.py`
- 编成位系数(6起步、每波+1.2)与兵力预算系数(6*w^1.25)都是**占位旋钮**，
  不是最终值——它们只是用来验证"曲线形状对不对"，真实系数要等
  `bindings/`+`train/` 落地、能跑批量对局后用黑箱优化标定
  （α 原取 1.6，2026-09-01 回调到 1.25：1.6 在低波段太陡——第 3 波人均
  战力已是首波的 4.3 倍，玩家没起经济就被数值碾过——而「超线性」这条
  结构只要求 α > 1，改的是旋钮、不是结构）

## 2. 守方堡垒等级轴

```
人口上限(K)     = 8 + 2*K             # 2026-09-02 定版；原占位 20+4K 不生效，见 §2.1
建筑等级上限(K)  = ceil(K / 2)          # 每2级堡垒开1级建筑上限（已落地）
兵种等级上限(K)  = K                    # 直接等于堡垒等级（已落地）
```

三者都不设上限（堡垒等级本身不设顶，靠"经济顶不满"防复利，见
`无尽模式与地形分层.md` §7.2）。

### 2.1 人口上限：严肃推导（2026-09-02，组长要求「严肃研究一下」）

> ⚠️ **先读 §2.2（2026-09-03 的再审）。** 本节的推导只用了**一条**判据
> （「咬得住」），而那条判据整个没有把攻方那条曲线放进来。§2.2 补了两条判据、
> 并查出本节选出的 `8+2K` **不是病根**——真正失败的是「`K` 涨不起来」，
> 因为升一级堡垒（200 石 + 200 木）买到的东西只有同样石材建塔的 1/1.8 火力。
> 本节以下保留原文，结论（取 `8+2K`）**没有被推翻，但也没有被证成**。

**结论先写**：原占位 `20 + 4K` **在城外拿到 4 座 `Mine` 之前一次都不会被碰到**
⇒ 那是个装饰品。改为 `8 + 2K`，理由是下面那条**唯一的**结构要求。

#### (1) 上限的设计职责：它是「数量 vs 质量」那个决策的一半

`CLAUDE.md`：「守方也在选编成形状——**人口上限管数量、金币管质量**（征兵时
选等级）」。所以人口上限**不是**一个防超载的护栏，它是一个**制造取舍**的装置：

> **上限必须咬得住**——即「上限 < 玩家把全部金币买 1 级兵能养的数量」。
> 咬不住时金币是唯一约束，玩家永远只买 1 级兵，
> **「人口上限」与「兵种等级上限」两个输出会一起变成装饰品**。

这两个输出是**互相支撑**的：上限逼你把余钱投进等级，等级上限（= K）
限制你能投多深。少了任一个，另一个失去意义。

#### (2) 算「能养多少」：守方的金币流量很窄

金币**只**来自 `Mine`（11/周期）与 `Keep` 地板（2/周期），城内恒 1 座 `Mine`。
稳态军队规模 = 每波可买数 ÷ (1 − 留存率)，留存取 0.75（`budget_curves.py` 的占位）：

| 城外 `Mine` 数 | 金币/波 | 每波买 1 级弓手 | 稳态可养 |
|---|---|---|---|
| 0（只有城内） | 196 | 3.3 | **13.1** |
| 1 | 362 | 6.0 | 24.2 |
| 2 | 528 | 8.8 | 35.2 |
| 4 | 861 | 14.3 | 57.4 |
| 6 | 1193 | 19.9 | 79.5 |

#### (3) 候选公式对比：只看「咬不咬得住」

| K | 城外 `Mine` | 稳态可养 | `20+4K`（原占位） | `8+2K` | `6+3K` | `10+2K` |
|---|---|---|---|---|---|---|
| 1 | 0 | 13 | 24 ✗松 | **10 ✓咬** | 9 ✓咬 | 12 ✓咬 |
| 2 | 0 | 13 | 28 ✗松 | **12 ✓咬** | 12 ✓咬 | 14 ✗松 |
| 3 | 1 | 24 | 32 ✗松 | **14 ✓咬** | 15 ✓咬 | 16 ✓咬 |
| 5 | 2 | 35 | 40 ✗松 | **18 ✓咬** | 21 ✓咬 | 20 ✓咬 |
| 8 | 4 | 57 | 52 ✓咬 | **24 ✓咬** | 30 ✓咬 | 26 ✓咬 |
| 12 | 6 | 80 | 68 ✓咬 | **32 ✓咬** | 42 ✓咬 | 34 ✓咬 |

取 **`8 + 2K`**，四条理由：

- **每一档都咬得住**（`10+2K` 在 K=2 松掉、`20+4K` 到 K=8 才开始咬）
- **上界落在可读区间内**：K=12 时 32，仍低于攻方那条 20–40 硬上限
  （守方的上界不来自 RL，来自**演示可读性与 `render/` 预算**，
  见 `无尽模式与地形分层.md` §8.2）
- **开局不越界且余量很小**：demo 开局 7 名守方单位，K=1 时上限 10 ⇒ 还能加 3。
  这个紧是**故意的**——它让堡垒等级轴从第 1 波就有压力，而不是二十波后才想起来
- **斜率比金币流量慢**：金币随「securing 了几簇」增长（§11 那条分析里每簇
  ≈ 11.4 当量/波），而上限只随 K 走，而 K 要花石+木 ⇒ 上限会**持续**咬住，
  取舍不会在中期自己消失

#### (4) K 涨得多快（上限的自变量）

`Keep` 每级 200 石 + 200 木 + 400 tick；城内石+木流量 = 136 + 211 = 347/波：

| 投入石木的比例 | 每级耗时 | 第 20 波约 K= |
|---|---|---|
| 20% | 5.8 波 | 3 |
| 33% | 3.5 波 | 6 |
| 50% | 2.3 波 | 9 |

⇒ `8+2K` 在第 20 波给出上限 **14–26**。这个量级与「演示可读性」相容，
也与守方是**存量**（墙塔与活兵留到下一波）这条相容。

#### (5) ⚠️ 一个**必须先修**的前提：造价与战力不同阶

**这条不修，人口上限会从「取舍」变成「战力天花板」。**

`§3` 写着「战力近似与 L 线性 ⇒ 造价也应该近似线性」，落地成
`World::train_cost_gold` = `cost_gold × level`（**造价 ∝ L**）。
但战力是 `B(L) = 1 + k(L−1)`，k = 0.22 —— **仿射，不过原点**。两者不同阶：

| L | 造价（× base） | 战力（× base） | 每金买到的战力 |
|---|---|---|---|
| 1 | 1.0 | 1.00 | 1.00 |
| 2 | 2.0 | 1.22 | 0.61 |
| 5 | 5.0 | 1.88 | 0.38 |
| 12 | 12.0 | 3.42 | 0.29 |

> **订正（2026-09-02，两处，详见 攻守实力模型与平衡分析.md §8 第 6 条）**：
> ① 上表「每金买到的战力」一列以 B（HP×DPS）为战力，是旧货币的口径——
> 单单位强度可以这么比，部队层面兑现的是 Σ√B（攻守实力模型 §2），
> 这张表只能读作「单个单位的性价退化」，不能读成部队强弱。
> 「造价 ∝ L 是悬崖」这条结论**不受影响**——它由
> `数值设计与成本产出矩阵.md` §12.3 的攻城表（用了塔与墙的真实结算）撑着。
> ② 上文「落地成 `× level`」已成历史：2026-09-02 起
> `World::train_cost_gold` 已改为 `c ∝ √B(L)`（§12.5 落地注），
> 本小节的「必须先修」已修，保留作决策史。

⇒ **堆量严格占优，且优势随等级拉大（渐近 1/k = 4.5 倍）**。而
`budget_curves.py` 的「结构结论一」明写：「`C` 比 `P` 涨得快 ⇒ 堆量严格占优
⇒ 永远买一堆 1 级兵」，那正是宏观层决策**塌到角上**的定义。

> **`§3` 那句话把「线性」和「正比」混为一谈了。** 战力是一条直线，但它的截距
> 不是 0；造价是过原点的正比。这与 `§1.4` 那次翻车是**同一类错误**（那次的
> 隐含假设是「每属性线性增长」）——都是「线性」这个词用松了。
>
> > **⚠️ 本段最初给的修法是「造价 ∝ 战力」，那个修法自己也带着一个没写出来的
> > 假设，2026-09-02 的模拟推翻了它。** 「两者等效」要求的不是「造价 ∝ 单位
> > 战力」，而是「造价 ∝ 单位战力^(1/α)」，其中 α 取决于**有多少人能同时交战**
> > （兰彻斯特：隘口 α=1、开阔地远程 α=2）。实测中性点在 **p ≈ 0.7**，
> > 而最贴合设计原话的是 **p = 0.5（造价 ∝ √B）**。
> > 完整推导、三条曲线的对拼模拟与指数扫描见
> > `数值设计与成本产出矩阵.md` **§12**——**以那一节为准，不要照抄本段**。
>
> **`budget_curves.py` 的结构检查为什么没抓到它**：那条检查里写的是
> `cost_L = base_cost * (1 + level_k*(L-1))`，即它**假设**造价 ∝ 战力，
> 然后验证「堆量堆级等效」——**它在检验自己的假设，而不是游戏的实现**。
> 这与第 14 条那次「`static_vision_radius_max` 手写成 8、而表里 `Watch` 是 12，
> 结构约束绿着失效」是同型的：**工具应当从唯一真相来源读，而不是复述它**。
> 修法照那条先例：让检查读 `train_cost_gold` 的公式（或至少断言两处同阶）。

修了这条之后，人口上限才是「取舍」——被顶住时把余钱换成等级，
每金买到的战力不变，选哪个由战术决定（集火脆弱性、AOE 吃亏程度、目标选择）。
不修的话，顶住之后升级的效率只有 1/4.5，守方战力会**撞上一堵墙**而不是继续
爬坡，`必败` 会来得又早又突然。

#### (6) 落地时要注意的两点（不是数值）

- **顶住之后没有「把数量换成质量」的出口。** 「批量升级」那条命令
  （`UpgradeForce`）已随 2026-09 编队移除删掉，而「解散」从来没有；
  于是玩家在顶格时只能**等有人阵亡**才能招一个更高级的。波次游戏里损耗每波
  都有，所以这不是死锁，但它意味着「换装」有一波的延迟——**要么接受，
  要么把 `CLAUDE.md` 里那条尚未实现的「买活」一并设计成出口**
- **`Keep` 自己不占人口**，否则 K=1 时上限 10 会被开局 7 兵 + 堡垒挤到只剩 2

#### (7) 本节的局限

- 留存率 0.75、一波 1510 tick 都是占位；**结论的方向不随它们变**
  （`20+4K` 要变成「咬得住」，得让稳态可养掉到 24 以下，那需要金币流量减半）
- 三种资源被等权折算（同 `数值设计与成本产出矩阵.md` §11.6），只用于比量级
- **`8+2K` 的两个系数仍是占位**：它们满足的是「咬得住」这条**结构**要求，
  而具体取值要等 `bindings/`+`train/` 能跑批量对局后与波次预算一起标

### 2.2 人口上限的再审（2026-09-03，试玩反馈「2 级 12 太少了，比敌人还少」）

**结论先写：`8+2K` 这个公式不是病根，`K` 不动才是。** 而且顺着这条线还查出
第二个与人口上限无关、改人口上限也修不掉的病（等级尺度）。
全部数字可跑：`py tools/balance/pop_cap.py`。

#### (1) §2.1 那条判据只看了守方的钱包，一次都没看攻方

§2.1 选 `8+2K` 时用的是**唯一一条**判据「上限必须咬得住」（上限 < 金币能养的
数量）。它是对的，但它整个没有把攻方那条曲线放进来。于是选出的公式满足
「咬得住」，却可能咬到「守方的兵力天花板追不上攻方」——而 **§2.1(5) 自己
已经描述过这个失效形态**（「战力会撞上一堵墙而不是继续爬坡，`必败` 会来得
又早又突然」），只是当时把成因只归给造价曲线（那条 2026-09-02 已修）。

补两条判据，与原来那条一起用：

| | 判据 | 不满足会怎样 |
|---|---|---|
| **C1** | **咬得住**（§2.1 原判据）：`cap(K) <` 金币能养的稳态数量 | 金币成为唯一约束，人口上限与兵种等级上限一起是装饰品 |
| **C2** | **不是天花板**：`cap(K) × √B(等级上限(K))` 跟得上攻方同波的 `Σ√B` | 顶住之后既不能加人也不能加质，取舍消失、只剩天花板 |
| **C3** | **够得着**：`K` 真的涨得起来 | 公式里 `K` 是自变量，实战里是常数 ⇒ **曲线的形状无关紧要** |

**C1 与 C2 不矛盾**：C1 要求**数量**被咬住，C2 要求**数量 × 质量**这个乘积
别被咬住。两者同时成立才是「顶住时把余钱换成质量」那条设计。

#### (2) C3 是现在唯一真正失败的那条：升堡垒是**错的花法**

`Keep` 一级 200 石 + 200 木。把同样 200 石花在别处：

| 200 石 + 200 木 买到 | 火力 | 血量 | 附带 |
|---|---|---|---|
| 升 1 级堡垒 ⇒ +2 人口 = 2 名弓手 | 20.0 dps | 400 | **还要另付 120 金**；兵种等级上限 +1；建筑等级上限 +0.5 |
| 2.5 座箭楼（只花 200 石 + 75 木） | **35.0 dps** | **1500** | **不占人口、不花金**，还省 125 木 |

⇒ **火力 1.8 倍、血量 3.8 倍，且省下 120 金与 125 木。**

而那个「兵种等级上限 +1」比看上去更弱：**编队移除之后就地升级
（`UpgradeForce`）已删**，抬高上限**只对以后招的兵生效**，存量兵一个不涨。

**所以「校准 runner 里每局只升 1 次堡垒」不是脚本写坏了，是这个比价下的
正确答案。** 实测 12 张池图全部 `upg=1`、`K` 停在 2 ⇒ `cap` 恒 10–12、
`building_level_cap = ceil(2/2) = 1` ⇒ **建筑等级上限那个输出一次都没打开过**
（`bupg=0`）。玩家看到的「2 级 12」正是这个：**不是曲线太平，是 K 卡在 2。**

要让 `K` 走到 §2.1(4) 那张表里「投 33–50%」那一档（`K ≈ w/2`，也正是让
`Σ√B` 比值稳住的那条轨迹），每级总价须降到 **约 115 石 + 115 木**
（投 33%）或 **约 70 + 70**（投 20%）——现价 200 + 200。

#### (3) C2：把两条曲线并排，形状其实取决于 K 的轨迹

守方天花板 = `cap(K) × √B(等级上限(K))`；攻方兑现 = `slots(w) × √B(名义等级(w))`
（`Σ√B` 而不是 `ΣB`，见 `攻守实力模型与平衡分析.md` §2）：

| 波 | 攻方兑现 Σ√B | K 停在 2（实测轨迹） | 比值 | K ≈ w/2 | 比值 |
|---|---|---|---|---|---|
| 1 | 6.0 | 13.3 | 2.21x | 10.0 | 1.67x |
| 4 | 17.7 | 13.3 | 0.75x | 13.3 | 0.75x |
| 8 | 33.8 | 13.3 | **0.39x** | 20.6 | 0.61x |
| 12 | 50.8 | 13.3 | **0.26x** | 29.0 | 0.57x |
| 20 | 84.7 | 13.3 | **0.16x** | 48.3 | 0.57x |

**右边那一列是健康的**：比值稳在 0.53–0.64（守方的兵单独顶不住攻方的兵，
差额由塔、墙与高度优势补——那正是「建筑定义防御的形状、部队补形状里的洞」）。
**左边那一列是崩的**：2.21x → 0.16x，单调塌陷。

**两列用的是同一个 `8+2K`。** 所以病在 K 的轨迹（C3），不在系数。

> ⚠️ 这张表**不是陷落波预测**，两个方向的偏差都不小：塔与墙不占人口（守方
> 实际更强），而 `defender_pop()` **数所有守方单位、工匠与斥候一样吃格子**
> （守方有效战斗人口更低，开局 7 个里就有 1 个工匠）。只能当形状看。

#### (4) 单改人口上限为什么不行：它是三层天花板里最松的那一层

| 层 | 现值 | 谁咬住 |
|---|---|---|
| 金币流量 | 城外 0 矿时稳态可养 **18.2** 名 1 级弓手 | 城外拿不到矿就是它 |
| 人口上限 | `8+2K`，K=2 ⇒ **12** | 现在是它 |
| 兵种等级上限 | `= K = 2` ⇒ 每单位 √B = **1.105** | 顶住之后是它 |

把人口上限抬到 16（`12+2K`）之后，**金币那一层立刻接手**（18.2），而
第 8 波要 33.8 ⇒ 就算把 18 个格子全填满 1 级兵也只有 18.2 × 1.0 = 18。
**所以人口上限单独抬不出所需的量级**，`pop_cap.py` 的候选表也印证：
`16+4K` 把「守得住的最后一波」从 2 推到 6，代价是 C1 松掉。

#### (5) 第二个病：两侧的等级尺度不可比（改人口上限修不掉）

攻方名义等级 = 人均预算反解、**除以 k = 0.22** ⇒ 每多 1 份人均预算 = 4.5 级：

| 波 | 攻方名义级 | √B(攻) | 守方 K=2 的 √B | 单位战力差 | 克制倍率还剩 |
|---|---|---|---|---|---|
| 4 | 14 | 1.96 | 1.10 | 1.78x | 0.31x |
| 8 | **23** | 2.42 | 1.10 | 2.19x | **0.21x** |
| 20 | **38** | 3.02 | 1.10 | 2.74x | **0.13x** |

克制比例随等级差衰减 `∝ B^-(p+q) = B^-1`，于是 **第 8 波整张克制二部图被
稀释到 21%**。`CLAUDE.md` §1.4 明写「`p+q` 太大则**克制关系被等级淹没**」
——这就是那条被触发的样子，而它是**结构**问题不是数值偏好。

成因不是 α 太大，是**两侧的「等级」根本不是同一把尺子**：攻方的是一个预算
商（到第 20 波 38），守方的是一个「200 石一级」的计数器（到得了 2–6），
而两者喂进**同一条** `level_permille`。守方于是被结构性地锁在这条曲线的底部。

#### (6) 推荐（按杠杆排序；具体取值归组里定）

1. **先动 `Keep` 的价 / 回报比，不要先动 `8+2K`。** 这是唯一能让 C3 成立的
   改动，而 C3 一成立，`8+2K` 就按设计工作（上面 (3) 右列）。两条等价路径：
   - **降价**：200/200 → 约 115/115（投 33% ⇒ K ≈ w/2）。风险是 `Keep`
     被 JSON 的 `_note_4` 认定为「整条升级轴的节奏闸门」，降价会同时加速
     建筑等级与兵种等级两条轴——**这正是要的，但要一起量**
   - **加回报**：价不动，改成一级买更多——`ceil(K/2)` 的除数去掉
     （建筑等级上限 = K）、兵种等级上限 = 2K。一级堡垒于是是
     「+2 人口 + 2 兵种等级 + 1 建筑等级」的整包，比现在的
     「+2 人口 + 1 兵种等级 + 半个建筑等级」值得买
2. **兵种等级上限的尺度要重定，而不只是抬系数**（(5) 那条）。它与
   「攻方名义等级怎么算」是同一个问题的两端，**不能只改一端**。
3. **人口上限的系数放在最后调**，而且要与金币流量一起——(4) 表明它现在
   不是最紧的那一层，单独抬它只会把约束交给金币。

#### (7) 本节没做的

- **没有改任何数值。** 三条推荐都跨越「结构 vs 数值」的边界（尤其第 2 条
  牵动攻方曲线），按 `CLAUDE.md`「关于数值」那条纪律，取值归组里定
- **C2 的判据里没有塔与墙**，所以它给的是「守方的兵单独够不够」，不是陷落波。
  要陷落波就跑校准 runner，那是唯一覆盖全部变量的模型（`攻守配平的数学模型.md` §8.1）
- **「顶住时把数量换成质量」仍然没有出口**：§2.1(6) 那条（没有解散、没有
  「买活」、就地升级已删）一字未变

### 2.3 提案：让堡垒轴真的启动（2026-09-04，**待人类实测复核**）

> **本节的结论分两类，只有第一类不依赖脚本守方。** 组长的质疑原话是
> 「我感觉是你的守方脚本不会玩吧」——那是对的，而且下面 (3) 列了它已知
> 玩得烂的四处。所有**测量出来**的数（陷落波、塔的边际收益、K 走到哪）都
> 只是「面对这个脚本」的读数，人类实测可能给出完全不同的形状。
> **在人类数据回来之前不要按 (4) 改数值。**

#### (1) 不依赖脚本的部分（表上的算术与代码的形状）

这几条改脚本、换玩家都不会变：

| # | 事实 | 出处 |
|---|---|---|
| 1 | 攻方名义等级 = 人均预算商 ÷ k ⇒ **第 4 波 14 级、第 8 波 23 级**；守方 `unit_level_cap = K`，开局 K=1 | §2.2(5) |
| 2 | 于是**守方开局被结构性锁在 1 级**，而单位战力差 √B(14)/√B(1) = **1.96x** | 同上 |
| 3 | 升 1 级堡垒（200 石 + 200 木）买到 +2 人口 = 20 dps；同样 200 石买 2.5 座塔 = 35 dps ⇒ **升级是错的花法** | §2.2(2) |
| 4 | 堡垒升级还要 **400 tick 的工匠工时**，而工匠同时要砌墙、建塔、维修——三者互斥 | `BldStats::upgrade_ticks` |
| 5 | `Lumber` 产 14 而 `Quarry` 产 9（0.64:1），十座建筑造价却是 430:325（1.32:1）⇒ **木材以约 2 倍速率净堆积** | `资源点分布与经济平衡.md` §4.5(2) |

**1 + 2 + 3 + 4 合起来是一个闭环**：守方唯一的成长轴要花钱、花工时，而它单位
成本买到的战力比替代品差 —— 于是**理性玩家也不会先升它**，而不升它就永远
是 1 级兵。这个闭环是**结构性**的，不是脚本的选择。

#### (2) 依赖脚本的部分（读数，待人类复核）

| 读数 | 值 | 为什么可能是脚本的锅 |
|---|---|---|
| 中位陷落波 | 4 | 见 (3) |
| 石材 +45% ⇒ 塔 +45%，陷落波 +0.2 | — | 脚本把塔摆在环上按「到主攻方向的距离」排序，人类会按地形/隘口摆 |
| 人口上限抬到 25，波末人口仍是 2 | — | 脚本只有一个出兵口、只招弓手 |
| **K 从来没离开过 1**（整组 12 局） | — | 脚本的工匠只有 1 个，升级排不上队 |
| 破口处只有 1 座塔够得着（覆盖率 20–25%） | — | 同上 + 方向轮换 |
| 金币冗余（拿外矿后 3387） | — | 花不出去，而花得出去的口子脚本不用（木栅、兵营） |

#### (3) 脚本已知玩得烂的四处（人类实测时的对照）

`game::DefenderMacro` 目前：

- **从不建兵营**（`Barrack`）⇒ 出兵口只有 `Keep` 一个，而一座一次只练一名
- **只招弓手** ⇒ 枪卫堵口、游骑摸攻城锤一条都没用上，克制二部图守方那半是空的
- **工匠只有开局那 1 个、从不补** ⇒ 建 / 修 / 升三件工程全挤在一个人身上，
  这直接解释了 (1) 第 4 条为什么在实测里表现为「升级命令发出去了却完不了工」
- **从不造木栅**（`Fence`，15 木）⇒ 而木栅正是那个堆到 3000 的资源的现成出口。
  §4.5「木材是死轴」这条**有一半可能是「脚本不用它」**，不是数值问题

#### (4) 提案（**不要在人类数据回来之前动**）

按「先让那个闭环能被打破」排：

1. **把堡垒的 `upgrade_ticks` 从 400 砍到 100–150**，并配组长提的
   **`人口上限 = 5 + 5K`**。后者把一级堡垒的回报从 +2 人口（20 dps）抬到
   +5（50 dps），**第一次超过同价的 2.5 座塔（35 dps）**；前者让它在一波之内
   升得完。两条要**一起**改：只改回报仍会卡在工时上，只改工时仍是错的花法。
   > `5+5K` 的开局值与现行**完全相同**（K=1 时都是 10），所以它不改早期难度，
   > 只改「升堡垒值不值」。**这也是为什么它单独跑出来与基线逐字节相同**——
   > K 不动的局里两个公式是同一个数。
2. **木材掰比例**（`Lumber.income_amount` 14 → 9 一档），但**先确认人类玩家
   是否在用木栅** ——若在用，这条可能不必改。
3. 兵种等级上限的尺度（§2.2(5) 那条「两侧不是同一把尺子」）留到最后，
   它牵动攻方曲线。

#### (5) 人类实测该回答的三个问题

1. **你会主动升堡垒吗？在第几波？** —— 直接检验 (1) 第 3 条那个比价结论
2. **金币/木材有没有堆起来？堆到多少？** —— 检验 §4.5 那两条里哪些是脚本的锅
3. **你的兵是几级的？** —— 若人类也一直在招 1 级兵，(1) 那个闭环就与脚本无关

## 3. 士兵造价曲线（兑现"k个1级兵≈1个k级兵"这条标定靶子）

因为一个L级单位的"战力"近似与L线性——**这条在 §1.4 改成「血量与伤害各开一份
平方根」之后仍然成立、结论不用改**：战力 = 血 × 伤 = `√B × √B` = `B(L)`，正是
开方前那个线性底。（原文写的理由是「等级只涨伤害(线性)、不涨血量」，那是被推翻的
那一版；换成平方根之后线性性来自两边相乘而不是来自单边。）
所以造价也应该近似线性，否则会出现"堆量"或"堆级"其中一个策略碾压另一个：

> **⚠️ 下面那行 `cost_gold(L) = base × L` 是错的，2026-09-02 查出，详见 §2.1(5)。**
> 本节把「线性」与「正比」混为一谈了：战力 `B(L) = 1 + k(L−1)` 是一条直线，
> 但**截距不是 0**；而 `base × L` 是过原点的正比。两者**不同阶**，于是
> 「每金买到的战力」从 L=1 的 1.00 掉到 L=12 的 0.29 ⇒ **堆量严格占优、
> 且优势随等级拉大（渐近 1/k = 4.5 倍）**，正是本节这句话要避免的结果。
> 应为 `cost_gold(L) = base_cost_gold * (1 + k*(L-1))`（与 `level_permille`
> 同一个 k）。~~**代码尚未改**（`World::train_cost_gold` 仍是 `× level`），
> 因为它动的是仿真行为、要团队过一下。~~
> **订正（2026-09-02，详见 攻守实力模型与平衡分析.md §8 第 6 条）**：
> 被划掉的句子已过时——代码**已改**，但最终落地的是 `c ∝ √B(L)` 而不是
> 本段写的 `∝ (1+k(L−1))`：§12 的等成本对拼推翻了「造价 ∝ 战力」这个修法
> （它隐含 α=1），定案与落地注见 `数值设计与成本产出矩阵.md` §12.5。

```
cost_gold(L)   = base_cost_gold * L        # ← 见上，已落地的修法是
                                           #   base * √(1 + k(L-1))（§12.5）
train_ticks(L) = base_train_ticks * (1 + 0.15*(L-1))   # 训练耗时温和增长，
                                                          # 不线性——否则批量升级
                                                          # 撤防线的代价会随等级
                                                          # 发散，堡垒等级曲线里
                                                          # "建筑/兵种等级上限"
                                                          # 前期就失去意义
```

## 4. 为什么不做成 L²（呼应 stats 设计里的选择）

若造价也搭配"两个都涨"(L²)的战力模型，会同时踩两个雷：
1. 造价必须跟着涨 L²才能保持效率不变，但"半价买两个1级"vs"全价买一个2级"
   这类比较会因为二次项而对整数取值极度敏感，调数值的容错空间很小
2. 与 §1.4 的选择自相矛盾——那一条取 `p + q = 1`（各开一份平方根），不是 2；
   两处若不一致，答辩时会被问出破绽

## 5. 这部分交付的定位

这一节**不是**最终契约，是"下一步该长什么样"的具体建议，附带论证。
真正要落地需要：
1. 团队讨论是否新增 schema（放 `StatsTable.global` 还是单开一张
   `wave_curve.json`/`keep_curve.json`）
2. 把 `composition_` 数组接上真正的消费逻辑（当前完全没有代码读它）
3. ~~`demo_driver.cpp` 的波次生成从"占位曲线"换成这套公式~~
   ✅ **已做（2026-09-01，起因是试玩反馈「20 分钟后玩家显著强过敌人」）**：
   `demo_driver.cpp` 的 `wave_slots`/`wave_level` 就是 §1 那两条曲线（含上面
   订正后的仿射反解），系数与 `budget_curves.py` 的 `PLACEHOLDER` 同源、
   等级系数 k 从数值表读。它是这套公式的**无决策退化消费者**（比例固定、
   预算全花）——正式形态里编成由攻方宏观层按同一对预算**决策**，所以
   第 1 条（schema 归属）不因此关闭
